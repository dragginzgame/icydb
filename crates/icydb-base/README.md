# IcyDB Base

Design-time helpers, builtin types, sanitizers, and validators used by IcyDB schemas.

Normally consumed via the `icydb` meta-crate; published separately to satisfy dependency resolution on crates.io.
