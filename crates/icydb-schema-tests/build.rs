// NOTE: This crate no longer needs a build script.
// Schema registration is automatic via derives, and this crate does not use
// actor codegen. Keep this file (renamed) as a reference if codegen is
// reintroduced later.
//
fn main() {
    // icydb::build!("icydb_schema_tests::schema::test::TestCanister");
}
