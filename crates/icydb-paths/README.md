# IcyDB Paths

Helper crate used by the IcyDB macros to resolve crate paths during codegen.

Users normally depend on the top-level `icydb` crate; this package is published only to satisfy dependency resolution.
