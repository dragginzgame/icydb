# IcyDB 0.236 Exact-Cardinality Planner Tie-Breaking Status

Status: Patches 1-2 complete; Patch 3 not started

Last updated: 2026-08-21

Design authority: [0.236 design](./0.236-design.md)

## Inspection Baseline

- released predecessor `v0.235.1` at
  `ddab0a1200687be369d34a98c373551db8eec60d`;
- `HEAD`, `main`, `origin/main`, and the release tag resolved to that commit at
  the audit cut;
- the worktree was clean; and
- this patch changes documentation only and does not edit Cargo package
  versions.

## Current-Surface Findings

The original roadmap proposal was not implementation-ready:

- its assigned 0.233, 0.234, and 0.235 predecessors were not the features
  actually released under those numbers;
- 0.230 already owns durable exact entity/index-prefix generations, populated
  build, live deltas, invalidation, and conservative fallback;
- structured and verbose explain already report chosen, alternative, and
  rejected routes plus structural/residual reasons;
- execution diagnostics already own actual keys, rows, materializations,
  results, instructions, time, and response observations; and
- no current evidence justifies persisted approximate statistics, histograms,
  optimizer modes, or another introspection surface; and
- released `v0.235.1` already contains the separate `#[newtype]` query-kind
  correction, which 0.236 must not absorb or reattribute.

One material gap remains. Equal structural and residual prefix-family
candidates are selected by lexicographic accepted-index name even when the
current exact prefix counts differ materially. The gap follows directly from
the maintained ranking and exact-cardinality owners and can be reproduced with
one common/rare predicate fixture.

## Frozen Scope

0.236 is limited to exact-cardinality tie-breaking for existing non-grouped
`IndexPrefix`, `IndexMultiLookup`, and `IndexBranchSet` routes.

It:

- extends the final lexicographic tie only;
- reads exact evidence through `StoreHandle` and 0.230 authority;
- uses one opaque nonpersisted lifecycle stamp only for cached unavailable
  fallbacks, derived from existing 0.230 lifecycle/source authority without a
  mutation revision;
- keeps one shared plan/template cache and one planner;
- pins a cardinality-ranked route by accepted `IndexId`, access family, and
  consumed prefix arity inside the current authenticated scalar cursor;
- extends the existing explain snapshot; and
- falls back to the exact predecessor decision whenever complete bounded
  evidence is unavailable.

It adds no persisted statistics, range/group cost model, generated-model
fallback, caller hint/budget, optimizer mode, second cache/planner/executor, or
new Candid endpoint.

The cursor change is a pre-1.0 hard cut. The sole current version-1 codec is
replaced in place, predecessor bytes fail closed, and no predecessor constant,
decoder, dual form, or fallback remains. A version-2 cursor is intentionally
not used because repository policy requires every current pre-1.0 internal
format discriminator to remain exactly version 1.

## Fixed Advisory Policy

| Resource | Maximum |
| --- | ---: |
| Tied candidates | 64 |
| Exact prefix probes | 256 |
| Cumulative lowered probe bytes | 4 MiB |
| Prefixes prepared together | 16 |
| Transient lowered bytes prepared together | 1 MiB |

An exceeded advisory policy preserves query admission and uses structural
lexicographic fallback. Maximum-shape IC measurements are required before
closeout and may lower these caps if necessary; implementation may not make
them caller-selectable or silently exceed them.

## Patch Tracker

| Patch | Outcome | Status |
| --- | --- | --- |
| 1 | Audit the current planner/cardinality/explain/cache/cursor authorities and freeze the narrowed design | Complete |
| 2 | Implement the exact selection-time tied-candidate decision, candidate-neutral template, unavailable-fallback lifecycle stamp, authenticated scalar route pin, explain evidence, and direct regression matrix through the one planner | Complete |
| 3 | Record production-shaped PocketIC performance, recovery/upgrade, maximum-bound, stable-state, Candid/export, Wasm, complexity, and closeout evidence | Not started |

These are the complete intended 0.236 patches. Generic continuation after
implementation authorization owns exactly the next incomplete patch.

## Review Feedback Resolution

The design review's three gates are resolved as follows:

1. `(IndexId, family)` is not assumed to be permanently unique. The route pin
   also stores consumed prefix arity, and continuation planning must find
   exactly one ordinary eligible match. Singleton branch canonicalization,
   differing prefix depths, zero matches, and multiple matches remain explicit
   closeout proof cases; Patch 2 covers current-pin round-trip, foreign-entity
   rejection, and successful exact-route resume.
2. An unavailable proof returns a cheap comparable lifecycle stamp for Absent,
   Building, malformed, or unreadable evidence. Only a cached unavailable
   fallback checks it; an unavailable-to-Ready change therefore retries the
   bounded proof without probing counts on every unchanged fallback hit or
   revalidating exact-selected plans after ordinary writes.
3. The requested version-2 cursor would violate the repository's explicit
   pre-1.0 version-1-only policy. The accepted resolution is a genuine hard cut
   of the sole current version-1 layout with no legacy reader or fallback.
4. The subsequent complexity review removes mutation-driven cache freshness.
   Exact counts are consulted at cold compilation or new parameter binding and
   retained as `exact_at_selection`. Only unavailable fallbacks compare the
   0.230 lifecycle stamp so Ready publication cannot remain hidden.

The review also confirmed that fewer exact matching prefix entries is the only
claimed fact. Implementation terminology may use `cardinality_tiebreak`, not
cost/estimate/cheapest or general optimizer scoring.

## Complexity And Size Budget

Local source complexity will increase moderately: one selection-proof DTO, one
final tie comparison, one unavailable-fallback lifecycle stamp,
candidate-neutral template retention, and one authenticated route discriminator
are required.
Architectural complexity remains bounded because these replace or extend the
current StoreHandle/planner/cache/token owners and add no persisted state,
cache, executor, mode, job, configuration, or public hint.

Patch 2 must stop for review if it needs a second candidate model/planning flow
or independent state axis. It must record production files and approximate
line delta and charge candidate-neutral templates by their actual bounded
retained authority.

Performance and artifact gates are frozen:

- non-tied queries perform no new cardinality reads or probes;
- warm exact-selected cache hits perform no lifecycle reads or probes, even
  after ordinary writes;
- only warm unavailable fallbacks perform one lifecycle-stamp check and no
  probes while unchanged;
- resumed pinned cursors perform no cardinality reads or probes;
- unavailable-stamp and cursor overhead each stay within the greater of 5% or
  2 million instructions on matched predecessor fixtures;
- the production-shaped selective fixture must reduce both entries visited
  and total instructions after including cold planning work; and
- raw non-gzipped Wasm growth against the exact same `v0.235.1` artifact stops
  for attribution/reduction above the smaller of 96 KiB or 3%. Gzip cannot
  waive the gate; Candid and exports remain unchanged.

## Patch 1 Evidence

Inspected maintained authorities include:

- `db/query/plan/planner/ranking.rs`: structural candidate ranking;
- `db/query/plan/access_choice`: residual reranking and chosen/rejected explain
  projection;
- `db/executor/index_prefix_cardinality.rs` and `db/registry/handle.rs`: exact
  accepted-prefix evidence;
- `db/schema/cardinality_generation.rs`: current version-1 generation identity;
- `db/session/query/cache.rs` and `cache/template.rs`: one shared concrete and
  parameterized cache;
- `db/session/query/dynamic.rs` and `db/cursor/token`: authenticated scalar
  continuation order and codec; and
- `db/executor/plan_metrics.rs`, explain descriptors, and execution traces:
  maintained planned/actual observability.

No runtime code, persisted bytes, stable allocation, public endpoint, Candid
type, export, generated binding, or cursor codec changed in Patch 1.

## Patch 2 Authorization

The design corrections above are frozen, grouped and range plans remain
outside the line, and the advisory limits remain subject to measured reduction
only. The user explicitly authorized Patch 2 on 2026-08-21 after the final
selection-time cache-staleness clarification.

## Patch 2 Evidence

Patch 2 extends the existing owners rather than adding parallel machinery:

- one complete all-or-nothing `StoreHandle` evidence operation returns exact
  counts or an opaque digest-sized unavailable lifecycle stamp; it has no
  cache, template, cursor, or selected-route knowledge;
- the existing access-choice owner exposes only a genuine final tied set, and
  the existing shared cache retains the result as selection-time advisory
  evidence;
- exact-selected and policy-fallback warm hits perform zero evidence reads;
  unchanged unavailable hits perform one lifecycle read and zero probes;
- parameter rebinding reruns the bounded decision for new values, while a
  repeated bound reuses its ordinary one-entry memo even after row writes;
- scalar cursor version 1 is hard-cut in place and authenticates the exact
  accepted index/family/prefix-arity route; resume performs no evidence read;
  and
- structured and verbose explain expose only bounded candidate counts and a
  closed evidence state.

Focused native evidence passes with 5 tie-break/cache/cursor tests and 6
current scalar-token tests. `icydb-core` checks pass without default features,
with SQL only, and with all features/tests; all-feature package Clippy is
warning-clean. Non-tied, warm exact-selected after writes, and resumed pinned
cursor fixtures record zero lifecycle reads and zero prefix-count proof calls.

The preliminary representative `one_entity_typed_query` build compares the
same production profile against a clean source archive of exact `v0.235.1`:

| Artifact | `v0.235.1` | Patch 2 candidate | Delta |
| --- | ---: | ---: | ---: |
| Compiler Wasm | 2,419,238 | 2,419,238 | 0 |
| Final raw Wasm | 2,118,733 | 2,118,711 | -22 |
| Deterministic gzip | 811,207 | 811,173 | -34 |
| Candid | 64 | 64 | 0 |
| Exports | 6 | 6 | 0 |

Raw Wasm therefore remains well inside the smaller-of-96-KiB-or-3% stop gate.
The clean archive produced the complete compiler/final/gzip/Candid artifacts;
only the report writer's source-revision metadata step was unavailable because
an archive deliberately has no `.git` directory. The candidate report writer
completed normally.

The complete current 0.236 line touches approximately 29 files at
`+2,900/-175` lines including about 1,200 lines of design/status documentation
and 670 lines of direct tests. Runtime code is approximately `+1,330/-100`
across 21 files. Local source complexity rises moderately for the bounded
decision, unavailable transition, and cursor continuity; architectural state
space stays bounded because there is still one planner, cache, executor,
cardinality authority, cursor codec, and no new persisted state or mode.

## Evidence Still Required

- production-shaped PocketIC equal-result and keys-visited improvement;
- maximum candidate/probe instruction and transient-byte measurements;
- explicit Building interruption plus reopen/upgrade behavior;
- exact route-pin uniqueness, scalar cursor stability, and
  tamper/foreign-authority rejection;
- explain and metric privacy evidence;
- stable-state measurement proving no new representation;
- final Candid/export and raw non-gzipped Wasm comparison after evidence
  corrections, with gzip secondary; and
- user-owned complete validation and release workflow.

## Current Verdict

Patch 2 is complete and remains within its raw-Wasm stop gate. Patch 3 owns the
production IC performance, maximum-bound, recovery, stable-state, and final
artifact evidence required before closeout. The broad roadmap proposal remains
narrowed to one demonstrated performance gap with one existing state authority
and one planner flow.
