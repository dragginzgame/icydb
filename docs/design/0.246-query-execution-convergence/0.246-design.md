# IcyDB 0.246 Query And Recovery Convergence

Status: ACTIVE — PATCH 4 COMPLETE, PATCH 2 CANDIDATE MATRIX PENDING

Last updated: 2026-08-28

Exact released predecessor: tag `v0.245.1`, revision
`630cc9c974294631ff77a77d69a2704581ccb2dd`, tree
`4b1c4464679e836bfb22da61b72080db1133b7bd`.

## Outcome

Reduce broad prepared-query cost, remove artificial healthy-recovery delay,
and separate deployable-store capacity from retained-batch backpressure without
adding another query route, recovery loop, cache, authority or mode.

The line has three bounded production opportunities. Query execution lets the
existing primary-map visitor deliver borrowed rows through retained-slot scans
without a second tree lookup, then orders large retained payloads by compact
positions. Recovery adopts the published progress-sensitive timer contract so
successful bounded pages continue immediately while retryable failures keep
their one-second backoff. Capacity policy caps journaled stores at the requested
16 and gives retained batches an independently evidenced limit.

Patch 1 produced the required clean complete P1, scale and P2 baseline. Patch 2
retains its candidate-matrix gate, Patch 3 is complete, and the explicitly
authorized Patch 4 is bounded below.

## Demonstrated Need

Patch 1's exact clean released-source evidence records these 2,048-row costs:

| Prepared family | Instructions | Physical observation |
| --- | ---: | --- |
| Aggregate `DISTINCT` plus `FILTER` | 109,234,618 | 2,048 rows ingested and 2,048 row gets |
| Nullable expression `DISTINCT` control | 118,738,995 | 2,048 candidates and 2,048 row gets |
| Expression-order `DISTINCT` control | 109,375,905 | 2,048 candidates and 2,048 row gets |

By contrast, exact synchronized-metadata aggregates complete below 1.4M
instructions. That comparison establishes a large ceiling but does not treat
all row work as removable. The clean aggregate attribution assigns 71,852,668
instructions to the store phase and 13,127,318 to required reducer work. The
candidate may remove per-key lookup and complete source-row retention; it must
still visit and decode every required value and must measure the actual saving.

Separately, ordinary mutation bursts can retain the database-wide 38-batch
ceiling faster than a one-batch-per-second watchdog drains it, producing E284
without failed recovery, concurrency, or large payloads. Keeping one bounded
page per callback while removing only the successful-progress delay preserves
the existing instruction and stable-write envelope.

The historical value 38 also conflates two independent resources. Store
capacity came from 155 application memory IDs minus three canister controls,
divided by four allocations per journaled store. The batch ceiling then reused
that store count despite representing callback debt rather than store identity.
The requested store ceiling is 16. The observed workflow needs more than 38
batches, so the batch candidate is 64: the smallest power-of-two ceiling above
the demonstrated pressure point, subject to unchanged record, encoded-byte,
heap, callback, stable-allocation, retry, and upgrade gates.

## Hard Scope

0.246 is internal cleanup and optimization only. It adds no SQL syntax, public
API, Candid or export, schema behavior, generated public surface, persisted
state, configurable mode or compatibility path. Patch 4 hard-cuts the current
version-1 registry acceptance bound from 38 to 16 without changing its encoded
shape; pre-1.0 databases that persisted more than 16 active-plus-retired store
quartets require recreation.

The maintained authorities remain:

- accepted schema snapshots select runtime schema;
- the planner selects one prepared access and execution contract;
- the existing request pins that contract and authority for its execution;
- the executor owns physical access, residual retry, corruption and
  physical-work semantics;
- the prepared contract fixes ordering and window requirements once;
- aggregate reducers own aggregate projection, folding, `NULL` and filter
  semantics; and
- scalar `DISTINCT` consumers own projection tuple identity, canonical
  equality, projection-order application, pagination and output-budget
  semantics under that prepared contract.

No candidate may add a process-, database-, schema- or publication-lifetime
query artifact. No candidate may reconstruct planner eligibility at runtime.
Unsupported or unproven cases continue through the existing canonical prepared
flow.

## Patch 1 — Clean Baseline And Ownership Audit

Patch 1 is documentation and measurement only. It used one exact raw Wasm
subject built from a clean `v0.245.1` source tree and run the complete checked-in
P1, scale and P2 profile. Partial, dirty-worktree and historical 0.237 profile
captures may provide context but cannot become the frozen 0.246 baseline.

The audit records:

- exact source revision, tree, toolchain, features, PocketIC identity and raw
  Wasm digest;
- every P1, scale and P2 shard plus strict merged outcomes;
- results, error identity, cache state and physical work for every retained
  candidate;
- a phase inventory from ordered key production through row loading, retained
  slots, row-vector retention, projected-value delivery and consumer work;
- which operations require complete materialization for sorting, pagination,
  retry, cursor, corruption or budget semantics;
- whether both semantic consumers reach one existing materialization boundary;
  and
- a build/no-build decision for Patch 2.

Production files, performance fixtures and profile membership remain unchanged
in Patch 1. The complete current top-20-per-metric selection proved the old P2
candidate ceiling internally inconsistent, so the ceiling is corrected without
truncating the union. That tooling-only correction remains explicit, bounded
and covered by the existing fail-closed cap test. No temporary instrumentation
is retained.

The completed baseline contains 1,787 passing P1 scenarios, 210 scale
observations and 521 passing P2 confirmations from one clean 4,513,444-byte raw
Wasm subject. Its exact identities, phase map and Patch 2 decision are
retained in the [Patch 1 audit](../../reports/recurring/2026/08/27/perf-audit/01/report.md).

## Patch 2 — Borrowed Row Delivery And Compact Ordering

Patch 1 identified repeated primary-row lookup and large-payload ordering before
aggregate and scalar `DISTINCT` consumers. Implementation evidence narrowed the
acceptable seam: carrying evaluated order tuples on each retained row added
per-row state and made the measured path materially slower, while the existing
borrowed primary-row visitor and canonical ordering helper already owned all
required semantics.

Patch 2 therefore extends those two existing owners rather than creating a
consumer cache or another execution route. Aggregate folding and scalar
`DISTINCT` admission remain separate consumers.

Conceptually:

```text
existing prepared key/row execution
  -> eligible primary-map visitor supplies the already-open raw row
  -> canonical retained-slot decode and residual predicate
  -> aggregate or scalar DISTINCT consumer remains unchanged

expression-backed ordering
  -> cache canonical order values once
  -> sort compact source positions
  -> move each retained row into final order once
```

Direct visitation is admitted only by the existing primary-map leaf and only
for unpaged retained-slot scans or the pre-existing bounded-order window. A
`DISTINCT` stream adapter may forward that capability only while the same leaf
still owns monotonic unique physical keys. Every other stream, paginated scan,
residual-retry shape and unsupported payload stays on the predecessor path.

Ordering continues to use the planner-resolved order and the existing cached
order-value comparator. Only the sorted payload changes: compact source
positions are sorted and the fallible reorder pass moves selected rows once.
No projected value, order tuple or eligibility result is cached on a row.

### Required semantic proofs

- `NULL`, numeric coercion and expression-error behavior are byte-for-byte or
  typed-error identical to the predecessor;
- multi-expression `DISTINCT` retains complete tuple identity;
- authored ordering, stable tie behavior, offset, limit and cursor semantics
  are unchanged;
- logical work, retained-state and nested-value budgets are charged at the same
  semantic boundaries, with no partial charge surviving fallback;
- corruption, missing-row, invalid-slot, borrow-conflict and residual-retry
  behavior remains fail-closed and equivalent;
- borrowed and lookup-backed consumers return identical result bytes and
  logical-work counters; reviewed physical changes must be limited to replacing
  per-key gets or source-row retention, with no added row or index visits; and
- exact routes and every unsupported prepared shape remain unchanged.

### Frozen performance and complexity gates

- prepared aggregate and scalar projection `DISTINCT` controls each save at
  least 25,000,000 instructions and 25% at 2,048 rows under repeated clean
  current-source measurement;
- the complete P2 profile confirms every selected affected matrix row and
  preserves all result and cursor signatures;
- no confirmed matrix row regresses by both more than 250,000 instructions and
  more than 3%;
- no result, error, logical row/index work or stable extent changes, and no
  physical row/index visits increase;
- final raw production SQL Wasm grows by at most 4,096 bytes against the exact
  predecessor subject; gzip is secondary context;
- at most one new private contract type, no new module, cache, mutable owner or
  behavior axis; and
- maintained production Rust grows by at most 120 net lines, with deleted
  intermediate-materialization code reported separately.

Missing either consumer-family gate closes Patch 2 as no-build. A large saving
for only aggregates, only scalar `DISTINCT`, one field type or one SQL spelling
does not qualify.

## Patch 3 — Immediate Healthy Recovery Continuation

Patch 3 adopts `ic-timers 0.7.0` at the generated startup-watchdog adapter.
Lifecycle reconciliation and commit-pressure wakeups request immediate
scheduler messages. A successful bounded page with more work returns
`ContinueImmediately`; retryable failure retains `Continue` at the fixed
one-second cadence; quiescence and terminal failure still stop.

This remains one retained watchdog and one recovery page per replicated
callback. It does not change the 38-batch limit, allocation policy, page size,
stable format, public errors, Candid, exports, schema behavior, application
retry contract, or add inline convergence.

## Patch 4 — Independent Store And Batch Capacity

Patch 4 makes 16 the one canonical maximum for active-plus-retired journaled
store quartets. The existing variable-length version-1 registry encoding stays
canonical and rejects a persisted or generated seventeenth store; no old-form
decoder, upgrade bridge, or fallback is retained.

Backlog admission receives its own 64-batch constant. Selection requires the
unchanged 16,384-record and 16-MiB limits to continue dominating payload work,
the maximum 16-head selector and 64-batch heap/chunk formulas to remain inside
their existing bounds, and a real canister to admit exactly 64 one-row commits,
reject batch 65 with complete E284 facts, drain in 64 bounded zero-delay
callbacks, retry, and reconstruct after upgrade.

The candidate passes. The formal maximum overlay is 339,886,080 bytes and the
callback peak is 526,532,992 bytes, both slightly below the predecessor bounds
because the selector owns 16 rather than 38 heads. Repeated 16-MiB-class fill,
drain, and refill retains a 513-page stable high-water. Cold and populated
real-canister histories each drain 64 batches in 64 callbacks over 145 ns,
then retry successfully; upgrade recovery preserves the boundary.

## Landing Plan

The 0.246 line contains four bounded landing patches:

| Patch | Bounded outcome | Canonical owner | Delivery domains | Required handoff evidence |
| --- | --- | --- | --- | --- |
| 1 | Freeze the clean current matrix and select or reject the production candidate | performance audit | Integration Tests, documentation | complete: P1/scale/P2 bundle, phase/owner map, one decision |
| 2 | Reuse borrowed primary rows and sort compact positions across aggregate and scalar `DISTINCT`, or no-build | scalar executor spine | Executor, Integration Tests | semantic proof matrix, instructions, physical work, raw Wasm, line/state delta |
| 3 | Continue healthy bounded recovery immediately and retain cadence-backed retry | generated startup-watchdog adapter | Build / Canister, Integration Tests | dependency hard cut, full-backlog timing, retry and upgrade proof, raw Wasm delta |
| 4 | Cap journaled stores at 16 and independently admit 64 retained batches | commit-control registry and backlog admission | Commit / Recovery, Integration Tests | complete: current-format rejection, formulas, exact pressure facts, full-backlog retry/upgrade, raw Wasm and complexity delta |

One patch is one worktree handoff. A no-build decision consumes its planned
patch without authorizing a replacement optimization. The line does not grow
narrow escape hatches or revive deleted roadmap candidates.

## Closeout

0.246 succeeds only if retained production work is broad, attributable and
proportionate. Closeout must run the complete frozen query matrix against the
exact candidate, retain Patch 3's full-backlog, retry and upgrade proof,
retain Patch 4's store/batch independence and 64-batch proof, compare final raw
Wasm with the same toolchain/profile/features, report complexity and
state-space deltas, and prove unchanged Candid, exports, canonical version-1
encoding shape and public behavior.

The valid line verdicts are:

- `BUILD GATES PASSED` for each retained patch;
- `MEASURED NO-BUILD` for an individually rejected patch; or
- `CLOSED — NO RETAINED PRODUCTION CHANGE` if all candidates fail.
