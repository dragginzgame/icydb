# IcyDB 0.246 Query And Recovery Convergence Status

Status: ACTIVE — PATCH 4/WASM CLOSEOUT CORRECTIONS PASS, PATCH 2 CANDIDATE MATRIX PENDING

Last updated: 2026-08-28

Design authority: [0.246 design](./0.246-design.md)

Exact released predecessor: tag `v0.245.1`, revision
`630cc9c974294631ff77a77d69a2704581ccb2dd`, tree
`4b1c4464679e836bfb22da61b72080db1133b7bd`.

## Patch Tracker

| Patch | Outcome | Status |
| --- | --- | --- |
| 1 | Clean complete P1/scale/P2 baseline and materialization ownership audit | Complete |
| 2 | Borrowed primary-row delivery plus compact payload-indirect ordering | Implemented; full candidate matrix pending |
| 3 | Published timer adoption and immediate healthy recovery continuation | Complete; focused gates passed |
| 4 | Independent 16-store and evidenced 64-batch capacity | Complete; focused gates passed |

## Current Boundary

- Production runtime changes in Patch 1: none.
- Performance profile membership and fixtures: unchanged. The tooling-only P2
  hard cap is corrected from 512 to 544 because the complete frozen selection
  contains 521 candidates; selection still fails rather than truncating.
- Clean baseline: 1,787/1,787 P1 scenarios, 210 scale observations and
  521/521 P2 confirmations passed from one exact clean `v0.245.1` raw Wasm
  subject.
- Raw Wasm subject: 4,513,444 bytes, SHA-256
  `0070381c8ca1e0f5a94a7da80b2c9235f51595f42a8fbc79ff7764e9ca2c9f95`.
- Patch 2's focused aggregate-and-scalar-`DISTINCT` breadth gates pass. The
  identity-bound complete candidate P1/P2 matrix remains outstanding.
- Patch 4 is the final retained 0.246 roadmap candidate.
- The closeout Wasm hard cut shares bounded control-plane sorting and adds a
  fail-closed 4-KiB-per-entity finalized raw-Wasm guard; it does not add a fifth
  roadmap candidate.

## Patch 1 Decision

The clean 2,048-row aggregate `DISTINCT` plus `FILTER` control costs
109,234,618 instructions and performs 2,048 row gets. Clean focused scalar
`DISTINCT` controls cost 109,375,905 and 118,738,995 instructions and also
perform 2,048 row gets. Both receive fully retained source rows before their
separate semantic consumers run.

Patch 2 reuses the existing executor-owned borrowed primary-row visitor for
unpaged retained-slot scans and makes the existing expression-order helper sort
compact source positions. Aggregate folding and scalar `DISTINCT` tuple
identity, ordering, pagination and budget semantics remain separately owned.
Paginated, residual-retry, cursor, corruption and unsupported paths retain the
canonical predecessor flow.

## Patch 2 Evidence

Repeated focused current-source measurements at 2,048 rows produced identical
results:

| Control | Predecessor | Candidate | Saving |
| --- | ---: | ---: | ---: |
| Aggregate `DISTINCT` plus `FILTER` | 109,234,618 | 70,759,228 | 38,475,390 (35.22%) |
| Nullable expression scalar `DISTINCT` | 118,738,995 | 87,601,894 | 31,137,101 (26.22%) |
| Expression-order scalar `DISTINCT` | 109,375,905 | 78,239,326 | 31,136,579 (28.46%) |

All three retain their exact result, candidate and physical-visit assertions.
The production-profile SQL Wasm is 4,517,281 raw bytes, up 3,837 bytes from
the exact predecessor and within the frozen 4,096-byte ceiling; its SHA-256 is
`9a39bf67708e3358cbb4eebf72f8e0fef5b7c5f0b4c2ec88643240f3da8378c4`.
The candidate adds no type, module, cache, mutable owner or behavior axis.

The first P2 shard attempt failed closed before query execution because the
frozen predecessor selection is intentionally bound to the predecessor source
and Wasm identity. Complete candidate P2 evidence therefore requires a new
candidate P1 selection and all candidate P2 shards; no receipt identity was
rewritten or bypassed.

Release validation exposed predecessor tests that treated positive point-get
counts as proof of prepared execution. The borrowed primary-row visitor now
performs the same complete reduction without those redundant gets, so the
tests instead prove the prepared scalar-aggregate owner and exact ingested-row
count. The unrelated prefix-family control retains its 65-entry physical work;
its repeated 5,301,284-instruction result is covered by a narrowly rebased
5,310,000 ceiling.

Full evidence: [Patch 1 audit](../../reports/recurring/2026/08/27/perf-audit/01/report.md).

## Patch 3 Evidence

Exactly one `ic-timers` package resolves at `0.7.0`. Generated lifecycle and
commit-pressure scheduling is immediate, successful bounded work with more
debt continues immediately, and retryable failure retains the one-second
cadence.

The real-canister closeout passes for cold, populated, retry, and upgrade
recovery. Before Patch 4 replaced the historical coupled ceiling, both full
38-batch backlogs drained in 38 callbacks over 97 ns of PocketIC time. Cold
convergence consumed 1,884,616,770 total instructions and the populated
equivalent consumed 1,926,423,441, within the existing callback and memory
assertions.

Against the exact 0.246 query candidate, compiler-emitted raw Wasm changes from
3,604,977 to 3,614,514 bytes (+9,537) and final raw non-gzipped Wasm changes
from 3,175,764 to 3,184,630 bytes (+8,866) for the production one-entity SQL
canister. Patch 3 adds no IcyDB type, mode, persisted state, loop, callback, or
watchdog owner.

## Patch 4 Evidence

- Maximum active-plus-retired journaled stores: 16.
- Retained-batch limit: 64, independently owned by backlog admission. Batch 65
  returns E284 with exact current, proposed, and limit facts.
- The current version-1 registry shape remains singular; a persisted or
  generated seventeenth store fails closed with no compatibility path.
- Formal maximum overlay and callback peaks are 339,886,080 and 526,532,992
  bytes, below the predecessor bounds. A 16,722,304-byte, 256-live-chunk fill
  drains and refills across three cycles at a stable 513-page high-water.
- The 16-head selector plus maximum retirement costs 287,890,767 instructions,
  5,727,601 below the prior 38-head measurement while the conservative combined
  callback remains below 30B instructions.
- Cold and populated 64-batch real-canister histories drain in 64 callbacks over
  145 ns. Their total work is 3,175,704,524 and 3,229,664,803 instructions;
  retry and upgrade reconstruction pass.
- Patch 4 changes compiler-emitted production one-entity SQL Wasm from
  3,614,514 to 3,611,160 bytes (-3,354) and final raw non-gzipped Wasm from
  3,184,630 to 3,181,601 bytes (-3,029).

## Closeout Correction Evidence

- Online DDL convergence no longer treats canonical-only cardinality metadata
  created by an earlier row fold as an overlay owned by the later DDL batch.
  Retirement skips only keys with no recorded position and no live/tombstone
  overlay; actual unpositioned overlay state still fails as E17.
- The correction is a hard cut to the singular provenance path. It adds no
  retry, compatibility decoder, legacy timing branch, persisted state, mode or
  fallback authority.
- The memory-ID invariant now requires `MAX_PERSISTED_STORE_ALLOCATIONS = 16`.
  The stale source-text check for 38 was the reason `make check-invariants`
  rejected the already-correct production constant; the full invariant target
  now passes without changing the runtime limit. The private theoretical
  38-store memory-range constant and its assertion are removed; one compile-time
  expression proves the canonical 16-store limit fits instead.
- Tests no longer require a visible one-second interval between healthy pages,
  equate scheduler deliveries with IcyDB work callbacks, or retain an audit
  endpoint whose only purpose was freezing a live journal-tail timing window.
  The application-owned retry test remains the sole explicit one-second
  advance in the affected durable fixture.
- Focused core, no-default compile, DDL convergence, durable mutation,
  read-authority, cardinality recovery, maximum accepted-index recovery and
  indexed-range gates pass against `ic-timers 0.7.0`.
- Before the bounded-sort hard cut, the production one-entity SQL Wasm was
  3,181,557 raw non-gzipped bytes
  (`9870a826e9c69310cf8412ce93901f8d4daab797c8959f6d4dadd123d3612a82`),
  44 bytes below the Patch 4 measurement.

## Wasm Hard-Cut Evidence

- Bounded schema, startup, recovery, integrity and metrics control-plane
  collections use one compact in-place heapsort owner. Stable-order operations
  and large user value, row and index-key sorts retain Rust's optimized sorters.
- The same finalized production one-entity SQL subject falls from 3,181,557 to
  3,077,777 raw bytes (-103,780, or 3.26%) with the same six exported methods;
  the new SHA-256 is
  `26f0bdcd008dd2a2a58f96a2df8719fc2066fe19f4b2732953a6ee7557bf44c4`.
- The SQL-off typed subject falls by 93,460 final raw bytes and 93,353
  code-section bytes. Symbol attribution leaves only about 4.5 KiB of unstable
  quicksort machinery on hot value/data-key paths and about 9.7 KiB of compact
  heapsort bodies.
- The corrected exact-key one-to-ten-entity pair adds 1,301 final raw bytes
  across nine entities (145 bytes per entity) and 37 code-section bytes in
  total. The size-report workflow fails closed above 4 KiB per added entity.
- Final-tree reruns measure maximum-fanout prefix work at 5,317,622
  instructions with 65 index entries, exact entity count at 865,427 with no
  row or index traversal, and indexed extrema at 1,742,673 with one index entry
  and one row read. Their ceilings retain the prior narrow review tolerances.
- The SQL performance audit no longer restores timer-owning canisters from
  snapshots: PocketIC snapshot restoration does not recreate external timer
  provider handles. Fresh fixtures remove that invalid isolation boundary, and
  the normal parallel target passes all 46 tests without changing production
  Wasm.
