# IcyDB 0.257 — Status

- State: balanced junction lowering, shared boolean normalization, compact
  typed membership and bounded CASE rewriting complete for 0.257.0.
  Shared bounded preparation is approved; typed input admission and safe owned
  request cleanup and SQL integration are complete. Shared preparation-work
  charging is split into two approved outcomes; fallible expression preparation
  is in progress. Explain and diagnostic bounds remain separate.
- The diagnostic split and comparator repair are approved. Slice 6 removes
  normalization re-entry during label sorting; slice 7 bounds CASE rewriting.
  Diagnostic bounds remain pending. See [ordering receipt](0.257-ordering-perf.md).
- Owner: [Typed Query Explain](0.257-design.md).
- User explicitly directed starting 0.257 on 2026-09-08 after reporting 0.256.1
  committed and pushed. Tag presence is not a prerequisite.
- Baseline: clean `bd40327d5657bd5700933d0d7e2dca162b4a4539` (0.256.1).
  Preflight changed no runtime code. The subsequent explicitly selected lowering
  slice is recorded in 0.257.0; Cargo versions and applications stay unchanged.

## Planned landing slices

| Slice | Bounded outcome | Status |
| --- | --- | --- |
| 1 | SQL-free typed `.explain()` over the existing planner, with current-authority validation, cursor rejection, safe owned-input rejection, direct tests, cost evidence and public guidance. | Split approved 2026-09-08; pending shared bounded diagnostics in slice 5. No typed terminal implemented. |
| 2 | Balance initial typed/structural `AND`/`OR` and `IN`/`NOT IN` lowering with one shared builder; preserve canonical normalization and identity. | Complete 2026-09-08, before slice 1. Focused tests and maintained lint gates pass. No new limits or SQL-parser changes. |
| 3 | Normalize each associative boolean group once while preserving canonical output, with direct parity tests and matched native performance evidence. | Complete 2026-09-08. Canonical-output checks and focused gates pass; native timing improves all measured cases. Compact typed membership and fluent append ownership are not included. |
| 4 | Lower typed membership directly into the existing compact representation; share construction with SQL and qualify semantics, cache rebinding and preparation cost. | Complete 2026-09-08. User explicitly approved the affected fingerprint/cursor hard cut; focused tests, maintained lint and repeated native probes pass. No compatibility path or format-version bump. |
| 5 | Bound shared logical diagnostic projection and hard-cut canonical renderers to fallible streaming output, preserving successful bytes and diagnostic-only identity isolation. | Pending shared preparation slices 8–10. Public renderer hard cut approved; numeric diagnostic cost gates remain open. |
| 6 | Remove normalization re-entry from canonical boolean ordering, preserving canonical output and identity, with direct parity tests and matched nested-CASE preparation measurements. | Complete 2026-09-08. Matched canonical corpus, focused tests and maintained lint pass; repeated native preparation improves. No CASE expansion-policy or diagnostic-limit change. |
| 7 | Bound CASE rewrite growth at its canonical owner, retain compact CASE above budget, and qualify semantics, canonical stability and preparation cost with the approved fingerprint/pagination hard cut. | Complete 2026-09-08. Fixed content-based budget, duplicate predicate expansion removed, focused tests/lint and native cost gates pass. No new query-rejection policy or compatibility identity path. |
| 8 | Bound typed/dynamic request input before conversion or cloning and safely dispose of rejected owned requests; direct boundary tests and release guidance. | Complete 2026-09-08; 86 final focused tests, maintained lint and focused all-feature lint pass. See input-admission contract. |
| 9 | Apply the same input policy at SQL preparation owners, covering retained dispatch, bindings, warm calls and rejected syntax ownership. | Complete 2026-09-08; shared input limits, effective binding-copy accounting and iterative SQL expression cleanup qualified. Parser/transport limits remain separate obligations. |
| 10 | Make expression preparation fallible and charge its actual work to existing request counters, including canonical ordering and safe intermediate cleanup. | In progress. Typed construction and schema-filter conversion charge introduced storage; text/blob/big-integer values reuse ownership, and big-literal size checks no longer encode. Parser/catalog internals, remaining copies and traversal bounds are unfinished; the preceding construction-cost instruction review flag is retained. |
| 11 | Charge ordinary access-planning candidate construction and actual cache validation/rebinding work using the same request authority. | Split approved; pending slice 10. No fresh-optimization promise or new planner route. |

## Explain pre-implementation gates

| Gate | Required evidence | Status |
| --- | --- | --- |
| Authorization | Reported 0.256 closeout followed by explicit user direction to start runtime work on 0.257. | Met 2026-09-08. |
| Shape validation | Exact shared owner for scalar/grouped misuse and grouped limits, with cursor rejection before lossy conversion. | Existing checks identified in dynamic session terminals; no extraction implemented. |
| Ordinary-query isolation | Diagnostic-only ceilings cannot change ordinary admission or identity. Shared input/preparation limits apply to ordinary queries too; admitted identity stays unchanged against slice 7. | Shared preparation approved; diagnostic projection/hash isolation qualification remains pending. |
| Cache freshness | Execution-planner validity/invalidation contract, not forced fresh optimization; distinguish count changes from invalidating lifecycle transitions. | Source contract identified; lifecycle tests pending. |
| Diagnostic construction | Fixed numeric depth/work/payload/render limits; enforcement before or during allocation; cleanup/drop qualification; specific missing checks at each owner. | Typed and SQL input limits and iterative owned-input cleanup pass native qualification in slices 8–9. Planner-produced construction/work and diagnostic projection/render limits remain pending. |
| Detached rendering | Fallible canonical writers enforce output incrementally for every invocation; DTO stays detached from session/request budget. | Hard cut approved; implementation pending slice 5. |
| Cost baseline | Immutable source/toolchain/features, matched actor/workload matrix, raw-Wasm/function/cycle ceilings established before production edits. | Source/lock/toolchain frozen; SQL-free baseline build passes. No performance measurements or ceilings: scope decision precedes numerical cost acceptance. |

## Verification and handoff

### Initial lowering (slice 2)

One balanced helper replaces both initial folds in `FilterExpr` lowering. For
128 comparison operands, the initial tree has seven junction levels instead of
127 (nine total expression levels including comparison and literal/field leaves).
Empty identities, source order and schema conversion are preserved. The only
production caller immediately canonicalizes before adding the expression to query
intent; canonical-association parity tests protect that unchanged boundary.
This does not change SQL parsing or make later canonical trees shallow.

Validation passes: 35 SQL-free filter/fingerprint tests; 51 all-feature
filter/fingerprint/SQL-predicate tests; SQL-free and all-feature focused lint;
the complete prescribed repository lint gate; formatting and whitespace.
An extra SQL-only test-target lint attempt fails with 64 unused migration-helper
errors outside this edit. The maintained SQL-only library lint passes; combined
SQL/migration test-target lint also passes. No unrelated migration code was
changed to silence the extra configuration. Root and detailed 0.257.0 notes are
current. No application edits or network lifecycle actions were needed.

Complexity: one Rust file, 15 fewer production lines and 191 net test lines;
six files in the full handoff including the earlier preflight receipt and
design/status/changelog updates. No new mode, limit, public API or runtime
authority. Cycle/Wasm measurements and full suites were not run.

### Explain preflight (slice 1, still pending)

Shared-normalization verification (slice 3) is recorded below; the explain
construction/renderer/authority gates remain open despite these improvements.

Preflight validation: SQL-free library build passes; shallow recursive cleanup
and deep iterative-cleanup controls pass. Deep recursive cleanup aborts the
isolated native probe after validation rejects, as recorded in the receipt.
This is not an implemented `.explain()` test or a Wasm stack measurement.
Local Markdown links and whitespace pass. No full suite, IC measurement,
downstream build or network lifecycle action ran. Existing 0.256 validation
results are not evidence that this proposed API exists.

After implementation, record focused SQL-free/parity/bounds/authority tests,
ordinary-read controls, cost and complexity deltas, and the active changelog
entry here before handoff. No additional feature is reserved in this line. If
the bounded-diagnostics prerequisite proves independently substantial, stop and
revise scope with the user rather than widening slice 1 invisibly.

### Shared normalization (slice 3)

The canonicalizer visits same-operator input groups before normalizing their
terms, then sorts/deduplicates/rebuilds once. Its existing flatten-only helper
handles groups exposed by normalization without re-normalizing their terms.
Final left association, comparator, deduplication, NULL and comparison rules
remain unchanged; recursive traversal and output disposal are not newly bounded.

Validation: the three direct canonical-output tests pass on baseline and
candidate. Final focused runs pass 46 SQL-free and 75 all-feature tests across
normalization, filters, fingerprints, intent and SQL lowering. The manual timing
test is ignored in those runs and passes when explicitly selected. The prescribed
lint gate and focused SQL-free lint pass; an initial test-helper style warning
was fixed before repeating the focused gate. Formatting/whitespace pass.

The [measurement receipt](0.257-normalization-perf.md) records frozen source and
binary identities, workload/timing boundaries and repeated baseline/candidate
runs. Balanced 64/128-term synthetic input takes about 83% less native time in
this pass; this is not IC-cycle or end-to-end query evidence. No full suites,
Wasm measurements, application edits or network lifecycle actions ran.

Complexity: seven files in this slice, 18 net production lines in the existing
canonicalizer plus 146 test/timing lines and three test-wiring lines. One
same-operator group collection replaces repeated subtree normalization; no new
mode, authority, public API, representation or collection buffer is added.
At the slice-3 handoff, compact typed membership and fluent append cloning
remained unimplemented; membership is now completed below.

### Compact typed membership (slice 4)

Nonempty typed `IN`/`NOT IN` moves its admitted list into the existing compact
operation through a constructor also used by SQL. This supersedes the membership
part of slice 2; the balanced junction builder now serves only `AND`/`OR`.
Empty typed lists keep their constant identities. Collection operands and mixed
coercions reuse the existing leaf-comparison predicate path, with no expression
tree re-expansion or new coercion authority.

The user explicitly approved the fingerprint/continuation-signature hard cut.
Restart affected pagination after deployment. The cursor format remains version
1, with current signature-mismatch rejection and no predecessor hash or decoder.

Validation passes: 55 SQL-free and 102 all-feature focused tests covering filter
lowering, canonicalization, fingerprints, cursor behavior, intent, SQL lowering,
stored scalar parity, schema rejection and A/B/A cache rebinding. The two manual
timing probes are ignored in normal SQL-free selection; membership timing passes
when explicitly run on both frozen binaries. Maintained repository lint and
focused all-feature lint pass after correcting one test-helper clone warning.
Formatting and whitespace pass. Full suites remain user-owned.

The [membership measurement receipt](0.257-membership-perf.md) isolates lowering,
normalization and predicate derivation. At 128 values, repeated native preparation
falls from about 528 microseconds to 6.8 microseconds. The baseline already
includes slices 2 and 3; this is not an additive percentage, full-query latency
or IC-cycle claim. No Wasm measurements, application edits, Cargo changes or
network lifecycle actions were needed.

Complexity: 13 files in this slice, seven fewer net production lines across five
owners, plus 277 net test/timing/wiring lines. Typed and SQL construction converge
on one existing representation, with no new mode or public API. The complete
pending worktree spans 17 files including previous slices and preflight. Active
root/detailed 0.257.0 notes are current. This handoff ends slice 4; fluent append
ownership and the explain/bounds prerequisites remain separate work.

### Shared diagnostics construction preflight (slice 5, pending)

Recorded the approved two-outcome split without renumbering earlier slices.
Inspection identifies an ordinary-query normalization/label-rendering feedback
path: sorting already-normalized operands invokes a helper that clones and
normalizes them again. Nested one-arm boolean `CASE` also duplicates conditions;
the local eight-arm cap is not a total expanded-node or work cap.

The [construction receipt](0.257-diagnostics-preflight.md) records source owners,
existing numeric limits and the controlled native probe. Six nested conditions
grow from 19 nodes/depth 7 to 694 nodes/depth 25. With debug assertions disabled,
that case takes about 279 ms in the opt-level-1 probe; this is not production
latency or IC-cycle evidence. The debug-enabled experiment completed that case
in about 37 seconds and was terminated during the deeper case, so that run is
not a passing test gate. The bounded debug-disabled control passes.

All temporary test-source edits were removed; the prior test file's exact hash
was restored. Four design/status/receipt files change in this handoff, with no
production, public API, Cargo, application or release-note change. Formatting,
whitespace and local document links pass. No full suite, production-profile
benchmark, Wasm/IC measurement or network lifecycle action ran. The split is
adopted, but neither bounded diagnostics nor typed `.explain()` is implemented.

The preflight recommended a separate owner-local normalization repair, first
proving unchanged canonical ordering/bytes/fingerprints and measuring the same
nested input. The user subsequently approved that outcome as slice 6. This
does not adopt a new CASE policy, relax diagnostic guarantees, or introduce a
duplicate expansion estimator merely to close the preflight.

Resumed after slice 6: recommend extending the existing CASE rewrite budget to
produced size and retaining the already-supported compact CASE when it would
exceed the cap. This avoids new ordinary-query rejection and an EXPLAIN-specific
copy of expansion rules, but may change canonical identity and plan selection.
It is not authorized by slice 5's identity-preservation contract; request explicit
approval before implementation or adding a separate tracker outcome. The updated
construction receipt records the alternative and disproves a suspected SQL
literal-REPLACE expansion path: its first operand must parse as a field. The
temporary probe failed at parsing, was not retried, and was fully removed.
Four focused canonicalizer tests pass, with one manual probe ignored. No net
runtime edits, release-note changes or new performance measurements result from
this preflight; the bounded-diagnostics feature remains unfinished.

### Non-reentrant boolean ordering (slice 6)

The comparator renders already-normalized operands directly with the existing
scalar renderer. Its two callers establish child normalization before comparing;
the general filter-label helper remains unchanged for other consumers. There is
no normalization/cloning re-entry from sorting, new renderer or sort-key cache.
Label ordering, Debug tie-breaking, deduplication, CASE expansion and canonical
identity remain unchanged. This slice introduces no additional hard cut.

Matched baseline/candidate canonical-model and label receipts agree. Final
focused validation passes 80 SQL-free and 139 all-feature tests, including SQL
lowering, accepted scalar parity, cache rebinding, verbose explain cache reuse,
fingerprint and cursor contracts. Two older manual probes are ignored in each
normal run; the new nested-CASE probe is compiled only with debug assertions
disabled and passes in four explicitly selected frozen-binary runs. Maintained
repository lint and focused all-feature lint pass after replacing a constant
test assertion with that compile-time benchmark gate. Formatting, whitespace and
local document links pass.

The [ordering receipt](0.257-ordering-perf.md) records the exact source/binary
identities and timing boundary. Six-level nested-CASE native preparation falls
from about 270 ms to 1.2 ms (99.6% less time), with debug assertions disabled in
both matched binaries. This is not an IC-cycle or end-to-end query result. No
full suites, Wasm/function/cycle measurements, application changes, Cargo edits
or network lifecycle actions ran.

Complexity: eight files in this slice; unchanged executable production line
count, two added intent-comment lines and 126 net test/timing lines. The flow is
simpler because an already-completed preparation stage is no longer re-entered.
The complete pending worktree spans 19 files including earlier slices and
preflights. Root/detailed 0.257.0 notes are current. Stop at this completed
handoff; next resume approved bounded diagnostics (slice 5), not typed explain
or a new CASE expansion policy.

### Bounded CASE rewrite (slice 7)

The user explicitly approved the CASE policy and fingerprint/pagination hard
cut. One fixed, content-based budget charges introduced nodes, both condition
copies and variable payload before expanding. Larger rewrites retain compact
CASE. The duplicate expansion in predicate compilation is removed; no new
evaluator, query-rejection rule, format or compatibility identity path is added.
Predicate access opportunities can change, including those of neighbouring
conjuncts under the existing whole-expression projection contract. Existing
execution admission is unchanged; there is no scan exemption.

The [CASE receipt](0.257-case-budget.md) records limits, exact source/binary
identities, qualification corrections and all five matched timing pairs.
Final validation passes 174 all-feature tests and 58 SQL-free expression tests,
with one/two manual benchmarks ignored respectively. Maintained three-lane
lint, focused all-feature lint, formatting, whitespace and local links pass.
Ten explicit native timing processes pass the unchanged cost ceilings: six
nested CASE levels fall from 1.23 ms to 0.10 ms; smaller cases remain near their
prior timings. This is not an IC-cycle or full-query result.

Twelve files in this slice, approximately 122 net production lines and 227
test/wiring lines plus docs. A private accounting walk is added while one
duplicate expansion flow is deleted. The full pending worktree spans 24 files;
active root/detailed 0.257.0 notes are current. Full suites remain user-owned;
no Wasm/IC measurements, application/Cargo edits or network lifecycle actions.
This is the completed slice-7 handoff. Next resume slice 5 against the current
hard-cut representation, not the still-pending typed terminal.

### Post-CASE diagnostic scope review (historical proposal, superseded)

The resumed review inventories the still-shared preparation/candidate work and
the separate diagnostic projection/formatting allocations. Recommendation:
retain ordinary planning's existing limits and bound the additional diagnostic
work in this release. The full construction proof remains part of the currently
approved design until that narrowing is authorized; neither bounded diagnostics
nor typed `.explain()` is reported complete.

Three design/status/receipt files change; no runtime, API, release-note, Cargo
or application changes. Fourteen existing explain-related tests pass on the
current all-feature binary; whitespace and local document links pass. These
are current-surface regression checks, not new limit tests. No full suite,
lint rerun, performance/Wasm/IC measurement or network lifecycle action ran.

### Shared typed input admission (slice 8)

The user selected shared bounded preparation and fallible rendering, not the
historical projection-only proposal. The tracker preserves completed slices and
adds separate typed-input, SQL-integration and preparation-work outcomes before
the pending diagnostic and typed-terminal handoffs.

One fixed borrowed-input budget admits depth 128, 4,096 nodes and 2 MiB of variable
payload across each typed/dynamic request before structural conversion. Filters
lower by reference, removing the prior whole-filter clone. Ordinary public and
trusted reads converge here; metadata-only exact count checks its input on the
existing bypass. No new semantic normalizer, execution route or authority is
introduced. Rejected owned requests detach recursive children during destruction;
the contract explicitly excludes caller construction/Clone/Debug/standalone DTO
drop, decoding and a fixed cleanup allocation/cycle bound for unadmitted input.

Final validation passes 74 all-feature core tests (input admission, accepted-row
and cache parity, fingerprints and cardinality/cursor lifecycle), eight SQL-free
input tests, three diagnostic-code round-trip/registry tests and one CLI hint
test: 86 total. The small-stack probe rejects and destroys 20,000-level filters
and values plus 10,000-level CASE input on a 512-KiB native stack. Maintained
three-lane lint and focused all-feature lint pass after making three cleanup
accessors const (two initial warnings and their direct propagation). Formatting
and whitespace pass. The initial broader input-name selection also passed 23
tests; those overlapping runs are not added to the final total.

Complexity: 24 files in this slice, approximately 470 net production Rust lines
and 280 test/wiring lines. This adds explicit admission and iterative cleanup,
while retaining one preparation/execution flow and removing one frontend clone.
The cumulative pending worktree spans 42 files including earlier slices. Active
root/detailed 0.257.0 notes are current. No performance improvement or Wasm-size
claim: benchmarks, IC cycles and Wasm/function measurements were not run. Full
suites remain user-owned; no application/Cargo edits or network lifecycle action.

This completes slice 8 only. Next: integrate the same policy into SQL preparation
and syntax ownership (slice 9), then shared work charging (10), bounded/fallible
diagnostics (5), and the typed terminal (1). The line is not at closeout.

### Shared SQL input admission (slice 9)

SQL now adapts authored syntax to the same depth/node/payload budget as typed
requests, replacing the depth-only lowering walker. Parsed query/write statements,
introspection and CHECK expressions are admitted before preparation. Current
compilation ingress checks retained/warm inputs; semantic normalization remains
accepted-schema owned, and DDL semantics remain catalog-owned. Existing binding
limits stay in place, with every effective scalar copy additionally charged before
substitution. Bound invocations retain their existing concrete-cache bypass.

The SQL expression owner now detaches recursive children during destruction,
covering partial parser failures as well as rejected and retained syntax.
Postfix parsing transfers its left operand, and BETWEEN admits both copies before
cloning. Ownership propagation preserves existing boxes/vectors and merges the
two scalar alias-rewrite traversals without changing their resolution policies.
Runtime-value cleanup is shared with typed requests rather than duplicated.

Final focused validation passes 308 tests: 211 SQL parser/lowering/input tests,
32 ordering/session tests, 26 fingerprint tests, 31 cursor tests and eight
SQL-free input/cleanup tests. These include exact limits, all query/write families,
effective binding copies, no-reparse A/B/A execution after rejection and deep
parser-error cleanup on a 512-KiB native stack. Initial execution-fixture failures
were corrected to use supported numeric CASE comparisons and explicit ordering;
no language or pagination policy was widened. Maintained three-lane lint and
focused all-feature lint pass after fixing ownership-propagation style warnings.
Formatting and whitespace pass.

Complexity: 27 files in this slice, approximately 25 net production Rust lines
and 225 test lines, plus documentation. Input checks and a syntax destructor add
explicit safety work; removing the old depth walker and duplicate alias traversals
keeps one admission authority and one preparation flow. The cumulative pending
worktree spans 63 files including earlier slices. Root/detailed 0.257.0 notes are
current; applications and Cargo versions remain unchanged. No full suites,
benchmarks, Wasm/function counts, IC-cycle measurements or network lifecycle
actions ran. No performance or Wasm-size change is claimed.

This completes slice 9 only. Next is actual shared preparation-work charging
(slice 10), then bounded/fallible diagnostics (5) and the typed terminal (1).
Authored-input limits do not bound planner-produced alias expansion or all
normalization work, and native cleanup tests do not establish Wasm stack safety.

### Preparation-work preflight (slice 10, implementation pending)

The [work-charging receipt](0.257-preparation-work-preflight.md) identifies two
independent owner changes: fallible expression preparation/ordering with safe
intermediate cleanup, and access-planning candidate/cache-work accounting.
The existing active-execution charge helper can no-op before execution, and
the request's 1,024 `PlanningSteps` limit currently counts planning operations,
not visited expression nodes. The receipt proposes existing-resource attribution
and a two-handoff split; neither is silently implemented as a new budget mode.

The user subsequently approved retaining slice 10 for fallible expression
preparation and adding slice 11 for ordinary access-planning/cache work, then
resuming 5 and 1. This follows the design's explicit rescope gate
before broad planner changes. No preparation-work completion is claimed.

Seventy-six existing focused budget/scope/canonicalization/fingerprint/cursor
controls pass; one manual timing probe is ignored. Three design documents change,
with zero runtime line delta. Whitespace and local document links pass. No
rebuild/lint rerun, full suite, benchmark, Wasm/IC measurement, application/Cargo
edit or network lifecycle action ran. Existing slice-9 release notes remain
unchanged because this handoff implements no release behavior.

### Expression ownership progress (slice 10, still in progress)

`Expr` now owns iterative destruction of planner trees, including intermediate
results. Dynamic-request cleanup delegates to this owner instead of maintaining
its own expression walk. Scope-preserving child transfer replaces rebuilding
and recursive CASE-child cloning in the affected boolean, CASE, aggregate-input,
affine and SQL normalization owners. The existing SQL syntax destructor remains
responsible for its distinct frontend type. No new variant, compatibility path,
semantic normalizer or request lifecycle is introduced.

This is not completion of slice 10: normalizers and ordering remain infallible,
and no new request-work charges are connected. An unused counter prototype was
removed rather than retained as production scaffolding. Next work must hard-cut
those signatures, wire the existing request authority, and qualify exhaustion
and costs before slice 11 begins. Existing operation-count limits are unchanged.

Nineteen files change in this progress checkpoint, approximately 200 fewer net
production Rust lines plus 52 new test lines. The native small-stack test disposes
of 20,000-level unary/binary/CASE/function/aggregate trees and nested values.
This proves disposal in that test environment, not recursive Clone/Debug safety,
bounded total preparation or a Wasm stack limit. Active 0.257.0 release notes
record only the implemented ownership changes.

Final focused validation passes 445 tests: 378 all-feature expression, SQL,
session, fingerprint, cursor, input and filter tests, plus 67 SQL-free expression
and input tests. Three manual timing selections are ignored. The maintained
three-lane lint gate and final focused all-feature lint pass after removing the
unused child-transfer helper and making two accessors const. Formatting,
whitespace and local document links pass. Full suites remain user-owned; no
benchmarks, Wasm/function counts, IC-cycle measurements, application/Cargo edits
or network lifecycle actions ran. This checkpoint does not close slice 10.

### Typed lowering and streaming-label progress (slice 10, still in progress)

Typed filter lowering now borrows the session's actual request scope and returns
typed exhaustion through the existing structural conversion path. Frontend node
visits, raw literal visits and raw text/vector construction are charged before
work. Instructions are sampled periodically and on completion, including errors.
The [preflight](0.257-preparation-work-preflight.md) records exact units and the
remaining uncovered owners. No root reset, optional metering mode or cache-held
budget is added. Public/trusted, grouped, exact-key candidate/fallback and filtered
exact-count conversions preserve their current routes.

Planner labels share one streaming writer rather than intermediate strings and
argument joins. Existing String consumers use that writer; a fallible sink can
stop at the first rejected write. This is not yet request-limited canonical
sorting, stack-bounded formatting or the public explain-renderer hard cut.

Direct tests cover nested-value exhaustion, cumulative retries through different
sessions sharing a root, cold/warm caches, public/trusted attribution and rejection
before row execution. Output-boundary tests preserve text escaping, precedence,
field paths and aggregate FILTER/DISTINCT identity. SQL-free and all-feature
filter/builder/expression controls pass, as do SQL, session, request, execution
budget, fingerprint and cursor controls: 501 focused passes across both feature
configurations, with four manual timing selections ignored in normal runs. The
maintained three-lane lint gate, focused all-feature lint, formatting and
whitespace checks pass. Initial compile errors and two fixture failures were
corrected before these final checks. The matched canonical-model/label receipt
remains `ac4fd65e7c16849940ffd1352c3ca10433696f2b8ae2fc36cf15ec3eceaf95aa`.
The existing A/B/A cache test now checks non-growing retained bytes rather than
requiring fresh buffers and cloned strings to have identical capacity. The new
budget fixture reuses its canonical registry constructor so it tests request
exhaustion, not admission against a different recovery domain.

Native cost qualification uses the existing typed-membership preparation probe,
five alternating before/after runs, each reporting the median of seven batches
of 64 calls. Query construction and request-root creation are outside the timer;
lowering, normalization, predicate extraction and disposal are inside. The new
borrowed work segment is inside the timer. Both binaries use the existing native
dev/test profile (opt-level 1, debug assertions enabled), all features and
`rustc 1.97.1 (8bab26f4f 2026-07-14)`.

| IN values | Before median | After median | Time increase |
| --- | ---: | ---: | ---: |
| 4 | 533 ns | 797 ns | 49.5% |
| 16 | 1,425 ns | 2,252 ns | 58.0% |
| 64 | 4,079 ns | 7,038 ns | 72.5% |
| 128 | 8,136 ns | 14,448 ns | 77.6% |

These are accounting costs, not a claimed speedup. The 128-value change is about
6.3 microseconds in this native preparation-only probe. It does not measure the
renderer improvement separately, ordinary read latency, IC cycles or Wasm size.
The before binary is the immediately preceding ownership checkpoint, not clean
HEAD alone. Frozen binary SHA-256 values:

- Before: `a7138f7c287adaa8b579eeb166aecd6ccdc098b5f28d2e28e6834a39228dda38`.
- After: `d83660f86c05ba50f60607af80b4336e2cfa7bf35e938bf7d70e0b4394f98368`.

This checkpoint touches 16 files including four documentation files, adding
approximately 170 production Rust lines and 300 test/wiring lines. Explicit
fallibility/accounting adds implementation complexity; streaming removes repeated
string construction. There is still one conversion route and one label grammar.
No Cargo/application changes, full-suite runs, IC/Wasm measurements or network
lifecycle actions were performed. Slice 10 remains open: canonical
normalization/ordering, SQL work and complete construction accounting are not
finished. Slice 11 and the explain slices have not started.

### Fallible canonical preparation (slice 10, still in progress)

Typed intent and SQL preparation now use the same fallible canonicalizer and
borrowed request scope. Canonical ordering charges comparisons, streamed key
bytes and requested buffer allocations; it sorts indices before permuting owned
operands, so rejection never supplies a partial key or fabricated ordering.
Successful label/Debug order, deduplication, CASE policy and left association
remain unchanged. Comparison reorientation/function arguments reuse allocations,
filter labels no longer clone/re-normalize, and HAVING append removes its unused
CASE-policy choice. Predicate-only filters avoid expression preparation.

SQL command misses and bound calls charge normalization actually performed;
valid concrete-command hits retain their compilation savings. Aggregate trial
lowering propagates query failures, including exhaustion, instead of treating
them as a shape miss. SQL read, explain and mutation preparation use their
existing trusted-read, diagnostic and mutation attribution. Current authority
resolution still precedes cache lookup.

Validation passes 602 focused tests: 505 all-feature and 97 SQL-free. These cover
expression/intent/filter/builder owners, SQL lowering and policies, accepted
session execution, cold/warm exhaustion, bound A/B/A rejection, request counters,
fingerprints and cursors. A 128-KiB native-stack probe rejects normalization and
safely disposes of a 20,000-level remainder; this is not a successful-traversal or
Wasm-stack guarantee. Four manual timing selections are ignored in normal runs;
the two measured probes pass when explicitly selected. Maintained three-lane
lint, focused all-feature lint, formatting and whitespace pass after correcting
test migration and lint issues. No validation failures remain.

Five alternating baseline/candidate runs use the existing all-feature native
opt-level-1 profile with debug assertions and rustc 1.97.1. Each sample is the
median of seven batches of 64 operations; table values are medians across five
runs, in nanoseconds. Boolean fixtures are reverse-ordered distinct field terms;
input cloning and finite-request construction are outside the timer, while
normalization, its debug checks and output disposal are inside. Each measured
shape has its own finite request outside timing. Membership includes accepted
literal lowering, normalization, predicate derivation and disposal; its borrowed
work segment is inside timing. No other validation/build was running.

| Terms / IN values | Left-chain normalization, before → after | Balanced normalization, before → after | Compact IN preparation, before → after |
| --- | ---: | ---: | ---: |
| 4 | 718 → 953 | 714 → 935 | 736 → 822 |
| 16 | 7,042 → 5,093 | 6,946 → 4,975 | 2,149 → 2,269 |
| 64 | 12,529 → 24,886 | 12,303 → 25,819 | 6,623 → 6,762 |
| 128 | 31,999 → 54,239 | 29,481 → 57,329 | 13,025 → 13,112 |

This is a safety/accounting cost, not a general optimization claim: 128-term
normalization rises 69.5%/94.5% (about 22/28 microseconds), while 128-value compact
membership is approximately unchanged in this probe (+0.7%). These are neither
release-profile/full-query results nor IC-cycle measurements. Baseline is the
immediately preceding typed-lowering/streaming-label checkpoint:

- Before binary SHA-256: `d83660f86c05ba50f60607af80b4336e2cfa7bf35e938bf7d70e0b4394f98368`.
- After binary SHA-256: `1935ff044585247d511d8fd3fa2278eda916f92c977485f10557c3ea88c49058`.
- Both produce canonical receipt `ac4fd65e7c16849940ffd1352c3ca10433696f2b8ae2fc36cf15ec3eceaf95aa`;
  the candidate now asserts that receipt as well as comparing semantic shapes.

Complexity: 46 touched files, including 42 Rust and four documentation files.
The new ordering owner adds 149 production lines and 140 direct-test lines;
remaining changes propagate the hard-cut signatures, reuse ownership and qualify
boundaries. Across these affected Rust paths, the pending delta from clean
0.256.1 is approximately +2,000 lines **including earlier 0.257 work**, not an
isolated delta for this checkpoint. Explicit fallibility and sorting machinery
add implementation complexity, with no new public mode, cache key, budget root,
normalizer or execution route; redundant re-preparation and one unused policy
are removed.

The [preparation contract](0.257-preparation-work-preflight.md) names remaining
uncharged copy/construction and classifier/deduplication work. Slice 10 is not
complete; slice 11 and typed explain have not started. No full suite, raw-Wasm
size/function count, IC-cycle measurement, Cargo/application edit, commit/push or
network lifecycle action was performed.

### Sort-key reuse and IC cost qualification (slice 10, still in progress)

The metered sorter now constructs each label once per term and each typed Debug
key only on its first label tie. Its charged, sort-local table is dropped before
operand permutation; request counters, canonical order and the abortable merge
remain unchanged. This removes repeated construction, not accounting. Retaining
all needed keys is an explicit concurrent-storage trade-off, not a new session
cache or an allocation-peak guarantee.

The [matched Wasm receipt](0.257-canonical-wasm-perf.md) compares frozen metered
before/after modules using the same canonical wasm-release audit actor. At 128
terms, application-built AND/OR instruction medians fall 25.24%/21.06%, and
whole-update cycles fall 17.79%/14.67%. Warm SQL keeps its compilation savings;
isolated IN controls match exactly. A shared-history IN instruction regression
disappears with fresh fixtures per workload and is retained as an investigative
finding, not silently discarded. Four-term AND instructions rise 0.26%: this is
not a blanket speedup. Raw Wasm grows 198 bytes and two defined functions.

The checkpoint spans nine files: +36 net core Rust lines, +33 audit-actor lines,
+180 test/harness lines, and documentation. One local table replaces repeated
rendering without adding an execution route or semantic authority. SQL AST/value
copies, CASE/projection construction, classifier/dedup work and traversal bounds
remain at the existing slice-10 boundary. Slice 11 and typed explain have not
started; application repositories and Cargo versions are unchanged.

Validation passes 524 focused native tests (460 all-feature and 64 SQL-free),
four isolated Wasm matrix runs, maintained/focused lint, formatting and
whitespace checks. Both full before/after replays reproduce every recorded
instruction/cycle sample exactly. The initial harness lint and flat-SQL fixture
issues are corrected; no failures remain. Full-suite validation is user-owned.
The measurement wrapper started/stopped isolated PocketIC servers and left
existing local networks alone. No commit, push or Cargo version change ran.

### Typed filter construction (slice 10, still in progress)

Typed lowering now charges copied field-name bytes and each introduced box or
function-argument slot immediately before allocation. Nonempty membership keeps
its shared compact constructor and value-vector ownership; empty membership and
empty junctions produce constants without planner backing storage. The repeated
text/collection branches now share charged field/function construction. No
accepted-schema coercion, canonical identity, frontend route or budget ceiling
changes.

Validation passes 192 focused native tests (179 all-feature and 13 SQL-free),
including exact/one-byte-short constructor limits and cold/warm public/trusted
retries rejecting before row execution without changing cached artifacts.
Canonical expression, fingerprint, cursor and intent controls pass, as do the
maintained three-lane and focused all-feature lint gates, formatting and
whitespace checks. Full-suite validation remains user-owned.

The [matched IC receipt](0.257-filter-construction-perf.md) reports under 1%
whole-update cycle growth across the measured matrix, +696 raw Wasm bytes and
four defined functions. One 16-term AND instruction case rises 4.655%
(+275,370), exceeding both existing review thresholds despite +0.240% cycles.
This is an explicit open performance-review flag, not a clean cost-gate verdict.
Further attribution or cost acceptance precedes calling this checkpoint's cost
qualification complete.
The three Wasm matrix runs pass functional checks, and both the frozen baseline
and candidate reproduce all recorded metrics exactly on their matched reruns.

Seven files change: +37 production Rust lines and +185 test/propagation lines,
plus five docs. Construction is less repetitive, with explicit fallibility but
no new execution route or runtime authority. Schema-directed value conversions,
SQL/canonical-copy construction, predicate projection/classification and
traversal bounds remain in slice 10. Slice 11 and typed explain have not started.
Only isolated PocketIC servers were started/stopped; applications, Cargo versions,
existing ICP networks, commits and pushes are untouched.

### Accepted-schema filter conversion (slice 10, still in progress)

The shared filter converter now borrows unchanged text/blob scalars and
recursively unchanged collections. Typed lowering retains its existing owned
value; a conversion materializes a separate list only at the first changed
element. All-element success is still required before replacing the original
typed literal. Introduced list buffers, borrowed-child copies, hex decode buffers
and enum-label copies charge before allocation, and scalar parsing consumes an
input-byte work allowance. Grouped HAVING retains its established partial
conversion semantics and propagates budget errors from the shared owner.

532 native tests pass, including 18 SQL-free controls. The maintained three-lane
and focused all-feature lint gates pass after correcting three style findings;
formatting and whitespace checks pass. No full-suite validation was run.

The [matched conversion receipt](0.257-value-conversion-perf.md) records
0.019–0.543% additional whole-update cycles and 0.050–1.172% additional
instructions across numeric AND/OR/IN controls. No new instruction review
threshold is crossed. SQL cold/warm samples remain identical. Raw Wasm adds
3,433 bytes and three defined functions. These controls measure accounting cost,
not text-copy latency; ownership tests establish unchanged-storage reuse.

Thirteen files change, about +161 production and +344 test/wiring Rust lines,
plus documentation. Ownership/error handling is more explicit, with fewer
redundant copies and no additional semantic owner or execution route. Scalar
parser internals, big-integer copies/encoding, deeper catalog admission and
grouped fallback copies remain open alongside the previously listed SQL/CASE,
predicate and traversal work. Slice 11 and typed explain have not started.
Applications, Cargo versions, commits and pushes remain untouched.
Three Wasm matrix runs pass; frozen baseline/candidate replays reproduce all 36
metric records exactly. Both measurement-owned PocketIC servers were stopped,
without changing existing local networks. A concurrent read-only ICYDB-022 check
confirmed the fix is already in IcyDB 0.256.1 while Toko Miner still pins 0.255.1;
no downstream dependency or issue record was changed.

### Big-integer literal validation (slice 10, still in progress)

The shared atom owner now reports exact signed/unsigned LEB128 lengths without
allocation. Filter conversion no longer clones a freshly parsed integer for
validation, and exact big values borrow unchanged storage. Strict SQL checks
exact input sizes before copying; its wider cast family is preserved. Necessary
container copies precharge a conservative magnitude-word allowance. Existing
text work charging remains, without claiming complete decimal-parser accounting.

143 focused native tests pass, including 58,443 encoder-oracle comparisons in
three atom tests. The initial concurrent session process was killed; its serial
retry passes. Maintained and focused lint, formatting and whitespace checks pass.
All four dedicated Wasm matrix runs pass and both frozen replays reproduce all
24 records exactly. Three measurement-owned PocketIC servers were stopped.

The [matched big-integer receipt](0.257-big-integer-perf.md) records lower warm
instruction medians in all six tested cells, but mixed cycles: −0.873% to
+0.605%. The unsigned 300-digit cycle increase remains visible. Matched raw Wasm
adds 375 bytes and loses one function; the audit fixture/probe separately adds
2,819 raw bytes and two functions. Fourteen files change, approximately +46
engine/atom and +254 test/measurement/wiring Rust lines. Conversion and size
validation are less repetitive, with no new semantic route or parser.

Full-suite validation remains user-owned. Applications, existing local networks,
Cargo versions, commits and pushes are unchanged. Decimal-parser internals and
other catalog encoding/copies remain open with the existing SQL/CASE, predicate
and traversal work. This checkpoint does not close slice 10 or start slice 11.

### Catalog big-integer size admission (slice 10, still in progress)

Accepted input normalization and canonical validation reuse the allocation-free
atom lengths instead of constructing four size-only encodings. A shared budget
helper preserves field-bound error precedence and exact five-byte overhead plus
payload charges. Actual persistence encoding and accepted families are unchanged.
This removes temporary work without claiming complete catalog request-work or
heap accounting.

The [matched structural-write receipt](0.257-catalog-admission-perf.md) records
0.394–3.074% lower warm instructions and 0.164–1.466% lower whole-update cycles
for three bounded widths. Raw Wasm shrinks 141 bytes, gzip shrinks 30 bytes and
defined function count is unchanged. Instrumentation separately adds 2,705 raw
bytes and five functions. All four successful Wasm runs pass; both frozen
replays reproduce all 12 records exactly. The first baseline build rejected a
concurrent test edit and was rerun with stable sources. Four measurement-owned
PocketIC servers, including that rejected attempt, were stopped.

54 focused native tests pass across all-feature, SQL-disabled and atom targets,
including 58,443 encoder-oracle comparisons. The maintained three-lane and
focused all-feature lint gates pass after replacing one test-only truncating
cast with checked conversion. Formatting and whitespace checks pass. Full-suite
validation remains user-owned.

Nine files change: four Rust and five docs. The production owner adds six net
lines and converges four repeated checks, with 175 additional test/measurement
lines. No new execution route or compatibility path is introduced. Applications,
Cargo versions, existing local networks, commits and pushes remain untouched.
Necessary catalog encoding/copies and the other preparation-accounting owners
remain in slice 10; slice 11 and typed explain have not started.
