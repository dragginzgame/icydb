# IcyDB 0.257 — Status

- State: forward design drafted; runtime implementation not started.
- Owner: [Typed Query Explain](0.257-design.md).
- User selected 0.257 for this capability and accepted design preparation.
  Explicit runtime-line start and the preflight gates below remain outstanding.
- Current 0.256.1 ICYDB-022 changes are separate and preserved. No Cargo versions,
  application files or 0.257 release entries are changed by this design draft.

## Planned landing slice

| Slice | Bounded outcome | Status |
| --- | --- | --- |
| 1 | SQL-free typed `.explain()` over the existing planner, with current-authority validation, cursor rejection, bounded diagnostic construction, direct tests, cost evidence and public guidance. | Designed; not implementation-ready. |

## Pre-implementation gates

| Gate | Required evidence | Status |
| --- | --- | --- |
| Authorization | Reported 0.256 closeout followed by explicit user direction to start runtime work on 0.257. | Closeout reported; runtime start pending. |
| Shape validation | Exact shared owner for scalar/grouped misuse and grouped limits, with cursor rejection before lossy conversion. | Contract specified; extraction scope unverified. |
| Ordinary-query isolation | Diagnostic ceilings cannot change ordinary admission, fingerprints or cursor identity; matched baseline/candidate proof including diagnostic-only rejection. | Invariant specified; caller inventory and parity qualification pending. |
| Cache freshness | Execution-planner validity/invalidation contract, not forced fresh optimization; distinguish count changes from invalidating lifecycle transitions. | Source contract identified; lifecycle tests pending. |
| Diagnostic construction | Fixed numeric depth/work/payload/render limits; enforcement before or during allocation; cleanup/drop qualification; specific missing checks at each owner. | Five-area source inventory added 2026-09-08; limits and coverage proof remain open. |
| Detached rendering | Conservatively admit both canonical renderers' output and intermediate memory before returning a plan; preserve detached DTO and infallible public renderer signatures. | Chosen approach; expansion proof pending. Guarantee is scoped to typed-explain results and unchanged clones. |
| Cost baseline | Immutable source/toolchain/features, matched actor/workload matrix, raw-Wasm/function/cycle ceilings established before production edits. | Open; no measurements or ceilings yet. |

## Verification and handoff

Design-only validation checks local Markdown links and whitespace. No runtime
tests, builds, IC measurements or downstream qualification are claimed. Existing
0.256 validation results are not evidence that this proposed API exists.

After implementation, record focused SQL-free/parity/bounds/authority tests,
ordinary-read controls, cost and complexity deltas, and the active changelog
entry here before handoff. No additional feature is reserved in this line. If
the bounded-diagnostics prerequisite proves independently substantial, stop and
revise scope with the user rather than widening slice 1 invisibly.
