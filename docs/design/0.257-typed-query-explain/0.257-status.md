# IcyDB 0.257 — Status

- Published: 0.257.8. Current authorized work continues in 0.257; publication
  does not close the remaining preparation or explain slices.
- State: typed `.explain()` and bounded detached diagnostics are delivered,
  with the measured diagnostic cost trade-off accepted. Input admission,
  boolean/membership lowering and CASE policy are complete. Shared expression
  and planner preparation (slices 10/11) remain in progress; this is not a
  complete preparation-bounds verdict.
- Owner: [Typed Query Explain](0.257-design.md).
- User explicitly directed starting 0.257 on 2026-09-08 after reporting 0.256.1
  committed and pushed. Tag presence is not a prerequisite.
- Baseline: clean `bd40327d5657bd5700933d0d7e2dca162b4a4539` (0.256.1).
  Preflight changed no runtime code. The subsequent explicitly selected lowering
  slice is recorded in 0.257.0; Cargo versions and applications stay unchanged.

## Planned landing slices

The post-.8 typed hash-error handoff removes substitute identity construction
and the grouped-key error wrapper. Continuation/SQL/grouped/mutation consumers
now propagate failures without adding a budget profile or changing successful
hashes. All 202 selected tests, full Clippy and relevant invariants pass. Matched
typed-explain raw Wasm adds 2,615 bytes/three functions; ordinary-read raw size
is unchanged. All nine reports match; warm instructions are unchanged and cold
totals rise less than 0.006%. Shared hashing/sorting admission remains the next
R1/R2 outcome, not completed by this handoff.
See the [implementation receipt](0.257-explain-remaining.md#typed-hash-errors--2026-09-12)
and [caller inventory](0.257-explain-remaining.md#shared-hash-authority-preflight--2026-09-12).

Pre-publication push-readiness review: the .8 handoff was ready for user-owned release
validation. All 124 focused cache, value-hash, map-order and fingerprint tests,
format checks and admission/authority invariants pass; previous full Clippy
qualification still applies to the unchanged runtime source. No new runtime
edits or cost claims in this review. Remaining hash/insertion work does not
require keeping this patch open; see the
[next-boundary review](0.257-explain-remaining.md#push-boundary-and-next-hash-work--2026-09-12).

Current R2 construction handoff uses fallible key constructors and existing
request helpers for names, containers, boxes and Rc backing. Completed memo
hits stay allocation-free; partial failures remain retryable. Hash failures
propagate, not common error identities. All 79 focused tests, full Clippy and
admission/authority checks pass; matched reports
agree and instruction totals change −0.15% to +0.19%. Typed-explain raw Wasm
adds 5,007 bytes/eight functions; ordinary-read raw Wasm adds eight bytes.
No new resource, policy or execution route. Literal-hash scratch, contract
construction and insertion remain separate; see the
[receipt](0.257-explain-remaining.md#cache-key-backing-admission--2026-09-12).

Current R2 handoff is qualified: concrete/template key construction, lookup and
warm lifecycle validation share existing request instruction accounting, with
compilation/rebinding separate and cache hits early. All 78 focused tests, core
Clippy and admission/authority checks pass. Typed-explain raw Wasm adds 1,401
bytes/three functions; diagnostic instructions rise 0.10–0.33%. Ordinary-read
Wasm is byte-identical. Production code loses 30 lines, with no new counter,
mode or cache. Allocation admission and insertion work remain open. See the
[receipt](0.257-explain-remaining.md#cache-lookup-instruction-admission--2026-09-12).

The user authorized hard-cut wire/storage size work on 2026-09-12, explicitly
including ULID and superseding its prior deferral. The
[storage tracker](0.257-storage-size.md) records ten bounded landing slices
and per-1,000 examples. S1 is complete: ULID Candid/binary Serde now carries
exact 16-byte blobs, saving 8,000 bytes per 1,000 single-ULID Candid messages.
Nineteen final focused ULID tests and all Clippy lanes pass; that transport
slice leaves database ULID bytes unchanged. S2 is also complete: direct narrow
integer fields use 1/2/4-byte payloads, saving 7,000/6,000/4,000 bytes per 1,000
non-null field saves. Affected databases require recreation. The 126 focused
storage/schema/query tests, all Clippy lanes, and relevant invariants pass.
One native timing benchmark remains ignored; raw Wasm, IC costs, and physical
stable allocation are unmeasured. Account's fixed 62-byte contract stays.
S3 is complete: structural big integers use minimal binary magnitudes, saving
40,000 bytes per 1,000 full 256-bit unsigned values or 53,000 for signed values.
Its 63 focused tests, core Clippy and relevant invariants pass; affected data
requires recreation. S4 is complete: fixed local scalars use direct payloads,
and Decimal uses a full i128 mantissa plus one scale byte. Nested Decimal saves
18,000 bytes per 1,000 values; ordinary structural Decimal saves 13,000.
Account's 62-byte payload stays intact. Its 108 focused tests, core Clippy and
relevant invariants pass; affected artifacts require recreation. S5 is complete:
binary bigint cursor values, compact Account/Decimal metadata and unpadded
URL-safe Base64 save 148,000 bytes per 1,000 representative grouped cursors,
or 402,000 per 1,000 authenticated scalar cursors. Saved cursors must be
regenerated and affected stored mutation-job records recreated. Forty focused
tests, affected-package Clippy and relevant invariants pass. S6 is complete:
ordered bigint index components use minimal binary magnitude, saving 46,000
bytes per 1,000 full 256-bit index keys. Numeric ordering, bounds, fold/reopen
and strict directional resume pass 94 focused tests, core Clippy and relevant
invariants. Affected databases/index artifacts require recreation and physical
position continuations require regeneration. S7 is complete: recursive field
encoding and row assembly remove temporary payload buffers. A row containing
1,000 Nat64 values avoids 27,020,000 bytes of temporary payload construction over
1,000 saves while retaining its 9,026-byte stored form. This source-derived total
is not a peak-memory or IC-cost measurement. Its 78 focused tests, all Clippy
lanes and relevant invariants pass; no additional migration is required. S8 is
complete: scalar/batch measurements retain Decimal text and native Candid
integers, with no encoded-byte reduction claimed. Decimal ingress now reads
only the text emitted by its current serializers. Its 31 focused tests,
schema/core Clippy and relevant invariants pass. The audit discovered an
existing IntBig CBOR/JSON round-trip defect and remaining generic binary-Serde
waste. S9 is complete: generic binary Serde uses native small integers and tagged
wide bytes, and human-readable bigints use decimal strings. Full 256-bit CBOR
values save 6,000/7,000/45,000 bytes per 1,000 NatBig/IntBig/U256 values. Sparse
bigints can grow; the storage receipt records binary and JSON trade-offs.
Regenerate affected external binary and bigint JSON snapshots; native Candid
and custom database formats are unchanged. Nineteen focused tests, all Clippy
lanes and relevant invariants pass. The read-only closeout found missing
default-feature test imports and further
Date/Subaccount binary-Serde savings; the user approved all three corrections
together. S10 resolves them. Date 2026-09-12 saves 8,000 CBOR bytes per 1,000
saves; a maximum Subaccount saves 32,000, including inside Account. Account's
62-byte database payload and Candid/JSON output shapes stay intact. Regenerate
affected external binary snapshots. Twenty-three focused tests, including the
default-feature write regression, and schema/core all-target/all-feature Clippy
pass. All reported storage closeout findings are resolved; the documented
representation trade-offs and deferred design candidates remain. This storage
verdict does not close the separate preparation/explain obligations.

The approved immutable cache-key sharing handoff is qualified. One private Rc
payload replaces deep expression/string copies on memo hits and cache insertion;
content identity, mutation invalidation and current authority remain unchanged.
Existing retention accounting conservatively includes each cache-owned handle.
Session-local SQL command/query handles also use Rc, matching their existing
plan caches; no new cache, mode, lock or persisted state is introduced.
All 78 focused tests, full Clippy lanes and admission/authority checks pass.
Matched .6 control Wasm is byte-identical. Typed-explain raw Wasm shrinks 4,673
bytes with 25 fewer functions. Scan/group diagnostic totals save 3,461–14,576
instructions; indexed cold saves 4,962 while indexed warm adds 51–60. All nine
reports match. This removes R2 construction work without claiming its remaining
admission/comparison bounds complete. See the
[receipt](0.257-explain-remaining.md#shared-immutable-cache-keys--2026-09-12).

The approved post-publication R9 qualification handoff is complete, with no
runtime edits. Current typed/SQL/mixed report parity and all Wasm gates pass.
The preserved pre-terminal ordinary-read gate now passes: 4,183,089 → 4,014,557
instructions (−4.03%), repeated in fresh fixtures using a verified artifact-only
wrapper. Seven of nine diagnostic samples still miss the unchanged +5% SQL
ceiling. The user explicitly accepts the measured diagnostic trade-off, closing
R9 and the associated slice-5 cost decision without passing or changing that
numeric ceiling. No further binding redesign or cache is authorized. This
supersedes older open-cost/missing-control verdicts, not their measurements.
See the [acceptance decision](0.257-terminal-qualification.md#accepted-diagnostic-trade-off--2026-09-11).
Preparation slices 10/11 remain separate obligations; ULID work is now owned
by the subsequently authorized storage tracker.

The approved .6 projection-alias correction is qualified: output aliases now
participate in shared plan-cache identity. A stored-row SQL regression reproduced
`second_label` returning the prior `first_label`; scalar/grouped A/B/A cases now
preserve labels and values. The existing tuple retention visitor includes alias
backing, with no new response overlay, cache policy, budget, API or persisted
format. Distinct aliases retain distinct entries under the same weighted limit.
All 74 focused tests, core Clippy and admission/authority invariants pass. Ordinary
read Wasm is byte-identical; typed explain adds 887 raw bytes and three functions.
Nine diagnostic reports match, with 5–16 extra instructions per call. Production
line count is unchanged; broader preparation bounds remain separate. See the
[receipt](0.257-explain-remaining.md#projection-alias-cache-identity--2026-09-12).

The bounded ORDER BY R2 correction is qualified: structural expression keys
replace display labels. Regressions reproduced both valid warm plans masking an
invalid typed order and invalid plans poisoning the valid order. Both A/B/A
sequences now match cold behavior; direction, canonical aggregate equivalence
and nested retention checks pass. All 71 focused tests, core Clippy and
admission/authority invariants pass. No new cache, budget, API or persisted state
is introduced; one rendering flow leaves key construction. Ordinary instructions
and raw Wasm size are unchanged; typed explain adds 11 bytes and one function.
Scan/sort diagnostic totals rise 0.04–0.20%; other measured shapes save one
instruction. Other R2 construction bounds remain open. See the
[receipt](0.257-explain-remaining.md#typed-order-cache-identity--2026-09-11).

The user-approved aggregate R2 correction is qualified: typed structural
input/filter keys replace display labels. A stored-row regression reproduced
decimal output from a U256 SUM after decimal SUM warmed the cache. Borrowed
aggregate semantic identity preserves COUNT/DISTINCT normalization, removes an
input-tree copy and makes the duplicate target-field label unnecessary. Both
new structural operands use existing retained-memory accounting. No new cache
or budget policy is added; other preparation bounds stay separate.
All 68 focused tests, full Clippy lanes and admission/authority checks pass.
Matched diagnostic totals save 342–3,503 instructions; ordinary-read instructions
are unchanged across three fresh pairs. Raw Wasm is unchanged for the ordinary
control and +360 bytes for typed explain, with three extra functions in the
latter. See the [receipt](0.257-explain-remaining.md#typed-aggregate-cache-identity--2026-09-11).

The bounded R2 identity correction is qualified: all structural-query setters
now use existing map_intent/try_map_intent helpers, discarding memoized identity
when intent changes. A session regression first reproduced a warmed limit-1
query returning its prior plan after changing to limit zero. Scalar/grouped
warm-versus-fresh checks and public/trusted/diagnostic A/B/A reuse cover the fix.
All 61 focused session tests, full Clippy lanes and admission/authority checks
pass. Full repository suites remain user-owned.
Matched diagnostic instructions are unchanged for indexed equality, −680 per
scan/sort sample and −82 per grouped sample. Typed-explain raw Wasm is +55 bytes
with unchanged function count; the ordinary-read control is byte-identical.
No new cache, accounting mode or semantic policy is introduced. Other R1/R2
bounds remain separate work; see the
[receipt](0.257-explain-remaining.md#query-mutation-cache-identity--2026-09-11).

The bounded R1/R2 instruction handoff is complete: scalar normalization,
parameter-contract derivation and predicate fingerprinting share one existing
PreparationWork interval, completed before cache reuse or publication.
Filterless/trivial fast hits and separate miss/rebind construction intervals
remain unchanged. No new resource, counter, mode or coercion owner is added.
All 58 focused session tests, core Clippy and admission/authority invariants
pass. Matched diagnostic totals rise 0.0007–0.298%; typed-explain raw Wasm grows
509 bytes with no new functions. The ordinary-read artifact is byte-identical.
See the [receipt](0.257-explain-remaining.md#pre-cache-instruction-accounting--2026-09-11).
Conversion/sort scratch and intra-operation checks remain separate obligations,
not a claimed complete normalization bound. Actual cache-key construction,
comparison and warm validation remain R2 work.

The bounded R1 construction handoff is complete: predicate normalization now
has one owned implementation. Schema normalization, expression lowering and
parsed schema predicates move their output instead of copying it again. The
test-only alternate normalizer and double-negation normalization re-entry are
removed. Borrowed identity consumers explicitly preserve their source.
Canonical predicates, fingerprints and constant outcomes remain covered by
75 predicate and 233 related planner/schema/identity tests; focused Clippy passes.
Ordinary-read instructions are unchanged. Raw Wasm is −96 bytes for the control
and +4 bytes for typed explain; both have one more function. Indexed diagnostic
totals change −4,675 to +1,170 instructions; six other samples are unchanged.
See the [receipt](0.257-explain-remaining.md#owned-predicate-normalization--2026-09-11).
This removes construction before budgeting it; schema conversion, sorting and
remaining normalization admission are still R1 obligations. No new accounting,
behavior mode or binding work is introduced by this handoff.

User-approved combined downstream follow-up: ICYDB-031 canonical build-path
resolution and single-package qualification; ICYDB-028 generated Serialize
opt-in and blob round-trip guidance; ICYDB-030 controlled native-memory
attribution. These remain three distinct outcomes, explicitly combined by the
user. ULID and application edits are excluded. Serialize uses the existing
trait-directive owner (one opt-in trait, no default change); the simplest
alternative, handwritten serializers, duplicates generated field contracts.
No memory-policy change is authorised merely by an OOM report. Measure native
memory only to attribute that reported failure, not as an IC performance proxy.

Combined downstream follow-up implemented: ICYDB-031 now resolves the authored
type's canonical `Path` and qualifies the single-package host/runtime layout;
ICYDB-028 adds opt-in generated serialization and a collection blob round-trip.
ICYDB-030 attribution reproduces a roughly 488-MiB per-database native backing
allocation dominated by the existing checkpoint region. It does not establish
the entire application OOM or change production memory policy. A separate
checkpoint storage correction remains open; see the
[downstream qualification receipt](0.257-downstream-qualification.md).

User-approved ICYDB-030 reduction: use ordinary variable-size B-tree pages for
checkpoint records and make absent checkpoint/migration/lineage reads
non-allocating. The existing record codecs retain their admission ceilings;
no memory policy, native-only behavior or persisted format is added. Keep the
fixed partition in place for this bounded correction: moving/converging the
two metadata regions is a separate persisted-layout decision. Qualify both
ordinary journaled startup and real checkpoint writes, since their remaining
allocation costs differ. Existing allocated memory is not reclaimed by a code
change alone.

The bounded correction is qualified: fresh non-migration journaled backing is
67,174,400 bytes (64.0625 MiB), down from 511,770,624 bytes (86.9% less), with
matching IC stable allocation. The migration-enabled native fixture is
318,832,640 bytes (304.0625 MiB); its real checkpoint writes still cross the
fixed partition. Eight checkpoint tests, four candidate/recovery tests, eleven
application-store tests and focused fixture/lint checks pass. Matched IC
watchdog/write cycles decrease; raw Wasm is +684 bytes with one fewer defined
function. The persisted-layout contraction remains an open follow-up, not a
claim that ICYDB-030 or the separate explain cost gate is fully closed.

User-authorized hard cut: replace the two fixed metadata partitions with one
variable-page schema-control B-tree after the existing bounded marker region.
Application and checkpoint records occupy separate key namespaces; their
codecs, limits and marker-owned compare/replace semantics remain authoritative.
This removes the 256-MiB gap without adding an allocator, policy or compatibility
route. The simpler alternative of shrinking a partition leaves arbitrary
capacity coupling; a shared physical owner removes that partition entirely.
The state-space change is one current shared encoding replacing two physical
trees, with two fixed record families, not a caller-selectable mode. Current
boot identity changes at format version 1; recreate databases, with no prior
decoder or migration bridge. Qualify namespace isolation, retention, checkpoint
recovery, migration/lineage and matched native/IC costs in this landing slice.

The hard cut is implemented and qualified. Fresh migration-enabled backing
drops from 304.0625 MiB to 64.0625 MiB on both native and IC builds (240 MiB,
78.93% less). Default backing remains 64.0625 MiB with 512 KiB less virtual
extent. Raw Wasm falls 5,398 / 5,483 bytes for default / migration builds;
both have three fewer functions. Default watchdog/write cycles rise 0.29% /
1.28%; migration rises 0.37% / 0.22%. Namespace, retention, corruption,
recovery, native and PocketIC lifecycle qualification plus repository Clippy
and invariants pass. Recreate databases; no in-place upgrade from the replaced
layout is supported. This closes the fixed-gap defect, not all downstream OOM
attribution or the separate explain cost gate. Full workspace tests remain
user-owned. See the [hard-cut receipt](0.257-downstream-qualification.md#authorized-hard-cut-one-compact-schema-control-tree).

Release-validation repair: remove duplicated projection/metadata/lowering cost
formulas from template-cache tests while retaining exact rejection, publication
and cold-versus-warm construction assertions. Journal-backlog coverage now
distinguishes journal quiescence from a missing generated-schema submission.
Both reported failures and the related focused regressions pass, including
no-default journal coverage. Production limits and startup behavior are unchanged;
full release validation and the explain cost gate remain separate.

The approved simplification contract and four remaining diagnostic handoffs are
in [boundary-based accounting](0.257-explain-remaining.md#boundary-based-accounting--2026-09-11).
Prefer removing construction and admitting conservative bounded batches over
per-operation bookkeeping; existing safety outcomes remain required.

Delivery order revised with user authorization: finish logical diagnostic
projection/rendering (5), then typed explain (1), then the remaining shared
preparation outcomes (10/11, including recovery-safe R5). All outcomes remain
required for line closeout; incomplete planner accounting is not a claim of
bounded planning and is no longer a prerequisite for delivering the terminal.
The [2026-09-10 reachability audit](0.257-explain-remaining.md#frozen-terminal-prerequisite-scope--2026-09-10)
freezes this boundary: runtime logical explain borrows a finalized shared plan;
its grouped residual check reads frozen shape facts. Initial residual derivation
and SQL execution-descriptor construction are not terminal prerequisites.
The prepared projection path is now enforced at the session-facing type boundary
and qualified with cold/warm scalar/grouped residual plans, cumulative rejection,
unchanged identity and detached rendering. Missing static facts reject during
prepared construction. Raw model projection stays inside the query module.
Scalar-label bigint conversion now uses the shared decimal magnitude owner:
limb/chunk backing and a conservative conversion allowance are admitted once by
the current preparation request before work; arithmetic has no budget callbacks.
Grouped decimal digits stream without intermediate
Strings. The schema limb helpers return borrowed iterators; key and Structural
Binary encoders use that handoff without changing their bytes or admission policy.
This is a low-level limb-API hard cut, not full formatter completion.
The user-approved diagnostic fingerprint hard cut is implemented. Logical explain
projects only metadata, operand DTOs and labels, and performs no identity hash.
The diagnostic source/profile, retained digest, label-matching hash path and
fallible hash-renderer plumbing are deleted. Continuation hashing consumes the
planner directly with its exact current bytes; schema/cache identity is unchanged.
Tests compare full DTOs/canonical reports and independently pin cursor identity.

Prepared projection, admitted direct/nested bigint labels, bounded operand copies,
128-level access projection and capped detached rendering remain in place.
Existing borrowed-key/value encoding simplifications remain useful to ordinary
identity. Map sorting, predicate hash buffers and hash traversal no longer belong
to diagnostic projection; preparation accounting remains required at R1–R6.
R7/R8 functional projection/rendering qualification is complete at the prepared
boundary; the [owner inventory](0.257-explain-remaining.md#projectionrendering-qualification--2026-09-11)
records the existing bounds and their tests. Early, intermediate and one-short
rejections cover all three construction resources on cold/warm scalar/grouped
plans without changing source identity or executing rows. This handoff changes
tests/docs only; no new runtime layer or accounting policy is needed.
R9's typed terminal is implemented through the existing binding, dynamic
conversion, shared planner/cache and prepared projector. SQL-free native tests
cover detached results, decode isolation and cursor/shape rejection; core tests
cover stale bindings, metadata lifecycle refresh and exact-evidence cache reuse.
Focused functional tests and lint pass. Initial matched Wasm gates pass, but all nine
typed instruction samples miss the frozen +5% SQL-comparison ceiling; the
ordinary-read instruction control is unmeasured. R9 is not qualified for closeout;
see the [terminal receipt](0.257-terminal-qualification.md). Do not relax the gate
or add caching/binding redesign without an explicit scope decision.
The user-authorized cost investigation identified a whole-schema-bundle clone
in typed binding issuance. The owner now projects the same owned binding from
the existing verified borrow; matched before/after instrumentation and focused
validation pass and are recorded in the terminal receipt: warm planning uses
5.60–5.94% fewer instructions, with +86 raw Wasm bytes and unchanged function
count. This owner-local result alone did not requalify the complete matrix.
No binding cache is added.
The subsequent investigation attributed warm costs primarily to binding issuance
and validation, not DTO projection. Binding validation performed two durable
incarnation reads (about 91,000 instructions each); catalog lookup between them
could select the session's commit allocation. The resulting correction
establishes that domain first and passes one checked incarnation
through the shared matcher, with cross-domain/stale-binding tests. The user
approved that correction and it is now implemented; matched qualification is
recorded in the terminal receipt. Matched explain planning uses 2.97–3.00% fewer
instructions cold and 11.38–12.20% fewer warm; direct exact-key samples improve
2.49–2.67%. Raw Wasm is 26 bytes smaller with one more defined function. The full
SQL/mixed/control matrix is now rerun: all reports and Wasm gates pass, while
eight of nine samples still miss the frozen instruction ceiling. Cold grouped
planning passes; warm planning remains 26.04–50.42% above SQL total before typed
rendering. Typed-only Wasm saves 847,328 raw bytes (25.7%) versus SQL explain.
Original ordinary-read instruction telemetry remains unmeasured. R9 cost
acceptance needs an explicit next scope decision; no ceiling is changed.
All temporary phase probes remain removed.
The user-approved borrowed commit-control correction is implemented and
qualified locally: names borrow the bounded payload, and only ownership consumers
materialize the registry. All 28 control-store, 16 adapter and three core explain
tests pass, as do focused lint and the matched PocketIC comparison. Explain
planning improves 0.59–0.61% cold and 1.04–1.27% warm; exact-key samples improve
about 0.54%. Raw Wasm grows 1,038 bytes and two functions. No persisted bytes,
authority checks, cache or accounting policy change. The terminal receipt records
the trade-off; full SQL/mixed/control and original-read qualification remain open.
Do not add diagnostic hash admission machinery.
The failed instruction gate and missing control remain outstanding.
Current construction handoff: logical explain now requires the caller's
`PreparationWork` and returns a fallible DTO. Scalar filter labels, HAVING,
group-key/path copies, aggregate labels and order labels use existing charged
copy/writer owners. SQL scalar/grouped/global logical explain supplies the current
diagnostic request scope, including cache hits. Verbose execution reads its
fixed-size scalar facts without constructing a full logical DTO.
Tests cover repeated borrowed projections, exact cumulative exhaustion, unchanged
continuation identity and detached rendering. Predicate DTO copies charge visits,
list/box backing, values and coercion parameters; no identity-only model is copied.
Execution descriptors use the same required, fallible DTO projector under the
existing SQL diagnostic request, without constructing an identity-model copy.
Access DTO projection now also charges nodes, operand/field copies and final
composite backing before construction in logical, load and aggregate explain.
The shared visitor borrows index-field iterators and receives child projections
on demand: labels, admission summaries and root access scoring no longer build
discarded child results. Hashing preserves its child-before-parent byte stream;
execution descriptors retain required children in order. No second access walker
or uncharged DTO fallback is retained.
Logical decision projection now charges candidate/alternative/rejection backing,
owned names and label writes, selected-identity comparisons and composite
constraint-count visits. Matching shares the candidate copy pass and preserves
the first matching identity. Candidate/rejection display and access-label writers
stream into the existing destination, including fallible diagnostic construction.
Grouped strategy's aggregate-family and streaming checks now inspect borrowed
identity facts without copying aggregate input expressions. COUNT row-identity
classification and owned normalization share one rule; DISTINCT facts reuse the
existing kind-specific normalization. No additional cached strategy is introduced.
Grouped order proof now checks canonical fields/constant offsets shallowly;
heap detection and Top-K admission use the shared short-circuit expression
visitor, with no custom recursive summary. Canonical prefix checks omit trailing
terms and cursor validation only renders a label for the error that consumes it.
Strategy-local aggregate visits, Top-K/heap/HAVING walks and field/index-key
comparisons now require a fallible observer in the shared evaluator. Logical
explain supplies current request charging and propagates exhaustion without a
fallback; ordinary identity stays infallible and independent of diagnostics.
The shared Expr visitor stops before later children on false or error.
Finalized effective-predicate inspection now borrows the frozen residual rather
than cloning it for presence, ranking or execution explain. One `Cow` view keeps
the existing owned pre-finalization derivation at the same semantic owner;
consumers do not select phases or create another cache. DTO copies remain
charged at projection. This closes the finalized inspection copy only, not
residual derivation or whole R7 admission.
Access-bound residual proof construction now borrows index fields and operands:
equalities are visited on demand, range clauses use two fixed borrowed slots,
and branch membership borrows its field and value slice. Authored comparisons
and these proof facts share the same scalar implication owner; strict access
coercion and authored coercion remain distinct. This deletes temporary proof
vectors and payload copies without changing pruning policy. Guard implication,
comparison traversal, text-prefix encoding and retained residual construction
still need qualification; no new resource limit or boundedness claim is added.
Implication classification and proof search now share one short-circuiting
borrowed clause visitor. It removes temporary clause vectors, non-null
deduplication and copied single-clause predicate shells in filtered guards and
sparse-index completeness checks. Conservative required/query classification
is unchanged. Proof searches revisit authored syntax instead of retained lists;
traversal/comparison accounting and matched IC cost qualification remain open.
Residual pruning now consumes an owned predicate, compacts AND backing in place
and moves collapsed children. Finalization admits the preparation copy and its
independently retained residual copy through current `PreparationWork` before
construction. Pre-finalization guard-to-bound handoff moves its temporary instead
of copying it again. Existing capacity-based retention includes spare AND backing.
Newly counted copies can exhaust a request; no limit or cache policy changes.
This closes these two retained-copy owners, not implication/comparison visits,
text-prefix proof allocation, residual-expression derivation or predicate-program
compilation. Typed `.explain()` remains pending. R7/R8 functional construction and
formatter qualification are complete as recorded above; their matched IC/Wasm
cost gate remains with R9. Upstream effective-residual
construction belongs to preparation; execution descriptors are a separate
diagnostic surface. Planner candidate enumeration is R3 work. No new resource,
limit, optional accounting route or budget reset.

Access visitor contract: eager field-name/child buffers prevented admission before
allocation, even when consumers discarded them. The smallest shared change is
borrowed field iterators and a lazy child callback at the existing visitor owner.
Each consumer retains its existing semantics; collecting diagnostics require the
current request budget, while ordinary identity remains independent of it. This
changes construction ownership, not a query mode, cache identity or stored format.

Prior rendering handoff: fallible canonical logical text/JSON with a fixed
1 MiB UTF-8 output ceiling, checked at each append. One shared streaming JSON
owner serves logical and execution output; execution reports retain their
existing output policy. DTO projection, recursive construction and complete
planner/callback bounds remain separate. No new session or budget scope.

Need: prevent unbounded output and lost formatter errors at detached rendering.
Simplest alternative (checking a completed String) allocates before rejection.
Canonical owner: explain writers; state delta: one local sticky output failure,
one diagnostic rejection code, fallible public canonical renderer signatures.
No encoding mode, public configuration, cache or persisted state is added.

| Slice | Bounded outcome | Status |
| --- | --- | --- |
| 1 | SQL-free typed `.explain()` over the existing planner, with current-authority validation, cursor rejection, safe owned-input rejection, direct tests, cost evidence and public guidance. | Complete with explicit user acceptance of the measured diagnostic instruction trade-off. Functional, report, Wasm and ordinary-read gates pass; seven original +5% SQL comparison misses remain recorded, not relabelled as passing. See the terminal receipt's acceptance decision. |
| 2 | Balance initial typed/structural `AND`/`OR` and `IN`/`NOT IN` lowering with one shared builder; preserve canonical normalization and identity. | Complete 2026-09-08, before slice 1. Focused tests and maintained lint gates pass. No new limits or SQL-parser changes. |
| 3 | Normalize each associative boolean group once while preserving canonical output, with direct parity tests and matched native performance evidence. | Complete 2026-09-08. Canonical-output checks and focused gates pass; native timing improves all measured cases. Compact typed membership and fluent append ownership are not included. |
| 4 | Lower typed membership directly into the existing compact representation; share construction with SQL and qualify semantics, cache rebinding and preparation cost. | Complete 2026-09-08. User explicitly approved the affected fingerprint/cursor hard cut; focused tests, maintained lint and repeated native probes pass. No compatibility path or format-version bump. |
| 5 | Bound shared logical diagnostic projection and hard-cut canonical renderers to fallible streaming output, preserving successful bytes and diagnostic-only identity isolation. | Complete: admitted projection, 128-level derived access depth and fallible 1-MiB summaries. Matched Wasm passes; diagnostic instruction trade-off explicitly accepted with slice 1. Preparation 10/11 remains separate and incomplete. |
| 6 | Remove normalization re-entry from canonical boolean ordering, preserving canonical output and identity, with direct parity tests and matched nested-CASE preparation measurements. | Complete 2026-09-08. Matched canonical corpus, focused tests and maintained lint pass; repeated native preparation improves. No CASE expansion-policy or diagnostic-limit change. |
| 7 | Bound CASE rewrite growth at its canonical owner, retain compact CASE above budget, and qualify semantics, canonical stability and preparation cost with the approved fingerprint/pagination hard cut. | Complete 2026-09-08. Fixed content-based budget, duplicate predicate expansion removed, focused tests/lint and native cost gates pass. No new query-rejection policy or compatibility identity path. |
| 8 | Bound typed/dynamic request input before conversion or cloning and safely dispose of rejected owned requests; direct boundary tests and release guidance. | Complete 2026-09-08; 86 final focused tests, maintained lint and focused all-feature lint pass. See input-admission contract. |
| 9 | Apply the same input policy at SQL preparation owners, covering retained dispatch, bindings, warm calls and rejected syntax ownership. | Complete 2026-09-08; shared input limits, effective binding-copy accounting and iterative SQL expression cleanup qualified. Parser/transport limits remain separate obligations. |
| 10 | Make expression preparation fallible and charge its actual work to existing request counters, including canonical ordering and safe intermediate cleanup. | In progress; not a boundedness closeout. Frontend-specific copies/traversals remain here. Retained planner-key construction and comparison belong with slice 11's shared request authority, not a second frontend resolver. Existing cost review flags remain open. |
| 11 | Charge ordinary planner construction, candidate work and actual cache validation/rebinding using the same request authority, including shared retained-key owners. | In progress: shared request authority reaches planner finalization and retained grouping-key construction/rebinding. Path copies, key backing and insertion comparisons charge existing resources; static index slots/compile targets share one budgeted traversal. Direct labels/types and template index lists are shared. Schema lookup internals, other planner copies/validation, candidate scoring and remaining cache checks stay open. Frontend-specific residuals remain in 10; both remain required for line closeout under the revised delivery order. |

## Memory allocation diagnostics qualification

The user approved the `ic-memory 0.13.0` follow-up on 2026-09-10: extend the
existing storage snapshot and qualify its accounting with a disposable
128-page versus 16-page bucket trial. Status: complete in active 0.257.4. This is one bounded
diagnostics outcome; the production bucket default and automatic placement
remain outside it.

- Need: current store snapshots cannot attribute physical bucket slack or
  allocations without current key bindings.
- Simplest alternative: project the dependency's bounded report into the
  existing controller-only `icydb_snapshot` response and CLI, preserving all
  slots and accounting meanings. Add no endpoint or independent memory scanner.
- Owner: `ic-memory` validates and measures manager metadata; IcyDB owns only
  the Candid projection and display. These are canister-wide allocation facts,
  not database payload occupancy or accepted-schema authority.
- State-space delta: one optional report field (absent when no default runtime
  exists), plus diagnostic DTOs. Collection failures fail the existing request;
  no caches, persisted state, production configuration or compatibility path.
  Fresh test runtimes exercise both bucket sizes using the upstream config API.

Focused native accounting/Candid, CLI and generated-endpoint PocketIC checks
pass. The matched snapshot-enabled Wasm probe grows by 21,989 raw bytes
(+0.630%). The native populated fixture uses 7,405,568 backing bytes with
16-page buckets versus 42,008,576 with 128-page buckets at equal virtual extent;
IC cycle/instruction deltas remain unmeasured. See the
[qualification and usage guidance](0.257-memory-allocation-report.md).

## Additional upstream follow-ups authorized in 0.257

The user requested ICYDB-023, ICYDB-021 and ICYDB-025 together. They remain
independently reviewable outcomes; this does not start another minor line.

| Slice | Bounded outcome | Status |
| --- | --- | --- |
| 12 | ICYDB-025: name and document the generated native driver for repeated convergence, with more-than-64-message qualification and unchanged production backpressure. | Complete in active 0.257.3: generated native driver renamed, all maintained callers migrated, 129-message fixture and exact 64-batch pressure checks pass. No alternate driver or bound override. Downstream adoption remains separate. |
| 13 | ICYDB-023: bounded metrics selection at the accumulator owner and unambiguous reset-window identity through the existing facade. | Complete in active 0.257.3: bounded lexical-prefix reporting, coverage metadata and heap-local reset identity; 11 focused tests and focused lint pass. No scheduler, history store or latest-sample cache. |

### ICYDB-023 implementation contract

- Need: bound source reporting before copying/sorting, and distinguish explicit
  resets even in the same millisecond. Metrics remain volatile by user choice.
- Simplest alternative: fixed lexical-prefix selection from the existing ordered
  accumulator, at most 64 entities and 64 KiB of copied path bytes. Stop at the
  first entry that cannot fit; do not scan skipped entries. Cost sorting applies
  only to selected entries, not a global top-K. Report total entity count so
  partial coverage is visible. Accumulator retention itself is not bounded here.
- Owner: existing metrics state/report facade; endpoint arguments stay unchanged.
  No selection modes, scheduler, persistence or alternate report route.
- State-space delta: one optional heap-local reset sequence. It starts at zero,
  advances on reset and becomes unavailable permanently on integer exhaustion,
  never wrapping or reusing an identity. Consumers compare only present IDs in
  a known common heap lifetime; restart/upgrade is a separate discontinuity.
  Timestamps remain wall-clock observations, not identity. Report construction
  adds fixed limits and coverage metadata, not configuration.

ICYDB-021 remains the existing typed `.explain()` outcome (slice 1), gated by
shared preparation and bounded diagnostics. Its acceptance includes SQL-free
typed inspection and no row execution; it is not closed by the accounting work.

Metrics selection now happens before copying/sorting at the existing accumulator
owner. The facade and generated endpoint retain their no-argument calls; the
current report adds coverage and reset identity, and CLI output labels both.
The application reports and sibling sources remain read-only. Consumers must
adopt the current Candid shape and check coverage before treating a sample as
complete; no compatibility decoder or application changes are included.

ICYDB-025 qualification: 19 focused tests pass (17 native actor tests, generated
driver contract and core exact-backlog/convergence coverage), alongside focused
model/actor lint, formatting and whitespace checks on Rust 1.98.1. The native
helper is now `__drive_native_database_for_tests`; its production-driver body
and request/admission rules are unchanged. A new fixture commits 129 individual
write messages, drives convergence every sixteen messages and verifies all rows
after the final drain. Generated docs and startup guidance explain that cadence
is test-specific and native tests do not execute IC timers.

This follow-up touches six files, about +45 net Rust lines (mostly tests) plus
documentation. There is one driver, no added runtime mode, timer, persistence
state or endpoint. Production execution is unchanged by this native-only rename;
raw-Wasm/function-count and cycle deltas were not measured. Full suites and IC
lifecycle testing were not run. No application file, Cargo version, network
lifecycle, commit or push was changed. ICYDB-021 remains open and has no new
public terminal in this handoff.

ICYDB-023 complexity: six files in this landing slice, approximately +163 net
Rust lines (mostly tests/API documentation) plus release/status notes. The
single report flow has a small added bounds/identity contract, not a second
execution route. Source rows copied/sorted fall from all accumulated entities
to at most 64; path copying is capped at 64 KiB. Raw-Wasm bytes, function counts
and IC cycle deltas are unmeasured; no numeric speed or size improvement is
claimed. Full suites and rebuilt canister artifact/lifecycle checks remain
user-owned validation. No application, Cargo version or network lifecycle changes.

ICYDB-023 qualification: six core metrics tests and five CLI metrics tests pass,
including UTF-8 byte accumulation and current Candid round-trip coverage.
Focused core/CLI lint, formatting and whitespace checks pass on Rust 1.98.1.

## Lifecycle gate follow-up (0.257.4)

The lifecycle test now completes its ordering, recovery, row-preservation and
stable-extent assertions and prints all four samples before enforcing the
reviewed instruction ceiling. This avoids hiding the converged-upgrade phase
behind the first cost failure. No production code or canister artifact changed.

The focused PocketIC 16 run passes those semantic assertions and measures
3,544,694 instructions at init, 9,850,276 at empty post-upgrade, 12,168,332 at
populated post-upgrade and 12,579,497 at converged post-upgrade. Stable extent
remains 511,770,624 bytes across the checked upgrades. Both populated phases
exceeded the previous 11,250,000 ceiling.

Source inspection confirms the 0.13 runtime reopens existing memory only after
validating its fixed 34,848-byte manager metadata (2,080-byte header plus
32,768 bucket entries). This is required read-only validation, not a result-row
scan. Its separate instruction contribution has not been measured, so it is
not claimed to account for the entire remaining increase. No small, proven
IcyDB optimization was identified in this inspection.

The user explicitly approved setting the fixture ceiling to 12,750,000,
approximately 1.36% above the largest measured sample, accepting the current
cost without changing any production resource limit. That ceiling is now
applied; this accepts the measured increase, not an optimization or complete
attribution of it. All three lifecycle tests now pass, including rollback/retry
and endpoint-surface checks, with the same four instruction samples. Focused
lint, formatting and whitespace checks pass. The known release blockers are
cleared; the user-owned full release gate is still required before publication.
The test-only change adds roughly
20 Rust lines; execution shape and deployed Wasm are unchanged. The temporary
PocketIC server was stopped after measurement; full suites were not run.

## Dependency qualification: ic-memory 0.13.2 (0.257.4)

The published `ic-memory 0.13.2` dependency is selected in the workspace and
lockfile; no other dependency changed. It removes the private backing adapter
in favor of upstream `Rc<M>` support and forwards runtime raw reads to upstream
virtual memory. IcyDB adds no unsafe code, dependency-cache edits, local override,
new execution path. The separate, user-approved lifecycle test-ceiling review
is documented above; production resource limits are unchanged.

Matched Rust 1.98.1 / PocketIC 16 fixture results against the preceding 0.13.1
measurements (same Cargo debug profile and maintained feature sets), before
the separately approved lifecycle ceiling adjustment above:

| Instruction gate | 0.13.1 | 0.13.2 | Delta | Existing ceiling | Result |
| --- | ---: | ---: | ---: | ---: | --- |
| Populated post-upgrade participant | 12,235,781 | 12,168,332 | -0.55% | 11,250,000 | Fail |
| Post-upgrade pinned continuation | 76,262,610 | 73,546,764 | -3.56% | 75,000,000 | Pass |
| Maximum-probe setup convergence callback | 33,132,232,202 | 27,831,002,367 | -16.00% | 30,000,000,000 | Pass |

Raw final Wasm bytes decrease for all three rebuilt artifacts:

| Artifact / feature set | Before | After | Delta |
| --- | ---: | ---: | ---: |
| SQL perf / LocalTest | 8,416,934 | 8,412,587 | -4,347 |
| Lifecycle / LocalTest | 4,058,501 | 4,057,390 | -1,111 |
| Lifecycle / Production | 3,865,623 | 3,864,372 | -1,251 |

The measured final-artifact SHA-256 values after this update are respectively
`9be9024eacf177a08fc737b47a721d9553ebaa78e3f0beecda2ede878841a7c9`,
`fc1cb92d4cc1ecb09d6eb245e20bdae027abb5fa7f41bb477c9e4148ed5323dd`, and
`b0a3d8c69d4ad284c11bdb57bd2c23462e67a605ba946ee55152a21337992ea5`.
Cycle deltas were not measured separately. Production here names the feature
set, not a release-optimized Cargo profile.

All seven recovery-closeout tests pass. Lifecycle initially had two passes and
one instruction-ceiling failure before its converged-upgrade phase; the later
follow-up above covers that phase too. Full `make clippy`, formatting and repository
invariants pass. The full release suite was not run and remains user-owned.
The upstream forwarding fix clears both recovery failures. The remaining
lifecycle test ceiling was subsequently reviewed and approved by the user;
its focused requalification is tracked above. Full release validation is
still required before publication.

The dependency update changes no production IcyDB Rust. The six-file current
handoff is approximately +90 net lines, mainly documentation, and includes the
prior test diagnostics and audit notes, dependency selection and release notes;
IcyDB's execution shape is unchanged and the
dependency's backing path is simpler. Temporary PocketIC servers were started
for the focused targets and cleaned up; existing networks were left alone.
No IcyDB package version, commit, tag or push was changed.

## Previous release gate audit: ic-memory 0.13.1 (0.257.4)

Before the 0.13.2 update, the release gate was not ready. The reported lifecycle participant used
12,235,781 instructions against its 11,250,000 ceiling. A focused PocketIC 16
rerun reproduces both cardinality closeout failures: the post-upgrade pinned
continuation uses 76,262,610 instructions against 75,000,000 (pre-upgrade:
52,187,528), and the maximum-probe fixture's convergence window records a
33,132,232,202-instruction callback against 30,000,000,000. That window also
deletes the preceding 240-row/64-index fixture; the expensive callback cannot
be attributed to the single new probe row alone. Both callbacks in the window
complete successfully, without retryable or invariant failures. Assertion
diagnostics now retain the actual measurements and relevant prior counters.

Source inspection identifies avoidable work in the new `ic-memory 0.13.1`
wrappers: neither `RuntimeMemory` nor `SharedBacking` forwards `Memory::read_unsafe`.
The trait default zeroes the destination before calling `read`, unlike the
underlying virtual/stable-memory implementations. This is a concrete lost
optimization; its contribution had not yet been
measured with a matched corrected build. Fixing the wrappers belongs to
`ic-memory`; no dependency-cache edits, local compatibility wrappers or ceiling
increases were made. The later published 0.13.2 qualification above supersedes
this audit's open measurement and upstream-authorization questions.

The focused rerun required localhost permission for its temporary PocketIC
server. The sandbox attempt could not bind; its stalled test and the temporary
server were stopped after evidence collection. Existing local networks were
left alone. Full-suite validation remains user-owned.

## Latest handoff: detached canonical output admission

Logical text/JSON rendering now returns `Result<String, QueryError>`, with a
fixed 1-MiB UTF-8 ceiling checked before destination growth. Exhaustion remains
sticky if a formatter ignores `fmt::Error`; neither exhaustion nor a formatter
failure returns partial output. A distinct query-origin output rejection records
only limit/actual numeric facts, not a session budget lifecycle or payload.
The one JSON writer now uses fallible streaming for both logical and execution
reports; only logical canonical rendering gets this output ceiling. Access JSON
no longer materializes and copies child Strings, and all control characters
receive valid JSON escapes. Successful other text/JSON bytes retain their order.

This handoff does not bound diagnostic projection, recursive Debug work, its
internal allocations, ordinary planning or complete callbacks. R7 and remaining
R8 qualification precede typed `.explain()`; R1–R6 and recovery admission remain
required afterward. No new planner, optional accounting mode or persisted state.
Complexity: 19 files including tests/docs and diagnostic propagation,
approximately 316 net Rust lines. The output flow is simpler, while explicit
failure handling adds local state. Raw Wasm and IC cycle/instruction deltas are
unmeasured; full-suite execution remains user-owned. All 50 focused tests pass
(23 explain-related, 26 diagnostic-registry, one CLI diagnostic). Repository
lint, final core all-feature/all-target lint, SQL-free core compilation,
invariants, formatting and whitespace checks pass. Initial missing diagnostic
fact registration and lint failure were repaired; no outstanding check failure.

## Previous handoff: mixed-state row-image bounds

Qualified the image-acquisition operand at the shared image/reader owner.
Derived separate bounds for canonical before-image payload, preparation
copies/reader construction and equality visits, and exercised all 32 old/final
combinations of insert, growing/shrinking update, delete and unchanged rows.
This is construction-proof coverage for R5, not another production estimator,
recovery mode or enabled limit. Retained backing, witness work and the complete
callback remain separate obligations of the combined admission implementation.

Result: acquisition needs the sum of per-row maximum lengths, not the maximum
of whole-batch endpoint lengths. Shared identical-image preparation already
bounds raw copying and reader construction counts by the original transitions
when final bytes are canonical under the selected authority. The source also
distinguishes normalized write before-images from physical predecessors; their
equivalence must not be assumed in admission. See the [bound](0.257-recovery-admission.md).

Validation: five focused image tests, core all-feature/all-target lint,
formatting and whitespace checks pass. Six files; 76 added Rust test lines plus
documentation. Production complexity is unchanged; raw Wasm and IC costs are
unmeasured. Full suites remain user-owned. Next: bind the remaining physical-
image/schema and witness/backing bounds into the combined R5 admission work,
not an additional clone-cleanup prerequisite. Typed `.explain()` remains pending.

## Previous handoff: explicit accepted-selection handoff

Need: write-time recovery qualification must use the logical predecessor's
accepted selection even when canonical storage lags. Moved live/canonical
selection to the existing runtime-entity caller and made the one context
constructor require an accepted selection. Entity/path/store identity derives
from that selection; the recorded operation fingerprint remains separate for
existing historical replay semantics. No generated-model reconstruction,
additional selector mode, compatibility wrapper or second constructor flow.

Canonical owners: runtime catalog chooses current/canonical authority; commit
preparation consumes the selected authority and its existing mechanical mode.
State-space delta: no new mode or retained state, only an explicit required
authority argument. Actual write/replay/Verify choices and operation-local
context cache lifetimes remain unchanged. Coverage checks live selection with
replay semantics before schema folding, wrong-entity rejection and mismatched
recorded-fingerprint rejection.
This enables the later qualification pass; it does not enable a recovery budget.

Validation: all 22 recovery-construction tests and five runtime-catalog tests
pass on Rust 1.98.1, as do focused core all-feature/all-target lint, repository
invariants, formatting and whitespace checks. Complexity: nine files including tests/docs,
approximately 18 fewer production Rust lines and 78 additional test lines.
The constructor is simpler, with no additional mode or retained state. Raw
Wasm and cycles/instructions are unmeasured; full suites remain user-owned.
Next: conservative construction bounds and remaining lifecycle qualification
for the combined R5 required-budget/encoder/admission outcome. Typed `.explain()`
remains pending; this handoff does not close R5.

## Previous handoff: pending-candidate authority qualification

Three new tests qualify candidate generation selection across live/canonical
schema divergence, folding before an interrupted delete and stage loss, and
exact live-schema checkpoint restoration before direct replay. They use the
existing accepted-catalog and write/recovery owners; no production behavior,
mode, limit, persisted state or generated-model fallback is added.
The direct fixture binds its deletion with an identity insert on another entity
in the same store. A heap-only delete is not a durable row payload; the initial
fixture's contrary expectation was corrected, not implemented as new behavior.

The source already validates candidate generation against activation epoch at
accepted-schema integrity admission. Ordinary writes select live authority;
journal replay/Fold selects canonical authority; direct replay restores its
accepted checkpoint first. These are different source lifetimes, not permission
to retain an accepted context across a callback or catalog transition.

Finding for the future admission implementation: selecting `RecoveryReplay`
today also selects canonical schema inside commit-context construction. When
an activation is live but not folded, that is not the future Fold predecessor.
Pre-publication qualification must supply the write's accepted logical
predecessor separately from recovery semantics. The smallest next boundary is
an explicit accepted-selection handoff into the existing context constructor,
not another decoder, schema mode or simulation route. The actual recovery
callers must retain their current canonical/checkpoint selection.

This completes pending-candidate selection/checkpoint qualification, not all
candidate lifecycle or recovery construction bounds. Populated staged
generations, promotion/abort interactions, other marker producers and the
per-resource construction inequality still need coverage in the combined R5
admission work. See the [admission contract](0.257-recovery-admission.md).

Complexity: seven files, approximately 281 new Rust test lines and documentation;
production execution shape is neutral. Raw Wasm and cycles/instructions are
unmeasured. Full suites remain user-owned. Typed `.explain()` is still pending.
Validation: all 21 recovery-construction tests pass, including the three new
authority cases. Focused core all-feature/all-target lint, formatting and
whitespace checks pass. The mistaken heap-only replay fixture was corrected;
no runtime fix or new durability promise was needed.

## Previous handoff: recovery image acquisition ownership

Need: the recovery batch reader retained a second copy of every final row and
key, while transition acquisition copied an already-owned canonical before-row.
These allocations are part of R5's identified upstream image-retention bound.
Implemented borrowed final-row/key views tied to the existing operation slice
and moved canonical row buffers into operations. The reader still returns owned
witness rows on demand and must preserve explicit deletion overrides, canonical
fallback and duplicate-key rejection.

Canonical owners: recovery transition acquisition and its canonical commit
reader. State-space delta: none; replace owned backing with lifetime-bound
references, with no new reader route, configuration, limit or persisted state.
This narrows retained image ownership, not the full callback admission proof.
The [admission contract](0.257-recovery-admission.md) inventories the remaining
allocations and their operands: operation buffers, reference-map/vector backing,
per-lookup witness copies and prepared results. A journal byte bound limits
recorded after-images, not separately loaded canonical predecessors. It is not
used to enable a new recovery limit.

Four new reader tests cover borrowed keys/images, independent owned results,
replacement/deletion overrides, missing canonical authority, duplicate keys
and exact/oversized row limits. Existing accepted-catalog routing shares the
isolated fixture; no test changes canonical authority rules.
All 23 focused tests pass, including the 18 direct/journaled recovery regressions.
Repository lint, invariants, formatting and whitespace checks pass on Rust
1.98.1. The initial test fixture expectation and pointer-comparison lint warning
were corrected; no validation failure remains for this focused handoff.

Complexity: eight files including tests and five documentation files, about
130 net Rust lines, mostly direct regression coverage. Ownership is simpler:
no second final-row snapshot or temporary before-row copy. Cycle/instruction
and raw-Wasm deltas are unmeasured. Full suites remain user-owned.

Next within R5: candidate/schema-checkpoint authority coverage and conservative
bounds for the inventoried acquisition/equality work, then the combined required
budget/encoder/admission change. This is not a complete callback bound or the
typed `.explain()` terminal.

## Previous handoff: shared identical commit images

Need: already-applied inserts give shared commit preparation identical before
and after bytes, which previously caused two raw-row copies and two validated
readers. Implemented change: represent byte-identical images once at that
owner and lend the same validated reader to both semantic sides. Do not omit
the before side, skip index/relation planning or weaken final verification.
The index planner only needs shared reader borrows; remove its unnecessary
exclusive-borrow requirement as direct propagation of this ownership change.

Canonical owner: commit preparation. State-space delta: one private,
content-derived shared-versus-distinct image representation, reused for raw
and decoded rows; no caller option, persisted state, replay mode or budget
profile. Failed conversion returns no partial pair. This targets the named R5
duplicate row construction obligation, not unrelated clone cleanup. Earlier
recovery image acquisition and other construction bounds remain open.

The raw and decoded image pair now preserves sharing through one fallible map.
Readers borrow the same accepted row contract instead of cloning it. Both sides
still reach candidate/forward/relation planning and after-image finalization;
distinct rows are validated independently. The existing row-size owner rejects
oversized sources before the equality walk or destination allocation.

Four new owner tests cover shared construction, distinct/absent sides, invalid
images, key mismatch and oversized inputs. Focused recovery and after-image
regressions retain final-row, unique, relation and verification checks.
All 27 focused tests pass, along with the repository lint gate, invariants,
formatting and whitespace checks on Rust 1.98.1. Lint identified stale mutable
test-helper borrows; those callers are updated and their focused gate passes.

Complexity: ten files including direct propagation, tests and five documentation
files; approximately -2 net production Rust lines and +185 new test lines.
The ownership flow is simpler, with no second decoder or independent execution
mode. Exact byte comparison adds a size-bounded linear walk for two-image
transitions; its work still needs coverage in the eventual recovery admission
contract. Raw Wasm and IC cycles/instructions are unmeasured; no general speed
improvement is claimed. Full suites remain user-owned.

Next within R5: establish bounds for upstream recovery image acquisition and
the remaining reader/authority differences before enabling combined budget,
encoder and recovery admission. This handoff does not expose `.explain()`.

## Previous handoff: recovery reader-state qualification

Six new unique-handoff regressions cover direct predecessor/partial/applied
states, online and startup journal folding, an unfolded predecessor, and lost
volatile recovery stages. They retain final-row, uniqueness, readiness and
identity-allocation assertions. A removed test-only probe confirmed zero unique
witness rebuilds during ordinary handoff preflight versus one during all-before
direct Replay or journal Fold. Those are construction observations, not cycles.

The [admission contract](0.257-recovery-admission.md) now records the evidence and
an additional bound obligation: already-applied direct inserts carry a before-
image as well as an after-image into row copying/reader construction, whereas
all-before qualification has no old row. Successful dry runs alone therefore
do not establish the complete construction inequality. Public same-store batch
admission narrows public partitioning; lower-level marker producers still need
their own coverage. Candidate/schema authority and quantitative bounds remain open.

Production code, ceilings, recovery stages and persisted formats are unchanged;
the temporary probe is fully removed. This is qualification within R5, not its
implementation or a complete preparation gate. Next is the explicit already-
applied row-image bound and remaining authority proof, followed by the combined
encoder/admission implementation. Do not start an unrelated cleanup or another
policy-approval loop. IC cycles/instructions and raw-Wasm deltas are unmeasured.

Validation: 19 focused tests (18 replay/construction tests plus the public
cross-store rejection regression), all-feature core/all-target lint, formatting,
whitespace and contract link checks pass on Rust 1.98.1. Full suites remain
user-owned. Scope: seven files in this handoff, approximately 160 added Rust test
lines plus documentation; no production Rust change or new runtime state. Existing
pending grouped-preparation work is preserved. No local network lifecycle action.

## Previous handoff: recovery construction admission contract

The user approved developing the admission policy before implementation. The
[contract](0.257-recovery-admission.md) proposes recovery-shaped transition and
final-after-image preparation before commit publication, through the maintained
constructor and existing budget policy. Actual qualification work also counts
against the write and any enclosing mutation job. No second encoder, persisted
receipt, callback stage or larger ceiling is proposed.

Source review distinguishes whole-marker direct Replay, one-batch Fold and
whole-marker Verify lifetimes. It identifies a concrete unique-value release
case where staged-write indexes can avoid a witness reconstruction that the
canonical replay reader performs. Ordinary after-image charging alone is not a
replay-cost proof. That case is source-derived, not a measured budget failure.

This handoff changes only design/status/release documentation. Reader/state
coverage, deterministic bounds and matched IC cost qualification remain gates
for the combined R5 implementation. Runtime, applications, Cargo versions and
existing pending code are unchanged; no tests or performance probes were run.
Typed `.explain()` remains pending. The next work is the contract's concrete
reader/state proof, not another policy-approval loop or standalone cleanup.

## Previous handoff: owned grouped aggregate attachments

This bounded R4 outcome stays within slice 11. Grouped attachment resolution
now consumes the unpublished aggregate-spec list and reuses its backing and
authored identity/input/filter buffers. Each spec visit charges the existing
request's `PredicateExpressionSteps` before work begins; its scalar compiler
continues charging compiled payloads. No second clone/collection route remains
at this attachment-resolution boundary.

Input compilation, filter compilation and accepted target-slot resolution must
all succeed before the spec installs any attachments. The complete list only
returns after every spec succeeds. The public-read and diagnostic lanes share
the same construction owner and cumulative request authority. No mode, budget
profile, numerical ceiling, cache lifetime, format or unsafe code is added.

This closes the root attachment-visit and duplicate-copy gap, not full R4.
Earlier projection/HAVING spec discovery, expression/identity comparisons,
schema lookup internals and other semantic validation remain open. The larger
R5 scalar-codec accounting/error-propagation contract remains a combined,
unfinished outcome; this handoff does not split or claim it complete.
Typed `.explain()` and diagnostic bounds remain pending.

Direct coverage checks list/syntax-buffer reuse, exact compiled-payload limits,
cumulative visits in both lanes, empty lists and failure before installation.
All 67 selected tests pass, including grouped/HAVING results, aggregate typing,
current bindings, cold/warm caches and failure-before-publication coverage.
All-feature core/all-target lint, SQL-disabled core compilation, repository
invariants and formatting/whitespace checks pass on Rust 1.98.1.
Scope: eight files including tests and docs, approximately 160 net added Rust
lines (almost entirely tests). Production ownership is simpler; no new helper
layer or execution route is introduced.
Cycle/instruction and raw-Wasm deltas are unmeasured; native construction
counter observations are not performance measurements. Full suites remain
user-owned; Cargo versions, applications and local networks are unchanged.

## Previous handoff: completed index-program reuse (0.257.4)

Execution preparation now retains the policy that produced its optional index
prefilter, including a completed unsupported result. Runtime no longer mistakes
that result for missing preparation and compiles it again. A supported program
is borrowed; a genuinely unprepared policy compiles through the same helper
used by eager preparation and remains owned by that execution attempt.

All maintained preparation constructors request at most one policy. One
optional policy/result record replaces the two independent program fields and
two requested-policy fields. The runtime's duplicate compiler dispatch and
custom none/borrowed/owned wrapper are removed in favor of the shared owner and
standard `Cow`. Strict aggregate/covering admission still consults only a
prepared strict result, and cannot reuse a conservative result. Compiler
eligibility, subset semantics, row predicates, query identity and accepted
authority checks are unchanged. There is no new compiler policy, cache lifetime,
public endpoint, budget profile or encoding format.

The five direct tests cover reused/cloned unsupported results without another
compiler invocation, borrowing supported programs, missing/different policies,
constructor intent and retained literal capacity. Actual scalar encoding and
fallible optional-predicate error propagation remain open: future exhaustion
must propagate as an error, never become a cached unsupported result. This
closes the duplicate optional-compilation route within R5, not R5 or `.explain()`.

Scope: ten files including tests, caller propagation and notes; approximately
55 fewer production Rust lines and 150 added test lines (+95 net Rust lines).
Implementation shape is simpler: one compilation-dispatch owner and one
prepared policy/result instead of two independently optional program slots.
Cycle/instruction and raw-Wasm deltas are unmeasured. Concurrent `ic-memory`
and allocation-report work prevents treating the prior frozen actor as a matched
before/after baseline for this change; no speedup or size improvement is claimed.

Validation: all ten selected tests, all-feature core/all-target lint and
formatting/whitespace checks pass on Rust 1.98.1. The selections cover the five
direct tests plus warm aggregate/binding caches, shared-cache retention,
covering projection and public compound-index read admission. The first offline
attempt could not obtain the concurrently selected
`ic-memory 0.13.1`; sandbox DNS also prevented registry access. The locked
dependency was downloaded with approved network access. A remaining covering-
projection method reference was propagated after compilation identified it.
The SQL-only test configuration reports existing dead-code warnings in
unrelated maintenance/test helpers; all-feature lint is warning-free.
Full suites remain user-owned. No network lifecycle action,
application edit, workspace version edit, commit or push is included.

## Previous handoff: indexed scalar upgrade qualification (0.257.4)

R5 now has a manual same-Wasm upgrade matrix for twelve accepted signed/
unsigned, decimal-width and batch-size shapes from the write baseline. Each
shape runs once with journal debt and once after that debt is drained. Ordinary
reads must remain gated until startup completes. Exact projected row values
must survive both upgrades; a selected secondary-index range must return the
complete row set. Structured SQL explain confirms the selected index so a
primary scan cannot accidentally satisfy that index check.

The first IC run correctly rejected a fixture in which the lone journal batch
drained before upgrade. The maintained fixed 64-batch small-row backlog now
precedes the target write; one watchdog delivery frees admission capacity and
the target is appended last. Nonempty startup must still be demonstrated by
watchdog work, not assumed from the write. Recovery totals include the remaining
small-row backlog, not just the indexed scalar batch.

The production startup watchdog supplies work sample counts, total instructions
and maximum callback instructions. The cycle-balance interval spans convergence
after upgrade, including timer scheduling, application startup polling and
observer-query charges; it is not an encoder-only or single-callback cycle cost.
Upgrade installation, initial writes and post-recovery row/index checks are
outside that interval. No native timing is measured.
Watchdog instruction totals cover the post-upgrade watchdog history; they are
not an instruction-to-cycle conversion for the observer-delimited cycle window.
The fixed backlog also warms write-side setup, so the reported write samples
are not matched cold-write comparisons with the preceding matrix.

The measured actor is the exact frozen write-baseline artifact (raw 4,353,617
bytes, SHA-256 `7b9c86f8fa392e37c79a8e439ed954f698512272d70e6c718821490971526f5d`).
The worktree's concurrent `ic-memory` 0.12.3 to 0.13.0 dependency update is not
part of this slice and is not present in that artifact. These results cannot
qualify that dependency update or imply a current-source Wasm/performance delta.

Qualification boundary: normal successful writes leave journal debt but no
pending commit marker. This exercises complete-batch startup folding and the
ordinary final readiness checks, not marker-owned row/index effect verification.
No new endpoint, engine charge, numerical limit, encoding route or recovery
policy is introduced. Marker-owned verification, wider fanout and shared scalar
accounting remain open; R5 and typed `.explain()` are not complete.

Scope: seven files, +182 Rust test lines and documentation, including a
one-line needless-borrow lint repair in the concurrently migrated test helper.
Production implementation complexity is unchanged; the test reuses existing
fixture endpoints, startup-driver helpers and the previous actor binary.

The final PocketIC 16.0.0 matrix passes all 24 upgrade cases (12 shapes, two
upgrades each). Every debt-bearing case records 53 watchdog work callbacks;
the largest callback uses 475,048,711 instructions, below the existing 30B
callback envelope for these tested shapes. This is observed qualification,
not a new admission ceiling or a proof for every accepted batch/fanout.

| Family | Decimal digits | Rows | Maximum callback instructions | Total watchdog instructions | Convergence-window cycles |
| --- | ---: | ---: | ---: | ---: | ---: |
| NatBig | 20 | 1 | 87,617,818 | 3,147,813,097 | 4,985,201,793 |
| NatBig | 20 | 32 | 106,764,356 | 3,196,411,229 | 5,033,800,159 |
| NatBig | 300 | 1 | 87,617,818 | 3,148,035,909 | 4,985,424,447 |
| NatBig | 300 | 32 | 117,519,064 | 3,208,410,270 | 5,045,798,610 |
| NatBig | 4,093 | 1 | 87,617,818 | 3,154,664,394 | 4,992,053,078 |
| NatBig | 4,093 | 32 | 365,900,207 | 3,450,225,524 | 5,309,071,445 |
| Negative IntBig | 20 | 1 | 87,617,818 | 3,147,829,598 | 4,985,218,559 |
| Negative IntBig | 20 | 32 | 107,170,675 | 3,196,780,446 | 5,034,489,722 |
| Negative IntBig | 300 | 1 | 87,617,818 | 3,148,388,929 | 4,985,777,206 |
| Negative IntBig | 300 | 32 | 125,610,005 | 3,216,473,992 | 5,053,862,390 |
| Negative IntBig | 4,092 | 1 | 87,617,818 | 3,158,070,951 | 4,995,459,489 |
| Negative IntBig | 4,092 | 32 | 475,048,711 | 3,559,374,028 | 5,418,219,949 |

All twelve second, debt-free upgrades record two work callbacks, 66,558,778
total watchdog instructions, 36,030,325 maximum callback instructions and
118,723,202 convergence-window cycles. The generated driver restores readiness
before its optional-work quiescence check; the initial one-callback expectation
was corrected after inspecting that existing owner. Both final row values and
the selected secondary-index ranges match in every case. All callbacks succeed,
with no retryable/invariant failures and no watchdog left armed.

Focused integration lint, formatting and whitespace checks pass on Rust 1.98.1.
An initial module-path error was corrected. The required repository lint run
then encountered three memory-type errors during the concurrent dependency
migration; those call sites were subsequently changed by that work. One remaining
needless-borrow warning in its test helper was repaired without changing the
memory contract. Repository lint requalification now passes, including its
SQL/no-default-feature and all-feature query/guard actor lanes. Full suites
remain user-owned. Temporary PocketIC servers were stopped by the governed
wrapper after both failed probes
and the final passing matrix.

## Previous handoff: indexed scalar cost qualification (0.257.4)

R5 measurement work found that the maintained big-literal write benchmark has
no secondary indexes and therefore does not exercise scalar index encoding.
The first IC run also exposed that fixture's default 256-byte field bound, which
rejects wide values before encoding. New fixed test-admin hooks add signed/
unsigned indexes to a separate empty audit entity with explicit 2,048-byte field
bounds through accepted DDL; the original unindexed benchmark stays unchanged.
The manual matrix tests 20 combinations of sign, decimal width and batch size,
including the maintained 4-KiB component edge (4,092 signed digits or 4,093
unsigned digits). Rejected 32-row shapes place the oversized value last and
check that no prefix rows were published.

Engine instruction intervals start after typed fixture construction and include
the request execution scope. Both success and rejection return an instruction
sample. Whole-call cycle deltas also include input fixture construction and
transport; they are not encoder-only costs. DDL/setup and post-write inspection
are outside the measured call. No native timing metric is used.

Scope: eight files, approximately +190 Rust lines in audit-only hooks and manual
integration coverage, plus notes. Production runtime complexity is unchanged;
there is one additional audit entity, but no new production endpoint, encoding
route, engine limit or budget policy. This is write-cost qualification, not
complete replay/final-verification headroom or shared encoder accounting.
R5 and typed `.explain()` remain unfinished.

Validation passes on Rust 1.98.1: the complete 20-case PocketIC 16.0.0 matrix,
all-feature audit/integration lint, the required repository lint lanes after the
checked-conversion repair, formatting and whitespace checks. Initial Candid
visibility, build-input drift and default-width fixture issues were corrected;
the final matrix passes on a frozen source tree. Full suites remain user-owned.
Temporary PocketIC servers were stopped by the governed wrapper.
The additional SQL-only (no test-admin hooks) actor check also compiles, but
reports pre-existing unused imports in its root module (`Error`, `FilterExpr`,
`ProgressJobInventory` and `PerfAuditCardinalityTie`). Those imports are unchanged
by this slice; their feature-gating cleanup is a follow-up, not a qualification
failure for the all-feature measurement actor.

Measured artifact: `wasm-release`, SQL enabled, Candid enabled, local-test profile;
raw Wasm **4,353,617 bytes**, SHA-256
`7b9c86f8fa392e37c79a8e439ed954f698512272d70e6c718821490971526f5d`.
These are absolute baseline costs, not a matched before/after improvement or a
production Wasm delta. No new limit is inferred from this small fixture matrix.

| Family | Decimal digits | Outcome | 1-row instructions | 32-row instructions | 1-row cycles | 32-row cycles |
| --- | ---: | --- | ---: | ---: | ---: | ---: |
| NatBig | 20 | Accept | 49,498,577 | 71,957,446 | 57,500,207 | 80,302,135 |
| NatBig | 300 | Accept | 49,951,085 | 92,632,299 | 58,000,047 | 102,491,195 |
| NatBig | 4,092 | Accept | 59,355,035 | 474,710,368 | 70,520,517 | 584,457,879 |
| NatBig | 4,093 | Accept | 59,358,512 | 474,144,906 | 70,524,571 | 583,932,539 |
| NatBig | 4,094 | Reject | 55,593,370 | 65,952,065 | 66,773,067 | 77,473,936 |
| Negative IntBig | 20 | Accept | 49,556,219 | 73,724,905 | 57,559,797 | 82,095,216 |
| Negative IntBig | 300 | Accept | 50,725,190 | 117,096,456 | 58,777,616 | 127,011,425 |
| Negative IntBig | 4,092 | Accept | 69,684,561 | 800,451,481 | 80,854,712 | 910,373,401 |
| Negative IntBig | 4,093 | Reject | 64,830,549 | 76,892,325 | 76,010,759 | 88,442,231 |
| Negative IntBig | 4,094 | Reject | 64,835,433 | 76,900,059 | 76,020,178 | 88,454,052 |

Rejected 32-row cases contain 31 small (20-digit) values and one final oversized
value; they are not 32 oversized conversions. Every rejected case preserves an
empty row set and returns an index-origin error. Successful batches retain the
expected complete row set. Marker replay, final verification, wider fanout and
whole-operation construction accounting remain unqualified by this matrix.
No application, engine budget, Cargo version, commit or push change is included.

## Previous handoff: shared verification setup (0.257.4)

The approved context-reuse slice is implemented. Write preparation, replay and
final marker verification share one operation-local context cache. Verification
now prepares accepted setup once per entity/schema pair instead of once per
surviving row. Current/canonical authority selection and every row/effect check
remain at their existing owners; no preparation mode or recovery stage changes.
Failed resolution is not cached. The verifier drops all contexts on return, so
retry and lost volatile recovery state cannot reuse setup from an earlier call.

The memory tradeoff is explicit: verification retains all distinct accepted
contexts and the cache vector's capacity until that call finishes, instead of
dropping each context after one row. No row images/results are retained in the
cache. Write/replay already retained contexts; their duplicate key tuples and
lookup loops are removed. Actual byte accounting and callback instruction
qualification remain part of R5, not a claim made by this reuse change.

Scope: twelve files including shared preparation, callers, direct/recovery tests,
status/checklist and both active release notes, approximately +150 net Rust lines
(mostly tests). Runtime ownership is simpler: one reuse owner and no new profile,
persisted state or cross-call cache. Raw-Wasm and IC cycle/instruction deltas are
unmeasured. Validation passes on Rust 1.98.1: all 69 focused identity/recovery
tests (including the 12 context/replay cases), the required lint lanes and focused
all-feature core lint, SQL-free compilation, formatting and whitespace checks.
The initial lint style warning was fixed and both lint gates rerun successfully.
Full suites remain user-owned. No application, Cargo version, commit, push or
network lifecycle changes were made.
Shared scalar accounting and typed `.explain()` remain unfinished.

## Previous handoff: replay setup qualification (0.257.4)

The R5 preflight confirmed that final marker verification rebuilds accepted
preparation context once per surviving row, while write/replay batch preparation
reuses it per entity/schema fingerprint. The existing mixed heap recovery test
passed with a temporary probe: one context for its mixed write, one for replay,
two for verification of its two surviving rows. The probe was removed. This is
call-count evidence, not IC instruction/cycle measurement or an observed budget
failure; equal scalar-key work is not a complete callback headroom proof.

The [remaining-work checklist](0.257-explain-remaining.md#r5-qualification-final-verification-repeats-accepted-setup)
records a proposed, separately reviewable context-reuse follow-up pending split
approval, including authority preservation and retained-memory accounting. It
does not silently make another cleanup a mandatory terminal prerequisite; keeping
the existing setup and proving headroom is the alternative. No scalar budget,
new limit or runtime change is retained from this audit; `.explain()` remains
unfinished.

Validation: the focused recovery scenario passed on Rust 1.98.1 with the probe;
formatting and whitespace checks pass after removing it. Two documentation files
change, approximately 70 added lines; runtime complexity is unchanged. Wasm and
IC cycle/instruction deltas are unmeasured. Full suites, canister measurements
and network lifecycle actions were not run. Existing release notes remain
unchanged because this handoff adds qualification evidence, not shipped behavior.

## Previous handoff: shared after-image admission (0.257.4)

R5 implementation checkpoint: the shared field-path and expression index
planners now derive the after-image key before the unchanged-input shortcut.
This is the ordering proposed by the replay-admission contract, not another
encoder or estimator. If further delta planning is needed, it consumes that
same key rather than encoding it again.

An unrelated-field update still avoids old-key encoding, physical index reads,
unique probes and index deltas when indexed inputs are unchanged. Partial-index
nonmembers still omit their component. The deliberate tradeoff is that these
updates now derive their after-image key, matching the key final verification
must reconstruct. A required derivation failure cannot be hidden by the shortcut.
New writes with an invalid unchanged after-image may therefore reject earlier.

Four direct tests cover field/expression admission and typed rejection, retained
zero-read/zero-delta behavior, changed expression inputs with equal derived keys,
and omitted partial-index membership. Existing identity/replay tests qualify
the shared write, direct replay, journal-fold and final-verification consumers.

This closes the known skipped-after-image construction mismatch, not the whole
combined encoder/admission outcome. Required scalar conversion/output charging,
operation-wide budget ownership, fallible predicate compilation and IC allowance
qualification remain open. No new numerical limit, mode, format or public API
is introduced; typed `.explain()` remains unfinished.

Scope: seven files: `index/plan/mod.rs`, the accepted-domain test module and its
new `after_image.rs` child, this status, the remaining-work checklist and both
active changelog entries. Production changes move existing derivation with four
net comment lines; test coverage adds approximately 230 lines. Implementation
shape stays neutral: one encoder, the existing shortcut and no new runtime flow.
Raw-Wasm and IC cycle/instruction deltas are unmeasured. No speedup is claimed.

Validation passes on Rust 1.98.1: 17 accepted-index tests and 67 identity/replay
tests, all required `make clippy` lanes plus focused all-feature core lint,
SQL-free compilation, formatting and whitespace checks. The initial parallel
identity/replay process was killed by the OS without a reported assertion
failure; the same 67-test selection passed serially. The cause of that kill is
unconfirmed. Full suites remain user-owned. No application, Cargo version,
network lifecycle, commit or push changes are included.

## Previous handoff: integrity resource-failure classification (0.257.4)

R5 caller qualification found that row and derived integrity inspection also
consume the shared scalar encoder. Their existing page/decode limits do not
provide scalar construction authority. The remaining-work checklist now records
that propagation and the error-preservation seams in predicate compilation,
unique-key validation and schema staging.

One independently reviewable bug is repaired in this handoff: Deep inspection
classified every failed candidate page as `Uninspectable`, including the typed
resource failures already produced by relation projection. All four Deep
failure-to-terminal sites now share one typed classifier. Execution-budget and
indivisible-unit failures become the existing `ResourceLimited` outcome; actual
authority failures retain their previous class and compact diagnostic code.
The existing terminal publisher still discards the failed candidate, retains
the preceding checkpoint and publishes a replayable terminal receipt.

No new variant, budget, state, public signature or persisted format is needed.
This is error propagation required by R5, not a completed encoder/admission
landing. Shared encoder charging, after-image admission, integrity page authority
and IC instruction qualification remain unimplemented. Typed `.explain()` is
still unavailable. Do not open another cleanup prerequisite from this repair.

Changed files: three Rust files (`integrity/deep.rs`, `integrity/job.rs`, and
`integrity/progress_codec.rs`), this status, the remaining-work checklist and
both active changelog entries. Approximately +100 net Rust lines, predominantly
tests. Four duplicated failure mappings converge on one classifier; complexity
is neutral with no new execution route. Matched raw-Wasm and IC cycle/instruction
deltas are unmeasured; no performance claim is made.

Validation: all 33 focused integrity tests, all-feature/all-target core lint,
SQL-free compilation, formatting and whitespace checks pass on Rust 1.98.1.
The new codec fixture initially used the wrong error type and page-count
convention; both were repaired. The maintained page count includes the terminal
receipt even though the failed inspection checkpoint does not advance.
Full suites and IC lifecycle/performance measurements were not run. No application,
Cargo version, network lifecycle, commit or push changed.

## Previous handoff: separate recovery stages (0.257.4)

User-approved hard cut: replay, one-batch folding and final marker verification
must not accumulate in the same driver call. This is the next R5 landing,
before shared scalar encoder accounting and replay-safe write admission.

- Need: a combined callback cannot safely reset its budget between these owners;
  rejecting an already committed marker against an unqualified combined limit
  is not an acceptable admission policy.
- Simplest alternative: reuse the existing recovery-in-progress owner with a
  volatile, marker-bound Replay/Fold/Verify cursor. No new persisted format,
  scheduler, public option or parallel recovery route. A lost cursor restarts
  idempotent replay; successful folds retain their durable watermarks.
- State-space delta: the existing in-progress state records which of three
  stages is next, plus the current marker identity. Only a successful stage
  advances it. No ordinary request is admitted before final verification.
- Empty startup and ordinary online one-batch convergence retain their current
  paths. Nonempty startup yields between replay, folds and final verification;
  an empty fold stage may be skipped. Complete batches remain atomic.
- This landing establishes operation boundaries, not numerical construction
  limits. Scalar charging, pre-commit replay allowance and IC cost qualification
  remain required before declaring resource admission complete.

The implementation retains marker decoding/validation on each call, but no
longer republishes the marker or resets disposable overlays before every fold.
Replay after cursor loss skips batches already covered by durable watermarks;
verification still checks their marker-owned effects before clearing authority.
An index removed before verification rejects instead of being replayed away.

Direct coverage includes marker-identity binding, heap replay/verification
separation, lost cursors before verification and after a completed fold, late
malformed batch rejection, atomic large-batch folding, and mutation-progress
recovery. The obsolete single-call native timing probe was removed; its
post-recovery identity behavior is covered by maintained stage tests instead.
The IC qualification uses the retained-backlog upgrade scenario; the existing
debt-free rollback fixture still completes in one call. The residual test
envelope is now 64 batches + 6 driver calls, accounting for the two additional
stage boundaries, not a relaxed runtime limit.

Scope: thirteen files, approximately +220 net Rust lines including tests.
Control flow gains explicit volatile stage state; repeated replay/reset work is
removed. There is still one recovery engine and no persisted state or format
change. Matched raw-Wasm/cycle/instruction deltas are unmeasured; no speedup or
full construction-boundedness claim is made. The retained-backlog PocketIC
upgrade scenario passes: the post-upgrade observation interval reports 61
successful work calls and 3,328,806,476 total instructions, with a recorded
79,142,254-instruction maximum. Work delivered before that observation interval
is not counted in its delta. This is current-run qualification, not a matched
performance comparison or proof for all admitted encoder shapes.

Changed files for this handoff:

- Core runtime: `crates/icydb-core/src/db/commit/recovery.rs` and migration-only
  feature guards in `crates/icydb-core/src/db/data/store.rs` and
  `crates/icydb-core/src/db/schema/store.rs`.
- Tests: `crates/icydb-core/src/db/session/write.rs`, its
  `identity_pre_key_tests/replay_construction_tests.rs` child,
  `crates/icydb-core/src/db/startup/mod.rs`, `testing/integration/src/lib.rs`, and
  `testing/integration/tests/recovery_closeout.rs`.
- Contracts/notes: `docs/contracts/ATOMICITY.md`, root `CHANGELOG.md`,
  `docs/changelog/0.257.md`, this status document, and
  `0.257-explain-remaining.md` beside it.

Validation covers 124 focused core tests (commit, identity/batch replay, and
startup), plus the retained-backlog upgrade and debt-free rollback PocketIC
scenarios. SQL-free compilation, repository lint, focused all-feature lint,
formatting and whitespace checks pass. Initial single-call assertions,
feature-specific dead-code warnings and the expanded test's length warning
were repaired; its final targeted rerun passes. This stage-separation handoff
is complete; shared encoder accounting and replay-safe admission remain next.
Full repository suites remain user-owned. Temporary PocketIC
servers have stopped; no application, Cargo version, commit or push changed.

## Previous handoff: direct identity replay repair (0.257.4)

Admission qualification found an existing supported recovery failure, not a
budget exhaustion: heap-backed identity markers contain ordinary row records,
but direct replay sent them to a journal-only volatile-row writer. The focused
marker-persistence regression aborted the native test process before the fix.
The writer also emits deletes in identity-bound mixed mutation batches, which
the direct-replay family check previously rejected.

Direct replay and journal fold now share complete-batch row preparation. Direct
publication retains and applies those prepared row/index operations through the
ordinary applier; journal folding retains its canonical preflight and applier.
Schema/control records keep their existing record-level owner. Recovery-only
empty deletions are admitted as already reflected, while ordinary empty writes
still reject. This restores idempotence across partial publication without
weakening final marker verification or changing the recovery callback cadence.

Unused single-row replay preparation, snapshot-loading and journal-delete helpers
are removed with their callers. Existing overlay/cardinality tests use the
maintained row-removal operation. No new mode, budget profile, persisted state,
format version or endpoint is introduced.

This independently reviewable replay-safety repair is the current handoff under
R5. Scalar encoder charging and the combined admission allowance remain open;
do not treat this bug fix as completed accounting or typed `.explain()`.
Validation: all 34 focused recovery, journal, cardinality and mutation tests pass,
including four new heap identity regressions. All-feature/all-target core lint,
SQL-free compilation, formatting and whitespace checks pass. The initial abort
and subsequent already-applied-delete failure are resolved by the final tested
source.

Scope: fourteen files, approximately 266 fewer production Rust lines and 79 net
added test lines (about 187 fewer Rust lines overall), plus release/status notes.
The implementation is simpler: retained complete-batch preparation replaces
discarded per-row preparation and an invalid backend dispatch. Wasm bytes,
cycles and instruction deltas are unmeasured; no performance gain is claimed.
Full suites and IC lifecycle/cost qualification were not run. Applications,
Cargo versions and networks are unchanged; no commit or push was performed.

Changed files for this handoff (excluding earlier dirty-worktree changes):

- Runtime: `commit/recovery.rs`, `commit/prepare.rs`, `mod.rs`,
  `runtime_entity_catalog.rs`, `data/store.rs`, `schema/store.rs`,
  `schema/mod.rs`, and `schema/fingerprint.rs`, under `crates/icydb-core/src/db`.
- Tests: `data/store/tests.rs` and
  `session/write/identity_pre_key_tests/replay_construction_tests.rs`, under the
  same database source directory.
- Notes: root `CHANGELOG.md`, `docs/changelog/0.257.md`, this status document,
  and `0.257-explain-remaining.md` beside it.

## Previous handoff: one prepared-row fold path (0.257.4)

The combined admission/encoder work exposed redundant recovery construction.
The apparent candidate-schema row path is already excluded by JournalBatch
admission: schema publication and ordinary row mutations are separate families.
The current handoff removes that unreachable resolver and reconstruction flow,
not a supported journal shape or verification check.

Every ordinary row transition is prepared as part of the complete batch before
canonical publication. Online retirement consumes that same preparation and
no longer decodes an after-image for a fallback. Apply cannot reconstruct missing
row preparation. The extra row-preflight mode, single-row fold helpers and
candidate-only runtime resolver are deleted; startup and online convergence keep
their existing lifecycle and final marker verification.

This is a reviewable R5 prerequisite, not completion of the approved combined
admission/encoder outcome. The incomplete encoder/budget propagation experiment
was removed while preserving earlier completed work. No write/replay limit,
public mode, journal format or weaker integrity contract is introduced.
Next resume admission plus actual encoder charging at the now-converged owners;
the unchanged-index verification discrepancy still needs qualification.

Focused validation passes on Rust 1.98.1: updated/deleted unique keys in online
and interrupted-startup folds, indexed lookup and reuse of both old values,
unchanged-index relation recovery, malformed-row/late-record rejection,
multi-entity interruptions, complete large-batch folding and schema-publication
admission/recovery. All-feature/all-target core lint, SQL-free core compilation,
formatting and whitespace checks pass. The initial new test reused a thread-local
fixture across scenarios; separate tests now provide independent storage, and
both pass.

Scope: seven files, approximately 373 fewer production Rust lines and 77 added
test lines (about 296 fewer Rust lines overall), plus release/status notes.
Runtime shape is simpler: one row-preparation flow and one fewer private mode.
Raw Wasm, cycles and instruction deltas remain unmeasured; full suites remain
user-owned. Applications, versions and networks are unchanged; no commit or
push was performed.

## Previous handoff: replayability qualification (0.257.4)

The approved write/replay investigation found a concrete counterexample to
bounding verification by original write construction: unchanged-index updates
skip scalar encoding, while final marker verification rebuilds their after-image
keys. A new interrupted relation-only update regression preserves the payload
index and proves the removed reverse relation releases its target. Temporary
test-only probes observed zero components in update/fold and one in verification;
the probes were removed. The existing three-entity interruption scenario also
passes, with separate write, fold and verification construction phases.

The source audit also narrows the earlier risk description: canonical batch fold
and watermark publication are atomic, and startup discards live overlays first.
The derived-rebuild facade is used by final marker verification, not by an
independent general maintenance workflow. These findings do not establish a
complete resource bound or an observed production exhaustion.

No runtime budget policy was changed. The proposed ownership-only landing cannot
prove replayability independently of encoder/admission semantics. The
[remaining-work decision](0.257-explain-remaining.md#r5-writereplay-dependency--admission-qualification-remains)
recommends combining replay-safe admission with encoder accounting, while
preserving verification and qualifying the unchanged-index fast path. That
combined boundary was subsequently approved; the current handoff above closes
only its recovery-path simplification. No new profile, certificate, execution
route or weaker verification contract has been introduced. R5 remains open.

Validation passes on Rust 1.98.1: the new regression, five mixed-entity
publication-interruption scenarios and late-record rejection before canonical
writes (seven focused tests), plus all-feature core lint, formatting and
whitespace checks. The final regression run used the source without probes.

Scope: six files, 59 added Rust lines (test and module wiring), plus design and
release notes; production runtime complexity is unchanged. Wasm, cycles and
instruction deltas are unmeasured. Full suites remain user-owned. Applications,
Cargo versions and networks are unchanged; no commit or push was performed.

## Previous handoff: write/replay construction dependency (0.257.4)

The next encoder landing is paused at the documented rescope gate. Caller
inspection found that ordinary commit preflight, single/batch journal replay
and live derived-state rebuild do not have the construction authority added to
maintenance pages. Installing the strict execution adapter would reject valid
out-of-execution callers. Installing a new finite replay allowance without an
admission/replayability contract is also not justified: replay reconstructs
before-images from current storage, so its encoding work can differ from the
original write.

The [remaining-work decision](0.257-explain-remaining.md#r5-writereplay-dependency--admission-qualification-remains)
records exact owners and recommends a bounded write/replay admission prerequisite
before the shared encoder change. This requires approval because it changes
write/recovery resource policy, not merely codec signatures. No runtime change,
new limit, format, fallback or retry behavior was implemented. R5 and typed
`.explain()` remain incomplete; the prior maintenance qualification is unchanged.

This handoff changes two design/status files only, approximately +90 net
documentation lines and zero Rust lines. Runtime complexity is unchanged.
Whitespace validation passes; runtime tests and Wasm/cycle/instruction
measurements were not run for this documentation-only decision handoff.
Existing dirty-worktree changes, applications, Cargo versions and networks
remain untouched; no commit or push was performed.

## Previous handoff: explicit maintenance construction authority (0.257.4)

The approved first half of the R5 split uses one explicit maintenance owner
around unique-validation scans and migration validation, rewrite and final
validation pages. Complete staged index replacement keeps the owner across row
observations and finish. All reuse the existing mutation hard-limit profile;
none depend on an ambient execution tracker or substitute a query request root.

The owner implements the same required ConstructionBudget interface. It charges
row/key visits and instruction watermarks and retains the first exhaustion as
fixed-size volatile state. Failure cannot be discarded into successful empty
progress or a later staged replacement. Existing soft limits still yield;
hard failure returns before publication, without inventing a persisted status
or automatic retry route. Earlier committed pages remain intact.

This is authority/lifetime completion, not encoder boundedness. Scalar encoding,
accepted enum conversion, query/write/rebuild propagation and fallible optional
index-predicate compilation remain the second landing. The complete contract is
in [remaining work](0.257-explain-remaining.md#r5-maintenance-authority-and-encoder-sequencing).

Focused validation passes: six new budget tests, thirteen schema-index staging
tests, three physical migration scenarios and twelve existing execution-budget
tests. Additional overlapping complete-domain checks pass. The new staging test
proves that a later-row failure prevents finish and leaves physical storage
unchanged. Required maintained lint recovery and focused all-feature lint pass;
SQL-free compilation, layer-authority/read-admission checks, formatting and
whitespace checks pass. Initial
test-only error-conversion/Debug-bound compile errors were corrected; no warning
or test failure remains in those completed gates.

Scope: eleven files including root/detailed release and design/status notes,
approximately +400 net Rust lines (about half tests). Runtime routing stays unchanged;
accounting is more explicit and adds one owner/latched-error state, without a
new encoder, policy profile or persisted mode. Raw-Wasm, IC-cycle and measured
instruction deltas are unmeasured; no timing probes or full suites were run.
Application, Cargo version and network lifecycle state are unchanged; no commit
or push was performed. Rebuilt canister/artifact validation remains outside the
focused native checks.

## Previous handoff: codec-authority preflight (0.257.4)

The next-boundary audit inspected the shared ordered encoder, accepted key
builders, predicate compilation, staged schema-index construction, constraint
activation and migration validation. Query preparation has suitable explicit
construction authority; background key derivation currently has page/source/
staged-output limits but no matching scratch/work authority. The strict execution
adapter rejects missing trackers, while the optional charger would bypass
accounting. Predicate encoding also currently erases errors into `Option`.

The concrete caller inventory and proposed two-part sequencing are recorded in
[remaining critical path](0.257-explain-remaining.md#r5-maintenance-authority-and-encoder-sequencing).
Maintenance budget lifetime and exhaustion/progress semantics need agreement
before connecting every shared encoder caller. No limit, fallback, adapter,
runtime behavior or persisted state was added. This is a source preflight, not
R5 completion, a newly discovered production failure or a line closeout.

Two design/status files changed, approximately +65 documentation lines and zero
Rust lines. Runtime complexity is unchanged. Whitespace validation passes;
runtime tests, Wasm/cycle/instruction measurements and full suites were not run
for documentation-only work. The preceding projection handoff remains intact.
No application, Cargo version, changelog, network, commit or push changes.

## Previous handoff: single budgeted projection handoff (0.257.4)

Slice 11 / R4 constructs the accepted projection once, validates it by reference,
then moves it into static finalization. Prepared structural and SQL consumers
borrow the retained projection; they no longer clone it or reconstruct metadata
from an unfinalized plan. The fast scalar route uses the same constructor.
Current accepted authority still reaches planning/cache validation and grouped
slot rebinding. Identity projection construction is unchanged and does not use
the new preparation admission checks.

Expression/alias copies share the existing projection-field copy owner. Field
labels, grouped paths, aggregate inputs and backing vectors are admitted before
copying. Schema-wide projection admits conservative scratch and heapsort work
before calling the existing slot-order sorter, including its two position
vectors; there is no parallel sorting algorithm. This bound is resource
reservation, not a measured instruction or cycle result. No resource, mode,
request root, persisted shape or public API was added.

Direct qualification covers sparse accepted slot order, duplicates, aliases,
typed payloads, exact/cumulative exhaustion, grouped paths and semantic DISTINCT.
The owned-handoff check verifies unchanged projection backing and expression
shape after grouped rebinding. Existing cache fixtures now include one charged
projection construction per cold/rebound plan, not an arbitrary budget increase.

Remaining semantic/type inference, schema lookup, candidate/cache work and
diagnostic projection/rendering are still open. This is projection construction
completion, not all R4 or preparation boundedness; typed `.explain()` remains open.

Qualification: 169 focused tests pass across accepted schema/projection,
ordering/cache publication and A/B/A rebinding, cardinality metadata,
fingerprints, grouped semantics and SQL/typed projection helpers. All-feature
core lint, SQL-free compilation, layer-authority/read-admission invariants,
formatting and whitespace checks pass on Rust 1.98.1. The first new grouped test
fixture incorrectly supplied a composite rather than admitted scalar leaf;
correcting its accepted nested metadata resolved both fixture failures.

Scope: 18 files, approximately +370 net Rust lines (about +100 production,
the remainder tests and fixture propagation). The handoff is simpler: one
accepted projection construction, one retained owner and borrowed consumers.
Explicit admission adds checks but no execution route or policy choice. Main
owners are projection lowering, shared field copying, schema slot-order
admission, semantic gates/finalization and their structural/SQL consumers.
Raw-Wasm, IC-cycle and measured instruction deltas are unmeasured; no timing
probes or full suites were run. Full validation remains user-owned. No
application/Cargo version edits, network lifecycle actions, commits or pushes.

## Previous handoff: budgeted order consistency validation (0.257.4)

Slice 11 / R4 now passes the current PreparationWork into shared semantic-gate
orchestration and both ordering consistency checks. Duplicate detection charges
term/name comparisons, scratch growth and computed-label writes. Direct field
names are borrowed; primary-key-only orders no longer allocate unused scratch
or copied names. Missing-key validation charges its visits and comparisons while
preserving empty-order behavior and the first missing composite-key position.
Duplicate errors preserve their first/repeated term positions and label-based
equality, including differently typed expressions with equal rendered labels.

The existing boolean-ordering text sink moved to preparation and is shared by
computed-order validation. Its typed failure remains sticky even if a formatter
swallows fmt::Error; no partial label is returned. The label grammar is unchanged.
Semantic gates now call their existing validators directly instead of accepting
unused interchangeable callbacks. No mode, request root, limit or error variant
was introduced, and the detached planner artifacts retain no preparation borrow.

This accounts ordering consistency and label writes/retention, not all semantic
validation, expression inference, numeric formatting internals or diagnostic
construction/rendering. Other preparation gates and typed `.explain()` remain
open.

Qualification passes on Rust 1.98.1: 162 focused test executions cover ordering
consistency, shared-sink failures/escaping, boolean ordering, cache publication
and A/B/A rebinding, cardinality selection and fingerprints. Focused all-feature
lint, SQL-free compilation, layer-authority/read-admission invariants, formatting
and whitespace checks pass. An initial policy-error conversion compile failure
was repaired without adding an error variant. Two ignored timing probes were not
run, and full suites remain user-owned.

Scope: 14 files, approximately +310 net Rust lines (about +40 production and
+270 tests). Runtime changes cover the preparation sink, label writer, canonical
boolean ordering, order validators, semantic gates and pipeline. Direct tests
and the template-cost fixture accompany the change; root/detailed release notes
and remaining-owner/status notes are updated. The structure is simpler: one text
sink and direct validator calls, with extra fallible accounting and no new route.
Wasm bytes, cycles and measured instruction deltas are unmeasured. No application
or Cargo version edits, network lifecycle operations, commits or pushes.

## Previous handoff: budgeted order canonicalization (0.257.4)

Slice 11 / R4 now charges the shared ORDER BY canonicalization owner. It admits
primary-key visits, direct-field name comparisons, vector growth and appended
key names before performing them. Callers supply names from accepted schema and
the existing PreparationWork; no active-execution lookup, new root or budget is
introduced. Access selection also budgets its temporary ordering copy, while
logical assembly consumes its already-budgeted order. Both paths preserve typed
budget failure. A trivial forwarding helper was folded into the canonical owner.

Absent ordering and grouped ordering still skip scalar primary-key tie-break
work. Scalar ordering preserves authored terms and duplicates, exact case-sensitive
field matching, composite-key order and the last authored direction (ascending
for an empty explicit order). Repeated canonicalization adds no duplicate key.
Template rebinding retains its existing route and only performs logical-order
canonicalization; identical bound memo hits still skip construction. Exact cache
fixtures distinguish that from the additional cold access-order copy/check.

This closes order canonicalization and its access-order copy, not all shared
preparation. Predicate normalization, projection lowering, semantic validation,
candidate/cache work and scalar/raw encoding remain open. Typed `.explain()` and
bounded diagnostic projection/rendering remain pending; no endpoint is added.

Qualification passes on Rust 1.98.1: 159 focused test executions cover canonical
ordering, exact rejection boundaries, repeated request charges, cache publication,
A/B/A rebinding, cardinality selection and fingerprints. Focused all-feature lint,
SQL-free compilation, layer-authority/read-admission invariants, formatting and
whitespace checks pass. Two ignored timing probes remain unrun.

Scope: ten files, approximately +220 net Rust lines (about +25 production and
+195 tests). Runtime changes are confined to logical builder, access planner and
pipeline; direct ordering tests and the exact template-cost fixture cover their
propagation. Root/detailed changelog and remaining-owner/status notes are current.
Execution structure remains neutral: one canonical owner, extra fallible checks,
one fewer forwarding helper and no new route or policy. Wasm bytes, cycles and
measured instruction deltas are unmeasured. No full suites, application or Cargo
version edits, network lifecycle operations, commits or pushes were performed.

## Previous handoff: budgeted logical-clause handoff (0.257.4)

Within slice 11 / remaining area R4, logical planning inputs now borrow the
intent's clauses. Shape-only checks do not copy them. The shared plan assembly
boundary requires PreparationWork and charges retained filter/order/group,
aggregate/HAVING and authored projection copies before allocation. Group keys
retain the same representation and accepted slot authority; direct slots share
their labels/kinds. Exact-key filter stripping precedes copying, and reranking
moves the existing logical plan and projection instead of cloning them again.
Trivial and ordinary assembly use the same copy owners. Errors remain typed and
unfinished plans cannot be published to the cache.

This is required retained-clause construction, not a claim that all R4 work is
closed. Order canonicalization, projection lowering, semantic validation, scalar
normalization, scalar/raw codecs and other listed preparation owners remain open.
No diagnostic limits, public terminal, request roots or execution modes are added.
The earlier R5-first sequence is adjusted within slice 11: the codec has callers
outside preparation/active execution that need an explicit authority decision;
this handoff covers a reachable owner whose request authority is already present.
It does not silently give those codec callers an optional unmetered fallback.

Qualification passes on Rust 1.98.1: 157 focused test executions cover clause
parity, exact backing rejection, cumulative public/trusted/diagnostic charges,
cache publication and A/B/A rebinding, fingerprints and grouping. The template
fixture now includes each binding's actual clause-copy cost rather than assuming
equal payload lengths. Two lint findings were repaired; maintained workspace,
SQL and actor lint plus focused all-feature lint pass. SQL-free compilation,
layer-authority/read-admission invariants, formatting and whitespace checks pass.

Scope: 14 files, approximately +420 net Rust lines (about +100 production and
+320 tests). Runtime changes cover intent model/state, logical builder/pipeline,
expression preparation, grouped-key and projection-copy owners. Direct tests
live beside the logical builder and in planner-watermark/template-accounting
fixtures; root/detailed release notes and the remaining-owner checklist are
updated. Ownership is simpler, with explicit fallible construction checks; no
new execution route is added. Wasm bytes, cycles and measured instruction deltas
are unmeasured. No timing probes, full suites, application/version edits or
network lifecycle changes were made.

## Previous handoff: explain critical-path audit (0.257.4)

The user requested an honest delivery estimate while continuing work. Source
inspection confirms four existing tracker slices remain incomplete (10, 11, 5,
1), with nine concrete remaining work areas, not one final terminal edit. See
[the remaining-owner checklist](0.257-explain-remaining.md) for source evidence,
completion criteria and the boundary between typed/shared preparation and
isolated SQL/execution work. Nine areas are not nine promised turns or patches;
calendar estimates remain unreliable while codec propagation and bounded
diagnostic construction are unresolved.

This is a documentation-only audit, not another completed accounting slice.
The approved preparation/diagnostic gates remain intact. Next implementation is
actual shared scalar-codec accounting with required authority and typed error
propagation, not another standalone clone cleanup. No runtime code, tests,
applications, versions, networks or performance measurements changed here.

## Previous handoff: direct big-integer digit emission (0.257.4)

Encoder inspection found an intermediate ASCII digit vector shared by signed
and unsigned big integers: base-1e9 chunks were expanded into that vector, then
copied/inverted into the final component. The codec now derives digit length
from chunk count and the leading chunk, enforces the same u16 length boundary,
reserves the final payload and emits chunks directly through fixed stack scratch.
Padded/unpadded and positive/negative emission share that owner. Canonical bytes,
zero handling and accepted values are unchanged; no format cut is needed.

This is one bounded encoder simplification under slice 11, not budget-interface
plumbing or a new accounting route. It removes construction before that remaining
work is charged. Limb/chunk conversion still allocates and performs division;
scalar-codec request accounting, raw bounds, remaining planner/cache construction
and bounded diagnostics stay open. `.explain()` is not implemented by this change.

Scope: six files, approximately +135 net Rust lines (eight production, 127 tests).
One temporary buffer/copy is removed; no mode, limit, public API or request root
is added. Wasm bytes, cycles and measured instruction deltas are unmeasured.
Qualification passes on Rust 1.98.1: 178 focused test executions cover canonical
encoding/order, numeric boundaries, raw envelopes, cardinality/cache behavior and
fingerprints. Maintained repository/SQL/actor lint, focused all-feature lint,
SQL-free compilation, index-ownership invariants, formatting and whitespace checks
pass. The test-only checked-conversion lint was corrected. No timing probes,
full suites, application or network lifecycle changes.

## Previous handoff: shared prefix-list accounting (0.257.4)

Encoded prefix lists now admit backing before encoding and charge each component
visit through the caller's required ConstructionBudget. Prefixes, ranges,
multi-lookups, borrowed branch operands and exact-count keys converge on this
owner. Branch specs expose borrowed prefix iterators instead of cloning temporary
Value lists; multi-lookups use
the same final spec assembly as ordinary prefixes, preserving deferred bounds.
Exact-count key-vector backing is also charged before construction.

The shared session count helper borrows the existing request scope in the
caller's public/trusted/diagnostic lane. Budget errors remain typed, not optional
metadata fallback or index-encoding errors. Successful canonical bytes, cache
identity, authority checks and warm reuse are unchanged. No new request root,
budget, limit, configuration or encoding route is introduced.

This accounts component-list construction, not scalar-codec allocation/work or
raw-bound construction. Exact-count candidate selection (including its owned
composite proof), remaining planner/cache work and bounded diagnostics remain
open. It does not close slice 11 or deliver `.explain()`.

Scope: seventeen files, approximately +300 net Rust lines, mostly tests. Shared
assembly and borrowed operands simplify the flow; explicit budget checks add
failure propagation. Raw-Wasm bytes, cycles and measured instruction deltas are
unmeasured. Qualification passes on Rust 1.98.1: 266 focused test executions
(including final branch-owner requalification), all-feature/all-target lint,
SQL-free compilation, repository invariants, formatting and whitespace checks.
The new metadata fixture was corrected to the existing 17-prefix ceiling; the
ownership gate remains unchanged. No timing probes, full suites, application
changes or network lifecycle changes.

## Previous handoff: accepted-schema lowering hard cut (0.257.4)

All maintained access-lowering callers already supply accepted SchemaInfo.
Private helpers now require that reference directly instead of carrying an
unreachable schema-free choice. The unused semantic range-request enum, its
dispatcher, text-prefix rediscovery and bulk prefix-encoding helper are removed
with that route. Maintained range lowering takes an accepted encoded prefix;
its endpoint encoding and the planner's text-prefix construction are unchanged.
No compatibility path, new request charge, policy limit or public API is added.

This is a source-flow simplification, not new scalar-encoder accounting. Inspection
also confirms that the scalar codec serves writes/rebuilds as well as queries;
budget propagation must preserve that shared owner and typed failures rather
than create a query-only encoder. Scalar and raw-bound allocation/work accounting,
remaining planner/cache construction and bounded diagnostics stay open before
`.explain()`; this does not close slice 11.

Scope: ten files, approximately -100 net Rust lines including direct composite
range coverage. The implementation has one fewer lowering route. Wasm bytes,
cycles and measured instruction deltas are unmeasured; no runtime gain is claimed
for deleting unreachable source. Qualification passes on Rust 1.98.1: 287 focused
test executions cover accepted composite ranges, cardinality/cache behavior,
ordering, raw envelopes, canonical key/enum encoding, fingerprints and SQL
lowering. Focused all-feature/all-target lint, SQL-free compilation, formatting
and whitespace checks pass. No timing probes, full suites, Wasm/cycle measurements,
application changes or network lifecycle changes.

## Previous handoff: index-spec allocation and ownership (0.257.4)

Lowering reserves outer prefix/range spec backing against TemporaryBytes before
leaf encoding. The existing semantic shape facts supply exact output counts;
composite growth charges the full new backing, not only the added slots. The
multi-lookup helper no longer performs a separate uncharged reservation.
Preparation and lowering share the moved container-growth owner; text appending
uses that same policy. Existing preparation reservation charges are unchanged.

Exact-prefix specs and count keys transfer their encoded component buffers rather
than copying their bytes. This removes two copying sites without changing the
encoder, accepted authority, canonical keys or eager/deferred bounds. Retained
accounting includes transferred spare capacity, so cache weight can rise where
the former copy had tightened capacity; there is no shrink/copy fallback.

Scalar encoding, raw-bound construction, component-list allocation and final
shared-resident allocation remain open, as do candidate enumeration, other
planner/cache work and bounded diagnostics. This is not an encoder boundedness
verdict, slice 11 closeout or `.explain()` delivery. No mode, counter, request root
or parallel encoding path is added.

Scope: thirteen files, approximately +350 net Rust lines, mostly tests. Ownership
and reservation policy are simpler; typed allocation checks add explicit failures
under existing limits. Wasm bytes, IC cycles and measured instructions remain
unmeasured. Qualification passes on Rust 1.98.1: 248 focused test executions
cover shared reservation/value/expression construction, index-spec ownership,
cardinality, cache/pinned-route behavior, ordering, fingerprints, SQL lowering
and retained accounting. Maintained repository/SQL/actor lint, focused
all-feature/all-target lint, SQL-free compilation, formatting and whitespace
checks pass. The test-only lint spelling was corrected. Full suites, timing
probes, rebuilt Wasm/cycle measurements and network lifecycle checks were not run.

## Previous handoff: index-spec traversal (0.257.4)

Access lowering now constructs only the prefix/range specs both consumers retain.
The discarded executable tree and composite-child allocations are deleted; the
remaining union/intersection walk shares one branch and charges one existing
PredicateExpressionSteps unit per node before descent or leaf encoding. The
prepared-plan handoff and cardinality candidate lowering require the caller's
ConstructionBudget. All cold, template-binding and pinned-route callers pass
their current PreparationWork; successful warm reuse does not enter lowering.

Budget exhaustion preserves its typed error, distinct from optional prefix/range
encoding failure. No request root, policy switch, cached budget or parallel
lowerer is introduced. Spec ordering, successful raw bounds and cursor identity
are unchanged. Encoder/spec-buffer allocation accounting, candidate enumeration,
other planner/cache work and bounded diagnostics remain open; this does not close
slice 11 or deliver `.explain()`.

Scope: fifteen files, approximately +200 net Rust lines, with production code
shrinking and direct boundary tests added. Implementation shape is simpler.
Wasm bytes, IC cycles and measured instruction deltas are unmeasured. By user
direction, native timing is not performance evidence or a release gate; previous
receipts remain historical only. No timing benchmarks are run for this landing.
Qualification passes on Rust 1.98.1: 199 focused test passes across cardinality
lowering/selection, cache and pinned-route behavior, ordering, fingerprints and
SQL lowering; maintained repository/SQL/actor lint, focused all-feature/all-target
lint, SQL-free compilation, formatting and whitespace checks. The exact template
budget fixture now includes one lowering visit for cold/rebound construction and
still proves zero construction work on an identical warm hit. No full suites,
timing probes, rebuilt Wasm/cycle gates or network lifecycle changes were run.

## Previous handoff: pre-lowering cardinality probe admission (0.257.4)

The existing 64-candidate, 16-prefix-per-candidate and 256-total-prefix policy
now admits semantic shape counts before any candidate encoding or output-buffer
allocation. It reuses the access layer's existing shape facts and the same policy
owner. Oversized optional tie-breaks retain PolicyFallback, not a new query error.
The bounded count pass is separate from value encoding; it does not visit values.

Preparation reserves its candidate/key buffers once against TemporaryBytes.
Shape visits, candidate visits, encoded-size observations, duplicate identity and
component comparisons use PredicateExpressionSteps; equal-length byte slices
charge their comparison allowance before equality. Exhaustion is a typed error,
never a duplicate or policy fallback. Byte-size inspection shares one pass.
Post-encoding byte ceilings remain unchanged, and lowered counts must agree with
the admitted capacity before successful publication. No new limits, modes,
request roots, persisted formats or cache artifacts are introduced.

Access encoding's own allocations, candidate enumeration/scoring, remaining
planner/cache validation and bounded diagnostics remain open. This is not a full
access-lowering bound or a slice 11 closeout. The next work must address those
actual owners rather than claim post-encoding observations bound construction.

Scope: eight files, about +340 Rust lines (100 production, 240 tests/fixtures).
Buffer ownership and size inspection are simpler; admission/accounting adds
explicit failure checks without a parallel execution flow. The native probe's
17/128-prefix fallback cases improve about 94% by skipping encoding. The admitted
16-prefix case rises from 10.99 to 18.14 microseconds median (+65%), with observed
after samples spanning 14.86–19.58 microseconds. Detailed notes retain conditions
and all case medians; this does not establish whole-query or IC-cycle performance.
Raw-Wasm/function-count and allocation-profiler deltas remain unmeasured. No
application, Cargo version, network lifecycle, commit or push is changed.

Qualification passes on Rust 1.98.1: probe/evidence boundary tests and native
probes; cardinality/cache/pinned-route, ordering, fingerprint and SQL-lowering
regressions; required repository lint, focused all-feature/all-target lint,
SQL-free checking, formatting and whitespace checks. The initial nested-return-
type lint finding was resolved with an owner-local type name for the paired
buffers. Full suites and rebuilt Wasm/canister lifecycle gates were not run.

## Previous handoff: exact-cardinality evidence construction (0.257.4)

Exact-cardinality evidence now consumes the existing PreparationWork supplied
by ordinary shared planning. Selection and evidence construction share one pass;
the intermediate ranked-candidate vector is removed. Only the current winner's
access data survives the pass, and evidence retains original candidate order.
Equal counts keep the first candidate. Missing count ranges, checked-sum overflow
and unavailable route identity remain optional-policy outcomes; exhausted request
work is a typed error propagated before selection publication, never fallback.

Charges precede candidate visits, each prefix-count addition, minimum comparisons,
final route-pin derivation, vector backing and copied index names. No new budget,
counter, request lifecycle, cached state or cursor representation is added. Warm
plans and pinned continuations that skip fresh selection still skip this work.
Candidate enumeration, access lowering, other planner/cache work and diagnostic
construction remain open; this is not slice 11 closeout or `.explain()` delivery.

Complexity: eight files, approximately +330 net Rust lines (about 300 test/fixture
lines). One pass and one fewer temporary vector simplify ownership; required
typed accounting makes failure handling explicit. No additional behavior axis
is introduced. The matched three-candidate native probe rises from 212 to 248 ns
median (+17%, +36 ns), including the added request charges. This is an isolated
evidence-owner cost, not whole-query latency or IC cycles. Raw-Wasm/function-count
and allocator-profile deltas are unmeasured. No application, Cargo version,
network lifecycle, commit or push is changed.

Qualification passes on Rust 1.98.1: evidence-owner tests/probe, cardinality/cache
and pinned-route tests, ordering, fingerprints and SQL lowering; all-feature/
all-target lint, SQL-free checking, formatting and whitespace checks. Exact and
one-below limits cover public/trusted/diagnostic lanes; repeated segments share
their counters, and the real tie-break caller propagates budget exhaustion with
zero row visits. The initial probe expected two candidates; the accepted fixture
has three, and the corrected baseline/candidate probes both pass. Full suites
and rebuilt canister artifact/lifecycle validation were not run.

## Previous handoff: borrowed candidate residual comparison (0.257.4)

Candidate ranking, exact-cardinality tie enumeration and explain candidate
projection now borrow scalar semantics and candidate access. The temporary whole
logical/projection copies and their unused constructor are removed. Residual rules
remain at the shared semantic owner; candidate shape and predicate are derived
once, while finalized chosen plans retain their cached contract. No authority,
selection policy, resource limit, retained format or execution route is added.

Scope: six Rust files plus four release/status documents; approximately neutral
production lines (+1) and 110 test/probe lines. This removes construction instead
of introducing budgeted copies. Remaining predicate derivation allocations,
candidate lowering/scoring and cache validation are still slice 11 work. This is
not preparation boundedness closeout or typed `.explain()` delivery.

Qualification passes on Rust 1.98.1: focused candidate parity, cardinality/cache,
ordering, fingerprint and SQL-lowering tests; all-feature/all-target lint;
SQL-free checking, formatting and whitespace checks. The manual native probe
also passes. An unused-constructor warning during the first test build was
resolved by deleting the orphaned constructor; final lint is clean. Full-suite
and rebuilt canister artifact/lifecycle validation were not run.

The matched native residual-owner probe improves median time by 69–73% for
1/8/64-term fixtures. See the detailed 0.257.4 changelog for conditions and results.
Full-scan candidate fixtures isolate comparison cost: this does not establish
whole-query speed, index-bound stripping cost, raw-Wasm size or IC-cycle savings.
Those broader measurements remain open. No application, Cargo version, network
lifecycle, commit or push is changed.

## Previous handoff: scalar compiler construction accounting (0.257.4)

The user approved an explicit construction-budget interface after the caller
inventory identified three existing contexts: request preparation, active row/
mutation execution, and standalone builder previews. Need: charge actual compiler
construction without new request roots or nested instruction intervals. Simplest
alternative (an optional active tracker everywhere) would silently skip planner
work outside execution and is not selected. Canonical owners remain PreparationWork
and the active execution tracker; standalone previews have no database request.
State-space delta: one internal required budget interface, no public mode,
configuration, counter, persisted state or optional compiler argument. Database
compilation must provide a real owner; only the private preview adapter is unmetered.
Reuse value-copy accounting rather than introducing a second recursive value walker.
Grouped result compilation, other planner work and diagnostics remain separate;
this landing covers scalar programs and their direct callers, not all preparation.

Qualification: 295 focused tests pass across accepted-schema compilation, shared
value copying, request/execution budgets, scalar evaluation, session/cache ordering,
SQL lowering, fingerprints, aggregate terminals and resumable-update admission.
Exact bounds include real specialization allocations and discarded CASE operands.
Cache checks cover rejection before publication, cumulative retries, memo hits
that skip construction and A/B/A metadata rebuilding. All-feature/all-target lint,
SQL-free checking, formatting and whitespace checks pass on Rust 1.98.1.

Complexity: 23 logical files (25 paths including two file moves), approximately
+530 net Rust lines, with about half the addition in tests. One required budget
interface and strict execution adapter add explicit accounting, not another
compiler or request lifecycle. Published baseline: 50da26d0f (0.257.3).
Cycle, allocation-profiler and raw-Wasm/function-count deltas are unmeasured;
exact resource-unit tests are not substitutes for those measurements. Full suites
and rebuilt canister lifecycle/cost gates remain user-owned or future qualification.
No application, Cargo version, network lifecycle, commit or push was changed.

Next-scope boundary: grouped-result compilation's current callers are executor
paths, not logical explain. Its remaining accounting is not automatically a
typed-explain blocker. Prioritize demonstrated on-path planner/candidate work and
bounded logical diagnostics; do not turn unrelated executor cleanup into a gate.

## Previous handoff: fuse scalar compilation (0.257.3)

Scalar ASTs compile directly to the final CompiledExpr program. The temporary
scalar IR, its wrappers and its separate final traversal are deleted. Preview
and schema-backed compilation share the recursive compiler while preserving
their existing leaf admission and typed errors. All CASE children compile before
parent specialization, including discarded branches. Projection, ordering,
aggregate input/FILTER and resumable SQL scope callers consume final programs
directly; the projection vector retains exact field-count capacity.

This removes construction instead of adding a separately metered compiler.
No runtime mode, resource counter, error kind or schema-authority route is added.
Source literals/labels still copy once into final programs; final containers and
path-label/byte encoding still need accounting. An invalid late operand can
discard an already-compiled prefix. This is neither complete preparation
boundedness nor delivery of typed `.explain()`; slices 10/11 and diagnostics
remain open. Cycle and raw-Wasm/function-count deltas are unmeasured.

Qualification: 268 focused tests pass across compiled expressions, previews,
accepted-schema compilation, session/cache ordering, SQL lowering, fingerprints,
aggregate terminals and resumable-update admission. All-feature/all-target lint,
SQL-free checking, formatting and whitespace checks pass on Rust 1.98.1.
Complexity: 17 files, roughly 200 fewer Rust lines including test changes;
production has fewer representations and recursive flows. Full suites remain
user-owned; applications, Cargo versions and network lifecycles are unchanged.

Delivery boundary: the user questioned the accumulated cleanup scope during
this handoff. Future work must distinguish demonstrated bounded-preparation/
diagnostic prerequisites from optional ownership cleanup. Do not make every
adjacent duplication finding a new `.explain()` gate. Finish this handoff and
identify the concrete remaining blockers before another implementation outcome.

## Previous handoff: remove aggregate compilation pre-scan (0.257.3)

Structural scalar aggregate inputs and FILTER expressions no longer traverse
the tree to find an unknown field before invoking the scalar compiler. That
compiler already rejects missing fields/root slots and unsupported scalar shapes;
the wrapper maps each unavailable program to the same payload-free executor
invariant. The redundant scan, discarded field-name allocation and unused role
argument are removed. There is one field-admission/construction owner, with no
new mode, counter, error kind or schema-authority route.

Successful compilation removes a whole traversal and repeated direct-field
lookups. Rejection follows compiler order: an invalid late field can now cause
earlier operands to be built and dropped, whereas the pre-scan avoided that
construction. Existing input admission is unchanged; this is not a claim that
compiler allocations are bounded or every rejection becomes cheaper. Scalar
compiler construction/accounting, shared label/comparison work and diagnostic
bounds remain open before typed `.explain()`.

Qualification: 146 focused tests pass across scalar aggregate terminals, scalar
compilation, session/cache ordering, SQL lowering and fingerprints. All-feature/
all-target lint, SQL-free checking, formatting and whitespace checks pass on
Rust 1.98.1. Tests compare input and FILTER programs
with the maintained compiler under current/renamed schema fields, missing path
roots, nested functions, unsupported aggregate leaves and a large literal before
an invalid field. Rejections retain the same class, origin, code and numeric facts.
Complexity: four files, approximately +95 net Rust lines including about 120 test
lines; production is smaller. Cycle and raw-Wasm/function-count deltas are
unmeasured. Full suites remain user-owned; applications, Cargo versions and
network lifecycles are unchanged.

## Previous handoff: compare accepted index order without rendering (0.257.3)

Candidate scoring, order-only access selection and boolean route-order checks
compare borrowed accepted index-key items directly. They no longer build a
temporary vector of canonical index labels or an owned diagnostic compatibility
object just to read its match kind. Scalar owned-label diagnostics and borrowed
keys use one classifier; the primary-key suffix is removed once before matching.
Grouped matching keeps its leading-sequence/trailing-key and positive-prefix
precedence rules under the shared range-checked term comparator.

Accepted expression comparison and owned rendering use one operation-to-label
grammar, including nested LOWER(TRIM(...)). Direct fields still compare by exact
canonical label bytes, including labels resembling expressions. This is not a
new semantic-expression equivalence rule. Retained diagnostics still construct
their owned text when required. Removed full/suffix wrappers and string-list
boolean entry points are hard-cut, with maintained callers migrated.

No state, budget resource or execution mode is added. This removes construction
from slice 11's comparison consumers; ORDER BY contract label construction,
comparison charging, candidate expansion and diagnostic bounds remain open.
It does not expose typed `.explain()` or close preparation boundedness.

Qualification: 118 focused tests pass across accepted expression labels, scalar/
grouped order contracts, key-item matching, session/cache ordering and fingerprints.
All-feature/all-target lint, SQL-free checking, formatting and whitespace checks
pass on Rust 1.98.1. Complexity: nine files, approximately
+125 net Rust lines, mostly about 165 lines of new parity tests; production is
smaller. Boolean comparisons allocate no index-label list or expression-label
string. Cycles and raw-Wasm/function-count deltas are unmeasured. Full suites
remain user-owned. Applications, Cargo versions and networks are unchanged.

## Previous handoff: share secondary-order primary-key storage (0.257.3)

The shared secondary-order constructor consumes the schema's existing
`Rc<[String]>` handle. Candidate scoring, order-only index selection and final
route projection all use that same constructor. It no longer allocates a second
primary-key vector or copies its labels, and cloned plans share the same backing.
This extends the existing accepted-schema ownership; it adds no representation
mode, resource counter, fallback or independent schema authority.

Retained accounting uses the existing Rc owner: each resident conservatively
counts the shared header, backing and string capacity. This can change cache
weight without increasing actual retained allocations. Tests prove shared backing,
detached lifetime, tuple/suffix parity and exact byte limits including UTF-8 labels
with spare capacity.

Source finding: secondary-order construction is also called by candidate scoring
and access-choice diagnostic projection, not only finalization. Adding a bounded
finalization-only constructor would leave a second unbounded flow. This landing
removes the primary-key copy at the common owner; non-primary label rendering,
container construction and comparison work still need the shared preparation
authority. Any fallible diagnostic propagation must preserve ordinary query,
fingerprint and cursor contracts. Slices 10–11 and typed `.explain()` remain open.

Qualification: 132 focused tests pass across order contracts, accepted schema
ownership, session/cache ordering, covering/explain and fingerprints. All-feature
all-target lint, SQL-free checking, formatting and whitespace checks pass on
Rust 1.98.1. Complexity: seven files, approximately +40 net Rust lines, mostly
direct ownership/accounting tests. Ownership is simpler;
no new operational state. Cycles and raw-Wasm/function-count deltas are unmeasured.
Full suites remain user-owned. No application, Cargo version or network changes.

## Previous handoff: reuse finalized secondary ordering (0.257.3)

Covering-read order classification, ordered DISTINCT group seeking and execution
explain consume the secondary-order contract already retained in PlannerRouteProfile.
They no longer render order labels and copy key names into temporary contracts.
Canonical planner finalization precedes these consumers on maintained cold, warm,
rebound and pinned routes; accepted runtime authority is unchanged. Primary-only
and absent ordering retain their existing handling. No fallback reconstruction,
new mode, shared owner or accounting resource is introduced.

This converges downstream consumers in slice 11; initial retained-contract
construction and candidate-time order contracts still need bounding. Remaining
scalar/compiler, planner and diagnostic projection/rendering gaps stay open.
This landing does not expose typed `.explain()` or close preparation boundedness.

Qualification: 119 focused tests pass across session/cache ordering, direct
covering/DISTINCT and explain assertions, fingerprints, order contracts, hybrid
covering and continuation. All-feature/all-target lint, SQL-free checking,
formatting and whitespace checks pass on Rust 1.98.1.

Complexity: seven files, about +90 net Rust lines including roughly one hundred
lines of session-level regression tests;
production is smaller and removes three contract-reconstruction sites. Cycle and
raw-Wasm/function-count deltas are unmeasured. Full suites remain user-owned;
applications, Cargo versions and network lifecycles are unchanged.

## Previous handoff: borrow primary-key names through ordering (0.257.3)

Planner selection, tie-break insertion/validation, covering reads and executor route/cursor
checks borrow the accepted `[String]` name slice. The finalized-plan getter
returns that same backing rather than allocating a reference vector. Temporary
schema-to-reference collection helpers are removed; empty-key ineligibility and
the typed missing-static-contract error remain unchanged. Exact composite order,
secondary suffix admission and current-authority selection retain their owners.

This removes temporary construction within shared preparation slice 11 and its
runtime consumers. Retained secondary-order contracts still copy their owned
names as before; their construction, other planner/compiler bounds and diagnostic
projection/rendering remain open. No typed `.explain()` terminal is exposed.

Complexity: twenty files including three documentation files, approximately
net-neutral Rust line count with about seventy new test lines replacing removed
production scaffolding. One borrowed representation, no new mode, counter or
retained state. Removed source allocations are not a measured cycle or raw-Wasm
delta; neither was measured for this landing.

Qualification: 141 focused tests pass across order contracts, schema ownership,
planner/cache ordering, fingerprints, covering reads and continuation/page handling.
All-feature/all-target lint, SQL-free checking, formatting and whitespace checks
pass on Rust 1.98.1. Full suites remain user-owned; applications, Cargo versions
and network lifecycles are unchanged.

## Previous handoff: reuse scalar admission analysis (0.257.3)

SQL aggregate-input and aggregate-local FILTER validation no longer compile a
ScalarProjectionExpr solely to discard it after checking availability. The
immutable AnalyzedLoweredExpr already carries the two relevant facts: outer
field/path roots and aggregate presence. Current-schema root checks still run
first; only then does aggregate presence select the caller's existing unsupported
error. This preserves first-unknown-field precedence and the global FILTER
caller's existing diagnostic mapping. No query syntax or execution semantics widen.

The loose expression-plus-analysis forwarding helper is removed. All grouped and
global callers consume the same coupled analysis artifact; the real scalar compiler
remains the execution owner. No extra traversal, resource, mode or retained state.
Source analysis construction and schema lookup accounting remain open, along with
actual scalar/projection compilation and the remaining planner/diagnostic bounds.
This removes one frontend construction gap within slice 10, not its closeout or
the typed `.explain()` terminal.

Qualification: 140 focused tests pass (58 SQL lowering, 56 session/cache/ordering
and 26 fingerprints). Focused all-feature lint, SQL-free checking, formatting
and whitespace checks pass on Rust 1.98.1. Full suites and canister artifact/cycle
qualification were not run. Applications, Cargo versions and networks are unchanged.

Complexity: five files, approximately +200 net Rust lines, including 208 lines
of direct admission/parity tests; production is eleven lines smaller. One
compile-and-discard flow and its forwarding helper are removed, with no new
semantic representation. Raw-Wasm bytes, function counts and IC cycles are
unmeasured; fewer temporary trees/copies are source findings, not measured speedups.

## Previous handoff: thread-local schema/plan ownership and shared key names (0.257.3)

Accepted primary-key names now have one immutable Rc-backed list shared by schema
clones and finalized plans. Finalization retains the current schema's list instead
of copying names, and plan clones share it. Detached prior plans keep their own
accepted view; refinalization selects the new schema's allocation and key order.
Cache retention still conservatively charges every resident for the full shared
header, list backing and string capacity. Schema creation remains the allocation
owner; temporary borrowed-name vectors in other helpers remain open.

The user requested Rc where this ownership chain is thread-confined. Accepted
runtime roots and query caches are thread-local; entity authority and prepared
residents already contain Rc. SchemaInfo and AccessPlannedQuery owners now use
Rc through accepted publication, executor preparation and grouped handoff. Native
and IC use the same representation; these internal handles no longer imply
cross-thread sharing. There is no new mode, resource, persisted state or alternate
execution flow. Unrelated Arc payloads and lazy-cell types are outside this change.

This removes retained key-name copying within slice 11, not all preparation work.
Schema construction/lookups, scalar/projection compilation, remaining planner
work and diagnostic bounds still precede typed `.explain()`.

Complexity: thirteen files, approximately +165 net Rust lines, including 152
lines of direct ownership/accounting tests. The ownership chain is simpler:
one shared key-name allocation and thread-local reference counting, without a
new execution route or configuration. Rc/Arc payload accounting retains the
same conservative shared-header policy. Raw-Wasm bytes, function counts and
IC cycles are unmeasured; removal of copies is a source finding, not a measured
end-to-end speedup.

Qualification: 124 focused tests pass (18 schema/ownership, 23 planner metadata,
56 session/cache/ordering, one lazy-resident retention and 26 fingerprint tests).
Repository lint, focused all-feature lint/checking, SQL-free checking, formatting
and whitespace checks pass on Rust 1.98.1. The new retained-byte test was corrected
to isolate the same contract's name payload instead of cloning unrelated vectors;
fixture casts now use checked conversions. Full suites and canister artifact/cycle
qualification were not run. Applications, Cargo versions and networks are unchanged.

## Previous handoff: bounded ORDER BY referenced-slot construction (0.257.3)

The frozen ResolvedOrder owner now constructs its referenced-slot list under the
existing PreparationWork. It preserves first-reference order and duplicates are
omitted without sorting or adding a second set. One PredicateExpressionStep is
charged per order field, compiled node (including slot-free nodes), slot visit,
and duplicate comparison. Backing growth uses the shared reserve_vec policy
before insertion; an insertion failure leaves prior entries/capacity unchanged.
Newly counted work can exhaust existing limits before static-contract or cache
publication. Identical bound-memo hits still skip this construction.

CompiledExpr has one exhaustive traversal with fallible node and slot callbacks.
Existing runtime marking/collection uses its infallible adapter, not another
variant walk. No callback or request scope is retained in a plan. The redundant
optional-order collector is removed in favor of direct fallible propagation.
No execution mode, new resource or cache identity is added. Schema lookups,
initial projection/scalar lowering and compilation, other planner/candidate work
and diagnostic construction remain open; typed `.explain()` is not yet exposed.

Qualification: 127 focused tests pass (22 compiled-expression, 23 planner
metadata/cardinality, 56 session/cache/ordering and 26 fingerprint tests).
Focused all-feature lint, SQL-free checking, formatting and whitespace checks
pass on Rust 1.98.1. Full suites and canister artifact/performance qualification
were not run. Applications, Cargo versions and local networks remain unchanged.

Complexity: ten files, approximately +225 net Rust lines, mostly boundary tests.
Production adds roughly 35 lines for fallibility and accounting; one exhaustive
expression walk remains, with no additional runtime state or execution route.
This is added admission behavior, not a claimed performance optimization.
Raw-Wasm bytes, function counts and IC cycle deltas are unmeasured.

## Previous handoff: allocation-free projection identity (0.257.3)

Projection identity now checks accepted field count, direct unaliased fields and
strictly increasing physical slots. Accepted slots are unique but need not be
contiguous: matching cardinality and strict ordering prove identity without
collecting or sorting schema names. Missing/computed fields, aliases, duplicates
and changed order remain non-identity; exhaustion propagates as a typed error.

The existing PreparationWork charges one PredicateExpressionStep for the count
check, one per field shape, field-label bytes before lookup, and one per adjacent
resolved-slot comparison. This check allocates no backing and charges no
TemporaryBytes. Cold/rebound finalization pays actual work; identical bound-memo
hits continue skipping construction. No new resource, mode or retained state.
Schema lookup internals and initial projection lowering remain open. This closes
the identity check, not slice 11 or the prerequisites for typed `.explain()`.

Qualification: 104 focused tests pass (two identity boundaries, 20 planner
metadata/cardinality, 56 session/cache/ordering and 26 fingerprint tests).
SQL-free checking, focused all-feature lint, formatting and whitespace checks
pass on Rust 1.98.1. Full suites and canister artifact/performance qualification
were not run; applications, Cargo versions and local networks remain unchanged.

Complexity: ten files, approximately +165 net Rust lines, including 125 lines
of direct boundary tests. Production has a longer explicit budgeted check but
removes temporary schema-name construction/sorting; one flow, no new state.
Raw-Wasm bytes, function count and IC cycles are unmeasured. Allocation removal
is a source finding, not a measured end-to-end speedup.

## Previous handoff: shared direct-projection layouts (0.257.3)

The consuming-reader and raw-data-row projection layouts now resolve fields
once from the already-lowered ProjectionSpec. The former is unavailable for
duplicates; the latter preserves duplicates in projection order. Grouped plans,
computed fields and unresolved direct fields retain their existing unavailable
outcomes, separate from typed budget exhaustion. Output layouts stay unchanged.

The single builder receives the existing PreparationWork. It reserves raw slots
at exact projection length, charges field visits/label bytes and each uniqueness
comparison, then copies the completed vector under budget only when the unique
layout is also available. Duplicate detection stops unnecessary comparisons,
not the remaining raw-row field resolution. No partial layout reaches the static
contract or cache. Identical memo hits still skip construction; actual rebuilds
pay for it. Existing resource limits may now reject newly counted work.

Removed both selection-dispatch loops, their temporary optional name vectors,
the duplicate slot collectors and the extra schema-ordering passes for All.
Initial projection lowering and schema lookup internals remain open, as do other
compiler/planner owners and diagnostic bounds. This closes direct-layout
construction within slice 11, not preparation as a whole or typed `.explain()`.

Complexity: ten files, approximately +70 net Rust lines with boundary tests;
production code is roughly 55 lines smaller. Two layout flows and their helper
collectors converge into one budgeted pass; no added mode or retained state.
Raw-Wasm bytes, function count and IC cycles were not measured. Fewer lookups,
schema sorts and temporary name vectors are source findings, not measured speedups.

Qualification: 102 focused tests pass (20 index/order/projection/cardinality,
56 session admission/cache/ordering, 26 fingerprints). All-feature core checking,
SQL-free checking, focused all-feature lint, formatting and whitespace checks
pass on Rust 1.98.1. Full suites and canister artifact/performance qualification
were not run. Applications, Cargo versions and local networks remain unchanged.

## Previous handoff: projection referenced-slot construction (0.257.3)

ProjectionSpec::referenced_slots_for_schema now receives the existing
PreparationWork and returns typed QueryError. The input-admitted expression
walk charges one PredicateExpressionStep per visited node and field-label
bytes before schema lookup. Direct fields and FieldPath roots share that lookup
branch. Aggregate leaves retain their existing traversal semantics.

Sorted unique insertion replaces linear duplicate probing followed by a final
sort. Each binary-search comparison and shifted slot charges work; vector growth
uses the shared reserve_vec construction policy. Comparisons, shifts and backing
are admitted before insertion, preserving existing entries on rejection. Static
contract/cache publication remains after successful construction. Returned slot
order, query fingerprints and cursor identity are unchanged; current limits can
reject newly accounted work. No new resource, mode or retained request handle.

This closes this slot-list construction owner within shared preparation, not
schema lookup internals, other compiler/planner allocations or diagnostic bounds.
Typed `.explain()` remains pending those gates.

Complexity: eleven files, approximately +215 net Rust lines, mostly boundary
tests and cache-fixture accounting. Production grows by about 25 lines for
explicit fallible insertion checks, while duplicate field/path handling and the
separate sort are removed. There is one projection slot-list owner and no new
state axis. Raw-Wasm bytes, function count and IC cycles were not measured;
this is a bounded-construction change, not a claimed performance improvement.

Qualification: 102 focused tests pass (18 index/order/cardinality, two local
slot-insertion boundaries, 56 session admission/cache/ordering, 26 fingerprints).
SQL-free core checking, focused all-feature lint, formatting and whitespace
checks pass on Rust 1.98.1. Full suites and canister artifact/performance checks
were not run. Applications, Cargo versions and local networks remain unchanged.

## Previous handoff: owned scalar-tree compilation (0.257.3)

Superseded by the fused compiler handoff above, which removes this temporary
representation entirely. The following records the earlier ownership step.

The final compiler now consumes ScalarProjectionExpr instead of borrowing it.
All maintained callers already owned the temporary tree: scalar projection and
filters, ORDER BY, grouped/scalar aggregate inputs, builder previews and
resumable SQL scope checks now transfer that ownership. Labels, literal Value
payloads and nested path strings move into CompiledExpr. CASE arms and function
arguments use consuming iteration while preserving specialization and arm order.
There is one compiler signature and no borrowed compatibility entry point.

ScalarProjectionFieldPath now retains only its PathSpec and resolved root slot.
The redundant CompiledPath segment wrapper, module and re-export are removed.
Final field-path programs still own the same dotted label, string segments and
byte-key segments; byte-key encoding remains required construction, not an
eliminated copy. No final IR, fingerprint, cursor or retained-cache format changes.

Initial Expr-to-ScalarProjectionExpr copying, compiler containers, path-label/
byte-key construction and broader planner accounting remain open. This removes
temporary handoff copies within shared preparation; it does not close diagnostic
boundedness or add `.explain()`.

Complexity: fourteen files including mechanical caller propagation and three
documentation files; approximately +65 net Rust lines with new tests, and
roughly 45 fewer production lines. One intermediate module is deleted and the
compiler has one consuming handoff. Raw-Wasm bytes, function count and IC cycles
were not measured. Tests establish payload reuse; container allocations and
byte-key encoding remain, so this is not an allocation-free compiler claim.

Qualification: 148 focused tests pass (81 expression/preview, 56 session
ordering/admission/cache, three order-construction, eight resumable-update).
One manual normalization benchmark remains ignored. All-feature core checking,
SQL-free checking, focused all-feature lint, formatting and whitespace checks
pass on Rust 1.98.1. Full suites and canister artifact/performance qualification
were not run. Applications, Cargo versions and local networks are unchanged.

## Previous handoff: owned binary specialization (0.257.3)

Expression-construction inspection found removable work before further budget
propagation: compile_binary already owned both compiled children but borrowed
them into two optional specialization probes. Successful probes cloned labels
and literal payloads, then discarded the owned originals. The compiler now
matches owned operands once and transfers those payloads into the same direct
slot/slot and slot/literal forms. Boolean slot pairs and other operand shapes
retain generic evaluation, with no new opcode, policy, request root or route.

This removes avoidable expression-construction copies within shared preparation;
it does not complete compiler accounting. ScalarProjectionExpr construction and
its borrowed compilation into CompiledExpr still copy payloads; expression
container allocation, schema lookup and other planner work remain open before
bounded diagnostics and typed `.explain()`. Existing retained-cache accounting
and successful compiled program shapes are unchanged.

Complexity: four files, +117 net Rust lines including tests; production code
is five lines smaller. Two optional borrowed probes become one owned operand
match plus direct slot-pair dispatch. Specialized slot pairs avoid two field
string copies; slot/literal pairs avoid a field string and Value clone.
Raw-Wasm bytes, function count and IC cycle deltas were not measured. Allocation
identity tests establish reuse, not a numerical timing or Wasm improvement.

Qualification: 84 focused tests pass (25 compiled-expression, 56 session
ordering/admission/cache, three order-construction boundaries), including a
240-case specialized/generic evaluation matrix. Focused all-feature lint,
SQL-free core checking, formatting and whitespace checks pass on Rust 1.98.1.
Full suites and canister artifact/performance checks were not run. No application,
Cargo version, network lifecycle, commit or push changes were made.

## Previous handoff: order preparation admission (0.257.3)

ORDER BY resolution no longer constructs a display label that the payload-free
error owner discarded. Its scalar-seam validation only checks supported node
families; accepted field resolution belongs to scalar compilation instead of
being repeated by the validator. Direct-field terms still resolve once without
constructing a compiled expression. Successful programs, directions, duplicate
slots and invalid-logical-plan diagnostics remain unchanged.

The existing PreparationWork reserves exact ResolvedOrderField backing before
construction. Direct resolution charges one plus field-label bytes; expression
seam validation charges one per visited node before descending an input-admitted
tree. Rejection occurs before replacing the static contract or publishing cache
artifacts. Actual changed-binding builds charge these owners; identical memo
hits do not. No new resource, limit, route or retained request handle is added.

This is an order-preparation outcome within slice 11, not full order boundedness:
schema lookup internals, expression compilation/copies and referenced-slot
construction remain open. Shared preparation, diagnostics and `.explain()` are
not closed by this handoff.

Complexity: eight files, approximately +170 net Rust lines, primarily new
boundary tests; production code is 22 lines smaller. One unused expression
render and the duplicate expression-field lookup pass are removed. The same
compiler, request resources and execution route remain. Raw-Wasm bytes,
function count and IC cycle deltas were not measured; no numerical performance
improvement is claimed.

Qualification: 99 focused tests pass (17 index/order/cardinality, 56 session
admission/cache/ordering, 26 fingerprints). Focused all-feature lint, SQL-free
core checking, formatting and whitespace checks pass on Rust 1.98.1. The initial
new fixture omitted pagination ordering; corrected before the passing rerun.
Full suites and canister artifact/performance qualification were not run.
Applications, Cargo versions and local network lifecycle remain unchanged.

## Previous handoff: shared static index metadata (0.257.3)

Static execution metadata previously walked the selected index key twice,
resolving the same accepted root slots into an execution slot map and predicate
compile targets. One helper now constructs both arrays from a single key walk
and one schema slot lookup per component. Their order, expression-operation
tags, repeated root slots and optional availability remain the same; persisted
and public shapes do not change.

The helper receives the existing planner PreparationWork. It reserves both
vectors at the known key arity through TemporaryBytes and charges one plus
field-label bytes to PredicateExpressionSteps before splitting each root.
Budget exhaustion propagates as QueryError, separately from unavailable access
metadata, and cannot publish a partial static contract or cache artifact.
Schema lookup internals and the other static-projection owners remain open.
No new resource, ceiling, mode, request root or retained accounting handle is
introduced. This converges two construction passes within slice 11; it does
not close shared preparation or introduce `.explain()` yet.

Complexity: eight files in this handoff, approximately +180 net Rust lines,
mostly boundary tests and updated cache-budget expectations. Production code
is about four lines smaller and has one key traversal instead of two; both
destination vectors reserve exact arity. Identical memo hits still skip this
construction; changed bindings pay for rebuilt static metadata, not another
candidate-array copy. Raw-Wasm bytes, function count and IC cycle deltas were
not measured; fewer lookups alone is not a measured performance result.

Qualification: 96 focused tests pass across index/cardinality/cache lifecycle,
session ordering/admission and query fingerprints. The adjusted template test
also passes after its fixture extraction. Maintained lint, focused all-feature
lint, SQL-free core checking, formatting and whitespace checks pass on Rust
1.98.1. Initial fixture-identity, stale budget-expectation and lint failures
were corrected. Full suites and canister artifact/performance qualification
were not run. No application, Cargo version or network lifecycle changes.

## Previous handoff: retained grouping-key construction accounting (0.257.3)

The existing request PreparationWork now reaches planner assembly/finalization,
parameterized rebinding, trivial planning and cardinality reselection/pins.
Cold and changed-binding paths pass the same guard already owned by the cache;
there are no nested instruction intervals, new request roots, optional modes or
budgets retained in detached plans. Frontend grouping uses the same key owner.

GroupField construction charges label traversal and path string/vector copies
before allocation. Direct keys continue sharing accepted labels/type metadata.
GroupFieldSet insertion charges each duplicate comparison and destination
backing; direct-to-path promotion reserves once before moving the existing
tuple. Failed insertion leaves the tuple unchanged. Rebinding returns a typed
budget error separately from unavailable schema resolution, before publishing
new grouped fields, cache artifacts or bound memos.

Units use existing resources: PredicateExpressionSteps charges one plus label
bytes for label scans/text copies, one per direct identity comparison, one plus
both path-label byte lengths for path comparisons, and one per promoted handle.
TemporaryBytes charges requested vector backing and copied UTF-8 bytes through
the existing preparation helpers. Existing resource ceilings are unchanged;
previously uncharged grouping work can now exhaust them. Schema lookup internals,
other planner cloning/validation, candidate scoring and remaining cache checks
are still open. This is not full preparation boundedness or a typed terminal.

Qualification: 119 focused tests pass across schema/key, grouping insertion,
intent, session/cache, cardinality/pinned routes and fingerprint coverage.
Two new fixtures initially stopped at existing semantic admission (a nominal
nested leaf and a grouped query without an aggregate); corrected scalar-leaf
and COUNT fixtures pass the intended budget boundaries. Focused all-feature
lint, SQL-free checking, formatting and whitespace checks pass on Rust 1.98.1.

Scope: sixteen files, approximately +290 net Rust lines, mostly tests and
explicit budget propagation. Accounting/error plumbing grows, while grouping
keeps one resolver and comparator; promotion now reserves once before moving
the old tuple. No new behavior mode or resource ceiling is introduced. Extra
charges and periodic instruction watermarks have runtime cost; raw-Wasm, gzip,
function-count and IC cycle/instruction deltas are unmeasured. No performance
win is claimed. Full suites and IC measurements were not run; applications,
Cargo versions, networks and prior worktree changes remain untouched.

Changed-file inventory (relative to the repository root):

- `crates/icydb-core/src/db/query/plan/`: `group_field.rs`, `pipeline.rs`,
  `semantics/logical.rs`, `validate/symbols.rs`.
- `crates/icydb-core/src/db/query/intent/`: `model.rs`, `query.rs`, `state/tests.rs`.
- `crates/icydb-core/src/db/session/query/`: `cache.rs`, `cache/template.rs`,
  `cardinality_tiebreak.rs`.
- `crates/icydb-core/src/db/schema/info.rs` and
  `crates/icydb-core/src/db/session/tests/unit_ordering/planner_watermark.rs`.
- `CHANGELOG.md`, `docs/changelog/0.257.md`, this status document and
  `0.257-planner-accounting-handoff.md` beside it.

## Previous handoff: planner-owned metadata finalization (0.257.3)

Retained-key authority propagation exposed an executor-side metadata construction
fallback and an unconditional second route-profile projection. All five
maintained prepared-plan callers are in the shared session cache: pinned,
ordinary cold, changing bindings, parameterized cold and trivial scalar loads.
Their planner paths already finalize route/static metadata before executor
handoff. Exact-cardinality reselection also uses the canonical planner finalizer.

The executor handoff now requires a finalized static contract before lowering
or retaining state, and consumes the planner's existing route profile unchanged.
Incomplete internal handoffs return the existing typed query invariant error;
they cannot begin unaccounted construction or translate future preparation
exhaustion into an invariant error. No new error variant, public mode, request
root, compatibility route or query restriction is added. The state-space change
removes a construction route; the existing planner remains the canonical owner.

This closes that duplicated finalization boundary within slice 11. Fine-grained
key/path, candidate and cache-validation work remains open; diagnostic bounds
and the typed `.explain()` terminal remain subsequent outcomes.

Qualification: 67 focused session/cache and cardinality tests pass, including
the new finalized-handoff boundary, grouped/bound queries, exact-cardinality
reselection and pinned continuation reuse. Focused all-feature lint, SQL-free
checking, formatting and whitespace checks pass on Rust 1.98.1.
Scope is eight files: -33 net production Rust lines and about +62 test lines,
plus release/status documentation. The implementation is simpler: one metadata
construction owner and one consumer handoff. One route-profile recomputation
per new prepared shell is removed; raw-Wasm, gzip, function-count and IC
instruction/cycle deltas are unmeasured. No numeric speed/size claim is made.
Full suites and canister measurements are not run. Existing worktree changes,
application files, Cargo versions and network lifecycle are left unchanged.

## Previous handoff: shared direct-key labels (0.257.3)

The next retained-key inspection found a removable allocation rather than work
that needs its own metered resolver. SchemaInfo now owns shared immutable field
labels, and the existing FieldSlot resolver returns label/slot/type authority in
one lookup. Grouping, aggregate targets, covering fields and schema/plan clones
all reuse that label. No new mode, resolver, request root or cache identity is
introduced. Detached slots retain only their field's label and kind, not the
whole schema or session. Scalar-path construction remains separate existing work.

The trade-off is one shared-allocation header per schema label and reference
count operations instead of repeated string allocations/copies. Unresolved
expression labels still require owned construction. Retained-cache accounting
includes both shared allocation headers and payloads for each resident, even
when allocations are shared; shared memory is not declared free. Successful
key content/equality and current-schema rebinding remain unchanged.

This is a concrete allocation removal within slice 11, not its completion or a
typed terminal. Request-authority propagation for remaining key/path work,
candidate work and cache validation still precedes diagnostic bounds and
`.explain()`. No new planner or diagnostic-only query limit is introduced here.

Qualification: 92 focused tests pass (12 schema/key, 54 session/cache and 26
fingerprint tests), alongside the maintained lint target, focused all-feature
test lint, SQL-free checking, formatting and whitespace checks on Rust 1.98.1.
Initial String-specific conversion and const-constructor findings were repaired.
This landing changes seven files, about +9 net Rust lines plus release/status
documentation. Ownership is simpler for resolved labels; there is still one
resolution flow. Cycle/instruction, raw-Wasm, gzip and function-count deltas are
unmeasured, so no numeric performance improvement is claimed. Full suites and IC
measurement were not run. Existing metrics/native-driver changes are preserved;
applications, Cargo versions, networks, commits and pushes are untouched.

## Previous handoff: construction completion accounting (0.257.3)

The user reported 0.257.2 pushed and authorized continuation. Its release notes
are unchanged; 0.257.3 is the new active changelog patch. Cargo versions remain
under release-tooling ownership.

Ordinary/trivial cold compilation, changed-binding rebuilds and pinned plan
construction now finish a request PreparationWork instruction interval before
cache publication, bound-memo replacement or return. Parameterized cold
construction widens its existing template interval instead of nesting another
one. Reusable artifact/memo hits still skip construction; no cache identity,
authority, cardinality freshness or continuation-selection rule changes.

This is real completion accounting, not a boundedness closeout for slice 11.
It does not interrupt uncharged inner work. Earlier schema/planning-state work,
cache validation and detailed retained-key/candidate allocation and traversal
charges remain open. No new resource, mode, execution route or retained request
borrow is introduced; the existing guards must be passed onward as those owners
are made fallible, without introducing nested instruction intervals.

Validation passes on Rust 1.98.1: 66 focused cache/session and cardinality tests,
the maintained full lint target and focused all-feature test lint, SQL-free
checking, formatting and whitespace checks. Initial test import and redundant
clone findings were repaired before the passing checks. Host tests inject an
already-exhausted instruction counter to exercise completion failure; they are
not IC performance measurements or evidence of an in-progress hard interruption.

Scope: seven files, about +21 production and +217 test Rust lines, plus release
and handoff documentation. Control flow adds checks using an existing guard,
with no parallel planner. Previously unguarded construction paths now read the
instruction counter at entry/exit; fast hits do not gain those readings. Raw
Wasm, defined-function and instruction/cycle deltas are unmeasured. Full suites
and IC measurement were not run; no network lifecycle, commit or push action
was performed. Retained-key accounting remains the next implementation boundary.

## Explain pre-implementation gates

| Gate | Required evidence | Status |
| --- | --- | --- |
| Authorization | Reported 0.256 closeout followed by explicit user direction to start runtime work on 0.257. | Met 2026-09-08. |
| Shape validation | Exact shared owner for scalar/grouped misuse and grouped limits, with cursor rejection before lossy conversion. | Existing checks identified in dynamic session terminals; no extraction implemented. |
| Ordinary-query isolation | Diagnostic-only ceilings cannot change ordinary admission or identity. Shared input/preparation limits apply to ordinary queries too; admitted identity stays unchanged against slice 7. | Shared preparation approved; diagnostic projection/hash isolation qualification remains pending. |
| Cache freshness | Execution-planner validity/invalidation contract, not forced fresh optimization; distinguish count changes from invalidating lifecycle transitions. | Source contract identified; lifecycle tests pending. |
| Diagnostic construction | Fixed numeric depth/work/payload/render limits; enforcement before or during allocation; cleanup/drop qualification; specific missing checks at each owner. | Typed and SQL input limits and iterative owned-input cleanup pass native qualification in slices 8–9. Planner-produced construction/work and diagnostic projection/render limits remain pending. |
| Detached rendering | Fallible canonical writers enforce output incrementally for every invocation; DTO stays detached from session/request budget. | Hard cut approved; implementation pending slice 5. |
| Cost baseline | Immutable source/toolchain/features, matched actor/workload matrix, raw-Wasm/function/cycle ceilings established before production edits. | Source/lock/toolchain frozen; SQL-free baseline build passes. No performance measurements or ceilings: scope decision precedes numerical cost acceptance. |

## Verification and handoff

### Initial lowering (slice 2)

One balanced helper replaces both initial folds in `FilterExpr` lowering. For
128 comparison operands, the initial tree has seven junction levels instead of
127 (nine total expression levels including comparison and literal/field leaves).
Empty identities, source order and schema conversion are preserved. The only
production caller immediately canonicalizes before adding the expression to query
intent; canonical-association parity tests protect that unchanged boundary.
This does not change SQL parsing or make later canonical trees shallow.

Validation passes: 35 SQL-free filter/fingerprint tests; 51 all-feature
filter/fingerprint/SQL-predicate tests; SQL-free and all-feature focused lint;
the complete prescribed repository lint gate; formatting and whitespace.
An extra SQL-only test-target lint attempt fails with 64 unused migration-helper
errors outside this edit. The maintained SQL-only library lint passes; combined
SQL/migration test-target lint also passes. No unrelated migration code was
changed to silence the extra configuration. Root and detailed 0.257.0 notes are
current. No application edits or network lifecycle actions were needed.

Complexity: one Rust file, 15 fewer production lines and 191 net test lines;
six files in the full handoff including the earlier preflight receipt and
design/status/changelog updates. No new mode, limit, public API or runtime
authority. Cycle/Wasm measurements and full suites were not run.

### Explain preflight (slice 1, still pending)

Shared-normalization verification (slice 3) is recorded below; the explain
construction/renderer/authority gates remain open despite these improvements.

Preflight validation: SQL-free library build passes; shallow recursive cleanup
and deep iterative-cleanup controls pass. Deep recursive cleanup aborts the
isolated native probe after validation rejects, as recorded in the receipt.
This is not an implemented `.explain()` test or a Wasm stack measurement.
Local Markdown links and whitespace pass. No full suite, IC measurement,
downstream build or network lifecycle action ran. Existing 0.256 validation
results are not evidence that this proposed API exists.

After implementation, record focused SQL-free/parity/bounds/authority tests,
ordinary-read controls, cost and complexity deltas, and the active changelog
entry here before handoff. No additional feature is reserved in this line. If
the bounded-diagnostics prerequisite proves independently substantial, stop and
revise scope with the user rather than widening slice 1 invisibly.

### Shared normalization (slice 3)

The canonicalizer visits same-operator input groups before normalizing their
terms, then sorts/deduplicates/rebuilds once. Its existing flatten-only helper
handles groups exposed by normalization without re-normalizing their terms.
Final left association, comparator, deduplication, NULL and comparison rules
remain unchanged; recursive traversal and output disposal are not newly bounded.

Validation: the three direct canonical-output tests pass on baseline and
candidate. Final focused runs pass 46 SQL-free and 75 all-feature tests across
normalization, filters, fingerprints, intent and SQL lowering. The manual timing
test is ignored in those runs and passes when explicitly selected. The prescribed
lint gate and focused SQL-free lint pass; an initial test-helper style warning
was fixed before repeating the focused gate. Formatting/whitespace pass.

The [measurement receipt](0.257-normalization-perf.md) records frozen source and
binary identities, workload/timing boundaries and repeated baseline/candidate
runs. Balanced 64/128-term synthetic input takes about 83% less native time in
this pass; this is not IC-cycle or end-to-end query evidence. No full suites,
Wasm measurements, application edits or network lifecycle actions ran.

Complexity: seven files in this slice, 18 net production lines in the existing
canonicalizer plus 146 test/timing lines and three test-wiring lines. One
same-operator group collection replaces repeated subtree normalization; no new
mode, authority, public API, representation or collection buffer is added.
At the slice-3 handoff, compact typed membership and fluent append cloning
remained unimplemented; membership is now completed below.

### Compact typed membership (slice 4)

Nonempty typed `IN`/`NOT IN` moves its admitted list into the existing compact
operation through a constructor also used by SQL. This supersedes the membership
part of slice 2; the balanced junction builder now serves only `AND`/`OR`.
Empty typed lists keep their constant identities. Collection operands and mixed
coercions reuse the existing leaf-comparison predicate path, with no expression
tree re-expansion or new coercion authority.

The user explicitly approved the fingerprint/continuation-signature hard cut.
Restart affected pagination after deployment. The cursor format remains version
1, with current signature-mismatch rejection and no predecessor hash or decoder.

Validation passes: 55 SQL-free and 102 all-feature focused tests covering filter
lowering, canonicalization, fingerprints, cursor behavior, intent, SQL lowering,
stored scalar parity, schema rejection and A/B/A cache rebinding. The two manual
timing probes are ignored in normal SQL-free selection; membership timing passes
when explicitly run on both frozen binaries. Maintained repository lint and
focused all-feature lint pass after correcting one test-helper clone warning.
Formatting and whitespace pass. Full suites remain user-owned.

The [membership measurement receipt](0.257-membership-perf.md) isolates lowering,
normalization and predicate derivation. At 128 values, repeated native preparation
falls from about 528 microseconds to 6.8 microseconds. The baseline already
includes slices 2 and 3; this is not an additive percentage, full-query latency
or IC-cycle claim. No Wasm measurements, application edits, Cargo changes or
network lifecycle actions were needed.

Complexity: 13 files in this slice, seven fewer net production lines across five
owners, plus 277 net test/timing/wiring lines. Typed and SQL construction converge
on one existing representation, with no new mode or public API. The complete
pending worktree spans 17 files including previous slices and preflight. Active
root/detailed 0.257.0 notes are current. This handoff ends slice 4; fluent append
ownership and the explain/bounds prerequisites remain separate work.

### Shared diagnostics construction preflight (slice 5, pending)

Recorded the approved two-outcome split without renumbering earlier slices.
Inspection identifies an ordinary-query normalization/label-rendering feedback
path: sorting already-normalized operands invokes a helper that clones and
normalizes them again. Nested one-arm boolean `CASE` also duplicates conditions;
the local eight-arm cap is not a total expanded-node or work cap.

The [construction receipt](0.257-diagnostics-preflight.md) records source owners,
existing numeric limits and the controlled native probe. Six nested conditions
grow from 19 nodes/depth 7 to 694 nodes/depth 25. With debug assertions disabled,
that case takes about 279 ms in the opt-level-1 probe; this is not production
latency or IC-cycle evidence. The debug-enabled experiment completed that case
in about 37 seconds and was terminated during the deeper case, so that run is
not a passing test gate. The bounded debug-disabled control passes.

All temporary test-source edits were removed; the prior test file's exact hash
was restored. Four design/status/receipt files change in this handoff, with no
production, public API, Cargo, application or release-note change. Formatting,
whitespace and local document links pass. No full suite, production-profile
benchmark, Wasm/IC measurement or network lifecycle action ran. The split is
adopted, but neither bounded diagnostics nor typed `.explain()` is implemented.

The preflight recommended a separate owner-local normalization repair, first
proving unchanged canonical ordering/bytes/fingerprints and measuring the same
nested input. The user subsequently approved that outcome as slice 6. This
does not adopt a new CASE policy, relax diagnostic guarantees, or introduce a
duplicate expansion estimator merely to close the preflight.

Resumed after slice 6: recommend extending the existing CASE rewrite budget to
produced size and retaining the already-supported compact CASE when it would
exceed the cap. This avoids new ordinary-query rejection and an EXPLAIN-specific
copy of expansion rules, but may change canonical identity and plan selection.
It is not authorized by slice 5's identity-preservation contract; request explicit
approval before implementation or adding a separate tracker outcome. The updated
construction receipt records the alternative and disproves a suspected SQL
literal-REPLACE expansion path: its first operand must parse as a field. The
temporary probe failed at parsing, was not retried, and was fully removed.
Four focused canonicalizer tests pass, with one manual probe ignored. No net
runtime edits, release-note changes or new performance measurements result from
this preflight; the bounded-diagnostics feature remains unfinished.

### Non-reentrant boolean ordering (slice 6)

The comparator renders already-normalized operands directly with the existing
scalar renderer. Its two callers establish child normalization before comparing;
the general filter-label helper remains unchanged for other consumers. There is
no normalization/cloning re-entry from sorting, new renderer or sort-key cache.
Label ordering, Debug tie-breaking, deduplication, CASE expansion and canonical
identity remain unchanged. This slice introduces no additional hard cut.

Matched baseline/candidate canonical-model and label receipts agree. Final
focused validation passes 80 SQL-free and 139 all-feature tests, including SQL
lowering, accepted scalar parity, cache rebinding, verbose explain cache reuse,
fingerprint and cursor contracts. Two older manual probes are ignored in each
normal run; the new nested-CASE probe is compiled only with debug assertions
disabled and passes in four explicitly selected frozen-binary runs. Maintained
repository lint and focused all-feature lint pass after replacing a constant
test assertion with that compile-time benchmark gate. Formatting, whitespace and
local document links pass.

The [ordering receipt](0.257-ordering-perf.md) records the exact source/binary
identities and timing boundary. Six-level nested-CASE native preparation falls
from about 270 ms to 1.2 ms (99.6% less time), with debug assertions disabled in
both matched binaries. This is not an IC-cycle or end-to-end query result. No
full suites, Wasm/function/cycle measurements, application changes, Cargo edits
or network lifecycle actions ran.

Complexity: eight files in this slice; unchanged executable production line
count, two added intent-comment lines and 126 net test/timing lines. The flow is
simpler because an already-completed preparation stage is no longer re-entered.
The complete pending worktree spans 19 files including earlier slices and
preflights. Root/detailed 0.257.0 notes are current. Stop at this completed
handoff; next resume approved bounded diagnostics (slice 5), not typed explain
or a new CASE expansion policy.

### Bounded CASE rewrite (slice 7)

The user explicitly approved the CASE policy and fingerprint/pagination hard
cut. One fixed, content-based budget charges introduced nodes, both condition
copies and variable payload before expanding. Larger rewrites retain compact
CASE. The duplicate expansion in predicate compilation is removed; no new
evaluator, query-rejection rule, format or compatibility identity path is added.
Predicate access opportunities can change, including those of neighbouring
conjuncts under the existing whole-expression projection contract. Existing
execution admission is unchanged; there is no scan exemption.

The [CASE receipt](0.257-case-budget.md) records limits, exact source/binary
identities, qualification corrections and all five matched timing pairs.
Final validation passes 174 all-feature tests and 58 SQL-free expression tests,
with one/two manual benchmarks ignored respectively. Maintained three-lane
lint, focused all-feature lint, formatting, whitespace and local links pass.
Ten explicit native timing processes pass the unchanged cost ceilings: six
nested CASE levels fall from 1.23 ms to 0.10 ms; smaller cases remain near their
prior timings. This is not an IC-cycle or full-query result.

Twelve files in this slice, approximately 122 net production lines and 227
test/wiring lines plus docs. A private accounting walk is added while one
duplicate expansion flow is deleted. The full pending worktree spans 24 files;
active root/detailed 0.257.0 notes are current. Full suites remain user-owned;
no Wasm/IC measurements, application/Cargo edits or network lifecycle actions.
This is the completed slice-7 handoff. Next resume slice 5 against the current
hard-cut representation, not the still-pending typed terminal.

### Post-CASE diagnostic scope review (historical proposal, superseded)

The resumed review inventories the still-shared preparation/candidate work and
the separate diagnostic projection/formatting allocations. Recommendation:
retain ordinary planning's existing limits and bound the additional diagnostic
work in this release. The full construction proof remains part of the currently
approved design until that narrowing is authorized; neither bounded diagnostics
nor typed `.explain()` is reported complete.

Three design/status/receipt files change; no runtime, API, release-note, Cargo
or application changes. Fourteen existing explain-related tests pass on the
current all-feature binary; whitespace and local document links pass. These
are current-surface regression checks, not new limit tests. No full suite,
lint rerun, performance/Wasm/IC measurement or network lifecycle action ran.

### Shared typed input admission (slice 8)

The user selected shared bounded preparation and fallible rendering, not the
historical projection-only proposal. The tracker preserves completed slices and
adds separate typed-input, SQL-integration and preparation-work outcomes before
the pending diagnostic and typed-terminal handoffs.

One fixed borrowed-input budget admits depth 128, 4,096 nodes and 2 MiB of variable
payload across each typed/dynamic request before structural conversion. Filters
lower by reference, removing the prior whole-filter clone. Ordinary public and
trusted reads converge here; metadata-only exact count checks its input on the
existing bypass. No new semantic normalizer, execution route or authority is
introduced. Rejected owned requests detach recursive children during destruction;
the contract explicitly excludes caller construction/Clone/Debug/standalone DTO
drop, decoding and a fixed cleanup allocation/cycle bound for unadmitted input.

Final validation passes 74 all-feature core tests (input admission, accepted-row
and cache parity, fingerprints and cardinality/cursor lifecycle), eight SQL-free
input tests, three diagnostic-code round-trip/registry tests and one CLI hint
test: 86 total. The small-stack probe rejects and destroys 20,000-level filters
and values plus 10,000-level CASE input on a 512-KiB native stack. Maintained
three-lane lint and focused all-feature lint pass after making three cleanup
accessors const (two initial warnings and their direct propagation). Formatting
and whitespace pass. The initial broader input-name selection also passed 23
tests; those overlapping runs are not added to the final total.

Complexity: 24 files in this slice, approximately 470 net production Rust lines
and 280 test/wiring lines. This adds explicit admission and iterative cleanup,
while retaining one preparation/execution flow and removing one frontend clone.
The cumulative pending worktree spans 42 files including earlier slices. Active
root/detailed 0.257.0 notes are current. No performance improvement or Wasm-size
claim: benchmarks, IC cycles and Wasm/function measurements were not run. Full
suites remain user-owned; no application/Cargo edits or network lifecycle action.

This completes slice 8 only. Next: integrate the same policy into SQL preparation
and syntax ownership (slice 9), then shared work charging (10), bounded/fallible
diagnostics (5), and the typed terminal (1). The line is not at closeout.

### Shared SQL input admission (slice 9)

SQL now adapts authored syntax to the same depth/node/payload budget as typed
requests, replacing the depth-only lowering walker. Parsed query/write statements,
introspection and CHECK expressions are admitted before preparation. Current
compilation ingress checks retained/warm inputs; semantic normalization remains
accepted-schema owned, and DDL semantics remain catalog-owned. Existing binding
limits stay in place, with every effective scalar copy additionally charged before
substitution. Bound invocations retain their existing concrete-cache bypass.

The SQL expression owner now detaches recursive children during destruction,
covering partial parser failures as well as rejected and retained syntax.
Postfix parsing transfers its left operand, and BETWEEN admits both copies before
cloning. Ownership propagation preserves existing boxes/vectors and merges the
two scalar alias-rewrite traversals without changing their resolution policies.
Runtime-value cleanup is shared with typed requests rather than duplicated.

Final focused validation passes 308 tests: 211 SQL parser/lowering/input tests,
32 ordering/session tests, 26 fingerprint tests, 31 cursor tests and eight
SQL-free input/cleanup tests. These include exact limits, all query/write families,
effective binding copies, no-reparse A/B/A execution after rejection and deep
parser-error cleanup on a 512-KiB native stack. Initial execution-fixture failures
were corrected to use supported numeric CASE comparisons and explicit ordering;
no language or pagination policy was widened. Maintained three-lane lint and
focused all-feature lint pass after fixing ownership-propagation style warnings.
Formatting and whitespace pass.

Complexity: 27 files in this slice, approximately 25 net production Rust lines
and 225 test lines, plus documentation. Input checks and a syntax destructor add
explicit safety work; removing the old depth walker and duplicate alias traversals
keeps one admission authority and one preparation flow. The cumulative pending
worktree spans 63 files including earlier slices. Root/detailed 0.257.0 notes are
current; applications and Cargo versions remain unchanged. No full suites,
benchmarks, Wasm/function counts, IC-cycle measurements or network lifecycle
actions ran. No performance or Wasm-size change is claimed.

This completes slice 9 only. Next is actual shared preparation-work charging
(slice 10), then bounded/fallible diagnostics (5) and the typed terminal (1).
Authored-input limits do not bound planner-produced alias expansion or all
normalization work, and native cleanup tests do not establish Wasm stack safety.

### Preparation-work preflight (slice 10, implementation pending)

The [work-charging receipt](0.257-preparation-work-preflight.md) identifies two
independent owner changes: fallible expression preparation/ordering with safe
intermediate cleanup, and access-planning candidate/cache-work accounting.
The existing active-execution charge helper can no-op before execution, and
the request's 1,024 `PlanningSteps` limit currently counts planning operations,
not visited expression nodes. The receipt proposes existing-resource attribution
and a two-handoff split; neither is silently implemented as a new budget mode.

The user subsequently approved retaining slice 10 for fallible expression
preparation and adding slice 11 for ordinary access-planning/cache work, then
resuming 5 and 1. This follows the design's explicit rescope gate
before broad planner changes. No preparation-work completion is claimed.

Seventy-six existing focused budget/scope/canonicalization/fingerprint/cursor
controls pass; one manual timing probe is ignored. Three design documents change,
with zero runtime line delta. Whitespace and local document links pass. No
rebuild/lint rerun, full suite, benchmark, Wasm/IC measurement, application/Cargo
edit or network lifecycle action ran. Existing slice-9 release notes remain
unchanged because this handoff implements no release behavior.

### Expression ownership progress (slice 10, still in progress)

`Expr` now owns iterative destruction of planner trees, including intermediate
results. Dynamic-request cleanup delegates to this owner instead of maintaining
its own expression walk. Scope-preserving child transfer replaces rebuilding
and recursive CASE-child cloning in the affected boolean, CASE, aggregate-input,
affine and SQL normalization owners. The existing SQL syntax destructor remains
responsible for its distinct frontend type. No new variant, compatibility path,
semantic normalizer or request lifecycle is introduced.

This is not completion of slice 10: normalizers and ordering remain infallible,
and no new request-work charges are connected. An unused counter prototype was
removed rather than retained as production scaffolding. Next work must hard-cut
those signatures, wire the existing request authority, and qualify exhaustion
and costs before slice 11 begins. Existing operation-count limits are unchanged.

Nineteen files change in this progress checkpoint, approximately 200 fewer net
production Rust lines plus 52 new test lines. The native small-stack test disposes
of 20,000-level unary/binary/CASE/function/aggregate trees and nested values.
This proves disposal in that test environment, not recursive Clone/Debug safety,
bounded total preparation or a Wasm stack limit. Active 0.257.0 release notes
record only the implemented ownership changes.

Final focused validation passes 445 tests: 378 all-feature expression, SQL,
session, fingerprint, cursor, input and filter tests, plus 67 SQL-free expression
and input tests. Three manual timing selections are ignored. The maintained
three-lane lint gate and final focused all-feature lint pass after removing the
unused child-transfer helper and making two accessors const. Formatting,
whitespace and local document links pass. Full suites remain user-owned; no
benchmarks, Wasm/function counts, IC-cycle measurements, application/Cargo edits
or network lifecycle actions ran. This checkpoint does not close slice 10.

### Typed lowering and streaming-label progress (slice 10, still in progress)

Typed filter lowering now borrows the session's actual request scope and returns
typed exhaustion through the existing structural conversion path. Frontend node
visits, raw literal visits and raw text/vector construction are charged before
work. Instructions are sampled periodically and on completion, including errors.
The [preflight](0.257-preparation-work-preflight.md) records exact units and the
remaining uncovered owners. No root reset, optional metering mode or cache-held
budget is added. Public/trusted, grouped, exact-key candidate/fallback and filtered
exact-count conversions preserve their current routes.

Planner labels share one streaming writer rather than intermediate strings and
argument joins. Existing String consumers use that writer; a fallible sink can
stop at the first rejected write. This is not yet request-limited canonical
sorting, stack-bounded formatting or the public explain-renderer hard cut.

Direct tests cover nested-value exhaustion, cumulative retries through different
sessions sharing a root, cold/warm caches, public/trusted attribution and rejection
before row execution. Output-boundary tests preserve text escaping, precedence,
field paths and aggregate FILTER/DISTINCT identity. SQL-free and all-feature
filter/builder/expression controls pass, as do SQL, session, request, execution
budget, fingerprint and cursor controls: 501 focused passes across both feature
configurations, with four manual timing selections ignored in normal runs. The
maintained three-lane lint gate, focused all-feature lint, formatting and
whitespace checks pass. Initial compile errors and two fixture failures were
corrected before these final checks. The matched canonical-model/label receipt
remains `ac4fd65e7c16849940ffd1352c3ca10433696f2b8ae2fc36cf15ec3eceaf95aa`.
The existing A/B/A cache test now checks non-growing retained bytes rather than
requiring fresh buffers and cloned strings to have identical capacity. The new
budget fixture reuses its canonical registry constructor so it tests request
exhaustion, not admission against a different recovery domain.

Native cost qualification uses the existing typed-membership preparation probe,
five alternating before/after runs, each reporting the median of seven batches
of 64 calls. Query construction and request-root creation are outside the timer;
lowering, normalization, predicate extraction and disposal are inside. The new
borrowed work segment is inside the timer. Both binaries use the existing native
dev/test profile (opt-level 1, debug assertions enabled), all features and
`rustc 1.97.1 (8bab26f4f 2026-07-14)`.

| IN values | Before median | After median | Time increase |
| --- | ---: | ---: | ---: |
| 4 | 533 ns | 797 ns | 49.5% |
| 16 | 1,425 ns | 2,252 ns | 58.0% |
| 64 | 4,079 ns | 7,038 ns | 72.5% |
| 128 | 8,136 ns | 14,448 ns | 77.6% |

These are accounting costs, not a claimed speedup. The 128-value change is about
6.3 microseconds in this native preparation-only probe. It does not measure the
renderer improvement separately, ordinary read latency, IC cycles or Wasm size.
The before binary is the immediately preceding ownership checkpoint, not clean
HEAD alone. Frozen binary SHA-256 values:

- Before: `a7138f7c287adaa8b579eeb166aecd6ccdc098b5f28d2e28e6834a39228dda38`.
- After: `d83660f86c05ba50f60607af80b4336e2cfa7bf35e938bf7d70e0b4394f98368`.

This checkpoint touches 16 files including four documentation files, adding
approximately 170 production Rust lines and 300 test/wiring lines. Explicit
fallibility/accounting adds implementation complexity; streaming removes repeated
string construction. There is still one conversion route and one label grammar.
No Cargo/application changes, full-suite runs, IC/Wasm measurements or network
lifecycle actions were performed. Slice 10 remains open: canonical
normalization/ordering, SQL work and complete construction accounting are not
finished. Slice 11 and the explain slices have not started.

### Fallible canonical preparation (slice 10, still in progress)

Typed intent and SQL preparation now use the same fallible canonicalizer and
borrowed request scope. Canonical ordering charges comparisons, streamed key
bytes and requested buffer allocations; it sorts indices before permuting owned
operands, so rejection never supplies a partial key or fabricated ordering.
Successful label/Debug order, deduplication, CASE policy and left association
remain unchanged. Comparison reorientation/function arguments reuse allocations,
filter labels no longer clone/re-normalize, and HAVING append removes its unused
CASE-policy choice. Predicate-only filters avoid expression preparation.

SQL command misses and bound calls charge normalization actually performed;
valid concrete-command hits retain their compilation savings. Aggregate trial
lowering propagates query failures, including exhaustion, instead of treating
them as a shape miss. SQL read, explain and mutation preparation use their
existing trusted-read, diagnostic and mutation attribution. Current authority
resolution still precedes cache lookup.

Validation passes 602 focused tests: 505 all-feature and 97 SQL-free. These cover
expression/intent/filter/builder owners, SQL lowering and policies, accepted
session execution, cold/warm exhaustion, bound A/B/A rejection, request counters,
fingerprints and cursors. A 128-KiB native-stack probe rejects normalization and
safely disposes of a 20,000-level remainder; this is not a successful-traversal or
Wasm-stack guarantee. Four manual timing selections are ignored in normal runs;
the two measured probes pass when explicitly selected. Maintained three-lane
lint, focused all-feature lint, formatting and whitespace pass after correcting
test migration and lint issues. No validation failures remain.

Five alternating baseline/candidate runs use the existing all-feature native
opt-level-1 profile with debug assertions and rustc 1.97.1. Each sample is the
median of seven batches of 64 operations; table values are medians across five
runs, in nanoseconds. Boolean fixtures are reverse-ordered distinct field terms;
input cloning and finite-request construction are outside the timer, while
normalization, its debug checks and output disposal are inside. Each measured
shape has its own finite request outside timing. Membership includes accepted
literal lowering, normalization, predicate derivation and disposal; its borrowed
work segment is inside timing. No other validation/build was running.

| Terms / IN values | Left-chain normalization, before → after | Balanced normalization, before → after | Compact IN preparation, before → after |
| --- | ---: | ---: | ---: |
| 4 | 718 → 953 | 714 → 935 | 736 → 822 |
| 16 | 7,042 → 5,093 | 6,946 → 4,975 | 2,149 → 2,269 |
| 64 | 12,529 → 24,886 | 12,303 → 25,819 | 6,623 → 6,762 |
| 128 | 31,999 → 54,239 | 29,481 → 57,329 | 13,025 → 13,112 |

This is a safety/accounting cost, not a general optimization claim: 128-term
normalization rises 69.5%/94.5% (about 22/28 microseconds), while 128-value compact
membership is approximately unchanged in this probe (+0.7%). These are neither
release-profile/full-query results nor IC-cycle measurements. Baseline is the
immediately preceding typed-lowering/streaming-label checkpoint:

- Before binary SHA-256: `d83660f86c05ba50f60607af80b4336e2cfa7bf35e938bf7d70e0b4394f98368`.
- After binary SHA-256: `1935ff044585247d511d8fd3fa2278eda916f92c977485f10557c3ea88c49058`.
- Both produce canonical receipt `ac4fd65e7c16849940ffd1352c3ca10433696f2b8ae2fc36cf15ec3eceaf95aa`;
  the candidate now asserts that receipt as well as comparing semantic shapes.

Complexity: 46 touched files, including 42 Rust and four documentation files.
The new ordering owner adds 149 production lines and 140 direct-test lines;
remaining changes propagate the hard-cut signatures, reuse ownership and qualify
boundaries. Across these affected Rust paths, the pending delta from clean
0.256.1 is approximately +2,000 lines **including earlier 0.257 work**, not an
isolated delta for this checkpoint. Explicit fallibility and sorting machinery
add implementation complexity, with no new public mode, cache key, budget root,
normalizer or execution route; redundant re-preparation and one unused policy
are removed.

The [preparation contract](0.257-preparation-work-preflight.md) names remaining
uncharged copy/construction and classifier/deduplication work. Slice 10 is not
complete; slice 11 and typed explain have not started. No full suite, raw-Wasm
size/function count, IC-cycle measurement, Cargo/application edit, commit/push or
network lifecycle action was performed.

### Sort-key reuse and IC cost qualification (slice 10, still in progress)

The metered sorter now constructs each label once per term and each typed Debug
key only on its first label tie. Its charged, sort-local table is dropped before
operand permutation; request counters, canonical order and the abortable merge
remain unchanged. This removes repeated construction, not accounting. Retaining
all needed keys is an explicit concurrent-storage trade-off, not a new session
cache or an allocation-peak guarantee.

The [matched Wasm receipt](0.257-canonical-wasm-perf.md) compares frozen metered
before/after modules using the same canonical wasm-release audit actor. At 128
terms, application-built AND/OR instruction medians fall 25.24%/21.06%, and
whole-update cycles fall 17.79%/14.67%. Warm SQL keeps its compilation savings;
isolated IN controls match exactly. A shared-history IN instruction regression
disappears with fresh fixtures per workload and is retained as an investigative
finding, not silently discarded. Four-term AND instructions rise 0.26%: this is
not a blanket speedup. Raw Wasm grows 198 bytes and two defined functions.

The checkpoint spans nine files: +36 net core Rust lines, +33 audit-actor lines,
+180 test/harness lines, and documentation. One local table replaces repeated
rendering without adding an execution route or semantic authority. SQL AST/value
copies, CASE/projection construction, classifier/dedup work and traversal bounds
remain at the existing slice-10 boundary. Slice 11 and typed explain have not
started; application repositories and Cargo versions are unchanged.

Validation passes 524 focused native tests (460 all-feature and 64 SQL-free),
four isolated Wasm matrix runs, maintained/focused lint, formatting and
whitespace checks. Both full before/after replays reproduce every recorded
instruction/cycle sample exactly. The initial harness lint and flat-SQL fixture
issues are corrected; no failures remain. Full-suite validation is user-owned.
The measurement wrapper started/stopped isolated PocketIC servers and left
existing local networks alone. No commit, push or Cargo version change ran.

### Typed filter construction (slice 10, still in progress)

Typed lowering now charges copied field-name bytes and each introduced box or
function-argument slot immediately before allocation. Nonempty membership keeps
its shared compact constructor and value-vector ownership; empty membership and
empty junctions produce constants without planner backing storage. The repeated
text/collection branches now share charged field/function construction. No
accepted-schema coercion, canonical identity, frontend route or budget ceiling
changes.

Validation passes 192 focused native tests (179 all-feature and 13 SQL-free),
including exact/one-byte-short constructor limits and cold/warm public/trusted
retries rejecting before row execution without changing cached artifacts.
Canonical expression, fingerprint, cursor and intent controls pass, as do the
maintained three-lane and focused all-feature lint gates, formatting and
whitespace checks. Full-suite validation remains user-owned.

The [matched IC receipt](0.257-filter-construction-perf.md) reports under 1%
whole-update cycle growth across the measured matrix, +696 raw Wasm bytes and
four defined functions. One 16-term AND instruction case rises 4.655%
(+275,370), exceeding both existing review thresholds despite +0.240% cycles.
This is an explicit open performance-review flag, not a clean cost-gate verdict.
Further attribution or cost acceptance precedes calling this checkpoint's cost
qualification complete.
The three Wasm matrix runs pass functional checks, and both the frozen baseline
and candidate reproduce all recorded metrics exactly on their matched reruns.

Seven files change: +37 production Rust lines and +185 test/propagation lines,
plus five docs. Construction is less repetitive, with explicit fallibility but
no new execution route or runtime authority. Schema-directed value conversions,
SQL/canonical-copy construction, predicate projection/classification and
traversal bounds remain in slice 10. Slice 11 and typed explain have not started.
Only isolated PocketIC servers were started/stopped; applications, Cargo versions,
existing ICP networks, commits and pushes are untouched.

### Accepted-schema filter conversion (slice 10, still in progress)

The shared filter converter now borrows unchanged text/blob scalars and
recursively unchanged collections. Typed lowering retains its existing owned
value; a conversion materializes a separate list only at the first changed
element. All-element success is still required before replacing the original
typed literal. Introduced list buffers, borrowed-child copies, hex decode buffers
and enum-label copies charge before allocation, and scalar parsing consumes an
input-byte work allowance. Grouped HAVING retains its established partial
conversion semantics and propagates budget errors from the shared owner.

532 native tests pass, including 18 SQL-free controls. The maintained three-lane
and focused all-feature lint gates pass after correcting three style findings;
formatting and whitespace checks pass. No full-suite validation was run.

The [matched conversion receipt](0.257-value-conversion-perf.md) records
0.019–0.543% additional whole-update cycles and 0.050–1.172% additional
instructions across numeric AND/OR/IN controls. No new instruction review
threshold is crossed. SQL cold/warm samples remain identical. Raw Wasm adds
3,433 bytes and three defined functions. These controls measure accounting cost,
not text-copy latency; ownership tests establish unchanged-storage reuse.

Thirteen files change, about +161 production and +344 test/wiring Rust lines,
plus documentation. Ownership/error handling is more explicit, with fewer
redundant copies and no additional semantic owner or execution route. Scalar
parser internals, big-integer copies/encoding, deeper catalog admission and
grouped fallback copies remain open alongside the previously listed SQL/CASE,
predicate and traversal work. Slice 11 and typed explain have not started.
Applications, Cargo versions, commits and pushes remain untouched.
Three Wasm matrix runs pass; frozen baseline/candidate replays reproduce all 36
metric records exactly. Both measurement-owned PocketIC servers were stopped,
without changing existing local networks. A concurrent read-only ICYDB-022 check
confirmed the fix is already in IcyDB 0.256.1 while Toko Miner still pins 0.255.1;
no downstream dependency or issue record was changed.

### Big-integer literal validation (slice 10, still in progress)

The shared atom owner now reports exact signed/unsigned LEB128 lengths without
allocation. Filter conversion no longer clones a freshly parsed integer for
validation, and exact big values borrow unchanged storage. Strict SQL checks
exact input sizes before copying; its wider cast family is preserved. Necessary
container copies precharge a conservative magnitude-word allowance. Existing
text work charging remains, without claiming complete decimal-parser accounting.

143 focused native tests pass, including 58,443 encoder-oracle comparisons in
three atom tests. The initial concurrent session process was killed; its serial
retry passes. Maintained and focused lint, formatting and whitespace checks pass.
All four dedicated Wasm matrix runs pass and both frozen replays reproduce all
24 records exactly. Three measurement-owned PocketIC servers were stopped.

The [matched big-integer receipt](0.257-big-integer-perf.md) records lower warm
instruction medians in all six tested cells, but mixed cycles: −0.873% to
+0.605%. The unsigned 300-digit cycle increase remains visible. Matched raw Wasm
adds 375 bytes and loses one function; the audit fixture/probe separately adds
2,819 raw bytes and two functions. Fourteen files change, approximately +46
engine/atom and +254 test/measurement/wiring Rust lines. Conversion and size
validation are less repetitive, with no new semantic route or parser.

Full-suite validation remains user-owned. Applications, existing local networks,
Cargo versions, commits and pushes are unchanged. Decimal-parser internals and
other catalog encoding/copies remain open with the existing SQL/CASE, predicate
and traversal work. This checkpoint does not close slice 10 or start slice 11.

### Catalog big-integer size admission (slice 10, still in progress)

Accepted input normalization and canonical validation reuse the allocation-free
atom lengths instead of constructing four size-only encodings. A shared budget
helper preserves field-bound error precedence and exact five-byte overhead plus
payload charges. Actual persistence encoding and accepted families are unchanged.
This removes temporary work without claiming complete catalog request-work or
heap accounting.

The [matched structural-write receipt](0.257-catalog-admission-perf.md) records
0.394–3.074% lower warm instructions and 0.164–1.466% lower whole-update cycles
for three bounded widths. Raw Wasm shrinks 141 bytes, gzip shrinks 30 bytes and
defined function count is unchanged. Instrumentation separately adds 2,705 raw
bytes and five functions. All four successful Wasm runs pass; both frozen
replays reproduce all 12 records exactly. The first baseline build rejected a
concurrent test edit and was rerun with stable sources. Four measurement-owned
PocketIC servers, including that rejected attempt, were stopped.

54 focused native tests pass across all-feature, SQL-disabled and atom targets,
including 58,443 encoder-oracle comparisons. The maintained three-lane and
focused all-feature lint gates pass after replacing one test-only truncating
cast with checked conversion. Formatting and whitespace checks pass. Full-suite
validation remains user-owned.

Nine files change: four Rust and five docs. The production owner adds six net
lines and converges four repeated checks, with 175 additional test/measurement
lines. No new execution route or compatibility path is introduced. Applications,
Cargo versions, existing local networks, commits and pushes remain untouched.
Necessary catalog encoding/copies and the other preparation-accounting owners
remain in slice 10; slice 11 and typed explain have not started.

### Owned grouped HAVING conversion (0.257.1; slice 10 remains open)

Grouped-key literal normalization now mutates its owned lowering intermediate.
It retains list/set storage, unchanged scalars and unconverted children, removing
replacement containers and fallback clones. Grouped traversal charges current
request work; introduced scalar storage uses the existing converter's charges.
Errors discard the intermediate instead of publishing partial results. Ordinary
filter conversion's all-elements-must-convert contract is unchanged, as are
parameter placement and successful query semantics. The copy helper's unused
shared export is removed and its remaining owner keeps it private.

128 focused native tests pass across all-feature and SQL-disabled selections,
including storage identity, partial conversions, budget failure/retry, retained
WHERE bindings with fixed HAVING, warm literal parity, fingerprint and SQL
predicate controls. The new binding test initially used unsupported HAVING
parameters; correcting it to the maintained WHERE placement passes without
expanding the feature. Maintained three-lane lint and focused all-feature lint
pass after removing the exposed unused import and correcting one semicolon.
Focused admission/branch/authority/panic invariants and whitespace checks pass.
Full-suite validation remains user-owned.

All four Wasm runs pass, and both frozen replays reproduce all 18 records exactly.
The [matched receipt](0.257-grouped-having-perf.md) records 0.019–0.040% lower
compilation cycles, but mixed instructions: the 4-KiB compilation median rises
4.050% and its cache-hit median rises 2.203%. No individual sample crosses both
existing instruction-review thresholds. This is not a blanket speedup or full
cost qualification. Raw Wasm shrinks 1,152 bytes, gzip shrinks 475 bytes, and
defined function count is unchanged; no actor instrumentation was introduced.
Three isolated measurement servers were stopped, leaving existing networks alone.

Thirteen slice files change, including five docs: roughly twelve fewer
production/wiring lines and 252 net test/measurement additions. The owned flow
is simpler and introduces no new mode, conversion authority or execution route.
Remaining SQL AST copies, parser/catalog internals, field-name/CASE work and
predicate/traversal accounting remain in slice 10. Slice 11 and typed explain
have not started. Applications, Cargo versions, commits and pushes are untouched.

### Slice 10 follow-up: borrowed grouped-field resolution (0.257.1)

Grouped HAVING now borrows accepted field/path kinds without rendering a label,
allocating path components, constructing a retained GroupField or copying the
direct field's accepted kind. GroupField owns one shared nested-key eligibility
check for retained keys and borrowed normalization. Direct-field eligibility,
dotted spelling, current supplied schema authority and later role-specific
errors are preserved. Necessary execution-key retention is unchanged.

112 focused native test passes cover both SQL configurations, direct/nested
authority and conversion, grouping ownership, session regressions and
fingerprints. Three-lane and focused lint pass after correcting a test-only
empty-string style warning; the initial fixture-access compile error is fixed.
Focused admission/branch/authority/panic invariants pass. Full suites remain
user-owned.

The [incremental measurement](0.257-grouped-having-perf.md#borrowed-field-resolution-follow-up)
replays both frozen modules exactly: all nine compilations save 2,557–2,579
instructions and cycles; all nine cache-hit samples are unchanged. Raw Wasm
adds 1,615 bytes, gzip adds 645, and defined function count increases by five.
Nested-path cost is not measured by this direct-field actor. Both isolated
measurement servers were stopped; existing networks were untouched.

This follow-up touches eleven files (six Rust, five docs), approximately +12
production and +130 test lines. The flow is simpler despite modest compiled-code
growth: borrowed resolution replaces temporary retained-key construction without
another conversion authority or runtime mode. Slice 10 remains open for the
other preparation owners; no subsequent planned slice has started.

### CASE rewrite-admission accounting (0.257.1; slice 10)

The existing content-based CASE walker now borrows current request work and
propagates typed exhaustion. Each fixed-budget operation and nested-value visit
charges before its work; content refusal short-circuits and preserves accrued
charges. The eight-arm/256-unit policy and successful canonical form are
unchanged. Exhaustion cannot become successful compact CASE or a cached partial
command. Warm concrete commands still skip semantic compilation and this walk.

93 focused native tests pass across canonicalization, fingerprints, session
regressions and SQL-disabled CASE controls. Focused all-feature core/integration
lint and admission/branch/authority/panic invariants pass. Full suites remain
user-owned; the unrelated native timing microbenchmark remains ignored.

Four matched Wasm runs pass and both frozen replays reproduce all 18 records
exactly. Compilation-cycle medians rise 0.022–0.761%, cache-hit samples are
unchanged, raw Wasm adds 1,993 bytes, gzip adds 472 and defined functions add two.
No sample crosses the existing instruction-review thresholds. See the
[CASE receipt](0.257-case-admission-perf.md). Both isolated servers were stopped.

This handoff touches ten files (five Rust, five docs), approximately +53
production and +204 test/probe lines. There is still one admission walker;
explicit fallibility adds no runtime mode or request authority. Slice 10 remains
open for CASE expansion copies/wrappers and other expression preparation. Then
come planning/cache work (11), bounded diagnostics (5), and typed `.explain()`
(1). This handoff advances a shared safety prerequisite rather than another
allocation-only cleanup, and does not start a later planned slice.

### CASE expansion ownership and construction (0.257.1; slice 10)

CASE now moves results/ELSE and one condition occurrence, retaining one condition
copy per arm. Declined expansion preserves its original backing. The existing
admission walker produces a fixed stack receipt for conservative copy charges;
introduced wrappers charge before allocating. Exhaustion cannot select compact
CASE as successful fallback. Successful canonical output remains unchanged.

109 focused native test passes cover canonicalization, fingerprints, session
regressions and SQL-disabled CASE controls. Focused all-feature lint passes.
Formatting, focused admission/branch/authority/panic invariants, admission
checker fixtures and whitespace checks pass.
Full suites remain user-owned; the native timing benchmark remains ignored.

Four matched Wasm runs pass, with all 18 records reproduced exactly for each
frozen module. Compilation-cycle medians fall 0.037–0.540%, but cache-hit cycles
are mixed and one hit's instructions rise 13.374%, crossing the existing review
threshold despite lower cycles. This remains an explicit cost-review flag, not
a blanket speedup. Raw Wasm adds 2,089 bytes, gzip adds 707 and defined functions
add three. See the [expansion receipt](0.257-case-expansion-perf.md). Both isolated
measurement servers were stopped; existing networks were untouched.

Nine files change: four Rust and five docs, approximately +84 production/wiring
and +394 test lines. Owned transfer is simpler; explicit accounting retains one
admission traversal and adds no mode, cache state or compatibility path. Slice
10 remains open for other expression preparation; no subsequent planned slice
has started. Applications, Cargo versions, commits and pushes remain untouched.

### SQL arithmetic construction convergence (0.257.1; slice 10)

SQL field/literal arithmetic now checks its unchanged dynamic numeric family,
then uses ordinary binary lowering. The temporary fluent wrapper, two redundant
field copies, one literal copy and two temporary boxes are removed. Numeric
scale checks borrow their input. Public fluent construction, successful planner
trees, comparison admission and typed numeric rejection remain unchanged.

66 focused native test passes cover SQL lowering, fluent projections,
fingerprints, SQL/prepared/fluent U256 parity and SQL-disabled controls. The
initial fixture/diagnostic import errors are fixed; final three-lane repository
lint, focused all-feature lint, formatting and focused invariants pass. Full
suites remain user-owned.

Four Wasm runs pass and both frozen replays reproduce all 24 records exactly.
Raw Wasm shrinks 804 bytes, gzip 370 bytes and defined functions by one.
Compilation instruction medians fall 1.838–1.850%; cycle changes are tiny and
mixed (compilation +0.0057%, hits -0.0269–0.0312%). No sample crosses the existing
instruction-review thresholds. See the [arithmetic receipt](0.257-sql-arithmetic-perf.md).
Both isolated measurement servers were stopped, without changing existing networks.

Nine files change: four Rust and five docs, approximately -80 production/wiring
and +188 test/probe lines. The implementation is simpler with one converged SQL
binary construction path and no new mode or compatibility flow. These copies
were removed rather than newly charged: necessary SQL construction and other
expression-preparation bounds remain in slice 10. Typed `.explain()` and later
planned slices have not started. Applications, Cargo versions, commits and
pushes are untouched.

### SQL scalar construction accounting (0.257.1; slice 10)

The existing SQL expression owner now charges source visits, field/path and LIKE
text work, and requested vector/box backing before allocation. Known-length
fallible vectors reserve once. Exhaustion discards private intermediate trees,
preserves retained syntax and accrued charges, and cannot populate the compiled
cache. Warm concrete commands still skip semantic lowering. No request ceiling,
planning-operation unit, execution route or cache policy changes.

103 focused native passes cover SQL lowering, session preparation/ordering and
fingerprints. Exact construction boundaries, partial failure, cumulative retry,
cold/warm cache controls and no-row-execution checks pass. Focused all-feature
lint, formatting, admission/branch/authority/panic invariants and whitespace pass.
Full suites remain user-owned; no SQL-disabled rebuild is claimed for this SQL-
only change.

All eight Wasm probe runs pass. Each frozen replay reproduces all 24 arithmetic
and 18 CASE records exactly. Arithmetic compilations add 2,809 instructions and
cycles (+0.0156% cycle medians); arithmetic hits are unchanged. CASE compilation-
cycle medians rise 0.0579–0.0735%, but three individual instruction samples cross
the review thresholds and remain open flags. Raw Wasm adds 431 bytes, gzip drops
397 and defined functions drop one. See the
[construction receipt](0.257-sql-construction-perf.md). Both isolated servers
were stopped, leaving existing networks untouched.

Nine files change: four Rust and five docs, approximately +58 production and
+242 test/wiring lines. The implementation keeps one lowering flow with explicit
accounting and known-size reservation, not another runtime mode. Nested literal
payloads, aggregate syntax copies and the other documented expression-preparation
gaps remain in slice 10. No subsequent planned slice or typed `.explain()` starts
here. Applications, Cargo versions, commits and pushes remain untouched.

### Shared literal-copy accounting (0.257.1; slice 10)

Normalized-filter materialization and SQL literal/membership payload copying
now use one preparation-owned copy-and-charge traversal. Nested visits, scalar
byte work and requested container/payload backing charge before allocation.
Owned converted values still move; no extra sizing walk, coercion, map sorting
or enum identity reconstruction from schema metadata is introduced. Partial
copies stay private, input values stay unchanged and failed work accumulates.

131 focused native passes cover shared copying, filter materialization, SQL
lowering, session preparation, fingerprints and SQL-disabled copying/conversion.
Focused all-feature lint, formatting and admission/branch/authority/panic
invariants pass. Full suites remain user-owned.

All eight matched Wasm probe runs pass; every frozen replay reproduces all 24
arithmetic or 18 HAVING records exactly. Arithmetic compilations add 396
instructions/cycles and HAVING adds 515 (+0.00193–0.00283% cycle medians).
All 21 measured cache-hit records are unchanged, with no new instruction-review
flags. Raw Wasm adds 1,166 bytes and one function; gzip adds 468. See the
[literal-copy receipt](0.257-literal-copy-perf.md). Both isolated servers were
stopped; SQL-disabled actor costs and unmeasured nested shapes are not qualified
by these SQL probes. Prior performance flags remain open.

Twelve files change: seven Rust and five docs, approximately +58 production and
+219 test/wiring lines. The existing filter copier is extracted and extended
instead of adding a parallel SQL copy walker; accounting grows without another
runtime mode or semantic authority. Aggregate syntax copies and the other
documented normalization/folding/projection gaps remain in slice 10. Typed
`.explain()` and later planned slices have not started. Applications, Cargo
versions, existing networks, commits and pushes remain untouched.

### Borrowed aggregate lowering (0.257.1; slice 10)

Aggregate input/FILTER lowering borrows syntax instead of cloning a complete
aggregate subtree. Retained names and operand boxes charge before allocation.
Grouped direct-field names now copy rather than move to retain one shared owner.
Analysis and normalization remain unchanged and not fully accounted by this step.

125 focused native passes, core all-feature lint, formatting and focused
invariants pass. Four matched Wasm runs pass with exact frozen replays. Raw Wasm
adds 100 bytes, gzip adds 16 and defined functions are unchanged. Compile cycle
medians improve 0.02072–0.04019%; hit medians range -0.01225% to +0.02111%.
Two reproducible individual hit instruction samples exceed the review threshold;
their cause remains unresolved. This is not performance closeout. See the
[aggregate-copy receipt](0.257-aggregate-copy-perf.md) for all medians and flags.

Ten files change (five Rust, five docs), approximately +10 production and +100
test/probe lines. The implementation removes an intermediate subtree copy
without another runtime mode. Both isolated measurement servers were stopped.
Full suites remain user-owned. Slice 10 remains open, including existing
normalization/folding/projection gaps and performance review; `.explain()` and
later planned slices have not started.

### Aggregate phase attribution (0.257.1; slice 10)

The existing warmed audit endpoint now exposes counter boundaries around
request setup, session acquisition, query execution/session drop and request
cleanup. No database behavior, request policy or production route changes.
The matched instrumented baseline/candidate demonstrate large local-counter
deltas offset by earlier phase changes: a +479,402 read sample accompanies -594
instructions through request cleanup and +590 whole-update cycles. This
establishes boundary redistribution in that experiment, consistent with pinned
PocketIC page charges, without identifying exact pages in the earlier artifacts.

The [phase receipt](0.257-aggregate-phase-perf.md) records the measurement limits,
paired phase examples and cycle medians. Original local-counter flags remain
visible; thresholds are not relaxed and prior CASE flags are not cleared by
aggregate-only evidence. The next preparation work need not assume these partial
samples represent equivalent whole-call regressions.

Six files change, approximately +111 audit/test lines and no net library-code
changes. The instrumentation adds 3,431 raw audit-Wasm bytes and three functions;
the matched aggregate change still adds only 100 raw bytes and no functions.
All temporary baseline lowering changes were restored exactly. Slice 10 remains
open and `.explain()` is not exposed by this diagnostic step.

Five focused Wasm runs pass, including exact 18-record frozen replays and the
existing total-only reader. Native probe compilation, audit-actor lint,
formatting and focused invariants pass. Three isolated PocketIC servers were
stopped; full suites remain user-owned.

### Owned identifier normalization (0.257.1; slice 10)

Field/path normalization moves retained strings and reuses its owned segment
vector instead of borrowing and cloning a normalized suffix. Plain fields retain
their original string. Longest-prefix and case-insensitive scope matching and
nested output shape stay unchanged. This removes unnecessary copying; scope
candidate construction, qualifier joins, alias expansion and complete accounting
remain open.

82 focused native tests and core all-feature lint pass. Initial compilation
errors in new ownership tests were corrected; formatting and focused invariants
pass. The aliased-query cost probe forces normalization and checks all six rows.
Compile cycle medians change -0.01482%, hits +0.00305%; costs are effectively flat
on this workload. Raw Wasm adds 202 bytes, gzip 81, with no extra functions or
new instruction-review flags. Prior flags remain visible. See the
[identifier-move receipt](0.257-identifier-move-perf.md).

All four matched Wasm runs pass with exact six-record frozen replays. Both
isolated servers were stopped; full suites remain user-owned.

Eight files change, +6 production and +105 test/probe lines. Owned payload flow
is simpler, with no new modes or routes. Slice 10 remains open; later planned
slices and typed `.explain()` have not started.

### In-place qualifier reduction (0.257.1; slice 10)

The shared identifier reducer removes a matched prefix from its owned String
instead of allocating a suffix. Candidate-order/direct-prefix and qualified-tail
matching remain unchanged, including Unicode boundaries and empty suffixes.
Retaining original capacity is an explicit trade-off; matching and compaction
still require work accounting, so this does not close slice 10.

87 focused native tests pass. The initial clippy range-notation warning was
corrected, the full repository clippy gate passed, and focused tests were rerun.
Formatting and focused invariants pass. Four final matched Wasm qualification
runs pass with exact six-record replays; both isolated servers were stopped.
Full repository test suites remain user-owned.

Compile cycle medians improve 0.01998%, hits 0.00769%. Raw Wasm adds 190 bytes,
gzip adds 103 and defined functions are unchanged. One hit has a +479,096 local
instruction flag, offset by -479,962 before the read window; request completion
and whole-update cycles decrease slightly. The flag remains visible in the
[qualifier-move receipt](0.257-qualifier-move-perf.md), not silently discounted.

Six files change, +5 production and +26 test lines. One shared ownership flow
remains; no modes, public query routes or compatibility paths are added.
Remaining normalization/alias/folding and construction accounting stays open.

### Normalization accounting boundary audit (0.257.1; slice 10)

The next cohesive implementation is request-metered normalization, including
its callers outside ordinary query compilation. The semantic compiler and bound
SELECT preparation already have `PreparationWork`, but preparation/normalization
do not receive it. Exact/prefix UPDATE policy and resumable UPDATE start also
reach the same preparation owner, so metering only the compiler would leave
those paths outside the new accounting. Nested EXPLAIN and INSERT SELECT must
retain the same request authority too.

The [preflight handoff](0.257-preparation-work-preflight.md#next-normalization-handoff-converge-request-authority-first)
records the caller inventory, propagation and rejection/cache/job-publication
tests required for one shared fallible owner. No runtime edits or new limits
are made in this audit; fixed input admission remains in place. Two design/status
documents change. Tests, performance and Wasm builds are not rerun for this
documentation-only handoff. Slice 10 remains open; later slices have not started.

### Shared normalization budget and scope construction (0.257.1; slice 10)

The boundary audit now has its request-authority foundation implemented:
ordinary/bound/nested preparation and both UPDATE policy entry paths use the
existing request work. Scope backing, candidate visits, search allowance and
text copies charge before allocation. The vector reserves once; ordering and
duplicates remain unchanged. Warm concrete commands skip this work.

119 focused native tests, core clippy, formatting, read-admission invariants and
fixtures pass. Eight matched/frozen replay measurement runs cover aliased queries
and literal-only controls. Aliased compile cycle medians rise 0.02166%; hit
medians fall 0.00737%. Literal-only compilation adds five cycles, hits remain
identical. Raw Wasm adds 1,462 bytes, gzip 630 and defined functions two. No new
positive local-counter review flags occur; earlier flags remain open. Both
isolated PocketIC servers are stopped. Full repository suites remain user-owned.

The [receipt](0.257-normalization-scope-perf.md) records costs and limitations.
This is roughly +60 production/+220 test Rust lines across 17 files including
docs: required propagation adds plumbing, scope allocation simplifies, and no
new modes or execution routes are added. Statement copies, expression/alias
traversal and job-publication rejection qualification remain open. This does
not close slice 10 or start the typed `.explain()` terminal.

### Shared alias resolver and one HAVING pass (0.257.1; slice 10)

ORDER BY and HAVING now share their identical projection-alias resolver, and
HAVING no longer collects an intermediate normalized clause vector before alias
resolution. Matching, single-pass substitution and aggregate-scope boundaries
stay unchanged. This removes duplication before metering the remaining alias
lookup and copied-subtree work; it does not claim that work is already bounded.

88 focused lowering/session tests, core and probe clippy, formatting and focused
read-admission invariants/fixtures pass. Four matched/frozen replay runs pass:
ORDER BY compilation saves 186 cycles per sample; HAVING saves 532; hits are
identical. Raw Wasm shrinks 303 bytes, gzip 62, with no function-count change.
Both isolated measurement servers are stopped; full repository suites remain
user-owned. No new local-counter review flags appear.

Seven files change: -34 production lines, +121 native-test/probe lines, plus
documentation. The implementation is simpler, with no new routes or modes.
See the [receipt](0.257-alias-owner-perf.md). Slice 10 remains open for traversal
and copy accounting; later planned slices and `.explain()` have not started.

### Charged alias traversal and operand copies (0.257.1; slice 10)

The shared alias owner now charges scalar traversal, candidate lookup and copied
SQL operands using the current request work. One fallible mutable-child visitor
stops at the first rejected sibling. Alias copies preserve typed values through
the existing value copier; no independent coercion or sizing/unchecked-clone
pass is introduced. ORDER BY/HAVING retain their clause vectors and unchanged
single-pass, aggregate-opaque alias semantics. Warm commands skip this work.

93 focused native tests, core clippy, formatting and read-admission invariants
and fixtures pass. One invalid test-only projection fixture was corrected to
admitted CASE syntax before final qualification. Eight matched/frozen replay
runs pass. Literal-only controls are identical; alias cycle medians vary within
about ±0.023%. Raw Wasm adds 6,724 bytes, gzip 2,292, with unchanged function
count. A +242,811 local hit instruction delta is largely offset before the read
window and retained in the [receipt](0.257-alias-budget-perf.md). No new joint
review-threshold flags occur; earlier flags remain open. Both isolated servers
are stopped. Full repository test suites remain user-owned.

Eleven files change, approximately +195 production/+220 test Rust lines plus
docs. Explicit bounded copying increases implementation size; vector ownership
converges and no modes or execution routes are added. Initial statement cloning,
identifier-normalization traversal/construction, and remaining projection copies
still need accounting. This completes the alias substitution boundary, not all
normalization or slice 10. Later planned slices and `.explain()` remain pending.

### Charged identifier rewriting and allocation removal (0.257.1; slice 10)

Identifier rewriting is now fallible across projections, predicates, aggregate
input/FILTER, grouping, ordering, assignment targets and RETURNING fields.
Visits, matching allowances and introduced field backing charge the existing
request work. The public pure matcher remains the semantic authority; SQL
charges its conservative byte-work allowance before invocation.

Nested paths retain their strings and vector rather than joining qualifiers and
building a second vector. Longest-prefix/full-or-tail matching is preserved by
matching each prefix's final component. Dotted construction shares the alias
copy owner, plain projection fields retain their strings, and NULL-test special
preparation reuses its field and box. Clause vectors stay owned in place.

135 focused native tests, core clippy, formatting and read-admission invariants
and fixtures pass. Twelve matched/replay runs pass; qualified/alias compile
cycle medians rise 0.016–0.051%, with unchanged literal-only controls. Raw Wasm
adds 5,238 bytes, gzip 1,657 and defined functions two. No new positive joint
instruction review flags appear; earlier flags remain open. Both isolated
PocketIC servers are stopped; full repository suites remain user-owned.

Eight files change, approximately +50 production/+160 test Rust lines plus docs.
Matching and allocation flow simplify while fallible propagation adds plumbing;
no modes, routes or request roots are added. See the
[receipt](0.257-identifier-budget-perf.md). Initial statement copies,
local-canonical classification and remaining projection copies are still open,
as is broader mutation-job rejection qualification. Slice 10 and the later
`.explain()` work are not closed by this landing.

### Typed/shared ownership handoff (0.257.1; slice 10)

The next preparation focus is the typed/shared path consumed by `.explain()`.
SQL statement cloning, SQL-local classification and UPDATE-job qualification
remain recorded work, but are not dependencies of a SQL-free terminal merely
because they also use preparation. The terminal still requires reachable shared
preparation (10), planner/cache bounds (11), and bounded diagnostics (5).

ORDER BY lowering now consumes its fluent wrapper and transfers its expression
directly into planner intent. Grouped aggregate conversion similarly consumes
the raw aggregate shape, preserving its input/filter boxes and raw DISTINCT bit.
This removes the second recursive copy after the dynamic request's borrowed
copy; there is no additional borrowed/owned conversion route. The initial
request copy, projection/group-key construction and downstream planner copies
are still open. No diagnostic limits or ordinary identities change here.

The repository and both CI toolchain pins now select Rust 1.98.1; installed
components include Rustfmt, Clippy and the Wasm target. Public package MSRVs and
the workspace Rust floor are unchanged. Existing Rust 1.97.1 receipts are
historical evidence, not a matched baseline for the new compiler or current
dependency lockfile. Cycle and raw-Wasm deltas for this handoff remain unmeasured.

Rust 1.98.1 Clippy also requires mechanical updates at 21 existing files:
const-capable helpers, fixed-size byte iteration and direct Result predicates
or defaults. Fixed-size decoding keeps its existing length/remainder policy;
the defaulted Result operations cannot panic. The complete handoff touches
36 files (six ownership/test files, 21 lint-propagation files, nine toolchain
and documentation files), approximately 120 net lines, mainly tests and notes.
The runtime shape is simpler: two recursive copies and one conversion helper
are removed, without new modes, request roots or semantic authorities.

Final validation passes: 70 focused ownership, aggregate/cache/fingerprint,
session-ordering, hex and entity-revision tests; the maintained Clippy gate and
focused all-feature core lint; SQL-free Wasm core checking; public `icydb`
dependency checking on Rust 1.88.0; workflow lint, formatting and whitespace.
Initial offline attempts needed missing locked dependencies; downloading them
did not alter the existing lockfile changes. Initial compiler lint failures
were repaired and their gates rerun. Full suites and cycle/raw-Wasm measurement
remain unrun. No local network lifecycle actions or application edits occurred.

### Charged typed operand copying (0.257.1; slice 10)

Dynamic/typed ORDER BY and aggregate declarations no longer use an unmetered
recursive clone when materializing shared intent. One expression copier charges
visits, text work, boxes and vector backing before construction, and delegates
literal payloads to the existing value copier. AggregateShape retains ownership
of raw declaration copying: kind, input, FILTER and raw DISTINCT are preserved
without invoking a normalizing constructor. The owned intent handoff still
moves its result, so this does not reintroduce the second clone removed earlier.

Input admission precedes the recursive copier; partial results cannot exceed
the admitted expression/value depth. Exact-limit, source-preservation,
first-rejected-child, repeated-root and admitted-depth cleanup tests cover the
copy owner. Session tests cover public/trusted and cold/warm ordered/grouped
calls, proving exhausted copies neither execute rows nor change cache residency;
the reusable request still executes under a fresh request authority.

This closes operand copying only. ORDER BY/aggregate clause-container growth,
implicit primary-key ordering, selected-field and group-key materialization,
and downstream planning/cache construction remain open. Shared preparation (10),
planner/cache bounds (11) and bounded diagnostics (5) still precede the terminal.
No additional public API, cache mode, query route or resource authority is added.

Incremental complexity: eight Rust files, approximately +150 production and
+240 test lines, plus five documentation files. Explicit fallible accounting
adds code; expression semantics and the execution route remain shared.

Validation passes: 59 all-feature focused tests, four SQL-free copier tests,
library/test Clippy, formatting, read-admission invariants and whitespace.
The matched SQL-free dynamic actor adds 3,818 raw Wasm bytes and removes three
defined functions. Replayed ordered-scan local instructions rise 0.2062%; point
controls rise 0.0289–0.0379%. See the [receipt](0.257-typed-copy-perf.md) for
the wiring-control baseline, exact artifacts and measurement limitations.
Both isolated PocketIC servers are stopped. Full suites and whole-message
cycles remain unrun. The preparation slice and typed `.explain()` remain open.

### Scalar clause materialization (0.257.1; slice 10)

Explicit ORDER BY, implicit accepted-primary-key ordering and selected-field
projection now allocate known-length clause vectors once under the existing
request budget. The expression copier's text/vector helpers are shared with
scalar clause construction. Complete clauses move through existing setters;
three incremental ordering helpers are removed. Authored order, duplicate
projections and absent-order behavior remain unchanged. There is no new resource
authority, cache mode, public API or execution route.

This closes scalar order/projection materialization only. Grouped clause vectors
and group-key resolution remain the next preparation work; shared planner/cache
bounds (11) and diagnostic projection/rendering (5) still precede `.explain()` (1).
Preparation slice 10 remains open.

Incremental complexity: ten Rust files, approximately +10 production and +150
test lines, plus five documentation files. The owned clause handoff simplifies
the implementation while extending fallible allocation accounting.

Validation passes: 70 focused all-feature tests, six SQL-free copier tests,
test-target Clippy, formatting, read-admission invariants and whitespace.
Matched raw Wasm decreases 213 bytes, gzip decreases 370 bytes and defined
functions increase by one. Ordered-scan local instructions rise 0.0070%; point
controls rise 0.0052–0.0105%. See the [receipt](0.257-scalar-clause-perf.md).
Both isolated PocketIC servers are stopped. Full suites and whole-message
cycles remain unrun; this does not claim complete preparation bounds.

### Grouped clause materialization (0.257.1; slice 10)

Typed/dynamic and SQL frontends now materialize grouped key and aggregate
clauses once, then move them through shared intent setters. Key construction
selects the existing direct/path representation before reserving backing, so a
later path does not promote an already-built direct prefix. The existing key
resolver and duplicate-identity owner remain authoritative. Aggregate operands
move without another copy; known-length backing uses the same reservation owner
as scalar clause copying. Empty scalar grouping skips the grouped wrapper.

This closes grouped clause containers, not group-key preparation as a whole.
Inspection found recursive accepted-kind copies at FieldSlot resolution and
path strings/segments at GroupField resolution, shared with downstream planner
consumers. Those copies and duplicate comparisons need their owner-level
accounting change next; no parallel metered resolver is introduced here.
Shared planner/cache bounds (11) and diagnostic projection/rendering (5) still
precede the terminal (1). Preparation slice 10 remains open.

The one-allocation policy reserves from authored key count. Duplicate-heavy
clauses can retain spare capacity; existing retained accounting counts that
capacity. This deliberately avoids an extra deduplication structure or shrinking
allocation. No runtime mode, cache policy or public API is added.

Incremental complexity: fourteen Rust files, approximately +40 production,
+190 test and +30 audit-probe lines, plus five documentation files. Complete
clause handoff replaces incremental frontend append paths; semantic owners stay
shared. Low-level insertion remains used by resolution/rebinding, not as a
compatibility path.

Validation passes: 102 focused all-feature tests, nine SQL-free tests, focused
core and probe Clippy, formatting, read-admission invariants and whitespace.
Matched raw Wasm adds 524 bytes, gzip adds 324 bytes and defined functions add
one. Grouped empty-query local instructions rise 0.2057%; scalar controls
decrease 0.0075–0.0179%. An initial empty-clause wrapper cost was found by the
probe and removed before handoff. See the [receipt](0.257-group-clause-perf.md).
Three isolated PocketIC servers are stopped. Full suites and whole-message
cycles remain unrun; typed `.explain()` is not yet exposed.

### Borrowed grouped-key validation (0.257.1; slice 10)

Grouped structural validation no longer resolves an owned replacement key just
to compare identity and discard it. The borrowed check retains current accepted
direct/path eligibility, root-slot validation and exact path spelling. It uses
the existing accepted-kind eligibility owner and borrows stored path components.
Owned insertion and validation now share one borrowed identity comparator.
Unknown-key and duplicate-key diagnostic roles and positions are unchanged.

This removes unnecessary payload-copy work before charging the remaining
retained copies. It does not rebind retained types, grant execution authority or
replace the planner's current rebinding. FieldSlot and GroupField construction
also serve covering, aggregate projection and logical rebinding, whose callers
do not yet carry PreparationWork. Their retained-copy and comparison accounting
needs the shared planner request authority, not a parallel frontend resolver.
Preparation (10), planner/cache bounds (11), diagnostics (5) and terminal (1)
remain open; no additional mode, format, counter or compatibility path is added.

Three Rust files add approximately 20 production and 86 test lines; four
documentation files complete the handoff. The data flow is simpler: no temporary
owned validation key and one identity-comparison owner. Timing, raw Wasm, gzip,
function-count and cycle deltas are unmeasured because an independent session
is changing planner eligibility concurrently. No cost improvement is inferred
from the source-line delta; matched qualification remains a follow-up.

87 focused all-feature tests and the new SQL-free authority-parity test pass.
Focused Clippy encountered unrelated warnings in the concurrent sparse-index
changes; their owner corrected them and both the required repository lint gate
and focused lint recheck now pass. Formatting, read-admission invariants and
whitespace checks pass.
No full test suite or network lifecycle action was run for this change.
See the [qualification note](0.257-group-validation.md).

### Shared planner-accounting handoff (0.257.1; sequencing correction)

The retained-key audit exposed a dependency cycle in the tracker: slice 11
waited for all shared preparation in slice 10, while shared FieldSlot/GroupField
copying and comparison required request authority in planner callers assigned
to slice 11. Retained planner construction is now explicitly owned by 11.
Frontend-only residuals remain in 10; neither is declared complete, and both
still precede diagnostic bounds (5) and `.explain()` (1).

The next implementation is the existing shared-planner outcome, not another
frontend clone helper or a plumbing-only handoff. It must carry the current
request scope through actual cold construction and warm rebinding, charge the
retained owners before work/allocation, and propagate typed exhaustion without
turning it into an unavailable candidate or scan fallback. Ordinary cache hits
must continue skipping compilation that they already avoid. Full candidate and
cache coverage remains part of 11's completion gate.

The [caller inventory and contracts](0.257-planner-accounting-handoff.md) cover
early filterless hits, trivial plans, cold compilation, template rebinding,
identical-bound reuse, pinned continuation planning and final metadata freezing.
This is a sequencing/ownership correction within the approved preparation and
planner scope, not a new feature or budget policy.

Four design documents change; no runtime, test, application, Cargo or changelog
edits. Source inspection and local document checks only. Tests, lint, Wasm,
instructions and cycles are not rerun for this documentation-only handoff.
No network lifecycle action occurred. The other session's bug fix is untouched.

### Issue 14: sparse-index completeness (0.257.1)

Completed the authorized correctness fix independently of the open preparation
slice. Ordinary planning, candidate diagnostics and exact-count selection now
reuse the accepted-schema completeness proof. Nullable trailing components and
nullable path ancestors cannot silently omit matching rows. Equality, range,
IN, text-prefix, non-null guards and supported disjunctions prove membership;
queries without a complete public access path reject under existing admission.

Planner index eligibility owns the shared proof. The new diagnostic rejection
`index_membership_unproven` explains omitted candidates without adding an
execution mode, persisted state or format. Stored index membership is unchanged.

Validation passes: 26 focused all-feature tests, the full required repository
Clippy gate, the focused all-feature test-target lint recheck, SQL-disabled
compilation, formatting and whitespace. The isolated v0.255.3 generated-canister
reproducer also passes both tests with the fix applied: eight formerly missing
prefix/full-composite results and eight populated-timestamp controls each return
one row. Current regressions cover mixed null/populated rows, public/trusted
and parameterized SQL reads, warm plans, pagination, exact counts and rejection
when no complete alternative exists.

Incremental complexity: ten Rust files add 383 net lines, mostly regression
coverage, plus three release/status documents. Proof logic grows slightly while
reusing one completeness authority and the existing execution flow. Raw Wasm,
gzip, performance and on-chain measurements were not run; no size or speed
improvement is claimed. Full suites remain user-owned. No network lifecycle
action, Cargo version mutation, commit or push was performed.

### Shared template candidate ownership (0.257.2; slice 11 in progress)

The user reported 0.257.1 published and authorized continuing on 0.257.2.
The first shared-cache owner now uses request preparation authority for actual
candidate-array construction. Each semantic index contract already shares its
accepted metadata; the avoidable work was copying the handle vector on every
template checkout and looking it up/copying it again when remembering a binding.

Templates now share an immutable reference-counted slice. Cold construction
charges one predicate-expression step per copied handle and temporary bytes for
the handle array plus two pointer-width reference counts before allocation.
There is no recursive metadata copy or premeasurement traversal at this owner.
Empty arrays still retain a reference-count header. Retained-capacity accounting
continues through the existing shared-allocation visitor, including that header.
Successful rebinding moves the checked-out template back into the existing
cache after replacing its memo; failures do not publish a new memo. Cache keys,
authority/readiness validation, operation-count units and route pins are unchanged.

This is an ownership simplification plus real request charging, not authority
wiring without charged work. The simplest alternative was to meter every copied
vector; sharing removes that repeated allocation entirely. State-space delta:
zero new modes, formats, cache routes or budget authorities. No session/request
borrow is retained in a template. Remaining retained-key construction/comparison,
candidate scoring, cache validation/rebinding traversal and frontend residuals
still gate diagnostics and `.explain()`; slice 11 is not complete.

Validation: 51 focused query/cache and 12 cardinality/lifecycle tests pass, as do
the required repository lint gate, focused all-feature test-target lint and
SQL-disabled Wasm build. Matched, repeated instruction samples improve identical
point bindings by 0.39% and changing bindings by 2.55%; scan/grouped controls rise
by less than 0.007%. Raw Wasm adds 37 bytes and one defined function. See the
[qualification](0.257-template-authority-perf.md) for exact counts, scope limits,
source complexity and network lifecycle actions. Full suites remain user-owned.

### Shared retained field metadata (0.257.2; slice 11 in progress)

Retained field slots now share their immutable accepted query kind with the
schema view. The existing FieldSlot resolver obtains slot and kind authority in
one schema lookup instead of two, without copying recursive type metadata.
Grouping, aggregate targets and covering fields continue through that resolver;
schema/plan clones share the same kind allocation. The retained handle owns only
that kind, not a whole schema, session borrow or new runtime authority.

This removes the recursive-copy work instead of adding a second metered kind
resolver. Each accepted schema field gains one shared allocation/reference-count
header; retained-cache accounting includes its backing and nested payload and
continues to count independent residents conservatively. Cache weights can change.
The borrowed kind API, labels, slots and field equality semantics are preserved.
Rebinding explicitly chooses current metadata; old retained slots stay immutable
and can outlive their original schema view. No mode, persisted representation,
public API or new query-admission limit is introduced by this ownership change.

Validation passes: 88 focused schema, grouping, query/cache and cardinality tests,
required repository Clippy, focused all-feature test-target lint, SQL-disabled
compilation, formatting and whitespace. New tests prove sharing across retained
slots and cloned views, detached lifetime, current-kind rebinding, and exact /
one-below retained-size accounting including shared headers and nested payload.
Test imports and one redundant-clone assertion were corrected during validation;
the concurrent facade-test lint warning was fixed by its owner.

Three Rust files add 15 net production lines and 102 test lines. Four active
release/design documents are updated. The ownership flow is simpler, but a
one-time schema-field allocation is traded for repeated metadata-tree copying;
no overall speed, cycle or Wasm-size improvement is claimed yet. The baseline
Wasm build failed its immutable-input gate due to concurrent edits and was not
used for comparison or repeatedly rerun. No local network was started/stopped;
full suites remain user-owned. Retained label/path construction and comparison,
remaining candidate/cache work and diagnostic bounds still gate `.explain()`.

### SQL validation-only group keys removed (0.257.2; preparation in progress)

Caller inspection found that SQL grouped-projection validation constructed a
temporary GroupFieldSet solely to prove membership, then discarded it. This
handoff removes that construction instead of extending accounting around it.
Validation borrows the authored labels and shared schema grouping eligibility;
the actual retained-key resolver remains the structural planner's owner.
Every declared label is still checked, including unreferenced invalid keys.

Membership compares direct names or exact root/segment identities without a
rendered path or retained key. Before each candidate comparison it charges one
step plus two passes over the label bytes to the existing request. It preserves
short-circuiting and propagates exhaustion as QueryError through SQL lowering,
not a negative match. Duplicate labels remain in the borrowed input: removing
the temporary deduplicated set also removes its insertion/promotion work, but
later membership checks can examine duplicates. No end-to-end speedup is claimed.

Validation: 66 focused tests pass (membership, schema and SQL/session regression
coverage), together with core all-feature test lint, SQL-free checking, formatting
and whitespace checks on Rust 1.98.1. The initial new tests had incorrect resource
and fact accessor names; those compile errors were corrected before the passing
run. Full suites and PocketIC group-path qualification were not run. No network
was started or stopped.

Complexity: five Rust files, approximately +38 production and +175 test lines,
plus four active documentation files. The validation ownership flow is simpler;
explicit fallible comparison accounting adds code but no mode or execution route.
Raw Wasm, function-count and instruction/cycle deltas are unmeasured for this
handoff; earlier template-only measurements do not qualify it.

This is narrower than the proposed retained-label/path accounting handoff.
Actual retained labels/paths, planner/executor request-authority propagation,
schema lookup and shared key comparisons remain open. It neither closes slices
10/11 nor makes `.explain()` ready. The next implementation remains that shared
retained-key boundary, not another SQL validation path.

### Small pre-push path-label cleanup (0.257.2)

Canonical grouped SQL projection identity now compares the rendered path label
without allocating a temporary String. PathSpec owns the comparison beside its
renderer; exact text semantics remain unchanged, including empty or dotted
components and UTF-8. This is deliberately not structural path equivalence.
No new budget mode, execution route or admission rule is introduced.

Four focused tests, all-feature core test lint, SQL-free checking, formatting
and whitespace checks pass on Rust 1.98.1. The tests cover renderer parity,
truncations, grouped alias policy and warm/bound grouped query results.
Scope: two Rust files and two documentation files, about +20 production and
+65 test lines. Temporary ownership is simpler; Wasm and instruction/cycle
deltas are unmeasured. Full release validation remains user-owned; no network
lifecycle, Cargo version, commit or push action was performed. Stop here for
the requested push boundary; broader retained-key accounting remains open.

### ICYDB-026: direct collection-relation admission (0.257.2)

The user requested this downstream correctness fix on 2026-09-09. A public
schema proposal containing a direct ULID-list relation published successfully,
then its first database operation reproduced E23. Accepted field contracts carry
plain element kinds while target identity belongs to the accepted relation edge;
the scalar/tuple projection incorrectly treated the whole list as a key.

Direct list/set sources now compile through the existing bounded relation path
interpreter, including optional roots. Scalar/tuple projection remains direct.
Runtime contract failures retain accepted source entity and relation IDs alongside
existing cause facts. The E23 fact ceiling is five; no E-code, fact tag, runtime
mode, schema encoding, compatibility path or generated-model authority is added.

Validation passes: 44 focused core relation, schema-application, error/facade and
fact-schema tests; the required repository lint gate and focused all-feature
library/test-target lint; formatting and whitespace checks. An isolated native
canister using the reported generated `value(many, item(rel = "Resource",
prim = "Ulid"))` declaration also initialises and completes its first query.
Required/nullable lists cover missing-target atomic rejection, duplicate/empty
references, pagination and clearing reverse references before restricted deletion.
The fixture's initial one-page assertion was corrected to follow continuations.
The function-length and test-allocation lint findings are resolved.

Scope: seven Rust files, approximately +360 net Rust lines, mostly direct tests;
eleven files including diagnostics guidance and release/status notes. Admission
and diagnostic code grows modestly while collection execution reuses one existing
flow. Performance and raw-Wasm deltas are unmeasured. No full suite, managed IC
qualification, dependency pin, Cargo version, network lifecycle or publication
operation was performed for this fix. Toko Miner's separately reported ICYDB-027
blocks 0.257.1 adoption and remains under investigation; its downstream CI is not
reported green by this handoff.

The read-only ICYDB-027 review confirms the downstream release comparison:
38 native failures on 0.257.1 versus 61 passing tests on 0.257.0, with the
candidate pin withdrawn. A minimal generated required Nat16 ordering fixture
with three unique indexes passes both cold typed and dynamic reads on this
worktree, so it does not yet isolate the application-specific trigger. The
working SQL EXPLAIN is evidence of accepted index availability, not proof that
the failing typed execution route is correct.

### ICYDB-027: catalogue query attribution corrected (2026-09-09)

Separate probes of the real downstream reads resolve the earlier uncertainty:
`read_nft_collection_rows` and `read_resource_rows` succeed on the empty store;
`read_nft_token_preview_rows` returns E173. The failing catalogue assertion calls
all three. The required collection index never lost its typed access path, and
its working SQL EXPLAIN did not describe the failing token query.

The token query constrains `catalog_preview = true`, but its composite index
also contains nullable `catalog_preview_order`. That predicate does not exclude
matching rows omitted from the index. The issue #14 completeness check correctly
rejects this public query; accepting it would restore silent row omission.
No IcyDB planner or admission change is warranted.

Toko Miner's query now explicitly adds the generated
`NftToken::CATALOG_PREVIEW_ORDER.is_not_null()` predicate. Null-order rows are
excluded by the query itself. Its feedback ledger and current status correct
the earlier attribution and close the upstream change request. The existing
public typed execution flow, schema and real dependency pin are preserved.

The isolated downstream source snapshot passes fixture reconciliation against
this worktree, and both focused NFT catalogue tests against unmodified published
0.257.1 (`5874e5590b7f958485be9921ad863dd213ffa95d`). Evidence is retained in
`/tmp/icydb-027-downstream.log`, `/tmp/icydb-027-corrected.log` and
`/tmp/icydb-027-published-corrected.log`; the minimal application correction is
also retained at `/tmp/icydb-027-toko-miner.patch`. Full adoption CI and managed
lifecycle qualification remain user-owned and pending. No network lifecycle,
schema/version mutation, publication or IcyDB runtime change was performed.
Scope is one added downstream Rust line plus feedback/status documentation;
execution complexity is neutral, with no new mode, duplicate flow or compatibility
path. Performance and raw-Wasm deltas are unmeasured.

Both focused catalogue tests also pass in the actual Toko Miner working tree
with its retained 0.257.0 pin (`/tmp/icydb-027-retained-pin.log`). Formatting and
scoped whitespace checks pass. The broader downstream whitespace check reports
an unrelated trailing blank line in `frontend/src/shop/fuel-shop-api.ts`, left
unchanged with the concurrent work.
