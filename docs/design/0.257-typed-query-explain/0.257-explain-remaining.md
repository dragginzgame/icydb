# Typed explain: remaining critical path

Source audit: 2026-09-09, updated 2026-09-10 with write/replay qualification and
after-image admission before the unchanged-index shortcut and shared accepted
setup during final verification.
This is a completion checklist for existing slices 10, 11, 5 and 1, not a new
minor, new feature scope or a preparation-boundedness verdict.

## Delivery assessment

Typed `.explain()` is not one small terminal edit away. Four original tracker
slices remain incomplete. The two preparation rows contain several substantial
owners; describing them only as "accounting remaining" obscures the distance.
The table below identifies nine work areas, not nine guaranteed coding turns,
nine new landing slices or nine SemVer patches. A calendar estimate is not yet
defensible: shared codec budget/error propagation and diagnostic construction
still require implementation decisions and qualification.

Recent prefix-list accounting closed real allocation/visit checks. The subsequent
big-integer buffer removal simplified encoding but did **not** close scalar-codec
accounting. Do not count cleanup handoffs as completed preparation gates.

## Reachable path and remaining owners

The approved terminal consumes typed syntax and its current binding, converts
through the existing dynamic/structural owner, uses the ordinary shared planner,
then projects a detached logical ExplainPlan. It does not execute result rows.

| Area | Source evidence / canonical owner | Completion evidence required |
| --- | --- | --- |
| R1: planning-state normalization (10/11 boundary) | `prepare_query_model_scalar_planning_state_with_schema_info` in [pipeline.rs](../../../crates/icydb-core/src/db/query/plan/pipeline.rs) borrows access inputs, then normalizes/folds predicates without PreparationWork. Access-input projection itself does not materialize a key override. [Dynamic conversion](../../../crates/icydb-core/src/db/session/query/dynamic.rs) already runs inside PreparationWork; that does not cover later normalization. | Required current request authority reaches the shared normalization owner. Charge actual visits/copies; successful canonical predicates and identity remain independent of exhaustion. Preserve early zero/false-query behavior. |
| R2: cache key construction, comparison and warm validation (11) | [Session cache](../../../crates/icydb-core/src/db/session/query/cache.rs) prepares planning state, derives parameter contracts/fingerprints and cache keys before its miss/rebind PreparationWork closures. Filterless hits return earlier. [Templates](../../../crates/icydb-core/src/db/session/query/cache/template.rs) already budget their candidate list and reuse bound plans. | Account performed key/comparison/validation work on each route. Keep early hits early, identical bindings reusable, changing bindings correctly rebound, and failed construction unpublished. Do not force fresh optimization when existing validity permits reuse. |
| R3: candidate enumeration, scoring and residual comparison (11) | [Access choice](../../../crates/icydb-core/src/db/query/plan/access_choice/mod.rs) still exposes candidate/projection/reranking owners without request work. Metered cardinality probe preparation and evidence selection happen downstream of candidate enumeration. | Bound enumeration/output construction and score/residual work before performing it. Preserve tie order, selected access, unavailable-evidence policy and cursor pins. Existing evidence/probe checks are coverage to preserve, not proof of the entire candidate path. |
| R4: logical assembly, semantic validation and remaining grouped metadata (11) | [Pipeline assembly/finalization](../../../crates/icydb-core/src/db/query/plan/pipeline.rs) borrows logical inputs and budgets retained clause/projection-selection copies; reranking moves them. Shared order canonicalization/access-order copying and duplicate/primary-key consistency checks are budgeted. Computed-label writes share the preparation sink. Accepted projection construction is budgeted once and moved from validation into finalization; prepared consumers borrow it. Identity projection is unchanged. Semantic/type validation and formatter internals still contain unmetered work. [Semantic gates](../../../crates/icydb-core/src/db/query/plan/validate/semantic_gates.rs) receive request work; [group layout](../../../crates/icydb-core/src/db/query/plan/group.rs) contains further owners. | Retained clauses, order canonicalization/consistency and accepted projection construction are implemented, not full R4 completion. Charge remaining shared operations at their owners, including schema lookup and expression/key comparisons. Keep semantic errors distinct from resource exhaustion; preserve scalar/grouped parity. Scope is reachable preparation, not a general executor rewrite. |
| R5: scalar index encoding (11) | [Ordered codec](../../../crates/icydb-core/src/db/index/key/ordered/mod.rs), [numeric conversion](../../../crates/icydb-core/src/db/index/key/ordered/normalize.rs), [segments](../../../crates/icydb-core/src/db/index/key/ordered/segments.rs) and [accepted encoding](../../../crates/icydb-core/src/db/index/key/build.rs) still allocate/encode without the caller's construction authority. Lowering currently charges component-list backing and visits, not these bytes or conversion loops. | Required accounting reaches output growth, text escaping, bigint limb/chunk allocation/division and accepted-value work. Preserve one codec for query/write/rebuild consumers and distinguish unsupported encoding from exhaustion. No optional active-budget fallback. Golden bytes/order and before-allocation/within-loop rejection tests qualify the change. |
| R6: raw bounds and deferred materialization (11) | [Raw key codec](../../../crates/icydb-core/src/db/index/key/codec/mod.rs) allocates/fills raw bound vectors. [Lowered prefix specs](../../../crates/icydb-core/src/db/access/lowering.rs) may materialize deferred bounds on demand. Retained-capacity reservation is not construction charging. | Charge raw backing, sizing traversal and filling/copying where performed. Preserve required request/execution authority at each caller, typed failure and successful bounds. Deferred cache hits must not pay for construction they skip; failed materialization must not install partial bounds. |
| R7: bounded diagnostic projection and identity isolation (5) | [ExplainPlan](../../../crates/icydb-core/src/db/query/explain/plan.rs) retains models and builds strings/collections. [Hash profiles](../../../crates/icydb-core/src/db/query/fingerprint/hash_sections/profile.rs) consume its filter/predicate models. | Bound model/string/candidate construction at the shared diagnostic owner. Diagnostic rejection must not change ordinary admission, hash inputs or cursor identity. Prove warm repeated-call charging and ordinary-query behavior before/after a rejected explain. No truncated or placeholder fingerprint model. |
| R8: detached fallible rendering (5) | Canonical renderers in [ExplainPlan](../../../crates/icydb-core/src/db/query/explain/plan.rs) still return String. [JsonWriter](../../../crates/icydb-core/src/db/query/explain/writer.rs) appends without a limit and its Debug path discards the formatting result. | Implement the approved incremental fallible text/JSON contract, including escaping/formatting expansion and error propagation. Establish numerical limits before implementation. Detached rendering must not borrow a session or create request counters. |
| R9: typed terminal and end-to-end qualification (1) | [Typed Query](../../../crates/icydb/src/db/query/typed.rs) has no `.explain()` terminal; the [design](0.257-design.md) defines its existing binding/planner/DTO seam. | Deliver the small consuming facade and session helper, reject every supplied cursor, revalidate current authority, permit logical inspection of otherwise execution-rejected scans/sorts, support grouped/scalar and SQL-free builds, and prove no result-row execution. Complete matched Wasm/instruction-or-cycle qualification and public guidance. |

R1–R6 are preparation work. R7–R8 are the existing diagnostic outcome, and R9
is terminal delivery. This checklist does not weaken the approved dependency:
slices 10/11 still gate 5, and 5 still gates 1. Each implementation handoff must
identify which checks it actually closes; partial coverage remains partial.

## Scope boundary

- Completed input admission, boolean lowering, membership representation, CASE
  policy, metrics selection and native convergence naming are not reopened.
- The typed terminal does not call SQL parsing/binding, SQL execution explain
  descriptors, specialised exact-count planning, result-row decoding, mutation
  orchestration or endpoint transport. Isolated work in those paths is not a new
  typed-terminal prerequisite merely because it appears in the same minor's
  historical notes. This does not silently close any outstanding line commitment
  or change an approved slice dependency.
- Shared owners still require complete propagation to their maintained callers.
  For example, changing the scalar encoder's budget/error contract must handle
  its mutation/rebuild and index-predicate consumers correctly. That is necessary
  propagation, not authorization to meter all unrelated mutation/execution work.
- Do not start additional clone/allocation cleanup as a standalone prerequisite
  unless it is necessary to close a named construction check in this table.
  A reduction in source duplication alone is not an accounting completion proof.
- No new public modes, budget profiles, caches, endpoint APIs, compatibility routes or permission
  bypasses are proposed. Any required expansion follows the existing design's
  explicit rescope rule instead of being folded silently into `.explain()`.

## Next implementation boundary

Completed bounded handoff within R5: preserve a completed optional index-program
result through runtime resolution. The shared preparation builder requests
either strict or conservative compilation (or neither). It previously stored
only each resulting `Option<Program>`, so runtime treated a completed `None` as
unprepared and repeated compilation. On-demand compilation now converges at
the existing preparation owner, which retains the existing policy that produced
the result, including `None`. This closes a duplicate
construction route; it does not complete scalar accounting or its fallible
error contract.

Need: distinguish "not requested" from "completed, unsupported" so genuine
absence does not trigger repeated construction. Simplest alternative: one
optional policy/result pair instead of two independently optional program
fields and two requested-policy fields. All maintained constructors request
at most one policy. Canonical owner: `ExecutionPreparation`; runtime borrows
its result or invokes that same owner's existing compiler for a different,
unprepared policy. State-space delta: preserve the provenance already present
during construction, remove the unused two-policy configuration combination;
no new compiler policy, cache lifetime, request option or persisted state.
Future compiler exhaustion must remain an error, never a completed absence.

The user approved separating recovery stages before completing replay-safe
admission and shared scalar encoder accounting. Nonempty startup now returns
between marker replay, each complete journal-batch fold and final verification.
One callback no longer combines all three construction owners. The existing
in-progress owner holds a volatile marker-bound cursor; a lost or differently
bound cursor restarts replay. Folded marker batches are recognized by durable
watermarks and are not appended again. Final verification remains mandatory.

Direct replay and journal folding retain complete-batch row preparation.
Direct replay applies those row/index operations with the ordinary applier;
folding uses its canonical applier after canonical-store preflight. Already
applied deletions remain idempotent. Ordinary empty writes still reject.

This landing changes callback boundaries, not the journal format or scalar
resource admission. It adds no numerical allowance, multiplier, optional
charging route or persisted certificate. The separate operation owners are
`replay_recovery_marker`, `fold_selected_journal_head` and `finish_recovery`.
Their existing marker/batch size caps do not establish instruction/temporary-
allocation bounds for every operation within a stage.

Next: required ConstructionBudget propagation together with actual scalar
output/conversion charging and typed optional-predicate error propagation.
Proposed implementation contract (not implemented): the shared codec receives a required borrowed
`ConstructionBudget`; an ordered-encoding construction failure retains its
`InternalError` rather than being converted into unsupported encoding. This
adds one internal error outcome, not another encoding mode: optional index
predicates may decline unsupported values but must propagate exhaustion.
Commit preflight owns one mutation-profile allocation for the complete batch
and also charges any enclosing durable-mutation execution; recovery owns one
allocation per separated stage. Neither ownership resets per row or component.
The shared index planner now derives after-image keys before the unchanged-
input shortcut. Both field-path and expression indexes reuse that derived key
if delta planning continues. Unchanged before-images still avoid encoding,
physical reads and index deltas; false partial-index membership still omits
the component. Required derivation errors now reject before the shortcut.
This closes the known zero-versus-one after-image gap structurally, not the
write-to-replay/final-verification allowance or IC instruction-headroom proof.
Every newly committed batch still needs resource admission before publication.
Do not retain a second encoder/estimator or install a larger replay profile as a
substitute for that proof.

R5 measurement qualification: the existing big-literal actor entity has no
secondary indexes, so its write probe does not qualify scalar index encoding.
The first IC attempt also found its default 256-byte field limit rejects wide
values before the index encoder. Qualification therefore uses one separate audit
entity with explicit 2,048-byte scalar bounds and fixed accepted DDL to add its
signed/unsigned indexes, then measures engine instructions and
whole-call cycles for single/batch writes at the maintained 4-KiB component
boundary. Oversized batches place the invalid value last and must publish no
rows. Need: reach the existing index-component boundary without changing the
unindexed baseline's schema. The smallest fixture is one three-field audit entity
using existing scalar types and ordinary accepted DDL. State-space delta: one
audit-only entity, two fixed index definitions and fixed test-admin hooks; no
production endpoint, new scalar type, budget, encoder or admission policy. Preserve
the existing unindexed measurement unchanged. These measurements qualify write
costs only; marker verification and complete replay allowance remain separate
unproven parts of R5. The complete 20-case IC matrix now passes; exact instruction,
cycle and artifact evidence is recorded in the current handoff in
[the status tracker](0.257-status.md). This provides a baseline for subsequent
encoder changes, not a numerical admission/headroom verdict.

Upgrade qualification now uses that same frozen actor for accepted single-row and
32-row writes followed by same-Wasm upgrade, behind an existing fixed backlog
so the small target batch cannot drain before upgrade. It checks actual startup
journal folding, gated admission, exact retained values, selected secondary-index
reads and a second debt-free upgrade. Watchdog instructions are separate from
the whole convergence-window cycle balance; totals include the filler backlog.
A successful write clears its commit marker, so this matrix does not exercise
marker-owned row verification and must not be described as complete replay/
final-verification qualification. It adds only integration coverage; no additional
actor hook, production route, budget or encoding policy. Current-worktree
dependency changes are not qualified by the frozen pre-change actor. See the
current status handoff for evidence.
All 24 upgrade cases pass. The maximum observed watchdog callback is 475,048,711
instructions; the second debt-free upgrade uses two callbacks and 66,558,778
total instructions for every tested shape. These fixed-workload observations
do not establish a universal write-to-recovery allowance.

### R5 qualification: final verification repeats accepted setup

Historical finding, repaired by the approved context-reuse landing below:
source inspection and a removed test-only probe on 2026-09-10 established another
difference between write work and final verification, not a demonstrated budget
failure:

- `executor/mutation/commit_window.rs::preflight_prepare_row_op_batch_structural`
  resolves one `CommitPrepareContext` per entity path/schema fingerprint across
  the batch. `runtime_entity_catalog.rs::prepare_row_commit_batch_for_replay`
  separately implements the same reuse pattern, with its required canonical
  replay authority selection.
- `commit/recovery.rs::verify_recovered_row_put` calls
  `runtime_entity_catalog.rs::prepare_row_commit_for_rebuild` for each surviving
  row. That helper reconstructs the accepted row/index/constraint schedule for
  every call. The verifier's target deduplication prevents repeated verification
  of the same row, not repeated accepted setup for different rows of one entity.
- The existing heap-backed mixed delete/update/insert recovery test passed with
  a temporary probe at `commit/prepare.rs::prepare_commit_context`: the mixed
  write built one context, replay built one, and final verification built two
  for its two surviving rows. Initial fixture insertion and the final independent
  uniqueness assertion each built another write context, outside that comparison.
  The probe was removed; no diagnostic endpoint, retained counter or fixture
  protocol was added.

These are construction-call observations, **not** cycle/instruction or allocation
measurements. They do not prove that recovery is slower overall, exceeds the
existing limit, or needs a larger profile. They do prevent treating equal scalar
key derivation alone as a proof of complete callback instruction headroom.

The user approved this bounded split. Write, replay and final verification now
share `CommitPrepareContextCache` at the existing commit-preparation owner. Each
operation fixes its existing preparation mode and retains successful setup per
entity path/schema fingerprint. Callers retain their current versus canonical
authority selection and per-row identity/schema checks. A failed setup is not
inserted; final verification drops the entire cache on return, including failure.
No context survives into a retry, another callback or catalog mutation. All
effect checks and the marker/admission barrier remain unchanged.

Retention accounting: final verification now retains one complete context per
distinct verified entity/schema pair until its callback returns, plus the cache
vector's backing capacity. Contexts retain accepted row/index/constraint metadata;
they do not retain row images or prepared row results. Write/replay already had
this lifetime; extraction removes their duplicate key tuples. Actual retained
bytes/peak and IC instruction deltas remain unmeasured. The future R5 authority
must charge these retained contexts and vector growth, not just key bytes.
Fewer constructions are not proof of a smaller memory peak or sufficient
instruction headroom. No new budget/profile, persisted state, callback stage or
verification shortcut is introduced. Scalar charging and the write-to-replay
allowance proof remain open; this landing does not complete R5 or `.explain()`.

### R5 propagation audit: integrity diagnostics

The 2026-09-10 caller audit found an additional maintained consumer that the
maintenance inventory omitted: `AcceptedIndexInspectionPlan::project` calls
the shared accepted key builders from both the row ForwardIndex atom and the
derived IndexEntry inspection. `DerivedInspectionLimits` and
`RowInspectionLimits` bound page/atom/decode work, not scalar conversion scratch
or bigint division. Encoder propagation must give these owners explicit page
construction authority; no per-witness reset or optional charging is acceptable.
This is required shared-caller propagation, not a new integrity feature.

The audit also found an independently reviewable error-classification bug:
`deep.rs::advance_job` mapped every candidate-page error to `Uninspectable`.
The row Relation atom already calls `RelationConstraintProjection::project_row`,
whose finite relation counters can produce the typed ExecutionBudgetExceeded
boundary. Such a failure is a resource limit, not failed accepted authority.
This handoff fixes that classification before changing the encoder contract.
All four Deep failure-to-terminal sites share `IntegrityTerminalOutcome::from_internal`;
typed execution-budget and indivisible-unit failures use the existing
`ResourceLimited` outcome. Other failures retain their authority class.
The current receipt format and acknowledgement protocol are unchanged.

The new tests cover typed classification, non-resource authority classes, Candid
outcomes and persisted resource-terminal receipts before/after acknowledgement.
They do not establish a page construction allowance or an exhausting production
fixture. R5 remains open; do not count this diagnostic repair as codec accounting.

Error seams identified in the same audit:

- `index/predicate/mod.rs::literal_index_component_bytes` currently uses `.ok()`;
  its compiler and both prepared/lazy execution callers need fallible optional
  results so exhaustion cannot become an omitted prefilter or cached absence.
- `index/plan/unique.rs::build_unique_index_key_from_row_slots` currently maps
  every key-build error to key-rebuild failure. Preserve typed construction
  failures when this caller receives the required authority.
- Schema staging already preserves its `KeyDerivation(InternalError)` through
  `StagedUserIndexDomainError::into_internal_error`; retain that behavior when
  propagating the budget. This is existing coverage, not another missing fix.

The combined encoder/admission outcome remains the next implementation target,
now with the complete integrity-caller boundary recorded. No additional public
variant, resource profile, persisted state or encoding format is introduced here.

#### Recovery construction boundary qualified by the current journal contract

- `journal/codec.rs::validate_accepted_schema_publication_family` rejects ordinary
  RowPut/RowDelete records in accepted-schema publication batches. JournalBatch
  construction, encoding and persisted decoding enforce the same admission.
- Schema publication therefore has no ordinary row transition to reconstruct;
  ordinary mutation batches can prepare against canonical accepted authority.
  The apparent candidate-row Apply dependency was an unreachable branch, not
  an observed production failure or a supported mixed-batch workflow.
- `commit/recovery.rs::prepare_recovered_row_transitions` prepares every ordinary
  row once. Online retirement no longer decodes an after-image solely to feed
  an unused reconstruction branch. Startup and online folding consume the
  prepared operations; neither constructs index keys during row Apply.
- Existing late-malformed-row/late-record tests protect pre-publication rejection.
  The added mixed update/delete regression checks new indexed values and reuse
  of both retired unique values through online and interrupted-startup folding.
  Existing journal-family admission tests are current-format boundary coverage,
  not tests preserving a retired API or predecessor format.
- The row-path deletion introduced no new budget, public option, persisted
  certificate or format. The subsequent approved stage split changes recovery
  callback boundaries; the final-verification work difference below remains.

### R5 write/replay dependency — admission qualification remains

The user approved working on this prerequisite. The follow-on qualification
found a real difference in performed construction, not an observed production
exhaustion. The maintenance landing did not cover ordinary commit preflight,
journal replay or final marker verification.

- `executor/mutation/commit_window.rs::open_commit_window_structural_inner`
  accumulates relation work across a batch, calls
  `preflight_prepare_row_op_batch_structural`, then creates the durable marker.
  That path does not install a construction tracker. The final optional
  instruction watermark does not establish one. Durable mutation jobs have an
  enclosing execution scope; ordinary writes cannot assume that scope exists.
- `runtime_entity_catalog.rs::prepare_row_commit_batch_for_replay` calls the same
  `prepare_row_commit_with_context` owner across recovered rows. Its relation
  budget is not a scalar encoder scratch/conversion budget. Direct replay now
  shares this batch owner. Final marker verification uses
  `prepare_row_commit_for_rebuild` and also lacks construction authority;
  fixing only the batch helper is insufficient.
- `commit/recovery.rs` rebuilds replay row operations from journal after-images
  and current canonical before-images before calling the batch helper. Recovery
  can therefore perform different key construction from original preflight.
  Equal numerical limits alone do not prove that every admitted commit can
  subsequently replay. This audit has not demonstrated an exhausting fixture.
- `commit/prepare.rs::prepare_row_commit_for_entity_impl` derives ordinary and
  candidate unique-index keys before producing prepared row operations. This is
  the shared propagation seam, not a safe place to reset counters per row/key.

The approved investigation initially proposed resolving commit/replay ownership
before encoder accounting. That is insufficient on its own. Prefer reusing the
explicit tracker and existing mutation profile, but do not simply wrap recovery
in a fresh maintenance allowance and call it safe. Do not install optional
ambient charging or a query-only codec.

#### Original qualification finding: final verification was not bounded by write work

The after-image ordering repair above addresses the skipped-key case below.
The original observations remain evidence for that change; they are not a
statement that the repaired planner still skips those after-images. Numerical
resource allowance and replay headroom qualification remain open.

- `index/plan/mod.rs::unchanged_index_inputs` lets normal mutation and fold
  planning skip a secondary index when its inputs did not change.
- `commit/recovery.rs::verify_recovered_row_put` constructs a new `CommitRowOp`
  with `before = None`, then calls `prepare_row_commit_op_for_rebuild`. That
  reconstructs all after-image index keys, including unchanged indexes omitted
  from the original mutation. This is the only caller of the derived-rebuild
  facade; the earlier reference to a separate live rebuild operation was too
  broad. Its actual owner is final marker verification.
- A temporary test-only probe at the existing scalar encoder and shared commit
  preparation boundary recorded three 9-byte components in the original
  three-entity insert, three during fold, and another three during verification.
  The existing row-prefix interruption test passed. Query assertions after
  recovery were outside these preparation phases.
- The new relation-only update regression is the stronger case: the update and
  fold encoded **zero** scalar components, while final verification encoded one
  9-byte payload component. The recovered row retained its indexed payload and
  the removed reverse relation released its target. The probe was removed;
  only this maintained-behavior regression remains.

These are construction observations, not cycle/instruction measurements or
allocator accounting. They do not demonstrate actual exhaustion at current
limits. They do disprove using original performed encoder work as a general
upper bound on final verification, even with one fresh allowance per phase.
The discrepancy can recur across multiple rows/indexes in a marker.

The canonical-fold concern is narrower than the earlier inventory implied:
`fold_selected_journal_head` prepares the complete batch before canonical
publication and commits its effects and watermark atomically; startup resets
disposable live overlays before selecting the oldest batch. Partial live-row
publication is not evidence of arbitrary canonical before-images. Preserve
these guarantees, rather than adding a new resumable fold route.

Approved next outcome: combine replay-safe write admission with the scalar
encoder landing, because its construction contract is needed to establish the
allowance. Keep the existing verification checks and budget profile unless a
different policy is explicitly approved. Merely adding a larger replay budget
does not establish the required bound. Removing full index verification changes
the recovery contract and is not an authorized shortcut. Reserving verification
work must also address the unchanged-index fast path and be qualified in IC
instructions/cycles; do not silently add a second encoder or recursive estimator.
The combined admission/encoder outcome remains incomplete. Shared after-image
derivation ordering is implemented; scalar charging, operation-wide authority
and the resource allowance proof are not. This is a checkpoint within R5, not
completion of that outcome or a new prerequisite outside it.
No new profile, persisted work certificate or weaker recovery verification
contract is introduced by the recovery-stage hard cut.

Required contract and qualification:

1. Reject over-budget new commit construction before durable marker/journal
   publication; preserve batch atomicity and candidate unique-index checks.
2. Establish how replay construction of every newly admitted commit fits its
   allowance, including changed before-images, partially applied recovery,
   multiple entities/indexes, and both single-row and batch recovery routes.
   Cover the maintained current format, not predecessor-format compatibility.
3. Preserve the existing durable mutation execution allocation when nested;
   the new authority must not reset or replace the job's enclosing counters.
4. Define final marker-verification lifetime at its actual owner, not per row.
   Keep earlier committed progress intact and preserve typed failure before new
   publication. No new retry/persisted state is authorized by this finding.

Need: one bounded encoder cannot silently omit legitimate non-query callers.
Simplest alternative (strict active execution adapter everywhere) rejects those
callers; optional charging leaves a bypass. Canonical ownership belongs at
commit preflight and recovery/rebuild operation boundaries, with explicit
authority passed through existing key construction. Working on this prerequisite
is approved; the exact additional admission policy is not yet settled. It changes
write/recovery resource admission even if it reuses counters and adds no public
mode or persisted representation.
The user has approved the recovery-stage hard cut described above. Any further
profile, persisted format or independent execution route still needs a decision.

### R5 maintenance authority and encoder sequencing

The user approved the split on 2026-09-10. The first landing implements explicit
maintenance construction ownership; the encoder landing remains open.

Contract and rationale:

- Need: shared encoding cannot rely on an active query tracker during schema
  maintenance. An optional ambient lookup would silently bypass bounds.
- Simplest owner: `MaintenanceConstructionBudget` beside the existing hard-budget
  tracker. Reuse `MUTATION_HARD_BUDGET` unchanged: 128 MiB temporary bytes,
  16,000,000 expression/nested-value steps, 30,000,000,000 instruction units and
  its existing 5,000,000,000-instruction failure reserve. Other resource ceilings
  remain those of that same profile, not a new maintenance configuration.
- Lifetime: one owner per unique-validation scan or migration validation,
  rewrite or final-validation page; migration programs within a page share it.
  Complete schema-index staging retains one owner across before/after projection,
  all observed rows and finish. No per-key or per-row counter reset.
- Failure: preserve the first typed exhaustion, reject later charges/segments
  and finalization, and return an error before the page result or staged
  replacement reaches publication. Soft page limits still yield as before.
  Hard exhaustion never becomes successful empty progress; an oversized item
  returns an explicit error on repeated attempts, not an internal retry loop.
  Earlier committed pages remain committed. No new persisted failure state or
  automatic retry behavior is introduced.
- State-space delta: one volatile existing tracker plus an optional fixed-size
  first-exhaustion record; no public mode, new resource, encoded discriminator,
  schema reconstruction, or thread-local fallback. It does not replace an active
  execution tracker. Ordinary write/request authority must still be propagated
  explicitly in the encoder landing; do not infer an active scope for every call.
- Qualification boundary: row visits, page source-byte visits, key derivation
  visits and instruction watermarks are charged now. Encoder scratch/output
  construction, numeric conversion and optional-predicate error propagation are
  NOT yet accounted. A post-key staged-output cap is still not an encoder bound.

The following preflight inventory explains why that ownership was required;
its descriptions of missing maintenance authority are historical findings.

The next codec change is not yet safe as mechanical signature propagation.
The following are current source findings, not observed production failures:

| Caller | Existing authority and missing contract |
| --- | --- |
| Query access lowering | `PreparationWork` already implements required `ConstructionBudget`; it can charge the shared encoder without another request lifetime. |
| Active execution | `ExecutionConstructionBudget` requires an active tracker and returns an invariant error when absent. `session/mutation_job.rs` installs a mutation scope for durable mutation advances; this does not establish one for every write, validation or migration caller. |
| Staged schema indexes | `StagedUserIndexDomainBudget` in `schema/mutation/user_index_domain.rs` counts source rows/bytes, row-index projection units and staged output. `derive_row` calls `PreparedUserIndex::derive_key` before charging the resulting key bytes; the key builder has no construction authority. Existing staged-byte accounting is not cumulative scratch accounting. |
| Constraint activation / migration | `UniqueConstraintProjection::derive_key` reaches the same rebuild key constructors. `constraint_activation_runner.rs::scan_unique_validation_page` has row/decoded/staged page limits, with staged bytes checked after key derivation. `migration_validation.rs` also carries page limits, not a codec construction authority. Neither key-derivation API accepts such authority. |
| Index predicates | `index/predicate/mod.rs::literal_index_component_bytes` calls `.ok()?` on encoding; `compile_index_program` and `compile_index_program_for_targets` return `Option`. Adding exhaustion to the encoder without changing this chain would turn resource failure into unavailable pushdown. |

Installing the strict execution adapter indiscriminately can reject legitimate
out-of-execution callers. Using the optional execution charger would leave them
unmetered. Charging scratch to staged-output bytes would silently change what
that existing limit means. None is an acceptable default implementation.

Approved sequencing:

1. Establish construction authority for the existing maintenance/validation
   operation lifetime. Inventory all write/rebuild callers, define the scratch
   and work allowance and its owner, and specify exhaustion versus page yield.
   Preserve progress/publication boundaries and prove that a single oversized
   row cannot produce an endless retry. Keep this private and volatile, with no
   new persisted format or public configuration. Any additional counters/limits
   require explicit design approval. Implemented using the existing mutation
   profile as described above, with no new numerical policy.
2. Land shared scalar encoding accounting with complete caller propagation:
   typed error preservation through predicate compilation, scalar/text/decimal/
   bigint construction checks, accepted enum encoding, and query/write/rebuild
   qualification. Retain one encoder and unchanged canonical bytes. Split further
   only if independently reviewable outcomes are identified and approved.

These boundaries stay within existing slice 11 / R5. The first is implemented;
the second remains the next implementation target. R5 and typed `.explain()` are
not complete, and no new minor is authorized.

## Original audit qualification

The original audit inspected source and updated planning documentation only. No runtime
code, tests, applications, Cargo versions or networks changed. No test suites,
timing probes or performance measurements were run. The preceding implementation
receipts remain valid for their recorded scopes; they are not whole-preparation
or typed-explain completion evidence.

Subsequent logical-clause, order-canonicalization and order-validation work and
focused qualification are recorded in the status handoffs. R4 remains partial.
