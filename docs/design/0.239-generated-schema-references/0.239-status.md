# IcyDB 0.239 Generated Schema References Status

Status: PATCH 4 COMPLETE — READY FOR USER REVIEW

Last updated: 2026-08-24

Design authority: [0.239 design](./0.239-design.md)

Evidence predecessor: `v0.238.0` at
`9865c37e6919e19ac986e1b03ebcae52649435b0`.

## Assignment

The user explicitly authorized the Toko Miner `ICYDB-012` correction as the
next IcyDB line on 2026-08-24. This is 0.239 rather than 0.238.1 because it is
an additive generated authoring surface unrelated to 0.238's ordered scalar
`DISTINCT` execution slice.

After publishing 0.239.0, the user explicitly authorized the first bounded
result of the module-surface cleanup audit: remove a test-only head-loading
operation from the production held-head stream protocol while preserving its
reference-oracle proof.

The user then authorized another bounded cleanup pass. Startup classification
and failure-receipt publication were verified as live driver-owned paths, so
their obsolete “next driver slice” dead-code suppressions can be removed.

The next explicitly requested cleanup pass narrows explicit entity-source
adoption planning to its real owners: migration-enabled production and its
no-feature test oracle.

## Patch tracker

| Patch | Canonical owner and bounded outcome | Delivery domains | Focused validation | Impact to report | Status |
| --- | --- | --- | --- | --- | --- |
| 1 | Model macros generate runtime-enabled entity/record `FieldRef` constants and entity source names; maintained examples and the renamed/schema-only fixtures prove the boundary | Build / Canister, Integration Tests, documentation | macro unit tests, model/runtime fixture checks, warning-denied focused checks | public generated API, raw Wasm, Candid/exports/stable format, files and line delta | Complete |
| 2 | Held-head key streams expose only production seek/consume capabilities; zero-target head loading remains owned by test oracles and physical fixtures | Canister, Unit Tests, documentation | warning-denied core checks, held-head oracle and physical-seek tests, layer invariants | internal surface, raw Wasm, Candid/exports/stable format, files and line delta | Complete |
| 3 | Remove staging-era dead-code suppressions from the now-live startup classifier and failure-receipt owner | Canister, Unit Tests, documentation | warning-denied all-feature and no-feature core checks, startup-owner tests, layer invariants | internal lint surface, runtime behavior, files and line delta | Complete |
| 4 | Compile explicit entity-source adoption planning only for migration-enabled production and its no-feature test oracle | Canister, Unit Tests, documentation | warning-denied migration/no-feature checks, focused adoption-planner test, layer invariants | feature surface, raw Wasm, Candid/exports/stable format, files and line delta | Complete |

No Patch 5 is planned. Any broader typed-field system, structural-write API
change, generated index vocabulary, downstream adoption, or further cleanup
finding is a separate decision.

## Frozen invariants

- Accepted schema snapshots remain runtime authority.
- Generated constants carry source spelling only.
- Schema-only consumers do not gain a runtime dependency.
- Runtime-enabled generated paths continue to support renamed dependencies.
- Queries and structural writes retain their one accepted-schema binding and
  execution routes.
- No compatibility layer or fallback reconstruction is introduced.

## Completed evidence

### Patch 1 — generated schema references

- All 154 `icydb-model-macros` tests pass, including entity, managed-field,
  record-member and entity-source token proofs.
- The renamed-dependency runtime fixture passes all six tests and compiles the
  generated constants through predicates, ordering, structural patches,
  embedded maps and generic structural insert selection.
- The schema-only fixture passes its test and all-target check without adding
  an `icydb` runtime dependency.
- Warning-denied all-target Clippy passes for the macro, model, runtime facade
  and both boundary fixtures. Schema/model boundary invariants also pass.
- The final deployable SQL audit Wasm is 4,459,680 raw bytes, +3,248 from the
  exact `v0.238.0` baseline of 4,456,432. Compiler-emitted Wasm is 5,098,544
  bytes, -1,617; deterministic gzip is 1,731,319 bytes, -417.
- The Candid SHA-256 remains
  `2df06ec3697e3392c16d80d0b2578c40005e489a3fb36cc75c7a17bd626c8ee5`,
  the final export count remains 70, and no stable representation changed.
- The complete worktree slice is 22 files, 552 inserted lines and 26 deleted
  lines including design, status, release notes, examples and focused proof.
  Production generation/facade code adds approximately 80 net lines across
  eight files. It introduces one immutable source-name trait and one shared
  macro emitter, with no mutable owner, cache, execution route or behavior
  axis.

### Patch 2 — held-head protocol narrowing

- Warning-denied all-feature, all-target Clippy passes for `icydb-core` before
  the focused test selections.
- All 10 repeated-pull held-head oracle tests and all 10 physical-seek tests
  pass. The complete repository invariant checker also passes, including layer
  authority.
- The SQL audit canister remains 5,098,544 compiler-emitted raw Wasm bytes. The
  final deployable artifact is 4,459,669 raw bytes, 11 below the recorded
  0.239.0 result; deterministic gzip is 1,731,681 bytes, 362 above it.
- Candid SHA-256 remains
  `2df06ec3697e3392c16d80d0b2578c40005e489a3fb36cc75c7a17bd626c8ee5`
  and the final export count remains 70. No stable representation changed.
- Maintained migration guidance states that `Entity::ENTITY` requires
  `EntitySource` in each using module and shows both the prelude and narrow
  anonymous-import forms, including the nested-test-module boundary.
- The focused code change is three files, 28 inserted lines and 42 deleted
  lines. It removes one trait requirement and two production forwarding
  methods, while keeping the repeated-pull semantic oracle test-only. There is
  no new mutable owner, cache, execution route or behavior axis. Including the
  tracker and release notes, the complete patch is seven files, 119 inserted
  lines and 48 deleted lines.

### Patch 3 — startup lint-surface cleanup

- Warning-denied all-feature/all-target and production no-feature Clippy both
  pass after removing the two suppressions, proving the paths are live in the
  maintained configurations.
- All 13 startup-owner tests pass, including classification, receipt encoding,
  publication, recovery and generated-schema failure behavior. The complete
  invariant checker also passes, including layer authority.
- The cleanup deletes 15 lines across the startup module and receipt owner. The
  cumulative `0.239.1` worktree is nine files, 147 inserted lines and 63 deleted
  lines. It changes no runtime statement, allocation, branch, mutable owner,
  state or behavior axis; the implementation shape is simpler.
- Because only lint attributes and obsolete staging comments were removed,
  there is no code-generation change to measure. The Patch 2 raw Wasm, Candid
  and export evidence therefore remains applicable.

### Patch 4 — migration-planner feature narrowing

- The adoption planner is now present only under `feature = "migration"` or
  `cfg(test)`. The production migration caller remains the sole runtime owner,
  while base production no longer compiles a dormant helper.
- Warning-denied all-feature/all-target and production no-feature Clippy pass.
  The focused no-feature adoption test also passes, proving the test oracle
  remains available, and the complete invariant checker passes.
- The code change is one insertion and two deletions in one production file,
  with no new type, owner, branch, cache, state or behavior axis.
- Compiler-emitted and final raw SQL audit Wasm remain 5,098,544 and 4,459,669
  bytes respectively; deterministic gzip is 1,731,679 bytes, two below Patch 2.
  Candid SHA-256 and the 70-export surface remain unchanged.

## Handoff

Patch 4 is the complete 0.239 line. Stop for user review and user-owned full
release validation; do not create Patch 5.
