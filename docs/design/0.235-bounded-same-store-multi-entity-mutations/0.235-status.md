# IcyDB 0.235 Bounded Same-Store Multi-Entity Mutations Status

Status: All three implementation patches complete; focused closeout passed

Last updated: 2026-08-20

Design authority: [0.235 design](./0.235-design.md)

## Inspection Baseline

- released predecessor `v0.234.0` at
  `8ef1cc07648076845e51e35e4d6788945b4a32e6`;
- Patch 1 started from clean post-release `main` at
  `8d4b6020590eb274ba180248a02369fc9b67e9e1` and does not edit Cargo package
  versions;
  and
- final 0.235 artifact comparison is recorded under Patch 2 evidence below.

## Demonstrated Need

Toko Miner `ICYDB-009` requires one synchronous local enrollment operation for
`User`, `UserPrincipal`, and `Robot` rows in one registered IcyDB store. The
current same-entity batch otherwise leaves the application responsible for
every partial committed prefix.

The design passes the no-build gate narrowly because the current marker,
journal, final overlay, multiple Identity range, entity-revision, and recovery
owners already carry most of the required atomic shape. 0.235 generalizes the
normal writer and preflight; it does not introduce transaction state.

## Frozen Decisions

- Requested primary rows must all resolve from one captured accepted runtime
  root and one registered store path.
- The existing structural batch method becomes multi-entity; the typed batch
  remains an adapter over it.
- Both general batch methods return `Vec<DynamicMutationResult>`, exactly one
  result per request in request order.
- Fixed limits are 4,096 operations, 64 distinct entities, 4 MiB per row,
  16 MiB staged keys/rows, 1 MiB encoded result, 16 MiB marker, and the existing
  16,384-unit prepared-row/index/Identity commit-work policy. Distinct context
  construction is independently bounded by the 64-entity limit.
- One current operation timestamp covers every item.
- The complete final overlay is visible to every entity's uniqueness and
  relation validation.
- Multiple Identity owners retain one tentative cursor/range per owner and
  commit under the same marker.
- Current marker and journal version-1 formats are unchanged.
- Batch-local references to generated values are unsupported. Newly related
  rows require caller-known keys.
- Toko's adoption fixture uses an ordinary required `User.id: Ulid` generated
  once as `Id<User>` by application code before constructing real generated
  typed writes. Generated scalar primary-key and direct scalar-relation inputs
  preserve `Id<E>` even though accepted row storage remains raw key material.
  0.235 adds no preallocation or explicit-value bypass for a schema-generated
  field.
- A facade-only mixed typed builder resolves each generated input's binding and
  produces sealed entity-typed item handles over the canonical ordered dynamic
  result vector. It adds no executor, core result schema, wire encoding, or
  durable state.
- Toko's Principal primary key, indexed `user_id` relation, and unique
  `(user_id, authentication_principal)` membership index all remain in the
  proof fixture.
- Lost-response idempotency remains application-owned.
- There is no cross-store direct row batch, cross-canister transaction,
  rollback, continuation, caller-selected budget, or second executor.

## Patch Tracker

| Patch | Outcome | Status |
| --- | --- | --- |
| 1 | Generalize the canonical structural writer and commit preflight into one bounded same-store multi-entity atomic flow | Complete |
| 2 | Converge `Id<E>` write inputs, mixed-binding builder/handles, downstream proof, and closeout evidence | Complete |
| 3 | Close the derived-recovery and 64-context IC evidence gaps and reconcile current documentation | Complete |

These three patches are the complete intended 0.235 line. Patch 3 is the
bounded correction produced by the final closeout audit. One patch is one
default implementation-turn and worktree-handoff boundary.

## Patch 1 Boundary

Patch 1 owns:

- one-root/same-store structural admission;
- the 64-entity bound and privacy-safe rejection facts;
- global exact staged/result/marker/work policy;
- batch-wide constraint scheduling and deleted-target grouping;
- per-entity accepted commit contexts and Identity cursors;
- ordered heterogeneous results;
- multi-entity journal/revision/recovery proof; and
- direct transaction, atomicity, relation, identity, and facade documentation.

Patch 1 may not add typed-only execution, generated-value aliases,
idempotency receipts, cross-store direct mutations, rollback, transaction
state, or compatibility wrappers.

## Patch 2 Boundary

Patch 2 owns:

- current mixed-binding typed-write admission over Patch 1;
- generated scalar primary-key and direct scalar-relation write inputs typed as
  `Id<E>` (or `Vec<Id<E>>` for a many relation), with raw key lowering retained
  inside the generated adapter and composite relation components unchanged;
- a binding-resolving mixed typed builder and sealed item handles over Patch
  1's canonical result vector;
- one Toko-shaped User/membership/Robot proof using real generated adapters and
  an ordinary application-generated `UserId`;
- one maintained no-SQL example that compile-checks fallible
  `Id::<User>::generate().map_err(Error::from)` through the public facade;
- retained compound membership uniqueness plus a negative generated-ULID
  authorship/bypass proof;
- stale and mixed-store typed rejection;
- result projection/order proof without post-commit mutation failure;
- representative PocketIC instructions and stable-state evidence;
- final Candid/export and raw-Wasm comparison; and
- final complexity and closeout reconciliation.

Patch 2 may not add another core/executor or wire batch result shape,
typed-specific budgets, a generated-model runtime route, generated-key
preallocation/override, or any deferred transaction machinery. Its typed
result view may only retain Patch 1's ordered dynamic vector and already issued
opaque bindings. A wrong-owner handle uses one payload-free
`BatchHandleMismatch` adapter reason. The `Id<E>` Rust input hard cut retains
the exact raw key Candid representation and must be verified as wire-
equivalent rather than assumed.

## Patch 3 Boundary

Patch 3 owns only closeout proof and documentation corrections:

- extend the existing three-entity interruption fixture with one retained
  cross-entity reverse relation and explicit indexed lookup assertions;
- measure the exact 64-distinct-entity context boundary in the maintained
  PocketIC closeout probe;
- refresh the marker, instruction, stable-state, complexity, and artifact
  evidence after that fixture change; and
- reconcile the design status and root same-store guidance.

Patch 3 adds no production execution behavior, format, state, public API,
budget, recovery route, or compatibility surface.

## Downstream Review Resolution

The first external review found one implementation blocker: the design's
caller-known User key did not distinguish an ordinary required ULID from
Toko's then-proposed `generated(insert = "Ulid::generate")` field. Generated
typed inserts omit that field and the structural writer correctly rejects an
explicit value.

The smaller resolution is frozen here. Toko's 0.235 adoption shape changes
`User.id` to an ordinary required ULID and generates it once in application
code as `Id<User>` before batch construction. IcyDB does not add a new
generator mode or write-policy exception. The same review confirmed that Toko
intentionally retains unique `(user_id, authentication_principal)` membership
authority; the Patch 2 proof must retain and exercise it.

The next downstream review found no atomic-execution blocker but identified a
typed-facade gap: generated write inputs erase entity identity to raw `Ulid`,
and callers must resolve three bindings and coordinate heterogeneous result
indexes manually. Patch 2 now closes that gap without widening the executor.
Generated scalar User identity positions use `Id<User>`, each generated write
associates its entity for automatic binding, and sealed builder-issued handles
map back to the existing ordered dynamic results. Miner still owns key
retention, authenticated Principal resolution, value construction,
authorization, retry operation IDs, lost-response classification, and domain
errors.

The maintained public-facade example uses
`Id::<User>::generate().map_err(Error::from)`, followed by construction of the
real three-item typed batch from the successful typed key. This documents the
existing `Id<E>` generation and public `Error` conversion surfaces; it adds no
new generator mode, error class, persistence, or execution path.

## Patch 1 Implementation Facts

- General structural admission resolves every item from one captured accepted
  runtime root, rejects cross-store input before the operation timestamp, and
  caps distinct entity tags at 64.
- The canonical structural state machine retains one operation timestamp, one
  global staged-byte ledger and target-key set, plus bounded per-entity
  Identity cursor state.
- Commit preflight caches exact contexts by entity path and fingerprint, using
  the same ownership rule as replay.
- The preflight row overlay itself is already entity-qualified and resolves
  fallback rows by entity tag.
- Recovery caches contexts by entity path/fingerprint and reads one
  canonical final-row overlay for a retained row-op batch.
- Commit payload construction already supports ordered records from multiple
  entity paths, multiple Identity owners, one database commit sequence, and
  multiple marker-local journal batches.
- Journal append already derives the affected entity-tag set and advances
  entity-scoped mutation revisions.
- The public prelude re-exports `Id`, `Id::<E>::generate()` delegates to the
  entity key's `GenerateKey`, and public `Error` converts its `InternalError`.
- At the Patch 1 boundary, generated write structs still exposed scalar
  primary-key and relation positions as raw schema types and callers resolved
  bindings/result indexes manually. Patch 2 replaces those facade shapes as
  recorded below.
- Delete relation validation consumes first-appearance target-path groups over
  that same complete overlay.
- The general and typed batches return an ordered vector containing one exact
  one-row result per request; single mutation and same-entity insert
  convenience retain their existing result owners.
- Marker and journal encodings are unchanged. Normal apply and recovery carry
  ordered multi-entity records, multiple Identity owners, and per-touched-
  entity journal revisions under one marker.

## Patch 1 Focused Evidence

- Exact 64-entity admission succeeds; the 65th rejects with bounded
  `ActualCount=65` and `Limit=64` facts.
- The 64-entity fixture proves one managed timestamp across all results.
- Same-store mixed rows, equal primitive keys under different entity tags,
  cross-entity target creation/deletion, unique swaps/releases, late failures,
  and cross-store rejection all pass focused native tests.
- Journaled two-entity Identity batches recover after marker, journal, first-
  row, all-row, and state-materialization interruptions with ordered row paths,
  both high-water marks, and both entity revisions intact.
- The maintained PocketIC closeout probe measures 5,940,421 instructions for
  three independently committed Identity inserts and 4,556,130 for the same
  three entities in one atomic batch: 1,384,291 fewer instructions (23.3%) and
  more than 99.98% headroom below the 40 billion instruction ceiling. Its
  existing 4,095-row maximum batch remains bounded at 1,358,722,288
  instructions.
- The representative `one_entity_typed_query` artifact is 2,117,462 raw Wasm
  bytes, 122 bytes smaller than `v0.234.0` (0.006%); compiler Wasm is 2,417,841
  bytes (-292), gzip is 811,494 bytes (+774), and Candid/export counts remain
  64 bytes/six exports. Raw Wasm is the primary comparison.
- Patch 1 adds no stable allocation or marker/journal format. Patch 2 records
  the exact retained marker/journal sizes and stable-memory comparison rather
  than treating unchanged code shape as byte-level proof.
- Diagnostic registry and exact fact-schema tests pass. Package-local compile,
  lint, invariant, instruction, and artifact checks pass; the user owns full
  repository validation.

## Patch 2 Implementation Facts

- Generated authored scalar primary-key inputs use `Id<Entity>`. Direct scalar
  relation inputs use `Id<Target>` or `Vec<Id<Target>>`; optionality remains a
  `WriteCell::Null` intent. Ordinary non-relation keys and composite relation
  components retain their declared primitive types.
- Generated adapters lower each typed identity to the exact raw key before
  accepted write admission. `Id<User>` and raw `Ulid` retain byte-identical
  Candid encoding, while compile-fail fixtures reject both raw-ULID and wrong-
  entity substitutions at generated input sites.
- `TypedWriteAdapter` identifies its generated entity. The ephemeral mixed
  builder resolves each current binding, dispatches once through the existing
  typed vector and structural executor, and returns sealed entity-typed handles
  over the existing ordered dynamic result vector.
- Handle ownership uses process-local allocation identity; a handle from
  another builder returns payload-free `BatchHandleMismatch`. No handle,
  binding, continuation, or result index becomes durable or caller-authored.
- Typed core admission re-resolves all bindings from the first binding's
  captured accepted root, rejects a stale binding without execution, rejects a
  cross-store item through the structural store boundary, and retains the same
  global operation, entity, staged-byte, result-byte, marker, and work limits.
- The maintained Toko-shaped schema uses one ordinary application-generated
  `User.id`, Principal-keyed membership, indexed `user_id`, retained unique
  `(user_id, authentication_principal)`, and a schema-generated Robot ULID.
  The generated-field omission and structural authorship rejection remain
  unchanged.
- The public guide and maintained no-SQL compile target demonstrate fallible
  `Id::<User>::generate().map_err(Error::from)` and automatic construction of
  the real User, membership, and Robot batch.

## Patch 2 Focused Evidence

- Generated macro tests, renamed-dependency adapter tests, the maintained
  no-SQL enrollment target, and dedicated compile-fail fixtures pass. They
  cover ordinary/generated and non-ULID scalar keys, required/optional/many
  direct relations, raw non-relation ULID, composite relation components, and
  raw/wrong-entity identity rejection.
- Core typed tests pass for mixed current bindings, input/result order,
  all-or-nothing late failure, stale authority, and cross-store rejection.
  All five marker/journal/row/state interruption tests pass with three
  journaled entities, exact Identity ranges, entity revisions, and paths.
- The retained three-entity recovery marker is exactly 770 bytes, including
  one 740-byte embedded journal batch. Current version-1 marker and journal
  encodings and the existing commit allocation are unchanged.
- The Toko-shaped PocketIC batch measures 18,146,812 instructions versus
  18,786,254 for three typed commits: 639,442 fewer (3.4%). The exact
  64-distinct-entity batch measures 59,134,225 instructions and retains more
  than 99.85% headroom below the 40 billion message ceiling. The independent
  4,095-row prepared-work boundary measures 1,564,725,324 instructions, and the
  stable-memory extent is exactly 511,770,624 bytes before and after the probe.
- The same real-canister proof decodes all three handle-selected rows, retains
  both membership indexes, rejects a generated Robot key bypass, rejects a
  foreign-builder handle, and proves a late membership conflict leaves neither
  its staged User nor Robot behind.
- Final representative `one_entity_typed_query` artifacts are 2,418,071
  compiler Wasm bytes, 2,117,677 raw deployable bytes, and 811,539 deterministic
  gzip bytes. Against `v0.234.0`, the primary raw delta is +93 bytes (0.0044%),
  compiler Wasm is -62 bytes, and gzip is +819 bytes. Candid remains 64 bytes
  and the six exports are unchanged.
- The final line touches 45 files at approximately +3,040/-424 lines including
  tests and documentation. Runtime shape becomes moderately more complex only
  in the existing per-entity scheduler/context set and typed facade; it retains
  one mutation state machine, executor, marker, journal, result vector, and
  accepted-schema authority with no new persisted state or behavior mode.
- Focused compile, lint, native, recovery, PocketIC, artifact, formatting, and
  repository invariant checks pass. The user owns complete repository
  validation and release publication.

## Patch 3 Closeout Evidence

- All five multi-entity interruption boundaries now recover three ordinary
  payload indexes plus one cross-entity reverse relation. Indexed lookup finds
  each recovered row, and deleting the retained target fails with the exact
  relation diagnostic after every recovery boundary.
- The relation-bearing source row increases the exact retained marker and
  journal batch by 18 bytes to 770 and 740 bytes respectively. This is row
  payload attribution inside the unchanged current version-1 encodings; no
  allocation or format changed.
- The exact 64-entity PocketIC batch completes at 59,134,225 instructions.
  Current three-entity structural and typed controls, the 4,095-row work
  boundary, and the unchanged stable extent were rerun in the same probe.
- The representative SQL-free artifact remains 2,418,071 compiler Wasm bytes,
  2,117,677 raw deployable bytes, 811,539 deterministic gzip bytes, 64 Candid
  bytes, and six function exports because Patch 3 changes test fixtures and
  documentation only.

## Validation Ownership

Each patch runs focused native, compile, recovery, and applicable PocketIC
checks only. The user owns the full repository validation workflow and release
publication.

0.235 changes the existing writer, generated Rust input types, and Rust facade
but adds no persisted format, production Candid endpoint/export, or stable
allocation. Its maintained test-only performance record gains two instruction
fields. Complete repository validation remains user-owned.
