# IcyDB 0.233 Durable Mutation-Job Liveness And Recovery Status

Status: proposed; implementation not started

Last updated: 2026-08-19

Design authority: [0.233 design](./0.233-design.md)

## Patch Tracker

| Patch | Outcome | Status |
| --- | --- | --- |
| 1 | Separate frozen statement time from per-advance managed-write time and prove later matching rows converge | Planned |
| 2 | Add writer-owned Forward byte packing and Verify scan-byte budgeting without checkpoint skips | Planned |
| 3 | Add sequence-zero cancellation, bounded progress inventory, and reserved integrity capacity | Planned |
| 4 | Add entity-scoped journal mutation revisions and isolate Verify from unrelated entity writes | Planned |
| 5 | Add bounded target/lifecycle diagnostics, repair current documentation, and complete real-canister/artifact closeout | Planned |

The five patches are the complete planned 0.233 line. One patch is one default
implementation-turn and worktree-handoff boundary. Generic continuation means
exactly the next Planned patch and never combines patches automatically.

## Promotion Evidence

Toko's durable mutation-job adoption audit established three immediate
blockers:

- a frozen start timestamp can make a later matching managed-timestamp row
  permanently unexecutable;
- row-count-only Forward admission can repeatedly select a page that exceeds
  structural staging bytes; and
- an unadvanced Active job has no safe cancellation path and permanently
  retains shared capacity.

Current source review also confirmed:

- Verify observes store-wide rather than entity-specific mutation revision;
- separate multi-job starts have no rollback, making sequence-zero cancellation
  the smallest coordinator recovery primitive;
- target errors and lifecycle metrics do not preserve the operational reason;
  and
- current documentation still says 224/56 and retains the removed
  caller-custodied mutation continuation description.

Commit-marker atomicity, exact successful-request replay, bounded count-based
happy-path steps, stable Verify safety, and upgrade recovery remain accepted
foundations. The promoted gap is safe failure without guaranteed progress.

## Accepted Predecessor

Pending. Before Patch 1 implementation or measurement begins, record the exact
final 0.232 tag, commit, tree, and subject. The current dirty worktree is not an
artifact baseline.

## Current Decisions

- Toko adoption remains blocked until Patches 1 through 3 land and pass their
  cross-message/wide-row recovery proofs.
- Patch 4 is required before production adoption under sustained multi-entity
  traffic.
- Statement time remains frozen; writer-managed time becomes per advance.
- Byte budgets are fixed engine policy and use structural-writer accounting.
- An anomalous managed-time regression or one candidate that cannot fit the
  current policy becomes a bounded terminal `RestartRequired` reason rather
  than an unchanged Active retry loop.
- Cancellation is limited to the exact initial sequence-zero state.
- The global 64-record cap remains; eight slots are reserved for integrity
  admission rather than adding owner quotas.
- Generic store-wide revision consumers remain unchanged; only mutation Verify
  uses entity revision.
- Verify restart cause is reported through bounded metrics rather than changing
  the persisted replay-receipt shape solely for observability.
- Atomic multi-job start, partial abandonment, rollback, ownership, and quotas
  remain out of scope.

## Implementation Status

No production code, tests, fixtures, public API, persisted state, metrics,
documentation authority, changelog, or artifact baseline has been changed for
0.233.

The design and this tracker are the only 0.233 files at this cut.

## Validation Status

Design review still required before Patch 1 begins.

Planned focused proof owners are recorded in the design. Repository-wide and
full-workspace validation remain user-owned and have not been run for this
design-only cut.

## Cost And Complexity Status

No runtime, instruction, stable-memory, Candid, or Wasm delta exists yet.

The planned state-space delta is one entity-tag journal revision authority.
Cancellation, inventory, fixed byte bounds, capacity reservation, diagnostic
reasons, and metrics extend existing owners without adding a second mutation
state machine. Every implementation handoff must report actual files, line
delta, delivery domains, performance, raw Wasm, and complexity shape.
