# IcyDB 0.233 Durable Mutation-Job Liveness And Recovery Status

Status: implementation in progress; Patches 1-4 complete

Last updated: 2026-08-19

Design authority: [0.233 design](./0.233-design.md)

## Patch Tracker

| Patch | Outcome | Status |
| --- | --- | --- |
| 1 | Separate frozen statement time from per-advance managed-write time and prove later matching rows converge | Complete |
| 2 | Add independent Forward scan/staged-byte packing and Verify scan-byte budgeting without checkpoint skips | Complete |
| 3 | Add fresh-ID sequence-zero cancellation, fail-closed bounded inventory, and exact reserved integrity capacity | Complete |
| 4 | Initialize entity-scoped journal revisions and retain one target revision across all Verify pages | Complete |
| 5 | Add bounded target/lifecycle diagnostics, repair current documentation, and complete real-canister/artifact closeout | Planned |

The five patches are the complete planned 0.233 line. One patch is one default
implementation-turn and worktree-handoff boundary. Generic continuation means
exactly the next Planned patch and never combines patches automatically.

Patches 1 through 4 are reviewable landings but are not independent deployment,
upgrade, tag, or release candidates. Only the complete Patch 5 line carries
the final 0.233 policy identity and is eligible for deployment.

## Promotion Evidence

Toko's durable mutation-job adoption audit established three immediate
blockers:

- a frozen start timestamp can make a later matching managed-timestamp row
  permanently unexecutable;
- row-count-only Forward admission can scan roughly 832 MiB of maximum-size
  nonmatching rows or repeatedly select a matching prefix that exceeds
  structural staging bytes; and
- an unadvanced Active job has no safe cancellation path and permanently
  retains shared capacity.

Current source review also confirmed:

- Verify observes store-wide rather than entity-specific mutation revision;
- separate multi-job starts have no rollback, making sequence-zero cancellation
  the smallest coordinator recovery primitive;
- target errors and lifecycle metrics do not preserve the operational reason;
  and
- current documentation still says 224/56 and retains the removed
  caller-custodied mutation continuation description.

Commit-marker atomicity, exact successful-request replay, bounded count-based
happy-path steps, stable Verify safety, and upgrade recovery remain accepted
foundations. The promoted gap is safe failure without guaranteed progress.

## Accepted Predecessor

Patch 1 uses the exact published predecessor:

- tag: `v0.232.1`;
- commit: `95dac0675538c539b4f7f19a74d718d4f030cebe`;
- tree: `e2cdba36fc4a4c6a4742e94e1dd1dc26a3a69b13`; and
- subject: `Release 0.232.1`.

The tag and `origin/main` resolved to that commit from a clean tracked
worktree before 0.233 changes began.

## Current Decisions

- Patches 1 through 4 now satisfy the mutation-job liveness prerequisites for
  Toko adoption, including cross-message, wide-row, and sustained multi-entity
  recovery proofs; Patch 5 remains the release-line diagnostics and closeout
  boundary.
- Statement time remains frozen; writer-managed time becomes per advance.
- Forward gets independent raw scan-byte and exact writer-owned staged-byte
  budgets; Verify keeps a separate raw scan-byte budget.
- An anomalous managed-time regression or one candidate that cannot fit the
  current policy becomes a bounded terminal `RestartRequired` reason rather
  than an unchanged Active retry loop.
- An admitted deterministic execution-budget failure also becomes terminal
  `RestartRequired(ExecutionBudgetPolicyExceeded)`; it cannot leave the same
  checkpoint Active.
- Cancellation is limited to the exact initial sequence-zero state, and every
  logical job incarnation requires a fresh job ID.
- Inventory validates all retained slots and returns typed
  `CorruptProgressStore` rather than partial capacity on corruption.
- With the global cap at 64 and reservation at eight, non-integrity insertion
  rejects at pre-insert count 56 or greater; integrity insertion may reach 64.
- Generic store-wide revision consumers remain unchanged; only mutation Verify
  uses entity revision.
- The current-convergence gate initializes every accepted predecessor entity
  tag to the validated store-wide revision; afterward missing metadata fails
  closed with no fallback.
- Forward-to-Verify stores the journal-prepared exact resulting target
  revision, and every Verify page compares that one retained baseline before
  and after its scan.
- Forward preparation has one measured fixed transient peak and does not clone
  a second page-sized candidate buffer.
- Verify restart cause is reported through bounded metrics rather than changing
  the persisted replay-receipt shape solely for observability.
- Atomic multi-job start, partial abandonment, rollback, ownership, and quotas
  remain out of scope.

## Implementation Status

Patch 1 is implemented as the `0.233.0` worktree handoff:

- retained operation time remains canonical statement time;
- Forward captures one current managed-write timestamp after authority and
  runtime validation and passes it to the structural writer for the whole page;
- the structural writer owns the shared monotonicity predicate;
- anomalous managed-time regression commits terminal
  `RestartRequired(ManagedTimestampRegression)` progress before row staging;
- the timestamp-policy component changes the batch-policy identity; and
- test-admin audit endpoints drive one advance per PocketIC message without
  changing the production export set.

No new job state, progress allocation, timestamp field, compatibility path, or
second mutation executor was added.

Patch 2 is implemented in the same `0.233.0` worktree handoff:

- Forward and Verify each enforce an independent 16 MiB raw-key-plus-row scan
  budget before full decode while retaining their 208-key maxima;
- Forward retains its 56-row maximum and packs against the structural writer's
  exact 16 MiB key/canonical-before/materialized-after staging charge;
- a pull-based scanner presents one candidate at a time, advances a pending
  candidate checkpoint only after writer admission, and never reads beyond the
  first staging-rejected candidate;
- the next advance revisits that excluded candidate, while scan-admitted
  nonmatching rows advance normally;
- one individually impossible candidate terminalizes as
  `RestartRequired(CandidateExceedsBatchPolicy)` without target-row effects;
- the final Forward/Verify count and byte limits and checkpoint rules all bind
  the current batch-policy identity; and
- progress-only pages and row-mutation pages continue through the existing
  progress replacement and structural commit authorities.

The current maximum valid raw key plus row is 4,194,566 bytes, and the maximum
valid exact key/before/after charge is 8,388,870 bytes, so both 16 MiB policies
admit one maximum valid row. The fixed row-buffer portion of the transient peak
is at most 25,166,086 bytes plus bounded writer metadata; no second page-sized
candidate vector is retained.

Patch 3 is implemented as the `0.233.1` worktree handoff:

- the trusted session and public Rust facade expose idempotent
  `cancel_unadvanced_mutation_job`, limited to a decoded current-format initial
  Forward continuation and the exact sequence-zero/no-effects state;
- cancellation validates and removes under one shared progress-store borrow,
  returns success when absent, rejects every nonzero retained sequence, and
  deliberately accepts a prior batch-policy identity without executing it;
- the application contract makes every mutation-job ID a single-incarnation
  identity, so a later logical job must use a fresh ID and no cancellation
  tombstone or reusable-ID ABA state is added;
- one trusted `progress_job_inventory` validates every retained key, envelope,
  family payload, and state closure before returning capacity, family counts,
  IDs, bounded lifecycle, and sequence facts; one bad slot fails the entire
  operation with `CorruptProgressStore`;
- mutation and generic resumable insertion now reject when the pre-insert
  retained count is 56 or greater, while integrity work may consume the exact
  eight reserved slots up to the unchanged hard limit of 64; and
- existing retained records and replay lookup remain valid at or above the
  non-integrity threshold, with no persisted quota, owner identity, format
  version, progress allocation, or public canister endpoint added.

Patch 4 is implemented as the `0.233.2` worktree handoff:

- `JournalTailStore` owns one current version-1 entity-revision control marker
  plus sorted packed revision chunks bounded by the accepted 4,096-entity
  schema limit;
- current convergence initializes a missing authority from the validated
  store-wide data revision without scanning rows, while a present incomplete,
  malformed, or accepted-tag-mismatched authority fails closed;
- direct initial schema publication and journaled schema publication both
  publish the exact accepted entity set, preserving retained revisions and
  conservatively initializing newly accepted tags;
- each row batch advances only its distinct affected entity tags to the exact
  journal-owned post-batch revision, including idempotent marker replay, while
  folding leaves the authority intact;
- Forward obtains the mutating page's exact resulting target revision from
  journal preparation, and zero-update Forward obtains the current target
  revision from the same owner; and
- every Verify message compares the same retained target baseline before and
  after its scan, retains it across intermediate checkpoints, ignores writes
  to other entities in the same store, and restarts on a target-entity write.

The policy-identity revision moves to `2`. A retained mutation job created by
an earlier 0.233 landing therefore follows the existing terminal
`BatchPolicyChanged` restart contract rather than executing under mixed Verify
semantics.

## Validation Status

The full implementation-readiness audit is closed and the exact predecessor
gate is satisfied. Focused Patches 1 through 4 evidence passes:

- native structural-writer monotonicity, policy-identity drift, terminal
  payload round-trip, and terminal acknowledgement tests;
- a multi-message PocketIC start -> later matching write -> exact replay ->
  stable completion proof;
- warning-denied Clippy for the affected core, integration, and audit-canister
  targets; and
- the production `sql_perf` Wasm/Candid report.

Patch 2 additionally proves:

- exact, maximum, and maximum-plus-one writer staging and raw scan accounting;
- maximum-row admission and the fixed transient row-buffer relationship;
- a 20-row, 900 KiB-per-row PocketIC fixture where a nonmatching Forward page
  stops at 18 scan-admitted rows and exact staging admits nine matching rows
  before scanning and revisiting the tenth candidate;
- exact replay plus eventual Forward/Verify completion with no skipped rows;
- one maximum accepted 64-secondary-index row can update and reach stable
  completion; and
- the existing later-write managed-time and revision-drift Verify flows remain
  convergent under byte packing.

Patch 3 additionally proves:

- current initial continuation acceptance, prior-policy cancellation, and
  malformed/noninitial rejection at the continuation owner;
- sequence-zero success, stale/nonzero/terminal rejection, absent retry, and
  failed-validation retention at the shared-store owner;
- complete three-family inventory plus whole-operation failure for one
  undecodable retained slot;
- exact non-integrity admission at retained counts 55 and 56, integrity
  reservation use at 63 and 64, replay at the threshold, and full-capacity
  rejection;
- a multi-message PocketIC cancellation -> lost-response retry -> fresh-ID
  start -> delayed old cancellation flow that retains the new incarnation;
  and
- warning-denied core, facade, integration, and audit-canister builds with the
  production cancellation/inventory wrappers absent from the export surface.

Patch 4 additionally proves:

- conservative missing-authority initialization and exact reentry, plus
  fail-closed missing-entry and accepted-tag drift;
- per-entity advance isolation, multi-entity row batches, identical replay,
  fold survival, direct initial schema publication, and row/progress atomicity
  at all four retained marker interruption boundaries;
- one original Verify revision surviving an intermediate continuation
  round-trip;
- a same-store write to `PerfAuditMutationToken` between
  `PerfAuditJournaledUser` Verify pages does not restart, while the subsequent
  target-entity write restarts exactly once and the job completes;
- packed metadata at three entities and the accepted 4,096-entity limit both
  remain within the journal allocation's existing nine 64 KiB stable pages,
  for zero measured page growth; and
- warning-denied core, audit-canister, and focused integration targets.

PocketIC itself rejects moving subnet time backward before a canister message
is delivered, so the anomalous-clock terminal path is proved at its native
writer, persisted-record, and acknowledgement owners rather than through a
synthetic backward-time PocketIC call. The repository-wide suite remains
user-owned and was not run.

## Cost And Complexity Status

Against released `v0.232.1`, Patch 1 measures:

- compiler Wasm: 5,019,058 bytes, `+12`;
- final raw Wasm: 4,382,571 bytes, `-1,407` (`-0.0321%`);
- deterministic gzip: 1,700,312 bytes, `+536`;
- Candid: 60,740 bytes, `+100`, solely the new restart variant;
- final exports: 70, unchanged; and
- focused Forward peak: 28,549,314 instructions for a 56-row page, below the
  unchanged 31,000,000 ceiling.

The isolated exact-predecessor instruction rerun compiled but its PocketIC
server failed to bind, so no instruction delta is claimed and the first
infrastructure failure was not repeatedly rerun. Patch 1 changes 12 worktree
files at approximately `+365/-59` lines after changelog/status closure.
Complexity increases narrowly by one restart reason and one shared read-only
predicate; the execution flow, persisted record shape, stable allocations, and
state machine remain unchanged.

Patch 2 measures against the Patch 1 worktree artifact:

- compiler Wasm: 5,023,355 bytes, `+4,297`;
- final raw Wasm: 4,387,738 bytes, `+5,167` (`+0.118%`);
- deterministic gzip: 1,700,866 bytes, `+554`;
- Candid: 60,847 bytes, `+107`, for
  `CandidateExceedsBatchPolicy`;
- final exports: 70, unchanged, with the wide-row loader remaining test-admin
  only; and
- focused Forward peak: 31,905,351 instructions (`+3,356,037`, `+11.8%` from
  the Patch 1 peak), below the recalibrated 34,000,000 review ceiling; stable
  Verify peaks at 7,462,037 under its unchanged 8,000,000 ceiling.

Patch 2 changes approximately ten delivery files at `+900/-200` lines beyond
Patch 1; the combined Patches 1-2 worktree spans 14 files. Complexity increases
in the local Forward packing flow because exact writer admission now controls
checkpoint publication, but remains neutral in durable state space: no
persisted field, allocation, mode, or mutation executor was added.

Patch 3 measures against the pushed Patch 2 artifact:

- compiler Wasm: 5,024,024 bytes, `+669`;
- final raw Wasm: 4,388,468 bytes, `+730` (`+0.0166%`);
- deterministic gzip: 1,700,813 bytes, `-53`;
- Candid: 60,847 bytes, unchanged;
- final exports: 70, unchanged, with cancellation and inventory wrappers
  remaining test-admin only;
- exact cancellation: 144,089 instructions; absent retry: 37,249;
- complete one-record inventory: 116,690 instructions; complete 64-record
  inventory: 5,869,961 under the 7,000,000 review ceiling; and
- excluded progress allocation at retained counts 55/56/63/64:
  38,993,920 / 38,993,920 / 43,319,296 / 43,319,296 bytes.

Patch 3 changes 12 delivery files at approximately `+938/-47` lines before
status/changelog closure. Complexity increases in one shared decoder-backed
inventory projection and one cancellation proof, while durable state space
stays neutral: the reservation is arithmetic, cancellation removes an existing
record, and inventory is read-only. No compatibility path, tombstone, quota,
cursor, allocation, or second lifecycle state machine was added.

Patch 4 measures against the pushed Patch 3 artifact:

- compiler Wasm: 5,039,289 bytes, `+15,265`;
- final raw Wasm: 4,401,778 bytes, `+13,310` (`+0.303%`);
- deterministic gzip: 1,706,987 bytes, `+6,174`;
- Candid: 60,972 bytes, `+125`, from added audit evidence fields;
- final exports: 70, unchanged;
- first and unrelated-entity Verify pages: 7,497,160 and 7,546,266
  instructions, both below the unchanged 8,000,000 ceiling; and
- target-revision restart: 1,508,773 instructions, followed by stable Verify
  pages at 7,505,156, 7,538,338, and 4,300,953 instructions.

The packed authority uses 102 raw metadata bytes for the representative
three-entity shape and 65,617 bytes across one control plus two data chunks at
the accepted 4,096-entity limit. Both shapes measured nine stable pages before
and after publication. Patch 4 changes ten delivery files at approximately
`+1,245/-80` lines before design/status/changelog closure. Complexity increases
in one journal-owned bounded persisted authority and startup gate; no new
mutation state, executor, cursor, allocation, public IcyDB endpoint, fallback,
or compatibility decoder is added.
