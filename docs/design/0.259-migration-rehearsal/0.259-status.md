# 0.259 — Status

Authorised by the user on 2026-09-18 after 0.258's completed closeout corrections.
Authority: [design](0.259-design.md).

## Current handoff

F60 complete: qualify application-owned bulk reads, validation and typed batch
writes using the maintained SQL test actor. The ordinary focused test passes
11 scenarios and 25 measured updates, with independent source/output readbacks.
At 128 items, bulk reads plus one atomic batch cost 236,855,733 cycles versus
414,975,697 for individual reads plus the same batch (42.92% fewer); eight
16-item bulk requests cost 326,895,613 cycles (38.01% more than one request).
A late application rejection leaves zero output rows in one request, or 112
previously committed rows when the last chunk rejects. No budget exhaustion
occurs at the tested sizes; no maximum safe size or new coordinator is inferred.

One focused integration test passes (98 filtered out); full required lint,
focused final lint, formatting and whitespace checks pass. Raw test actor:
4,245,111 bytes; incremental Wasm cost is unmeasured. These compare existing
API usage, not an engine optimization. Eight files, approximately 500 added
lines including evidence/notes and 278 test Rust lines; runtime shape unchanged.
Eleven disposable PocketIC instances were created/dropped, no application
network changes. No production API, budget, state, Cargo version or dependency
changes. Full repository validation remains user-owned. Exact inputs,
per-message attribution and exclusions: [F60 report](../../reports/investigations/2026/09/19/application-batch-workload/01/report.md).

F57–F59 complete, authorized together: converge ordinary/progress-bearing
commit completion on the existing window bundle (F57); remove unused phase
and row-validation key arguments (F58), using Db only for shared store selection;
measure and converge single/multi-row application (F59). The commit-window owner keeps all
preflight, publication, readiness and recovery ordering. Test rollback setup
and multi-row-only prefix interruption remain test-owned. No new state, mode,
format or generic runner. Focused mutation/recovery tests and matched retained
Wasm probes qualify the application-loop change. F59 is measured against the
F57/F58 candidate so its branch effect is isolated from those cleanups.

29 focused native tests pass before and after F59, plus strict core lint,
formatting and whitespace checks. The matched probes pass 36 calls, identical
response hashes and independent readbacks. F59 reduces raw Wasm by 702 bytes
(4,217,244 to 4,216,542); successful calls use 203–476 more cycles/instructions
(at most +0.00276% cycles). Rejection controls are unchanged. Retain the simpler
flow with this small measured cost trade-off; no cycle speedup is claimed.
F57/F58 incremental performance remains unmeasured. Exact sources, costs and
exclusions are in the [completion report](../../reports/investigations/2026/09/19/unified-commit-completion/01/report.md).

Eight files in this combined handoff: four Rust files (-75 net lines, including
test support) and four notes/report documents. No new behavior axis or parallel
authority. Twelve disposable PocketIC instances were created/dropped; no
application network changed. No Cargo/version changes, commits, pushes or full
suites; full release validation remains user-owned.

F56 complete: filter synchronized store handles directly inside the existing
registry borrow, removing the intermediate all-handles vector. The same paired
data/index predicate and registration order remain unchanged. Scope is one
mutation helper plus notes; existing focused batch/index/recovery tests
qualify it. No new state, authority, API or execution route.

15 focused native tests, strict core all-target/all-feature lint, formatting
and whitespace checks pass. Coverage includes unique-index swaps, multi-entity
batches, heap/journaled parity and interrupted row/progress recovery. Existing
behavior tests are reused; no incidental allocation-shape tests were added.
Three files change in this slice: one Rust file (-4 net production lines),
detailed notes and this tracker. One temporary vector is removed; structure
is simpler. Cycles, instructions and raw Wasm deltas remain unmeasured.
No PocketIC/network actions, version edits, commits, pushes or full suites.
Full release validation remains user-owned.

F55 complete: move the mutation-progress operation into its commit marker
and borrow that exact operation for normal application, removing the parallel
owned copy. Derive progress-commit selection from the marker as well. Existing
predecessor preflight, publication order and recovery remain authoritative.
This touches commit-marker access and mutation preparation only, plus release
notes; focused marker/progress/recovery tests qualify the handoff. No new mode,
format, state or execution route. The unrelated store-vector cleanup stays out.

17 focused native tests pass, including marker round-trip/absence, exact
idempotent progress replacement, neither-side mismatch rejection, empty-page
progress, normal commits and interrupted row/progress recovery. Strict core
all-target/all-feature lint, formatting and whitespace checks pass. The initial
test build exposed an unused import; it was removed before final qualification.

Incremental footprint: three Rust files (+6 net production lines and +2 net
test lines), plus detailed release notes and this tracker. One owned field,
one duplicate payload clone and one independently supplied selection flag are
removed; the marker provides a shared borrowing accessor. Structure is simpler.
Cycles, instructions and raw Wasm deltas are unmeasured for F55. Earlier F53/F54
measurements apply only to their recorded candidates, not this final source.
No PocketIC/network actions, Cargo/version changes, commits, pushes or full
suites. Full validation remains user-owned.

F54 complete: stage preflight overlay changes only when another row needs
to read them, and drop the overlay immediately after preflight. Initial complete
batch visibility, earlier-row canonical/index staging and all validation stay
unchanged. No new mode or alternate preparation route.

31 focused native tests, strict core lint, formatting and 36 matched-Wasm calls
pass. Against the F53 candidate, raw Wasm grows 45 bytes. Wide successful
updates cost 0.184–0.188% more cycles on their first call and save 0.030–0.072%
on later calls; small-row cycles and instruction deltas are also mixed. This
is a shorter-lifetime/copy cleanup, not a demonstrated overall cycle win.
See the [F54 report](../../reports/investigations/2026/09/19/preflight-overlay-lifetime/01/report.md)
for exact inputs, per-call costs and exclusions.

F54 footprint: two Rust files (+5 net production lines, +14 test lines), four
notes/report documents. Combined F53/F54: seven files, +7 net production lines
and +14 test lines. No new abstraction. Twelve disposable PocketIC instances
were created/dropped for F54; no application network changed. No Cargo/version
changes, commits, pushes or full suites. Full validation remains user-owned.

F53 complete: consume preflighted row operations when constructing journal
records, moving their keys and after-images into the existing validated record
constructors. No change to preflight, record ordering, replay validation or the
preflight overlay. One copy boundary is removed; no new ownership type or mode.

20 focused native tests, strict core all-target/all-feature lint, formatting
and whitespace checks pass. Fresh published-.5/candidate Wasm probes pass 36
calls with identical response hashes and independent readbacks. Wide successful
updates save 2.82–4.57 million cycles (0.71–1.15%); raw Wasm grows 422 bytes.
Small-row cycles are mixed (-9,789 to +2,077); some small RETURNING calls use
150–160k more actor-interval instructions. Rejection controls improve but never
reach the changed handoff, so no direct clone-removal attribution is made.
See the [F53 report](../../reports/investigations/2026/09/19/row-journal-ownership/01/report.md)
for exact inputs, per-call costs and exclusions.

Footprint: one Rust file (+2 net production comment lines, no new test lines)
and four notes/report documents. Twelve disposable PocketIC instances were
created/dropped; no application network changed. No Cargo/version changes,
commits, pushes or full suites. Full validation remains user-owned.

F51–F52 complete, explicitly authorized together: extend marker-owned
journal batches to schema publication and migration rewrite pages, and consume
prepared schema-position metadata at its final apply boundary. Existing marker,
schema and migration owners remain authoritative; preflight and publication
ordering are unchanged. No new mode, persisted state, encoding or cache owner.
Internal Candid buffer tuning stays deferred.

Qualification: 25 focused native tests pass, none ignored, covering physical
migration validation/rewrite, every existing rewrite interruption checkpoint,
nested-relation migration, terminal handoff, schema activation/Abort, and
mixed-entity/identity recovery. Strict core all-target/all-feature lint,
formatting and whitespace checks pass. Existing behavior tests are reused;
the final-use position DTO no longer implements Clone.

Incremental footprint: four Rust files (+13 net production lines, no new test
lines) and three release/tracker documents. Payload ownership is simpler;
migration keeps a parallel store-binding vector in the same preflight order.
Cycles, instructions and raw Wasm deltas for this follow-up are unmeasured.
Earlier F45–F50 results below describe their measured source candidates, not
the additional F51–F52 edits. No PocketIC/network lifecycle actions or full
suites were run for this follow-up; full validation remains user-owned.

F50 complete: size-only SQL RETURNING checks keep Candid's encoder but send
its output to one checked byte counter instead of allocating a final Vec just
to take its length. Both row and complete-payload checks share the owner-local
helper. Internal Candid buffering remains; no handwritten wire-size model,
new admission policy, public surface or compatibility path is added.

Seven focused native tests, strict core lint, formatting and whitespace checks
pass. The unchanged Wasm probe passes 18 calls with matching response hashes
and independent readbacks. Against the recorded F49 baseline, wide rejections
save 1.06–3.62 million cycles; successful RETURNING calls save 5,955–9,441 cycles.
Raw Wasm grows 12 bytes. The third rejection remains 1.446% above the earlier
pre-F49 result: the final-copy cleanup reduces, but does not close, that rise.
The remaining follow-up is internal Candid buffer growth, not journal rollback
or a new wire-size model. Baseline actors/logs were removed during user cleanup;
this comparison uses recorded before results, not a fresh paired run. See the
[F50 report](../../reports/investigations/2026/09/19/returning-counted-output/01/report.md).

Incremental footprint: one Rust file (+25 production / +66 test lines) and four
notes/report documents. Six disposable PocketIC instances were created/dropped;
no application network changed. No large temporary dumps or worktrees were
recreated. Full repository validation and publication remain user-owned.

F49 complete, authorized after the read-only write-cost review: keep each
journal batch solely in the prepared commit marker. Marker publication borrows
that marker and tail append resolves the same batch by its existing ordinal.
Remove the duplicate batch and intermediate payload wrapper; keep encoded
envelopes, admission, publication order and replay checks unchanged. No new
behavior axis, reference-counted ownership or compatibility route.

Qualification: 26 focused native executions, strict core all-target/all-feature
lint, formatting and whitespace checks pass. Two unchanged matched-Wasm probe
runs pass 36 calls with identical results and reproducible costs. Wide successful
count/id writes save 3.44–5.87 million cycles (0.856–1.503%); small-row costs are
mixed. Raw Wasm increases 22 bytes. One oversized-response rejection costs
3.47 million more cycles (+2.077%); its cause was unlocalized in that qualification and was not dismissed
as noise or described as an improvement. The
[F49 report](../../reports/investigations/2026/09/19/journal-batch-ownership/01/report.md)
records exact message costs, artifact/source identity and exclusions.

Incremental footprint: six Rust files, about +7 net production lines, no net
test-line change, and four notes/report documents. One wrapper and one deep-copy
boundary are removed. Twelve disposable PocketIC instances were created/dropped;
no application network changed. Full suites and publication remain user-owned.

F46–F48 complete, explicitly authorized together: replace bitwise CRC32C
with one table-driven implementation at the existing format owner; derive
count-only SQL output capture at the existing writer boundary; avoid re-encoding
preserved update slots merely to compare them with themselves. These are hard
cuts, with no compatibility path, persisted-format change or new public mode.
Whole-row validation, accepted canonicalization, managed timestamps and commit
checks remain authoritative. No broader canonical-row/commit rewrite is included.

Final qualification: 11 focused SQL-enabled and five SQL-disabled test executions
pass, none ignored. The complete `make clippy` gate, formatting and whitespace
checks pass. The SQL-enabled native selection retains the same 65 unused-code
warnings. The matched-Wasm probe passes 18 fresh calls against the prior F45
baseline, with identical response hashes and independently checked stored values.
Small-row writes save about 2.9–3.7% of cycles. Wide count-only writes save about
214.77 million (35.5%), wide RETURNING id 205.34 million (33.8%). Raw Wasm grows
1,255 bytes (+0.030%). These are combined F46–F48 measurements, not per-item
attribution. The [write-cost report](../../reports/investigations/2026/09/19/write-cost-reduction/01/report.md)
contains exact identities, instruction counts, exclusions and corrected
development compilation/test/lint failures.

Incremental footprint: six Rust files (+32 production, +119 test lines) and four
notes/report documents. The complete candidate, including preserved F45, has
nine Rust files (+51 production, +390 test/measurement lines) and five documents.
Six disposable local PocketIC instances were created/dropped; no application
network changed. Full suites and publication remain user-owned. No Cargo/version
changes, commits, pushes or downstream edits.

F45 complete: borrow rows for SQL `RETURNING field_list` pre-commit checks.
Both the incremental response-size probe and exact payload preparation cloned
whole rows before discarding unselected cells. Use one borrowed projection
method on the existing field-selection owner, copying only selected values in
requested output order. Keep owned final-response projection, field validation,
size encoding/policy and pre-commit ordering unchanged. No new configuration,
cache, execution mode or retained metadata. F39 remains deferred.

Eight focused tests pass, none ignored: selection order/alignment, unreturned
large payloads, existing field/bound diagnostics, and an accepted-schema UPDATE
that rejects before commit at a small response limit and succeeds at a larger
one. The initial test build found a private-module import in the new fixture;
use the existing accepted-schema policy facade instead, without widening any
visibility. Strict core all-feature/all-target clippy, formatting and whitespace
checks pass. SQL-only tests retain the 65 previously recorded unused-code
warnings. Logs: `/tmp/icydb-returning-selection-{tests,tests-final,clippy}.log`.

Matched PocketIC follow-up passes on the published baseline and current actor:
36 measured UPDATE calls, with matching results and independent readbacks.
RETURNING id saves 11,774–19,387 cycles on small rows and 2,113,876–2,116,417
cycles with a 1.05 MB unreturned field. Instructions fall by 13,665–18,936 and
2,113,793–2,116,190 respectively; count-only and full-row controls are unchanged.
Raw Wasm increases 1,008 bytes. Strict focused integration lint passes.
The [measurement report](../../reports/investigations/2026/09/19/returning-selected-cells/01/report.md)
records exact source/artifact identities, exclusions and a discarded host-cache
mismatch before the correctly rebuilt current-host run.

Footprint: four Rust files (+19 production lines, +271 test/measurement lines),
plus four release/tracker/report documents. The copy boundary is narrower; one
owner-local helper replaces two full-row clone sites. Twelve disposable local
PocketIC instances were created/dropped; no application network was changed.
Full suites remain user-owned. No Cargo/lockfile changes, commits or pushes.

F44 complete: share global aggregate execution-diagnostic construction between
text and JSON EXPLAIN. Both paths previously assembled the same descriptor,
converted the terminal and attached the same diagnostic admission summary.
One owner now returns the existing finalized diagnostic DTO; presentation-only
loops stay separate. Each terminal is still constructed, admitted and rendered
before the next terminal, preserving failure/resource ordering without a new
mode, generic runner or buffered DTO vector. Cache and authority paths remain
unchanged; F39 stays deferred.

The new regression passed against the pre-refactor implementation and again
afterward. It checks ordered single/combined terminal output, FILTER/admission
metadata and cold/warm text/JSON rendering without data-store row fetches.
Eight focused post-change test executions pass, none ignored, including existing
prepared diagnostic admission, SQL exhaustion and aggregate cache coverage.
Strict core all-feature/all-target clippy, formatting and whitespace checks
pass. SQL-only tests retain the 65 previously recorded unused-code warnings.
Logs: `/tmp/icydb-aggregate-explain-{before,tests,clippy}.log`.

Incremental footprint: one production file (-22 net lines), one regression file
(+51 lines), and three release/tracker documents. One duplicate helper is removed;
the flow is simpler. Wasm/cycle/instruction deltas are unmeasured. No full suites,
Cargo/lockfile edits, local network lifecycle actions, commits or pushes.
This completes the five approved F40–F44 cleanup outcomes; full release
validation remains user-owned.

F43 complete: delete the second schema
failure-receipt comparison in startup observation. The earlier comparison uses
the exact same captured receipt, incarnation, submission and accepted head and
returns on a match before recovery gating. Keep that precedence and carry only
the reconciled boolean afterward, rather than retaining the head for a redundant
comparison. No new helper, lifecycle state, persistence change or authority.
Existing startup tests cover matching terminal failures ahead of pending
markers/Ready, stale submissions and journal proofs, retryable failures and
readiness without writes. Earlier cleanup and the F39 deferral are preserved.

Nine focused startup tests pass without SQL enabled, none ignored; strict core
all-feature/all-target clippy, formatting and whitespace checks pass. Logs:
`/tmp/icydb-startup-receipt-{tests,clippy}.log`. Incremental footprint is one
production file (-7 net lines) and three release/tracker documents, with no
test-source additions. The flow is simpler; Wasm/cycle/instruction deltas are
unmeasured. No full suites, Cargo/lockfile edits, local network lifecycle
actions, commits or pushes.

F42 complete: lower same-entity insert
patches directly through `lower_dynamic_save_intent` with one accepted catalog.
The public API already supplies one entity name, so wrapping every patch in a
named `DynamicMutation` and resolving that name again adds no authority check.
The private wrapper's remaining single-mutation caller now resolves and lowers
its one request directly; the wrapper is deleted rather than generalized.
Both paths converge on the unchanged lowered-batch and atomic writer owners.
Count-before-entity validation, per-row diagnostic positions, one operation
timestamp, generated identities and precommit result preparation are preserved.
Mixed-entity and typed batch paths are unchanged; no new behavior axis or API.

Two owner-local regressions cover count-check precedence and late failures
without committed rows or consumed identities, followed by ordered generated
results. Existing single/mixed/typed writes, constraints and journaled unique
release checks provide the focused neighboring coverage.

Fresh qualification passes 14 focused test executions (eight SQL-enabled and
six SQL-disabled), none ignored. Strict core all-feature/all-target clippy,
formatting and whitespace checks pass. SQL-only tests retain the 65 previously
recorded unused-code warnings. Logs:
`/tmp/icydb-direct-insert-{tests,no-sql-tests,clippy}.log`.

Incremental footprint: one Rust file (-17 production lines, +79 regression
lines) and three release/tracker documents. One intermediate named-request
vector and its private wrapper disappear; the canonical writer is unchanged.
Wasm/cycle/instruction deltas are unmeasured. No full suites, Cargo/lockfile
changes, local network lifecycle actions, commits or pushes. Earlier worktree
changes are preserved, including the explicit F39 deferral.

F41 complete: borrow SQL mutation
selectors through projection planning. Both helpers previously took ownership
only to borrow again, forcing their caller to clone the query. The existing
planner remains the owner of plan construction; exact-update primary-only
visibility, scan ceilings and candidate bounds are untouched. No new behavior
axis, cache or public API. INSERT SELECT now retains any existing structural-key
memo on its cached source query rather than a disposable clone; the memo holds
query identity, never result rows or replacement schema authority.

Two focused regressions cover exact-update rejection before mutation, exact
and ordered-prefix selection, and cold/warm INSERT SELECT reading a changed
source row. Earlier F40 work and the F39 deferral are preserved.

Nine focused SQL-enabled tests pass, none ignored. The initial new fixtures
used inadmissible SQL (missing UPDATE predicate, then incompatible arithmetic
and standalone-literal INSERT projections); fixtures were corrected to the
maintained WHERE and CASE surfaces without weakening production admission.
SQL-disabled all-target compilation and formatting/whitespace checks pass.
Strict core all-feature/all-target lint passes on the final sources.
SQL-only tests retain the 65 existing unused-code warnings. Qualification logs
are `/tmp/icydb-borrowed-selectors-{tests-final,clippy-final,no-sql}.log`.

Incremental footprint: two production files (-2 net lines), one existing
regression file (+63 lines), and three release/tracker documents. Two temporary
query clone sites disappear; ownership is simpler with no new helper or route.
Wasm/cycle/instruction deltas remain unmeasured. No full suites, Cargo/lockfile
changes, local network lifecycle actions, commits or pushes.

F40 complete: remove the duplicate optional executor authority from compiled
SQL contexts. Cold and bound
compilation previously cloned authority from the same accepted catalog that the
context already retained, while warm compilation omitted it and consumers
selected between these equivalent sources. The catalog is now the sole owner;
SELECT and EXPLAIN obtain authority from it when needed. Current-authority
checks, cache identity and bound-command cache bypass are unchanged. The
smallest correction deletes the extra field, fallback helpers and unused
cache argument; it adds no mode, cache, public API or persisted state.

The direct regression retains cold/warm SELECT and EXPLAIN contexts across a
field rename and checks typed stale-revision rejection through both execution
entrypoints. Existing binding and diagnostic coverage is reused. F39 remains
deferred; the other read-only audit opportunities are separate follow-ups.

Fresh qualification passes nine focused SQL-enabled tests, none ignored,
including retained bindings, cold/warm diagnostics, aggregate cache reuse and
the new stale-context regression. Strict core all-feature/all-target clippy,
SQL-disabled all-target compilation, formatting and whitespace checks pass.
SQL-only tests retain the 65 previously recorded unused-code warnings. Logs:
`/tmp/icydb-sql-catalog-{tests,clippy,no-sql}.log`.

Footprint: five production files (-62 net lines), two test files (+52 net
lines), and three release/tracker documents. Authority ownership is simpler;
Wasm/cycle/instruction deltas are unmeasured. No full suites, local network
lifecycle actions, Cargo version/lockfile edits, commits or pushes. The tagged
release is preserved; active changelog selection follows release governance.

F38 complete within 0.259.3: replace the full bounded heap's root in
place in COUNT and generic grouped selection. `BinaryHeap::peek_mut` repairs
the heap once instead of removing and reinserting a slot. Strict comparison,
zero-bound handling, filtering, admission charges and final sorting retain
their existing owners and order. No new helper, execution route or state axis.

Focused core selections pass 62 tests (40 SQL-enabled, 22 SQL-disabled), none
ignored. Two matched manual PocketIC runs pass, covering nine calls per actor;
strict core and focused measurement-target lint pass. Formatting and whitespace
checks pass. SQL-only compilation retains 65 existing unused-code warnings. The first
sandboxed PocketIC attempt could not bind localhost and was stopped; rerunning
the retained baseline with local-network permission succeeded. Disposable
PocketIC instances were created for both successful measurement runs.

The isolated heap delta is -192 raw Wasm bytes and -4,052 to -6,835 instructions
and cycles per sampled call. These six-group probes do not establish larger
workload gains or the whole .3 performance delta. See the
[measurement report](../../reports/investigations/2026/09/19/grouped-heap-replacement/01/report.md).
Incremental footprint: five Rust files (-2 production lines, +154 regression/
measurement lines) and four documentation files. Existing .3 work is preserved;
no Cargo version/lockfile changes, full suites, commits or pushes.

F39 deferred by explicit user choice: keep the unbounded candidate buffer.
That path finalizes every group's aggregates before HAVING/projection. Removing
the buffer would interleave those fallible stages and change which error wins;
preserving that precedence with error-buffering machinery is not justified.
Revisit only with measured need and a simpler error-order-preserving design.

F37 complete within 0.259.3: centralize grouped page-selector setup in its existing
constructor. Remove repeated projection/HAVING setup from the ordered and
materialized callers and read pagination directly from the route. Preserve
projection -> HAVING -> top-k compilation order and the ordered-route guard.
This removes duplicated wiring, not repeated compilation within one query;
no new owner, mode, cache, budget or retained state is needed.

Focused SQL-enabled and SQL-disabled selections pass 69 test executions, none
ignored (38 and 31). Existing ordered-refill pagination, cache-enabled/disabled
grouped results, projection identity/reordering, top-k budget rejection and
cursor checks are reused without adding implementation-shape tests. Formatting,
whitespace and strict core all-feature/all-target lint checks pass. SQL-only compilation
retains 65 existing unused-code warnings.

Incremental footprint: two Rust files (-25 net production lines, no test-source
changes) and two release/tracker documents. Both callers now use the existing
selector constructor; compilation order and invocation timing remain unchanged.
Wasm/cycle/instruction deltas are unmeasured. Logs:
`/tmp/icydb-grouped-selector-*.log`. No full suites, Cargo version/lockfile edits,
commits, pushes or local network lifecycle actions; earlier .3 changes preserved.

F36 complete within 0.259.3: narrow grouped fold setup and lookup plumbing.
Borrow the planner target slot during aggregate setup, derive materialized direction from
the existing aggregate-kind owner, and return the existing typed lookup invariant
directly instead of passing identical error callbacks and unused context. Shared
compiled expressions, accepted authority, reducers and lookup semantics stay
unchanged. No new configuration, retained state, execution route or public API.

Focused SQL-enabled and SQL-disabled selections pass 108 test executions,
none ignored (58 and 50 respectively). Owner tests verify independent MIN/MAX
results and bucket lookup matching, absence, invalid-index rejection and typed
comparison-error propagation. Existing shared-expression independence, ordered
folds, grouped pagination, DISTINCT, attachment validation and cold/warm EXPLAIN
coverage pass. Strict core all-feature/all-target lint, formatting and whitespace
checks pass; SQL-only test compilation retains 65 existing unused-code warnings.
No new validation failures occurred.

Incremental footprint: three Rust files (-26 net production lines, +71 net test
lines including inline tests) and three release/tracker documents. The setup
contract loses a redundant field, lookup loses an unnecessary callback, and the
field-slot clone disappears without changing expression sharing. Wasm/cycle/
instruction deltas are unmeasured. Logs: `/tmp/icydb-grouped-fold-cleanup-*.log`.
No full-suite execution, Cargo version/lockfile edits or local network lifecycle
actions; prior .3 changes remain intact.

F34–F35 complete within 0.259.3, explicitly combined by the user: remove duplicated
DISTINCT field labels (the accepted FieldSlot already owns the label), then stop building
discarded layout/specification outputs. Layout derives positions against frozen
aggregate slots; preparation collects specifications without layout vectors.
Keep first-seen ordering, semantic deduplication and existing validation. No new
cache, mode, retained field, public surface or persisted representation.

Focused qualification passes 70 test executions, none ignored: 39 SQL-enabled
and 31 SQL-disabled. Existing cold/warm diagnostics, aggregate attachment failure,
cache-retention, grouped pagination and DISTINCT cases pass. New owner tests
cover repeated/computed outputs, filtered and DISTINCT semantic identities,
HAVING-only slots and missing frozen metadata. Strict core all-feature/all-target
lint, formatting and whitespace checks pass. SQL-only tests retain the 65 existing
unused-code warnings; no new validation failures occurred.
The layout visitor still walks the expression tree and uses a transient bitmap
to count first encounters; it no longer constructs owned aggregate syntax.
The collector retains its original first-seen deduplication semantics, without
building either layout position vector. The now-unused scan DTO is removed.

Combined .3 footprint: eight files; two production files (-100 net lines), three
test files (+88), and three release/tracker documents. These two follow-ups touch
seven of those files beyond F33, removing 93 net production lines and
adding 52 net test lines. No independent behavior axis is added; schema authority,
layout validation and compilation remain at their existing owners. Wasm, cycles
and instructions are unmeasured. Logs: `/tmp/icydb-grouped-cleanup-*.log`.
No full-suite execution, Cargo version/lockfile edits or network lifecycle actions.

F33 complete within 0.259.3: borrow frozen aggregate specifications and DISTINCT
strategy in the grouped planning handoff. Grouped EXPLAIN no longer copies
those frozen payloads only to discard them; execution materializes its owned
route payload at the existing consuming boundary. Layout derivation and all
validation remain at their current owners. No new cache, runtime mode, budget,
persisted state or public API; broader layout reconstruction was outside F33
and is addressed separately by F35 above.

Qualification: final focused selections pass 48 test executions, none ignored
(28 SQL-enabled, 20 SQL-disabled). Cold/warm diagnostics cover compiled aggregate
input and FILTER programs and verify complete owned execution metadata. Existing
group layout, cache-retention, grouped pagination and global DISTINCT tests pass.
The expanded test initially exceeded the function-length lint; extracting its
aggregate fixture corrected that without relaxing lint rules. `make clippy`,
strict core all-feature/all-target lint, formatting and whitespace checks pass.
SQL-only test compilation retains its
65 existing unused-code warnings. Full repository/workspace tests remain
user-owned and were not run.

Incremental footprint: five files, with -7 net production lines, +36 test lines
and three release/tracker documents. Borrowed handoff ownership is simpler;
execution still copies once and layout construction remains unchanged.
Wasm/cycle/instruction deltas are unmeasured. Logs are retained locally as
`/tmp/icydb-grouped-handoff-*.log`. No Cargo version/lockfile changes, commits,
pushes or local ICP/PocketIC lifecycle actions.

F32 complete: repair F30's stored-scale validation of canonical decimal
group boundaries, including decimals in collections. Reuse the accepted-value
validator with a private group-key entry that returns no persistence proof.
The sole representation distinction permits exact decimal rescaling for query
keys; stored validation remains strict. A top-level exception misses nested
decimals, while another recursive type checker would duplicate catalog logic.
State-space delta: one private call-local validation flag, no public mode,
configuration, retained state, new budget, wire change or data migration.

Qualification: an unchanged server-issued decimal cursor reproduced the F30
rejection before correction. Final focused runs pass 109 test executions,
none ignored (57 SQL-enabled, 52 SQL-disabled). Independent seeded scalar/list
groups resume across all three pages with caching enabled and disabled. Owner
tests cover nested list/set/map decimals, exact rescaling versus rounding or
overflow, strict stored values and opaque enum payloads, nullability, depth and
byte limits. Existing authentication, malformed-boundary and grouped accounting
tests pass. Strict core all-feature/all-target clippy, formatting and whitespace
checks pass on Rust 1.98.1; SQL-only compilation retains its 65 existing unused-code
warnings. Full repository/workspace validation remains user-owned and was not run.

Incremental footprint: six Rust files (+55 net production lines and +260 test
lines) plus four contract/release/tracker documents. This adds a small private
validation distinction at the existing recursive owner, not another recursive
checker or public mode; implementation complexity increases narrowly to fix a
demonstrated regression. Wasm/cycle/instruction deltas remain unmeasured.
Pre-edit sources, failing reproduction and final logs are retained locally in
`/tmp/icydb-grouped-decimal.5JmWm2/`. The first test fixture compile needed the
public list-element type corrected before the regression was reproduced.
Earlier dirty work and the lockfile are preserved. No Cargo version changes,
commits, pushes or local ICP/PocketIC lifecycle actions.

F31 complete within 0.259.2: retain the grouped cursor bytes already encoded/charged by
the executor instead of encoding again during response finalization. Charge
their actual unpadded Base64 length, using the existing cursor text owner.
The existing structural result carries bytes instead of a domain token; no
new wrapper, mode, budget, key or wire format. All admission remains inside
the current execution budget scope.

Qualification: 86 focused test executions pass, none ignored (41 SQL-disabled,
45 SQL-enabled). Exact binary-byte, Base64-byte and cursor-step limits admit;
one unit below rejects with the matching resource fact. All Base64 remainder
lengths, absent cursors, real ascending/descending resumes, cold/warm tampering
rejection and physical-refill-spanning grouped pages pass. The first new test
fixture omitted required failure reserves; correcting that fixture resolved
both initial test failures without changing production limits. Strict core
all-feature/all-target clippy, formatting and whitespace checks pass. SQL-only
compilation retains its 65 existing unused-code warnings. Full-suite execution
remains user-owned and was not run.

Incremental footprint: six Rust files (-1 net production line and +100 test
lines) plus four contract/release/tracker documents. One binary encoding/key
read replaces two, and the now-unused query serialization-error helper is
removed. The ownership flow is simpler; Wasm/cycle/instruction deltas remain
unmeasured. Pre-edit sources and logs: `/tmp/icydb-grouped-encode.H4GDhq/`.
Earlier dirty work and the lockfile are preserved. No version edits, commits,
pushes or local ICP/PocketIC lifecycle actions.

F30 complete within 0.259.2: authenticate grouped cursors through the scalar cursor's
existing HMAC envelope and durable key owner, then validate incoming grouping
boundaries against the current plan/schema. The demonstrated gap is an unsigned
group boundary despite the public tamper-rejection contract. Reusing the codec
is simpler than a second authentication scheme. Hard-cut grouped version-1
tokens in place; no legacy decoder, new key, mode or persisted database state.
Saved grouped cursors must be regenerated; scalar wire bytes stay unchanged.
The separate duplicate response encoding/accounting follow-up is completed by F31.

Qualification: 90 focused test executions pass, none ignored (47 SQL-enabled,
43 SQL-disabled). SQL-enabled request tests cover real ascending/descending
resumes and cold/warm rejection of modified payloads and correctly signed
wrong-arity, wrong-type and non-nullable-null boundaries. Codec tests cover
every-byte tampering, truncation, trailing bytes, a different key, and excessive
nesting even after successful authentication. Existing scalar round trips,
grouped policy diagnostics, refill-spanning pagination and accepted-identity
tests pass. Strict core all-feature/all-target clippy, formatting and whitespace
checks pass. The SQL-only build retains its 65 existing unused-code warnings.
Full repository/workspace validation remains user-owned and was not run.

Incremental footprint: 13 Rust files and four contract/release/tracker documents,
approximately +150 net lines including tests/docs. The shared envelope replaces
separate framing paths, and test-only duplicate grouped validation is removed;
the added semantic boundary check reuses accepted value/type owners without
retained metadata or coercion. Wasm/cycle/instruction deltas are unmeasured.
Logs are in `/tmp/icydb-grouped-auth.YTyz7G/` (local, not release artifacts).
Earlier dirty work and the lockfile are preserved. No Cargo version changes,
commits, pushes or local ICP/PocketIC lifecycle actions.

The user approved F29: report grouped continuation policy failures as invalid
cursor requests, not internal invariants. The existing grouped-policy owner
keeps both decisions. Two numeric validation reasons distinguish missing LIMIT
from unsupported global DISTINCT continuation under the existing invalid-cursor
code/fact shape; the simpler generic token-decode reason would misdescribe a
successfully decoded request. State-space delta: two diagnostic reason values,
no new cursor variant, execution route, policy mode or persisted state.
Complete within 0.259.2.

Qualification: the request regression reproduced the internal-failure code before
correction using a real returned cursor. After correction, 51 focused test
executions pass with none ignored: 12 SQL-enabled core, 10 SQL-disabled core,
26 facade and 3 diagnostic-registry cases. The same cursor resumes a supported
query successfully; cold/warm no-LIMIT and global DISTINCT requests reject with
their exact reasons, while cursorless controls succeed. Candid round trips
preserve both reasons, and existing genuine-invariant tests pass. Strict
all-feature/all-target clippy for core, facade and diagnostics, formatting and
whitespace checks pass. Full repository/workspace tests remain user-owned.

Incremental footprint: four Rust files (+15 net production lines, +130 test
lines) and four contract/release/tracker documents. Existing owners and execution
flow are unchanged; two diagnostic reasons replace an incorrect invariant
classification. Wasm/cycle/instruction deltas are unmeasured. Local logs and
pre-edit sources: `/tmp/icydb-grouped-policy.nz96kB/` (not release artifacts).
Existing work and lockfile changes are preserved; no Cargo version or local
network lifecycle actions were taken.

The user approved F28: preserve cursor-invariant classification at the existing
query-error conversion boundary. External cursor rejections retain their
planner-owned numeric facts; invariants use the existing runtime conversion.
No new error code, public API, execution mode or persisted state. Qualification
is complete within 0.259.2; the existing dirty batch and lockfile are preserved.

Qualification: the new invariant regression failed before correction while the
external-cursor rejection regression passed. After correction, 53 focused test
executions pass with none ignored: 14 core cases with SQL, the same 14 without
SQL, and 25 public-facade cases including Candid round trips. Strict all-feature,
all-target clippy for core/facade, formatting and whitespace checks pass on
Rust 1.98.1. The SQL-only test build retains its 65 existing unused-code warnings.
Full repository/workspace tests remain user-owned.

Incremental footprint: three Rust files (+6 net production lines, +79 test
lines) and three release/tracker documents. One existing conversion distinguishes
internal failure from external rejection; implementation shape is otherwise
unchanged. Wasm/cycle/instruction deltas are unmeasured. Local logs and pre-edit
sources: `/tmp/icydb-cursor-classification.GqiXfD/` (not release artifacts).
No Cargo version changes or local network lifecycle actions.

The user approved F27: consolidate retained-slot requirement state, construct
override metadata only when needed and share byte-length eligibility at the
accepted row-layout owner. Existing value modes and default-empty metadata
remain canonical; full-value requirements still override byte-length-only
requirements. The simplest alternative keeps parallel flags, discarded default
vectors and duplicate eligibility checks. No new mode, execution path, public
API or persisted state. Complete within 0.259.2.

Qualification: 148 focused tests pass with no ignored cases, including
requirement precedence in both marking orders, sparse override alignment,
default-empty capacity, accepted blob/text codec eligibility, mixed and repeated
byte-length projections, UTF-8 lengths, ordering and live pagination. Existing
retained-output coverage checks blob, empty and NULL decoding. Strict core
all-feature/all-target lint, SQL-disabled all-target compilation, formatting
and whitespace checks pass. SQL-only tests retain 65 existing unused-code
warnings. The first run found an incorrect total-limit assumption in the new
pagination test; bounded continuation traversal corrected that fixture, and
the final focused run passes.

Incremental footprint: five Rust files (-16 net production lines, +157 test
lines), plus two documentation files. One requirement vector replaces two;
override metadata is populated only when needed, using existing empty-default
semantics. Eligibility is shared by the existing accepted row-layout owner.
Wasm/cycle/instruction deltas are unmeasured. Local logs and pre-edit sources:
`/tmp/icydb-retained-preparation.BTboSR/` (not release artifacts). Existing
lockfile changes were preserved; no network lifecycle actions or full repository
tests were performed.

The user approved F26: require planner-supplied grouped limits and return grouped
pages directly instead of retaining debug-only fold-result state. The existing
grouped output owner checks cardinality at construction; global DISTINCT keeps
its implicit-group exception. The simplest alternative retains an unused
executor default branch and a redundant result envelope. No new mode, public API,
persisted state or policy owner. Complete within 0.259.2.

Qualification: 157 focused tests pass with no ignored cases, including explicit
planner defaults/custom limits, grouped budget checks, pagination, and global
DISTINCT with zero matching input rows across retained/unretained and cold/warm
execution. Strict core all-feature/all-target lint, SQL-disabled all-target
compilation, formatting and whitespace checks pass. SQL-only tests retain 65
existing unused-code warnings. Incremental footprint: fourteen Rust files
(-81 net production lines, +34 test lines), plus two documentation files.
The fold envelope, its flag/counter and the unused executor default branch are
removed. Global DISTINCT also drops the counter that only fed that envelope;
budget accounting remains intact. Wasm/cycle/instruction deltas are unmeasured.
Local logs and pre-edit sources: `/tmp/icydb-grouped-contracts.9yYc1W/` (not release
artifacts). Existing lockfile changes were preserved; no network lifecycle
actions or full repository tests were performed.

The user approved F25: use the fast-stream request as its sole discriminator
and share the identical row-collector/page materialization request and builder.
The simplest alternative retains duplicate tags and field assembly. Existing
scan handlers, terminal eligibility checks and cursor policy remain authoritative;
no new execution path, mode, cache or public surface. Complete within 0.259.2.

Qualification: 132 focused tests pass with no ignored cases, covering primary-key
reads, secondary ordering, range pagination, projections and cold/warm reuse.
Strict core all-feature/all-target lint, SQL-disabled all-target compilation,
formatting and whitespace checks pass. SQL-only tests retain 65 existing
unused-code warnings. Incremental footprint: nine Rust files (-58 net production
lines), plus two documentation files. One duplicate enum, one duplicate request
struct and the mismatch-only error helper disappear; request assembly has one
owner. Existing behavior tests are reused. Wasm/cycle/instruction deltas are
unmeasured. Local logs and pre-edit sources: `/tmp/icydb-stream-contracts.SKdvZn/`
(not release artifacts). Existing lockfile changes were preserved; no network
lifecycle actions or full repository tests were performed.

The user approved F24: compare borrowed aggregate semantic keys in grouped
projection identity checks and deduplication. Retain an owned aggregate spec
only for a genuinely new first-seen entry; remove the now-unused comparison
wrapper. The simplest alternative copies input/filter expressions before
comparison. Existing semantic-key normalization remains authoritative, and
collection still precedes schema resolution/compilation. No new cache, mode,
configuration or semantic owner. Complete within 0.259.2.

Qualification: 126 focused tests pass with no ignored cases, including explicit
first-seen slot expectations, repeated/HAVING aggregate reuse, FILTER and
DISTINCT distinctions, COUNT literal/NULL normalization, owned/borrowed identity
parity and cold/warm execution. Strict core all-feature/all-target lint,
SQL-disabled all-target compilation, formatting and whitespace checks pass.
SQL-only tests retain 65 existing unused-code warnings. Incremental footprint:
three Rust files (-6 net production lines, +55 test lines), plus two
documentation files. Both comparison paths reuse the existing semantic-key
owner instead of constructing temporary owned execution specs.
Wasm/cycle/instruction deltas are unmeasured. Local logs and pre-edit sources:
`/tmp/icydb-aggregate-identities.fQbK71/` (not release artifacts). Existing
lockfile changes were preserved; no network lifecycle actions or full
repository tests were performed.

The user approved F23: borrow grouped HAVING from the retained plan and read
the scalar residual filter directly from execution preparation. Remove the
redundant payload fields and HAVING forwarding chain; preserve typed grouped
plan validation at compilation. The smallest alternative keeps an owned
expression copy and a duplicated reference. Existing planner/compiler and
preparation owners remain authoritative; no new cache, abstraction, mode or
persisted state. Complete within 0.259.2.

Qualification: 138 focused tests pass with no ignored cases, including
accepting/rejecting HAVING predicates across cold/warm and rebound execution,
grouped windows, scalar filtering and cache reuse. Strict core
all-feature/all-target lint, SQL-disabled all-target compilation, formatting
and whitespace checks pass. SQL-only tests retain 65 existing unused-code
warnings. Incremental footprint: eight Rust files (-14 net production lines,
+19 test/fixture lines), plus two documentation files. The grouped window
fixture now carries a grouped logical plan, not a scalar placeholder.
Wasm/cycle/instruction deltas are unmeasured. Local logs and pre-edit sources:
`/tmp/icydb-filter-ownership.8nfS5n/` (not release artifacts). Existing lockfile
changes were preserved; no network lifecycle actions or full repository tests.

The user approved F22: remove unused covering-plan prefix fields and the
redundant grouped route-intent flag. Prefix length remains an input to the
existing planner ordering proof, while grouped intent derives directly from
the retained planner strategy. The simplest alternative keeps unused payload
and duplicate state. Existing covering, routing and retention owners suffice;
no new API, configuration, persisted state or execution route. Complete within
0.259.2.

Qualification: 61 focused tests pass with no ignored cases, including covering
output, current secondary-order proofs, grouped pagination, cold/warm queries
and EXPLAIN. Strict core all-feature/all-target lint, SQL-disabled all-target
compilation, formatting and whitespace checks pass. SQL-only tests retain 65
existing unused-code warnings. Incremental footprint: six Rust files (-8 net
production lines), plus two documentation files. Four stored fields disappear;
existing behavioral coverage is reused. Wasm/cycle/instruction deltas are
unmeasured. Local logs and pre-edit sources: `/tmp/icydb-route-fields.BKW5Pb/`
(not release artifacts). Existing lockfile changes were preserved; no network
lifecycle actions or full repository tests were performed.

The user approved F21: remove redundant state in prepared-query structs.
Derive retained-slot override presence from the canonical mode slice, require
accepted continuation identity in prepared cores, and require projection
metadata in projection-specific handoffs. General planner, aggregate and lazy
cache optional states remain where absence is meaningful. The simplest
alternative keeps duplicate state and impossible optional cases; the existing
preparation owners and retained-byte visitor suffice. No new API, mode,
persisted state or ownership layer. Complete in 0.259.2.

Qualification: 148 focused SQL-enabled tests pass with no ignored cases,
including retained-slot modes, projection, continuation, cold/warm queries and
cache-retention boundaries. Strict core all-feature/all-target lint,
SQL-disabled all-target compilation, formatting and whitespace checks pass.
SQL-only tests retain 65 existing unused-code warnings. Incremental footprint:
six Rust files (-5 net production lines, +37 test/fixture lines), plus three
documentation files. State is simpler without another semantic owner.
Wasm/cycle/instruction deltas are unmeasured. Local logs and pre-edit sources:
`/tmp/icydb-query-types.kzye4u/` (not release artifacts). Existing lockfile
changes were preserved; no network lifecycle actions or full repository tests.

The user approved F20: leaner scalar projection preparation. Remove the
executor's retained syntax copy, use compiled-expression count for output
capacity, and build direct-slot metadata from borrowed slots without an
intermediate vector. The planner remains syntax authority and the existing
retained-byte visitor accounts for the smaller execution contract. The
alternative keeps redundant state and temporary storage; no new sharing layer,
cache, mode, public API or persisted state is needed. Complete in 0.259.2.

Qualification: 141 focused SQL-enabled tests pass with no ignored cases,
covering direct/repeated and dense projections, DISTINCT, prepared-plan
retention, cold/warm execution and field admission. Strict core
all-feature/all-target lint, SQL-disabled all-target compilation, formatting
and whitespace checks pass. SQL-only tests retain 65 existing unused-code
warnings. Local logs and pre-edit sources: `/tmp/icydb-projection-preparation.ZmeaGU/`
(not release artifacts). Incremental footprint: three Rust files (-13 net
production lines, +48 test lines), plus two documentation files. Ownership is
simpler; no new behavior axis or semantic owner. Wasm/cycle/instruction deltas
are unmeasured. Existing lockfile changes were preserved; no local network
lifecycle actions or full repository tests were performed.

The user approved F17–F19 together: borrow the grouped projection for the fold,
converge three remaining lazy preparation helpers on `try_initialize_lazy`,
and share immutable aggregate input/filter expressions across groups. These
items are complete within 0.259.2. The simplest alternative keeps redundant
copies and repeated cache control flow. Existing planner, cache-retention and
reducer owners remain authoritative; no new cache, mode, API or persisted state
is introduced. Expression sharing adds one `Rc` allocation per present input
or filter expression in the aggregate blueprint, rather than copying that
expression into every group. Reducers and distinct sets remain group-local;
group state accounting continues to derive inline size from the actual state
type. No performance improvement is claimed without permitted measurements.

Qualification: 162 focused SQL-enabled tests pass with no ignored cases,
covering shared expression lifetime, independent reducers/DISTINCT sets,
NULL/filter behavior, cold/warm grouped results, ordered group accounting and
cache failure/retention boundaries. The new SQL test initially combined
DISTINCT with FILTER, which the maintained SQL admission contract rejects;
the fixture was corrected to supported expression-plus-FILTER syntax without
changing admission. Strict core all-feature/all-target lint and SQL-disabled
all-target compilation pass; SQL-only tests retain 65 existing unused-code
warnings. Local logs and pre-edit sources: `/tmp/icydb-grouped-expression-cleanup.9GjLDs/`
(not release artifacts). Incremental footprint: six Rust files (-12 net
production lines, +76 test lines), plus two documentation files. No new
behavior axis or cache owner; three repeated lazy flows converge and temporary
projection/per-group expression copies disappear. Wasm/cycle/instruction
deltas are unmeasured. Existing lockfile changes were preserved; no local
network lifecycle actions or full repository tests were performed.

The user approved F14–F16 together: keep the existing grouped resident `Rc`
through stream/fold handoffs, share scalar-aggregate preparation through the
existing lazy cache, and borrow the verbose EXPLAIN access-choice snapshot.
The smallest alternative is retaining copies whose consumers only read them.
Grouped preparation/layout provenance stays paired; aggregate strict policy,
typed failures and cache accounting stay with their existing owners. Verbose
diagnostics keep owned output strings but need no owned planner snapshot.
No new cache, mode, public API, persisted state or compiler is added. Aggregate
preparation adds one shared allocation when built; grouped preparation reuses
its existing allocation. These three explicitly combined items are complete
within 0.259.2: all 135 focused tests pass with no ignored cases, including
strict aggregate sharing/retention failures, filtered grouped pagination with
cold/warm and disabled caches, and stable cold/warm verbose planner output.
Repository clippy, strict core all-feature/all-target lint and SQL-disabled
all-target compilation pass. The initial focused lint identified that the
simplified grouped constructor can be `const`; this was corrected before the
passing reruns. SQL-only compilation retains the same 65 unused-code warnings.
Local logs: `/tmp/icydb-preparation-followups.buMICS/` (not release artifacts).
Incremental footprint: seven Rust files (-7 net production lines, +72 test
lines), plus two documentation files. Ownership is simpler; no second cache
or shared mutable execution state is introduced. Wasm, cycle and instruction
deltas are unmeasured. Existing lockfile changes were preserved; no local
network lifecycle actions or full repository tests were performed. Release
validation remains user-owned.

The user approved F13: share immutable scalar execution preparation through
the existing cache and runtime handoffs with `Rc`, replacing owned bundle
copies on retention and warm reads. The existing lazy initializer and retained
byte visitor remain authoritative, including payload and shared-allocation
overhead. The smallest alternative is retaining deep copies of programs,
literals and slot metadata. No new cache, mode, public API or persisted state
is needed. One shared allocation is added when building a bundle; its costs
must not be inferred from fewer copies. Aggregate/grouped ownership and the
separate verbose EXPLAIN cleanup are outside this slice. F13 is complete within
0.259.2: 127 focused SQL-enabled tests pass with no ignored cases, including
new shared-backing/retention coverage, existing changed-binding and cache
admission cases, projection, continuation and explain checks. Strict core
all-feature/all-target clippy and SQL-disabled all-target compilation pass.
The new regression initially double-counted the inline pointer slot in its
expected lazy charge; correcting that test expectation produced the passing
rerun without changing runtime accounting. SQL-only compilation retains the
same 65 unused-code warnings. Local logs: `/tmp/icydb-scalar-sharing.tHbI6Y/`
(not release artifacts). Incremental footprint: three Rust files, +3 net
production lines and +109 test lines, plus three release-documentation files.
Ownership is simpler: warm handoffs share immutable backing instead of copying
it. Wasm, cycle and instruction deltas are unmeasured. Existing lockfile changes
were preserved; no local network lifecycle actions or full repository tests
were performed. Release validation remains user-owned.

The user approved F12: remove strict predicate preparation from grouped route
selection and grouped execution EXPLAIN. The resulting materialization flag
is not consumed by the grouped execution-stage builder; actual grouped runtime
filtering remains separately prepared. The smallest alternative is retaining
unused compilation and temporary copies. No new behavior axis, cache, public
API or persisted state is needed. Removing this work also removes its budget
charges and possible failures, not semantic admission or runtime/diagnostic
budgets. F12 is complete within 0.259.2: 132 focused SQL-enabled tests pass
with no ignored cases, including matching/nonmatching grouped filters with
cold/warm and disabled caches, grouped execution-descriptor parity, diagnostic
construction exhaustion, field rejection and grouped strategy validation.
Repository clippy, strict core all-feature/all-target clippy, SQL-disabled
all-target compilation, formatting and diff checks pass. Initial lint
attempts encountered a dependency-download restriction and then the extended
test's function-length limit; dependency access and a focused assertion helper
resolved these. The SQL-only test build retains the same 65 unused-code
warnings. Local logs: `/tmp/icydb-grouped-route.ApSBdn/` (not release artifacts).
Incremental footprint: eight production files (-18 net lines), two test files
(+49 net lines), and two documentation files. The grouped route input and its
two caller paths are simpler, without a replacement compiler or budget owner.
Wasm, cycle and instruction deltas are unmeasured. Existing lockfile updates
were preserved. No local network lifecycle actions or full repository tests
were performed; release validation remains user-owned.

The user approved F11: compile the strict covering predicate directly from
borrowed planner inputs through the existing index-program compiler. The
projection caller consumes only that program, so its temporary full execution
bundle and unused capability classification can be removed. Preserve the
residual gate, strict policy, construction budget, typed failures and pure/
hybrid route ordering. The alternative is retaining unused copies and derived
metadata. No new cache, behavior axis, public surface or persisted state is
needed. F11 is complete within 0.259.2: 126 focused SQL-enabled tests pass
with no ignored cases, including direct/bundled strict-program parity,
unsupported and absent-input results, typed construction exhaustion, covering
reads, cold/warm preparation and lazy retention. Strict core all-feature/
all-target clippy, SQL-disabled all-target compilation, formatting and diff
checks pass. The SQL-only test build retains the same 65 unused-code warnings;
the other checks are clean. Local logs: `/tmp/icydb-covering-compiler.hWEeeK/`
(not release artifacts). Incremental footprint: two production files,
+28/-21 lines (+7 net), one test file (+53 net lines), and two documentation
files. The flow is simpler despite the small line increase: the existing
compiler gets one borrowed-plan entry point, and the caller owns only the
program it consumes. Wasm, cycle and instruction deltas are unmeasured. No
local network lifecycle actions or full repository tests were performed;
release validation remains user-owned.

The user approved F10: classify borrowed planner predicate/index inputs for
covering eligibility and load explain instead of constructing a temporary
owned execution-preparation bundle. Reuse the existing capability classifier;
keep real strict/conservative execution preparation and retained accounting
unchanged. The alternative is retaining predicate/target/slot copies whose
only consumer reads the capability result. Remove the superseded constructor,
with no new behavior axis, cache, public API or persisted state. F10 is complete
within 0.259.2: 57 focused SQL-enabled tests pass with no ignored cases,
including the new target-precedence/optional-input regression, maintained
strict/conservative preparation, expression indexes, covering reads and
cold/warm explain behavior. Strict core all-feature/all-target clippy,
SQL-disabled all-target compilation, formatting and diff checks pass.
The SQL-only test build retains the same 65 unused-code warnings; the other
checks are clean. Local logs: `/tmp/icydb-borrowed-capability.WjIDQM/`
(not release artifacts). Incremental footprint: four production files,
+31/-45 lines (-14 net), and one test file, +65/-3 lines (+62 net), plus active
release documentation. The constructor is removed and both read-only callers
borrow predicate, compile-target and slot-map inputs through the same existing
classifier; actual execution preparation remains owned. Wasm, cycle and
instruction deltas are unmeasured. No local network lifecycle actions or full
repository tests were performed.

The user approved the next two scalar-route audit findings together as F8/F9:
remove the always-absent caller probe override and borrow authority through
route construction instead of cloning/boxing it for read-only helpers. The
existing route owner retains aggregate probes, continuation/window hints and
covering eligibility; runtime hint normalization and retries are unchanged.
The alternative is retaining unused plumbing and temporary ownership. Neither
change adds a behavior axis, public surface, cache owner or persisted state.
F8/F9 are complete within 0.259.2: 126 focused SQL-enabled tests pass with no
ignored cases, covering cold/warm preparation, initial/resumed pagination,
covering and DISTINCT reads, explain parity, aggregate direction, range
pushdown, residual fetch widening and lazy retention. Strict core
all-feature/all-target clippy and SQL-disabled all-target compilation pass,
as do formatting and diff checks. The SQL-only test build retains the same
65 unused-code warnings; the other checks are clean. Existing behavioral
coverage is reused; no implementation-shape or retired-option tests were added.
Local logs: `/tmp/icydb-scalar-routing.hBZod9/` (not release artifacts).
Incremental footprint: six production files, +17/-49 lines (-32 net), plus
three updated documentation files. Two temporary authority clone sites and
two box-construction sites are removed; runtime ownership remains unchanged.
Wasm, cycle and instruction deltas are unmeasured. No local network lifecycle
actions or full repository tests were performed; release validation stays
user-owned.

The user reports 0.259.1 published and approved F7 for 0.259.2: require the
grouped preparation/layout bundle from its existing prepared-plan owner and
delete the runtime reconstruction fallback. Its absence required missing
metadata that grouped route validation already rejects, so reconstruction
cannot enable a valid execution. The smallest alternative is retaining this
unreachable duplicate flow. This adds no behavior axis, public API, cache owner
or persisted state; route predicate preparation remains separate because it
uses a different compilation policy. F7 is complete: 112 focused SQL-enabled
tests pass with no ignored cases, including grouped output/DISTINCT/ordering,
cold/warm and cache-disabled grouped sums, missing planner contracts, failed
lazy publication and declined retention. Strict core all-feature/all-target
clippy, SQL-disabled all-target compilation, formatting and diff checks pass.
The SQL-only test build retains its 65 pre-existing unused migration-helper
warnings; strict lint and SQL-disabled checks are clean. Qualification logs:
`/tmp/icydb-grouped-preparation.eixaDQ/` (local, not release artifacts).
Footprint: three production files, +48/-83 lines (-35 net); one test file adds
46 lines; three documentation files. No new modes or state; the implementation
is simpler. Wasm, cycle and instruction deltas are unmeasured. No local network
lifecycle actions or full repository tests were performed.

The `v0.259.0` tag now exists. The user subsequently authorised both findings
from the scoped query flow-convergence audit at `096eb9f2c789`. These are bounded
maintenance follow-ups in 0.259.1, not a new minor or an extension of the
migration rehearsal contract. F1 narrows the losing single-key probe; F2 carries
one canonical order from access selection to logical assembly. Their smallest
alternative is retaining the repeated work; neither needs a new behavior axis,
public API, cache owner or persisted state. Focused test qualification is recorded
below; full repository validation remains user-owned.
The user also authorised the subsequent complexity-audit finding C1: remove the
unused key-access override contract and its internal diagnostic variants. This
is deletion within the existing planner owner, with no new behavior axis.
The user then authorised F3: converge cold plan construction and parameterized
template rebinding. The same pipeline owns order construction, empty-result
selection and assembly; the cache still owns current authority/visibility,
candidate retention and publication. Keeping duplicate pipeline functions is
the smallest alternative, but leaves equivalent semantics maintained twice.
F3 adds no behavior axis, cache owner, public API or persisted state.
The next scoped audit found a duplicate cache probe after a filterless miss and
unneeded owned authority arguments in cache-key construction. The user approved
both together as F4. Keep the early warm-hit shortcut, carry its miss key into
the existing compile/publication owner, and borrow authority for key assembly.
This replaces repeated work rather than adding a cache, mode or budget. Scope
is session cache code, direct regressions, active notes and a matched raw-Wasm
comparison; no public or persisted contract changes.

The user approved the next two related authority findings together as F5:
require complete row/schema metadata in `EntityAuthority` and remove the unused
partial-schema construction/reconstruction path. The accepted-root compiler
remains the production owner; no new state or execution route is introduced.
The smallest alternative is retaining unreachable missing-metadata branches
and a test-only incomplete schema mode. Direct fallout includes removing the
cache's snapshot wrapper and unused pinned-continuation snapshot arguments.

F6 is the subsequently approved prepared-plan ownership cleanup: borrow
authority in cold/rewrite builders and lazy metadata initializers that only
inspect it. Existing plan/runtime objects keep their owned authority, and lazy
retention/accounting stays unchanged. The alternative is retaining temporary
shared-handle clones, including before warm resident lookups. No new behavior
axis, lifetime-bearing stored object or cache owner is needed.

M1–M3 are complete. The independent
[closeout audit](../../reports/releases/0.259/closeout/2026-09-18/02/report.md)
then reproduced watchdog retries after a `Prepared` restart and found two broken
idea links following archive moves. The user authorised these corrections on
2026-09-18. M4 completes that bounded follow-up; the
[correction qualification](../../reports/releases/0.259/closeout/2026-09-18/03/report.md)
passes. The line is ready for user-owned full release validation, not publication
or production application approval.
The earlier [qualification](../../reports/releases/0.259/closeout/2026-09-18/01/report.md)
remains historical evidence, not the current closeout verdict. No real downstream
migration, application change or deployment is authorised.

The subsequent read-only audit reproduced the current artifacts and command
cycle totals without finding a correctness or availability blocker. Its sole
non-blocking finding was missing pre-fix provenance for the historical M2
Wasm-size attribution; that claim is now qualified below and in the active
release notes. Historical reports remain unchanged.

Source baseline: `fb69db0329535554b0131f5cc77a7286217e2ae9` (commit subject
`0.258.1`). At inspection the workspace still declares 0.258.0 and there is no
tag on HEAD; the user reports the prior work is pushing. Do not infer completed
publication from the commit subject. Freeze the then-current source/lockfile
again for M2 builds. Cargo package versions remain release-tooling owned.

| Landing slice | Bounded outcome | Status |
| --- | --- | --- |
| M1 — Design and documentation reconciliation | Audit the current example and owners; select a bounded rehearsal contract and tidy promoted/stale ideas and release notes. | Complete; documentation validation recorded below. |
| M2 — Reproducible current-format rehearsal | Automatically build the existing two schema shapes through retained artifacts; run successful control, interrupted recovery and rejected checked-cast scenarios in ordinary focused tests; update the fixture instructions. | Complete, including the user-authorised startup correction. No engine or generic runner. |
| M3 — Application handoff and qualification | Document adaptation to application-owned seeds/expected results, record exact current artifact and cost evidence, qualify remaining scope gaps and close the line. | Complete; scoped closeout PASS. No production-data import, deployment or automatic downstream edits. |
| M4 — Approved closeout corrections | Converge Prepared startup waiting on the existing pending signal, prove quiescent restart and eventual publication, and repair the two audited links. | Complete; focused qualification passes. |
| F1 — Point-read probe | Avoid a losing first plan for multi-value primary-key membership while preserving pagination and validation. | Complete; focused regression and surrounding tests pass. |
| F2 — Canonical order ownership | Build order once on plan construction and move it through the existing pipeline. | Complete; ownership, accounting and SQL-free tests pass. |
| C1 — Unused override contract | Remove the always-absent key override, dead branch and diagnostic reasons; preserve maintained key planning. | Complete; 48 focused planner/explain tests pass. |
| F3 — Shared plan construction | Converge cold and template-rebound construction, retaining cache authority and accounting boundaries. | Complete; 146 SQL-enabled and 15 SQL-free focused tests pass. |
| F4 — Cache miss and identity handoff | Reuse the first filterless miss key and borrow key-construction authority; remove the unused template-lookup argument. | Complete; 117 focused tests, SQL-free build and core lint pass; matched SQL actor shrinks by 1,222 raw Wasm bytes. |
| F5 — Complete accepted authority | Require row/schema metadata at construction and reuse that schema directly; remove incomplete-metadata reconstruction and mechanical caller branches. | Complete; 185 focused tests, SQL-free all-target build and repository clippy gate pass. |
| F6 — Borrowed preparation authority | Borrow authority for read-only cold/rewrite construction and lazy metadata initialization; retain ownership at runtime handoffs. | Complete; 119 focused tests, SQL-free all-target build and core all-feature/all-target clippy pass. |
| F7 — Required grouped preparation | Require the existing preparation/layout bundle, remove runtime reconstruction and preserve execution when cache retention is declined. | Complete; 112 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F8 — Remove unused route probe override | Delete the always-absent caller override chain while preserving live aggregate, window and runtime hints. | Complete with F9; 126 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F9 — Borrow scalar route authority | Borrow authority during initial/resumed route construction and warm route lookup; remove temporary clones/boxes. | Complete with F8; existing runtime ownership and covering eligibility remain intact. |
| F10 — Borrow capability inputs | Classify covering/explain inputs directly from the frozen plan and remove the unused owned preparation constructor. | Complete; 57 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F11 — Direct covering predicate compilation | Compile the required strict program from borrowed plan inputs instead of constructing an otherwise unused execution bundle. | Complete; 126 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F12 — Grouped route inputs | Remove unused strict compilation from grouped execution and EXPLAIN route selection; preserve runtime filtering and scalar aggregate compilation. | Complete; 132 focused tests, repository clippy and SQL-disabled all-target compilation pass. |
| F13 — Shared scalar preparation | Share the immutable scalar preparation bundle across cache retention and execution handoffs; preserve failure-safe admission and binding identity. | Complete; 127 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F14 — Shared grouped handoff | Carry the existing preparation/layout resident through grouped stream/fold execution without cloning its contents. | Complete; combined 135-test qualification, lint and SQL-disabled compilation pass. |
| F15 — Shared aggregate preparation | Share cached scalar-aggregate preparation without changing strict compilation or routing. | Complete; same combined gate includes strict sharing, retention and typed failure checks. |
| F16 — Borrow verbose snapshot | Render verbose diagnostics from the borrowed planner access-choice snapshot. | Complete; same combined gate includes cold/warm planner-output parity. |
| F17 — Borrow grouped projection | Fold through the planner-owned projection without a temporary deep copy. | Complete; combined 162-test qualification, strict core lint and SQL-disabled compilation pass. |
| F18 — Converge lazy preparation | Reuse the existing fallible initializer for projection shape, scalar route and cursorless slot layout. | Complete; same gate covers failed construction, warm reuse and declined retention. |
| F19 — Share group expressions | Share immutable input/filter expressions while keeping reducer and distinct state independent per group. | Complete; same gate covers expression lifetimes, group independence and cold/warm results. |
| F20 — Lean scalar projection preparation | Remove redundant retained syntax and construct direct-slot metadata without an intermediate vector. | Complete; 141 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F21 — Required prepared-query state | Remove the redundant retained-slot flag and require accepted identity/projection metadata at their prepared boundaries. | Complete; 148 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F22 — Lean covering and route state | Remove unused covering prefix payload and derive grouped intent from its planner strategy. | Complete; 61 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F23 — Borrow filter inputs | Borrow HAVING from the retained plan and delegate scalar residual-filter access to preparation. | Complete; 138 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F24 — Borrow aggregate comparisons | Reuse semantic-key views for projection identity and deduplication before copying a new aggregate spec. | Complete; 126 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F25 — Converge stream/materialization contracts | Dispatch by the request variant and share terminal request construction without merging execution paths. | Complete; 132 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F26 — Narrow grouped execution contracts | Require planner limits and check grouped result cardinality before returning the page, without debug-only retained state. | Complete; 157 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F27 — Simplify retained-slot preparation | Keep one requirement state per slot, defer override-vector allocation and share accepted-codec eligibility. | Complete; 148 focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F28 — Preserve cursor invariant errors | Retain runtime classification and cursor origin through query/session conversion and public error projection. | Complete; 53 focused test executions and strict core/facade lint pass. |
| F29 — Correct grouped cursor policy errors | Reject unsupported cursor requests with typed policy reasons while preserving genuine runtime invariants. | Complete; 51 focused test executions and strict core/facade/diagnostics lint pass. |
| F30 — Authenticate grouped continuation | Reuse the authenticated cursor envelope/key and reject malformed boundaries against accepted plan/schema metadata. | Complete; 90 focused test executions and strict core lint pass; regenerate grouped cursors. |
| F31 — Retain admitted grouped cursor bytes | Encode once inside execution admission, move bytes to the response and charge their exact Base64 length. | Complete; 86 focused test executions and strict core lint pass. |
| F32 — Preserve canonical decimal group boundaries | Validate normalized decimal keys recursively without weakening stored-value admission or rewriting cursor boundaries. | Complete; 109 focused test executions and strict core lint pass. |
| F33 — Borrow grouped diagnostic metadata | Borrow frozen aggregates and DISTINCT strategy until execution needs owned route metadata, preserving layout derivation and validation. | Complete; 48 focused test executions and `make clippy` pass. |
| F34 — Remove duplicate DISTINCT labels | Keep the target label only in its accepted field slot and update retained accounting. | Complete; combined F34–F35 qualification passes. |
| F35 — Avoid discarded grouped preparation outputs | Collect aggregate specifications independently and derive layout against frozen slots without copying syntax. | Complete; 70 focused test executions and strict core lint pass. |
| F36 — Narrow grouped fold setup and lookup | Borrow target slots, derive fold direction and remove unused lookup error/context inputs. | Complete; 108 focused test executions and strict core lint pass. |
| F37 — Centralize grouped page-selector setup | Compile projection/HAVING/top-k once per setup through the existing constructor and obtain pagination from the route. | Complete; 69 focused test executions and strict core lint pass. |
| F38 — Replace bounded grouped heap roots in place | Repair COUNT and generic/top-k heaps once on replacement, preserving existing ordering and charges. | Complete; 62 focused core tests and two matched PocketIC runs pass; -192 raw Wasm bytes and small measured instruction/cycle savings. |
| F39 — Remove unbounded candidate staging | Avoid the temporary finalized-candidate vector without changing error precedence. | Deferred by user: current aggregate-finalization-before-HAVING/projection ordering stays intact; no error-buffering machinery. |
| F40 — Single compiled SQL authority owner | Remove duplicate optional authority and consume the existing accepted catalog directly. | Complete; nine focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F41 — Borrow SQL mutation selectors | Pass read-only structural queries directly through ordinary and primary-only projection planning. | Complete; nine focused tests, strict core lint and SQL-disabled all-target compilation pass. |
| F42 — Direct same-entity insert preparation | Resolve one accepted catalog and lower patches without per-row named mutation wrappers. | Complete; 14 focused SQL-enabled/disabled test executions and strict core lint pass. |
| F43 — Single startup schema-receipt comparison | Preserve the early failure-priority check and carry only reconciliation status afterward. | Complete; nine focused startup tests and strict core lint pass. |
| F44 — Shared aggregate EXPLAIN construction | Construct and admit each terminal once through one owner before format-specific rendering. | Complete; eight focused post-change tests and strict core lint pass; pre-refactor regression also passes. |
| F45 — Borrow selected RETURNING cells | Avoid full-row copies during pre-commit response checks while preserving existing projection and rejection contracts. | Complete; eight focused native tests, matched Wasm cost probes and strict core/integration lint pass. |
| F46 — Byte-driven CRC32C | Replace runtime bit steps with a compile-time byte table at the existing format owner, keeping identical checksums and corruption gates. | Complete; checksum/corruption proofs, full lint and combined matched-Wasm probe pass. |
| F47 — Count-only SQL result capture | Derive the existing writer output flag from RETURNING, retaining row counts and all validation/commit checks without materializing discarded values. | Complete; count/RETURNING, constraints/atomicity, full lint and matched-Wasm probe pass. |
| F48 — Preserved-slot update comparison | Use already-resolved write provenance to avoid re-encoding unchanged slots in logical change detection, including accepted historical fills. | Complete; patch/timestamp/constraint tests, full lint and matched-Wasm probe pass. |
| F49 — Single prepared journal-batch owner | Borrow the marker for publication and tail append instead of deep-cloning its batches. | Complete; 26 focused native executions, strict core lint and two matched-Wasm runs pass. Cost trade-off recorded above. |
| F50 — Count size-only Candid output | Use the maintained encoder with a checked counting sink, avoiding its final payload copy in RETURNING admission. | Complete; seven focused tests, strict core lint and 18 fresh Wasm calls pass. Remaining third-call cost rise recorded above. |
| F51 — Extend marker-owned publication batches | Move schema and migration journal batches into the marker and borrow them for append/application. | Complete; combined 25 focused tests and strict core lint pass. Incremental Wasm/cycles unmeasured. |
| F52 — Consume prepared schema positions | Move final-use publication keys rather than cloning them during mutation apply. | Complete; combined 25 focused tests and strict core lint pass. Clone removed from the final-use DTO. |
| F53 — Move row operations into journal records | Consume final-use row operations after preflight rather than cloning their keys and after-images into journal records. | Complete; 20 focused native tests, strict core lint and 36 matched-Wasm calls pass. Mixed small-row costs recorded above. |
| F54 — End overlay work at its last consumer | Omit final-row staging and release the preflight overlay before journal preparation. | Complete; 31 focused native tests, strict core lint and 36 matched-Wasm calls pass. Mixed costs and +45 raw Wasm bytes against F53 recorded above. |
| F55 — Marker-owned mutation progress | Remove the parallel progress-operation copy and use the marker for commit selection and application. | Complete; 17 focused tests, strict core lint and formatting pass. Incremental cycles/instructions/Wasm unmeasured. |
| F56 — Direct synchronized-store selection | Filter registry handles directly into the final selected vector without staging every handle. | Complete; 15 focused tests, strict core lint and formatting pass. Incremental cycles/instructions/Wasm unmeasured. |
| F57 — One commit completion flow | Consume the existing window bundle in one apply/readiness owner for both entrypoints. | Complete; combined qualification above passes. |
| F58 — Remove unused mutation arguments | Delete unused phase/key plumbing; use Db only for shared store selection. | Complete with F57; validations unchanged. |
| F59 — Converged row application | Measure removal of the production single-row branch while preserving test interruption behavior. | Complete; -702 raw Wasm bytes, +203–476 cycles/instructions on measured successful calls; retain the simpler flow. |
| F60 — Application batch qualification | Measure exact reads → application validation → typed writes, rejection and request chunking in the maintained fixture. | Complete; 11 scenarios/25 updates pass, current APIs suffice for this workload; no runtime changes. |

## Scope controls

- Logical entity source revisions use the maintained migration contract;
  internal representations remain current version 1. This is not a bridge
  between incompatible pre-1.0 releases.
- SQL stays only where already used by the fixture. New SQL endpoints, migration
  phases, budget frameworks, cache owners and configuration languages are out.
- A source-inspection finding is not proof of runtime failure. The ignored
  closeout target has not been freshly executed in M1.
- Canic's memory-admission integration and Toko Miner adoption remain separately
  owned; they are not prerequisites for this isolated current-format fixture.
- Runtime defects or demands for a generic runner require evidence and a scope
  decision before widening this line.

M1 validation passed: all 399 local file links in the affected Markdown files
resolve, no references to the removed idea files remain in the searched docs,
root changelog or README, and the diff passes whitespace checks. Link anchors
and external URLs were not checked. No tests, builds, network lifecycle actions
or performance measurements were run for these documentation-only edits;
implementation/behaviour is unchanged.

## M2 qualification and scope decision

The existing fixture now builds both source revisions automatically and reads
each retained artifact before another variant can replace its output. Two
ordinary tests cover three scenarios; both actors build before PocketIC starts.
After upgrades, the existing bounded startup-watchdog helper delivers recovery.

Reproduced and corrected blocker: with the successor deployed and no explicit migration yet
prepared, ordinary generated reconciliation reaches
`preflight_ordinary_source_application` and returns `RUNTIME_UNSUPPORTED`.
Startup classifies that as terminal schema reconciliation and persists a failure
bound to the submission and accepted head. Advancing to `Validating` does not
change that head, so the matching failure masks the expected typed recovery
gate. Before the fix, the success/control assertion observed code 22 rather
than 259. After the fix, that assertion and the later publication/interruption
assertions pass without relaxing their expected results.

Relevant owners are
[ordinary source admission](../../../crates/icydb-core/src/db/schema/application.rs),
[startup classification](../../../crates/icydb-core/src/db/startup/mod.rs),
[failure recording](../../../crates/icydb-core/src/db/startup/driver.rs) and
[startup observation](../../../crates/icydb-core/src/db/startup/observe.rs).
The test does not accept the wrong code or bypass the generated endpoint.
The user authorised the narrow startup/migration correction on 2026-09-18.
Keep it inside M2's reproduced boundary: after successful existing preflight,
reuse the typed migration-pending error instead of `Unsupported`; the generated
watchdog stops while explicit controller commands own progress, without storing
a terminal failure or busy-retrying the pending plan. Genuine validation errors
still propagate. No new migration engine, startup mode, error code, persisted
failure format or compatibility repair is authorised. The runtime correction is
implemented and the focused qualification passes. Generic unsupported schema
failures retain their terminal classification; invalid proposals still fail
existing preflight before the pending signal can be returned.

The integration test also checks subnet update counts across simulated idle
rounds after draining already queued timer work: waiting in Idle and after the
gated upgrade adds no background update transactions. These are execution
behaviour checks, not elapsed-time performance measurements. No actor endpoint
or instrumentation was added.

The rejection scenario passes after using the correct application lifecycle:
typed Transform finding, three validated rows, zero rewrites/rebuilt indexes,
unchanged accepted head, permitted Abort, then redeployment of the exact
current-format source actor and comparison of all selected source rows/IDs.
Abort does not make the successor's generated API the accepted source API.

M2 evidence uses source `fb69db0329535554b0131f5cc77a7286217e2ae9` plus this
worktree, Rust 1.98.1, PocketIC 16.0.0 and lockfile SHA-256
`2f54290603809514e794d6340aa0a950fcb2fa788d5b524b688553e18a69cb72`.
The final retained actor artifacts are:

| Actor | Raw Wasm bytes | BLAKE3 |
| --- | ---: | --- |
| Source | 8,767,079 | `620d41d47b01da18956ed004166e25ef67b29743d4c44e31db71e0a763b4fdf9` |
| Successor | 8,803,709 | `06f8c1c50c27cff5a8ad88aa7eaa5b208c91cbb130ef7a4f80514fad4f9d71d4` |

The recorded pre-fix sizes (8,767,053 and 8,803,666 bytes) differ from these
M2 artifacts by +26 and +43 raw bytes. Attribution to the startup correction
is UNVERIFIED: the evidence does not retain exact pre-fix source and artifact
identities sufficient to establish otherwise-identical inputs. This limitation
does not affect the verified current artifacts or migration behavior. Do not
compare the two different actor shapes as a speed claim. M2 did not measure IC
cycles or instruction deltas; M3 records whole-call cycle observations below,
not a before/after optimisation claim.

Validation: the complete migration target passes (two tests, three scenarios,
none ignored), along with three focused physical-migration core tests, nine
startup tests with no default features and seven generated-store model tests.
The initial sandbox run could
not bind a local port; its waiting test process was stopped, and subsequent
PocketIC runs used approved local-port access. A doc-paragraph clippy warning
was repaired; full `make clippy` and the repeated focused clippy gate pass.
All three logical-memory integration tests also pass through the shared build
helper. Formatting, diff whitespace and all 17 local file links in the M2
design/status/changelog/guide documents pass; link anchors/external URLs were
not checked. Full repository tests remain user-owned.

One build attempt correctly rejected inputs edited during compilation; the
final successful rehearsal used a stable source snapshot. Local PocketIC
instances were started for these disposable tests; application networks were
not changed.

Complexity: M2 touches five Rust files, approximately +245 net lines, mostly
integration coverage. The runtime fix changes two existing owners and reuses
the existing pending signal and watchdog stop result.
The common retained-artifact read is shared rather than duplicated; host test
coverage grows to the three planned scenarios. No independent runtime state,
configuration or format is added. The guide, design, tracker and active release
notes are updated alongside the tests.

## M3 closeout

The guide now explains adapting exact actors, application-owned seeds, expected
results, bounded advancement and authorised abort. It distinguishes core
`Prepared` admission from generated startup readiness. No application repository,
production data, canister endpoint or persisted representation was changed.

Fresh qualification lists and executes both ordinary rehearsal tests: two pass,
none fail or are ignored. Focused integration clippy and formatting pass.
The [report](../../reports/releases/0.259/closeout/2026-09-18/01/report.md) owns
exact source/fixture hashes, features, costs and scope limits. Actors remain
byte-identical to M2 (zero raw Wasm delta). Host measurement records explicit
command totals of 1,310,057,766 cycles for the control, 1,325,731,314 for the
interrupted run and 541,852,863 for rejection plus abort. Install, upgrade and
separate startup/read/seed calls are outside these totals; instruction counts
and before/after cycle deltas remain unmeasured.

M3 adds approximately 13 Rust lines in the existing test plus guidance and one
report. Runtime shape is unchanged: no new runner, report DTO, budget or
configuration. Full repository validation and publication remain user-owned.
A new minor requires explicit authorisation.

## M4 closeout corrections

The reproduced need is a `Prepared` restart retrying without progress: 24 updates
and 283,437,277 whole-window cycles in the bounded idle probe. The maintained
regression reproduces the same failure before the fix. Rather than adding a
watchdog mode or another receipt, the existing core generated-schema guard now
returns the existing typed pending error for every validated nonterminal
migration. Deployment, plan, database and accepted-head checks still precede
that signal. Terminal records proceed through ordinary schema application.
Core row-operation admission in `Prepared` is unchanged.

The old boolean deferral handoff is removed and renamed in place to a unit-valued
admission check, including its facade/re-export propagation. No compatibility
wrapper, public endpoint, persisted state or independent behavior axis is added.
One additional integration test reuses the existing completion helper, restarts
at `Prepared`, verifies unchanged progress/read gating and quiescent waiting,
then completes publication and checks final rows. Owner-local coverage also
checks that a mismatched deployment remains a typed plan error rather than
being hidden by the pending signal. The two audited idea links now target their
archived design owners; the broader archive move remains separately owned.

Qualification: the three physical-migration core tests and all three integration
tests pass, including Prepared and Validating restart/resume. The maintained
Prepared regression failed before the fix with 24 extra updates and 283,437,277
cycles, then passed with zero updates and zero cycle loss in the same bounded
idle check. Source raw Wasm remains 8,767,079 bytes; successor falls from
8,803,709 to 8,803,598 bytes (-111). Instruction deltas remain unmeasured.
The no-default-feature facade build and focused lint/format checks pass. Local
PocketIC servers were used only for disposable test instances; application
networks were untouched. Full release tests remain user-owned.

M4 changes five Rust files by approximately +10 net lines including regression
coverage, plus the direct guide, links, design/status, changelogs and one report
(13 files total). Production structure is simpler: the boolean deferral and its
consumer branch disappear. No new behavior axis or duplicate runtime flow.
Both migration closeout findings are resolved. The later query-audit follow-ups
are tracked separately above.

## F1–F2 qualification

The combined user-authorised follow-up passes 145 focused SQL-enabled tests for
logical assembly, planning, continuation identity, explain, index selection,
binding reuse and preparation accounting, plus 15 SQL-free ordering/continuation
tests. The initial focused run had one obsolete expectation for a second order
copy; it now checks only cold candidate backing, retaining exact-boundary
rejection and cache-publication assertions. The SQL-only test build emits
dead-code warnings in unchanged migration-related code.
The all-feature, all-target core Clippy check passes with warnings denied;
formatting and diff whitespace checks also pass.

The new membership regression independently checks expected IDs and bounded
continuation, one cold plan compilation, no new warm compilation, zero-limit
results and invalid operand rejection. The order handoff preserves backing
ownership and adds no construction charges. These are resource-contract tests,
not cycle or instruction measurements. Raw Wasm, cycles and instruction deltas
remain unmeasured. No network lifecycle actions, Cargo version/lockfile changes,
commits or publication were performed.

The handoff changes 12 files, approximately +180 net lines including tests and
notes; production code is approximately unchanged in line count and simpler in
flow. It removes repeated preparation and redundant order ownership without
adding a mode, persisted state, cache, public surface or budget.

## C1 qualification

Remove the always-absent override parameter from both planner functions, its
unreachable branch, the two internal reason variants and their projection/code
mapping. Comments now describe the maintained predicate and resource-summary
handoff. Historical release notes remain unchanged. No current query route,
public API, diagnostic output or persisted format changes.

All 48 focused access-choice, planner-pipeline and typed-explain tests pass,
including current key membership, range reads and warm diagnostic reuse. The
SQL-free core build, all-feature core library Clippy with warnings denied,
formatting and whitespace checks pass. No
legacy-absence tests or compatibility aliases were added. This follow-up touches
four production files (approximately -23 net lines) and three documentation
files. Complexity is lower; no new behavior axis. Raw Wasm, cycle and instruction
deltas remain unmeasured. Full release validation remains user-owned.

## F3 qualification

Cold planning and template rebinding now enter the same pipeline with accepted
candidate index contracts. Remove the duplicate construction/empty-result
selector and redundant forwarding methods. The cache still selects current
authority and index visibility, retains candidate backing, skips construction
on an identical-bound memo hit, applies current exact-cardinality selection and
publishes only after successful preparation. Cold-only compilation charges and
candidate allocation remain unchanged.

All 146 focused SQL-enabled tests pass across pipeline, logical ordering,
continuation, cardinality/explain and shared-cache owners. Coverage includes
A/B/A/A accounting, changed membership/range values, invalid bindings, index
lifecycle and failure-safe publication. One new owner-local regression pins
empty-key access and zero-limit diagnostic precedence over a false predicate.
All 15 focused SQL-free ordering/continuation tests also pass. The all-feature,
all-target core Clippy gate passes with warnings denied; formatting and diff
whitespace checks pass. The SQL-only test build still emits 65 warnings in
unchanged migration-related code. Logs are retained locally under
`/tmp/icydb-f3.dxQ3bJ/`; full repository validation remains user-owned.

F3 touches seven production Rust files and three documentation files, removing
approximately 120 net production lines and adding 47 lines of regression
coverage/test-module documentation. The structure is simpler: one semantic
construction path, no new public API, behavior axis, state or budget. Raw Wasm,
cycle and instruction deltas remain unmeasured; resource-counter checks are not
performance measurements. No Cargo version/lockfile changes, network lifecycle
actions, commits or publication were performed.

## F4 qualification

Ordinary cache lookup now returns its key alongside the optional hit. A
filterless miss keeps that key through either trivial or general construction;
filtered queries still normalize before their lookup. Both ordinary miss paths
use the same compile/publication helper, without a second probe. Current
authority, visibility and cardinality checks, warm filterless bypass, request
completion charges and failure-safe publication remain with their existing
owners. Key assembly borrows authority; completed plans take ownership where
possible. Template lookup no longer accepts an unused authority argument.

All 117 focused session ordering/cache, cardinality/explain and cache-identity
tests pass. The new regression covers trivial and ordered filterless queries in
public, trusted and diagnostic lanes: exhausted cold compilation leaves no
resident, one admitted compilation publishes the plan, and warm reuse succeeds
without a compilation allowance or row execution. Existing schema/index
lifecycle, A/B/A bindings and final-charge publication tests also pass. The
SQL-free core build, all-feature/all-target core Clippy with warnings denied,
formatting and whitespace checks pass. The SQL-only test build retains the 65
warnings in unchanged migration-related code; full repository tests remain
user-owned.

Matched measurement uses the maintained `sql` actor, production build policy,
`wasm-release`, SQL and Candid export enabled, defaults disabled, and the same
retained Cargo/post-link staging tool. Rust is 1.98.1; lockfile SHA-256 remains
`5ed0f3e3eebcf5a958d0bfcde7b8fd33e93377b15264023c7e8080bff2266e5c`.
The baseline is HEAD `096eb9f2c789` plus the pre-F4 dirty worktree, including
F1–F3/C1, captured in `baseline.diff` with SHA-256
`26662da0189f79ab9749742828328af54be227e1da5f0540e2cb07652c9c675f`.
Only the two F4 production files change runtime build inputs; the regression is
test-only. The candidate source SHA-256 identities are:

- `cache.rs`: `1fb30de30bfd2d174be487faa7e8a8a024f6706ce8e44dc7a9f3db6df2289b71`.
- `cache/identity.rs`: `2f83917b2f6569d13cfdde94a59484b873c966f0c72a9b245743bab39ad2cf2d`.

| SQL actor | Before bytes | After bytes | Delta |
| --- | ---: | ---: | ---: |
| Compiler-emitted raw Wasm | 4,360,680 | 4,359,359 | -1,321 |
| Final deployable raw Wasm | 3,789,437 | 3,788,215 | -1,222 |

Final artifact SHA-256: before
`aaeb213b7af950002768eaef91fedb6541a3446623bc039a7eacacf82369ce6c`, after
`8fedb25908ef304a14b8772f18808e0fda9d894a90437b04e662c8e7590f9084`.
Local patches, source copies, build/validation logs and before/after bytes are
retained under `/tmp/icydb-cache-cleanup.pyEKPu/`. An initial typed-only actor
build was not used for this comparison because its direct-key endpoint does not
qualify the cache path. No PocketIC/ICP network was started or changed; only
local fixture build outputs were staged. Cycles and instruction deltas remain
unmeasured; no execution-speed or production-ceiling claim follows from size.

F4 changes two production files (-20 net lines), one regression file (+73 net
lines) and three active-note/tracker files. The session-owned flow is simpler:
one lookup per ordinary preparation, one ordinary miss publication boundary,
and borrowed key identity inputs. No new public API, persisted state, runtime
mode, budget, dependency or Cargo version is added. No commit or publication
was performed.

## F5 qualification

Complete accepted authority is now enforced by the existing constructor:
row layout and schema are required fields, and accepted schema projection
always includes supported expression-index metadata. Query preparation clones
the captured schema handle directly. The unused snapshot wrapper and pinned
continuation snapshot arguments are removed; cache identity still carries
accepted root, revision, version and fingerprint, and visibility is unchanged.
The scalar runtime adapter is also infallible now that its only failure source
was the removed missing-layout state. Genuine access-plan, field, current-root
and index-readiness checks remain fallible.

The former incomplete-schema reconstruction fixture is replaced by a
complete-expression-index sharing regression. Retained-root isolation,
cold/warm expression lookups and ranges, pinned continuations, grouped/scalar
execution, cache publication and accounting tests remain in the focused gate.

Fresh validation on Rust 1.98.1:

- 185 focused core tests pass after the final adapter change. Two existing
  manual native-timing benchmarks remain ignored; no timing measurement ran.
- The repository clippy gate passes, including its workspace/all-target,
  SQL-only core and selected all-feature canister checks. Its initial
  unnecessary-Result finding in the scalar adapter was corrected.
- Core SQL-disabled compilation passes for all targets.
- Formatting and whitespace checks pass. The SQL-enabled focused test build
  retains the 65 pre-existing unused migration-code warnings; clippy is clean.
- Full repository/workspace tests remain user-owned and were not run.

Scope/complexity: 41 Rust files (+141/-332, net -191) plus the root changelog,
minor notes and this tracker. Most files only propagate removed arguments or
infallible accessors. No public API, stored format, dependency, Cargo version,
budget, configuration or lifecycle change. No local ICP/PocketIC network was
started, stopped or restarted. Wasm, cycles and instructions are unmeasured for
F5; do not carry F4's measured artifact delta forward as an F5 result.

The preserved pre-F5 source snapshot, baseline diff and focused/lint logs are
local evidence in `/tmp/icydb-complete-authority.2LYzhN/`. The lockfile SHA-256
remains `5ed0f3e3eebcf5a958d0bfcde7b8fd33e93377b15264023c7e8080bff2266e5c`.
Earlier uncommitted F1/F2/C1/F3/F4 work is preserved.

## F6 qualification

Read-only prepared-core builders and projection/covering/grouped/retained-slot
initializers now accept borrowed authority. The shared plan and scalar rewrite
handoff retain their original owned authority after building the core. The
route request that consumes boxed authority and the runtime handoffs that
retain it remain owned; no borrow is stored in a returned plan or resident.
Lazy cache lookup, construction, failure handling and retention accounting
are unchanged.

- 119 existing focused tests pass with SQL enabled; no cases are ignored.
  Selection covers session unit-ordering/cardinality suites and prepared-core
  retention tests, including DISTINCT queries, warm reuse, authority isolation
  and rejected lazy attachment. No clone-count instrumentation or test-only
  execution mode was introduced.
- Core all-feature/all-target clippy and SQL-disabled all-target compilation
  pass. Formatting and whitespace checks pass.
- The SQL test build retains 65 pre-existing unused migration-code warnings;
  clippy is clean. Full repository/workspace tests remain user-owned.
- Complexity: five production files (+25/-28, net -3), removing eight temporary
  authority-clone sites, plus the three active release/tracker documents.
  No new API, configuration, budget, persisted state or lifetime-bearing DTO.
- Wasm/cycle/instruction deltas are unmeasured. These were shared-handle clones,
  not deep metadata copies; no execution-speed claim is made.
- No local ICP/PocketIC lifecycle actions, Cargo version edits, commits or pushes.

Pre-F6 sources, baseline diff and qualification logs are retained locally in
`/tmp/icydb-borrowed-preparation.NNynfs/`. Earlier dirty-worktree changes are
preserved.
