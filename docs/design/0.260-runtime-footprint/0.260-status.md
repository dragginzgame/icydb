# 0.260 — Status

Authorised by the user on 2026-09-19: push finished; start 0.260, with ICYDB-033
first and collection materialization next. Authority: [design](0.260-design.md).

## Current handoff

W1 is **complete for the six controlled composition subjects**. The
[reconciliation](w1-reconciliation.md) accepts Canic's 2026-09-20 return against
the [original handoff](canic-handoff.md), including retained compiler/final
Wasm, source identity, section/function accounting and named reachability.
The first typed page adds 892,813 raw bytes; nine further matching entities add
8,202. The structural projection/page owner is the bounded W2 investigation
target, not a proven reduction. W2 is now **complete: no-build**, after the
[bounded source investigation](w2-disposition.md) found no justified simpler
owner-local candidate. Q1's read-only closeout audit is next.
Full Toko Miner attribution and final 0.260 closeout remain outstanding.

The 2026-09-19 prerequisite checks found only the older empty-participant pair
and no returned six-subject evidence. That blocker is superseded by this
reconciliation; do not reopen the discarded startup-outlining experiment.

The user authorised C1 investigation ahead of W1/W2 on 2026-09-19. Its
[source preflight](collection-preflight.md),
[baseline](../../reports/investigations/2026/09/19/collection-materialization/01/report.md)
and [matched correction](../../reports/investigations/2026/09/19/collection-materialization/02/report.md)
complete the bounded investigation. Retain exact null-sentinel recognition at
the nullable by-kind boundary: it fixes valid floating-point list rejection
and removes a redundant generic-wire traversal. Raw actor Wasm is 252 bytes
smaller; all 54 query instruction/cycle observations are unchanged. The fixture
uses named CatalogValue lists, not the corrected by-kind boundary, so it is a
non-regression control, not proof of the removed traversal's IC cost.

C1 records no-build for broader streaming/materialization changes: allocation
attribution and a simpler canonical admission replacement are not established.
Existing ownership and complete validation remain intact. Do not turn the
remaining collection cost into an open-ended implementation programme.

R1 is complete with a [matched typed-catalogue measurement](../../reports/investigations/2026/09/19/typed-catalogue-reads/01/report.md).
Sharing immutable facade bindings removes repeated deep mapping comparisons
from generated field conversion. Ordinary 128-row reads use 16.1–16.3% fewer
instructions and 13.1% fewer explicit-call cycles; raw actor Wasm is 744 bytes
smaller. The same nested fixture and all returned values are checked before
and after. This is not application/deployment headroom evidence. There is no
new execution route, cache or authority shortcut. Arc preserves Send/Sync and
content equality for independently issued equivalent bindings. Composed
attribution is qualified separately by W1.

R2's [selected-field investigation](../../reports/investigations/2026/09/19/selected-field-ergonomics/01/report.md)
is complete. Existing structural selection can serve label-only reads; the
gap is checked conversion into application DTOs, not missing execution support.
Four focused projection/admission tests passed. No API or application changes.
Recommend a matched existing-surface measurement before deciding whether typed
projection ergonomics warrant an extension. The user authorised that bounded
measurement as R3 on 2026-09-19; no new API is authorised.

R3's [matched label measurement](../../reports/investigations/2026/09/19/selected-field-ergonomics/02/report.md)
is complete: 128-row selected reads use 38.8–39.3% fewer cycles and 45.5–46.2%
fewer instructions than full typed rows reduced to identical labels. The
fixture checks exact output and real two-page continuation. No runtime or
downstream changes; the improvement requires callers to choose selection.
Retain the existing selection API; a new typed DTO API is not justified by
performance alone. Any ergonomic extension needs a separate explicit decision.

D1 was authorised on 2026-09-20 after reviewing Toko Miner's ICYDB-004.
The exact-count owner now maps unavailable metadata to
`QUERY_EXACT_COUNT_METADATA_UNAVAILABLE` (E275, Unsupported class, Query origin).
Unsupported count shapes retain their own rejection, and the terminal remains
metadata-only. The public diagnostic registry, facade API docs, Candid coverage
and CLI rendering carry the distinction. Applications must update code matching;
removing Toko Miner's bounded fallback still requires downstream qualification.
No sibling source, CLI installation or dependency pin was changed.

| Slice | State | Next action |
| --- | --- | --- |
| W1 — Composed attribution | Complete: six controlled subjects reconciled; application attribution remains open | Preserve the returned source/artifact identities and qualification limits. |
| W2 — Footprint reduction | Complete: bounded source investigation; no-build | No measured reduction or runtime change. Reopen only with a concrete simpler candidate and matched qualification. |
| C1 — Collection materialization | Complete: narrow nullable correction; broader streaming no-build | Preserve the measured fixture and corruption coverage; revisit only with new consumer-specific evidence. |
| R1 — Full-row typed catalogue reads | Complete: shared immutable facade binding; matched IC reduction | Preserve exact-value and binding-admission coverage. Applications unchanged. |
| R2 — Selected-field ergonomics investigation | Complete: existing route and ergonomic gap documented; no new API | Existing route is qualified by R3; do not add an API without a separate scope decision. |
| R3 — Selected-field measurement | Complete: same-output IC saving demonstrated | Preserve the fixture; downstream adoption and any ergonomic API extension remain separate. |
| D1 — Exact-count diagnostics | Complete: distinct E275 for unavailable metadata; focused validation passes | Publish/adopt the diagnostic; qualify application metadata availability before removing its bounded fallback. |
| Q1 — Closeout | Preliminary read-only audit performed; final audit next | Reconcile the delivered W2 no-build disposition and current-candidate qualification; no complete-minor verdict yet. |

## Historical preflight inputs — 2026-09-19

- IcyDB HEAD: `4698cecb8dbba6786079b759b43494b020559997`, initially clean.
  The user reports the push complete. Its subject says `0.258.7`, but that is
  not version/publication authority: the root notes include 0.259.7, while
  Cargo still says 0.259.6. Leave release tooling/version state untouched.
- IcyDB lock SHA-256:
  `8024973c209ab9244ac24687f3a8d42e772f0c3c4454d7a5f57aeaaa0c34c658`.
- Canic HEAD at preflight: `522ea1894e9adaa7489baffd54537bad5207e1b5`,
  clean when inspected. Its standalone composed fixture pins IcyDB 0.259.6.
  At preflight the runner accepted only `host` and `participant`, the schema
  had zero entities and the report measured IcyDB 0.257.9. The new W1 source
  and artifact identities are recorded in the reconciliation above.
- Toko Miner HEAD: `d333cb8ece219526bd436dc0a0b2a72e6c86569b`, dirty and
  changing. Its manifest names Canic v0.110.27 and IcyDB v0.259.6. This is a
  read-only source reference, not a frozen application qualification baseline.

The upstream issue remains a Candidate for application attribution. The initial
preflight established no fresh measurements; the returned W1 artifacts now
establish controlled reachability increments only. Earlier whole-application
and empty-pair sizes retain their original scope and source identities.

## Validation and scope

W2's no-build investigation passes ten focused SQL-disabled core tests: four
page tests cover authenticated mixed projection, changing output envelopes,
test-only DISTINCT continuation and empty physical progress; six preparation
tests cover policy-specific reuse, optional inputs and construction admission.
All 29 local links in the four detailed documents resolve; the whitespace
check passes. Five documentation files change, approximately +85 net lines;
runtime implementation and state space are unchanged. Raw Wasm and IC
cycle/instruction deltas are unmeasured. No actor builds, Clippy, full suites,
sibling edits or network lifecycle actions ran. No Rust edits required formatting.
The existing lockfile/dependency and earlier slice changes are untouched.

W1's IcyDB reconciliation is documentation-only. All 83 selected artifact
hashes, twelve compiler/final metric sets, five adjacent deltas, five participant
Candid sidecars and eleven current source-input hashes passed independent
checks. Canic's build/lint and post-link mapping results are inspected returned
evidence, not new IcyDB executions. Runtime/lifecycle and IC costs remain
unmeasured for W1; no builds, tests or network lifecycle actions ran here.

C1 adds the nullable correction, direct tests
and an IC measurement fixture. Final focused validation: 18 core tests, one
integration test with 54 measured messages, and two core Clippy selections
passed. The SQL-only unit selection retains its pre-existing dead-code warnings.
Both measurement runs started a local PocketIC server and created/dropped three
disposable instances each. No dependency/version changes or sibling edits. Full repository
validation and publication remain user-owned. This work adds no production
runtime behavior axis, execution path, cache or state. It does correct the
nullable by-kind rejection behavior. Canic remains the composed fixture/build
owner; do not create a competing composition harness or edit the sibling.

R1 final focused validation: five facade unit tests, 16 core typed-adapter
tests, one native generated-driver read/write test and the catalogue
integration test passed; the latter checks eight
measured calls per paired actor. Focused Clippy passed. Initial fixture probes
hit existing pending-journal and public-page limits; the final fixture chunks
seed writes and follows real continuations without weakening those limits.
Each of four local probes started the existing PocketIC harness and created
two disposable instances. No downstream edits or dependency/version changes.

R3 validation: the extended catalogue test passed with eight label comparisons
and eight existing full-row observations; none failed or were ignored. A
too-long test-function Clippy warning was fixed by extracting its label
measurement helper; required `make clippy` and the focused feature lane passed.
One local PocketIC server was started through the existing harness, with two
disposable instances. Three test-support source files changed (about +149 net
lines); no production code or behavior axis added. Full suites remain user-owned.

D1 focused tests pass: six core exact-count tests with SQL disabled, ten with
all features, the facade Candid round-trip test, and all 27 diagnostic-registry
unit tests. Missing entity and index-prefix metadata is distinguished through
both dynamic and typed bindings; physical row/index reads remain zero. Existing
available-count, SQL aggregate, stale-binding and prefix-admission coverage also
passes. The CLI lookup recognizes E275 with Unsupported class, Query origin and
the metadata-unavailable explanation. Warning-denied Clippy passes for the four
affected packages with all targets/features; formatting and diff checks pass.
Raw Wasm, IC cycles and instruction deltas are unmeasured for D1. Its complexity
delta is 15 files, approximately +180 net lines including tests/docs; execution
shape stays neutral with one additional diagnostic identity. No network lifecycle
actions, version changes or sibling edits. The pre-existing `rand` lockfile update
is untouched. Full suites and release publication remain user-owned.
