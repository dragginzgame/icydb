# 0.216 Explicit Application Behavior And Durable Rule Ergonomics

Status: accepted; implementation active

Design cut: 2026-07-28

Accepted: 2026-07-31

Implementation order: after 0.215 closeout

Maintained baseline: `v0.215.7`

## Decision Summary

0.216 makes the application-behavior and reusable durable-rule surfaces
established by 0.213 easier to author, review, evolve, and diagnose. It does
not reopen their ownership.

The maintained separation remains:

1. a durable rule is explicit bounded source metadata that lowers into the
   accepted targeted-rule constraint owned by its persisted root and nominal
   target;
2. the accepted schema and existing accepted evaluator are the only database
   authority for that rule;
3. a validator is explicit application-only checking;
4. a normalizer is explicit application-only transformation; and
5. SQL, structural writes, recovery, integrity checking, and database
   admission never execute or infer Rust callbacks.

The current durable-rule declaration is mechanically precise but unnecessarily
loose:

~~~rust
ty(rule(
    name = "range",
    kind = "numeric_range_inclusive",
    args(0, 100)
))
~~~

It selects a closed operation through a string and places typed operands in a
positional heterogeneous argument list. Those degrees of freedom do not
represent real variability.

0.216 hard-cuts that spelling to a typed, locally identified declaration:

~~~rust
ty(rule(
    name = "range",
    numeric_range_inclusive(min = 0, max = 100)
))
~~~

The compiler validates the bounded local rule name and stores it directly as
the proposal-side `RuleSourceKey`. The pair `(TypeSourceKey, RuleSourceKey)`
identifies one rule declaration within the source model. Fragment lowering
then derives the root-scoped `ConstraintSourceKey` from the persisted root
field key, target type key, and local rule key through the frozen
domain-separated algorithm. Accepted catalog IDs remain the durable identity
after acceptance. The operation and operands are represented by one closed
typed enum. There is no string dispatch, positional `Args`, callback path, or
second evaluator.

The frozen version-1 rule vocabulary becomes:

- inclusive numeric minimum;
- inclusive numeric maximum;
- inclusive numeric range;
- inclusive text, blob, or collection length range; and
- exact integer or decimal multiple-of.

`MultipleOf` reuses the exact integer/decimal semantics already owned by
accepted checks, but it lowers as a new closed targeted-rule operation rather
than returning to the retired source-rule-as-check path. It does not add a
second runtime evaluator or float semantics. It extends the closed source and
accepted targeted-rule operation enums and is executed by the existing bounded
targeted-rule evaluator. A zero divisor, inexact decimal operand, unsupported
kind, or impossible conversion rejects during proposal construction.

0.216 also freezes explicit application behavior:

- normalization and validation remain separately callable;
- a named convenience operation may compose normalization followed by
  validation, but it is still application-only;
- normalizers run in declared traversal order;
- validators observe the value supplied to validation;
- retries and database recovery never rerun either operation;
- callback identity and diagnostics remain typed application concerns; and
- generated write adapters do not invoke application behavior implicitly.

Before changing that behavior surface, 0.216 freezes the trait authority of
generated application value types. Traits required by every generated value
type are emitted unconditionally by the compiler and are not author options.
An explicit attempt to add or remove one rejects during macro expansion.
Ergonomic, shape-derived, opt-in, and custom-override traits remain unchanged
until separately audited; the required-trait cut is not a general trait-list
redesign.

0.216 consumes the completed 0.213.39 nominal-target contract and the 0.213.40
one-name authoring contract. A rule on one named type is instantiated once for
each persisted root field whose accepted type graph reaches that type, and it
governs every finite runtime occurrence below that root, including records,
enums, collections, maps, tuples, repeated values, and recursive named-type
graphs. Top-level uses follow the same targeted-rule path.

0.216 changes only how authors spell and locally identify those rules. It does
not add expanded schema paths, per-route selectors, occurrence-specific
opt-outs, another traversal engine, or another accepted constraint kind.

The line is planned as eight substantive landing patches. It must not begin
until 0.213, 0.214, and 0.215 have their own accepted closeout boundaries.

### Non-Negotiable Invariants

1. Accepted schema snapshots remain the sole runtime authority for durable
   database rules.
2. Generated `EntityModel`, `IndexModel`, type nodes, and rule metadata are
   proposal inputs only; they are never runtime fallback authority.
3. Every durable rule lowers to one accepted targeted-rule constraint for each
   persisted root field whose accepted graph reaches the ruled nominal type.
4. The accepted targeted-rule compiler and evaluator remain the only database
   evaluator for durable rules.
5. No rule is inferred from a Rust callback body, path, function name, type
   name, local name, documentation, or argument convention.
6. The public durable-rule operation is a closed typed enum, not a string plus
   positional arguments.
7. A local rule name is the rule's current authored name within one type.
8. The proposal-side `RuleSourceKey` is the bounded local rule name. Source
   declaration identity is the pair `(TypeSourceKey, RuleSourceKey)`; the
   root-scoped constraint source key is derived from the persisted root field,
   target type, and rule keys through one domain-separated algorithm.
9. Editing a rule's operands or operation while retaining its local name is a
   semantic schema change, not removal and recreation.
10. Renaming a rule or containing type is removal plus addition in 0.216.
    Accepted-ID-preserving correspondence is deferred to the explicit
    per-entity migration contract designed for 0.218; 0.216 infers no rename.
11. Reordering declarations or moving modules does not change proposal
    identity; changing a type, field, relation, constraint, or rule name does.
12. Copying the same local name onto two different current type names does
    not create a collision.
13. Local names are nonempty, bounded, unique within their containing type, and
    validated before proposal lowering.
14. Accepted constraint identity remains derived from the persisted root field
    proposal key, target type proposal key, and rule proposal key.
15. A representable durable restriction is not also attached as a Rust
    application validator.
16. An application validator never claims SQL, structural-write, recovery,
    integrity, or database-wide enforcement.
17. A normalizer never becomes accepted schema, persisted behavior, a recovery
    callback, an implicit write hook, or a database rewrite language.
18. Generated conversion and write adapters do not implicitly normalize or
    validate.
19. A convenience operation that composes application behavior always
    normalizes before it validates and is named so the behavior is visible at
    the call site.
20. Direct validation validates the supplied value; it does not silently
    normalize a copy first.
21. Normalizer and validator failures preserve typed operation identity and
    complete bounded value paths without error-string classification.
22. Normalizer and validator execution order is deterministic and derived from
    the maintained generated traversal, not registration order at runtime.
23. Database retries, statement retries, marker replay, recovery, and
    integrity checking never execute application validators or normalizers.
24. `MultipleOf` is exact for supported integer and decimal kinds, rejects a
    zero divisor, and is never defined through floating-point remainder.
25. Rule literals are admitted once against the exact accepted field kind.
    Runtime evaluation performs no parsing, narrowing, or kind inference.
26. Unsupported rule/kind combinations, unresolved nominal targets, and
    unreachable root/target pairs reject before an accepted candidate can be
    published.
27. A reachable nominal rule applies to every finite occurrence below its
    persisted root. It never disappears, degrades to application validation,
    or applies only to typed writes.
28. Rule evolution uses existing catalog-native accepted-constraint
    reconciliation, activation, bounded proof, publication, and recovery
    ownership.
29. SQL DDL remains a frontend to catalog-native schema mutation. 0.216 does
    not add SQL syntax for source-rule declarations.
30. The authoring cut preserves the 0.213.39 target identity, traversal,
    diagnostic-path, activation, and recovery contracts and the 0.213.40
    current-name proposal-key contract: local rule name plus containing type
    identity at model scope, and root/target/rule derivation at constraint
    scope. New closed operation variants change the source and accepted
    targeted-rule codecs as the explicit current-form hard cut described
    below; row, cursor, journal, and marker formats do not change.
31. The old `name` plus string `kind` plus positional `args` rule grammar is
    removed in the
    same patch that introduces the typed grammar.
32. No deprecated grammar, compatibility parser, alias, wrapper, dual macro
    expansion, feature flag, or anti-resurrection test is retained.
33. Public documentation explains durable rules, validators, and normalizers
    as three different authorities and does not imply interchangeable
    enforcement.
34. The no-rule database write path performs no per-row source-rule work.
35. Raw non-gzipped Wasm bytes are the primary size evidence. Gzip is
    secondary context.
36. 0.216 does not introduce a generic policy, callback, plugin, annotation,
    reflection, or migration framework.
37. `Clone`, `Debug`, `Path`, `CandidType`, `Deserialize`, `Eq`, `PartialEq`,
    `NormalizeAuto`, `ValidateAuto`, and `Visitable` are required generated
    application-value traits. They are emitted without an author declaration,
    and explicit `traits(add(...))` or `traits(remove(...))` requests reject.
38. Required-trait resolution is separate from ergonomic and override
    resolution. The first patch does not remove, rename, or change `Default`,
    `From`, `NormalizeCustom`, `ValidateCustom`, collection, dereference,
    arithmetic, wrapper, or shape-derived trait behavior.

## Normative Contract

The durable-rule contract is:

> A durable rule is bounded compiler-authored metadata owned by one reusable
> source type. Each persisted root field that can reach the ruled nominal type
> instantiates the rule as one accepted targeted-rule constraint. That
> constraint governs every finite occurrence of the target below that root.
> Runtime enforcement, reconciliation, activation, recovery, integrity
> verification, and diagnostics consume that accepted constraint and never
> the source model.

The application-validation contract is:

> A validator is an explicitly invoked Rust application check. It may express
> behavior outside the accepted database language, but it has no database-wide
> enforcement claim and is never invoked by database admission.

The application-normalization contract is:

> A normalizer is an explicitly invoked Rust application transformation. It
> changes an authored application value before the caller submits that value.
> It is not accepted schema and is never repeated by retries, recovery, SQL, or
> structural mutation lanes.

The identity contract is:

> Each named type and durable rule has one current source name. The compiler
> uses the type key and local rule key as source declaration identity and
> derives each root-scoped constraint key from the root, target, and rule keys.
> Accepted catalog IDs own durable identity. Semantic edits under unchanged
> names reconcile in place; renames preserve identity only through explicit
> catalog-native migration rules.

The hard-cut contract is:

> The maintained authoring surface accepts the typed rule grammar only.
> Pre-0.216 source declarations are edited directly. IcyDB does not parse,
> translate, preserve, or test the retired grammar.

The generated-trait contract is:

> The model compiler owns traits required for every generated application
> value type. It emits them exactly once and rejects attempts to add or remove
> them. Author trait directives remain available only for behavior that is
> genuinely optional or overrideable for the applicable node.

## Context And Dependencies

0.216 builds on:

- [0.211 accepted-catalog
  constraints](../0.211-accepted-catalog-constraints/0.211-design.md) for
  stable accepted constraint identity, bounded `CheckExprV1`, exact
  `MultipleOf`, activation, mutation gating, and typed violations;
- [0.212 bounded resumable integrity
  checking](../0.212-bounded-resumable-integrity-check/0.212-design.md) for
  accepted-native integrity verification without application callbacks;
- [0.213 schema authority and application-model
  separation](../0.213-schema-authority-and-application-model-separation/0.213-design.md)
  for the standalone application model, current-name proposal keys, explicit
  validators and normalizers, fragment lowering, and single durable-rule
  authority;
- the [0.213 implementation
  status](../0.213-schema-authority-and-application-model-separation/0.213-status.md)
  for the current source-rule vocabulary and standalone source-model boundary;
- [0.213.39 nested durable constraint
  targets](../0.213-schema-authority-and-application-model-separation/0.213.39-nested-durable-constraint-targets.md)
  for the sole source/accepted targeted-rule representation, nominal target
  identity, bounded recursive traversal, concrete diagnostic paths, and
  mutation/activation/integrity/recovery convergence that 0.216 must reuse;
- [0.214 exact unsigned identity
  generation](../0.214-exact-unsigned-identity-generation/0.214-design.md) for
  the accepted generated-write policy that remains distinct from validation
  and normalization;
- [0.215 SQL structural coverage and range
  remediation](../0.215-sql-structural-coverage-and-range-remediation/0.215-design.md)
  for current structural-schema evidence at the point 0.216 begins;
- [WRITE_ADMISSION](../../contracts/WRITE_ADMISSION.md) for complete
  final-after-image admission;
- [ATOMICITY](../../contracts/ATOMICITY.md) and
  [DURABILITY](../../contracts/DURABILITY.md) for accepted schema transition
  and recovery ownership; and
- [PERSISTED_FORMAT_INVENTORY](../../contracts/PERSISTED_FORMAT_INVENTORY.md)
  for the required evidence when an implementation changes durable bytes.

The later [0.218 explicit versioned schema migration
design](../0.218-explicit-versioned-schema-migrations/0.218-design.md) owns
accepted-ID-preserving rename correspondence. It is not a dependency of 0.216;
until that line lands, a changed current name remains removal plus addition.

The [0.216 contract and inventory
freeze](0.216-contract-inventory.md) records the Patch 2 repository evidence:
all maintained rule declarations and application behaviors, the current and
target proposal shapes, exact identity vector, accepted codec decision,
invocation boundaries, and measured source/Wasm baseline.

This proposal does not amend an unfinished earlier line. If the maintained
0.213 through 0.215 implementation differs from the assumptions above, 0.216
must be revised before implementation rather than adding an adapter around
the discrepancy.

## Current State And Problem

0.213.39 established the correct ownership shape:

- reusable model types may carry explicit source-rule metadata;
- fragment lowering derives one root/target/rule constraint source identity;
- the proposal contains one targeted rule per reachable persisted-root and
  nominal-target pair;
- accepted-schema construction binds and compiles that targeted rule;
- all write lanes and integrity verification use the accepted evaluator;
- supported built-in numeric and length restrictions no longer retain
  equivalent Rust validators; and
- nested and recursive occurrences use one bounded accepted traversal with
  stable concrete diagnostic paths.

The remaining weakness is authoring shape, not runtime authority.

### Loose Operation Selection

The macro parser accepts a string such as
`"numeric_range_inclusive"` and translates it to a closed enum. The string is
not genuine extensibility. It delays typo and operand-shape failures, obscures
editor support, and makes generated examples harder to understand.

### Positional Untyped Operands

All source-rule operands currently travel through a general `Args` collection.
The rule node then checks argument count and numeric shape. The type system
already knows whether an operation needs `min`, `max`, or `divisor`; the
transport does not encode that invariant.

### Current Name, String Operation, And Positional Operands

The baseline authoring grammar already gives each rule one short current name.
Its remaining weakness is the string operation plus positional `Args`
transport. The typed grammar removes those two loose dimensions without
reintroducing a second stable identity in source declarations.

### Application Behavior Discoverability

`normalize()` and `validate()` are correctly explicit, but the public contract
must make their ordering and composition unambiguous. Users should not have to
guess whether validation normalizes, whether write adapters invoke either
operation, or whether a retry can run a normalizer twice.

### Required Traits Are Incorrectly Author-Configurable

The current generic trait builder places compiler-required traits in the same
`add` and `remove` mechanism as genuine author choices. A redundant explicit
addition may fail as an accidental duplicate, while removal can be accepted
and later overridden by node-specific generation. That makes source text an
unreliable description of the emitted Rust type.

0.216 separates required application-value traits from authored directives.
The compiler emits the frozen required set directly. Both explicit addition
and explicit removal reject with a macro diagnostic. This patch preserves the
existing emitted Rust implementations and does not classify convenience or
custom-hook traits as required.

### Incomplete Frozen Vocabulary

0.213 classified exact numeric/decimal multiple-of as accepted-constraint
behavior, and the accepted check evaluator already owns its exact arithmetic.
The current targeted-rule operation enum does not expose it, leaving a
representable reusable rule without the maintained durable authoring path.

### Nominal Target Semantics Must Stay Closed

0.213.39 made nested durable rules accepted-native by selecting one nominal
target below one persisted root. The accepted target is not an expanded route:
the same ruled type reused through several branches below one root remains one
constraint, and every finite occurrence is evaluated.

An ergonomics change must not accidentally reintroduce route identity,
generate one constraint per occurrence path, or let an author opt out of one
record member, collection position, or recursive branch. Those would change
cardinality, identity, evolution, and runtime semantics and require a separate
design.

## Authority Map

| Concern | Sole owner | Source-model input | Runtime consumers |
| --- | --- | --- | --- |
| Type identity | current named-type declaration | bounded `name`, defaulting to the Rust type name | fragment lowering and reconciliation input |
| Local rule identity | containing type declaration | bounded `name`, encoded directly as `RuleSourceKey` | type-scoped source-rule lookup |
| Source declaration identity | model compiler | `(TypeSourceKey, RuleSourceKey)` | reachable-rule deduplication and lowering |
| Rule operation and operands | typed `SourceRule` variant | typed macro form | fragment lowering |
| Root/target constraint identity | schema fragment lowering | domain-separated hash of persisted root field, target type, and local rule proposal keys | accepted source catalog |
| Accepted rule meaning | accepted constraint catalog | lowered targeted rule | admission, activation, recovery, integrity, introspection |
| Rule compilation | accepted targeted-rule compiler | accepted target and operation | compiled accepted-rule cache |
| Rule evaluation | accepted targeted-rule evaluator | compiled nominal traversal and operation | every maintained mutation ingress and integrity |
| Application validation | `icydb-model` generated traversal | explicit validator attachments | explicit application calls only |
| Application normalization | `icydb-model` generated mutable traversal | explicit normalizer attachments | explicit application calls only |
| Schema evolution | catalog-native schema mutation | changed proposal facts | activation/publication/recovery machinery |
| SQL mutation enforcement | accepted write admission | final after-image | SQL write frontend |
| Diagnostics | typed owner at each boundary | stable identities and paths | Rust, generated, SQL, and CLI projections |

There is no source-rule registry at database runtime. There is no callback
registry in accepted schema. The table describes one lowering chain, not
several synchronized authorities.

## Typed Durable-Rule Authoring

### Public Grammar

The proposed macro grammar is:

~~~rust
#[newtype(
    item(prim = "Nat8"),
    ty(
        rule(
            name = "range",
            numeric_range_inclusive(min = 10, max = 100)
        ),
        rule(
            name = "step",
            multiple_of(divisor = 5)
        )
    )
)]
pub struct Rating {}
~~~

Exactly one operation appears in each `rule(...)`. The parser rejects:

- a missing operation;
- multiple operations;
- unknown operation names;
- operands belonging to another operation;
- missing or repeated operands;
- unknown operands;
- an empty, malformed, oversized, or duplicate local name; and
- a literal that cannot be admitted exactly for the declared primitive.

The parser does not retain a catch-all `args(...)` field. The generated schema
node does not retain a string operation name.

### Source Representation

The target authoring representation is conceptually:

~~~rust
pub struct SourceRule {
    local_key: RuleSourceKey,
    operation: SourceRuleAuthoringOperation,
}

pub enum SourceRuleAuthoringOperation {
    LengthRangeInclusive { min: ExactNat, max: ExactNat },
    MultipleOf { divisor: ExactNumber },
    NumericMaximumInclusive { value: ExactNumber },
    NumericMinimumInclusive { value: ExactNumber },
    NumericRangeInclusive { min: ExactNumber, max: ExactNumber },
}
~~~

The exact implementation may use bounded literal nodes already owned by
`icydb-schema`, but it must preserve the invariant-bearing shape above. It
must not use `Args`, `Vec<Value>`, `AnyValue`, `serde_json::Value`, or another
general transport.

`RuleSourceKey` is constructed directly from the bounded current authoring
name. It is not public runtime state and does not enter accepted schema
separately from the derived root/target/rule constraint source key.

### Identity Derivation

One source declaration is identified within the proposal model as:

~~~text
(TypeSourceKey, RuleSourceKey)
~~~

`RuleSourceKey` is the validated local rule name itself; it is not a hash of
the type name and rule name. The root-scoped constraint key is derived as:

~~~text
ConstraintSourceKey =
    "rule:" + lowercase_hex(
        sha256(
            "icydb:constraint-source:targeted-field-rule:v1"
            || frame(FieldSourceKey)
            || frame(TypeSourceKey)
            || frame(RuleSourceKey)
        )
    )

frame(value) = u32_be(value.byte_length) || value.bytes
~~~

Each input is a canonical source-key string of at most 128 bytes. The output
is `rule:` followed by 64 lowercase hexadecimal digits, for 69 bytes total.
The frozen golden vector is:

~~~text
field = field/user/age
target = type/age
rule = rule/range
key = rule:9ec7c7a93d5c6ee1f23f0310ee6b58113454a1e04c2eb0f34f3748e2c5c2e260
~~~

Concatenation without the frozen length framing is forbidden.

The constraint derivation includes the persisted root field, current type,
and local rule names. It does not include:

- Rust module path;
- operation variant;
- operands;
- declaration ordinal;
- accepted catalog IDs; or
- generated token order.

Those exclusions are intentional. A semantic edit under the same names
addresses the existing proposed constraint identity and is routed through the
catalog-native transition owner in Patch 6. A rename requires a catalog-native
migration to retain accepted identity.

### Rule-Name Evolution

The authoring rules are:

- keep the local name when changing the rule's supported operands or operation
  as an intentional semantic evolution;
- accompany a local or containing-type rename with an explicit catalog-native
  rename migration when accepted identity must be preserved;
- use different local names for distinct restrictions on one type;
- never encode version numbers in a name merely because operands change; and
- never recover a removed name through an alias.

Without a rename migration, a changed rule name is a removal plus addition and
is subject to the same ownership and removal checks as other proposed schema
changes.

## Frozen Rule Vocabulary

### Numeric Minimum

~~~rust
rule(
    name = "nonnegative",
    numeric_minimum_inclusive(value = 0)
)
~~~

The operand must be exactly representable by the newtype's accepted numeric
kind. The rule lowers to the canonical accepted targeted minimum operation
applied to every selected nominal value. It does not introduce a special
minimum evaluator.

### Numeric Maximum

~~~rust
rule(
    name = "maximum",
    numeric_maximum_inclusive(value = 100)
)
~~~

The operand must be exactly representable by the newtype's accepted numeric
kind. The rule lowers to the canonical accepted targeted maximum operation
applied to every selected nominal value. It does not require an artificial
lower bound and does not introduce a special maximum evaluator.

### Numeric Range

~~~rust
rule(
    name = "range",
    numeric_range_inclusive(min = 10, max = 100)
)
~~~

Both operands must be exactly representable and `min <= max` under the exact
accepted kind semantics. Lowering uses the existing canonical accepted
targeted inclusive-range operation.

### Length Range

~~~rust
rule(
    name = "length",
    length_range_inclusive(min = 1, max = 64)
)
~~~

`min` and `max` are exact nonnegative cardinalities with `min <= max`.
Semantics remain those frozen by the accepted targeted-rule contract:

- Unicode scalar count for text;
- octet count for blobs; and
- logical cardinality for a directly eligible collection value.

The source compiler rejects a kind whose accepted targeted operation cannot
express the documented measure. It never substitutes encoded byte length.

### Multiple Of

~~~rust
rule(
    name = "step",
    multiple_of(divisor = 5)
)
~~~

`MultipleOf` is supported only for exact integer and fixed-scale decimal
accepted kinds already supported by the accepted numeric operation. The
divisor:

- must be nonzero;
- must be exactly admitted against the accepted numeric kind;
- uses the existing exact signed/unsigned/decimal semantics;
- does not use binary float remainder;
- does not round or normalize an authored value; and
- produces the same accepted targeted operation regardless of generated or
  manually authored proposal origin.

Float, cross-kind, row-dependent, callback-defined, or approximate
multiple-of semantics reject.

### Deliberately Excluded Rules

0.216 does not add durable regex, URL, MIME, phone, ISO, hash, case, locale,
authorization, external lookup, arbitrary predicate, or callback rules.
Those remain application validators unless a later design freezes a bounded
accepted database expression for them.

Enum membership remains an accepted enum/type fact where possible. Not-null
remains a structural constraint. Decimal scale remains an accepted field-kind
fact. Managed timestamps and generated identities remain write policies.
Normalizing trim, case conversion, and rounding remain application behavior.

## Placement And Reuse

A durable rule is reusable below every persisted root field whose accepted
nominal graph reaches the ruled type. The same rule governs direct uses and
every finite occurrence through records, enums, lists, sets, maps, tuples,
repeated values, newtypes, and recursive named-type graphs.

For example, `Rating` may be used by many entities and every reachable
persisted-root use receives its own accepted constraint identity derived from:

~~~text
persisted root field proposal key
    + Rating type proposal key
    + Rating rule proposal key
~~~

The reusable type does not create one global database constraint shared by all
entities, nor one constraint per route below a root. Fragment lowering emits
one source-bound targeted rule for the root/type/rule identity, and accepted
evaluation visits every finite occurrence. A violation reports the first
deterministic concrete path, but that path is evidence rather than constraint
identity.

0.216 may improve the typed spelling of the operation and local rule name. It
must not add route selectors, occurrence-specific opt-outs, expanded
schema-path identity, or a second nested traversal.

## Explicit Application Behavior

### Separate Operations

The maintained direct operations remain conceptually:

~~~rust
icydb_model::normalize(&mut value)?;
icydb_model::validate(&value)?;
~~~

Generated types may expose equivalent methods. A direct `validate()` call
never normalizes. A direct `normalize()` call never claims that validators
have passed.

### Named Composition

The application-surface patch selects one consuming convenience trait:

~~~rust
use icydb_model::NormalizeAndValidate as _;

let value = authored.normalize_and_validate()?;
~~~

`NormalizeAndValidate` is blanket-implemented for every sized `Visitable`
application value. It consumes and returns the same concrete value without a
clone. Its semantics are:

1. operate on an application-owned value;
2. run the complete generated normalizer traversal;
3. stop and return typed normalization errors if normalization fails;
4. validate the resulting value through the complete generated validator
   traversal; and
5. return the normalized value only when both stages succeed.

It does not:

- evaluate accepted database constraints;
- open or resolve a database binding;
- persist a value;
- retry callbacks;
- hide a write;
- run automatically from `insert_typed`, `save`, structural mutation, or SQL;
  or
- imply that application validation is database enforcement.

The composed method returns the existing `VisitorError`. Its typed
`ApplicationOperation` identifies whether normalization or validation failed;
every issue retains the exact callback class and concrete Rust callback/type
path. Callback identities use borrowed static type paths on successful
traversals, so merely preparing the diagnostic boundary does not allocate.

### Deterministic Traversal

Normalizers execute in the generated declaration and value-traversal order
frozen by `icydb-model`. Validators execute through the corresponding
read-only traversal. Map and set traversal must use their canonical generated
order rather than hash iteration.

The order is an application contract, not accepted schema. Reordering
normalizer declarations may change application output and must be visible in
source review. Validator ordering may affect diagnostic order but never
database acceptance.

### Retry Boundary

Application behavior executes only when application code explicitly requests
it. Once a value crosses into an IcyDB mutation:

- the mutation carries authored intent or a canonical value;
- accepted write policy constructs the final after-image;
- accepted constraints validate that after-image;
- a retry reuses or reconstructs database-owned mutation state according to
  the maintained mutation contract; and
- no application callback is invoked.

Normalizers therefore need not be made safe for database replay. Applications
remain responsible for avoiding accidental repeated explicit normalization if
a normalizer is not idempotent.

## Schema Evolution

Changing a durable rule while preserving its local name changes the proposed
accepted constraint meaning under one current proposal key.

The existing catalog-native owner decides whether the change:

- can be proved atomically within the direct bound;
- requires a bounded activation;
- conflicts with an existing activation;
- is unsupported for the affected accepted schema; or
- must reject before publication.

0.216 does not add a source-rule-specific scanner, job, staging store,
publication controller, or recovery path.

The catalog-native representation is one exact same-ID pair: the existing
accepted targeted rule retains its ID, name, origin, target, and current
operation, while a generated activation with the same ID/name/origin/target
carries the changed operation. Both operations gate new writes until proof
completes. Direct or durable promotion replaces the accepted operation under
that ID; abort removes only the candidate activation and retains the source
binding. Any other accepted/activation ID or name collision is corruption.

Future writes must never begin using changed rule semantics before the
accepted transition and any required gate are atomically authoritative.
Existing rows must never be declared valid without the proof required by the
accepted constraint design.

Changing or removing an application validator or normalizer has no accepted
schema, row-layout, recovery, integrity, or persisted-format effect.

## Diagnostics

Durable-rule authoring failures use typed source-model diagnostics. They
identify the type source, local name, operation, operand, and persisted-root or
nominal-target context needed to correct the declaration.

After lowering, database diagnostics use accepted constraint identity. They
must not leak the source macro's string spelling or depend on the generated
model being present.

Application behavior failures preserve:

- whether the failure came from normalization or validation;
- the declared callback/type identity;
- the bounded logical value path;
- typed child causes; and
- deterministic aggregation order.

They must not be parsed from prose to select behavior. A validator and an
accepted constraint that happen to describe similar concepts remain different
error domains because their authorities differ.

## Public And Generated Surface

0.216 may change:

- macro diagnostics for attempts to configure required generated traits;
- `icydb-model` source-rule authoring nodes;
- `icydb-model-macros` rule parsing and generated metadata;
- source and accepted targeted-rule operation enums, codecs, and
  fingerprints;
- generated application-only normalization/validation convenience methods;
- typed application behavior errors;
- model/base reusable type declarations; and
- public documentation and examples.

0.216 does not add:

- a database callback registration API;
- a public accepted-expression builder through the rule macro;
- a general runtime value type;
- a caller-authored accepted constraint ID;
- SQL source-rule DDL;
- runtime source-model loading;
- persisted callback identity;
- dynamic callback dispatch; or
- an application behavior hook in mutation admission.

Public types expose only variability that callers can legitimately choose.
Compiler-only local-name and operation nodes should remain compiler/model
surface rather than being re-exported through the top-level database prelude
unless a demonstrated source-authoring use requires them.

## Persistence And Hard Cut

0.216 preserves the 0.213.39 targeted-rule kind and target representation, but
it is not codec-neutral:

At the maintained N7 baseline, `ProposalContractVersion::CURRENT` is `1` and
the accepted schema snapshot is version 1 with contract profile `ICYT`.

- source rules continue to lower only to the existing targeted-rule
  representation;
- `NumericMaximumInclusive` and `MultipleOf` extend the closed source and
  accepted targeted-operation enums;
- the sole version-1 `SchemaProposal` contract hard-cuts its nested
  fragment/operation shape in place, with no version-2 wrapper or old-shape
  decoder;
- the accepted-schema codec moves directly to the version-1 `ICYU` contract
  profile (`49 43 59 55` in bytes);
- accepted targeted-operation fingerprint tags retain
  `LengthRangeInclusive = 1`, `NumericMinimumInclusive = 2`, and
  `NumericRangeInclusive = 3`, then append
  `NumericMaximumInclusive = 4` and `MultipleOf = 5`;
- the semantic fingerprint domain remains
  `icydb.accepted-schema.semantic.v1`; it already hashes the exact encoded
  accepted snapshot bytes, so the `ICYU` bytes and closed operation variants
  distinguish the new current form without a second fingerprint domain;
- rule local names are compiler input and do not persist as parallel runtime
  authority;
- the derived constraint identity occupies the existing root/target/rule
  constraint-source identity position; and
- rows, layouts, indexes, relations, journals, markers, cursors, and integrity
  progress do not change.

Patch 2 freezes `ICYU` as the exact replacement accepted-profile identity and
the operation fingerprint tags above. Patch 4 hard-cuts the version-1 proposal
shape and replaces `ICYT` atomically with the new operation variants, then
updates the persisted-format inventory. The retired proposal shape and `ICYT`
profile receive no dedicated decoder, translation, or fallback.

0.213.40 already changed rule proposal identity from a separately authored
source key to the current names: the local name is the `RuleSourceKey`, the
containing `TypeSourceKey` scopes it in the model, and the root/target/rule
triple derives the accepted constraint proposal key. 0.216 preserves that
current-name contract while hard-cutting the operation grammar and codec.
Existing development accepted schemas may still require recreation when the
closed operation representation changes. Before 1.0.0 the resolution is an
explicit hard cut:

- development stores may be recreated;
- no old-key lookup is retained;
- no alias maps old keys to new keys;
- no decoder rewrites old keys;
- no proposal emits both keys;
- no migration guesses correspondence from kind and operands; and
- no test preserves the old authoring or accepted identity.

The implementation release notes must state the recreation requirement
precisely if maintained development stores from the preceding release are
affected.

## Performance And Complexity Budget

The expected runtime database effect is neutral:

- no-rule writes execute no source-rule-specific work;
- existing ruled writes use the existing accepted targeted evaluator;
- the new maximum and multiple-of branches execute only for explicitly
  declared rules;
- accepted targeted rules remain compiled once per accepted fingerprint;
- application validators and normalizers run only on explicit application
  calls; and
- no source graph ships as database runtime authority.

The implementation must measure:

- representative no-rule and ruled write instructions;
- explicit normalize, validate, and composed-call instructions where the
  convenience operation lands;
- generated token and compile-time change for representative ruled types;
- raw non-gzipped Wasm bytes for the maintained comparison canister;
- gzip only as secondary context;
- host package size where typed rule nodes change it; and
- files touched, approximate line delta, and complexity direction per landing
  patch.

The primary complexity goal is a net reduction:

- delete string rule dispatch;
- delete positional source-rule argument validation;
- retain one bounded local rule key and one root/target/rule derivation;
- do not add a second evaluator;
- do not add an expanded or route-specific nested-path framework;
- do not add callback registration; and
- do not add compatibility machinery.

A convenience application API is rejected if it requires more state,
dispatch, cloning, or error translation than the two explicit operations it
would compose.

## Failure Model

Failures before accepted schema include:

- invalid or duplicate local rule name;
- malformed typed rule operands;
- unsupported rule/kind combination;
- inexact or out-of-range literal;
- inverted range;
- zero `MultipleOf` divisor;
- derived identity collision; and
- invalid model traversal or callback attachment.

Failures during schema evolution use the existing typed accepted-schema and
activation errors. Failures during mutation use the existing typed accepted
constraint violation. Application normalization and validation failures stay
in their application error domain.

No failure is converted to:

- silent rule omission;
- generated-only enforcement;
- `Unknown` accepted-check success caused by corrupt metadata;
- callback fallback;
- default or zero operands;
- generic boxed prose;
- panic or assertion in maintained production paths; or
- compatibility reconstruction.

## Validation Plan

### Required Trait Authority

- every generated application value type emits the complete frozen required
  trait set without an author declaration;
- explicit addition and removal of every required trait reject before token
  generation;
- required-trait diagnostics distinguish redundant addition from forbidden
  removal;
- current `Default`, `From`, `NormalizeCustom`, and `ValidateCustom`
  declarations retain their accepted behavior;
- list, set, map, newtype, enum, dereference, arithmetic, collection, wrapper,
  and other shape-derived trait sets and generated behavior remain equivalent;
  and
- schema-only and typed-adapter downstream packages compile without source
  changes.

### Rule Grammar And Identity

- compile-pass coverage for every typed rule operation;
- compile-fail coverage for missing, duplicate, unknown, and cross-operation
  operands;
- exact literal admission for every supported integer and decimal kind;
- zero-divisor and inverted-range rejection;
- local-name grammar, length, and per-type uniqueness;
- direct local-rule-key construction and the frozen domain-separated
  root/target/rule constraint-key vector;
- identity stability across module move, declaration reorder, and operand edit;
- identity difference across local-rule-name and type-name changes; and
- explicit rename-migration coverage preserving accepted IDs; and
- per-root constraint identity difference for repeated uses.

### Lowering And Accepted Semantics

- each operation lowers to the exact canonical accepted targeted operation;
- generated and directly constructed proposals produce identical accepted
  meaning;
- no application validator is emitted for a durable rule;
- accepted evaluation parity across typed, structural, SQL, batch, recovery,
  and integrity-relevant paths;
- exact integer and decimal `MultipleOf` boundaries;
- text/blob/collection length unit semantics;
- direct, nested, repeated, and recursive nominal-target resolution;
- unsupported kind/operation rejection;
- current source/accepted codec round trips and typed rejection of noncurrent
  profiles; and
- no source-model dependency after accepted runtime construction.

### Application Behavior

- explicit normalize-only behavior;
- explicit validate-only behavior;
- normalize-then-validate composition when retained;
- deterministic nested traversal and diagnostic ordering;
- typed path and callback identity preservation;
- no implicit behavior in generated conversion or write adapters;
- no behavior in SQL, structural writes, recovery, or integrity;
- no retry invocation; and
- package-shaped downstream examples using the real macro expansion.

### Evolution And Recovery

- semantic rule edit under stable identity;
- direct clean publication where bounded;
- activation where historical proof is required;
- failure and abort leave accepted authority unchanged;
- local-name replacement without a rename migration follows explicit
  remove/add ownership;
- recovery resumes accepted transition state without the source model;
- validator/normalizer edits have no schema fingerprint or persisted effect;
  and
- development stores with old rule identity fail or require recreation exactly
  as the hard cut documents.

### Structural Guards

- no source-rule string dispatch or positional `Args` remains;
- no runtime generated-model fallback;
- no application callback path enters accepted schema;
- no duplicate durable/application evaluator for maintained base types;
- no nested durable rule silently disappears;
- no compatibility parser, alias, or old grammar test remains;
- no public re-export exposes compiler-only internals without a current
  consumer; and
- no new persisted family is omitted from the format inventory.

### Focused Release Evidence

Each patch runs only the focused package, target, and named tests affected by
that patch. The human push workflow owns the full workspace suite. Closeout
evidence must additionally include repository architecture/layering guards,
generated downstream checks, warnings-denied focused Clippy, formatting,
raw-Wasm comparison, and diff hygiene.

### Closeout Audit Carry-Forward

The current-state cleanup audit established several boundaries that Patch 8
must preserve:

- the audited 52 helper proc-macro entry points are all reached by maintained
  generated output; they are implementation machinery, not currently dead
  exports;
- after Patches 3 through 7 change generated rule and application behavior,
  Patch 8 must repeat that construction-site audit and delete any helper whose
  final generated consumer disappeared rather than retaining a forwarding
  macro or an anti-resurrection test;
- diagnostics capture types, recorders, and hidden facade exports require a
  maintained report or runtime consumer; the three capture-only families found
  by the audit were removed in a separate cleanup, while live phase attribution
  remains outside this design's scope;
- `icydb_model::build::{BuildOptions, generate_with_options}` remains reachable
  through current configured actor generation and therefore is not a 0.216
  deletion target; its endpoint-selection responsibility is owned by the
  0.217 hard cut; and
- recursive value-storage list/map allocation is a materialization-performance
  concern owned by the streaming-materialization design, not durable-rule or
  application-behavior sediment.

These classifications prevent the closeout pass from treating reachability as
architectural approval or, conversely, deleting a live boundary merely because
its eventual replacement belongs to a later minor.

## Planned Landing Patches

The line is planned as eight reviewable patches. One implementation turn lands
one patch and then stops for handoff.

| Patch | Bounded outcome | Primary domains | Required focused evidence |
| --- | --- | --- | --- |
| 1. Required generated-trait authority | Freeze and enforce the traits required by every generated application value type; emit them unconditionally and reject explicit addition or removal without changing ergonomic, opt-in, override, or shape-derived traits | Schema / Model, Build / Canister | exhaustive resolver tests, representative macro diagnostics, schema-only and typed-adapter package compilation, generated-output parity |
| 2. Contract and inventory freeze | Audit every current 0.213.40 targeted rule, validator, normalizer, invocation point, proposal key, public example, and accepted lowering; freeze the exact typed grammar, version-1 proposal shape, accepted-profile identity, key vectors, and baseline size/cost evidence without changing runtime semantics | Design / Docs, Schema / Model, Build / Canister | complete classification ledger, current grammar/codec fixtures, callback invocation map, raw-Wasm and source baseline |
| 3. Typed rule grammar and key preservation | Replace string `kind` and positional `args` with locally named typed rule operations while preserving the 0.213.40 local `RuleSourceKey`, type-scoped declaration identity, and root/target/rule constraint-key derivation; hard-cut the old grammar in the same patch | Schema / Model, Build / Canister | macro pass/fail matrix, golden key vectors, reorder/evolution tests, old callers updated directly |
| 4. Exact rule lowering and codec cut | Lower the typed minimum, maximum, range, length, and `MultipleOf` variants into canonical accepted targeted operations through one model-to-fragment owner; hard-cut the sole version-1 proposal shape and accepted schema profile and delete generic operand reconstruction | Schema / Model, Other Core, Persisted Formats | exact-kind boundary tests, targeted-operation/codec/fingerprint parity, direct/nested typed/structural/SQL write parity, unsupported-kind and noncurrent-profile failures |
| 5. Explicit application behavior | Freeze normalize, validate, and optional normalize-then-validate application APIs, deterministic traversal, and typed diagnostics; prove write adapters remain callback-free | Schema / Model, Build / Canister | explicit operation tests, nested traversal, error identity/path tests, generated package fixture, no-implicit-invocation guards |
| 6. Evolution and accepted-authority convergence | Exercise source-rule semantic edits and identity replacements through catalog-native reconciliation, activation, publication, admission, and recovery; remove any source-specific parallel path exposed by the audit | Schema / Model, Schema Mutation, Commit / Recovery | direct/activation/abort/recovery tests, stable accepted identity, no source model at runtime, validator/normalizer edits remain application-only |
| 7. Consumer hard cut and documentation | Convert all maintained base types, examples, fixtures, canisters, and downstream package tests to the current grammar and application behavior; document the three authorities and development-store recreation boundary | Build / Canister, Integration Tests, Design / Docs | packaged downstream compilation, generated-source checks, public examples, obsolete spelling and callback absence searches |
| 8. Closeout simplification and evidence | Perform the second semantic hard-cut scan, repeat the generated-helper construction-site audit, delete exposed sediment and capture-only diagnostics with no maintained consumer, verify public surface and persistence inventory, and record focused cost, raw-Wasm, and complexity evidence | Cross-cutting closeout | focused affected tests/Clippy, helper/export reachability evidence, architecture guards, raw-Wasm delta, no-rule/rule/application cost comparison, file/line/owner delta |

Patch 2 may amend this accepted document when repository evidence disproves an
assumption. It may not implement a new architecture implicitly. Patches 3
through 8 begin only after the amended contract remains accepted.

## Non-Goals

0.216 does not:

- infer durable behavior from Rust;
- persist Rust callback paths;
- execute callbacks from IcyDB;
- add accepted normalizers, triggers, rewrites, or computed columns;
- add arbitrary custom accepted predicates;
- make application validators authoritative for SQL or structural writes;
- automatically invoke application behavior from write adapters;
- add expanded or route-specific accepted field-path rules;
- add regex or locale semantics;
- add floating-point `MultipleOf`;
- merge not-null, enum membership, field kind, generated values, managed
  timestamps, or relation rules into the source-rule enum;
- expose accepted constraint IDs for authors to allocate;
- add compatibility for the retired source-rule grammar or identity;
- migrate old development stores automatically;
- add a general rule registry, provider, plugin, callback, or policy
  framework;
- redesign accepted constraint activation;
- redesign integrity checking;
- change query, cursor, index, relation, or row-layout semantics; or
- start 0.216 implementation before 0.215 closeout.

## Documentation Obligations

Implementation must update:

- the `icydb-model` public README with one concise comparison of durable rules,
  validators, and normalizers;
- macro documentation with the complete typed grammar;
- application examples showing explicit normalization and validation;
- accepted-schema documentation showing that source rules disappear into
  accepted targeted constraints;
- write-admission documentation confirming callbacks are never invoked;
- persisted-format inventory with the exact current version-1 proposal shape
  and new accepted-schema targeted-operation profile identity;
- migration/release notes with the old grammar and development-store hard cut;
  and
- the 0.216 status tracker with patch boundaries, focused validation,
  raw-Wasm, cost, and complexity evidence.

Documentation must not call all three concepts “validation” without naming
their authority. It must not imply that `validate()` checks accepted database
constraints or that a durable rule executes in generated Rust.

## Closeout Checklist

- [ ] Every current durable rule, validator, and normalizer is classified.
- [ ] The public rule grammar is typed and contains no string operation or
      positional operand transport.
- [ ] `RuleSourceKey` is the bounded local name, source declaration identity is
      `(TypeSourceKey, RuleSourceKey)`, and root/target/rule constraint keys
      match the frozen domain-separated vector.
- [ ] Rule identity is stable across non-name semantic and declaration edits.
- [ ] Name changes remain removal plus addition; no inferred rename, alias, or
      premature migration owner is added before 0.218.
- [ ] Each supported operation lowers to one canonical accepted targeted
      operation.
- [ ] Exact `MultipleOf` is available for supported integer and decimal kinds.
- [ ] Zero, inexact, float, and unsupported divisors reject before acceptance.
- [ ] Every persisted root/type/rule use receives one accepted constraint
      identity.
- [ ] Direct, nested, repeated, and recursive occurrences use the existing
      bounded targeted evaluator and typed path diagnostics.
- [ ] No rule silently becomes an application validator.
- [ ] No maintained durable restriction retains a duplicate Rust validator.
- [ ] Normalization and validation are explicit, separate operations.
- [ ] Any composed convenience API visibly normalizes before validation and
      remains application-only.
- [ ] Generated database write adapters invoke neither operation implicitly.
- [ ] SQL, structural writes, retries, recovery, and integrity invoke no
      application callback.
- [ ] Schema evolution uses existing catalog-native accepted-constraint
      activation and recovery.
- [ ] The accepted evaluator remains the only database evaluator.
- [ ] No generated model or source rule survives as runtime fallback
      authority.
- [ ] No old grammar, alias, compatibility parser, dual expansion, feature
      flag, or anti-resurrection test remains.
- [ ] Public/compiler-only types have the narrowest justified visibility.
- [ ] Accepted-schema, row, marker, journal, cursor, and integrity format
      impact is explicitly proven and documented.
- [ ] Exactly one current version-1 proposal shape and accepted-schema
      targeted-operation profile remain, with no decoder or translation for
      their predecessors and no version-2 proposal wrapper.
- [ ] Development-store recreation requirements are explicit where identities
      changed.
- [ ] No-rule write cost remains neutral.
- [ ] Rule and application-operation costs are measured through maintained
      paths.
- [ ] Raw non-gzipped Wasm, package size, files, lines, owners, and complexity
      direction are reported.
- [ ] Public documentation clearly distinguishes all three authorities.
- [ ] Required generated traits are emitted without author configuration, and
      explicit addition or removal rejects.
- [ ] Ergonomic, opt-in, override, and shape-derived traits retain their
      separately owned behavior after the required-trait cut.
- [ ] All eight patches have focused passing evidence.
- [ ] The human-owned full push suite is reported separately and is not
      represented as agent-run evidence.

## Acceptance Decision

This design is accepted and active after the completed 0.215 closeout.

Implementation remains bounded to the required generated-trait authority,
typed grammar, derived identity, exact `MultipleOf`, explicit application
behavior, and preservation of the completed nominal-target contract. It does
not reopen the 0.213 decision that accepted schema owns database semantics and
Rust validators/normalizers are application behavior.
