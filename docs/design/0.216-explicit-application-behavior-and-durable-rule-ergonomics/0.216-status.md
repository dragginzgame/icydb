# IcyDB 0.216 Implementation Status

Status: original Patches 1 through 8 and generated trait-policy Landings A
through I complete and final-closeout audited for proposed 0.216.5. The
closeout records one cross-line no-rule mutation-cost follow-up.

Design:
[0.216-design.md](0.216-design.md)

Generated trait-policy addendum:
[0.216-generated-trait-policy-addendum.md](0.216-generated-trait-policy-addendum.md)

Contract inventory:
[0.216-contract-inventory.md](0.216-contract-inventory.md)

Closeout audit:
[0.216-closeout-audit.md](0.216-closeout-audit.md)

Accepted: 2026-07-31

## Baseline

- Release baseline: `v0.215.7`
- Commit: `7b8d1153c45f5de8eafe47a574b2fea802eefe7e`
- Branch: `main`
- Worktree at kickoff: clean
- Declared workspace package version: `0.215.7`

No Cargo package version is changed by implementation slices. Release tooling
owns version bumps.

The removal of `icydb_model::build_with_options!` and its adjacent Unreleased
note appeared concurrently after kickoff. They are separate work and are not
part of Patch 1.

The repository cleanup immediately after Patch 1 is also a separate handoff,
not Patch 2. It removed the unconsumed structural-read, row-check, and scalar
lane capture families, their hidden facade exports, and their per-row recorder
plumbing. The direct/kernel phase-attribution path consumed by current SQL
diagnostics remains intact. The code portion is approximately `+35/-802` lines
across 19 files; minimal, query-only, diagnostics, SQL/facade, focused reader
and executor tests, and warnings-denied focused Clippy all pass. No persisted,
query, mutation, or response behavior changed.

The same audit confirmed that the 52 internal helper proc-macro entry points
are currently reached by generated output. Their post-0.216 visibility and
reachability re-audit is assigned to Patch 8. Live configured-actor generation
options remain assigned to 0.217, and recursive value materialization remains
assigned to the streaming-materialization design.

## Landing Plan

| Patch | Outcome | Status |
| --- | --- | --- |
| 1. Required generated-trait authority | Emit the frozen required application-value traits unconditionally and reject explicit addition or removal without changing any other trait behavior | Complete |
| 2. Contract and inventory freeze | Freeze the exact durable-rule, validator, normalizer, identity, codec, invocation, and baseline evidence before semantic implementation | Complete |
| 3. Typed rule grammar and key preservation | Replace string operations and positional operands while preserving local rule keys and root/target/rule constraint-key derivation | Complete |
| 4. Exact rule lowering and codec cut | Lower the closed rule vocabulary through one owner and hard-cut the sole current proposal and accepted profiles | Complete |
| 5. Explicit application behavior | Freeze explicit normalize, validate, and optional composed application APIs without database callbacks | Complete |
| 6. Evolution and accepted-authority convergence | Prove catalog-native rule evolution, activation, publication, admission, and recovery | Complete |
| 7. Consumer hard cut and documentation | Convert maintained consumers and document durable rules, validators, normalizers, and recreation boundaries | Complete |
| 8. Closeout simplification and evidence | Remove sediment and close the line with focused correctness, cost, raw-Wasm, and complexity evidence | Complete |

These eight patches are the accepted 0.216 landing budget. One implementation
turn completes one patch and stops for review.

## Patch 1 — Required Generated-Trait Authority

Status: complete.

The required set for every generated application value type is:

- `Clone`;
- `Debug`;
- `Path`;
- `CandidType`;
- `Deserialize`;
- `Eq`;
- `PartialEq`;
- `NormalizeAuto`;
- `ValidateAuto`; and
- `Visitable`.

The compiler already emits this set by default. Patch 1 makes that ownership
explicit and rejects both redundant addition and forbidden removal before
token generation. It does not change `Default`, `From`, `NormalizeCustom`,
`ValidateCustom`, collection, dereference, arithmetic, wrapper, enum, or other
shape-derived behavior.

### Implementation

- The application-value resolver now begins from one compiler-owned required
  and default set before applying the remaining configurable directives.
- Entity, enum, list, map, newtype, record, set, and tuple generation all use
  that resolver; the redundant entity-specific `CandidType` repair is gone.
- Explicit `add` and `remove` directives for every frozen required trait fail
  before token generation with action-specific diagnostics.
- `Default`, `From`, `NormalizeCustom`, `ValidateCustom`, and all node-specific
  ergonomic and shape-derived traits retain their previous ownership.
- Compile-fail fixtures exercise representative redundant-add and
  forbidden-remove diagnostics through the public macros.

### Evidence

- `icydb-model-macros`: 128 tests passed.
- `icydb-model`: 121 unit tests and one compile-test harness passed; both UI
  fixtures matched their accepted diagnostics.
- Schema-only, typed-adapter, and maintained schema fixture packages checked
  successfully without source changes.
- Warnings-denied Clippy passed for all targets in `icydb-model-macros` and
  `icydb-model`.
- Required-trait searches found no maintained explicit directives outside the
  intentional compile-fail fixtures and no remaining `with_type_traits` path.
- Formatting and diff-hygiene checks passed.

### Scope And Complexity

- Nineteen files participate in this slice, including five new compile-test
  and status files; the concurrent `build_with_options!` removal is excluded.
- The slice is approximately `+360/-95` lines, with most additions in the
  accepted design, tracker, and diagnostic coverage.
- Generated runtime behavior and persisted/public formats are unchanged.
  Resolver complexity stays neutral: one implicit merge-and-repair path is
  replaced by an explicit application-type authority and validation boundary.
- Raw Wasm and instruction measurements are not applicable because this slice
  changes procedural-macro validation while preserving emitted trait behavior.

## Patch 2 — Contract And Inventory Freeze

Status: complete.

The repository audit corrected one design assumption before semantic work:
`RuleSourceKey` is the bounded local rule name, not a hash of the containing
type and rule names. Source declaration identity is
`(TypeSourceKey, RuleSourceKey)`, while each accepted constraint proposal key
is the frozen domain-separated hash of the persisted root field, target type,
and local rule keys. The accepted design now matches that simpler maintained
architecture.

### Frozen Contract

- The target grammar has five closed typed forms: inclusive length range,
  numeric minimum, numeric maximum, numeric range, and exact integer/decimal
  multiple-of.
- One rule contains exactly one operation with closed named operands. The
  string `kind` and positional `Args` transport disappear together in Patch 3.
- All 15 maintained declarations, 19 normalizer definitions, 37 validator
  definitions, 15 maintained normalizer attachments, 14 maintained validator
  attachments, and five intentional custom validator hooks are classified in
  the inventory.
- The callback map freezes one explicit application traversal boundary and one
  accepted database evaluator; database writes, retries, recovery, replay,
  derived work, and integrity invoke no application callback.
- `ProposalContractVersion::CURRENT` remains 1. Patch 4 hard-cuts its nested
  operation shape in place and changes the accepted version-1 snapshot profile
  from `ICYT` to frozen `ICYU`, with no old decoder or version-2 wrapper.
- Existing targeted-operation fingerprint tags 1 through 3 stay fixed;
  maximum and multiple-of append tags 4 and 5. The accepted semantic
  fingerprint domain remains unchanged because it consumes the exact snapshot
  bytes.
- Same-identity semantic rule edits are assigned to the existing
  catalog-native transition owner in Patch 6. The audit found that the current
  path otherwise rejects such an edit; 0.216 must not describe that transition
  as already implemented.

### Evidence

- The current source-key golden vector and version-1 proposal codec tests
  passed.
- The maintained built-in durable-rule/application-validator ownership test
  passed.
- The current accepted targeted-rule snapshot round-trip test passed.
- A fresh maintained SQL performance Wasm build passed its focused build test.
  The raw artifact is 5,302,780 bytes with SHA-256
  `1f02abeb9373dcc079593e0187cb6d76f9b4659805018aeab8e8a7a16cbb15b0`.
- The 16 audited source-rule, schema, lowering, evaluator, and visitor owner
  files total 15,873 source lines.
- Markdown links and diff hygiene pass.

### Scope And Complexity

- Patch 2 changes only the accepted 0.216 design, status tracker, and new
  contract/inventory ledger. Concurrent runtime cleanup remains separate.
- The Patch 2 documentation delta is approximately `+460/-20` lines across
  those three files; the 339-line inventory is the dominant addition.
- Runtime, generated output, public API, proposal bytes, accepted snapshot
  bytes, and persisted formats are unchanged.
- Complexity stays neutral. The patch replaces one incorrect design assumption
  with the already maintained local-key plus root/target/rule identity model
  and freezes later implementation boundaries without adding an owner.
- The published 0.213.39/N7 rule-cost figures remain historical context. There
  is no maintained normalize/validate/composed instruction cohort yet; Patch 5
  owns that measurement when it settles the application API.

## Patch 3 — Typed Rule Grammar And Key Preservation

Status: complete.

### Implementation

- `rule(...)` now requires exactly one of the five frozen typed operations:
  length range, numeric minimum, numeric maximum, numeric range, or
  integer/decimal multiple-of.
- Each operation owns its named operands. Missing, repeated, unknown, and
  cross-operation operands reject during macro parsing; invalid, empty, or
  duplicate local rule names reject at the same boundary.
- The generated source model stores one `SourceRuleAuthoringOperation` rather
  than `SourceRuleKind` plus general `Args`. Direct model construction retains
  the same shape checks during schema validation.
- All 15 maintained base declarations use the typed grammar. No maintained
  Rust caller retains the string `kind` or positional rule-argument form.
- Existing minimum, range, and length rules continue through the same
  fragment-lowering owner and produce the same `RuleSourceKey` and
  root/target/rule `ConstraintSourceKey` values.
- Maximum and multiple-of now have closed source-model variants and compile as
  declarations. Their target-bound exact lowering remains deliberately
  unavailable until Patch 4 atomically adds the accepted operation variants
  and codec/profile hard cut.
- The version-1 proposal shape, accepted `ICYT` snapshot, accepted evaluator,
  runtime behavior, and persisted formats are unchanged in this patch.

### Evidence

- `icydb-model-macros`: 131 tests passed, including the five-operation parser
  matrix and malformed-operation/name coverage.
- `icydb-model`: 121 unit tests and both compile-test harnesses passed. The new
  downstream fixture compiles all five forms and freezes missing-operand and
  multiple-operation diagnostics.
- The standalone schema-only package passed its fragment/actor test, proving
  current `Degrees` rules retain their exact accepted proposal identity and
  lowering without the full IcyDB facade.
- Warnings-denied Clippy passed for `icydb-model-macros`, `icydb-model`, and the
  schema-only package across all targets.
- Formatting, relative Markdown links, obsolete-spelling searches, and diff
  hygiene pass.

### Scope And Complexity

- Patch 3 touches 17 files: the macro/model/lowering owners, four maintained
  base declaration files, five compile fixtures, the compile harness,
  changelog, inventory correction, and this tracker.
- The patch is approximately `+540/-75` lines. Most additions are the explicit
  operation-specific parser/model variants and their diagnostic matrix.
- Production source shape is more complex by line count at this boundary, but
  narrower in authority: one typed enum replaces string dispatch plus a
  heterogeneous argument transport, with no additional runtime evaluator.
  Patch 4 owns deletion of the remaining generic operand reconstruction as it
  completes exact lowering.
- Raw Wasm and mutation-instruction measurements are unchanged/not applicable:
  emitted accepted operations and runtime evaluation are identical for every
  maintained rule, while the two new source-only variants do not yet lower.

## Patch 4 — Exact Rule Lowering And Codec Cut

Status: complete.

### Implementation

- All five typed operations now lower through the single model-to-fragment
  owner and bind to closed accepted operations. Numeric maximum uses the
  existing exact comparison path; integer and fixed-scale decimal
  `MultipleOf` use one exact remainder evaluator and reject floats and zero.
- Rule operands use a rule-specific exact number node. Unsuffixed decimal text
  remains textual until the declared primitive is resolved, so rule lowering
  neither passes decimal operands through binary floats nor rounds values to
  the target scale. Generated and directly constructed proposals apply the
  same exact target-kind admission.
- Accepted integer multiple-of covers every signed and unsigned width plus
  bounded big integers. The evaluator also handles the signed minimum divided
  by `-1` without primitive remainder overflow.
- The version-1 proposal operation enum now contains maximum and multiple-of
  directly. `ProposalContractVersion::CURRENT` remains 1; its current digest
  vector changes with that hard-cut nested shape and no old decoder exists.
- The accepted version-1 snapshot profile is now `ICYU`. Maximum and
  multiple-of use activation-fingerprint tags 4 and 5; the existing semantic
  fingerprint domain remains unchanged.
- Constraint description retains its existing public shape and reports the
  two new operation semantics. Rows, keys, indexes, relations, journals,
  markers, cursors, integrity progress, and public response types are
  unchanged.

### Evidence

- `icydb-schema`: 70 unit tests passed, including the new current proposal
  digest, exact decimal target admission, zero/float rejection, and operation
  round trips.
- `icydb-model`: 124 unit tests passed. The focused durable-rule compile
  harness passed all five public forms and its malformed grammar fixtures.
- `icydb-core`: the 11-test targeted-rule cohort passed across lowering,
  accepted compilation, nested traversal, codec, fingerprint, diagnostics,
  and dynamic/typed/SQL/default/batch mutation ingress. Two additional exact
  arithmetic tests cover every runtime integer representation, decimal,
  signed overflow boundaries, zero, and float exclusion.
- The current operation wire round-trips maximum and multiple-of; the generic
  noncurrent-profile fixture still fails closed.
- Formatting and focused all-feature/no-default-feature checks pass.

### Scope And Complexity

- Patch 4 touches 19 files across the schema proposal, derive/model lowering,
  accepted catalog/evaluator/codec, focused tests, persisted-format inventory,
  changelog, and this tracker.
- The approximate Patch 4 delta is `+900/-100` lines, dominated by exact-kind,
  evaluator-boundary, cross-frontend, codec, and corruption tests. The precise
  mechanical delta is not separable from Patches 1 through 3 because the
  shared worktree has no intermediate commits.
- The slice adds two bounded closed operations and one exact authoring literal
  boundary. Runtime branching grows only at the accepted targeted evaluator;
  frontend count and callback ownership remain unchanged, so architectural
  complexity stays neutral while the old generic operand reconstruction is
  removed from durable-rule lowering.
- The only persisted-format change is the deliberate version-1 `ICYU`
  accepted snapshot/profile hard cut. Development stores encoded with `ICYT`
  must be recreated.
- The current mixed worktree builds a 5,321,640-byte raw SQL performance Wasm
  with SHA-256
  `ad4ba3c2d5ca3454b69104e816e41ca37117cae4edad23839d8458d5adaae7a0`.
  That is 18,860 bytes, or 0.356%, above the frozen Patch 2 artifact. Because
  unrelated concurrent cleanup is present, this is current-worktree size
  evidence rather than an isolated attribution of every byte to Patch 4.

## Patch 5 — Explicit Application Behavior

Status: complete.

### Implementation

- The existing `icydb_model::normalize(&mut value)` and
  `icydb_model::validate(&value)` operations remain separate and explicit.
  Direct validation still observes the supplied value and never normalizes a
  copy first.
- `NormalizeAndValidate` is blanket-implemented for every sized `Visitable`.
  Its consuming `normalize_and_validate()` method normalizes first, stops on a
  normalization error, validates only the normalized value, and returns the
  same concrete value without cloning.
- `VisitorError` now carries a typed `ApplicationOperation`. Every issue
  emitted through a top-level application traversal carries a typed callback
  class plus the concrete static Rust callback/type path, alongside the
  existing logical value path and ordered child messages. The former
  `VisitorError`-to-`VisitorIssues` conversion is removed so an error cannot
  silently discard its operation identity.
- Declared normalizer and validator attachments override the surrounding
  generated auto-hook identity through nested callback contexts. Custom hooks
  retain the concrete application-value type identity. Successful traversals
  allocate no callback-path string.
- Application diagnostics are Rust application state, not a persisted or wire
  contract. Their former unused `Deserialize` surface is removed rather than
  accepting forged dynamic strings as static callback identity.
- Generated typed adapters still contain no normalize, validate, or composed
  call. SQL, structural writes, mutation retries, recovery, replay, derived
  work, and integrity retain no application callback path.

### Evidence

- `icydb-model`: 129 unit tests passed, including separate operations,
  consuming composition, normalization short-circuiting, validation-stage
  identity, preorder nested traversal, callback class/type identity, indexed
  paths, and deterministic auto/custom ordering.
- `icydb-model-macros`: 131 unit tests passed. Generated write-adapter token
  coverage rejects any normalize, validate, or composed invocation.
- The renamed-dependency typed-adapter package passed three tests using real
  generated base types. It proves normalization does not occur during direct
  validation, composition returns normalized values, and generated
  normalizer/validator failures retain their exact callback identities.
- Warnings-denied Clippy passed for all targets and features in
  `icydb-model-macros`, `icydb-model`, the renamed-dependency fixture, and the
  SQL probe canister. The integration target itself remains blocked in the
  concurrent runtime-value cleanup by removed enum/kind re-exports and unused
  map canonicalization helpers; Patch 5 does not alter those files.
- The focused PocketIC instruction probe passed over 256 calls per surface:
  normalize used 713,987 instructions (about 2,789 per call), validate used
  724,207 (about 2,829 per call), and composed normalize-then-validate used
  1,297,903 (about 5,070 per call).
- The Patch 5 canonical SQL performance Wasm snapshot is 5,321,640 raw bytes
  with SHA-256
  `ad4ba3c2d5ca3454b69104e816e41ca37117cae4edad23839d8458d5adaae7a0`.
  It exactly matches Patch 4, proving the application API is linker-eliminated
  when it has no canister consumer. Later concurrent runtime-value cleanup is
  outside that snapshot; Patch 8 owns the final mixed-worktree measurement.
- Focused absence searches found no application entry point or typed callback
  diagnostic in core database execution, facade write adapters, generated
  named-value adapters, or generated entity write adapters.

### Scope And Complexity

- Patch 5 touches 20 files across the model visitor/application boundary,
  generated callback attribution, adapter guards, one downstream package
  fixture, one instruction-probe canister boundary, changelog, design, and
  this tracker.
- The approximate slice is `+790/-75` lines. Most additions are focused
  operation, traversal, identity, downstream, and instruction evidence.
- Runtime database complexity is unchanged: there is no callback registry,
  persisted callback identity, database adapter, retry hook, or second
  evaluator. Application complexity grows by one blanket consuming method and
  one typed diagnostic scope, while success-path callback identity remains
  allocation-free.
- Persisted schema, rows, indexes, relations, markers, journals, cursors, and
  public database response formats are unchanged. The only public hard cut is
  the application visitor error surface; Patch 5 adds typed operation/callback
  identity and removes its unused deserialization contract.

## Patch 6 — Evolution And Accepted-Authority Convergence

Status: complete.

### Implementation

- A targeted-rule operation or operand edit under one existing source key now
  stages one catalog-native activation under the accepted rule's existing ID,
  name, origin, and target. The non-reusing constraint allocator does not
  advance.
- The prior accepted operation remains present and authoritative while the
  paired activation evaluates the candidate operation as an additional
  new-write gate. The exact accepted/activation collision is valid only for a
  generated targeted rule with a different operation; every other ID or name
  collision remains invalid.
- Direct bounded proof replaces the accepted operation immediately. Historical
  proof uses the existing validation job, marker publication, continuation,
  finding, promotion, and recovery owners. Promotion removes the activation
  and replaces the accepted operation without changing its identity.
- Continuation now distinguishes a completed accepted rule from a paired old
  accepted rule plus live candidate. It cannot finalize a semantic edit merely
  because the old accepted owner still exists.
- Abort removes only the candidate activation for a semantic edit and retains
  the old accepted operation and source binding. Abort of a newly added rule
  still removes its binding and permanently retires its reserved ID.
- A local rule-name replacement remains an explicit removal plus fresh
  addition. No inferred rename, alias, source-specific scanner, staging store,
  publication path, or recovery path was added.
- Constraint introspection orders the validated operation before its same-ID
  candidate activation. This changes only pending-edit projection cardinality;
  the public response shape is unchanged.
- Validator and normalizer attachment edits lower to an identical database
  fragment. They therefore change neither accepted fingerprints nor persisted
  state and remain application-only.

### Evidence

- Ten targeted-rule core tests passed together, covering accepted lowering,
  stable-ID semantic edits, candidate new-write admission, direct promotion,
  explicit remove/add naming, typed diagnostics, codec operations, frontend
  convergence, and bounded evaluator limits.
- Eight schema-application tests passed together. The semantic-edit lifecycle
  proves direct empty-domain publication, staged accepted publication, guarded
  recovery, durable job start, bounded continuation, promotion, stable source
  identity, and terminal accepted state without a generated source model.
- Focused abort coverage passed for both same-ID semantic edits and newly added
  generated checks. The former retains accepted authority and binding; the
  latter retains its non-reuse behavior.
- The current accepted snapshot codec round-trips the paired same-ID
  accepted/activation representation, and transition policy accepts only its
  exact allocator-neutral generated activation shape.
- Five model fragment tests passed, including the proof that changing only
  validator and normalizer attachments produces the same database fragment.
- `icydb-core` compiled without default features. Warnings-denied production
  Clippy passed both with all features and without defaults; warnings-denied
  all-targets Clippy reached one unrelated concurrent schema-mutation test
  redundant-clone finding after Patch 6's own test lint was corrected.
- Formatting and diff-hygiene checks pass. Full workspace and push-time suites
  remain user-owned under repository policy.
- The maintained SQL performance Wasm build passed its single focused test.
  The current mixed worktree artifact is 5,326,809 raw bytes with SHA-256
  `7236f5441cbffc464d67ccf7b939d810907c94f2f21140f34c4c8ec6ce3f5be0`.
  This is 5,169 bytes, or about 0.097%, above the Patch 5 snapshot. Concurrent
  runtime-value cleanup remains in the same worktree, so the delta is a current
  snapshot rather than isolated byte attribution. No separate activation
  instruction cohort is maintained; steady-state accepted-rule evaluation is
  unchanged, while a pending semantic edit intentionally evaluates both old
  and candidate operations.

### Scope And Complexity

- Patch 6 touches 17 files across accepted constraint lifecycle, proposal
  lowering, application continuation/abort, transition closure, persisted
  validation, model-fragment proof, contracts, changelog, design, and status.
- The approximate slice is `+1,100/-55` lines, dominated by direct,
  activation, abort, recovery, rename, codec, transition, and application-only
  regression evidence.
- Architectural complexity stays neutral: one existing activation record now
  has an exact replacement pairing rule, and every scan, job, marker,
  publication, recovery, integrity, and evaluator owner is reused. There is no
  second runtime evaluator or source-model fallback.
- No wire field, codec version, proposal shape, accepted-profile tag, row,
  index, relation, marker, journal, cursor, integrity-job, or Candid response
  shape changes. The current version-1 accepted snapshot now admits the exact
  same-ID replacement pair described in the persisted-format inventory.

## Patch 7 — Consumer Hard Cut And Documentation

Status: complete.

### Implementation

- All 15 maintained base-type durable-rule declarations use the current typed
  grammar. The exact ledger remains five identifier length rules, four geo
  length rules, three numeric ranges, and three nonnegative decimal rules.
  No maintained canister or integration source owns an additional rule
  declaration.
- The renamed-dependency schema-only package now declares and lowers all five
  current operations: length range, numeric minimum, numeric maximum, numeric
  range, and exact decimal multiple-of. Its accepted proposal evidence binds
  every operation to the expected root, nominal type, and local rule key.
- The same package combines an application normalizer and validator with a
  durable rule, while generated actor-source assertions prove those callback
  paths and the composed application method do not enter runtime actor wiring.
- The typed-adapter downstream package exercises direct normalization, direct
  validation, consuming normalize-then-validate composition, and typed
  operation/callback diagnostics through renamed package dependencies.
- The workspace and four public package READMEs now document the three
  authorities, complete typed rule grammar, operation-specific operands,
  explicit application calls, callback-free writes, accepted-only runtime
  ownership, name-change semantics, and the `ICYT` to `ICYU` development-store
  recreation boundary.
- The macro crate includes its README as crate-level rustdoc. Grammar fragments
  are marked as text because they are nested attribute syntax rather than
  standalone Rust programs.

### Evidence

- The complete `icydb-model` package suite passed: 130 unit tests and both
  compile-test groups, including the closed grammar, required-trait
  diagnostics, 15-rule authority ledger, explicit application composition,
  and fragment callback exclusion.
- The complete macro package suite passed 130 unit tests and its crate
  documentation build.
- The schema-only downstream package passed its generated fragment, canonical
  proposal codec, five-operation lowering, renamed runtime path, and generated
  actor-source assertions. The typed-adapter downstream package passed all
  three adapter and application-behavior tests.
- Warnings-denied all-targets Clippy passed for the model and macro packages
  together and for both downstream package manifests independently.
- The schema/model boundary invariant script passed. Focused searches found
  exactly the frozen 15 maintained base declarations and no live Rust or
  public-documentation use of the retired string operation grammar outside
  its explicitly historical design record.
- Formatting and diff-hygiene checks pass. Full workspace and push-time suites
  remain user-owned under repository policy.

### Scope, Cost, And Complexity

- Patch 7 touches nine files: one downstream package fixture, five public
  READMEs, macro crate-level documentation wiring, changelog, and this status
  tracker. The approximate slice is `+300/-0` lines, dominated by public
  examples and focused downstream proof.
- Production runtime and generated actor complexity are unchanged. The only
  Rust production edit includes the macro README in host-side crate rustdoc;
  it emits no canister token, callback owner, evaluator, or storage path.
- Runtime instructions and raw Wasm were not remeasured for this
  documentation/test-only slice because it changes no production canister
  code. Patch 8 owns the final matched cost and raw-Wasm comparison for the
  complete line.
- Proposal version, accepted profile, accepted fingerprint tags, persisted
  rows, keys, indexes, relations, markers, journals, cursors, integrity state,
  public Candid, and database error shapes are unchanged by Patch 7.

## Patch 8 — Closeout Simplification And Evidence

Status: complete, with the measured no-rule cross-line cost deviation retained
as an explicit follow-up rather than hidden by a wider budget.

### Simplification And Surface Audit

- The second construction-site audit found 35 distinct
  `icydb::__macro` helpers emitted by maintained generated code. The hidden
  facade now exports exactly those 35 helpers; 13 unreferenced facade aliases
  were removed rather than retained as forwarding or compatibility surface.
- Generated actor glue reaches only `candid` and `ic_cdk` through
  `icydb::__reexports`. Five unconsumed dependency aliases were removed, and
  the facade dropped its now-unneeded direct `ctor`, `derive_more`, and
  `remain` dependencies. Those crates remain where their actual model/core
  owners consume them.
- The renamed-dependency typed-adapter fixture now proves named-value adapter
  traits through their owning `icydb-model` dependency instead of through the
  runtime facade. No generated construction site or application API depends
  on the removed aliases.
- Focused searches found all 15 maintained durable rules using the typed
  grammar, no live string-operation or positional-operand rule authoring, no
  database invocation of application normalization or validation, no
  generated-model runtime fallback, and no compatibility decoder. Remaining
  `ICYT` references are explicit recreation documentation. The maintained
  `SourceCheckExpr` machinery is the separate authored SQL/check-expression
  contract, not retired targeted-rule transport.
- The public/persisted wire inventory is unchanged by Patch 8. The consolidated
  `0.216.1` handoff narrows the accepted `Date` epoch-day domain to calendar
  years 0000 through 9999 while preserving its `i32` row, cursor, and Candid
  encoding; out-of-domain values now fail decoding without a compatibility
  fallback. Across the
  complete 0.216 line, the version-1 proposal operation shape gains exact
  numeric maximum and multiple-of, the version-1 accepted snapshot profile is
  hard-cut from `ICYT` to `ICYU`, and the Rust application `VisitorError`
  exposes typed operation/callback identity without deserialization. Row,
  key, index, relation, marker, journal, cursor, integrity-progress, database
  error, and public Candid response shapes do not change.

### Focused Correctness Evidence

- The facade, model, macro, and schema packages passed 376 unit tests and five
  compile-contract harness tests. The renamed-dependency typed-adapter fixture
  passed three tests, and the focused accepted targeted-rule cohort passed 15
  core tests.
- After the bounded Date work was consolidated into `0.216.1`, 18 focused
  schema Date tests and 71 Date-filtered core codec, cursor, projection, and
  adjacent tests passed. Warnings-denied all-targets Clippy passed for schema,
  model, and all-feature core configurations.
- The complete maintained SQL performance target passed 21 tests. The
  explicit application-operation probe and raw-Wasm build target each passed
  their single ignored release-evidence test.
- The facade checked with all features and without defaults. Warnings-denied
  all-targets Clippy passed for the facade in both configurations and for the
  renamed-dependency typed-adapter fixture. A representative generated
  SQL-free typed-query canister also checked successfully.
- Schema/model boundary, layer authority, dependency graph, generated build
  configuration, durability-documentation, and mutation-atomicity guards all
  passed. Formatting and focused hard-cut/reachability searches pass.
- The human-owned full workspace and push-time suites were not run by this
  agent. The complete audited release boundary is tagged `v0.216.2`; focused
  evidence for its bounded post-closeout corrections is recorded below.

### Cost And Size Evidence

- The matched journaled no-rule/accepted-check probe measured steady insert
  `23,625,859` versus `23,809,615` instructions (`+0.778%`), update
  `23,602,761` versus `23,926,283` (`+1.371%`), delete `24,602,359` versus
  `24,878,061` (`+1.121%`), and write/read `24,203,554` versus `24,482,447`
  (`+1.152%`). This is the maintained evidence for the incremental cost of one
  accepted rule.
- Against the isolated `v0.215.7` matched canister, the released 0.216.1
  no-rule path is not neutral. Heap first/steady insert rose `13.772%` and
  `12.744%`; journaled first/steady insert rose `13.038%` and `12.323%`.
  Steady update, delete, and write/read rose `11.857%..=12.687%`. The roughly
  constant mutation/guard increase is much larger than the matched rule
  overhead and spans concurrent runtime cleanup, so it is not attributed to
  durable-rule evaluation without evidence. It remains the one explicit
  performance follow-up.
- Over 256 calls, explicit application normalization measured `711,683`
  instructions (about `2,780` per call), validation `721,903` (about `2,820`
  per call), and composed normalize-then-validate `1,293,295` (about `5,052`
  per call).
- The final released SQL performance artifact is `5,327,610` raw bytes
  with SHA-256
  `e8cbe879d0a642f9824dc555ee4ddc3fea4edd514d2f30ff99200c5b73b3ac78`.
  It is `24,830` bytes (`0.468%`) above the frozen Patch 2 artifact and 462
  bytes (`0.009%`) below released 0.216.1.
- The packaged `icydb-model` crate is `101,684` compressed bytes across 109
  files, versus `94,379` bytes across 98 files at `v0.215.7`: `+7,305` bytes
  (`+7.740%`), dominated by the new rule/application implementation,
  diagnostics, documentation, and compile fixtures.

### Scope And Complexity

- The committed `v0.215.7..v0.216.2` line changes 210 files with
  `+7,100/-5,363` lines: 17 additions, 19 deletions, 172 modifications, and
  two renames.
- The frozen 16-file owner cohort retains the same authority topology: one
  source grammar/lowering route, one accepted compiler/evaluator, one
  catalog-native evolution path, and one explicit application traversal.
  Patch 8 removes duplicate facade exposure and three unused direct
  dependencies without adding a runtime owner, codec, callback registry,
  compatibility path, or response type. The closeout direction is simpler.
- All eight planned landing patches have focused evidence. The strict
  cross-release no-rule-neutrality checklist item remains intentionally open
  until the measured 12–13% common mutation/guard increase is attributed or
  reduced; no budget was widened for that follow-up in Patch 8.

## 0.216.2 Post-Closeout Corrections

Status: complete and closeout-audited. This is bounded fallout and correctness
work, not a ninth planned design slice.

- Timestamp/duration wide arithmetic and negative sub-millisecond rounding are
  exact before saturation. Overflowing duration text rejects.
- Blob is engine-neutral; the proposal literal alone retains the 64 KiB bound.
- Full SQL preserves Unicode `UPPER` semantics, reduced predicate-only `UPPER`
  forms reject, and lowercase casefold lookup cannot select an incompatible
  `UPPER` expression index.
- Collection membership avoids its temporary vector, and SQL/demo fixture
  helpers compile only with the SQL feature that owns them.
- Schema and model suites passed 88 and 132 tests respectively, with both
  model compile-contract harnesses passing. Focused core cohorts passed 12
  `UPPER`, 6 `contains_any`, and 5 `contains_all` tests.
- SQL and RPG canisters check without default features. The affected schema,
  model, core, and CLI packages pass warnings-denied all-feature Clippy.
- Formatting, diff hygiene, durability, schema/model, layer-authority, and
  dependency graph guards pass. The human-owned full workspace and push-time
  suites were not rerun by this audit.
- Release tooling refreshed `clap` and `clap_builder` uniformly to 4.6.5;
  dependency-tree inspection found no duplicate CLI version.
- No persisted byte or Candid wire layout changed. Blob admission, time
  conversion, removed Rust APIs, and reduced-predicate rejection are explicit
  behavioral hard cuts.

## Generated Trait-Policy Follow-Up

Status: Landings A through I complete and final-closeout audited for proposed
0.216.5.

This follow-up preserves `traits(add(...), remove(...))` as the stable-Rust
escape hatch for ergonomic opt-ins and manual implementation overrides. It
replaces the remaining generic resolver behavior with the closed per-node
contract in the generated trait-policy addendum.

| Landing | Outcome | Status |
| --- | --- | --- |
| A. Authority and inventory freeze | Account for all 37 trait kinds, all eight application node kinds, and every maintained explicit directive without changing generated output | Complete |
| B. Trait vocabulary hard cut | Replace `Inherent` with an emitter mode, move `Sorted` to top-level enum `sorted`, and delete redundant `Collection`/`MapCollection` traits | Complete |
| C. Resolver authority and phase order | Compute node/shape baselines before directives and reject a selected trait without an emission strategy | Complete |
| D. Generated/custom strategy audit | Enforce and test `From`, application overrides, `Inner`, and `NumericValue` one trait at a time | Complete |
| E. Derive/helper strategy audit | Preserve `Default`, then enforce standard, dereference, display, and arithmetic/helper traits one trait at a time | Complete |
| F. Follow-up closeout | Remove obsolete generic state, run focused consumer/fixture validation, and refresh 0.216 closeout evidence | Complete |
| G. Exact Rust capabilities | Separate generated Rust derives from database scalar semantics and add unit-enum `Ord` | Complete |
| H. Standard collection protocols | Add standard iteration and collection construction while auditing the broad `From` convenience | Complete |
| I. Operator-family completion | Evaluate `RemAssign`, `Product`, and signed `Neg`, then trim unused helper features | Complete |

Landing A found seven maintained production declarations containing eight
meaningful operations: two `Default` opt-ins, one `From` removal, and five
`ValidateCustom` removals. Maintained macro fixtures add five newtype defaults
and one sorted enum. These uses are the migration boundary; none is a policy
exception.

The accepted target removes four non-target entries from `TraitKind`:
`Inherent` becomes an emitter mode, `Sorted` becomes the enum-only top-level
`sorted` flag, and the redundant public `Collection` and `MapCollection`
traits are deleted in favor of the existing `Deref`/`DerefMut` ergonomics.

The unrelated aggregate cache-identity work already present in the worktree
when this follow-up began is excluded from this inventory landing.

### Landing B Evidence

- `Inherent` is no longer a `TraitKind`; the implementation emitter now owns
  an explicit inherent-versus-trait target.
- Enum ordering is authored as top-level `sorted`. The maintained fixture is
  migrated, and focused macro coverage proves the current flag emits the
  compile-time ordering attribute.
- The custom `Collection` and `MapCollection` traits, generated
  implementations, prelude imports, and facade reexports are deleted. Generated
  validation and visitation traverse the wrapped standard containers
  directly, while an application fixture proves `iter`, `len`, and `is_empty`
  remain available through `Deref` without either custom trait in scope.
- Macro tests passed 131 tests. Model tests passed 132 tests plus both
  compile-contract harnesses. The schema fixture checks and its focused native
  collection-ergonomics test pass.
- Warnings-denied all-targets Clippy passes for the macro, model, facade, and
  generated schema fixture packages. The facade also checks without default
  features.
- The maintained raw SQL-performance Wasm build passes. The mixed concurrent
  worktree artifact is 5,326,704 bytes with SHA-256
  `1aaebb59222c4cb4011b1053177815113239ade308892cbd6ab3a4b41a9809a9`,
  865 bytes (`0.0162%`) below released 0.216.3. Concurrent query-owner cleanup
  prevents attributing that complete delta to this landing.
- The implementation and fixture cohort changes 16 files by approximately
  `+90/-240` lines. One custom implementation module and two public traits are
  removed, so the implementation shape is simpler.
- This is a Rust source and generated-surface hard cut. Generated container
  representation, accepted schema, rows, keys, markers, journals, cursors,
  Candid, and endpoint surfaces are unchanged. There is no runtime algorithm
  change requiring an instruction-cost probe.

### Landing C Evidence

- One `ApplicationTypeKind` authority now identifies entity, enum, list, map,
  newtype, record, set, and tuple during generated-trait resolution.
- Every application node computes its complete required, node, shape, and
  capability baseline before authored additions and removals are applied.
  `From` is consequently baseline-selected only for the wrapper nodes that
  actually emit it rather than existing as a silent universal default.
- Validation and token generation share one strategy resolver. Validation
  rejects any selected trait without a derive or generated implementation,
  and token generation emits a compile error if that invariant is bypassed.
- Trait emission order is deterministic. Focused tests prove shape-derived
  removal happens after baseline construction, fake `From` selection is gone,
  and an intentionally unsupported record strategy fails during validation.
- Macro tests pass 135 tests with one non-runnable syntax doctest ignored.
  Model tests pass 132 tests plus both compile-contract harnesses. The schema
  fixture checks and its focused collection-ergonomics test pass.
- Warnings-denied all-targets Clippy passes for the macro, model, and generated
  schema fixture packages. The facade checks without default features.
- The resolver landing centralizes selection and emission validation while
  retaining eight small node-owned baseline declarations. The implementation
  shape is slightly more explicit but removes the previous late trait
  mutation and silent no-op paths.
- No persisted representation, runtime authority, Candid surface, or endpoint
  changes. Maintained generated trait implementations are preserved, so this
  landing does not require a runtime instruction-cost measurement.

### Landing D Evidence

- One closed generated/custom policy now classifies `From`,
  `NormalizeCustom`, `ValidateCustom`, `Inner`, and `NumericValue` for all
  eight application node kinds before generic trait resolution.
- `From` is generated and removable only for newtype, list, set, and map.
  Both application hooks are generated and removable everywhere. `Inner` is
  generated and removable only for newtype. `NumericValue` is generated for
  compatible primitive newtypes, opt-in for other newtypes, and unsupported
  elsewhere.
- Redundant additions, absent removals, unsupported directives, and a trait
  appearing in both directive lists receive explicit node-and-trait macro
  diagnostics. The former unsupported `Inner` helper-derive path now rejects
  before Rust derive expansion.
- A compile-pass fixture removes all five policies from one numeric newtype,
  supplies manual implementations, and proves the custom-inner
  `NumericValue` opt-in. Five compile-fail fixtures cover the unsupported
  `From`, `Inner`, and `NumericValue` states plus redundant additions of both
  application hooks.
- Macro tests pass 137 tests. Model tests pass 132 tests plus all three
  compile-contract harnesses. The generated schema fixture checks.
- Warnings-denied all-targets Clippy passes for the macro, model, and generated
  schema fixture packages. The facade checks without default features.
- The landing adds one small policy enum and centralized validation rather
  than distributing conditionals across eight emitters. The implementation is
  more explicit without adding a runtime owner or algorithm.
- No persisted representation, accepted-schema authority, generated Candid,
  endpoint, or maintained runtime behavior changes. No instruction-cost or
  Wasm-size measurement is required for this compile-time policy landing.

### Landing E Evidence

- The configurable matrix now accounts for all 23 non-required target traits;
  together with the ten required traits, every application node/trait pair has
  one code-owned policy before directives are resolved.
- `Default` remains generated and removable for list, set, and map and opt-in
  elsewhere, with the existing constructibility and default-variant checks
  unchanged. `Copy`, `Hash`, `Ord`, and `PartialOrd` are generated when selected
  by shape and otherwise remain standard opt-ins.
- `Deref` and `DerefMut` are generated for collection wrappers, opt-in for
  newtypes, and unsupported elsewhere. `Display` is newtype-only. All ten
  arithmetic and assignment helpers are generated or opt-in only on newtypes
  according to the maintained primitive capability baseline.
- Resolver validation now checks that every complete node baseline agrees with
  its policy before applying directives. Redundant additions, absent removals,
  conflicts, and unsupported nodes reject before helper-derive expansion.
- One compile-pass fixture exercises manual and opt-in `Default`, all four
  standard derives, dereference/display, and all ten arithmetic helpers.
  Grouped compile-fail fixtures cover every one of those configurable traits.
  Combined with Landing D, every configurable trait has positive and negative
  compile-contract evidence.
- Macro tests pass 139 tests. Model tests pass 132 tests plus all four
  compile-contract harnesses. The generated schema fixture checks.
- The landing extends the existing centralized policy rather than adding a
  second resolver. Most line growth is compile fixtures and stable diagnostic
  snapshots; runtime implementation complexity is unchanged.
- No accepted-schema, persisted, Candid, endpoint, or maintained generated
  runtime behavior changes. This compile-time policy landing requires no
  instruction-cost or Wasm-size measurement.

### Landing F Evidence

- The final inventory proves exactly 33 target traits: ten required and 23
  configurable. All eight application node kinds resolve through the same
  code-owned policy without a generic fallback.
- Fixed generated canister, store, normalizer, and validator nodes now obtain
  their exact `Clone`, `Debug`, and `Path` set directly. They no longer create
  an empty application `TraitBuilder` or enter application directive
  resolution.
- `TraitSet` uses deterministic ordered storage directly. Its unused generic
  token-rendering and vector-conversion paths are removed, while maintained
  emission order is unchanged.
- Repository searches found no live retired trait variants, retired source
  forms, silent selected-trait path, or obsolete generic builder route.
- Macro tests pass 140 tests with one non-runnable syntax doctest ignored.
  Model tests pass 132 tests plus all four compile-contract harnesses. The
  generated collection-ergonomics fixture passes its focused test.
- Warnings-denied all-targets Clippy passes for the macro, model, and fixture
  packages. The facade checks without default features; formatting and diff
  hygiene pass.
- The cleanup changes six production macro files with a small net deletion
  and removes generic machinery, so the implementation shape is simpler. It
  changes no runtime algorithm, maintained generated output, accepted schema,
  persisted representation, Candid method, or endpoint and therefore requires
  no new instruction-cost or raw-Wasm measurement.
- The concurrent query-owner cleanup in the shared worktree remains outside
  this follow-up. Full workspace and push-time validation remain user-owned.

### Landing G Evidence

- Primitive-newtype trait selection now follows the operations implemented by
  the concrete wrapped Rust type rather than database query, index, or scalar
  capability metadata. The database capability helpers remain authoritative
  only for their database concerns.
- Unit enums now derive the missing total `Ord`. Blob and Unit newtypes gain
  their valid `Hash`, `Ord`, and `PartialOrd` implementations. Duration
  newtypes gain only `Add`, `AddAssign`, `Sub`, and `SubAssign`; unsupported
  multiplicative, remainder, and `Sum` implementations remain absent.
- A compile-pass fixture proves the generated implementations against real
  Rust trait bounds. A compile-fail fixture proves that adding any newly
  automatic capability is diagnosed as redundant rather than silently doing
  nothing.
- Serde `Serialize` remains deliberately absent. The broad newtype
  `From<T: Into<Inner>>` convenience is unchanged and remains in scope for the
  collection-protocol audit rather than being narrowed without evidence.
- Macro tests pass 142 tests. Model tests pass 132 tests plus all four compile
  contract harnesses. The generated schema fixture checks, warnings-denied
  all-targets Clippy passes for all three affected packages, and the facade
  checks without default features.
- The maintained SQL performance canister is byte-identical to a clean
  0.216.4 build: 5,326,277 raw non-gzipped bytes with SHA-256
  `573836b8e05179b438bb2ef2b24adfac79766290e127a97811c843b96613261b`.
  The linker therefore removes the newly available but unused trait methods.
- The landing changes three production macro files and adds focused compile
  fixtures. Its implementation shape is slightly more explicit, with no new
  runtime owner, algorithm, persisted representation, Candid surface, or
  endpoint behavior. Concurrent query-owner edits remain outside this landing.

### Landing H Evidence

- List, set, and map baselines now generate the standard `FromIterator` and
  `IntoIterator` protocols. Owned and shared-reference iteration are available
  on all three wrappers; list and map also expose the mutable-reference
  iteration supported by `Vec` and `BTreeMap`, while set elements retain
  `BTreeSet` immutability.
- `FromIterator` accepts the exact item or key/value pair type. This preserves
  ordinary `collect` inference and avoids a second broad conversion surface.
  The existing collection `From<Vec<I>>` and newtype `From<T>` implementations
  retain their `Into` bounds because compile evidence found no coherence or
  method-resolution overlap and demonstrates their useful convertible-input
  behavior alongside the new protocols.
- Both protocols are generated-and-overrideable only for collection nodes.
  Redundant additions and non-collection requests reject with node-specific
  diagnostics; removal permits an application-owned manual implementation.
- One focused emitter owns the protocol implementations, and the existing
  implementor can now target borrowed wrapper types without duplicating
  generic or where-clause assembly. Generated iterator lifetimes are chosen to
  avoid authored lifetime names.
- Macro tests pass 144 tests. Model tests pass 132 tests plus all four compile
  contract harnesses. One executable schema-fixture test proves list, set, and
  map order, ownership, and mutation behavior. Warnings-denied all-targets
  Clippy passes for the three affected packages, and the facade checks without
  default features.
- The maintained SQL performance canister remains byte-identical at 5,326,277
  raw non-gzipped bytes with SHA-256
  `573836b8e05179b438bb2ef2b24adfac79766290e127a97811c843b96613261b`.
  Newly available iterator methods therefore add no retained Wasm in this
  representative canister.
- The maintained target-trait inventory grows deliberately from 33 to 35.
  Runtime algorithms, accepted schema, persisted representations, Candid, and
  endpoints are unchanged. Concurrent query-owner edits remain outside this
  landing.
- Landing H touches 16 files at approximately `+558/-16` lines, dominated by
  the centralized emitter, compile fixtures, and documentation. Host-side
  macro complexity increases to model the three standard-container iterator
  shapes; canister runtime complexity and retained Wasm remain unchanged.

### Landing I Evidence

- Primitive newtypes now generate `RemAssign` for exactly the fixed-integer
  and decimal remainder set, `Product` for the complete decimal and
  fixed/arbitrary-precision integer arithmetic set, and `Neg` for decimal plus
  signed fixed/arbitrary-precision integers. Finite floats remain outside
  `Neg` because their atom invariant canonicalizes negative zero away.
- The canonical `Decimal`, `IntBig`, and `NatBig` atoms provide the missing
  concrete traits directly. Decimal negation retains saturating overflow
  behavior; iterator products use the exact multiplicative identity and the
  existing multiplication semantics.
- The model facade reexports all three helper derives. Positive compile
  fixtures cover fixed, decimal, and arbitrary-precision wrappers; negative
  fixtures prove redundant generated selection and unsupported record use.
  An executable maintained fixture proves remainder assignment, product, and
  negation delegate to the wrapped type.
- The private parser wrapper's unused `IntoIterator` derive and the macro
  crate's direct `derive_more` dependency are removed. The unused workspace
  `deref`, `deref_mut`, `into_iterator`, and `sum` features are also removed;
  only `add` and `add_assign` remain, both for maintained big-integer atoms.
- The maintained target-trait inventory grows from 35 to 38. No Serde output,
  accepted-schema fact, persisted representation, Candid surface, endpoint, or
  runtime owner changes.
- Schema, macro, and model packages pass 90, 147, and 132 tests respectively;
  all four model compile-contract harnesses pass, along with the focused
  executable schema fixture. Warnings-denied all-targets Clippy passes for the
  four affected packages, and the facade checks without default features.
- The current shared-worktree SQL performance canister is 5,326,191 raw
  non-gzipped bytes with SHA-256
  `060064ca24498948357ad9fa29711ab6c453d9f7af1a87a1ea4a6853587a007a`.
  That is 86 bytes (0.002%) smaller than Landing H, so the operator family adds
  no retained Wasm growth; concurrent query-owner edits prevent attributing
  the small reduction solely to this landing.
- Landing I touches approximately 22 files at roughly `+400/-20` lines,
  dominated by exact capability evidence and documentation. Host-side helper
  complexity increases modestly for three standard traits, while dependency
  surface shrinks and canister runtime structure remains neutral.
