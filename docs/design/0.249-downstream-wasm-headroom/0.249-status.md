# IcyDB 0.249 Measured Downstream Wasm Headroom Status

Status: PATCH 8 FOCUSED VALIDATION COMPLETE — AWAITING USER-OWNED RELEASE VALIDATION AND ARTIFACT REQUALIFICATION

Last updated: 2026-08-30

## Decision

The exact current Toko `project_instance` remains above the IC code-section
limit after the current IcyDB-owned Binaryen 132 `-Oz` pipeline. Patch 1 found
that the largest IcyDB family is ordered-overlay traversal, with 31 monomorph
rows, about 28,503 bytes of bloat and a 29,518-byte complete pre-transform
body. It was initially rejected against the provisional 32 KiB intake gate.

Review admitted this one sub-threshold family because a smaller exact win was
still useful and the replacement could stay allocation-free and structurally
neutral. Patch 2 saves 13,781 final raw bytes, 13,746 final code bytes and 20
declared functions. Static key projection leaves `call_indirect` unchanged at
1,038. No public surface, runtime behavior or persisted format changes.

The Patch 2 code section is 10,786,296 bytes: still 300,536 bytes above the
platform limit and 562,680 bytes above the selected reserve in its original
Canic 0.109.22 pipeline. Remaining deployment headroom therefore still belongs
primarily to Canic's generated/runtime build boundary and Toko's final
optimizer or canister architecture.

Patch 3 tested the next-ranked covering-projection family against Patch 2. A
concrete fallible iterator preserved existing-row checks and effective-window
semantics, but saved only 502 final raw bytes and 507 code bytes. It added two
declared functions, one `call_indirect`, 233 gzip bytes and a new iterator state
type. The candidate was rejected and removed; the retained artifact and
production source remain the Patch 2 result.

The user explicitly authorized ICYDB-013 for `0.249.0` after the Wasm session
stopped. Patch 4 completes that narrow contract: one fixed-size operation-local
result for an ordinary dynamic or typed/fluent live page, using the existing
accepted-schema, planner, admission, executor, cursor and typed-decode owners.
It suppresses retained metrics and exposes no query or caller-controlled
labels. It does not reopen rejected Wasm candidates or authorize Patch 5
production work by itself.

The user then supplied new isolated evidence for a different boundary than the
rejected Patch 1 structural-mutation family: Toko repeats typed mutation
execution, cardinality validation and accepted-row projection for every
generated entity even though only final row decoding depends on that entity.
Patch 5 adds concrete IcyDB single-write and same-entity structural-batch row
terminals. The exact production A/B saves 79,140 final raw bytes, 77,887 code
bytes and 126 declared functions. Indirect dispatch, Candid and exports are
unchanged. Patch 6 measured the then-complete retained line against exact
`v0.248.1`: 92,354 fewer final raw bytes, 91,275 fewer code bytes and 147 fewer
declared functions.

Before the untagged release was published, the user explicitly folded the
separately measured pagination finding into the same active `0.249.0`. Patch 7
adds one non-entity page driver and adopts it in the disposable Toko typed and
projected-ULID consumers. Its exact sequential A/B saves another 14,776 final
raw bytes and 14,433 code bytes, with three added declared functions and
unchanged indirect dispatch. The extended cumulative result is 107,159 fewer
raw bytes, 105,737 fewer code bytes and 144 fewer functions than `v0.248.1`.
The actor still exceeds the platform code limit by 134,147 bytes, so remaining
installability work belongs to Canic role specialization or a Toko actor split.

The first user-owned release validation then exposed two deterministic SQL
instruction regressions plus one timestamp-sensitive attribution test. Patch 8
restores the ordinary route-free scalar projection result, captures the
physical route only for the explicit attributed terminal, and removes an eager
live-key tombstone lookup from the ordered-overlay iterator. Visibility is now
checked only for the merge entry actually selected, matching the retained
visitor's work contract without restoring visitor/error monomorphs. The two
focused SQL gates pass at 1,742,018 and 1,514,826 instructions against ceilings
of 1,760,100 and 1,525,972. The retained-metrics proof now compares counters,
entity counters and active-window identity while excluding the report-generation
end timestamp. Because Patch 8 changes production source, the Patch 7
cumulative downstream Wasm totals below remain the last exact artifact receipt
until the user-owned release flow requalifies the final tree.

## Frozen Subject

| Input | Exact receipt |
| --- | --- |
| IcyDB | `v0.248.1`; revision `80fdea8f87895b46f6cd708b32853df0ee5f1cf3`; tree `a63af5ab8c371ea852cbd89b3ecf24025bbf60a1` |
| Toko | revision `20ff137d5e0c26be4de013ce08b2272c927ba0dd`; tree `4321b42c209d93f7a3e968b60d103fcae9f1fcae`; clean source cloned to a disposable worktree |
| Canic | `v0.109.22`; revision `674f1098fffb3cb51b59b3213b175b4e1ee769fb`; tree `c45e5b0c8103c562636f56b6530e000f1953d112` |
| Toko schema | 60 `project_instance` entities; sorted source-list fingerprint `c2eb860619478255a48f40c8cb98604f0d7a1ee03502b3f9aee014c36a6c43ee` |
| Generated actor | 321,974 B; SHA-256 `6cf4174451f45d0c1e9811cea5668b9d942ab932248ae5362e19871e0f816947` |
| Generated Canic source | 43,996 B; SHA-256 `b7a86d47866775de1544881589a43f3dd025e1cdda6c966b3a671e8311c1181f` |
| Resolved lockfile | SHA-256 `864c96c68571de4653d41f0bb1f8dce9ecf632e6832d8dff78ab8aa38ee2371c` |
| Public Candid | 128,886 B; SHA-256 `5669f07404d85d5bed9c3491e67a8eba5dfb68cf93ca60d02df841f135468695` |
| Public exports | 274 name/kind pairs; fingerprint `8a3bbf6d2dfc7e12a358a656f026d15ee5874208c63e13d1b2676eb74d64b7d6` |

The disposable Toko manifest changed dependency resolution only: it points to
the clean Canic checkout and the released IcyDB source tree above. The original
Toko and Canic working directories were read-only and were not changed.

## Toolchain

- Rust and Cargo 1.97.1, `wasm32-unknown-unknown`, `opt-level = "z"`, fat LTO,
  one codegen unit and stripped output.
- Canic CLI binary SHA-256
  `665a3c15e9111e8c634daf14491c3c09f6a6b4bcb861daea276a6102b1e3c04b`.
- Binaryen 132 SHA-256
  `1014958e6f20d412f1542320b43970214b0fb1ed780595e8f7c0d8761ed53725`.
- Exact current final transform: `wasm-opt -Oz --enable-mutable-globals
  --enable-nontrapping-float-to-int --enable-bulk-memory --enable-sign-ext
  --enable-bulk-memory-opt`.
- Installed `ic-wasm` 0.11.1 has no `optimize` subcommand. Historical
  `ic-wasm` 0.9.11, SHA-256
  `e4759fc86b20191dc93ac09b5f16b4779a602d83e1978557ea86e0656e366b8c`,
  was used only to reproduce Toko's legacy `-O3` path.

## Exact Artifact Results

| Stage | Raw Wasm | Code section | Declared functions | Gzip `-n -9` | SHA-256 |
| --- | ---: | ---: | ---: | ---: | --- |
| Rust/Canic preflight input | 13,312,592 B | 12,440,384 B | 52,803 | 3,325,857 B | `3a2cb0295bd9eb8148702349359a1fadfeff7488fdc439cf38e591a279971416` |
| Legacy Toko `-O3` | 12,216,971 B | 11,384,961 B | 22,161 | 4,147,401 B | `65db94f1894c61e57842208d99a607fc21091c6d834251eab8eaa572bf89e424` |
| Released `v0.248.1`, Binaryen 132 `-Oz` | 11,633,404 B | 10,800,042 B | 22,881 | 3,943,686 B | `9ee7d894ea2e9193c1b9c37a0db82807d12e848cf23852774879856383465aac` |
| Patch 2 preflight input | 13,294,067 B | 12,421,906 B | 52,758 | — | `0432f89176b76e511715f1e1e10ad3d6c990a4753e7808cc8ecdaa6de3677852` |
| Patch 2, Binaryen 132 `-Oz` | 11,619,623 B | 10,786,296 B | 22,861 | 3,942,418 B | `ce956a5eb1ddd0ef336ec13357b133d63ed8c4f6fe841bc3b4f75bfbb1f68fe0` |
| Rejected Patch 3 preflight input | 13,293,649 B | 12,421,484 B | 52,762 | — | `4713a4102dd42962424011ec08654fc745640dc0dd0597f14559a31077d1dbca` |
| Rejected Patch 3, Binaryen 132 `-Oz` | 11,619,121 B | 10,785,789 B | 22,863 | 3,942,651 B | `020f4b6532f0a5e1155e3bf1ef1ca1c3e139d7da6b60de4e30b1647a3dd8e4f1` |

The retained Patch 2 `-Oz` artifact:

- exceeds the official 10,485,760-byte code limit by 300,536 bytes;
- exceeds the selected 10,223,616-byte reserve target by 562,680 bytes; and
- remains safely below the 50,000 declared-function threshold.

Canic's normal fast build fails its exact code-section preflight before Toko's
legacy post-processing. No replica install was attempted because the final
`-Oz` artifact still deterministically violates the same platform limit and
the local network lifecycle is user-owned.

No canister instruction A/B was captured because the frozen actor still fails
the deterministic preflight before an installable artifact exists. The retained
iterator adds no allocation or indirect dispatch, preserves the comparison and
tombstone work per entry, and passes focused host semantics. Replica-level
instruction validation remains part of the user-owned downstream flow.

The released, Patch 2 and rejected Patch 3 artifacts have identical normalized
export name/kind lists and identical public Candid custom-section bytes. All
retain 274 exports and the frozen 128,886-byte generated Candid surface.

## Patch 4 Operation-Local Attribution Evidence

The new explicit terminals return `AttributedRead<LiveQueryPageOutput>` for
dynamic reads and `AttributedRead<LivePage<E::Row>>` for typed/fluent reads.
Their fixed attribution contains total, engine and typed response-decode local
instructions, bounded access/execution/cache enums and scanned/emitted row
counts. Its complete current Candid encoding is 200 bytes under a 256-byte hard
test ceiling. It contains no strings, blobs, entity/index identity, predicates,
literals, query text, caller identity or caller-controlled labels.

The focused native test returns the exact ordinary result through both dynamic
and typed-binding attributed paths, preserves the participating-store revision
proof and leaves compact retained metrics byte-for-byte unchanged. The focused
PocketIC exact-primary-key probe records a cold cache miss and warm cache hit,
one scanned/emitted row, non-zero dynamic engine/total counters and non-zero
typed response-decode attribution. The query-context instruction comparison is:

| Warm exact-key call | Local instructions |
| --- | ---: |
| Ordinary | 520,017 |
| Attributed outer call | 521,736 |
| Delta | +1,719 / +0.3306% |
| Cheap-path allowance | max(25,000, 3%) = 25,000 |

The attribution itself reports 520,613 total and 271,182 engine-local
instructions for the dynamic call. The remaining measured work is the accepted
schema/facade boundary; dynamic response decode is correctly zero. Typed proof
separately verifies a positive response-decode counter.

An exact Binaryen 132 `-Oz` A/B used released `v0.248.1` plus only the four
retained Patch 2 production files as the control. Actors that do not select the
new terminal are byte-identical:

| Unused-attribution subject | Raw Wasm control/candidate | Code control/candidate | Functions control/candidate | Candid |
| --- | ---: | ---: | ---: | --- |
| `default_empty` | 1,744,902 / 1,744,902 | 1,640,403 / 1,640,403 | 4,420 / 4,420 | identical, 13 B |
| `one_entity_typed_query` | 1,907,052 / 1,907,052 | 1,793,898 / 1,793,898 | 4,865 / 4,865 | identical, 64 B |

The final Wasm SHA-256 values are respectively
`2f803135968acd1b2545ab466d2c3fd3c44c4c28e0f50718d504cbe34769d845`
and `09dc45ac858fe0434d02f61a7c5a49fe86044d75f59c520ae8418d30d35cb54b`
on both sides. The frozen Toko source does not call the new terminal, so Patch 4
does not claim another Toko byte reduction or alter the retained Patch 2
artifact result.

For cost context, the audit actor deliberately selects both dynamic and typed
terminals, performs cold/warm/control calls and exports the proof envelope. Its
final raw Wasm changes from 4,212,631 to 4,252,807 bytes (+40,176), code from
3,957,146 to 3,994,552 bytes (+37,406), functions from 10,297 to 10,363 (+66),
and deterministic gzip from 1,673,213 to 1,689,883 bytes (+16,670). Its Candid
grows by 3,144 bytes for the new audit method and return types. This is a proof-
harness ceiling, not an estimate for Toko's eventual single authorized typed
wrapper; that adoption size remains downstream-owned. The control and candidate
final Wasm SHA-256 values are respectively
`b01d21b8dd13a841208c88d2d23ea5509cf770239d536b28d20fa03c0579108d`
and `c6ca37d8290056645c7a737efd6d527ef6b96f021bc8423c51e5aec42676a311`.

## Patch 5 Typed Mutation Projection Evidence

Patch 5 uses Toko revision
`20ff137d5e0c26be4de013ce08b2272c927ba0dd`, Canic `0.109.23`, Rust
1.97.1 and Binaryen 132 on both sides. Both disposable Toko builds resolve the
same lockfile, SHA-256
`63f0fbb8fd5214d242fc7011272f81c161af1c040a203612e4de89c7a5208e4b`.
The control uses released `v0.248.1` plus only retained Patch 2 production
source. The candidate adds Patch 4, which Toko does not select, and Patch 5,
then changes only the disposable Toko persistence adapter to call the new
concrete terminals before its final generated row decode.

| Stage | Raw Wasm | Code section | Declared functions | Gzip `-n -9` |
| --- | ---: | ---: | ---: | ---: |
| Control preflight | 13,190,004 B | 12,328,703 B | 52,196 | — |
| Patch 5 preflight | 13,102,167 B | 12,242,140 B | 51,828 | — |
| Preflight delta | -87,837 B | -86,563 B | -368 | — |
| Control, Binaryen 132 `-Oz` | 11,534,411 B | 10,712,256 B | 22,612 | 3,902,774 B |
| Patch 5, Binaryen 132 `-Oz` | 11,455,271 B | 10,634,369 B | 22,486 | 3,888,000 B |
| Final delta | **-79,140 B** | **-77,887 B** | **-126** | **-14,774 B** |

The control and candidate final SHA-256 values are respectively
`dfaf86c69010e86a571904d955f84e8ea3980540182b8961609b265e52af4e9e`
and `a7d80e10f76d94a395f1c503cd066d5c0a62145f4f1100aff2756ca95177a68a`.
Both have 1,037 `call_indirect` instructions. Their normalized 274-entry
export ledgers are byte-identical, SHA-256
`74af8aa53116cbd504db65dd7207519b607514373bcfc6ff48670c1c5535b6f9`,
and their 128,886-byte public Candid files are byte-identical, SHA-256
`5669f07404d85d5bed9c3491e67a8eba5dfb68cf93ca60d02df841f135468695`.

The final candidate remains 148,609 code bytes above the 10 MiB platform
limit and 410,753 bytes above the selected reserve. No replica install or
instruction A/B was attempted because the exact actor still fails this static
boundary. Focused native proof covers generated single-row execution,
same-entity structural batches, result order and pre-execution foreign-entity
rejection. The public facade compile-pass contract selects both concrete
terminals beside the retained dynamic-result surfaces. The mutation,
admission, commit, journal and recovery owners are unchanged; the new boundary
consumes the existing dynamic result and moves only cardinality validation
plus accepted-row projection out of downstream entity monomorphs.

Unused-surface checks show no regression. `default_empty` remains byte-
identical at 1,744,902 final raw bytes with 4,420 functions and SHA-256
`2f803135968acd1b2545ab466d2c3fd3c44c4c28e0f50718d504cbe34769d845`.
The one-entity typed-query actor changes from 1,907,052 to 1,906,968 raw bytes
and from 4,865 to 4,864 functions, a further 84-byte reduction rather than a
dead-code cost.

## Patch 6 Cumulative Qualification

Patch 6 rebuilds exact released `v0.248.1` and compares it directly with the
complete retained 0.249 production source. Both sides use Toko revision
`20ff137d5e0c26be4de013ce08b2272c927ba0dd`, Canic 0.109.23, Rust 1.97.1,
Binaryen 132 and lockfile SHA-256
`63f0fbb8fd5214d242fc7011272f81c161af1c040a203612e4de89c7a5208e4b`.
The Canic CLI and `wasm-opt` binaries have SHA-256 values
`8b64052b02c3c9cd6c73fa587d41e55b33d06aa9a6be9a798e9c146522401259`
and
`1014958e6f20d412f1542320b43970214b0fb1ed780595e8f7c0d8761ed53725`.
The 18-file retained IcyDB production-source ledger has SHA-256
`ace755dca187cf91c5724609269af3f5635e5cb64826c7afdb16dedb05514286`.
The disposable Toko adapter changes only terminal selection and final row
decode; its diff has SHA-256
`e43d2dea7a424532040aaed95c1edb6d20a81cd67292ae155cc854247719b1c7`.

| Sequential retained stage | Raw Wasm | Code section | Declared functions | Gzip `-n -9` |
| --- | ---: | ---: | ---: | ---: |
| Exact `v0.248.1` preflight | 13,208,244 B | 12,347,029 B | 52,238 | — |
| Patch 2 preflight | 13,190,004 B | 12,328,703 B | 52,196 | — |
| Final Patch 5 preflight | 13,102,167 B | 12,242,140 B | 51,828 | — |
| Exact `v0.248.1`, Binaryen 132 `-Oz` | 11,547,625 B | 10,725,644 B | 22,633 | 3,904,438 B |
| Patches 2-4 retained, Binaryen 132 `-Oz` | 11,534,411 B | 10,712,256 B | 22,612 | 3,902,774 B |
| Final 0.249, Binaryen 132 `-Oz` | 11,455,271 B | 10,634,369 B | 22,486 | 3,888,000 B |
| Cumulative 0.249 delta | **-92,354 B** | **-91,275 B** | **-147** | **-16,438 B** |

Patch 2 contributes 13,214 raw bytes, 13,388 code bytes, 21 functions and
1,664 gzip bytes under this final current pipeline. Patch 3 retains no source.
Patch 4 is not selected by the frozen Toko actor and therefore leaves its row
unchanged. Patch 5 then contributes its independently measured 79,140 raw,
77,887 code, 126-function and 14,774-gzip reduction. Patch 6 adds no production
code and does not derive the cumulative result by summing isolated deltas.

The exact control and final Wasm SHA-256 values are respectively
`3b44830c626d3d8daf7041954a5704a51e42fdae1729263e29108f016f661789`
and `a7d80e10f76d94a395f1c503cd066d5c0a62145f4f1100aff2756ca95177a68a`.
Their deterministic gzip SHA-256 values are
`2bc38fabc8d425afbbc5d27e135dacff900276feff4fccefca5c165c992dcc35`
and `18f8195d76dd536cb9c6caac8c519c7c4e0e46f1eb82a12cb9c7d532d4b5eca4`.
Both retain 1,037 `call_indirect` instructions. Their normalized 274-entry
export ledgers are byte-identical with SHA-256
`74af8aa53116cbd504db65dd7207519b607514373bcfc6ff48670c1c5535b6f9`,
and their 128,886-byte public Candid files are byte-identical with SHA-256
`5669f07404d85d5bed9c3491e67a8eba5dfb68cf93ca60d02df841f135468695`.

| Closeout boundary | Result |
| --- | --- |
| Supplied dependency-upgrade floor | `-458,155 B` final raw under the supplied matching `-Oz` comparison; prior released Canic/IcyDB work, not credited to 0.249 |
| Final IcyDB 0.249 framework delta | `-92,354 B` raw, `-91,275 B` code, `-147` functions and `-16,438 B` gzip after minimal downstream terminal selection |
| Independent Canic role-specific delta | None included or released in this qualification; the earlier 644,835-byte artificial removal remains a ceiling, not a product result |
| IC 10 MiB code limit | Final code remains `148,609 B` over |
| Selected 256 KiB code reserve | Final code remains `410,753 B` over |
| Repository raw-Wasm budget | No machine-enforced Toko raw budget exists; the informal 256 KiB reduction objective is short by `169,790 B` |
| Function limit | 22,486 declared functions; passes with 27,514 functions of headroom |

Focused cumulative semantics pass for ascending/descending overlay order,
live precedence, tombstones, early stop, operation-local attribution, retained-
metrics invariance, typed mutation projection, startup recovery and recovered
index folding. The PocketIC exact-key receipt remains 520,017 instructions for
the ordinary call and 521,736 for the attributed call, a 1,719-instruction
(0.3306%) delta. The fixed attribution envelope remains 200 Candid bytes.

Canic emits the final preflight artifact and rejects its 12,242,140-byte code
section before the external final transform. The transformed artifact still
fails the platform code limit by 148,609 bytes, so no replica install or
replica instruction journey was attempted. Toko's complete repository CI and
live-canister qualification remain downstream-owned and were not run.

## Patch 7 Shared Live-Page Advancement Evidence

Patch 7 uses the same Toko revision, Rust 1.97.1, Canic 0.109.23 and
Binaryen 132 as the retained mutation-projection build. The disposable control
and candidate both point at the same current IcyDB source path and resolve the
same lockfile, SHA-256
`63f0fbb8fd5214d242fc7011272f81c161af1c040a203612e4de89c7a5208e4b`.
The control leaves Toko's entity-generic page loops in place; the candidate
changes only those disposable typed-row and projected-ULID loops to select the
new IcyDB driver. The bounded count probe shares the projected-ULID path and
therefore needs no separate terminal.

The exact Canic 0.109.23 CLI was rebuilt from its release tag for both sides;
its SHA-256 is
`7139c52db0f69dcf1722400d8e22de6ab07b8acabde9eb37c59f9d40ede5e689`.
Both sides use the recorded Binaryen 132 binary and exact final transform.

| Stage | Raw Wasm | Code section | Declared functions | Gzip `-n -9` |
| --- | ---: | ---: | ---: | ---: |
| Same-path control preflight | 13,102,213 B | 12,242,184 B | 51,830 | — |
| Shared-driver preflight | 13,086,618 B | 12,226,969 B | 51,834 | — |
| Preflight delta | -15,595 B | -15,215 B | +4 | — |
| Same-path control, Binaryen 132 `-Oz` | 11,455,242 B | 10,634,340 B | 22,486 | 3,887,977 B |
| Shared driver, Binaryen 132 `-Oz` | 11,440,466 B | 10,619,907 B | 22,489 | 3,886,258 B |
| Final delta | **-14,776 B** | **-14,433 B** | **+3** | **-1,719 B** |

The control preflight/final SHA-256 values are
`37ad89ff92c8643d6aac535df7364c4e423482de8aa755b4a9060aa37bb7b6a6`
and
`11a2a4ba6327aa16a8f85642658e813c36f45c3cdcb161c4d6525c152ed6cf29`.
The candidate values are
`d4e20fdaa31ab3d765379ddc098c85462b4ce8b1481f4059a78ea4d65e3c21dc`
and
`dbe672533f3b7b402d71ad13d44b350d87a3a9d248ae09e45c5b74cb0d0d13d7`.
Both final artifacts retain 1,037 `call_indirect` instructions. Their 274
export names and 128,886-byte public Candid custom sections are byte-identical.

The driver executes exactly one public or trusted page, moves its continuation
into caller-owned state, clears that state on exhaustion and rejects a repeated
non-null token with the existing cursor-origin invariant diagnostic. Toko
projects or decodes the returned page before requesting the next one, so peak
page memory and public/trusted lane separation remain unchanged. Existing
one-page APIs remain available and no collect-all API was added.

Against the exact `v0.248.1` artifact already qualified in Patch 6, the
extended final candidate is 107,159 raw bytes, 105,737 code bytes, 144 declared
functions and 18,180 gzip bytes smaller. Its 10,619,907-byte code section is
still 134,147 bytes above the platform limit and 396,291 bytes above the
selected reserve. The informal 256 KiB raw-reduction objective remains short
by 154,985 bytes. The preflight rejection is therefore expected; no replica
install or instruction A/B was attempted.

## Patch 8 Release-Validation Performance Correction

The failed release candidate measured 1,766,998 instructions for the indexed
secondary minimum and 1,538,492 for the ascending secondary ordered limit,
exceeding their maintained ceilings by 6,898 and 12,520 instructions. Restoring
the ordinary scalar projection result ABI removed only a small part of the
cost, proving that operation-local attribution was not the primary regression.

The ordered-overlay iterator performed a live tombstone lookup before comparing
canonical and live keys. When the canonical entry won, it then performed a
second lookup for the canonical key. Patch 8 compares first and checks
visibility only after selecting Canonical, Live or Both. This preserves the
allocation-free iterator and its visitor/error monomorph reduction while
matching the previous per-entry lookup count.

Focused PocketIC results on the corrected tree are:

| Maintained query gate | Corrected instructions | Ceiling | Margin |
| --- | ---: | ---: | ---: |
| Indexed secondary `MIN` | 1,742,018 | 1,760,100 | 18,082 |
| Ascending secondary ordered `LIMIT 1` | 1,514,826 | 1,525,972 | 11,146 |

The attributed live-page state proof also no longer compares two independently
sampled `window_end_ms` values. It still proves identical retained global and
entity counters plus the same active window, so attribution cannot hide a
retained-metrics mutation behind a timing race.

## Downstream Proof Boundary

The IcyDB-owned downstream gate for this line is the frozen exact-source Toko
`project_instance` build, Canic code-section preflight, raw/code/function A/B,
export and Candid comparison, and focused owner semantics recorded here. That
gate is complete through the Patch 7 cumulative exact `v0.248.1` comparison,
retained Patches 2, 5 and 7, and rejected Patch 3. Patch 8's final artifact
requalification remains pending. Patch 4's operation-local
attribution has its separate focused IcyDB proof above because the frozen Toko
source does not select it. Patch 7's disposable Toko adoption proves both
typed-row and projected-ULID traversal without changing the frozen public
surface.

Toko's complete repository CI, live canister install and replica instruction
journey remain downstream- and user-owned. They were not run and are not an
IcyDB 0.249 completion requirement. The deterministic Canic preflight failure
and skipped replica work remain explicit; neither is represented as a passing
complete Toko gate. A later Toko full-CI result would prove adoption
compatibility, not retroactively widen IcyDB's release evidence.

## Candidate Ranking

The symbol-preserving twin is 28,017,784 raw bytes, SHA-256
`47e55c0cbb8e539537217b093b3f85a513db1441f2b5532121e08078ff0713ba`.
Its Twiggy monomorph report has SHA-256
`f845ca7d3b049d087c0225e73198296c696f7ade5ffc16f02e5333d96ead81b0`.

| Candidate | Approximate monomorph bloat | Complete body ceiling | Disposition |
| --- | ---: | ---: | --- |
| Ordered overlay traversal | 28,503 B | 29,518 B | Retain after reviewed sub-threshold A/B: -13,781 raw, -13,746 code, -20 functions |
| Covering projection | 5,442 B | 24,602 B | Reject after exact A/B: -502 raw and -507 code, but +2 functions, +1 indirect call and +233 gzip |
| Structural mutation | 778 B | 1,563 B | Reject |
| Planner traversal | 234 B | 2,803 B | Reject |
| Residual retry | 0 B | 0 B | Reject; no retained generic family |

These are conservative body ceilings, not predicted final savings. Patch 2's
exact result confirms that the post-optimizer saving is smaller than its named
pre-transform bloat. Review accepted that measured result because it remains a
clean reduction with neutral structure and no indirect-dispatch increase. The
decision does not admit unrelated sub-gate families or authorize combining
them merely to reach a numeric target.

The rejected `Structural mutation` row is the original core packing/commit
family. Patch 5 does not reopen it. Patch 5 targets the later-discovered
downstream typed projection shell, supported first by a 77,461-byte isolated
prototype and then by the 79,140-byte production A/B above.

## Ownership And Closeout

- IcyDB: retain the ordered-overlay iterator, concrete typed mutation
  projection terminals and shared live-page driver; together they reduce
  framework-owned repetition but do not yet make the exact actor installable.
- Canic: review role-specific managed-runtime generation and place final
  optimization before enforceable installability checks.
- Toko: adopt the concrete mutation projection terminals and shared live-page
  driver, replace the removed `ic-wasm optimize` invocation and split
  `project_instance` if the remaining framework-owned reductions cannot supply
  enough headroom. No machine-enforced Toko raw-Wasm budget was found in the
  frozen source.

The Patch 2 retained artifact improves code by 26,327 bytes relative to the
earlier 0.247 Binaryen result while raw Wasm grows by 93,511 bytes. That raw
comparison is dominated by the larger generated Candid/public surface, not the
retained ordered-overlay change. Patch 5 is instead a strict paired reduction
on the current 128,886-byte Candid surface.

Complexity delta: four production files, 254 insertions and 248 deletions
(net +6). One visitor/error-specialized merge function becomes one private
allocation-free iterator, and store-specific visitation remains at the store
boundary. The implementation shape stays neutral, with zero new runtime modes,
behavior axes, dependencies or persisted-format changes.

Patch 3 retained zero production lines and zero new types. Its experimental
iterator was removed after the exact A/B showed a structurally worse result.

Patch 4 changes 13 production Rust files by approximately +490/-19 lines,
including the 237-line fixed attribution owner, plus focused tests, one audit
endpoint and contract documentation. The implementation becomes more complex
by one explicit public terminal and one fixed result DTO, but adds zero runtime
modes, dependencies, persisted formats or retained-metrics state. Existing
planner, admission, executor, cursor and typed-decode authorities remain
singular.

Patch 5 changes one production Rust file by approximately +104 lines and adds
focused host and public facade compile-pass coverage. Two explicit public
terminals and two private concrete projection kernels replace downstream
mutation/cardinality/projection shells; only final generated row decode remains
entity-generic. This adds no runtime mode, dependency, indirect dispatch,
persisted format or new execution owner. The implementation is modestly more
complex inside IcyDB but removes 126 functions from the exact downstream actor.

Patch 6 changes zero production files. Cumulatively, the retained line changes
18 production Rust files by approximately +848/-267 lines. That width is the
direct sum of one private traversal convergence, one opt-in bounded attribution
surface and two concrete typed-mutation projection terminals. It adds no
runtime mode, dependency, persisted format, compatibility path or second
execution owner.

Patch 7 adds one 166-line production module and two small re-export edits, plus
focused unit/public-facade coverage and contract documentation. Two outward
lane-specific methods share one private `#[inline(never)]` kernel generic only
on `CanisterKind`; no entity type, callback, collection mode, dependency,
persisted format or execution owner is added. The IcyDB surface becomes
modestly more explicit while the exact downstream actor loses 14,776 raw bytes
and its generated adapters shed the repeated traversal state machines.

Patch 8 changes six production Rust files plus one focused test by
approximately +92/-46 lines before documentation. The ordinary projection
result becomes smaller, route capture remains one explicitly selected path and
the iterator performs fewer tombstone lookups. The implementation shape is
simpler on ordinary reads and adds no public surface, behavior axis, allocation,
dependency or persisted state.

## Patch Ledger

| Patch | Outcome | Status |
| ---: | --- | --- |
| 1 | Exact current baseline, platform result and named candidate ranking | Complete; ordered overlay selected in review |
| 2 | Allocation-free ordered-overlay iterator and exact frozen A/B | Complete; retained |
| 3 | Concrete covering-projection presence/window traversal | Complete; rejected and removed |
| 4 | Bounded query-safe operation-local attribution for dynamic and typed/fluent live pages | Complete; exact state, cost and dead-strip proof retained |
| 5 | Concrete typed mutation cardinality and accepted-row projection terminals | Complete; exact 79,140-byte downstream reduction retained |
| 6 | Original cumulative production qualification and closeout | Complete |
| 7 | Shared non-entity live-page advancement and extended closeout | Complete; exact 14,776-byte downstream reduction retained |
| 8 | Restore ordinary query cost and remove timestamp-sensitive attribution proof | Focused SQL, attribution, ordering and lint gates complete; cumulative artifact requalification pending |
