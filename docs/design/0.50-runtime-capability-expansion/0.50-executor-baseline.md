# 0.50 Executor Baseline

Status: In progress  
Scope: Slice 0 structural + behavioral baseline

## Purpose

This document stores baseline metrics used to measure simplification outcomes
across slices A-E.

## Structural Metrics

| Metric | Baseline | Measurement Method | Notes |
| --- | --- | --- | --- |
| executor module count | `213` total `.rs` files; `167` non-test module files | `rg --files crates/icydb-core/src/db/executor -g '*.rs'` (+ test-file filters) | Baseline line starts from non-test `167` |
| max LOC per module | `1,046` (`explain/descriptor.rs`) | `find ... -name '*.rs' ! -path '*/tests/*' ! -name 'tests.rs' | xargs wc -l | sort -nr` | No non-test module above `1,046` lines |
| LOC distribution | `24,774` total non-test LOC, avg `148.35`, p50 `103`, p90 `372`, p95 `428` | `find ... | xargs wc -l | awk ...` percentile summary | Distribution used for slice E size targets |
| continuation call surface count | `485` keyword matches across `50` non-test files | `rg -n \"continuation|resume|cursor token|token\" ...` | Heuristic coverage proxy; not API-only count |

## Dependency Baseline

| Metric | Baseline | Measurement Method | Notes |
| --- | --- | --- | --- |
| cross-module dependency edges | `14` non-self top-level edges (`29` including self edges) | top-level `executor::<module>` reference extraction constrained to module set from `executor/mod.rs` | First-pass static coupling baseline for A-E |
| continuation owners | `1` declared authority owner | ownership matrix | `executor::continuation` |
| orchestration owners | `1` declared authority owner | ownership matrix | `executor::pipeline::orchestrator` |

## Query/Plan Benchmark Baseline

| Benchmark | Baseline Result | Command | Notes |
| --- | --- | --- | --- |
| executor regression suite | `pass` (`588 passed; 0 failed; 1054 filtered; finished in 6.75s`) | `cargo test -p icydb-core db::executor::tests:: -- --nocapture` | Run on `2026-03-11` |
| explain regression suite | `pass` (`32 passed; 0 failed; 1610 filtered`) | `cargo test -p icydb-core db::query::explain::tests:: -- --nocapture` | Run on `2026-03-11` |
| invariants suite | `pass` | `make check-invariants` | `index-range`, `layer-authority`, `architecture-text-scan`, `memory-id`, `field-projection` all green |

## Target Simplification Signals

- executor module count (non-test): `167 -> <=145`
- max module LOC: `1,046 -> <=800`
- cross-module dependency edges (non-self, top-level): `14 -> <=10`
- continuation owners: `1 -> 1` (preserve single-owner authority)
- orchestration owners: `1 -> 1` (preserve single-owner authority)
