# 0.50 Status Tracker

Current state: slice D ready to start (slice 0 review hold still open).

## Planned slices

- [ ] 0. preflight guardrails
- [x] A. continuation authority consolidation
- [x] B. pipeline/orchestrator simplification
- [x] C. grouped runtime duplication elimination
- [ ] D. kernel/operator relocation
- [ ] E. executor LOC reduction sweep (mechanical only)

## Active Slice: 0. Preflight Guardrails

Goal: lock contracts and establish baseline measurements before structural
executor refactors begin.

### Core invariants to lock

- plan semantics: identical plan -> identical execution behavior
- continuation semantics: resume tokens remain stable and monotonic
- explain semantics: explain node correlation remains deterministic

### Slice 0 checklist

- [x] 0.1 lock invariants in docs and slice notes
- [x] 0.2 capture executor module ownership matrix
- [x] 0.3 capture cross-module dependency graph
- [x] 0.4 map continuation call surface and owners
- [x] 0.5 add/confirm characterization coverage for continuation, grouped runtime, terminal behavior
- [x] 0.6 add/confirm plan + explain snapshot regression coverage
- [x] 0.7 record structure baseline metrics (module count, max LOC per module, LOC distribution)
- [x] 0.8 record baseline query/plan benchmark runs
- [x] 0.9 declare authority owners for continuation + orchestration in writing

### Slice 0 artifacts

- [x] `docs/design/0.50-runtime-capability-expansion/0.50-slice-0-notes.md`
- [x] `docs/design/0.50-runtime-capability-expansion/0.50-executor-ownership-matrix.md`
- [x] `docs/design/0.50-runtime-capability-expansion/0.50-executor-baseline.md`
- [x] `docs/design/0.50-runtime-capability-expansion/0.50-slice-a-continuation-authority.md` (prepared for next slice)

### Suggested command set

- `rg --files crates/icydb-core/src/db/executor | wc -l`
- `rg -n "continuation|resume|cursor token|token" crates/icydb-core/src/db/executor`
- `cargo test -p icydb-core db::executor::tests:: -- --nocapture`
- `cargo test -p icydb-core db::query::explain::tests:: -- --nocapture`
- `make check-invariants`

### Exit gates for Slice 0 completion

- [ ] invariants are explicitly documented and reviewed
- [x] continuation and orchestration each have one declared authority owner
- [x] baseline structure metrics are captured in-repo
- [x] characterization and snapshot regressions pass
- [x] no executor semantic changes were introduced during Slice 0

Review hold:

- pending explicit reviewer sign-off on invariant wording and target thresholds

## Slice A Summary

Slice A design/start doc:

- `docs/design/0.50-runtime-capability-expansion/0.50-slice-a-continuation-authority.md`

Immediate execution order:

- [x] A1 classify continuation call sites (authority vs consumer vs derivation)
- [x] A2 move policy derivation helpers under continuation authority
- [x] A3 tighten continuation API surface exposed to route/pipeline
- [x] A4 re-run continuation + explain + invariant verification

A-slice progress note:

- A2 complete: route-continuation derivation and mutation-route initial continuation construction moved under continuation authority, with route planner/contracts rewired and targeted checks/tests green.
- A3 complete: continuation constructor exposure tightened (`RouteContinuationPlan::new` internalized and `from_scalar_access_window_plan` narrowed to continuation visibility), and non-continuation construction leakage was re-scanned.
- A4 complete: cursor-validation, explain, route-mutation tests, and invariant checks all pass on this slice state.

## Active Slice: B Pipeline/Orchestrator Simplification

Slice B design/start doc:

- `docs/design/0.50-runtime-capability-expansion/0.50-slice-b-orchestrator-simplification.md`

Immediate execution order:

- [x] B1 normalize mode branching in orchestrator/payload stages
- [x] B2 reduce entrypoint surface adapter boilerplate
- [x] B3 audit stage-state boundary field access and tighten remaining leaks

B-slice progress note:

- B1 complete: centralized mode branching behind `LoadExecutionMode` helpers and normalized stage envelope transitions in orchestrator stage loop.
- B1 validation complete: route load matrix + pagination limit-no-cursor matrix + invariants all pass.
- B2 complete: scalar/grouped entrypoint surface projection helpers now centralize adapter-shape invariant handling.
- B3 complete: no remaining raw mode-field branch reads in orchestrator + entrypoint paths.

## Active Slice: C Grouped Runtime Duplication Elimination

Slice C design/start doc:

- `docs/design/0.50-runtime-capability-expansion/0.50-slice-c-grouped-runtime-duplication.md`

Immediate execution order:

- [x] C1 flatten grouped runtime projection wrapper
- [x] C2 grouped ownership boundary audit
- [x] C3 grouped continuation surface tightening

C-slice progress note:

- C1 complete: removed `GroupedRuntimeProjection` and flattened grouped route/runtime projection into `GroupedExecutionContext`, reducing grouped runtime layering.
- C1 validation complete: grouped wrapper matrix + grouped semantic type preservation + invariants all pass.
- C2 complete: grouped ownership boundaries were audited and cross-boundary grouped type imports migrated off pipeline re-export paths.
- C3 complete: grouped continuation/pagination re-exports removed from pipeline grouped-runtime module and consumer imports retargeted to continuation-owned boundaries.

## Next Slice: D Kernel/Operator Relocation

Slice D design/start doc:

- `docs/design/0.50-runtime-capability-expansion/0.50-slice-d-kernel-operator-relocation.md`

Immediate execution order:

- [ ] D1 kernel/operator responsibility audit
- [ ] D2 relocate operator mechanics from kernel where ownership belongs to pipeline operators
- [ ] D3 lock and verify kernel/operator contract boundaries

## 0.50.0 Push Readiness

Line-open push is ready when both conditions are true:

- [ ] Slice 0 review hold is cleared (invariants + thresholds explicitly approved)
- [x] Slice A kickoff plan doc exists and is linked from status
