# IcyDB 0.237 Accepted-Schema Runtime Observation And Cold-Root Scaling Status

Status: CLOSED — MEASURED NO-BUILD — NOT RELEASED

Last updated: 2026-08-22

Design authority: [0.237 design](./0.237-design.md)

## Closeout Outcome

0.237 was measured and was not released. Patch 2 correctly removed repeated
accepted-bundle selection, identity-closure validation, entity projection, and
warm root observation work. Patch 3 proved that this work was not a material
contributor to complete production-shaped cold-root cost: first maximum-shape
accepted-schema read improved by only 1,804,019 instructions, from 95,346,651
to 93,542,632 (1.892%), against the frozen minimum 25% improvement. The
2,048-row/three-index recovery control regressed by 736,155 instructions, from
3,952,036,708 to 3,952,772,863 (0.019%), which is flat within noise.

No correctness failure or public-behavior need justifies retaining the
candidate. All Patch 2 production/test work and Patch 3 integration,
schema-audit, and canister-audit fixtures were reverted. Runtime behavior,
generated surfaces, Candid, exports, stable formats, and package versions are
the exact `v0.236.1` boundary. No further implementation under this
accepted-schema design is authorized; future accepted-schema work requires new
baseline evidence and a separately accepted design. The later explicit
reassignment of the unreleased 0.237 number to the independent SQL performance
audit does not reopen this design.

## Released Baseline

- exact released predecessor: `v0.236.1` at
  `2abc8af306743138b9d35b1c0903ef596c985f2c`;
- worktree inspection began from that exact code; unrelated user-owned moves of
  historical 0.231-0.234 design directories were left untouched;
- no Cargo package/workspace version, runtime source, generated file, Candid,
  stable format, or release changelog changed in Patch 1; and
- exact release artifact measurements are recorded below and in the corrected
  0.236 status.

| Surface | Compiler Wasm | Final raw Wasm | Gzip | Candid | Exports |
| --- | ---: | ---: | ---: | ---: | ---: |
| Typed query | 2,419,238 | 2,118,718 | 812,164 | 64 | 6 |
| SQL query | 3,726,899 | 3,284,298 | 1,298,460 | 57 | 6 |

The build reports name exact source revision
`2abc8af306743138b9d35b1c0903ef596c985f2c`. Their dirty flag is caused only by
the unrelated documentation moves above; Rust and Cargo inputs match the tag.

## Predecessor-Surface Findings

The released predecessor runtime root was already one immutable database-wide
authority and remains so. Its repeated work was inside the observation/build
flow:

- warm lookup captures and sorts every store root before consulting the
  runtime-root cache;
- cold construction enumerates accepted entities from verified store bundles;
- each entity then re-enters current accepted catalog selection, causing
  selected-root/bundle/identity-closure work to repeat; and
- every entity runtime is compiled before the root is published.

`SchemaStore::accepted_bundle_cache` already retains the selected verified
bundle, value catalog, and entity selections and is invalidated by accepted
root-key mutation and recovery projection reset. The maintained
`current_accepted_schema_authority_matches` path already treats a cached
selection as the current in-memory witness. This is sufficient authority for a
cheap warm check and a single-pass cold store projection; a second cache or
generation is unnecessary.

## Frozen Decisions

1. Cold root validity remains global. Every accepted entity still compiles
   before root publication, and unrelated corruption still fails the root.
2. Each store's current accepted bundle/closure is selected once per cold root,
   and every entity selection is derived from that same verified authority.
3. Each accepted entity runtime compiles exactly once per cold root.
4. Warm lookup first uses the existing `SchemaStore` bundle-cache selection as
   a witness. A fully witnessed match performs no stable root read or rebuild.
5. Missing, mismatched, or absent-root witnesses take the sole authoritative
   full capture; they never prove freshness. Borrow conflicts and corruption
   fail closed. No absent-root cache state is added.
6. Target and unrelated schema publication both invalidate the database-wide
   root. Granular invalidation is not added.
7. SQL DDL call-site invalidation may be deleted only after the existing
   `SchemaStore` root-key invalidation is proven complete across publication,
   replay, fold, reset, and retirement.
8. No lazy entity validity, extra cache, epoch, persisted derivative, generated
   fallback, public surface, mode, job, timer, or compatibility path is allowed.

## Retained Evidence

- 0.227 fully ready populated post-upgrade: 10,593,191 instructions against a
  10,750,000 gate.
- 0.228 three-index recovery: 21,617,488,641 instructions isolated versus
  45,190,180,079 with an unrelated same-store 64-index entity.
- 0.236 unchanged maximum-prefix route: about 5.288 million instructions, 65
  index entries, zero row fetches.
- 0.236 unchanged sparse intersection: about 2.264 million instructions, 41
  entries, one row fetch.

These are need/provenance evidence. The exact `v0.236.1` structural phase
baseline captured before Patch 2 changed production logic appears below.

## Patch Tracker

| Patch | Outcome | Status |
| --- | --- | --- |
| 1 | Close 0.236 release provenance; audit accepted root/bundle/cache/publication/recovery authority; freeze the hard-cut design, performance gates, and complexity stop conditions | Complete |
| 2 | Implement single-pass cold store projection, existing-witness-first warm reuse, direct corruption/publication/recovery proofs, and preliminary size/performance evidence through the one runtime root | Measured; reverted |
| 3 | Record production-shaped IC scaling, upgrade/recovery, stable-state, Candid/export, final Wasm/complexity, and independent closeout evidence | Complete; acceptance failed; fixtures reverted |

These were the complete intended patches for this accepted-schema attempt. The
design is closed as a measured no-build; generic continuation does not
authorize more accepted-schema implementation.

## Reverted Patch 2 Implementation Evidence

The now-reverted implementation replaced the old per-entity re-entry and
capture-before-cache order in place:

- cold construction retains the selected physical root with each existing
  `StoreHandle`, admits the selected bundle once, and derives every runtime
  entity plus catalog selection from that same verified borrow;
- `AcceptedSchemaEntityRuntime` consumes that preselected catalog authority and
  still compiles every inspection plan, row contract, `SchemaInfo`, and
  `EntityAuthority` before one complete root is published;
- a cached runtime root first compares every non-empty retained store root with
  the existing verified bundle-cache selection, without a stable read;
- a missing, mismatched, empty-root, or topology witness falls back to the sole
  full store-root capture, while borrow/corruption errors remain typed failures;
  and
- root-key mutation remains the sole cache-invalidation owner. Seven SQL DDL
  call-site invalidations and the session invalidation method were deleted; no
  callback, epoch, new cache, or second publication route replaced them.

Test-only phase evidence records:

| Shape | Root selections | Bundle selections | Identity closures | Entity projections | Compilations |
| --- | ---: | ---: | ---: | ---: | ---: |
| `v0.236.1` one-store cold | 4 | 2 | 2 | 1 | 1 |
| Patch 2 one-store cold | 2 | 1 | 1 | 1 | 1 |
| Patch 2 one-store warm | 0 | 0 | 0 | 0 | 0 |
| Patch 2 two-entity store projection | 0 | 1 | 1 | 2 | n/a |
| Patch 2 unrelated two-store publication rebuild | 4 | 2 | 2 | 2 | 2 |

The warm assertion is repeated across 1,000 new request sessions. A registered
store with no accepted root deliberately performs one root selection on a warm
reuse attempt, then reuses the matching database root without recompilation.
Target and unrelated publication both replace the root while old contexts keep
their immutable old identity. The maintained PocketIC DDL journey confirms
that publication still updates `DESCRIBE`, `EXPLAIN`, and indexed reads after
removing caller-side invalidation. The focused journaled replay/fold test also
proves that projection reset clears a populated bundle witness before reopening
the folded accepted root.

Focused no-default, SQL-only, and all-feature core checks passed during the
candidate. All-feature core Clippy was warning-clean. These results document
the discarded candidate; they do not describe retained runtime code.

## Patch 3 IC And Lifecycle Evidence

The closeout fixture holds `PerfAuditUser` and its query fixed while retaining
one unrelated 64-index entity in the same store and an identical 64-index
entity in another store. Exact predecessor and candidate worktrees use their
committed runtime and lockfile inputs plus the same audit-only fixture change.

| Probe | Exact `v0.236.1` | `0.237` candidate | Delta |
| --- | ---: | ---: | ---: |
| First maximum-shape accepted-schema read | 95,346,651 | 93,542,632 | -1,804,019 (-1.892%) |
| 2,048-row/three-index upgrade recovery | 3,952,036,708 | 3,952,772,863 | +736,155 (+0.019%) |

The structural optimization is real, but neither complete production-shaped
cold probe approaches the frozen 25% improvement requirement. The accepted
design therefore requires the line to stop for review. No lazy entity
validity, granular root cache, new epoch, or raised instruction ceiling was
added to manufacture a larger result.

The final candidate lifecycle journey separately proves the maintained warm
boundary after an update message deliberately retains the heap cache:

- warm target reads use 537,134 instructions initially, 538,423 after target
  publication, 542,687 after unrelated-store publication, and 547,257 after
  same-Wasm upgrade/recovery; every delta stays inside the greater-of-3%-or-
  250,000 gate;
- target and unrelated DDL publication use the existing catalog-native path,
  and every following cold observation resolves the complete current root;
- the fixed target query returns the same result with identical structural,
  data-store, and index-store work across initial, publication, and upgrade
  phases;
- both unrelated entities retain exactly 64 accepted indexes; and
- accepted-root reads leave the 578,879,488 stable-byte fixture unchanged,
  while same-Wasm upgrade adds no stable extent.

The production-shaped three-index recovery still completes in one successful
watchdog work sample below the maintained 30-billion ceiling. This is safety
and lifecycle evidence, not evidence that the 0.237 performance gate passed.

## Reverted Candidate Artifact And Complexity Evidence

The discarded candidate artifacts compared with exact released
`v0.236.1` as follows:

| Surface | Released raw Wasm | Final raw Wasm | Delta | Released gzip | Final gzip |
| --- | ---: | ---: | ---: | ---: | ---: |
| Typed query | 2,118,718 | 2,117,024 | -1,694 | 812,164 | 811,573 |
| SQL query | 3,284,298 | 3,286,681 | +2,383 (0.073%) | 1,298,460 | 1,302,563 |

Compiler Wasm is 2,417,685 bytes for typed and 3,729,609 bytes for SQL.
Candid remains 64/57 bytes and both production surfaces retain six exports.
The authoritative raw deltas remain far inside the smaller-of-32-KiB-or-1%
stop gate. These final artifacts include the pre-existing compatible
`Cargo.lock` refresh in the working tree; the exact-lock IC comparison above
uses each revision's committed lockfile instead.

Six production/test files change by approximately `+722/-103` lines. Test
fixtures and test-only counters own most of the increase. Maintained runtime
code grows by approximately 170 net lines, below the review signal, while
removing per-entity accepted-authority re-entry and the session/DDL
invalidation policy. There is no new production module, public/persisted state,
mode, setting, cache, epoch, or compatibility path, so architectural complexity
is neutral-to-lower despite the explicit captured-root proof.

Patch 3 changes three audit fixture/integration files by approximately
`+344/-17` lines before documentation. Production runtime source remains
unchanged. The extra width is test evidence: a second 64-index fixture, two
test-admin measurement/publication endpoints, and one lifecycle journey.
Independent behavior axes, public/persisted states, production owners, and
technical-debt entries added: none. The implementation structure remains
neutral; the runtime remains simpler than the predecessor, but the measured
benefit is insufficient for the frozen acceptance contract.

## Performance And Complexity Gate Result

Patch 2's now-removed test-only phase counters established the structural
result: one verified bundle/closure selection per store, one compilation per
entity, and zero stable root/bundle/compile work on a fully witnessed warm hit.

The large-unrelated fixture must hold target query/result/physical work fixed.
Warm and target-owned cold phases remain within the greater of 3% or 250,000
instructions of the small-unrelated control. Complete large-unrelated cold work
must improve at least 25% against its fresh predecessor measurement or stop for
review. Patch 3 met the warm/result/physical-work requirements but measured
only a 1.892% first-read improvement and a 0.019% recovery regression, so the
stop condition is active.

The preferred implementation goal was non-positive runtime-source and raw-Wasm
growth. The explicit captured-root proof costs approximately 170 net maintained
runtime lines, below the 200-line review signal, while raw-Wasm remains well
inside the smaller-of-32-KiB-or-1% stop gate. A new production module or
behavior axis remains forbidden. Candid, exports, stable state, and public APIs
remain unchanged.

## Closed Boundary

- the 0.237 runtime optimization and all measurement fixtures are reverted;
- the frozen gate is not weakened and no instruction ceiling is raised;
- release notes are removed because no 0.237 release exists; and
- any future optimization requires current evidence and a separate design.

## Current Verdict

The accepted-schema 0.237 attempt is **CLOSED — MEASURED NO-BUILD — NOT
RELEASED**. Warm reuse,
publication invalidation, upgrade/recovery, stable state, physical query work,
raw Wasm, Candid, exports, structural counts, and complexity bounds passed for
the candidate, but the required complete cold-path improvement did not: 1.892%
is materially below 25%, while recovery is flat within noise. With no
correctness or public-behavior reason to retain it, the runtime implementation
and audit fixtures were reverted completely.
