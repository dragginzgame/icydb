# IcyDB 0.219 Implementation Status

Status: Patch 1 complete; Patch 2 pending

Design: [0.219-design.md](0.219-design.md)

Design cut: 2026-08-03

Implementation baseline: released 0.218.1

## Baseline

- Tag: `v0.218.1`
- Commit: `e05f4a9707952545402284c4933b27276f2b97b9`
- Branch: `main`
- Package version: `0.218.1`
- Worktree at kickoff: clean
- Facade feature graph: default empty; optional `sql`, `diagnostics`, and
  `migration` capabilities unchanged from the baseline

The code-owned numeric vocabulary and staged per-code owner inventory are
recorded in
[0.219-diagnostic-fact-inventory.md](0.219-diagnostic-fact-inventory.md).

No Cargo package version is changed by implementation landings. Release
tooling owns version bumps.

## Landing Plan

| Patch | Bounded outcome | Canonical owner | Expected delivery domains | Focused validation | Public/format/cost impact | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1. Compact fact contract and public hard cut | Freeze the fact registry and bounds, add the common public fact record, hard-cut `Error.constraint` to `Error.facts`, and provide numeric CLI fallback | `icydb-diagnostic-code`, core error projection, public facade | Other Core, Build / Canister, Integration Tests | registry/Candid tests, facade conversion tests, minimum-feature checks, raw Wasm baselines | Public Candid hard cut; no persisted format; first measured common Wasm delta | Complete |
| 2. Cursor and recovery pilot | Preserve already-owned cursor decode/signature/window and database-format/marker facts through the common boundary | cursor and recovery error owners | Executor / Planner, Other Core, Integration Tests | focused cursor, database-format, facade, CLI tests; warnings-denied Clippy | More informative E6/E19; no semantic/persisted change; error-path-only cost | Pending |
| 3. Mutation, bounds, and stale-authority context | Add operation counts/byte limits, deterministic duplicate positions, field/entity IDs, and expected/current accepted revisions | mutation scheduler/session and accepted-schema authority | Lowering / Session, Executor / Planner, Integration Tests | mixed-batch negative tests, structural/typed/SQL parity, stale-cache tests | More informative E196/E233-E240 and stale conflicts; no key/value exposure | Pending |
| 4. Query planner and SQL context | Project existing order/group/expression/cursor-adjacent indexes, kinds, arity, widths, counts, and parameter positions | query validation/planner and SQL lowering/session | Parser, Lowering / Session, Executor / Planner, Integration Tests | focused planner/lowering/write-boundary tests and query-only/SQL feature rows | E3 and SQL errors gain numeric parameters; no query/response success change | Pending |
| 5. Accepted-constraint hard cut and finding split | Move E223/E225 onto facts, thread operation/batch position, remove immediate names/key bytes, and separate historical validation finding output | accepted constraint scheduler/evaluators and public schema progress DTOs | Lowering / Session, Executor / Planner, Other Core, Integration Tests | all constraint families, batch parity, activation/replay tests, public Candid tests | Public error and finding DTO hard cuts; durable validation codec unchanged; likely removes error-path string work | Pending |
| 6. Tooling resolver and diagnostic artifact | Add exact fingerprint-bound introspection identity, bounded host artifact export/import, schema-aware CLI rendering, and numeric fallback | CLI/tooling and explicit schema introspection | Lowering / Session, Build / Canister, Integration Tests | wrong/missing/stale artifact tests, introspection-disabled fallback, downstream CLI fixtures | Introspection DTO extension and host-only artifact; no runtime authority/persisted format | Pending |
| 7. Storage/index/relation completion and closeout | Add safe component/layout/slot/index/relation facts, justify remaining code-only errors, audit dependency/text leakage, and close with final raw Wasm attribution | store/codec/commit/index/relation owners plus diagnostic closeout | Executor / Planner, Other Core, Build / Canister, Integration Tests | focused corruption/component tests, feature matrix, source guards, dependency tree, warnings-denied Clippy, final Wasm report | No raw bytes/keys/values; no persisted change; complete measured complexity/Wasm verdict | Pending |

Seven patches match the repository's normal minor-line target. Each patch is
one reviewable worktree handoff and stops before the next patch begins.

## Frozen Cross-Patch Invariants

- E-code values remain stable.
- The E-code owns the reason; facts carry parameters only.
- One `tag: u8, value: u64` fact representation serves every immediate error.
- The public fact sequence has a global bound of 80 and tighter per-code bounds.
- No fact contains a name, path, principal, SQL text, value, key, row, arbitrary
  bytes, hash of user data, or rendered/debug text.
- Full accepted-schema fingerprints may be carried only to bind schema-owned
  numeric identities to exact host metadata.
- `ConstraintDiagnostic` does not remain as a second immediate-error payload.
- Historical validation findings retain the bounded row locator required by
  their explicit operational lifecycle but do not constrain ordinary errors.
- No feature changes the public error ABI.
- Host renderers and artifact parsers do not enter production canister
  dependencies.
- Successful execution paths allocate no diagnostic facts.
- No persisted database format changes.
- No compatibility alias, dual decoder, or legacy fact spelling is added.

## Required Cost Evidence Per Patch

Every implementation handoff reports:

- files touched and approximate line delta;
- primary delivery domains;
- whether complexity became simpler, neutral, or more complex;
- raw non-gzipped Wasm bytes and delta for affected maintained subjects;
- gzip only as secondary context;
- whether successful-path behavior or instruction cost changed; and
- exact focused validation plus user-owned skipped full-suite validation.

## Patch 1 Entry Conditions

Patch 1 may begin only after:

1. 0.218 has a reported ready/complete closeout verdict;
2. the user explicitly directs implementation of 0.219;
3. HEAD, package version, feature graph, and dirty worktree are recorded;
4. the public Candid baseline and representative raw Wasm baselines are
   captured; and
5. the complete numeric fact-tag and per-code schema inventory is checked in
   beside this tracker.

All five entry conditions were satisfied before the public error edit. The
production baseline is:

| Subject | Raw Wasm bytes | SHA-256 | Candid bytes | Candid SHA-256 |
| --- | ---: | --- | ---: | --- |
| `one_entity_typed_query` | 2,993,598 | `6975de8f66633832578e36e5ed1641c673a5b16a5aaf78a1f44d11b7cae001aa` | 64 | `1005c63f5415d1de4bb7da89190f33212d1c926c881108a1acfeadfa93fff766` |
| `one_entity_dynamic_query` | 2,962,097 | `690b857df87125ad6047e2282e352099ea8808e63ef8e07fa06afa472afbb0c6` | 66 | `b5a769d032002fe7a2f32c3b30bbbdf6a28456deea7bfe721a0756afe2863757` |
| `sql_perf` | 4,600,938 | `89927fabe28c31cfaedf84675c21415f61dabd24187abb9aed86aecf5a6ea043` | 42,350 | `b32a6a869a23545ef5f6c2aa0671d10566518a57d4cf627605235e004d2ef8d0` |
| `sql` | 4,067,544 | `9b44ae823c21c8f4164cbfdd2144ffb207fc334dcd850ec59ce7e221f5c4493c` | 26,501 | `719521614bd37ca4efe2a92ce98c2dfc1fb3f25f230392daf5cf29a3e25443ac` |

The typed and dynamic subjects each export one maintained application query.
The SQL performance and mutation-capable SQL export sets were captured from
the same exact production builds and are compared again at handoff.

## Patch 1 — Compact Fact Contract And Public Hard Cut

Status: complete.

### Implementation

- `icydb-diagnostic-code` owns one contiguous fixed registry of 89 numeric fact
  tags, the global 80-fact bound, and exact big-endian accepted-ID pair packing.
  It retains no Candid dependency, fact labels, prose, or subsystem renderer.
- Public `Error` is now exactly `code`, `class`, `origin`, and
  `facts: Vec<DiagnosticFact>`. `DiagnosticFact` is exactly `tag: u8` and
  `value: u64`; the former optional immediate `ConstraintDiagnostic` field and
  `constraint_diagnostic()` accessor are removed without an alias or dual wire
  shape.
- Existing producers emit an empty fact sequence at this landing. Typed family
  projections activate in Patches 2 through 7 at their existing subsystem
  owners; Patch 1 does not manufacture context at the facade.
- The CLI renders every known tag with one host-owned numeric label and every
  unknown tag as `tag#N=value`. Production canister crates do not acquire the
  label table or formatting path.
- Historical constraint-validation output retains its separate operational
  `ConstraintDiagnostic` payload until the Patch 5 finding split. It is no
  longer reachable through ordinary `Error`.
- Both maintained transitive Candid goldens carry the current public hard cut.
  The migration-only golden drops the rich constraint types that were
  reachable solely through the retired error field.

### Format And Behavior

- This is a breaking public Candid change. Downstream actors must regenerate
  bindings for the sole current pre-1.0 error record.
- E-code values 1 through 268, class/origin semantics, authorization, success
  responses, execution behavior, and every persisted database format are
  unchanged.
- Fact construction is error-path-only. Patch 1 adds no allocation or new
  branch to successful query, mutation, commit, or recovery execution.

### Cost Evidence

| Subject | Baseline raw bytes | Patch 1 raw bytes | Delta | Patch 1 SHA-256 | Export count |
| --- | ---: | ---: | ---: | --- | ---: |
| `one_entity_typed_query` | 2,993,598 | 2,990,500 | -3,098 | `18427c06cc71636d39c2a4ef076924ec3e7646e22ecd4e6046b742cb822953fc` | 1 |
| `one_entity_dynamic_query` | 2,962,097 | 2,961,282 | -815 | `7eecaf434335a90949c81947431a447a59c304487e9c9d0331587c12ac46ee71` | 1 |
| `sql_perf` | 4,600,938 | 4,599,597 | -1,341 | `7313d93960cb6ed5c04f92532c7e7b1d5fd655f8f7d487c7c6311aece3447dda` | 48 |
| `sql` | 4,067,544 | 4,067,701 | +157 | `3ec6e9ff174e8edfc20ce74d9dbeace0cfbd556ec3d67ae767c17a0dcfbabc76` | 10 |

The two query-only Candid files remain byte-identical because their custom
methods do not expose `Error`. The SQL performance and SQL Candid files each
grow by 254 text bytes for the current `DiagnosticFact` declaration and field;
their SHA-256 values are respectively
`7fab9f6f8df73c137bcd40ab7b44d6e08ee9ed83ce246656364c97547bab4ba8`
and `9909e7824bb9f28a9bb737fec8238c3d3a05e23f0634bb69d14be50e9d9959c3`.
All raw Wasm export sets are unchanged.

### Validation

- The diagnostic-code registry tests pass, including fixed contiguous tags and
  exact pair packing.
- The complete facade library tests with SQL pass, including exact Candid
  record shapes and numeric fact round-trip.
- CLI diagnostic tests pass with known and unknown numeric fallback.
- Both transitive Candid ABI golden tests pass.
- Warnings-denied Clippy passes for the diagnostic-code crate, the facade with
  no default features and all features, and the CLI with all targets.
- All four exact production `wasm-release` builds pass with Candid export on.
- Formatting and diff hygiene pass. Full workspace tests and `make test` remain
  user-owned push validation under repository policy.
