# 0.230 Durable Exact Cardinality Generations Closeout Audit

Audit date: 2026-08-18

Released gate: `v0.230.3`, commit
`eb36bdb4281de873f62e71df8c7142c856232eb2`, tree
`f4f2a3b7c3725330381f9ffbfbeff98eda840198`

Accepted predecessor: `v0.229.1`, commit
`cfba69b013d635b0ae09238bbea2bfe05081ee7c`, tree
`a11bed3a4e4ac5ff4330dfe7e27d944987a05807`

## Findings

### Critical, resolved: `v0.230.3` binds the corrected released source

The immutable `v0.230.2` tag identifies the preceding candidate and does not
contain the later audit corrections. The corrected source candidate is commit
`8e72cbb3c4ac1697a5f2aa13986970ad00af0901`. The user-owned release flow
validated that exact candidate, performed the mechanical version transition,
and produced release commit `eb36bdb4281de873f62e71df8c7142c856232eb2`,
tree `f4f2a3b7c3725330381f9ffbfbeff98eda840198`.

`HEAD`, `main`, `origin/main`, and annotated tag `v0.230.3` all resolve to that
release commit. The candidate and release-gate receipts verify, and
the recorded release-transition diff SHA-256 is
`724b9e6b3bc7cd90d062954c8649765d231ee3dd2b820364543f70e581437d20`.
`icydb 0.230.3` is available from crates.io. The audit did not create, move,
push, or publish the release.

### High: journaled consumers accepted volatile counts as authority

Pre-audit `StoreHandle::exact_entity_count` and
`StoreHandle::exact_user_index_prefix_count` returned populated volatile
full-count caches before reading or validating the durable generation. This
bypassed generation state, source identity, watermark, accepted index identity,
full digest, and build-cursor validation. It contradicted the one-authority
contract even when the volatile value happened to agree.

The correction is in `crates/icydb-core/src/db/registry/handle.rs` at
`StoreHandle::exact_entity_count`, `exact_user_index_prefix_counts`, and
`ready_cardinality_counts`. Heap stores retain their volatile exact-count
convenience. Journaled stores require one validated `Ready` base plus the exact
signed post-watermark delta.

### High: partial accepted user-index prefixes were rejected

The consumer predicate required prefix component count to equal full index
arity. A `k`-component accepted index persists all nonempty prefixes `1..=k`, so
the predicate incorrectly made shorter valid prefixes unavailable.

`CardinalityBuildAuthority::accepts_user_index_prefix` in
`crates/icydb-core/src/db/schema/cardinality_build.rs` now admits precisely
`1..=accepted_arity`. The regression
`accepted_user_index_authority_covers_every_non_empty_component_prefix` covers
zero, every valid prefix, over-arity, and unknown identity.

### High: a newly accepted live root did not invalidate consumers immediately

Consumer authority was derived only from the folded canonical accepted root.
After a successor root published into the positioned live overlay but before
its batch folded, the preceding canonical root and old cardinality header could
still validate together.

`CardinalityBuildAuthority::derive_for_current_consumer` now compares the
effective accepted authority with the canonical accepted-root selection and
makes evidence unavailable on drift. The journaled regression in
`crates/icydb-core/src/db/session/write.rs` proves immediate invalidation and
proves that populated volatile entity and prefix caches cannot rescue the stale
evidence.

### High: bounded child enumeration bypassed the proof boundary

`expand_index_prefix_family_with_exact_child_prefixes` called `IndexStore`
directly. That made a volatile-only child set planner-visible without durable
source or count proof. It now calls
`StoreHandle::exact_user_index_child_prefixes_for_parent_set`. For journaled
stores the volatile structure may propose a bounded set, but synchronized
durable parent and child totals must prove the proposal complete before use.

Same-index prefix families are validated in one batch through
`StoreHandle::exact_user_index_prefix_counts`; direct `COUNT`, empty pruning,
prefix sums, covering execution, and bounded child enumeration therefore share
the same proof boundary.

### High, corrected: consumer proof redundantly decoded the accepted bundle

The first correctness-only correction made each exact consumer reread and
decode the complete canonical accepted bundle. The maintained
`sql_perf_token_branch_set_limit50_pressure_beats_overcap_fallback` regression
therefore measured 125,867,260 execute instructions for the exact wide branch
against 16,474,822 for the conservative fallback. Batching same-index proofs
reduced that to 17,413,664 versus 16,402,920 but still failed the contract.

The first correction borrowed the existing root-keyed verified effective
bundle only after an independent canonical root read proved the effective and
canonical selections identical. It added no cardinality cache, proof lifetime,
persisted state, or alternate authority and avoided cloning the bundle. Its
exact rerun passed at 5,357,533 execute instructions versus 10,379,743 for
fallback, about 48 percent lower. The complete durability slice then passed all
88 tests at that audit cut.

### High, resolved: the broad gate exposed a maximum-fanout instruction regression

The later user-owned validation log
`target/validation-failures/20260818T122729Z-35746-7-test.log` measured
`prefix_family_max_fanout` at 6,264,502 instructions, above its unchanged
5,150,250 regression ceiling. This ceiling is a maintained query-cost budget,
not the IC's 40-billion message limit; exceeding it still blocks release
because it proves that exact evidence made a supported query materially more
expensive.

The repeated cost came from rebuilding source proof through stable authority
reads already admitted earlier in the same request. The correction keeps
`StoreHandle` as the proof boundary but carries the request-admitted accepted
root and database incarnation, binds the exact positioned index delta to its
canonical fold watermark, batches root/control reads, precomputes the store
allocation identity, and reuses accepted-domain/header decodes only while the
full root identity or exact raw stable bytes still match. No cached count,
estimate, generated model, or alternate generation becomes authority.

The exact post-correction rerun passes at 5,075,414 instructions: 74,836
instructions, or about 1.45 percent, below the frozen ceiling. The margin is
tight but deterministic and is not an operational IC-limit concern; it remains
a useful regression guard against adding another stable proof read.

### Medium, resolved: two schema fixtures skipped required exact-count readiness

The same broad log failed
`generated_startup_driver_resumes_pending_activation_and_promotes_without_source_model`
and
`generated_startup_driver_persists_e223_for_a_retained_historical_finding`
with internal code 16. Both fixtures attempted a successor schema application
after committing canonical rows but before startup recovery and the bounded
cardinality builder had published the exact `Ready` evidence now required by
schema admission.

The tests now drive the existing recovery owner to completion and the existing
bounded cardinality owner to `Ready` before requesting the successor. Both
tests pass with no default features and with `sql,diagnostics,migration`.
Production admission was not weakened and no test-only alternate authority was
introduced.

### Medium, resolved: optimized proof metadata needed explicit fail-closed tests

The performance correction added two volatile optimizations that are not count
authority: a fold-watermark binding on the positioned index delta and a decoded
header cache guarded by equality with the complete raw stable header. New
regressions prove that stale/future watermark movement is rejected and that a
changed malformed raw header is decoded and rejected instead of being served
from cache.

### Medium, resolved: the `ic-testkit` 0.8 pool migration did not compile

The final 65,536-key rerun initially failed to compile because three
integration fixtures still called the removed `acquire_with_outcome` pool
method. `ic-testkit` 0.8 exposes the same structured guard/outcome tuple through
`acquire`. The three callers now use that maintained method. Both affected
test binaries compile, and the 65,536-key test passes. This correction is test
infrastructure only and changes no production cardinality or canister surface.

### Medium, resolved: empty-tail recovery retained a stale generation assertion

The later user-owned no-default core log
`target/validation-failures/20260818T154055Z-9029-1-_test-core-no-default.log`
passed 1,429 tests and the earlier corrected schema/cardinality cases, then
failed
`journaled_identity_recovery_quiesces_every_publication_interruption_before_reallocation`.
The assertion expected data and index store generations not to move during an
empty-tail recovery. The new recovery path intentionally resets each
disposable live projection and binds the index delta to the durable fold
watermark, so each volatile generation advances once even though canonical
stable rows and indexes are neither scanned nor rewritten.

The regression now requires exactly one projection-generation advance and
also snapshots both row and index lengths to prove their authoritative
contents remain unchanged. The exact no-default test passes. Production
recovery was not weakened or changed.

### Informational, resolved: the canister cache rejected concurrent source mutation

The same user-owned workspace run failed
`query_only_typed_and_dynamic_canisters_execute_without_sql` because its exact
Cargo input digest changed from
`3deac6ff9f8163cdf9b5c17227ce6ecd3817612396ff851546ab4ad5d4b3f82d` to
`39826a4f367014ebd90bb372797bf28f1e1a6d30be70c2eba6840445ed99b387`
while Cargo was running. This overlapped the recovery-test source correction.
Rejecting the artifact is the intended fail-closed cache behavior: publishing
it under the pre-edit key would make stale output authoritative.

The exact query-canister test passes against a quiescent worktree. The first
focused attempt was blocked by the restricted sandbox's local-port policy; the
approved local-bind rerun passed. The concurrent `ic-testkit` 0.8 hard-cut
update's four focused canister-cache tests also pass. The released lockfile
finalizes that dependency at 0.8.5. No cache validation was weakened and no
production or cache correction was required for this failure.

### High, resolved: the user-owned complete release validation passed

Repository instructions prohibit agents from directly running `make test`,
`make validate`, `cargo test --workspace`, `cargo test --all`, or equivalents.
The user-owned `make patch` flow completed after the focused corrections. Its
maintained target requires `make validate` against the exact source candidate
before any version mutation and rejects source-tree drift. The verified
candidate and release-gate receipts bind that successful validation to
`8e72cbb3c4ac1697a5f2aa13986970ad00af0901` and the final release commit.

`make validate` includes the complete maintained unit, workspace, canister,
format, invariant, feature-matrix, check, and warning-denied Clippy gates.
Therefore the missing post-correction broad evidence is closed without the
audit agent duplicating the user-owned full suite.

### Medium, resolved: exact release and artifact provenance

The committed bundle also contains user-owned deployment-inventory,
canister-build, and `ic-testkit` 0.8 changes. They were preserved and are
included in the source/artifact provenance, but this audit did not attribute
them to the cardinality correction. The production artifact was rebuilt from
the clean exact `v0.230.3` release with Rust 1.97.1. Its report records
`source_dirty: false`, the release commit and tree above, and lockfile SHA-256
`f3ce07cea8db6c0b9240383fe2245692b80f444039a1aba3054e2f64fd694d8a`.
This supersedes the earlier dirty-candidate artifact evidence.

## Reconstructed Scope And Authority Map

| Boundary | Production authority | Audit result |
| --- | --- | --- |
| Entity/index meaning | Accepted schema snapshot and accepted physical index set | Pass after live/canonical consumer correction; generated models never reconstruct runtime authority |
| Canonical facts | Store-local canonical `DataStore` rows and accepted user `IndexStore` entries | Pass; construction reads only physical canonical domains and excludes overlays, system/candidate/rejected/missing entries |
| Canonical boundary | Journal `FoldWatermark`, including epoch | Pass; header source validation rejects any mismatch |
| Durable exact counts | One `CardinalityGenerationHeader` plus count records in schema namespaces `0x05`, `0x06`, and `0x07` | Pass; one semantic generation, two reusable physical slots, no fifth allocation |
| Candidate construction | `drive_cardinality_generation_page` / `advance_cardinality_build_page` | Pass; one bounded clear/row/index action, exclusive physical cursor, isolated `Building` slot |
| Incremental maintenance | `prepare_folded_cardinality_maintenance` at complete-batch canonical fold | Pass; all count/header arithmetic preflights before canonical mutation, followed only by mechanical writes or IC trap rollback |
| Visible delta | Positioned `DataStore` entity and `IndexStore` prefix deltas | Pass; exact signed deltas only, with the index delta bound to its canonical base watermark and unavailable on malformed, stale, or impossible state |
| Consumer proof | `StoreHandle` | Corrected; journaled consumers now require current or request-admitted accepted authority, `Ready`, exact source/watermark, no cursor, accepted target, record generation/full digest, and exact delta; decoded projections never supply counts |
| Conservative consumption | Direct count, liveness/pruning, prefix sums, covering/child expansion, SQL DDL, accepted-schema historical-row proofs | Pass after correction; unavailable evidence remains `None` and each caller retains its existing conservative/error classification |
| Scheduling | Existing generated startup watchdog and `db/startup/driver.rs` | Pass; one optional store-local action per step, duplicate scheduling coalesced, ordinary readiness independent, install/upgrade only arm the driver |

## Core Correctness Reconstruction

### Authority and persisted identity

`CardinalitySourceIdentity` binds database incarnation, the fingerprint of all
four store allocations, accepted root revision and fingerprint, accepted index
count and canonical set fingerprint, and complete fold watermark. The current
version-1 header adds generation ID, `Building`/`Ready`, and slot. Header,
cursor, and count codecs accept only their single exact length/magic/version
and validate fingerprints. `CardinalityCountRecord::validate_identity` checks
both generation and the full 32-byte stored digest after the 15-byte physical
key selection, closing physical digest collisions.

Zero is represented by record absence. All generation, count, signed-delta,
page-total, entry, byte, and prefix arithmetic relevant to authority uses
checked operations. Saturation found in the diff is confined to diagnostic
instruction measurement or pre-existing bounded executor helpers, not count or
generation authority.

No legacy decoder, predecessor inspector, migration translator, generated-model
fallback, estimate, alternate statistics epoch, or compatibility path exists.
Malformed, unsupported, incomplete, contradictory, identity-mismatched,
collision-affected, and stale evidence returns a typed error or unavailable
proof.

### Exact count semantics

Rows contribute one entity presence transition only when their decoded entity
is accepted by the bound snapshot. Present entries from the exact accepted
physical user-index set contribute every nonempty component prefix. `Missing`,
nullable omission, system indexes, candidate/rejected indexes, retired
identities, malformed keys, and nonaccepted generations cannot contribute.

Recovery derives changes from final prepared raw-key transitions, validates row
and entry witnesses, coalesces duplicate digests, and preflights checked add or
subtract. Insert, delete, replacement, same-key repetition, nullable
transition, index membership change, hidden older fold, overflow, underflow,
unknown identity, arity mismatch, and collision controls are covered by the
focused cardinality, schema-store, commit, convergence, and trap tests.

### Bounded construction and publication

The frozen page limits remain constants in
`crates/icydb-core/src/db/schema/cardinality_build.rs`:

- 4,096 source entries;
- 16,777,216 source key/value bytes;
- 16,384 prefix updates.

`BuildPageBudget::admit` computes the next totals with checked arithmetic and
declines before consuming the first entry that would exceed any dimension.
Row and index collectors resume exclusively after a validated raw checkpoint,
including maximum-sized keys and the row-to-index transition. Each page is
fully decoded, validated, sorted/coalesced, arithmetically preflighted, and
encoded before `apply_prepared_cardinality_build_page` begins stable mutation.
After that point only stable-map insert/remove operations occur. Ordinary
errors are returned before mutation; backend contradictions in complete fold
application trap and rely on IC whole-message rollback.

Empty canonical row and accepted-index domains publish exact zero without a
row or index scan. No startup, query, repair, or alternative scan builder was
found.

Publication requires complete row and index phases, a matching complete
cursor, unchanged source identity, `Building` state, and an isolated slot.
Publication removes the cursor. `Ready` with a cursor is rejected everywhere.
Source drift during clear/build/completion/publication starts a checked next
generation in the alternate slot; it does not merge prior candidate counts.
Repeated maximum-shape use reaches both slots and then remains at fixed
physical high-water.

### Fold maintenance and live visibility

`prepare_folded_cardinality_maintenance` runs only after complete journal-batch
validation and before `apply_preflighted_fold`. Schema/index mutations
invalidate rather than reinterpret the source. `SchemaStore` preflights every
coalesced physical key, detects same-page shortened-key collision, validates
the current `Ready` header and absent cursor, computes every count, encodes the
next source-bound header, and reserves output before the first canonical row or
index write. Count records and the next watermark therefore describe the same
completed transition; partial batches cannot publish a new boundary.

The maintained derivation bound remains
`D <= R_insert_delete + 4I_user_actual`. Positioned overlay maps carry exact
signed deltas relative to canonical state and account for insert, replace,
delete, same-key newer effects, hidden older folds, pressure, quiescence, and
reopen. Recovery binds the index delta to the durable journal watermark without
scanning, each complete fold preflights the current binding before canonical
mutation, and the same transition advances the binding after mechanical
retirement. A journaled visible count now has only one derivation: validated
durable `Ready` base plus exact post-watermark delta.

### Scheduling, safety, and scope

The generated startup watchdog remains the sole timer/driver. It tries normal
recovery first, advances at most the first nonquiescent store-local cardinality
action, self-chains while work remains, and stops when all stores quiesce.
Optional cardinality failure terminates optional work without changing row or
index authority or ordinary database readiness. Generated init and
post-upgrade paths initialize/arm runtime state and do not scan canonical
domains.

Contextual review of added `unwrap`, `expect`, `panic`, assertion,
`unreachable`, unsafe, conversion, indexing, saturation, allocation, and clone
sites found no new production panic or unsafe path. Matches were tests,
diagnostic measurement, `debug_assert`, or already bounded executor code.
Stable decoding is length-bounded and fallible. No unbounded collection clone
or ordinary fallible work after page mutation was found.

The line adds no fifth store allocation; the deployment bound remains 38. It
adds no second row/index lifecycle, second statistics epoch, public policy,
setting, endpoint, method mode, export, persisted work flag, timer, scheduler,
or instruction-ceiling increase. The audit-only
`mutate_cardinality_closeout_index` endpoint remains behind `test-admin-api`
and is absent from production Candid and Wasm exports.

## Corrections And Tests Added

| Correction | Files | Regression |
| --- | --- | --- |
| Remove volatile full-count authority for journaled entity and prefix consumption; batch same-index durable proofs | `db/registry/handle.rs`, executor prefix/scan/covering callers | `journaled_cardinality_rejects_volatile_counts_and_unfolded_accepted_root_drift` |
| Accept every nonempty accepted prefix through full arity | `db/schema/cardinality_build.rs` | `accepted_user_index_authority_covers_every_non_empty_component_prefix` |
| Reject live/canonical accepted-root drift | `db/schema/cardinality_build.rs`, `db/registry/handle.rs` | Journaled drift regression above |
| Route bounded child enumeration through `StoreHandle` and prove proposal completeness | `db/executor/index_prefix_cardinality.rs`, `db/registry/handle.rs`, propagation callers | Journaled bypass fixture in the drift regression |
| Remove the duplicate canonical bundle read/decode while retaining root equality | `db/schema/store.rs`, `db/schema/cardinality_build.rs` | Root-keyed bundle-cache regression plus the maintained wide-branch performance test |
| Remove repeated stable source-proof reads at maximum fanout while retaining admitted source, delta-watermark, header, accepted-index, prefix, and digest checks | `db/registry/handle.rs`, `db/schema/store.rs`, `db/schema/cardinality_build.rs`, `db/index/cardinality.rs`, `db/index/store.rs`, `db/journal/store.rs`, executor propagation callers | Frozen one-shot maximum-fanout matrix; exact watermark-binding and raw-header-cache regressions |
| Bring generated-startup fixtures through required recovery and exact-count readiness before successor schema admission | `db/schema/application.rs` | Both generated-startup tests in no-default and SQL/diagnostics/migration configurations |
| Complete the `ic-testkit` 0.8 fixture-pool API hard cut | `testing/integration/tests/sql_perf_audit.rs`, `testing/integration/tests/sql_canister.rs` | SQL canister target compiles; final 65,536-key real-canister test passes |
| Distinguish the required empty-tail volatile projection reset from canonical row/index rebuilding | `db/session/write.rs` | Exact no-default identity-recovery interruption regression requires one projection reset and unchanged row/index lengths |
| Delete the obsolete single-variant logical count-key wrapper | `db/schema/cardinality_generation.rs` and entity-digest callers | Persisted digest golden test and all 50 cardinality tests retain the exact frozen bytes |

The first version of the journaled regression used an invalid empty parent
prefix and failed 1 of 49 cardinality tests. The fixture was corrected to a
valid full parent. The final selection, including the later raw-header cache
regression, passes all 50 tests.

## Independent Resource And Artifact Evidence

### Instruction and stable-memory evidence

| Evidence | Independent observation | Boundary/result |
| --- | ---: | --- |
| 4,096 rows, zero indexes | 2,329,378,503 fold instructions | Pass, below 30B |
| 2,048 rows, three indexes | 3,736,427,024 fold instructions | Pass, below 30B |
| 240 rows, 64 indexes | 10,575,418,790 fold instructions | Pass, below 30B |
| 65,536-key accepted index | 32,566,926,423 producer; 33,186,355,292 recovery total; 21,089,837,408 maximum of five callbacks | Pass; callback below 30B and every message below 40B |
| Populated active-index create/drop/recreate | 6,868,434,605 / 7,195,350,945 / 6,649,671,715 | Pass, each below 40B |
| Generated lifecycle init/post-upgrade | 3,169,568 init; 7,927,100 empty; 10,254,716 populated; 10,669,642 converged post-upgrade | Pass, below 25M; deferred driver and unchanged stable extent prove no canonical scan in hook |
| Direct exact `COUNT` | 0 row-store gets; 0 index-entry reads | Pass in populated recovery closeout assertion |
| Unit two-slot physical growth | slot A 851,968 bytes; both 1,703,936; reuse 1,703,936 | Pass, fixed high-water |
| Real-canister two-slot reuse | first Ready/drop/recreate all 796,983,296 stable bytes | Pass, no retained growth |

### Independently rebuilt artifacts

The predecessor was rebuilt from a clean detached local clone at the exact
accepted tag. The released `v0.230.3` source was rebuilt from the clean exact
release commit with the same Rust 1.97.1 toolchain and maintained
`wasm-size-report` pipeline.

| Artifact | Clean `v0.229.1` | Clean `v0.230.3` | Delta |
| --- | ---: | ---: | ---: |
| Compiler-emitted Wasm | 4,888,976 | 4,997,103 | +108,127 |
| Final raw Wasm | 4,269,683 | 4,364,835 | +95,152 |
| Deterministic gzip | 1,648,186 | 1,692,313 | +44,127 |
| Candid | 60,428 | 60,428 | 0 |
| Exports | 70 | 70 | 0 |

The authoritative raw delta is 35,920 bytes inside the accepted 131,072-byte
allowance. Candid SHA-256 is unchanged at
`5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`.
The final compiler, raw, and gzip SHA-256 values are respectively
`082a12335e13976ceed1958b77da6fd8272a87bb776481d2c845c146df16c7de`,
`5b90301376ea35634ec5afdd3aec601dbd428c3e1a609dbeb67d70ba196aa53d`,
and `1f5f051838e8c4a871edcf5e6d889f981cb2851a1786345851b0547e4118458c`.
The final artifact has 27 query methods, 39 update methods, one init, one
post-upgrade, one global timer, and one Candid pointer export, identical by
Candid hash and export count to the predecessor. The audit endpoint is absent
from the `.did` and both compiler/final export reports.

## Command And Result Ledger

### Provenance, diff, policy, and safety inspection

| Command or command family | Result |
| --- | --- |
| `git status --short`, `git rev-parse HEAD`, `git rev-parse HEAD^{tree}`, `git log --oneline --decorate v0.229.1..HEAD`, `git show v0.230.3`, `git tag --contains HEAD`, `git branch -a --contains HEAD` | Pass; exact release identity established: `HEAD`, `main`, `origin/main`, and annotated `v0.230.3` agree on commit `eb36bdb4281de873f62e71df8c7142c856232eb2` and tree `f4f2a3b7c3725330381f9ffbfbeff98eda840198` |
| `git diff --stat v0.229.1..v0.230.3`, `git diff v0.229.1..v0.230.3`, `git diff v0.230.2..v0.230.3` | Pass; complete released and inter-patch changes traced, including separately identified user-owned deployment/testkit changes |
| `git diff --check v0.229.1..HEAD`, `git diff --check` | Pass before corrections; final diff hygiene rerun is recorded below |
| Targeted `rg`, `rg --files`, `sed`, `jq`, `wc`, and `ps` inspections over governance, 0.228/0.229/0.230 contracts, production owners/consumers, generated wiring, tests, logs, and artifacts | Pass; no alternate authority, scan route, allocation, scheduler, public surface, legacy path, or production panic found |
| `git diff -U0 v0.229.1 -- '*.rs'` piped to the panic/arithmetic/unsafe pattern audit | Pass after contextual review; matches were tests, diagnostics, `debug_assert`, or bounded non-authority helpers |

### Static and focused validation

| Command | Result |
| --- | --- |
| `make fmt-check` | Pass before correction |
| `make lint-workflows shellcheck check-invariants` | Pass: workflow, shell, dependency, executor-panic, endpoint, index-range, authority, atomicity, release, durability, read-admission, schema/model, SQL ownership, Wasm post-link, and memory-ID guards |
| Post-release reconciliation `make fmt-check`, `make check-invariants`, and `git diff --check` | Pass; formatting, deployment inventory, dependency, authority, durability, release, endpoint, Wasm, memory-ID, and diff-hygiene guards remain clean |
| `make check-feature-matrix check` | Pass across maintained feature combinations and workspace check |
| Final `make clippy` | Pass; both focused core feature lanes and warning-denied workspace all-targets coverage pass after every correction |
| `cargo clippy --locked -p icydb-core --no-default-features --all-targets -- -D warnings` | Initial final-correction run found five structural lints; corrected rerun passes |
| `cargo clippy --locked -p icydb-core --features sql,diagnostics,migration --all-targets -- -D warnings` | Pass after all log-driven corrections |
| `cargo check --locked -p icydb-core --all-targets` | Pass after the proof-reuse correction |
| `cargo fmt --all`, `cargo fmt --all --check`, `git diff --check` | Pass; final code is formatted and the diff is clean |
| First prefix-watermark test invocation after the user-owned lockfile update | Pre-test failure in the restricted sandbox because `ic-testkit` 0.8 was not cached and crates.io DNS was unavailable; approved dependency-resolution retry succeeded, then the fully qualified exact test passed 1/1 |
| `cargo test --locked -p icydb-core --no-default-features cardinality -- --nocapture` | Final run: 50 passed, 0 failed, including raw-header cache corruption and fixed two-slot high-water evidence |
| Exact prefix-delta watermark regression | Pass 1/1; unbound, matching, stale, and advanced canonical bases fail or succeed as designed |
| `cargo test --locked -p icydb-core --no-default-features generated_startup_driver_ -- --nocapture` | Pass 2/2 after fixture correction |
| Same generated-startup selection with `--features sql,diagnostics,migration` | Pass 2/2 after fixture correction |
| `cargo test --locked -p icydb-core --no-default-features exact_intersection_ -- --nocapture` | Pass 7/7 after batched durable-proof correction |
| Exact `one_shot_baselines_keep_exact_queries_rows_and_evidence` | Pass 1/1; maximum fanout 5,075,414 / 5,150,250, 65 index-entry reads, zero row-store gets; sparse two-index intersection 2,065,644 / 2,100,000 |
| `cargo test --locked -p icydb-testing-integration --test sql_canister --no-run` | Pass after replacing the removed `ic-testkit` fixture-pool method |
| Warning-denied Clippy for the `sql_perf_audit` and `sql_canister` integration targets | Pass after the `ic-testkit` 0.8 API correction |
| Focused accepted-bundle cache regression | Pass 1/1; borrowed root and bundle track both selected roots with one decode per root |
| Focused `icydb-core` schema-store selection | 46 passed, 0 failed |
| Focused `icydb-core` startup selection | 9 passed, 0 failed |
| Focused generated-watchdog source test | 1 passed, 0 failed |
| Focused direct-count diagnostics test | 1 passed, 0 failed; compile emitted unrelated migration-feature dead-code warnings, while maintained warning-denied Clippy remained clean |
| User-owned `scripts/ci/run-validation-targets.sh test-unit test-canister-artifact-contract` captured in `20260818T122729Z-35746-7-test.log` | Pre-correction fail: no-default core and workspace lanes exposed the same two stale generated-startup fixtures; workspace also exposed maximum fanout 6,264,502 / 5,150,250. All three focused reruns now pass, but the broad command has not been rerun |
| User-owned no-default core run captured in `20260818T154055Z-9029-1-_test-core-no-default.log` | Pre-final-correction fail: 1,429 passed, 1 failed, 2 ignored. The earlier schema/cardinality failures passed; one stale empty-tail generation assertion failed at 45 versus 44 |
| Exact `journaled_identity_recovery_quiesces_every_publication_interruption_before_reallocation` reruns | Pass 1/1 with no default features and 1/1 with `sql,diagnostics,migration` after requiring one volatile data/index projection reset and unchanged authoritative row/index lengths |
| User-owned workspace run captured in `20260818T154055Z-9029-2-_test-workspace.log` | Pre-final-correction fail: the same stale core assertion failed, and one query-canister build correctly rejected a source digest that changed while Cargo was running; other recorded targets continued |
| Exact `query_only_typed_and_dynamic_canisters_execute_without_sql` rerun | Restricted-sandbox attempt could not bind PocketIC's ephemeral local port and was stopped; approved local-bind rerun passed 1/1 against quiescent source |
| Focused `canister_build_cache::tests` selection during the `ic-testkit` 0.8 hard cut | Pass 4/4; exact input/environment, retention, progress, and multi-artifact failure reporting remain clean; released dependency is 0.8.5 |
| First `make test-durability` audit run | Fail: 87 passed, 1 failed. Core commit 46/46, mutation job 6/6, convergence candidate 1/1, mutation scale 1/1, recovery closeout 4/4, SQL perf 29/30 |
| Final `make test-durability` at the preceding audit cut | Pass 88/88: core commit 46/46, mutation job 6/6, convergence candidate 1/1, mutation scale 1/1, recovery closeout 4/4, SQL perf 30/30; later exact-candidate `make validate` closes the post-correction broad evidence |
| Exact reruns of `sql_perf_token_branch_set_limit50_pressure_beats_overcap_fallback` | Initial corrected path failed at 125,867,260 versus 16,474,822; batching failed at 17,413,664 versus 16,402,920; final proof reuse passes at 5,357,533 versus 10,379,743 |

### Real-canister, upgrade, rollback, and instruction evidence

| Command | Result |
| --- | --- |
| Focused `maximum_accepted_index_publication_records_canonical_watchdog_promotion_evidence` | First final-worktree compile exposed the removed `ic-testkit` pool method; after the API correction, approved local-bind rerun passed 1/1 with the 65,536-key evidence above |
| Focused `joint_` SQL-perf selection | Pass 3/3: 4,096-entry, 2,048/three-index, and 240/64-index boundaries |
| Focused `patch1_near_maximum_row_and_derived_indexes_fit_one_real_watchdog_work_message` | Pass 1/1; 558,297,437 work instructions |
| Focused `sql_perf_journaled_upgrade_driver_then_ready_probe_stays_bounded` | Pass 1/1; driver restores Ready and metadata-backed repeat probe remains bounded |
| Focused `framework_neutral_root_orders_both_lifecycle_phases_before_deferred_database_work` | Pass 1/1; direct generated init/post-upgrade measurements above |
| Focused `populated_cardinality_build_upgrade_maintenance_and_slot_reuse_close_cleanly` | Final pass 1/1; publication, Building fallback, exact direct count with zero row/index reads, live maintenance, upgrade, revision changes, and stable high-water fixed at 796,983,296 bytes |
| Maintained recovery-closeout batch inside `make test-durability` | Pass 4/4, including complete-batch trap/rollback and canonical watchdog retry |
| Test-runner process inspection and `kill 53514` | After all 88 tests exited successfully, one test-owned PocketIC server retained the validation log pipe; the exact process was terminated and the runner recorded all targets passed. The user-owned local ICP network was not touched |

### Artifact commands

| Command | Result |
| --- | --- |
| `make wasm-size-report SIZE_REPORT_ARGS="--canister sql_perf"` at clean exact `v0.230.3` | Pass; clean release artifact values and provenance above |
| `git archive v0.229.1` into `/tmp/icydb-0229-audit`, then the same report command | Aborted after dependency setup because an archive cannot provide required Git provenance |
| Local shared clone, `git switch --detach v0.229.1`, then `make wasm-size-report SIZE_REPORT_ARGS="--canister sql_perf"` | Pass; clean predecessor commit/tree and artifact values independently reproduced |
| `jq` export/mode counts and `rg mutate_cardinality_closeout_index` over production `.did` and info files | Pass; 70 exports with unchanged mode surface; audit-only endpoint absent |
| `scripts/ci/release-candidate-receipt.sh verify-commit` and `scripts/ci/verify-release-gate-receipt.sh eb36bdb4281de873f62e71df8c7142c856232eb2` | Pass; exact validated candidate, release diff, and final release commit receipts agree |
| `cargo info icydb@0.230.3` from an isolated temporary directory | Pass; the published crate resolves from crates.io |

### User-owned complete release validation

| Command | Result |
| --- | --- |
| Complete post-correction `make test` / maintained validation target group | Pass as part of the user-owned exact-candidate `make validate`; not duplicated by the agent because repository hard rules forbid direct full-suite execution |
| `make validate` | Pass in the user-owned `make patch` flow against candidate `8e72cbb3c4ac1697a5f2aa13986970ad00af0901`; candidate and release-gate receipts verify the tested source and no-drift transition |
| `cargo test --workspace`, `cargo test --all`, equivalents | Not separately invoked by the agent; the repository-maintained complete test coverage is owned and passed through `make validate` |
| `make patch`, release staging/commit/push, and `cargo publish` | Completed by the user; the audit performed only read-only post-release verification and did not mutate release state |

## Baseline-To-Final Scope And Complexity

The accepted `v0.229.1..v0.230.2` implementation changed 37 files with 6,232
insertions and 210 deletions. The released post-`v0.230.2` bundle changes 41
files with 2,912 insertions and 630 deletions relative to that tag. The complete
`v0.229.1..v0.230.3` release changes 56 files with 8,824 insertions and 520
deletions. File counts are reporting signals: much of the complete line delta
is mechanical propagation of the admitted root/incarnation and traversal proof
context rather than another execution route.

The post-release reconciliation touches only this audit, the line status, and
the detailed 0.230 changelog. It is documentation-only and approximately
line-neutral; it adds no production or persisted-state complexity.

The correction increases local implementation complexity. Stable generation
proof is explicit for journaled prefix families and bounded child proposals;
the positioned index delta also gains one derived volatile base-watermark
binding. That binding is reset from the durable journal, advanced only with a
preflighted complete fold, and fails closed, so it is not an independent owner
or policy axis. Semantic complexity decreases because volatile full-count and
direct child-enumeration bypasses are gone. Persisted/public state-space delta
remains zero: the line still has one generation, one cursor, two reusable
physical slots, the existing four allocations, one builder, one fold boundary,
one proof boundary, and one scheduler.

Performance and Wasm deltas are reported above. The maintained wide branch is
now about 48 percent cheaper than its conservative fallback; no assertion or
threshold was weakened. The separate frozen maximum-fanout boundary passes by
74,836 instructions (about 1.45 percent), so its gate should remain unchanged
and be watched in future planner work.

## Remaining Risks And Required User Action

No release blocker remains. The maximum-fanout query retains a relatively
tight but deterministic 74,836-instruction margin below its frozen regression
ceiling; keep that ceiling unchanged and monitor future planner changes. The
only user follow-up is to review and commit these post-release documentation
corrections without moving or rewriting `v0.230.3`.

## Verdict

**READY for the user-owned 0.230.3 release gate** — release completed.

The released source, exact-candidate complete validation, durable-generation
design and authority, construction and fold atomicity, slot reuse, instruction
ceilings, post-upgrade behavior, direct-count storage-read contract, raw-Wasm
allowance, Candid, and exports are all supported by independently reconciled
evidence. No release blocker remains.
