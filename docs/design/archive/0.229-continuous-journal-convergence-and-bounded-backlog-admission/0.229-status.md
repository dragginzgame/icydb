# 0.229 Continuous Journal Convergence And Bounded Backlog Admission Status

Status: Patch 8 complete; closeout findings corrected; focused evidence
refreshed; implementation line ready for a release target

Patch 8 closes the activated convergence route against populated
real-canister, exact-pressure retry, current-form mid-debt upgrade, trap
rollback, instruction, memory, and artifact evidence. It also removes repeated
per-row catalog work discovered by that evidence and makes each watchdog
callback own exactly one globally oldest complete batch. The final route adds
no cursor, timer, persisted scheduler state, mode, configuration, or
compatibility path.

Design authority:
[0.229 design](0.229-design.md)

Evidence cut: 2026-08-17

## Exact Evidence Identity

Patch 1 is anchored to accepted release `v0.228.3`, commit
`6c4ac68fbb7f8e51bb807240c22d1dec6b039c97`, tree
`3bfa8329ff05a5923541e25109cb0a84f9c69e9f`. The local tag, `HEAD`, and
`origin/main` resolved to that exact commit at the Patch 1 cut.

The tag landed during Patch 1 validation. Its delta from the inspected
pre-release candidate changes only `Cargo.toml`, `Cargo.lock`, and `README.md`
release surfaces, so the convergence evidence did not require revision. A
moved tag requires reconciliation to be repeated before implementation.

The Git identity is audited release-process comparison evidence only. Runtime
accepts solely the current version-1 persisted forms and does not use the tag,
commit, or tree as compatibility authority.

## Patch Tracker

| Patch | Status | Result |
| --- | --- | --- |
| 1. Baseline evidence and reconciliation | Complete | Cursor-free comparison baseline, two gates, bounded registry, global selection, exhaustive current-state overlays, fit/heap/residual obligations, and proof-before-activation order are frozen without runtime changes |
| 2. Dormant positioned overlay state machines | Complete | Data/index/schema owners carry exact volatile provenance; every maintained journal and derived-index effect is classified; online use remains unreachable |
| 3. Exact controls and current-format initialization | Complete | Journal/control authority, exact tail aggregates/head, global commit sequence, bounded no-reuse registry, and atomic initialization are implemented without online convergence activation |
| 4. Dormant Gate 2 and measurement instrumentation | Complete | Exact test-only B/R/Y evaluator, typed pressure facts, Gate 1 priority, zero-debt maximum multi-store shape, and measurement fidelity are proved without a production call edge or frozen limit |
| 5. Version-1 hard-cut correction | Complete | Every genuine current format discriminator is version 1, predecessor activation is deleted, and noncurrent state requires reinstall/recreation |
| 6. Integrated dormant candidate and evidence freeze | Complete | Frozen tuple, fit, heap, driver, fairness, residual, and stable-allocation evidence passes without a production call edge |
| 7. Atomic production activation | Complete | Gate 2, positioned live publication/retirement, oldest-first online folding, and existing-watchdog self-chaining are one production route |
| 8. Populated real-canister closeout | Complete | Cold and populated histories have identical bounded residue; pressure retry, mid-debt upgrade, exact final state, rollback, memory, instruction, and artifact evidence pass |

Patch 2's positioned overlays, Patch 4's cumulative evaluator, and Patch 6's
integrated coordinator are active through the single Patch 7 production route.
Patch 8 validates that route without introducing a second scheduler, cursor,
or recovery model. All eight planned landing patches are complete. The
same-minor closeout audit found three narrow production defects and several
stale evidence fixtures; the correction record below supersedes the affected
pre-closeout claims.

## Patch 1 Reconciliation Results

### Complete-batch baseline

- `FoldRecordCursor` was removed in accepted 0.228 with no replacement.
- The fold watermark plus oldest retained complete batch are the only
  owner-local progress authority.
- Raw inspection, complete Validate/preflight, mechanical Apply, watermark
  publication, and immutable batch retirement occur in one synchronous
  message.
- Recovery stops between complete batches only. A later mutation can occur
  before a callback or between callbacks, never inside Validate/Apply.

### Ordered admission and zero-debt theorem

- Accepted 0.228 already applies exact prepared-work admission
  `W = 4R + I + 4A + 4P <= 16,384`, plus existing record, byte, fanout,
  marker, and structural limits.
- Those individual failures remain non-pressure errors and have priority.
- Every maximum Gate-1-valid batch/record/byte and multi-store contribution
  must fit the final Gate 2 tuple against zero retained debt.
- Gate 2 may reject a legal proposal only because existing debt consumes
  capacity; sufficient convergence must make retry possible.
- Admission and publication consume the same encoded prepared batch bytes;
  identity and database commit sequence may be previewed but never durably
  advanced on rejection.
- Patch 4's evaluator is test/audit-only and cannot reject a production commit.

### One-message fit and heap

- The 0.228 one-batch proof is not assumed to cover new 0.229 work.
- Patch 6 evidence below includes exact aggregate maintenance, maximum
  positioned retirement, 38-head selection, and driver bookkeeping in the
  callback instruction proof without tightening Gate 1.
- Overlay heap bytes and maximum decode/preflight/Apply/retirement scratch are
  bounded mechanically, not inferred from effect count alone.
- Gate 2 bounds outstanding debt and cannot substitute for per-batch fit.

### Initialization and persisted authority

- Fresh installation requires absent recognized authority plus provably fresh
  declared allocation roots, then atomically creates the incarnation, format
  identity, bounded registry, database commit sequence, and zero controls.
- Noncurrent pre-1.0 persisted state is not eligible for in-place activation;
  operators must reinstall or recreate the database.
- Current initialization writes only the sole version-1 control, journal,
  receipt, cursor, and progress forms. There is no predecessor inspector,
  decoder, translation path, or compatibility drain.
- Missing controls after current-format activation are corruption.

### Current-format registry and cross-store order

- Application memory identifiers span `100..=254`; three canister controls and
  four allocations per journaled store yield absolute `S_max = 38`.
- The convergence-format identity, registry, and global sequence use the
  existing commit-control allocation, so they add no fourth canister memory
  reservation.
- Persisted current-format authority retains every active or retired quartet;
  retired identifiers never reuse within the database incarnation.
- New stores require fresh collision-free allocations and atomic zero-control
  initialization; removal requires an empty tail and leaves a retired entry.
- The current 93-byte version-1 envelope adds one fingerprint-bound database
  commit sequence to the owner-local journal sequence.
- The driver selects minimum `(database commit sequence, store allocation,
  journal sequence)` across at most 38 exact heads, preventing hot-store
  starvation without a scheduler cursor.

### Current-state overlay authority

- Current-process `Ready` may carry bounded debt only with a complete live
  overlay; install/upgrade without overlays uses `Recovering` for retained
  current tails.
- Query execution, mutation preparation, constraints, identity allocation,
  validation, and migration all consume live-before-canonical state.
- Patch 2 must classify every accepted `JournalRecord` and derived effect with
  an exhaustive no-wildcard Rust match as positive, tombstone, terminal-only,
  or prohibited online.
- Position starts with store allocation, tail-local batch sequence, and logical
  target. A family-local ordinal requires evidence of a repeated same-batch
  target; no general ordinal is authorized.

### Driver and residual bounds

- Tail controls prove actionable work; no durable `work_pending` Boolean is
  added.
- The existing driver self-chains while debt remains, and every normally
  returned actionable callback progresses, quiesces, or publishes terminal
  failure.
- Diagnostic and integrity surfaces remain available through the existing
  authorized failure lane when ordinary database work is blocked.
- Residual work snapshots `B_0` at the start of a no-new-debt interval:

```text
P_fold_normal <= B_0
M_total_normal <= B_0 + C_driver
I_total_normal
  <= B_0 * I_fold_callback_max
   + C_driver * I_driver_overhead_max
```

`C_driver` includes every fixed normal driver/lifecycle message. The formula
does not misuse current remaining `B`. It covers normal callbacks after
one-message fit excludes instruction exhaustion. Unexpected rollback traps are
outside the normal bound and cannot falsely claim durable terminalization.

### Steady-state and physical bounds

```text
B <= B_max, R <= R_max, Y <= Y_max, S <= S_max <= 38
O <= g(B, R, Y, W_0_229_max, F_max)
H_overlay
  <= H_fixed + sum(alpha_f * O_f) + beta * Y + gamma * B + delta * S
H_callback_peak
  <= H_overlay + H_head_scan_max + H_decode_max
   + H_preflight_max + H_apply_max + H_retire_max
```

Physical retained chunk entries satisfy
`K = sum(ceil(y_b / 65,536)) <= B + floor(Y / 65,536)`. Patch 6 also proves
allocator reuse through repeated fill/drain/refill cycles; logical bytes alone
remain insufficient.

No right-hand side contains historical population, historical commits, or
retired journal history. Wall-clock timer delivery remains outside the claim.

## Patch 2 Positioned Overlay Results

### Singular owner-local representation

- The existing data, index, and schema live maps and tombstone sets remain the
  only current-state value authority. Patch 2 adds one parallel position map
  to each journaled owner; it does not add a second value projection.
- Each position is exactly `(store allocation, tail-local journal sequence)`;
  the family map key is the logical target. There is no general record ordinal,
  cursor, persisted overlay, scheduler state, or configuration axis.
- Publication preflights allocation consistency and monotonic sequence before
  changing the live effect. Retirement preflights the exact position before
  removing anything. An older batch observes `Superseded` and leaves the newer
  value or tombstone intact; an absent, future, or cross-allocation position
  fails typed and closed.
- At the Patch 2 handoff, production owner paths did not touch positioned
  provenance. Patch 7 has now routed online publication through these state
  machines and included position reset in the established projection-reset
  boundaries; no mixed positioned/unpositioned production state is authorized.

### Exhaustive family classification

The production classifier matches `JournalRecord` without a wildcard:

| Maintained record | Owner | Effect |
| --- | --- | --- |
| `RowPut` | Data | Positive |
| `RowDelete` | Data | Tombstone |
| `SchemaPut` | Schema | Positive |
| `AcceptedSchemaPublish` | Schema | Positive publication family |
| `AcceptedSchemaIndexDelete` | Index | Tombstone |
| `AcceptedSchemaIndexPut` | Index | Positive |
| `ConstraintValidationJobPut` | Schema | Positive |
| `ConstraintValidationJobDelete` | Schema | Tombstone |
| `ConstraintValidationIndexPut` | Index | Positive |
| `IdentityRangeAdvance` | Schema | Positive |
| `SchemaMigrationRowPut` | Data | Positive |
| `SchemaMigrationIndexPut` | Index | Positive |

Derived ordinary index effects use their maintained `Option<IndexEntryValue>`
semantics directly: `Some` is positive and `None` is a tombstone. Counter-delta
annotations remain observability-only and do not select semantics.

### Same-batch target proof

- Journal construction continues to reject duplicate ordinary row targets,
  migration row/index targets, validation-job transitions, accepted-index keys,
  and identity-owner ranges under their existing authorities.
- Accepted-schema suffix and validation-index chunks retain strict key
  ordering/uniqueness checks.
- Prepared ordinary index mutations can revisit one full logical index key
  while a multi-row commit window is staged. The existing preflight overlay and
  physical index live map already coalesce that key to its final effect. Patch 2
  assigns every occurrence the same batch position, so the position map also
  contains one target and exact retirement occurs once. No family-local ordinal
  is required.

### Current-state consumer audit

- Row point reads, ordered/range traversal, and entity-cardinality maintenance
  already merge the data live map/tombstones before canonical rows.
- Index point, equality, ordered, unique, non-unique, reverse, accepted-schema,
  validation-candidate, and migration reads already use the index owner's
  live-before-canonical point/range boundaries.
- Schema snapshot/root/bundle selection, transition planning, validation jobs,
  identity allocation/inventory, and migration schema reads already use the
  schema owner's effective live-before-canonical boundaries.
- Mutation-window row/index predecessor reads, unique/conflict checks,
  relation/constraint readers, and migration preparation ultimately consume
  those same owner boundaries. No generated model or audit-only projection is
  introduced as runtime authority.

### Dormancy, cost, and state-space impact

- Repository call-site inspection finds positioned publication/retirement only
  in their owner definitions and focused unit tests. The 0.228 recovery engine,
  commit coordinator, driver, and scheduler have no call edge to them.
- Persisted state, stable-memory identifiers, journal format, controls, public
  Rust API, Candid, exports, timers, and modes are unchanged.
- Volatile fixed layout cost is one map-header-sized field in each data, index,
  and schema store backend value; the journaled variant initializes it empty,
  while the enum's maximum-variant layout also reserves that capacity for heap
  stores. Outstanding positioned targets later add one bounded family key plus
  one allocation/sequence position and B-tree node overhead; Patch 6 freezes
  conservative Wasm-heap coefficients and maximum-shape evidence below.
- Existing unpositioned writes, recovery, and reads perform no position-map
  operation. Only owner construction initializes the empty map header; no
  positioned publish, retirement, fold, or driver work executes. Patch 6's
  integrated instruction evidence remains test/audit-only before activation.
- Against a fresh build from the exact accepted `v0.228.3` tag, the current
  `sql_perf` wasm-release fixture's
  compiler-emitted Wasm is 4,783,730 bytes (`+1,369`) and the final deployable
  raw Wasm is 4,176,027 bytes (`+1,165`). Deterministic gzip is 1,611,272 bytes
  (`-55`) as secondary context. An earlier draft added empty-map removal to
  every mutation and measured `+10,185` raw bytes; that accidental hot-path
  work was removed before handoff.
- The fixture Candid remains byte-identical at 60,428 bytes with the same
  SHA-256 digest, and the final artifact retains exactly 70 exports.
- Complexity increases by one shared provenance/retirement primitive and one
  position map in each existing owner. It adds no alternative read authority,
  online execution route, durable codec, compatibility path, or scheduler.

### Focused validation

- Eight positioned-overlay unit tests pass across all three owners, including
  later-batch supersession, delete/reinsert, exact retirement, same-batch index
  target coalescing, allocation/sequence rejection, and derived-effect polarity.
- All 62 maintained journal codec/store tests pass, including exhaustive
  12-family classification and every existing duplicate-target restriction.
- Twenty-two journaled owner/identity tests, both commit-window overlay tests,
  the complete-final-state relation validation test, and the physical migration
  replay/publication test pass against the unchanged current-state readers.
- Warning-denied core checking passes with SQL, diagnostics, and migration
  enabled, as does warning-denied default-feature checking. The production
  Wasm/Candid/export report, formatting, durability-document invariants, and
  diff hygiene pass.

## Patch 3 Exact Control Results

### Current journal and database order

- The sole current journal envelope is version 1. Its codec-derived fixed
  header is exactly 93 bytes and adds one nonzero database commit sequence
  before the record count and fingerprint.
- The domain-separated SHA-256 preimage is the exact bytes before the
  fingerprint field plus the record bytes after the 32-byte field. Raw
  length, count, version, local sequence, database sequence, marker identity,
  and fingerprint checks precede record materialization.
- One marker previews one checked database sequence, every owned batch carries
  that same value, successful empty-slot marker publication advances the
  high-water atomically, and rejection consumes no value. Tail-local sequence
  remains the owner-local order and tie-break input.
- Noncurrent versions have no decoder. Runtime accepts only the exact current
  version-1 journal and database-control forms.

### Exact controls and bounded registry

- Every current journal tail persists exact batch count, record count, encoded
  envelope bytes, and optional oldest database sequence in the existing
  journal allocation. Empty closure, checked addition/subtraction, physical
  head/watermark order, identical replay, and selected-batch retirement fail
  typed and closed on contradiction.
- The existing commit-control allocation now owns the nonzero incarnation and
  cursor key, database-sequence high-water, current marker, and a canonical
  active-plus-retired allocation registry. The fixed derivation remains
  `floor((155 - 3) / 4) = 38`; generated registrations are proposals only.
- New quartets require four fresh roots and collision-free canonical IDs/keys.
  Removal requires an exact empty tail, records a retired entry, and never
  permits identifier reuse in the incarnation. Registry exhaustion and
  control-allocation collision reject before publication.

### Lifecycle and atomicity

- Fresh installation preflights every declared allocation and all encoded
  controls before mechanically publishing the boot record, zero tail controls,
  and current database control in one message.
- Current-form reopen compares any terminal startup receipt against current
  database, tail, or accepted-schema evidence. A stale receipt does not
  permanently block corrected current authority.
- Current reconciliation validates bounded tail authority, initializes only
  fresh additions, retires only empty removals, and leaves an unchanged marker
  intact. All fallible lookup, decoding, arithmetic, and sizing checks complete
  before capacity growth; a failed growth publishes no bytes, and after a
  successful growth the remaining writes are mechanical and rely on IC message
  rollback if stable storage traps.
- Recovery remains the accepted synchronous complete-batch route. Patch 3 does
  not invoke positioned overlays, add Gate 2, add a selector/cursor, create a
  scheduler or timer, or add a new stable-memory identifier.

### Focused validation and footprint

- All 67 maintained journal codec/store tests, 24 commit-store tests, five
  convergence-format tests, eight startup tests, the generated four-role
  allocation test, and the malformed-journal persisted corpus pass. Focused
  schema publication, commit-window, identity recovery, interruption recovery,
  journaled schema-store, and physical-migration replay tests also pass.
- Warning-denied all-feature and default core checks, warning-denied all-feature
  core Clippy, formatting, durability documentation invariants, and memory-ID
  drift guards pass.
- Against the Patch 2 artifact, `sql_perf` compiler-emitted Wasm is 4,820,737
  bytes (`+37,007`) and final deployable raw Wasm is 4,208,943 bytes
  (`+32,916`). Deterministic gzip is 1,625,184 bytes (`+13,912`) as secondary
  context. Against accepted `v0.228.3`, the final raw increase is 34,081 bytes.
- Fixture Candid remains byte-identical at 60,428 bytes with SHA-256
  `5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`,
  and the final artifact retains exactly 70 exports.
- Complexity increases by one current control-envelope revision, one bounded
  no-reuse registry, one database sequence, and one fixed tail control per
  journal allocation. It adds no alternate replay route, compatibility drain,
  persisted scheduler state, configuration axis, or duplicate database-global
  aggregate.

## Patch 4 Dormant Gate 2 Results

### Exact measurement and ordered admission

- The test/audit evaluator consumes the database-wide sum of exact owner-local
  tail controls and the exact immutable envelopes encoded from one prepared
  marker. Batch count, record count, and encoded bytes therefore use the same
  codec authority and units as later journal append; Patch 4 adds no second
  approximate metric.
- Projection uses checked `u64` arithmetic and rejects malformed store counts
  or overflow as corruption. Capacity pressure carries the exact dimension,
  current contribution, proposed contribution, and candidate limit, with a
  deterministic batch/record/byte comparison order.
- Canonical Gate 1 runs first. Its existing exact work boundary remains
  `W = 4R + I + 4A + 4P <= 16,384`, and its existing non-pressure diagnostic is
  returned without invoking the dormant evaluator when both gates would fail.
- At zero retained debt, the evaluator admits a candidate limit equal to the
  exact proposal contribution. The focused maximum multi-store shape uses all
  38 store allocations and 4,096 zero-index row records. The maintained Gate 1
  tests continue to pin 240 maximum-fanout inserts and the 4,095-row plus one
  identity-range boundary. Patch 6 now freezes the measured maxima and the
  candidate tuple that dominates all three shapes.

### Dormancy and complexity

- The evaluator, pressure taxonomy, prepared-envelope harness, exact tail
  control re-export, and call sites are all compiled only under `cfg(test)`.
  Production commits have no call edge to Gate 2 and cannot return cumulative
  pressure.
- Patch 4 adds no public diagnostic, Candid method, export, persisted field,
  stable-memory identifier, timer, scheduler, mode, feature, compatibility
  path, or caller-configurable limit. Candidate limits exist only as test
  inputs and do not freeze policy.
- Four focused evaluator tests cover exact codec/owner measurement fidelity,
  every pressure dimension, zero-debt maximum multi-store admission, checked
  projection overflow, and the 38-store bound. The canonical commit-window
  test separately proves Gate 1 diagnostic priority. Warning-denied
  all-feature test Clippy and all-feature/default-minimal core checks pass.
- Production implementation complexity and hot-path work remain unchanged.
  The test-only slice adds one closed evaluator and one once-encoded proposal
  owner for use by the Patch 6 evidence harness.
- The production `sql_perf` artifact is byte-identical to Patch 3: 4,820,737
  compiler-emitted Wasm bytes, 4,208,943 final deployable raw Wasm bytes, and
  1,625,184 deterministic gzip bytes. Fixture Candid remains exactly 60,428
  bytes with SHA-256
  `5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`,
  and the deployable artifact retains 70 exports.

## Release-Candidate Bound Correction

- The 10,001-row canister fixture exposed a 33,368,724-instruction Forward
  step above the frozen 31,000,000 ceiling. The ceiling remains unchanged;
  durable pages now inspect at most 224 keys and stage at most 56 row updates.
- The production commit hot path reuses the validated control prefix, previews
  the fixed sequence field before complete publication validation, retires the
  exact live marker retained by the synchronous guard, and avoids duplicate
  mutation-progress decoding. Recovery still performs complete checksummed
  control inspection and verified retirement.
- The same collection-scale fixture now completes in 179 Forward and 45 Verify
  calls. Tier maxima are 28,375,024 Forward and 7,183,800 Verify instructions;
  scoring maxima are 26,073,761 and 7,125,926 respectively. The maintained
  31,000,000 Forward, 8,000,000 Verify, and 2,000,000 control ceilings were not
  raised.
- Successful post-upgrade recovery may fold pending batches and grow stable
  memory, so the collection fixture proves non-shrink plus exact final state.
  The dedicated trap/rollback fixture remains the byte-for-byte rollback
  authority.
- The corrected production `sql_perf` artifact is 4,817,462 compiler-emitted
  Wasm bytes, 4,205,855 final deployable raw Wasm bytes, and 1,624,491
  deterministic-gzip bytes. Candid remains 60,428 bytes with SHA-256
  `5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`,
  and the deployable artifact retains 70 exports.

## Patch 5 Version-1 Hard-Cut Results

- Commit marker, commit control, journal batch, scalar cursor, startup failure
  receipt, integrity-job record, test/audit artifact, and SQL-generator
  discriminators now all encode and decode exactly version 1. The durability
  invariant scans production and test Rust version constants and rejects values
  above 1 without exceptions.
- The bounded predecessor commit-control parser, predecessor observation
  variant, activation helper, startup special case, and upgrade-only fixtures
  are deleted. Fresh initialization and current-form reopen remain the only
  production routes; noncurrent state requires reinstall or recreation.
- The maintained real-canister fixture now proves that explicit reinstall
  recreates clean current state without retaining an old-format mutation
  fixture. Current round-trip, malformed, checksum/fingerprint, and unknown
  future-version rejection coverage remains under each codec owner.
- This is an intentional pre-1.0 persisted and cursor-wire hard cut. It adds no
  stable-memory identifier, API, Candid method, export, timer, scheduler,
  runtime mode, configuration, decoder, or state axis; deleting the transition
  machinery makes the production shape smaller.
- Focused validation passes 24 commit-control/marker tests, 62 journal tests,
  three convergence-format tests, eight startup-observation tests, three
  startup-receipt tests, ten integrity-progress tests, four authenticated
  scalar-cursor tests, 13 persisted-format corpus tests, all 38 SQL-generator
  tests, two schema-proposal version tests, warning-denied core Clippy,
  integration all-target checking, durability invariants, formatting, shell
  syntax, diff hygiene, and the real-canister clean-reinstall fixture.
- The production `sql_perf` artifact is 4,812,136 compiler-emitted Wasm bytes,
  4,201,108 final deployable raw Wasm bytes, and 1,618,738 deterministic-gzip
  bytes: `-5,326`, `-4,747`, and `-5,753` respectively from the corrected
  `0.229.0` artifact. Candid remains byte-identical at 60,428 bytes with
  SHA-256
  `5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`,
  and the final artifact retains 70 exports.
- The slice touches 42 maintained files at approximately `+261/-704` lines
  (`-443` net). Normal commit/recovery work gains no operation; the removed
  activation branch and smaller raw Wasm make the implementation simpler.

## Patch 6 Integrated Dormant Candidate Results

### Frozen policy and individual-fit proof

- The candidate Gate 2 tuple is `B_max = 38`, `R_max = 16,384`, and
  `Y_max = 16,777,216` encoded bytes. It uses the absolute no-reuse registry
  bound, the canonical maximum journal-record count across maintained producer
  families, and the existing 16-MiB marker/envelope ceiling. The limits are
  engine-owned test evidence rather than caller configuration and become
  production authority only through atomic Patch 7 activation.
- The zero-debt maxima are `B_gate1_max = 38`, `R_gate1_max = 16,384`, and
  `Y_gate1_max <= 16,777,216`. The exact 38-store/4,096-row proposal, the
  240-row/64-index proposal, the 4,095-row plus one identity advance, the
  65,536-key accepted-index publication, and the 16,384-effect migration cap
  all fit the tuple. Existing Gate 1 keeps priority and remains
  `W = 4R + I + 4A + 4P <= 16,384`.
- `F_max = 64`. The conservative complete-callback candidate work maximum is
  `W_0_229_max = 81,959`: 16,384 inherited work units, one aggregate-control
  update, all 38 head comparisons, and 65,536 positioned retirements. No Gate
  1 ceiling was raised or relaxed.

### Instruction, selector, and residual evidence

- The current maintained 2,048-row/three-index recovery is the largest frozen
  canonical callback at 22,067,141,613 instructions. The isolated audit body
  performs the more expensive publish-plus-retire lifecycle for all 65,536
  positioned accepted-index keys and scans 38 heads in 293,529,644
  instructions. Its conservative sum with the maintained callback is
  22,360,671,257, below the 30-billion candidate callback ceiling by
  7,639,328,743 instructions and below the IC's 40-billion message limit by
  17,639,328,743.
- Fresh maximum accepted-index evidence remains one callback: 65,536 rows and
  keys use 30,810,697,510 producer instructions and 20,979,917,804 recovery
  instructions. Exact rows, keys, and one-message completion remain intact.
- The other maintained Gate-1 maxima also remain one callback: 4,096 zero-index
  rows use 16,268,056,830 recovery instructions, while 240 rows with 64
  secondary indexes use 4,363,668,249 producer and 15,545,694,673 recovery
  instructions. The 241st maximum-fanout row still rejects before publication.
- Selection uses only
  `(database commit sequence, journal allocation ID, journal sequence)` across
  at most 38 exact heads. Same-commit ties select the lower allocation; a cold
  older head wins while the lower-allocation hot tail continues to append.
  Newer positions on the same logical target survive older retirement and then
  retire exactly with their own batch.
- `C_driver = 4`: initial request, final quiescence/readiness handoff, at most
  one coalesced duplicate/no-work delivery, and the fixed watchdog transition.
  `I_driver_overhead_max = 5,000,000`; the current real scheduler sample is
  16,979. Therefore a no-new-debt interval satisfies
  `P_fold_normal <= B_0`, `M_total_normal <= B_0 + 4`, and
  `I_total_normal <= 30,000,000,000 * B_0 + 20,000,000`.

### Heap and physical stable bounds

- The frozen overlay function is `O <= 64R + 16,384`, giving
  `O_max = 1,064,960`. Every family uses the conservative common coefficient
  `alpha_f = 256` bytes; `H_fixed = 65,536`, `beta = 4`, `gamma = 256`, and
  `delta = 4,096`. Thus
  `H_overlay <= 65,536 + 256O + 4Y + 256B + 4,096S`, or 339,969,536 bytes at
  the tuple maximum.
- The 38-head scratch maximum is 912 bytes. Decode, preflight, Apply, and
  retirement reserve `Y`, `Y + 64O`, `Y + 32O`, and `32O` respectively, so
  `H_callback_peak <= 526,616,976` bytes at the maximum. Checked arithmetic
  rejects an unrepresentable bound instead of wrapping. The real-canister
  maximum positioned-metadata lifecycle grows Wasm memory from 2,031,616 to
  4,325,376 bytes while stable memory remains exactly 578,879,488 bytes, well
  inside the frozen callback heap ceiling.
- The exact physical bound remains
  `K <= B + floor(Y / 65,536)`, hence `K_max = 294`. A near-byte-ceiling fill
  retains 38 batches, 16,699,556 encoded bytes, and 266 live chunks. Three
  complete fill/drain/refill cycles reuse the same 513-page stable-memory
  high-water; historical cycles do not grow allocation.

### Dormancy and failure ordering

- The integrated coordinator is compiled only under `cfg(test)`. It admits one
  once-encoded marker proposal, appends those exact envelopes, publishes real
  positioned metadata, selects the global oldest head, completes decode and
  every retirement preflight, and then mechanically retires overlay and tail
  state. Pressure and malformed/missing candidate evidence fail before the
  modeled canonical mutation boundary.
- The IC instruction probe is available only in the audit canister's existing
  `test-admin-api` build. Production builds omit the endpoint. Production Gate
  2, positioned publication/retirement, global selection, online folding, and
  driver self-chaining still have no call edge; Patch 6 adds no persisted
  state, memory ID, configuration, scheduler cursor, timer, mode, or
  compatibility route.
- The production `sql_perf` artifact is byte-identical to Patch 5: 4,812,136
  compiler-emitted Wasm bytes, 4,201,108 final deployable raw Wasm bytes, and
  1,618,738 deterministic-gzip bytes. Production Candid remains exactly 60,428
  bytes with SHA-256
  `5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`,
  and the final artifact retains 70 exports.
- Patch 6 adds one test-only integrated coordinator and one local audit probe;
  production runtime complexity remains unchanged. The slice is approximately
  14 files and `+975/-15` lines, dominated by evidence code and this status.
  It adds no production state axis or duplicate runtime flow.

## Patch 7 Atomic Production Activation Results

### Exact admission and publication

- The canonical commit owner now runs cumulative Gate 2 after existing
  individual preparation/admission and before marker publication. It sums the
  exact current owner-local tail controls and the exact marker-owned envelopes
  later consumed by append, with checked arithmetic and the frozen
  `B <= 38`, `R <= 16,384`, and `Y <= 16,777,216` tuple.
- Sequence-zero direct-publication batches do not create retained journal debt.
  A cumulative rejection publishes neither marker nor tail bytes and does not
  consume the previewed database commit sequence. It returns retryable E284
  with the exact resource/current/proposed/limit facts and wakes the existing
  watchdog so sufficient drain makes retry succeed.
- Row, derived-index, accepted-schema, accepted-schema-index, validation-job,
  validation-index, identity, and migration publishers preflight their exact
  positions before marker publication. Migration append now consumes the same
  marker-owned encoded envelope instead of encoding a second authority.

### One online convergence route

- Successful debt-retaining commits schedule the existing generated startup
  watchdog. Its callback now drives both `Ready` and `Recovering`; quiescence,
  terminal failure, and generated-schema handoff still use the existing stop
  behavior. No second timer or scheduler is introduced.
- Each callback scans the bounded registered journal owners and selects the
  minimum `(database commit sequence, journal allocation id, journal
  sequence)`. It completely validates one selected batch and preflights its
  canonical effects, exact owner-local overlay retirements, and tail retirement
  before canonical mutation begins.
- Apply remains synchronous and complete-batch. A post-preflight contradiction
  traps through the existing fold boundary so IC whole-message rollback
  applies; otherwise canonical effects, only exact non-superseded overlays,
  and the tail watermark/control retire mechanically in the same message.
- Fold preparation reads the canonical predecessor row, index, accepted-schema,
  activation, validation-job, and identity views. Newer positioned effects
  remain current-state authority but cannot alter reconstruction of an older
  batch's canonical derived effects.
- Ready-state callbacks preserve positioned live overlays. Startup or
  post-upgrade recovery still clears every volatile position together with its
  live projection and rebuilds unpositioned state from canonical authority, so
  no mixed positioned/unpositioned runtime interval exists.

### Focused validation and footprint

- The 38-batch identity-backed fixture reaches exact batch pressure, verifies
  E284 facts and unchanged sequence/control/marker authority, drains through 38
  complete-batch callbacks, retries successfully, and preserves exact rows and
  identity high water. A same-key insert/update/update regression proves that
  newer live row and index effects remain visible while every older batch
  derives from its canonical predecessor and retirement leaves one exact final
  index membership. Startup receipt/corruption tests, migration recovery,
  position supersession tests, synchronous-update invariants, and
  generated-watchdog token checks pass.
- All seven integrated candidate tests pass. The isolated IC maximum remains
  65,536 positioned effects across 38 stores with 293,618,368 measured local
  instructions, 22,360,759,981 conservative combined instructions, unchanged
  stable-memory size, and 4,390,912 observed Wasm-memory bytes beneath the
  frozen callback/heap ceilings.
- The activated production `sql_perf` artifact is 4,881,595 compiler-emitted
  Wasm bytes, 4,263,315 final deployable raw Wasm bytes, and 1,645,434
  deterministic-gzip bytes. Relative to Patch 6 this is `+69,459`, `+62,207`,
  and `+26,696` bytes respectively. Production Candid remains exactly 60,428
  bytes with SHA-256
  `5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`,
  and the final artifact retains 70 exports.
- Patch 7 adds owner-local preflight tokens and one shared selector/fold route;
  it does not add a durable field, stable-memory ID, cursor, compatibility
  decoder, public method, runtime mode, timer, or caller configuration. The
  implementation is necessarily larger than the dormant candidate but keeps
  one semantic authority and one converged execution flow.

## Patch 8 Pre-Closeout Real-Canister Results

### Bound correction and singular callback shape

- The initial populated run exposed two accidental costs rather than a missing
  protocol bound. A soft multi-batch callback could begin another legal batch
  after earlier work, and row folding repeatedly resolved the same immutable
  canonical accepted authority while separately rebuilding row transitions for
  validation, retirement, and Apply.
- The production driver now selects and retires exactly one globally oldest
  complete batch per successful callback. It shares one batch-resolved
  canonical commit context and one prepared row transition across complete
  validation/preflight, positioned retirement, and mechanical Apply. Every
  referenced canonical owner is preflighted before mutation; malformed late
  rows still fail before canonical writes.
- No ceiling changed. The correction deletes the soft instruction/byte page
  budget and duplicate preparation route. It adds no persisted proof, cursor,
  overlay authority, recovery mode, configuration, timer, or compatibility
  path.

### Populated convergence, retry, and upgrade evidence

- At the Patch 8 handoff, a 2,048-row/three-index production commit folded
  online in one watchdog callback at 3,946,083,082 instructions, with
  10,813,440 observed Wasm-memory
  bytes. That is 26,053,916,918 instructions below the frozen 30-billion
  callback ceiling and well below both the 526,616,976-byte callback-heap bound
  and the IC's 40-billion message limit.
- Cold and 2,048-row populated histories loaded identical current debt of 38
  one-row batches, returned identical E284 batch-pressure facts, and each
  converged through exactly 38 successful callbacks. Their measured totals were
  1,114,818,866 and 1,127,326,339 instructions respectively; history changes
  cost, but not the residual message bound or the fixed ceiling.
- Both rejected rows became admissible after drain and each retry converged in
  one callback. The populated canister then upgraded with another 38 retained
  batches: startup reported `Recovering`, ordinary queries stayed gated, the
  existing watchdog drained all 38 batches in 1,923,573,708 instructions, and
  startup returned to `Ready`. Exact final counts, first/last/rejected row
  visibility, and all four maintained indexes pass before and after retry.
- Normal canonical materialization grew stable memory from 578,879,488 to
  721,485,824 bytes for the large history and later to 729,874,432 bytes; the
  mid-debt upgrade and its recovery reused that high-water. This expected
  population-dependent canonical growth is separate from the trap fixture,
  which proves byte-for-byte stable length and digest rollback before the
  canonical watchdog retries once and restores exact rows and indexes.

### Validation and artifact closure

- Focused complete-batch, online predecessor, late-row corruption, populated
  convergence, trap rollback/retry, and reinstall/recreation tests pass.
  Warning-denied core, integration, and generated audit-canister checks pass,
  including the generated watchdog match under current Clippy.
- The Patch 8 production `sql_perf` artifact was 4,885,008 compiler-emitted Wasm
  bytes, 4,266,504 final deployable raw Wasm bytes, and 1,646,534
  deterministic-gzip bytes. Relative to Patch 7 this is `+3,413`, `+3,189`,
  and `+1,100` bytes respectively.
- Production Candid remains exactly 60,428 bytes with SHA-256
  `5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`,
  and the final artifact retains 70 exports. Test-admin closeout endpoints do
  not enter the production build.

## Same-Minor Closeout Correction

The read-only closeout audit found three production defects that blocked the
initial closeout verdict:

- recovery unique-conflict validation read only canonical predecessor rows,
  so a valid same-batch unique swap or delete/reuse could fail after online
  publication;
- direct marker replay returned ordinary errors from Apply after complete
  validation instead of trapping an impossible contradiction for whole-message
  rollback; and
- global oldest-head selection fully decoded every nonempty store head rather
  than selecting from bounded fixed controls and decoding only the winner.

The correction keeps the existing owners and protocol shape. The canonical
commit reader now overlays the exact batch-final row image for conflict and
release checks while explicit row operations continue to own canonical before
images. Marker recovery resolves and validates all direct batches first,
finishes fallible retained-tail publication before direct mutation, and traps
any post-preflight Apply contradiction. The selector validates each exact
fixed tail control, compares `(database commit sequence, journal allocation,
journal sequence)`, and materializes only the selected complete batch.

Evidence maintenance in the same correction:

- the journaled unique regression covers swap and delete/reuse convergence;
- isolated convergence-format tests no longer require unrelated process-global
  commit wiring merely to register test-only backlog handles;
- interruption coverage drives one complete batch per recovery call until
  quiescence instead of assuming accumulated debt drains in one call;
- the 2,048-row/three-index assertion checks the frozen 30-billion ceiling
  rather than a stale 20-billion lower bound;
- the maximum accepted-index fixture drains older setup pages one at a time and
  retains a bounded 15,000-row suffix, leaving exact Gate 2 room for the
  65,536-key index batch;
- complete accepted-index staging reuses identical row-decode authority,
  avoids decoding non-unique keys for uniqueness, and does not repeat stable
  point reads after the complete current domain proves final-key absence; and
- the intentional inline scalar route enum carries one documented,
  target-neutral Clippy allowance.

No public API, persisted field, stable-memory identifier, cursor, compatibility
decoder, scheduler, timer, mode, configuration, or admission limit changes.
The refreshed 2,048-row/three-index callback is 2,615,527,509 instructions with
5,439,488 observed Wasm-memory bytes. The 65,536-key accepted index publishes
in 29,946,837,958 instructions; its one complete-batch fold peaks at
21,011,006,924 instructions, and the four retained setup batches plus that
batch converge in five callbacks totaling 31,113,972,716 instructions.

The refreshed production `sql_perf` artifact is 4,890,650 compiler-emitted
Wasm bytes, 4,270,970 final deployable raw Wasm bytes, and 1,647,976
deterministic-gzip bytes. Candid remains exactly 60,428 bytes with SHA-256
`5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`,
and the final artifact retains 70 exports.

## Release Validation Convergence Correction

The release-scale durable mutation fixture initially filled retained journal
debt without delivering the replicated watchdog between successful setup
commits. Gate 2 correctly rejected the next 1,024-record page at
`15,394 + 1,024 > 16,384`. Delivering the existing watchdog between setup
commits fixed the fixture's lifecycle model without changing Gate 2, adding a
scheduler, or relying on a later user mutation for progress.

That corrected production-shaped run exposed small duplicate costs at the
durable-job ceilings. Mechanical positioned publication repeated provenance
lookups already proved by complete preflight, mutation-progress Apply repeated
its stable predecessor comparison, and production commits built best-effort
rollback snapshots after their full preflight boundary. Apply now consumes the
preflighted position and progress evidence directly. Production no longer
builds rollback snapshots; an impossible post-preflight contradiction traps so
IC whole-message rollback remains authoritative. Test-only rollback guards
remain available for deterministic native failpoint coverage.

The sole current version-1 durable mutation policy narrows Verify inspection
from 224 to 208 authoritative keys while retaining the 56-row staging limit.
Its policy identity changes in place under the pre-1.0 hard cut. The 10,001-row
fixture therefore remains at 179 Forward calls and moves from 45 to 49 Verify
calls, still totaling 228 job advances beneath the maintained 256-call bound.

The refreshed exact fixture passes with maximum tier steps of 27,862,838
Forward and 7,597,129 Verify instructions against the unchanged 31,000,000 and
8,000,000 ceilings. The scoring maxima are 27,523,087 and 7,524,236;
post-upgrade recovery maxima are 7,765,300 and 7,370,271. Recovery returns all
10,001 rows, preserves stable-memory length at 1,124,139,008 bytes, observes
`Recovering` across the mid-debt upgrade, and returns to `Ready` after two
watchdog ticks. No public method, stable-memory identifier, persisted state
axis, admission limit, timer, scheduler, cursor, or compatibility path changes.
The refreshed production `sql_perf` artifact is 4,888,976 compiler-emitted
Wasm bytes, 4,269,683 final deployable raw Wasm bytes, and 1,647,662
deterministic-gzip bytes. Candid remains exactly 60,428 bytes with SHA-256
`5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`,
and the final artifact retains 70 exports.

Release validation also exposed stale SQL performance-fixture lifecycle
assumptions. Repeated reset/load and multi-ingress write measurements submitted
independent journaled calls without delivering the replicated watchdog, so the
exact 38-batch Gate 2 correctly rejected the next batch. Fixture reset now
drives that same watchdog to observed quiescence before measurement, distinct
write ingress calls are separated by convergence delivery, and startup
delivery covers the already-proved `B_0 + C_driver = 38 + 4` normal residual
bound rather than a stale 32-message test limit. The complete maintained
`sql_perf_audit` target passes all 30 tests with no production runtime or
artifact change.

## Closeout Disposition

- No implementation landing patch remains. Focused closeout evidence and the
  production artifact refresh pass; the user-owned release workflow may name
  the target and run the full repository validation suite.

## Markdown Validation

The repository sources use ordinary `#` headings, `|` table delimiters, `-`
list markers, unescaped links, and valid fenced code blocks. The escaped forms
reported in pasted review text are not present in the raw files.

## Patch 1 Change Surface

- Runtime and persisted-format delta: none.
- Public API, Candid, and generated-code delta: none.
- Performance, stable memory, heap, and raw Wasm delta: none.
- Complexity direction: design state space is more explicit. It adds one
  justified future database commit sequence and bounded no-reuse registry while
  removing unsafe activation order, current-`B` residual math, query-only
  overlay authority, and starvation ambiguity.
