# IcyDB 0.216 Final Closeout Audit

Date: 2026-08-01

## Verdict

**Ready for the proposed 0.216.5 release-owner push validation, with one
recorded non-blocking performance follow-up.**

All eight original patches, generated-trait Landings A through I, and the
bounded duplicate-shape/query-feature follow-ups are complete. The audit found
no correctness, authority, recovery, compatibility, or format blocker through
the proposed 0.216.5 boundary. The most serious remaining finding is still the
broad 12–13% instruction increase on the no-rule mutation/guard cohort relative
to 0.215.7. It is not specific to accepted-rule evaluation or widened by the
0.216.5 compile-time/internal-reexport work.

## Audit Identity And Scope

| Item | Audited value |
| --- | --- |
| Baseline tag | `v0.215.7` |
| Baseline commit | `7b8d1153c45f5de8eafe47a574b2fea802eefe7e` |
| Latest published baseline | `v0.216.4` |
| Audited worktree base | `5e5111b49fa5a891a2835ad08e548d1d741a3b34` (`v0.216.4`) |
| Proposed release boundary | `0.216.5` |
| Branch | `main` |
| Original line diff | `v0.215.7..v0.216.4` plus the audited `.5` worktree |
| Final-extension scope | generated traits, exact numeric atoms, internal query/SQL reexport gates, fixtures, dependencies, and release evidence |
| Final-extension footprint | 57 files, approximately `+1,576/-244`, including seven new emitter/compile-fixture files |

The earlier `v0.216.1..v0.216.2` correction footprint remains historical
evidence below. Release tooling owns package-version changes; this audit does
not edit workspace package versions. The `.5` worktree removes only the macro
crate's direct `derive_more` dependency edge from `Cargo.lock`.

## Reconstructed Landing Line

| Patch | Demonstrated result |
| --- | --- |
| 1 | The compiler emits the ten required application-value traits and rejects their explicit addition or removal while retaining separately owned ergonomic traits. |
| 2 | The exact rule grammar, local source-name identity, root/target/rule constraint-key derivation, callback boundaries, codec cut, and baseline evidence were frozen. |
| 3 | String-selected operations and positional operands were replaced by one typed five-operation source grammar with stable local rule names. |
| 4 | All five operations lower through one accepted compiler/evaluator; exact integer and decimal `MultipleOf` and the `ICYU` accepted profile were hard-cut without an old decoder. |
| 5 | Normalize, validate, and normalize-then-validate became explicit application operations with typed paths; database writes remain callback-free. |
| 6 | Same-identity semantic rule edits use the existing accepted-constraint activation, bounded validation, publication, abort, and recovery owner. |
| 7 | All maintained consumers, downstream fixtures, examples, READMEs, and macro rustdoc use the current grammar and authority model. |
| 8 | Hidden generated wiring and unused runtime conveniences were reduced to maintained construction sites, with focused correctness, cost, package, Wasm, and format evidence. |

The eight patches are demonstrably complete. The accepted schema remains the
only database authority. Generated model data is proposal/application
convenience only; no runtime reconstruction fallback was found. Rust
normalizers and validators execute only when application code calls them.
Typed, structural, SQL, retry, recovery, replay, derived work, and integrity
paths do not invoke application callbacks.

## 0.216.2 Post-Closeout Corrections

The final patch does not add a ninth design slice. It closes bounded fallout
and correctness issues adjacent to the completed line:

- timestamp/duration arithmetic now uses the complete signed/unsigned domain,
  and negative sub-millisecond conversion floors consistently;
- Blob is engine-neutral while the proposal literal retains its dedicated
  64 KiB admission bound;
- reduced predicate-only `UPPER` wrappers reject rather than masquerading as
  lowercase casefolding, full expression evaluation preserves Unicode
  uppercase semantics, and incompatible `UPPER` expression indexes are not
  selected for lowercase casefold lookup;
- scalar/list membership avoids temporary allocation without changing its
  public result contract; and
- minimal SQL/demo fixture builds compile only the helpers owned by their SQL
  feature, including the explicit signed RPG timestamp fixture.

## Rule, Trait, And Evolution Assessment

- The maintained catalog contains 15 durable rule declarations, all using the
  closed typed grammar.
- `RuleSourceKey` remains the bounded local rule name. Source declaration
  identity is `(TypeSourceKey, RuleSourceKey)`, and each persisted-root use
  derives its accepted constraint key from root field, target type, and rule.
- Length range, numeric minimum, numeric maximum, numeric range, and exact
  integer/fixed-scale-decimal multiple-of each have one canonical lowering and
  accepted evaluator. Zero, float, inexact, and wrong-kind divisors reject.
- Same-name semantic edits retain accepted identity and use catalog-native
  activation. Name changes remain removal plus addition; 0.216 adds no rename
  inference or migration framework.
- Required traits are compiler-owned. The ten required and 28 configurable
  target traits now use one closed node-specific matrix. `Default`, `From`,
  custom normalize and validate, dereference, arithmetic, wrapper, and
  shape-derived behavior retain their documented output ownership.
- Searches found no live old `kind = "..."`/`args(...)` authoring, dual
  expansion, old `ICYT` decoder, callback registry, source-model evaluator, or
  compatibility alias. Historical and recreation documentation is retained as
  documentation only.

## Persisted And Public Format Assessment

The complete 0.216 line changes these current-form contracts:

- the version-1 proposal's nested durable-rule operation is now one of five
  typed operations with named operands;
- the version-1 accepted schema profile is `ICYU`, with fingerprint tags 4 and
  5 appended for numeric maximum and multiple-of;
- `Date` keeps its `i32` epoch-day representation in rows, cursor tokens, and
  Candid while current construction and decoding accept only years 0000
  through 9999;
- public Rust application diagnostics carry typed operation, callback, and
  bounded value-path identity, and removed Rust conveniences have no aliases.
- Blob Candid values may exceed the proposal-literal limit, overflowing
  duration text rejects, and pre-epoch timestamp conversions floor rather than
  truncate toward zero.
- Reduced predicate-only `UPPER` forms now fail with the maintained typed
  unsupported-feature diagnostic; full SQL `UPPER` expressions remain
  available with their authored semantics.

Rows, primary/secondary keys, layouts, relations, commit markers, journal
records, Identity state/ranges, integrity progress, database-error wire, and
public database Candid response layouts otherwise remain unchanged. The Date
domain restriction is nevertheless a behavioral hard cut: out-of-domain
development state must be recreated and has no fallback decoder.

## Findings And Corrections

### Medium — no-rule mutation cost is not neutral

The released 0.216.1 artifact compared with an isolated 0.215.7 build shows:

| Cohort | 0.215.7 | 0.216.1 | Delta |
| --- | ---: | ---: | ---: |
| Heap first insert | 27,089,674 | 30,820,407 | +13.772% |
| Heap steady insert | 20,465,034 | 23,073,074 | +12.744% |
| Journaled first insert | 27,331,411 | 30,894,947 | +13.038% |
| Journaled steady insert | 21,083,607 | 23,681,836 | +12.323% |

Steady update, delete, and write/read increases range from 11.857% to 12.687%.
In contrast, the matched incremental cost of one accepted rule is 0.778% to
1.371%. Symbol comparison shows only a modest increase in the shared mutation
preparation functions and a larger rule evaluator, while query cost is nearly
neutral. A temporary early return for an empty targeted-rule program changed
steady measurements only within noise and was discarded. No speculative code
change was made.

This is the only unmet original checklist target. It is a performance risk,
not evidence of a second authority or a semantic defect, and remains an
explicit follow-up.

### Low — release evidence required a final 0.216.2 refresh

The status and detailed changelog recorded the pre-release artifact and crate
package. They now record the released outputs:

- SQL performance Wasm: 5,327,610 raw bytes, SHA-256
  `e8cbe879d0a642f9824dc555ee4ddc3fea4edd514d2f30ff99200c5b73b3ac78`;
- delta from the 5,302,780-byte Patch 2 artifact: 24,830 bytes, 0.468%;
- delta from released 0.216.1: -462 bytes, -0.009%;
- `icydb-model` crate: 101,684 compressed bytes, 109 files, SHA-256
  `e6eb8730e52d3aba843740f5b294f1e978ccf5adbd8f2c95aa010033a20f8562`.

The design status, release diff, Date domain inventory, and missing detailed
changelog separator were corrected. This evidence refresh changed no runtime
implementation.

### Inherited — advertised dependency MSRV is not independently provable

`icydb` advertises Rust 1.88, while unconditional model/macro dependencies use
the workspace Rust 1.96 declaration. The focused Rust 1.88 check fails during
dependency admission rather than compiling the facade. This mismatch already
exists at `v0.215.7`; it was neither introduced nor widened by 0.216 and is not
a 0.216 correction. It remains a repository-level release concern.

## Validation Evidence

The release and closeout evidence includes:

- formatting and diff-hygiene checks: passed;
- `icydb-schema`: 88 tests passed;
- `icydb-model-macros`: 130 tests passed;
- `icydb-model`: 132 unit tests and two compile-contract harnesses passed;
- accepted targeted-rule core cohort: 15 tests passed;
- exact multiple-of core cohort: 2 tests passed;
- schema-only fixture: 1 test passed;
- renamed typed-adapter fixture: 3 tests passed;
- all-feature and no-default facade/core checks: passed;
- warnings-denied focused Clippy for affected packages and feature matrices:
  passed;
- generated SQL actor, SQL-free typed-query, architecture, durability,
  dependency, schema/model, generated-build, and mutation-atomicity checks:
  passed;
- maintained SQL performance target: 21 tests passed;
- final 0.216.2 raw-Wasm build target: 1 test passed;
- focused 0.216.2 core cohorts: 12 `UPPER`, 6 `contains_any`, and 5
  `contains_all` tests passed;
- SQL and RPG canisters checked without default features, and the RPG fixture
  package test/doc-test target passed;
- direct release-artifact and package measurements: reproduced;
- targeted no-rule performance comparison and rejected shortcut probe:
  completed.

The full workspace and push-time suites were not run by the agent, as required
by repository policy; they remain user-owned validation. The Rust 1.88 check
failed for the inherited manifest mismatch described above. No current test
failure is attributed to 0.216.

## Remaining Risk

The 12–13% common mutation/guard regression still lacks exact attribution. It
should be profiled from the frozen `v0.215.7` and `v0.216.2` artifacts before a
future performance-sensitive release, but it does not justify reopening the
0.216 authority design or landing an unmeasured shortcut. The focused `.2`
validation found no additional correctness or format risk.

## Generated Trait-Policy Follow-Up Closeout

**Verdict: ready to close.** The bounded follow-up accepted after `v0.216.3`
is complete through proposed 0.216.5 and introduces no new blocker.

| Item | Audited value |
| --- | --- |
| Final-extension baseline | `v0.216.4` |
| Audited worktree base | `5e5111b49fa5a891a2835ad08e548d1d741a3b34` |
| Branch | `main` |
| Planned landings | A through I complete |
| Target matrix | 38 traits across eight application node kinds |

The audit reconstructed one policy authority: ten required traits and 28
configurable traits are classified before directives for entity, enum, list,
map, newtype, record, set, and tuple. Every accepted addition creates output,
every accepted removal suppresses output, and any selected trait without one
derive or generated-implementation strategy rejects before emission.

The hard cut is complete. `Inherent`, `Collection`, `MapCollection`, and the
trait form of `Sorted` have no live variant, implementation, reexport, source
form, alias, or fallback. Enum ordering is the enum-only top-level `sorted`
option; list, set, and map ergonomics use standard-container dereference.

The most serious closeout finding was bounded implementation sediment rather
than a behavioral defect: fixed canister/store/normalizer/validator nodes
still instantiated an empty application trait builder, and `TraitSet` retained
unused token-rendering and vector-conversion routes. The correction gives
fixed generated nodes their exact trait set directly, uses ordered set storage
for deterministic emission, and deletes the unused generic routes. Maintained
generated output is unchanged.

Focused validation passes:

- `icydb-schema`: 90 tests passed;
- `icydb-model-macros`: 147 tests passed; one non-runnable README syntax
  doctest remains intentionally ignored;
- `icydb-model`: 132 unit tests and all four compile-contract harnesses passed;
- generated schema fixtures: collection and exact-operator tests passed;
- warnings-denied all-targets Clippy passed for schema, macro, model, and
  fixture packages;
- the facade passed its no-default-features check; and
- formatting and diff hygiene passed.

This follow-up changes Rust macro policy and generated Rust ergonomics only.
It changes no accepted schema, persisted byte format, Candid response or method,
endpoint, or runtime execution algorithm. Landings G through I add exact
primitive capabilities, standard collection protocols, and the completed
operator family without Serde output. The final maintained SQL performance
Wasm is 5,326,191 raw bytes with SHA-256
`060064ca24498948357ad9fa29711ab6c453d9f7af1a87a1ea4a6853587a007a`.

## 0.216.5 Final Extension Audit

The concurrent query-owner cleanup is included in this final extension. It
narrows SQL-only and SQL-diagnostics-only internal reexport chains to their
actual consumers while leaving implementation modules under the
engine-neutral query substrate. Query-only and query-plus-diagnostics
warnings-denied all-target Clippy pass after deleting the now-unneeded
crate-wide `unused_imports` expectation. The separate dormant-code expectation
remains and is not disguised as completed feature decomposition.

No removed trait target, no-op trait directive, generated Serde output,
duplicate collection abstraction, or unused macro dependency feature remains.
The dependency graph shows only `derive_more` `add` and `add_assign`, both
consumed by canonical big-integer atoms. `RemAssign`, `Product`, and `Neg`
compile through their exact fixed, decimal, and arbitrary-precision wrapper
sets; finite floats do not receive `Neg`.

The audit found no implementation defect. Its bounded corrections are release
evidence only: the main design status is advanced from follow-up-in-progress,
the root Unreleased notes are consolidated into one 0.216.5 entry, the detailed
changelog records the exact public Rust additions and manual-implementation
opt-out, and this report advances its audit identity through the current
worktree.

Current validation additionally includes warnings-denied query-only and
query-plus-diagnostics core all-target Clippy; no-feature, SQL, SQL diagnostics,
and SQL-explain core builds; SQL diagnostics library Clippy; two public query
facade compile contracts; and three trusted-SQL facade compile contracts.
Formatting, diff hygiene, focused package tests/Clippy, dependency inspection,
and the raw-Wasm build pass.

Full repository/workspace tests and the push-time workflow were not run, per
repository governance, and remain release-owner validation. SQL all-target
Clippy retains the separately documented pre-existing test-helper dead-code
findings; production SQL library Clippy passes. The original no-rule
mutation-cost follow-up remains non-blocking and is not caused or widened by
the compile-time trait-policy or internal reexport changes.
