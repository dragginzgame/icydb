# 0.47 - Audit Pass

## Status

Closeout Workstream Active
Target: `0.47.0`
Dependency: `0.46` standards-alignment baseline
Last updated: 2026-03-10

---

## 0. 0.47.0 Kickoff Preparation

Kickoff readiness status: `Ready`

Preparation completed before `0.47.0` implementation start:

- recurring audit definition tree normalized under stable domain boundaries
- recurring report storage migrated to month/day layout:
  - `docs/audits/reports/YYYY-MM/YYYY-MM-DD/`
  - month-level `summary.md` rollups added
- design/status tracking co-located under versioned design directories:
  - `docs/design/0.47-audit-pass/0.47-design.md`
  - `docs/design/0.47-audit-pass/0.47-status.md`
  - AccessPath architecture contract now consolidated into this file (`Section 10`)
- governance docs updated to reflect active recurring/report layout contracts

`0.47.0` start checklist:

- [x] baseline audit bundle identified (`2026-03-09`)
- [x] report path conventions stabilized and documented
- [x] status/design trackers linked and active
- [x] execute first `0.47.0` structural-pressure reduction slice

---

## 1. Objective

Run a full recurring architecture/correctness audit pass for the `0.47` line,
use findings to prioritize hardening slices, and keep audit history append-only
with explicit ownership and verification evidence.

Primary goals:

- execute all recurring audits and publish one report set per run day
- reduce high structural-risk surfaces before new feature expansion
- preserve boundary ownership and invariant enforcement contracts
- execute AccessPath architecture hardening from the consolidated contract in:
  - `docs/design/0.47-audit-pass/0.47-design.md` (`Section 10`)

---

## 2. Non-Goals

- no query-surface expansion by default
- no speculative refactors without a linked audit finding
- no compatibility shim work
- no redesign outside the owner boundary implicated by the accepted finding

---

## 3. Entry Gate Baseline (2026-03-09)

Baseline reports:

- `docs/audits/reports/2026-03/2026-03-09/summary.md`
- `docs/audits/reports/2026-03/2026-03-09/meta-audit.md`

Current risk snapshot:

- complexity-accretion: `7/10`
- module-structure: `6/10`
- velocity-preservation: `6/10`
- layer-violation: `5/10`
- correctness-focused audits (`boundary`, `cursor`, `index`): `3/10`

Operating constraints for `0.47`:

- keep invariant checks green in every relevant slice:
  - `scripts/ci/check-layer-authority-invariants.sh`
  - `scripts/ci/check-index-range-spec-invariants.sh`
  - `scripts/ci/check-field-projection-invariants.sh`
  - `scripts/ci/check-memory-id-invariants.sh`
  - `scripts/ci/check-architecture-text-scan-invariants.sh`
- do not let decision-owner counts drift upward without explicit justification
- preserve `docs/audits/reports/YYYY-MM/YYYY-MM-DD/` append-only history

### 3.1 Complexity-Accretion Feedback Intake (2026-03-09)

Feedback source:

- complexity-accretion review after `docs/audits/reports/2026-03/2026-03-09/`

Assessment:

- the rise from `6 -> 7` is treated as a growth-pattern warning, not a failure
- dominant pressure source is route-shape decision-owner expansion (`3 -> 5`)
- secondary pressure sources are route/validation branch growth and
  continuation surface expansion

Accepted interpretation:

- no catastrophic architectural break detected in this run
- cross-layer policy re-derivations remain `0`
- comparator authority leakage remains `0`
- continuation conceptual authority remains centralized

Committed actions:

- status: `Accepted`
- implementation state: `In Progress`
- owner boundary: `query/plan` + `executor/route` + `query/plan/validate`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/`

Target metrics:

- RouteShape decision owners: `5 -> <=3`
- `executor/route` branch pressure: reduce `if` density toward shape-`match`
  dispatch
- `query/plan/validate` split into narrower policy-owned helper modules
- complexity risk index target: `7 -> 6` (or lower) within one audit cycle

### 3.2 Module-Structure Feedback Intake (2026-03-09)

Feedback source:

- module-structure review after `docs/audits/reports/2026-03/2026-03-09/`

Assessment:

- no architectural invariant break was detected
- directional/layer invariants remain intact
- primary risk is coordination-surface growth, not storage/correctness collapse

Accepted interpretation:

- route-planner containment and AccessPath authority remain the highest-leverage
  structural controls for this cycle

Committed actions:

- route planner hub containment via explicit staged contract:
  - route planner -> `RouteShape` -> executor dispatch
  - route planner should consume access-route contracts, not access internals
- AccessPath decision authority limited to:
  - `query/plan/route`
  - `query/plan/feasibility`
  - `executor/dispatch`
- AccessPath may be constructed only in planner-owned route/feasibility
  authority and consumed elsewhere through staged contracts or capability
  methods.
- module size policy:
  - soft cap: `600 LOC`
  - hard cap: `900 LOC`
  - modules above soft cap must be evaluated for decomposition
- capability-first consumption:
  - prefer `guarantees_order()` / `supports_resume()` / `is_index_backed()`
  - avoid fan-out from scattered enum-branch logic

Target metrics:

- status: `Accepted`
- implementation state: `In Progress`
- AccessPath decision owners: `5 -> <=3`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/`

### 3.3 Velocity-Preservation Feedback Intake (2026-03-09)

Feedback source:

- velocity-preservation review after
  `docs/audits/reports/2026-03/2026-03-09/velocity-preservation.md`

Assessment:

- score rise `5 -> 6` indicates coordination-pressure growth, not velocity
  collapse
- dominant pressure signals:
  - average files touched per slice: `2.3 -> 15.0`
  - p95 files touched: `3 -> 17`
  - top gravity-well fan-in proxy: `117 -> 255`
- route-planner and load surfaces are the highest-leverage containment seams

Accepted interpretation:

- hard boundary leakage remains controlled:
  - planner -> executor types: `0`
  - index -> query types: `0`
  - cursor -> executable internals: `0`
- current risk is cross-subsystem coordination cost, not boundary violation

Committed actions:

- status: `Accepted`
- implementation state: `In Progress`
- owner boundary: `executor/route/planner` + `executor/load` + `query/plan`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/velocity-preservation.md`

Target metrics:

- enforce route planner staged contract:
  - planner -> `RouteShape` -> executor dispatch
  - route planner consumes access-route contract surfaces, not access internals
- decompose load hub to reduce coordination fan-in:
  - `executor/load/dispatch`
  - `executor/load/strategy`
  - `executor/load/terminal`
- add slice-size containment guardrails:
  - median files touched per slice: target `<=8`
  - p95 files touched per slice: target `<=15`
- reduce recurring velocity risk index toward `5` in one audit cycle

---

## 4. Workstreams

### Execution Order

1. structural pressure containment
2. route-shape / AccessPath authority consolidation
3. load hub decomposition
4. grouped DISTINCT drift reduction
5. correctness parity locks
6. full rerun and closeout

---

### Non-Regression Guardrails

- do not increase RouteShape decision owners above baseline without documented
  exception
- do not increase cross-layer import count in `executor/route/planner` without
  owner-approved mitigation
- do not add new runtime modules at `>=600 LOC` without decomposition note
- do not add new AccessPath variant-branch sites without capability-first review

---

## 4.1 Structural Pressure Reduction

Scope: complexity/module/velocity recurring audits.

Checklist:

- [ ] reduce route planning cross-layer hub pressure
- [ ] reduce large-module concentration in high-growth runtime hubs
- [ ] reduce branch-surface growth in planner/route hot seams
- [ ] enforce module size guardrails (`soft 600`, `hard 900`) with explicit
      decomposition review for oversized modules
- [ ] keep route planner on staged route contracts (`RouteShape` boundary)
      rather than direct access-internal dependencies
- [ ] decompose `executor/load` hub into `dispatch` / `strategy` / `terminal`
      seams with explicit ownership boundaries
- [ ] drive slice-size containment back under guardrails:
      `median <=8`, `p95 <=15` files touched per feature slice

Evidence targets:

- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/complexity-accretion.md`
- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/module-structure.md`
- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/velocity-preservation.md`

Workstream operating rule:

- treat `0.47` findings as growth-pressure management, not architecture-failure
  response, while invariants remain green

## 4.2 Layer Ownership and Duplication Control

Scope: layer-violation + dry-consolidation recurring audits.

Checklist:

- [ ] keep policy ownership planner-centered and executor fail-closed
- [ ] keep comparator/continuation authority index-centered
- [ ] keep intentional defensive duplication explicit and bounded
- [x] reduce divergence-prone grouped DISTINCT planner/runtime overlap by
      centralizing policy-reason projection and minimizing runtime policy
      re-derivation at route/load boundaries

Evidence targets:

- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/layer-violation.md`
- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/dry-consolidation.md`

Current high-risk DRY seam (from recurring history + current code scan):

- grouped DISTINCT policy/runtime overlap across:
  - `db/query/plan/validate/grouped/policy.rs` (planner policy gates)
  - `db/query/plan/group.rs` (planner handoff policy contract)
  - `db/executor/load/grouped_distinct.rs` (runtime fail-closed checks)
  - `db/executor/aggregate/contracts/grouped/context.rs` (runtime DISTINCT
    budget admission)

Current mitigation status:

- policy-reason projection has been centralized under
  `GroupDistinctPolicyReason` (`planner_group_plan_error` +
  `invariant_message`) in the working tree; next recurring rerun remains
  required to confirm DRY-risk reduction.

`0.47` handling decision:

- this is worth fixing in `0.47` as a drift-reduction hardening item
- keep planner/executor split intact; do not collapse layers
- focus on one shared policy-reason authority and thinner runtime guard adapters

Operational output requirements:

- one shared planner-owned policy-reason surface
- runtime checks reduced to fail-closed contract assertions
- no new planner/runtime semantic fork introduced

## 4.3 Correctness Contract Preservation

Scope: boundary semantics, cursor ordering, index integrity, invariants,
state machine, and recovery consistency.

Checklist:

- [ ] preserve continuation monotonicity and envelope containment
- [ ] preserve commit/recovery replay equivalence
- [ ] preserve invariant parity across normal and recovery paths
- [ ] add explicit parity checks for unique-enforcement behavior between live
      mutation apply and replay apply paths (same violations, same outcomes)
- [ ] validate composite-prefix continuation anchors keep full ordering-key
      resume semantics (no duplicate-row replay on resume)
- [ ] validate reverse-traversal continuation rewrite semantics at correct bound
      edge for DESC paging paths (if/when enabled)

Evidence targets:

- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/boundary-semantics.md`
- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/cursor-ordering.md`
- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/index-integrity.md`
- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/invariant-preservation.md`
- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/state-machine-integrity.md`
- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/recovery-consistency.md`
- `index-integrity.md` must include explicit live-apply vs replay
  unique-parity note

## 4.4 Contract Taxonomy Compliance

Scope: error-taxonomy + resource-model compliance recurring audits.

Checklist:

- [ ] keep class/origin mapping explicit and stable
- [ ] keep grouped-resource boundedness policy fully compliant
- [ ] verify budget-exhaustion paths fail closed with deterministic error
- [ ] verify budget propagation across planner -> route -> executor boundaries
- [ ] verify runtime resource usage monotonicity and per-query reset semantics
- [ ] verify grouped operator resource-class coverage remains complete
- [ ] verify Class B operators cannot route through unbudgeted paths and
      Class C shapes are rejected pre-execution

Evidence targets:

- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/error-taxonomy.md`
- `docs/audits/reports/YYYY-MM/YYYY-MM-DD/resource-model-compliance.md`

---

## 5. Required Artifacts Per Audit Run

- one report per recurring audit definition
- one `meta-audit.md`
- one `summary.md`
- report preamble fields required by `docs/audits/AUDIT-HOWTO.md`
  - same-day reruns compare to day-baseline files (`<scope>.md`)
- `Verification Readout` with `PASS`/`FAIL`/`BLOCKED` outcomes
- explicit follow-up actions whenever:
  - any finding is `PARTIAL` or `FAIL`
  - any overall risk index is `>= 6`
- closeout snapshot block:
  - starting risk indices
  - ending risk indices
  - items reduced
  - items deferred with owner/action/target-run
  - intentionally accepted residual risk

---

## 6. Slice Plan (Patch-Neutral)

Use slice labels unless a concrete patch tag has already been assigned.

- Slice A: structural pressure containment
- Slice B: layer ownership and duplication boundaries
- Slice C: correctness contract hardening
- Slice D: taxonomy/compliance lock pass
- Slice E: rerun full recurring audit bundle and publish closeout summary

---

## 7. Definition of Done

`0.47` audit pass is complete when:

- all recurring audits are rerun with dated report artifacts
- no required report sections are missing
- no verification command is unresolved without a concrete blocker reason
- high-risk surfaces (`>=6`) are either reduced or explicitly deferred with
  owner/action/target-run
- meta-audit confirms coverage and governance conformance

---

## 8. Open Findings Queue (Append Here)

- [x] route-shape policy authority fragmentation addressed in working tree
      (`RouteShape decision owners: 5 -> 3`);
      owner boundary: `query/plan` + `executor/route`; action landed:
      route-planner contract containment, entrypoint decomposition, and
      feasibility decomposition; rerun confirmation target:
      `docs/audits/reports/2026-03/2026-03-12/`
- [x] route and validation branch growth pressure originally measured at
      (`executor/route if=89`, `query/plan/validate if=74` baseline) has been
      reduced through shape/feasibility contract decomposition and
      grouped-validation helper extraction
- [x] route and validation branch growth pressure materially reduced in reruns
      (`executor/route if=89 -> 18`,
      `query/plan/validate if=74 -> 9`) with `Complexity Risk Index 7 -> 6`
      in `docs/audits/reports/2026-03/2026-03-09/complexity-accretion-5.md`
- [ ] _(add finding)_ owner boundary: ___, action: ___, target run: ___

---

## 9. Running Notes (Append-Only)

- 2026-03-09: document initialized from recurring audit baseline.
- 2026-03-09: route/load/query structural containment slices landed in working
  tree:
  - route planner entrypoints moved from `planner/mod.rs` to
    `planner/entrypoints.rs`
  - route contracts split into `contracts/{execution,continuation,shape,capabilities}`
  - route feasibility split into
    `planner/feasibility/{mod,gates,grouped_strategy}`
  - load contracts and entrypoint pipeline split into directory modules
  - grouped validate policy split into `grouped/policy/{mod,rules}`
  - validate root taxonomy moved into `validate/errors.rs`
- 2026-03-10: AccessPath architecture companion design was consolidated into
  this file under `Section 10`; companion path is retained as a historical
  snapshot to avoid information loss.


---

## 10. AccessPath Architecture (Consolidated Companion)

Consolidation status:

- merged from `docs/design/0.47-accesspath-architecture/0.47-design.md` on 2026-03-10
- rationale: keep one primary `0.47` design source while preserving prior companion context
- note: companion doc remains in-place as a historical snapshot to avoid loss

### Status

Consolidated
Target: `0.47.0`
Dependency: `0.47` audit-pass baseline
Implementation state: `Complete (monitoring mode)`
Last updated: 2026-03-10

---

### 1. Purpose

Define `AccessPath` as a physical storage traversal contract, not a planning
policy surface.

`AccessPath` answers:

- how storage is traversed
- what traversal guarantees the executor can assume
- what continuation semantics apply to that traversal

`AccessPath` does not answer:

- why a path was selected
- whether the path is legal for a query shape
- which path should be preferred

Those decisions remain planner-owned.

---

### 2. Ownership Model

Owner boundary:

- owner subsystem: `db/access/*`
- authoritative types:
  - `db/access/path.rs` (`AccessPath`)
  - `db/access/plan.rs` (`AccessPlan`)
  - `db/access/dispatch.rs` (canonical path dispatch)

Primary consumers:

- `db/query/plan/*` constructs/lower paths
- `db/executor/*` executes lowered contracts
- `db/query/explain/*` renders path semantics

Non-owners:

- `cursor`
- `index`
- `codec`
- `query/intent`

Rule:

- only planner-side boundaries construct semantic `AccessPath` contracts for
  production query flow
- non-owner layers treat `AccessPath` as immutable and consume through
  dispatch/capability boundaries

---

### 3. Decision Authority Rule

Target budget:

- `AccessPath` decision owners `<= 3`

Allowed decision-owner boundaries:

- `query/plan/route` (path selection policy)
- `query/plan/feasibility` (shape legality/pushdown feasibility)
- `executor/dispatch` (execution dispatch only, no planner policy)

Core authority sentence:

- `AccessPath` may be constructed only in planner-owned route/feasibility
  authority and consumed elsewhere through staged contracts or capability
  methods.

Forbidden locations for new `AccessPath` decisions:

- `executor/load`
- `cursor`
- `index`
- `storage/commit`

Those layers may validate and execute contracts, but should not re-derive
path-selection policy.

Current baseline (2026-03-09):

- `AccessPath decision owners: 5` (above target)

Required correction for `0.47`:

- converge current ownership to the three allowed boundaries above
- all other layers must treat `AccessPath` as immutable data contracts

---

### 4. Variant Contract

Current semantic variants (`db/access/path.rs`):

- `ByKey`
- `ByKeys`
- `KeyRange`
- `IndexPrefix`
- `IndexMultiLookup`
- `IndexRange`
- `FullScan`

#### 4.1 Variant Invariants

| Variant | Required Invariant | Ordering Guarantees | Cursor Compatibility | Resume Semantics |
|---|---|---|---|---|
| `ByKey` | one canonical primary-key payload | deterministic singleton | compatible | resume is trivial (terminal/single-hit) |
| `ByKeys` | key set semantics (dedupe/canonical order) | planner-defined deterministic key-set order | compatible | resume by stable key-set progression |
| `KeyRange` | bounded primary-key range | primary-key order | compatible | strict monotonic continuation over key bounds |
| `IndexPrefix` | prefix arity and strict value coercion validity | index-defined ordered traversal | compatible | strict continuation within prefix envelope |
| `IndexMultiLookup` | canonicalized lookup set over leading index slot | per-lookup ordered traversal + union contract | compatible with planner constraints | resume must preserve per-lookup canonical order contract |
| `IndexRange` | equality prefix + bounded range component contract | index-ordered traversal | compatible | strict continuation monotonicity with bounded envelope |
| `FullScan` | no index-assisted narrowing | storage/default traversal order contract | compatible only where planner policy permits | resume semantics depend on planner+route paging contract |

#### 4.2 `IndexRange` Explicit Contract

`IndexRange` requires:

- ordered index traversal source
- explicit semantic envelope bounds
- valid index id/arity/prefix contract

`IndexRange` guarantees:

- deterministic ordered traversal
- strict continuation monotonicity when continuation is present
- bounded envelope revalidation at runtime

---

### 5. Execution Contract

Executor may assume:

- planner legality checks already passed
- lowered access envelope/bounds are shape-valid
- index identity contracts are already validated at planner/lowering boundaries

Executor must still:

- fail closed on runtime invariant violations
- execute traversal exactly as declared by the contract
- apply projection/filter/paging semantics without mutating access semantics

This keeps planner policy and executor runtime responsibilities cleanly split.

---

### 6. Routing Model

Adopt/maintain a two-step mapping:

`AccessPath` -> route/access classification -> executor route shape dispatch

Current building blocks:

- `AccessRouteClass` (`db/access/execution_contract/route.rs`)
- `RouteShapeKind` / `ExecutionRouteShape`
  (`db/executor/route/contracts.rs`)

Goal:

- keep direct `AccessPath` branching centralized (`db/access/dispatch.rs`)
- route/executor consumers operate on route/capability contracts instead of
  variant-specific branching spread

Containment rule:

- route planner hub follows `route planner -> RouteShape -> executor dispatch`
- route planner should not directly depend on access internals beyond exposed
  access-route contract surfaces
- route planner cross-layer dependency pressure should trend toward a single
  cross-layer contract boundary (`<=1` tracked cross-layer family)

#### 6.1 AccessPath -> RouteShape Mapping Contract

Mapping must remain explicit and centralized.

Current-direction mapping examples:

| AccessPath Variant | Route/Dispatch Shape Intent |
|---|---|
| `ByKey` | single-lookup load/aggregate route shape |
| `ByKeys` | key-set lookup route shape |
| `KeyRange` | ordered primary-key scan route shape |
| `IndexPrefix` | ordered secondary-index scan route shape |
| `IndexMultiLookup` | multi-lookup secondary route shape |
| `IndexRange` | ordered bounded secondary-index scan route shape |
| `FullScan` | full-scan route shape |

Rule:

- mapping tables are planner/route-authority data, not ad-hoc executor/load
  branching logic

Expected outcomes:

- fewer branch sites
- cleaner EXPLAIN coupling
- lower fan-out in runtime routing modules

---

### 7. Extension Rules

Before adding a new `AccessPath` variant, answer:

1. Is this a new physical traversal mode?
2. Does it change ordering guarantees?
3. Does it change continuation/resume behavior?

If all answers are `no`, do not add a variant.

Should not become new variants:

- predicate pushdown preference
- covering-detection policy
- cost-based preference/candidate ranking

May justify a new variant:

- new traversal primitive over storage/index substrate
- materially different ordering contract
- materially different continuation contract

---

### 8. Capability API Direction

Preferred consumption pattern:

- consumers call capability methods
- consumers avoid scattered `match AccessPath::...` branching

Capability examples (target API direction):

- `guarantees_order()`
- `supports_strict_resume()`
- `is_index_backed()`
- `supports_reverse_traversal()`

Example contract direction:

```rust
impl<K> AccessPath<K> {
    fn guarantees_order(&self) -> bool;
    fn supports_strict_resume(&self) -> bool;
    fn is_index_backed(&self) -> bool;
}
```

Current baseline:

- `AccessRouteClass` already carries route-facing capabilities; continue moving
  runtime consumers toward this contract surface.

---

### 9. Explain Integration

Rule:

- `AccessPath` must remain explainable through stable contract surfaces without
  leaking planner internals.

Direction:

- preserve one explain-description boundary for access traversal semantics
- avoid EXPLAIN call sites reaching into variant internals outside designated
  rendering adapters

Target API direction:

```rust
pub(crate) struct AccessPathDescription {
    // stable explain-facing traversal description fields
}

impl<K> AccessPath<K> {
    fn describe(&self) -> AccessPathDescription;
}
```

---

### 10. Metrics and Guardrails

Track in recurring audit/CI:

- `AccessPath` variant count
- `AccessPath` decision-owner count
- `AccessPath` reference-site count
- `AccessPath` direct match/branch site count
- route-planner hub import pressure (HIP)
- feature-slice touch-size pressure tied to access/route changes

Suggested thresholds:

- variants `<= 10`
- decision owners `<= 3`
- branch-site growth must be justified when increasing
- module size guidance:
  - soft cap: `600 LOC`
  - hard cap: `900 LOC`
  - modules above soft cap require decomposition review
- route-planner cross-layer count `<=1` in HIP table
- slice-size containment guardrails:
  - median files touched per slice `<=8`
  - p95 files touched per slice `<=15`

Current baseline snapshot (2026-03-09):

- variant count: `7`
- decision owners: `5`
- reference sites: `121` across `12` files
- direct match-arm lines containing `AccessPath::` in non-test runtime scan:
  `17` (method-sensitive proxy)
- route-planner HIP cross-layer count: `2`
- slice touch-size pressure (`velocity-preservation`):
  - average files touched: `15.0`
  - p95 files touched: `17`

Trigger:

- if thresholds are exceeded, require architecture review in the next recurring
  run and explicit owner/action/target report date.
- if route-planner HIP or slice-size guardrails exceed threshold for two
  consecutive runs, require decomposition plan execution in the same cycle.

---

### 11. Anti-Patterns (Prohibited)

- mutable `AccessPath` rewriting in non-planner runtime flow
- planner-policy checks reimplemented in executor runtime modules
- variant-specific logic scattered across unrelated layers
- new decision owners in non-authorized boundaries

Bad pattern:

```rust
if path.is_index_range() && predicate_has_x {
    // planner policy leakage in runtime
}
```

Good pattern:

```rust
// planner emits access + route contracts
if route_shape.is_streaming() {
    // executor dispatches only
}
```

Prefer:

- planner encodes semantics once
- access/route contracts expose capabilities
- executor dispatch executes declared route shape

---

### 12. Implementation Slices (`0.47`)

Patch-neutral slices:

- Slice A: establish this contract + metric baselines in docs/design/audit notes
- Slice B: reduce AccessPath decision-owner count from `5` toward `<=3`
- Slice C: reduce scattered variant branching by extending capability-contract
  usage (`AccessRouteClass` / route-shape contracts)
- Slice D: rerun recurring audits and record deltas in
  `complexity-accretion`, `module-structure`, `velocity-preservation`,
  and `dry-consolidation`
- Slice E: route/load velocity containment:
  - enforce planner -> `RouteShape` -> executor dispatch contract boundary
  - reduce route-planner HIP cross-layer count (`2 -> <=1`)
  - execute `executor/load` decomposition (`dispatch`, `strategy`, `terminal`)
  - re-measure median/p95 files touched in next recurring run
