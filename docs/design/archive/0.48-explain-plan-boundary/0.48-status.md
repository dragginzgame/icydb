# 0.48 Status (as of 2026-03-10, 0.48.0 closeout complete)

## 0.48 Scope

This status tracks `0.48` explain-boundary hardening work defined in:

- `docs/design/0.48-explain-plan-boundary/0.48-design.md`
- `docs/design/0.47-audit-pass/0.47-status.md` (source deferral context)

`0.48` focuses on ownership cleanup at the explain/fingerprint/cursor seams
without expanding query-surface features.

---

## Progress Summary

Completed today:

- created `0.48` design + status trackers under one versioned slice directory
- captured objective and owner boundaries for:
  - planner-owned identity hashing
  - cursor-owned continuation contracts
  - explain boundary narrowing
- validated in-flight direction in local workspace:
  - `cargo check -p icydb-core` (pass)
  - `cargo test -p icydb-core db::query::fingerprint:: -- --nocapture` (pass)
- completed planner-owned identity hashing convergence across touched runtime
  call paths
- completed cursor-owned continuation contract convergence across cursor/index
  boundaries
- completed explain/session parity reruns for touched surfaces
- completed full workspace quality gates:
  - `make fmt-check` (pass)
  - `make clippy` (pass)
  - `make check` (pass)
  - `make test` (pass)
- completed release-doc alignment:
  - root `CHANGELOG.md` updated with `0.48.0` summary
  - `docs/changelog/0.48.md` added with detailed notes

---

## 0.48 Kickoff Readiness

Preparation status: **ready to execute**

Kickoff prerequisites:

- [x] design tracker created
- [x] status tracker created
- [x] objective and non-goals frozen
- [x] baseline compile/test readout captured
- [x] explain parity lock rerun captured
- [x] closeout criteria satisfied

---

## In Scope (`0.48`)

- decouple identity hashing and continuation signatures from explain DTO
  projection authority
- consolidate continuation guard ownership under cursor-owned contracts consumed
  by index scans
- preserve deterministic explain output semantics while reducing coupling to
  identity paths
- record explicit command-based verification for touched boundaries

## Out of Scope (`0.48`)

- new explain node-family vocabulary
- new query language features
- unrelated subsystem decomposition beyond touched ownership seams
- compatibility shim support for legacy plan-hash derivation paths

---

## Roadmap Snapshot (`0.48`)

- [x] A. design/status trackers established
- [x] B. baseline verification captured for active working tree
- [x] C. planner-owned identity hashing convergence complete
- [x] D. cursor-owned continuation contract convergence complete
- [x] E. explain boundary narrowing + parity locks complete
- [x] F. closeout readout and release-doc alignment

Current `0.48` state: **closed (`0.48.0` complete)**.

---

## Post-Closeout Follow-Up Track (Patch-Neutral)

Follow-up explain observability work after `0.48.0` remains constrained by the
same ownership boundaries from this slice.

Note:

- this status file now absorbs the follow-up slice status that was previously
  tracked in a separate `0.48.1` design/status directory so `0.48` has one
  design directory and one merged status stream.

Completed follow-up items (from the Tier roadmap in `0.48-design.md`):

- Tier 1 items `1-4` complete (covering indicator, prefix length, limit
  pushdown visibility, sort elimination metadata)
- Tier 2 items `5-7` complete (predicate split reporting, explicit residual
  filter stage, cursor resume diagnostics)
- Tier 3 items `8-10` complete (fast-path reason codes, access-choice ranking
  metadata, projection pushdown/projection-field reporting)
- Tier 4 OR scope expansion complete: strict same-field OR equality now lowers
  through the bounded strict-IN planning path (fail-closed for mixed/non-strict
  branches)
- Tier 4 `IN` route explain visibility now projects canonical `prefix_values`
  for index-multi-lookup access roots (execution behavior unchanged)
- Tier 4 IN hardening complete: strict secondary IN pushdown now fails closed
  when any literal in the set is schema-incompatible

Pending follow-up items:

- none in the Tier roadmap

Recent validation for follow-up additions (`2026-03-10`):

- `cargo test -p icydb-core db::query::plan::tests:: -- --nocapture`
  - result: pass (`116 passed`, `0 failed`)
- `cargo test -p icydb-core query_explain_execution_ -- --nocapture`
  - result: pass (`25 passed`, `0 failed`)
- `cargo test -p icydb-core session_load_explain_execution_ -- --nocapture`
  - result: pass (`5 passed`, `0 failed`)
- `cargo test -p icydb-core session_load_terminal_explain_ -- --nocapture`
  - result: pass (`5 passed`, `0 failed`)
- `cargo check -p icydb-core`
  - result: pass
- `cargo clippy -p icydb-core --lib -- -D warnings`
  - result: pass

---

## Risk Assessment (2026-03-10)

Risk level: **medium**, scope-constrained to identity and continuation
boundaries.

Most likely regression surfaces:

- fingerprint regression behavior
- continuation error mapping behavior
- explain tests that implicitly rely on DTO shape details

Mitigation path:

- keep fingerprint/signature test suite green for semantic stability
- run targeted explain/session parity tests for touched call paths
- keep cursor-owned fail-closed continuation invariants explicit in design docs

---

## Workstream Tracker

| Workstream | State | Owner Boundary | Next Gate |
|---|---|---|---|
| Planner-owned identity hashing authority | Complete | `query::plan` + `query::fingerprint` + `intent/session` | complete |
| Cursor-owned continuation contract consolidation | Complete | `cursor` + `index::scan` + `access` | complete |
| Explain boundary narrowing and parity lock | Complete | `query::explain` + `session` | complete |
| Docs and closeout evidence | Complete | design/status/changelog boundaries | complete |

---

## Verification Readout (2026-03-10)

- `cargo check -p icydb-core`
  - result: pass
- `cargo test -p icydb-core db::query::fingerprint:: -- --nocapture`
  - result: pass (`86 passed`, `0 failed`)
- `cargo test -p icydb-core db::query::explain::tests -- --nocapture`
  - result: pass (`27 passed`, `0 failed`)
- `cargo test -p icydb-core session_load_terminal_explain_ -- --nocapture`
  - result: pass (`5 passed`, `0 failed`)
- `cargo test -p icydb-core session_trace_query_reports_plan_hash_and_route_summary -- --nocapture`
  - result: pass (`1 passed`, `0 failed`)
- `cargo test -p icydb-core query_explain_execution_ -- --nocapture`
  - result: pass (`25 passed`, `0 failed`)
- `cargo test -p icydb-core db::cursor:: -- --nocapture`
  - result: pass (`26 passed`, `0 failed`)
- `cargo test -p icydb-core db::executor::tests::cursor_validation:: -- --nocapture`
  - result: pass (`20 passed`, `0 failed`)
- `cargo test -p icydb-core db::executor::tests::pagination::index_range_matrix:: -- --nocapture`
  - result: pass (`33 passed`, `0 failed`)
- `cargo test -p icydb-core plan_hash_snapshot_is_stable_across_explain_surfaces -- --nocapture`
  - result: pass (`1 passed`, `0 failed`)
- `make fmt-check`
  - result: pass
- `make clippy`
  - result: pass
- `make check`
  - result: pass
- `make test`
  - result: pass (`1615` `icydb-core` unit tests and workspace test matrix green)

Planned next verification:

- none for `0.48.0` closeout; optional follow-up validation applies only to
  deferred post-`0.48.0` items

---

## Open Items

- [x] confirm explain regression coverage breadth for touched call paths
- [x] add one explicit plan-hash parity snapshot for long-term guard strength
- [x] finalize closeout summary text once implementation lands

---

## Running Notes (Append-Only)

2026-03-10:

- `0.48` scope initiated from explicit `0.47` deferral to next line.
- Added:
  - `docs/design/0.48-explain-plan-boundary/0.48-design.md`
  - `docs/design/0.48-explain-plan-boundary/0.48-status.md`
- Expanded docs with explicit identity contract, continuation safety
  invariants, explain parity semantics, boundary diagram, and risk readout.
- Recorded baseline compile/fingerprint verification for current in-flight
  implementation.
- Recorded targeted explain/session parity verification:
  - `db::query::explain::tests` (`27` pass)
  - `session_load_terminal_explain_*` (`5` pass)
  - `session_trace_query_reports_plan_hash_and_route_summary` (`1` pass)
  - `query_explain_execution_*` (`25` pass)
- Recorded cursor/index continuation verification:
  - `db::cursor::*` (`26` pass)
  - `db::executor::tests::cursor_validation::*` (`20` pass)
  - `db::executor::tests::pagination::index_range_matrix::*` (`33` pass)
- Recorded plan-hash parity snapshot verification:
  - `plan_hash_snapshot_is_stable_across_explain_surfaces` (`1` pass)
- Recorded full workspace readiness gates:
  - `make fmt-check` (pass)
  - `make clippy` (pass)
  - `make check` (pass)
  - `make test` (pass)
- Recorded release-doc closeout:
  - root `CHANGELOG.md` includes `0.48.0` summary
  - `docs/changelog/0.48.md` added
