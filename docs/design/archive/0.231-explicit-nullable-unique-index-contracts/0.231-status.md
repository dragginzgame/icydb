# 0.231 Explicit Nullable Unique-Index Contracts Status

Status: 0.231.0 released; all five planned patches complete; independent
closeout corrections are ready for the user-owned 0.231.1 release gate

Last updated: 2026-08-19

Design authority:
[0.231 design](./0.231-design.md)

Accepted predecessor:

- tag `v0.230.3`;
- commit `eb36bdb4281de873f62e71df8c7142c856232eb2`; and
- tree `f4f2a3b7c3725330381f9ffbfbeff98eda840198`.

## Patch Tracker

| Patch | Outcome | Status |
| --- | --- | --- |
| 1 | Reconcile v0.230.3 and freeze the accepted nullable-unique contract | Complete |
| 2 | Implement accepted-contract hard cut and frontend/constraint convergence | Complete |
| 3 | Extend existing planner implication and close the line | Complete |
| 4 | Close independent-audit regression and bypass-path gaps | Complete |
| 5 | Measure remaining admission, stable-byte, and write-overhead evidence | Complete |

## Patch 1 Findings

The review feedback is accepted. Reconciliation found one necessary scope
contraction and otherwise confirmed the proposed semantic solution.

- Accepted schema remains the sole runtime authority. v0.230.3 accepts unique
  indexes whose nullable key sources can omit physical membership without an
  explicit matching guard.
- Every accepted unique index must have exactly one accepted unique constraint.
  The smaller consistent correction is to expose its backing index and exact
  predicate through constraint descriptions and `SHOW CONSTRAINTS`, not to
  remove filtered indexes from constraint identity closure.
- Predicate execution binds top-level fields only. Dotted parser spelling is
  not nested-path execution. Patch 2 will reject omit-capable nested unique
  sources rather than add a new predicate language.
- Omission is now frozen as a bounded schema-only classification:
  `NeverOmits`, `TerminalNullOmits`, `UnsupportedNullableAncestor`, or
  `InvalidSourcePath`. Rows are never inspected to derive it.
- Accepted records require exact member shape. Missing members and type
  mismatches remain errors. A nullable intermediate ancestor currently causes
  typed traversal failure, not omission, and is rejected for affected unique
  indexes instead of being guessed or reclassified.
- The integrity call graph does not justify a global typed refactor. Patch 2 is
  limited to `Structural`, `Predicate`, and `NullableUnique` acceptance
  outcomes across the eight direct production `has_valid_integrity` call sites,
  while existing base/index/relation/constraint `Option<()>` owners remain
  unchanged.
- Root rename currently has a stale-text fallback after predicate rewrite
  failure. Patch 2 removes that acceptance fallback and rebinds root rename,
  record-member rename, generated reconciliation, candidate creation, and
  promotion against the complete final schema revision.
- Candidate promotion already constructs and integrity-checks the final
  accepted-after snapshot. Patch 2 keeps that revalidation point and adds no
  persisted proof bit.
- v0.230.3 has no maintained public write-quiescence or accepted
  constraint-reference-transfer route. Reinstall/recreation is the sole
  supported remedy for noncompliant active or candidate state.

## Frozen Patch 2 Boundary

Patch 2 may add:

- one schema-owned nullable-unique validator;
- one ephemeral fallible top-level predicate binder over a structurally valid
  candidate;
- one narrow three-category snapshot-acceptance result;
- exact generated and SQL error projection;
- exact final-revision rename and promotion revalidation;
- constraint-description predicate projection; and
- focused tests, documentation, and artifact measurements for those changes.

Patch 2 may not add nested predicate execution, a persisted proof, a new
uniqueness mode, a compatibility path, a replacement workflow, or a general
typed diagnostic hierarchy.

## Exact Persisted-State Boundary

A well-formed current version-1 snapshot that violates only the new semantic
contract maps to `InternalError::serialize_incompatible_persisted_format()`,
`ErrorClass::IncompatiblePersistedFormat`, `ErrorOrigin::Serialize`, and
`RuntimeIncompatiblePersistedFormat`. Structural or predicate corruption stays
corruption. The format discriminator remains version 1.

## Accepted Production Baseline

| Measure | v0.230.3 |
| --- | ---: |
| Compiler-emitted Wasm | 4,997,103 bytes |
| Final raw Wasm | 4,364,858 bytes |
| Deterministic gzip | 1,692,326 bytes |
| Candid | 60,428 bytes |
| Candid SHA-256 | `5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1` |
| Exports | 70 |

The export shape is 27 query methods, 39 update methods, one init, one
post-upgrade, one global timer, and one Candid pointer export.

## Patch 1 Complexity

Patch 1 changes documentation only. It removes hypothetical replacement
routes, prevents a global integrity-error refactor, and rejects unsupported
nested predicate semantics. The planned runtime shape is smaller and has no
new persisted/configurable uniqueness mode. Public admission, planner, and
constraint-description behavior will change only to consume and expose the
narrowed accepted contract.

The landing touches two 0.231 documentation files: 362 inserted and 249
deleted lines in the design, plus this 128-line status record. Runtime,
persisted-state, Candid, Wasm, and instruction deltas are zero in Patch 1.

## Validation

- `git diff --check`: passed.
- Repository formatting check: passed.
- Repository invariant checks: passed, including durability documentation,
  schema/model boundaries, mutation atomicity, generated endpoints, and Wasm
  post-link invariants.
- No production tests or artifacts were rebuilt because Patch 1 changes only
  documentation. Focused runtime tests and before/after artifacts belong to
  their production patches; full suites remain user-owned.

## Patch 2 Outcome

Patch 2 implements the reconciled contract without creating a second schema,
predicate, uniqueness, or persistence authority.

- `validate_schema_snapshot_acceptance` is the canonical boundary. It composes
  the unchanged structural integrity owners with one bounded nullable-unique
  validator and preserves only the three frozen result categories:
  `Structural`, `Predicate`, and `NullableUnique`.
- `source_omission_class` derives omission from accepted schema metadata only.
  It covers field-path and expression sources, deduplicates repeated sources,
  admits only terminal-null omission, rejects nullable ancestors, and leaves
  missing members, non-map ancestors, and type mismatches as row-admission or
  row-decode errors rather than guessing from values.
- Exact top-level `IS NOT NULL` conjuncts are the only guards. Duplicate guards
  are canonicalized; incomplete composite guards, OR/NOT/comparison forms,
  unbound fields, malformed predicates, and dotted nested spelling fail
  closed.
- Generated initial lowering, SQL DDL binding, accepted/candidate publication,
  nullability mutation, promotion, codec ingress/egress, and reopen all consume
  that authority. Existing frontends only project its result.
- A production defect exposed during implementation was corrected: field
  nullability mutation previously updated the field but left accepted index
  source metadata stale, causing structural rejection before semantic
  validation. Active and candidate field-path/expression sources now receive
  the exact top-level nullability transition before final validation.
- Root rename now rewrites predicate text fallibly and rebinds the complete
  final snapshot. Rewrite failure rejects; the former stale-text fallback is
  gone. Generated migration uses the same fallible rule.
- Unique constraint descriptions retain the accepted backing constraint and
  index identities. They now expose the backing index name and exact optional
  predicate, with `unique_index_v1` versus `partial_unique_index_v1` semantics;
  `SHOW CONSTRAINTS` projects the same facts.
- Well-formed current version-1 active state that violates only this semantic
  rule reopens as incompatible persisted format. Structural or predicate
  corruption stays corruption. No legacy decoder, migration, or repair path
  was added; reinstall/recreation remains the supported hard cut.

## Patch 2 Artifact Evidence

The maintained production `sql_perf` artifact was rebuilt from the dirty
candidate worktree with Rust 1.97.1 and the repository `wasm-size-report`
pipeline. The worktree also contains user-owned 0.230/tooling and dependency
changes, so this is exact final-candidate evidence, while attribution of every
byte solely to Patch 2 is not claimed.

| Measure | v0.230.3 baseline | Patch 2 candidate | Delta |
| --- | ---: | ---: | ---: |
| Compiler-emitted Wasm | 4,997,103 | 5,007,032 | +9,929 |
| Final raw Wasm | 4,364,858 | 4,373,425 | +8,567 |
| Deterministic gzip | 1,692,326 | 1,695,560 | +3,234 |
| Candid | 60,428 | 60,456 | +28 |
| Exports | 70 | 70 | 0 |

Candidate SHA-256 values are:

- compiler-emitted Wasm:
  `8cba8e95a7ff035d548e1384c25c7d4aaff6af816c82613deabccb0f8d7e92cb`;
- final raw Wasm:
  `a09c4b43e37d042703428aae51d46c33408a01a8744aca110d89e1ce860c7d2e`;
- deterministic gzip:
  `af362ac0c86451c6e753c2f06a2a6ee753caeac943ff88f67219a38aa4b62ec8`;
  and
- Candid:
  `6caca71287d87c900ff750e6a2f5aa3ec40e0e88f6c575c935706dd02df6208c`.

The export shape remains 27 query methods, 39 update methods, one init, one
post-upgrade, one global timer, and one Candid pointer export. The intentional
Candid delta is the optional constraint predicate. No method name or mode was
added or changed. The final raw growth is 8,590 bytes; raw Wasm remains the
authoritative size measure.

No persisted schema/index wire field, stable-memory allocation, or steady-state
state was added. Valid accepted snapshots and index entries retain their exact
encoding because Patch 2 changes acceptance before the unchanged encoders; no
per-row key, uniqueness-probe, or mutation path calls the validator.

## Patch 2 Instruction Evidence

Current-candidate real-canister focused probes passed:

- startup observation: 161,041 instructions fresh, 379,494 ready, and 253,518
  after upgrade, all below the maintained 25,000,000 ceiling; and
- schema exact-match application: 29,675,587 instructions for repeated ready
  queries, 29,675,603 for the update probe, and 29,737,263 after upgrade.

An independent exact-v0.230.3 archive reproduction of the same startup probe
could not provide the intended paired delta: the predecessor `ic-testkit`
PocketIC server panicked with `Failed to bind PocketIC server to address
127.0.0.1:0`, after which the test hung and was interrupted. The current
candidate measurement is valid, but the before/after instruction delta remains
unexecuted evidence rather than an inferred result. No maintained real-canister
fixture exposes the Patch 2 SQL DDL admission matrix, so DDL admission
instructions were not measured.

## Patch 2 Validation

- Formatting and diff hygiene pass.
- Warning-denied Clippy passes for `icydb-core` all targets with SQL+migration,
  core-only library, migration library, and `icydb` all targets with SQL.
- 87 distinct focused tests pass across canonical acceptance, codec reopen,
  SQL DDL binding, generated lowering, nullability/candidate lifecycle, rename,
  constraint projection, Candid/table golden output, migration planning, and
  real-canister startup/schema application.
- The production `sql_perf` size/export report passes and confirms unchanged
  method modes and export count.
- The complete repository invariant-script matrix passes, including executor
  panic, schema/model authority, mutation atomicity, durability, generated
  endpoint, Wasm post-link, and memory-ID guards.
- Full repository/workspace suites remain user-owned under `AGENTS.md` and were
  not run.

## Patch 2 Complexity

The patch touches 24 production/test/contract/changelog files plus the two
0.231 design records and adds one 300-line schema-owned validator module. The
implementation is moderately more complex locally because it adds a bounded
semantic proof and public constraint projection, but the authority shape is
simpler: all accepted/generated/DDL/candidate routes converge on one validator,
the stale rename fallback is removed, and no persisted state, mode, scheduler,
or compatibility axis is introduced.

The current declared Patch 2 production/test/contract/changelog paths are
approximately +1,554/-148 lines across 25 files, including the 300-line new
validator; tests and fixtures account for most of the added lines. The shared
0.231 design/status evidence is +609/-249 lines across the two records and
also includes the already-landed Patch 1 reconciliation.

## Patch 3 Outcome

Patch 3 closes planner usability without adding a route, optimizer, predicate
representation, persisted proof, or configuration axis.

- `predicate_implies_predicate_for_planner` remains the sole filtered-index
  implication owner. Its existing bounded conjunction collector now also
  recognizes exact `IS NOT NULL` clauses.
- Supported strict equality and ordered comparisons prove the matching
  non-null guard only for a concrete non-null bound value. `NULL` and the
  internal unknown/unit value fail closed.
- Every composite guard and every additional accepted index-filter comparison
  must still be implied. Extra query conjuncts do not invalidate an otherwise
  complete proof.
- `OR`, `NOT`, cross-field comparisons, expression-to-source inference, null
  uncertainty, and unsupported predicate families remain conservative. A
  query that can observe rows omitted from the filtered index cannot select
  that index.
- The existing residual-filter owner now removes an exact non-null clause only
  when the accepted filtered guard itself guarantees it. Access-bound stripping
  remains unchanged.
- One real-canister test creates and validates the guarded nullable unique
  index through SQL DDL, proves the new `IndexPrefix` route, proves an `IS NULL`
  query and unsupported `OR` proof stay off that index, and checks indexed
  output against both the pre-index scan and an equivalent conservative route.

## Patch 3 Instruction And Read Evidence

The focused production-shaped canister probe recorded:

| Route | Total | Compiler | Planner | Store | Executor | Decode | Data gets | Index ranges | Index entries |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Pre-index full scan | 712,515 | 156,659 | 288,369 | 86,730 | 176,239 | 1,398 | not sampled | not sampled | not sampled |
| Proven filtered `IndexPrefix` | 8,271,327 | 7,589,809 | 358,462 | 62,510 | 255,683 | 1,398 | 1 | 1 | 1 |
| Equivalent conservative full scan | 8,486,517 | 7,837,307 | 355,737 | 87,176 | 201,598 | 1,398 | 3 | 0 | 0 |

The same-schema, equal-result proven route is 215,190 instructions (2.54%)
below the deliberately conservative `OR` full scan. It performs one physical
index range/entry read and reduces data gets from three to one. Its executor
bucket is 54,085 instructions higher, but store work falls by 24,666 and total
work is lower. The much smaller pre-index total is not an implication-only
comparison: the schema has no accepted filtered predicate to compile. With
the accepted filtered index present, predicate compilation dominates both
routes at about 7.6-7.8 million instructions. The maximum observed query is
8.49 million,
well below the maintained 40-billion-message ceiling.

The detailed read counters come from one diagnostics-only, test-admin query
wrapper around the existing attributed SQL execution method. It adds no
production endpoint; production export evidence below remains unchanged.

## Final 0.231 Artifact Evidence

The production `sql_perf` artifact was rebuilt after Patch 3 from the dirty
candidate worktree using the maintained pipeline and Rust 1.97.1. Concurrent
user-owned 0.230/tooling/dependency edits remain present, so the artifact is
exact candidate evidence without claiming sole byte attribution to 0.231.

| Measure | v0.230.3 | Patch 2 | Final 0.231 | Patch 3 delta | Line delta |
| --- | ---: | ---: | ---: | ---: | ---: |
| Compiler-emitted Wasm | 4,997,103 | 5,007,032 | 5,007,617 | +585 | +10,514 |
| Final raw Wasm | 4,364,858 | 4,373,425 | 4,373,952 | +527 | +9,094 |
| Deterministic gzip | 1,692,326 | 1,695,560 | 1,695,921 | +361 | +3,595 |
| Candid | 60,428 | 60,456 | 60,456 | 0 | +28 |
| Exports | 70 | 70 | 70 | 0 | 0 |

Final SHA-256 values are:

- compiler-emitted Wasm:
  `4be2ee49001f3c3faef689dc62a900692589df68836f7a619e76b994a99b05ae`;
- final raw Wasm:
  `2ed15abbb1c566607a4d0e1027e1e94ef55166a2799cde20a2efd2c708b5380e`;
- deterministic gzip:
  `d74446be9b29ad2e8918fe01a350425c75a3c783de52dac4f757d51e6f2bdb35`;
  and
- Candid:
  `6caca71287d87c900ff750e6a2f5aa3ec40e0e88f6c575c935706dd02df6208c`.

The final export shape remains 27 query methods, 39 update methods, one init,
one post-upgrade, one global timer, and one Candid pointer. Patch 3 changes no
Candid byte, method name, method mode, lifecycle export, persisted encoding,
stable-memory allocation, or steady-state stable byte.

## Patch 3 Validation

- Formatting and diff hygiene pass.
- Warning-denied Clippy passes for `icydb-core` all targets with SQL+migration,
  `icydb` all targets with SQL, the SQL canister test target, and the
  diagnostics/test-admin SQL fixture canister.
- Five implication-owner unit tests and one production-shaped real-canister
  regression pass. Together with Patch 2, the line has 93 distinct focused
  passing tests.
- Repository invariant scripts pass, including executor panic, schema/model
  authority, mutation atomicity, generated endpoint, Wasm post-link, and
  memory-ID checks.
- The production `sql_perf` report passes and confirms the final raw, gzip,
  Candid, export-count, and method-mode evidence above.
- Full repository/workspace suites remain user-owned under `AGENTS.md` and
  were not run.

One intermediate test formulation used `predicate OR FALSE` as a forced scan;
the maintained SQL normalizer correctly reduced it to the exact predicate and
selected the index. The final test uses a nontrivial equivalent `OR` query,
which remains conservative as designed.

## Post-Closeout Validation Reconciliation

The user-owned full test workflow exposed four stale evidence failures, not a
production behavior defect. Two exact Candid reply measurements now include
the six-byte type-table increase from the accepted constraint predicate, and
the maintained endpoint ABI golden includes that optional field. The
candidate logical-fill collision fixture now supplies its required
`nickname IS NOT NULL` predicate, compiles the accepted row contract, evaluates
the real guard, and still proves the candidate uniqueness conflict.

The affected seven describe tests, candidate collision test, and endpoint ABI
golden test pass. Warning-denied SQL, diagnostics, and integration-library
Clippy lanes, formatting, diff hygiene, and repository invariants also pass.
The complete repository/workspace workflow still requires its user-owned
rerun.

## Patch 3 Complexity And Closeout

Patch 3 touches one production planner file, one test canister, one integration
test, the SQL contract, the active 0.231.0 changelog notes, and these two design
records.
The production/test delta before documentation is approximately +425/-57 lines
across the planner, diagnostics-only test canister, and integration target;
most additions are direct test coverage and the bounded clause collector.

Local implementation complexity increases slightly because the existing
implication clauses now carry one additional fact class. Architectural
complexity stays neutral: there is still one predicate representation, one
implication owner, one access-planning flow, and zero new durable or public
behavior axes. This was the Patch 3 handoff before the independent closeout
audit and the subsequent 0.231.0 release.

## Independent Closeout Audit And Patch 4

The independent audit found no production correctness defect or duplicate
nullable-unique authority. It withheld a ready verdict because direct portions
of the frozen validation matrix and four required measurement families were
not attributable to executed evidence. The release receipt subsequently
proved that the complete post-fixture `make validate` gate ran against exact
candidate `b1bfb380050777637e1a19631e950ec9befb2e61`; the earlier status claim
that this rerun remained outstanding was stale.

Patch 4 closes the direct regression gaps without changing production code:

- maximum-size mixed field/expression keys prove repeated nullable sources
  deduplicate by canonical identity, report one deterministic missing guard,
  and admit when every distinct source is guarded;
- malformed predicates cannot pre-empt structural invalid-source precedence;
- promotion revalidates the current candidate and a rejected promotion leaves
  its candidate, constraint catalog, allocator, and physical generation
  exactly unchanged;
- SQL `IF NOT EXISTS` distinguishes exact active and candidate no-ops from
  same-name and same-contract conflicts in both lifecycle states;
- guard-false rows create no physical unique entry or collision, while the one
  guard-true rebuilt entry equals the accepted incremental key exactly; and
- the real canister exposes exact filtered and unfiltered backing-index facts
  through `SHOW CONSTRAINTS`, keeps conservative `OR` queries off the filtered
  index, and proves both equality-prefix and ordered-range output against an
  independently forced full scan.

Five new focused tests and the expanded production-shaped canister regression
pass. Warning-denied Clippy passes for `icydb-core` all targets with
SQL+migration and for the integration `sql_canister` target. Formatting passes.
The equal-result equality route records 8,381,775 instructions and one data
get/index entry versus 8,605,731 instructions and three data gets for the
forced full scan; the expanded range parity also passes.

Patch 4 adds tests and documentation only. It introduces no production branch,
persisted byte, public surface, allocation, mode, or compatibility path. The
remaining measurement work was assigned to Patch 5 rather than folded into
the regression patch.

## Patch 5 Outcome

Patch 5 closes every measurement family withheld by the independent audit.
The harness uses only diagnostics/test-admin endpoints around the existing DDL,
exact-update, and accepted-catalog owners. Production builds exclude those
endpoints, and the maintained production artifact confirms that none appears
in Candid or the Wasm export list.

The same test-only instrumentation was applied in an isolated archive of exact
predecessor commit `eb36bdb4281de873f62e71df8c7142c856232eb2`. This gives a
paired comparison rather than an estimate:

| Measurement | v0.230.3 | Current 0.231 | Delta |
| --- | ---: | ---: | ---: |
| Non-unique DDL admission | 43,296,638 | 43,554,755 | +258,117 (+0.60%) |
| Non-null unique DDL admission | 41,294,822 | 41,713,824 | +419,002 (+1.01%) |
| Guarded nullable unique DDL admission | 43,284,832 | 44,296,980 | +1,012,148 (+2.34%) |
| Unguarded nullable unique admission | 36,703,992, accepted | 8,231,590, rejected | Intentional contract change; percentage not comparable |
| Accepted-schema read after reopen, 1 index | 7,437,183 | 7,439,851 | +2,668 (+0.04%) |
| Accepted-schema read after reopen, 8 indexes | 9,585,417 | 9,592,104 | +6,687 (+0.07%) |
| Accepted-schema read after reopen, 64 indexes | 51,512,519 | 51,596,454 | +83,935 (+0.16%) |
| Exact update through an unchanged accepted unique index | 3,821,324 | 3,743,162 | -78,162 (-2.05%) |

The 1/8/64 cases read accepted entity snapshots only after generated startup
has reopened catalog authority; the eight-index fixture survives a same-Wasm
upgrade before measurement. The 64-index fixture is the maintained accepted
maximum. All samples remain orders of magnitude below the 40-billion-message
ceiling. The modest DDL increases occur only at the changed admission boundary,
while the unchanged per-row unique write is slightly cheaper and therefore
shows no new steady-state validation path.

Both versions retain exactly 511,770,624 stable bytes before and after an
identical `CREATE UNIQUE INDEX IF NOT EXISTS` over the same accepted guarded
index. Each run also compares the entire stable-memory fingerprint and finds
it unchanged. This proves zero persisted-byte or physical-index-domain change
for the identical valid snapshot operation; it is stronger than extent-only
evidence.

## Reconciled Artifact Evidence

The earlier Patch 2 and Patch 3 tables are historical dirty-candidate
measurements. They are not the exact released artifact and are superseded by
the independent exact-tag rebuild below. The v0.230.3 baseline was also
corrected by 23 raw bytes and 13 gzip bytes from the originally reported table.

| Measure | Exact v0.230.3 | Exact v0.231.0 | Release delta | Active dirty candidate |
| --- | ---: | ---: | ---: | ---: |
| Compiler-emitted Wasm | 4,997,103 | 5,008,089 | +10,986 | 5,008,089 |
| Final raw Wasm | 4,364,858 | 4,374,594 | +9,736 | 4,374,594 |
| Deterministic gzip | 1,692,326 | 1,693,060 | +734 | 1,693,058 |
| Candid | 60,428 | 60,456 | +28 | 60,456 |
| Exports | 70 | 70 | 0 | 70 |

Exact v0.231.0 SHA-256 values are:

- compiler-emitted Wasm:
  `d44bf610454625100d52c8f01bf9ff877b8bc908fc15915f384b2844fab3508b`;
- final raw Wasm:
  `7e98d002cd4a952a8706f0aaa236f2f83eb4b934362d316002399e19c45d663a`;
- deterministic gzip:
  `07632f9f59a7216aa08b53d28090bbd880e65cc3c40ab13057aa346c40a5f622`;
  and
- Candid:
  `6caca71287d87c900ff750e6a2f5aa3ec40e0e88f6c575c935706dd02df6208c`.

The active dirty-candidate SHA-256 values are, respectively,
`31fed63a86636a0652cb72ed77412f4e16092c22bbca8209e3fcaf641b4831e4`,
`f05e63397ea016823ba1579f69a2bc803b0e7e37b30176af138fff76143820a3`,
`b3f56f8e8491b598ccdfe46a6e496243d2e9525558dcee5ccaa811e92d5fd910`,
and the same Candid hash. The production surface remains 27 queries, 39
updates, init, post-upgrade, global timer, and Candid pointer. The three
measurement endpoint names are absent from both production Candid and Wasm
inspection output.

## Patch 5 Validation And Command Record

Passed:

- `cargo test -p icydb-testing-integration --test sql_canister nullable_unique_closeout_measures_admission_stable_bytes_and_unchanged_write_path -- --nocapture`;
- `cargo test -p icydb-testing-integration --test sql_canister accepted_schema_reopen_reads_are_measured_at_one_and_eight_indexes -- --nocapture`;
- `cargo test -p icydb-testing-integration --test schema_application_cost maximum_accepted_schema_reopen_read_is_bounded_at_sixty_four_indexes -- --nocapture`;
- the isolated predecessor target `nullable_unique_closeout_baseline` with both
  real-canister tests passing;
- warning-denied Clippy for the local/test SQL canister, local/test `sql_perf`
  canister, and both changed integration targets;
- `cargo fmt --all`, `git diff --check`, and direct production-endpoint absence
  inspection; and
- `scripts/ci/wasm-size-report.sh --canister sql_perf` in the active worktree.

The exact-tag archive built all four production artifacts successfully. Its
final report-writer step returned `git exited with status exit status: 128`
because a `git archive` intentionally has no `.git` metadata. Direct byte,
SHA-256, Candid, and Wasm-info inspection supplies the artifact evidence above;
the active-worktree invocation proves the maintained wrapper itself passes.

Two intermediate harness failures were corrected without production changes:
the first write probe used the broad trusted mutation surface, which correctly
rejects `UPDATE`, and the first fresh-fixture schema read observed transient
E280 before the bounded startup window completed. The final probe uses the
canonical exact-update owner and explicit bounded watchdog delivery.

Full repository/workspace suites were not rerun because `AGENTS.md` reserves
them for the user-owned validation/release workflow. The v0.231.0 release
receipt already proves complete `make validate` on exact candidate
`b1bfb380050777637e1a19631e950ec9befb2e61`; Patch 4 and Patch 5 add tests,
test-admin instrumentation, and evidence only.

## Final Complexity And Verdict

Patch 5 adds diagnostics-only wrappers and three focused real-canister tests;
it changes no production validator, planner, row/index mutation, persisted
format, allocation, scheduler, Candid method, or export. Local test complexity
increases modestly to make missing evidence reproducible. Architectural and
production complexity remain neutral.

The worktree remains dirty with concurrent user-owned dependency and canister
build-pipeline changes. The complete predecessor-to-worktree diff therefore
cannot be attributed solely to nullable uniqueness; relevant evidence above is
identified as exact tag, historical candidate, active candidate, or
audit-owned test instrumentation rather than conflated.

**READY FOR THE USER-OWNED 0.231.1 RELEASE GATE.** No 0.231 correctness,
artifact, persistence, public-surface, or measurement blocker remains. The
user-owned full validation/release workflow is the only remaining action.
