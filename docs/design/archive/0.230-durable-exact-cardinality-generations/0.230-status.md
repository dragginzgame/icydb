# 0.230 Durable Exact Cardinality Generations Status

Status: Independent closeout audit is **READY for the user-owned 0.230.3
release gate**; the user-owned release is complete. The exact corrected source
candidate passed the complete maintained `make validate` gate before version
mutation. Candidate and release-gate receipts verify the no-drift transition,
and `HEAD`, `main`, `origin/main`, and annotated `v0.230.3` all resolve to
commit `eb36bdb4281de873f62e71df8c7142c856232eb2`, tree
`f4f2a3b7c3725330381f9ffbfbeff98eda840198`. A clean rebuild of that exact
release closes the artifact evidence. Existing `v0.230.2` remains immutable
preceding history.

Patch 1 freezes the current volatile cardinality owners, exact consumers,
fallback behavior, populated-reopen gap, future durable identity, build cursor,
count-key space, lifecycle transitions, and resource gates. It changes no
runtime code, persisted bytes, stable-memory allocation, public API, Candid,
export, scheduler, timer, or configuration.

Patch 2 adds the sole current version-1 generation header, count-key/value
format, bounded build cursor, typed source validation, and exact reserved keys
inside the existing schema allocation. It deliberately does not activate
construction, publication, maintenance, planner consumption, driver work, or
startup scanning; those outcomes remain owned by Patches 3 through 5.

Patch 3 adds the single bounded construction owner for an isolated `Building`
generation. It derives source authority from the canonical accepted root,
scans canonical rows and index entries in exclusive physical-key pages, and
atomically publishes coalesced count changes with the next cursor. It does not
publish `Ready`, restart source drift, schedule driver work, or expose counts to
planning; Patch 4 now owns that lifecycle connection and Patch 5 owns consumption.

Patch 4 connects that builder to the existing replicated watchdog, validates
the complete source before and after every build page, rotates stale evidence
through the other reusable slot, and atomically publishes only a completely
exhausted candidate. Ordinary startup readiness remains independent of this
optional work.

Patch 5 maintains a matching `Ready` generation at the complete canonical fold
boundary, combines that base with exact bounded live-overlay deltas, and routes
all maintained exact consumers through one proof-bound store handle. Missing,
Building, stale, malformed, or nonaccepted evidence remains conservative.

Patch 6 supplies complete-lifecycle evidence in a populated production-shaped
canister across upgrade, conservative Building reads, exact publication, live
maintenance, accepted-index revision changes, and fixed two-slot physical
reuse. The independent closeout audit found and corrected consumer defects,
and the exact-candidate complete release validation now closes the line.

Design authority:
[0.230 design](0.230-design.md)

Implementation evidence cut: 2026-08-17

Independent closeout audit cut: 2026-08-18

## Exact Release Identity

The released gate is `v0.230.3`, commit
`eb36bdb4281de873f62e71df8c7142c856232eb2`, tree
`f4f2a3b7c3725330381f9ffbfbeff98eda840198`. The validated pre-bump source
candidate is `8e72cbb3c4ac1697a5f2aa13986970ad00af0901`. Both maintained release receipts
verify, and the workspace lockfile SHA-256 is
`f3ce07cea8db6c0b9240383fe2245692b80f444039a1aba3054e2f64fd694d8a`.

## Exact Predecessor Identity

The accepted predecessor is release `v0.229.1`, commit
`cfba69b013d635b0ae09238bbea2bfe05081ee7c`, tree
`a11bed3a4e4ac5ff4330dfe7e27d944987a05807`. At the Patch 1 cut, `HEAD`,
`main`, `origin/main`, and the tag resolved to that release and the worktree
was clean.

Git identity is comparison evidence only. Runtime compatibility remains the
sole current version-1 persisted form defined by repository policy.

## Patch Tracker

| Patch | Status | Result |
| --- | --- | --- |
| 1. Current-state inventory and frozen baseline | Complete | The maintained volatile owners, consumers, reopen gap, exact durable shape, lifecycle transitions, and acceptance gates are frozen without runtime change |
| 2. Durable generation identity and codecs | Complete | The sole current version-1 header, exact count records, bounded build cursor, typed source validation, and reserved schema-allocation keys are implemented and reopen cleanly |
| 3. Bounded populated-store build | Complete | One isolated candidate advances through bounded canonical row/index and inactive-slot-clear pages with exact checked totals |
| 4. Revision invalidation and Ready publication | Complete | The existing watchdog advances one optional page, source drift rotates to the other reusable slot, and only a complete unchanged candidate publishes Ready |
| 5. Incremental maintenance and planner consumption | Corrected by audit | Complete folds apply checked coalesced count changes and the next watermark; audited consumers now reject volatile journaled authority and live/canonical accepted-root drift |
| 6. Populated real-canister evidence and closeout | Complete | Focused correctness, the repaired frozen performance gate, maximum-shape instruction bounds, fixed two-slot high-water, clean exact-release artifacts, and the user-owned complete validation pass close the line |

## Maintained Baseline Reconstruction

| Concern | Maintained owner and production behavior | Patch 1 conclusion |
| --- | --- | --- |
| Entity cardinality | `db/data/store.rs`: `DataStore::entity_cardinality`, `EntityCardinality`, and `DataStore::exact_entity_count` | Heap, empty journaled stores, and current-process mutations can be exact; a populated journaled reopen intentionally returns `None` without scanning |
| Prefix cardinality | `db/index/cardinality.rs`: `IndexPrefixCardinality`; `db/index/store.rs`: `IndexStore::exact_prefix_cardinality`, sums, child enumeration, and generation stamp | Volatile counts are exact only while decodable and synchronized to the volatile row-store generation; populated reopen intentionally returns `None` |
| Query consumption | `db/executor/index_prefix_cardinality.rs`, `db/executor/aggregate/count_terminal.rs`, and `db/session/sql/execute/direct_count.rs` | Exact evidence proves empty branches, bounded child expansion, and direct COUNT; missing/stale evidence retains the maintained scan/execution route |
| Schema consumption | `db/schema/sql_ddl.rs::require_exact_empty_sql_ddl_entity` and `db/schema/application.rs::require_exact_empty_entity_count` | SQL DDL treats absence as migration-required; internal accepted-schema application treats absence as corruption. Neither path guesses zero |
| Canonical boundary | `db/journal/store.rs::FoldWatermark` | Highest folded journal sequence plus fold epoch is the sole durable store-local canonical boundary |
| Live visibility | positioned row/index/schema overlays introduced by 0.229 | Live-before-canonical values remain authority; future exact visible counts may use only a matching Ready base plus exact bounded post-watermark deltas |
| Accepted schema/index identity | `AcceptedSchemaRoot` and the accepted snapshots' physical `IndexId` values | The generation must bind the exact current store root and canonical sorted accepted user-index set |
| Store/database identity | `DatabaseIncarnationId`, the active four-role `StoreAllocationIdentities`, and the persisted registry | The generation must bind the database incarnation and exact active store quartet; generated models are not authority |
| Existing durable statistics | None | Repository search found no durable cardinality/statistics generation, codec, build, epoch, or alternate planner source |

The gap is therefore confirmed: exact cardinality is a maintained planning and
schema input, but a populated upgrade cannot recover it without a scan. Empty
reopen remains exact zero. Malformed row or user-index keys invalidate volatile
evidence; system and Missing index entries do not contribute.

The existing `u64` volatile counters use saturation because they are optional
cache evidence. That behavior is not carried into the durable protocol:
Patch 2 and later use checked arithmetic and make overflow, underflow, or an
impossible removal typed invalid evidence.

## Frozen Durable Shape

### Storage owner and key space

Cardinality uses the existing store-local schema `StableBTreeMap`; it adds no
stable-memory ID and does not change the four-role registry or `S_max = 38`.
`RawSchemaKey` remains exactly 16 bytes. The currently unused namespaces are
reserved as follows:

| Namespace | Purpose |
| ---: | --- |
| `0x05` | One generation header and one bounded build-cursor control record |
| `0x06` | Count slot A |
| `0x07` | Count slot B |

Count keys are `slot namespace || digest[0..15]`. The complete 32-byte digest
is stored in the value and must match on every read/write; a truncated-key
collision makes the candidate unavailable rather than selecting another
logical count. The domain-separated SHA-256 preimage is:

```text
"icydb.cardinality-count-key.v1"
|| kind
|| entity_tag_be
|| [index_id_bytes || component_count || (component_len_be || component_bytes)*]
```

`kind = 0` identifies an entity count and omits the bracketed suffix.
`kind = 1` identifies an accepted user-index prefix and includes the fixed
`IndexId` bytes plus one to four canonical encoded components. This is the
durable form of the existing `UserIndexPrefixCardinalityKey`; system,
candidate, Missing, and rejected generations have no count key.

The two count slots are isolated candidate/Ready buffers, not two semantic
owners. Before reuse, the inactive slot is cleared through bounded pages. At
most one Ready and one Building slot exist, and retired generations are not
retained as history.

### Generation identity

Every header binds all of the following current authority:

- format version `1`;
- database incarnation;
- a domain-separated fingerprint of the active data/index/schema/journal
  allocation quartet, including each memory ID and stable key in role order;
- accepted store-root presence, revision, and 32-byte fingerprint;
- the count and domain-separated fingerprint of canonical sorted accepted
  user `IndexId` values;
- `FoldWatermark::{highest_folded_journal_sequence, fold_epoch}`;
- checked monotonic generation ID;
- active slot and `Building` or `Ready` state.

The accepted root already commits to the accepted bundle, but the explicit
index-set digest keeps the counted physical generations reviewable and
independently checkable. Missing, malformed, unsupported, mismatched, or stale
identity is unavailable evidence. No predecessor decoder or generated-model
reconstruction is permitted.

### Build cursor

The cardinality build cursor is unrelated to the removed convergence cursor.
It is a bounded scalar checkpoint for optional derived-evidence construction
and uses only the current version-1 form. It binds the candidate generation and
all source identities above, then stores:

- phase `Rows` or `Indexes`;
- the last completed physical row or index key, resumed exclusively;
- checked source-entry, source-byte, prefix-update, and distinct-count-key
  totals.

The row key is bounded by `RawDataStoreKey` at 262 bytes. The index key is
bounded by `IndexKey` at 16,668 bytes, so the complete cursor record must remain
within 20 KiB. A page admits at most 4,096 source entries, 16,777,216 source
key/value bytes, and 16,384 prefix updates, stopping before the first exceeded
dimension. The page must also remain below the inherited 30-billion
instruction policy and the IC 40-billion message limit. These are fixed
internal limits, not caller configuration.

Rows are scanned first in canonical physical-key order, followed by index
entries in canonical physical-key order. Index build input must decode to a
present accepted user-index entry in the bound set; every non-empty component
prefix contributes exactly once. A changed root, index set, watermark, store
identity, or database incarnation discards the candidate and starts a fresh
inactive slot. No page scans live overlays as canonical input.

### Frozen lifecycle transitions

| From | Event | To and required behavior |
| --- | --- | --- |
| Absent/unavailable | Empty canonical row and accepted-index domains | Publish Ready exact zero at the current identity/watermark without a scan |
| Absent/unavailable | Populated current store | Clear the inactive slot and enter Building; ordinary planning remains conservative |
| Building | Bounded page, source unchanged | Advance the exclusive physical cursor in the same candidate |
| Building | Source identity or watermark drift | Discard/clear the candidate and restart from the new exact source |
| Building | Rows and indexes complete, source unchanged | Atomically publish the candidate slot as Ready |
| Ready | Prepared canonical batch | Preflight checked count deltas and target generation before canonical mutation; publish the new complete watermark with the exact counts |
| Ready | Accepted root/index-set drift | Make old evidence unavailable and begin a fresh isolated build |
| Ready | Live debt after its bound watermark | Derive exact visible counts only from the Ready base plus exact positioned-overlay deltas; otherwise fall back |

The existing 0.225 replicated driver owns optional build wake-ups, duplicate
coalescing, and self-chaining. Startup never scans rows or indexes and may
reach ordinary `Ready` while cardinality is Building/unavailable because the
planner fallback is correctness-preserving. No timer, scheduler, persisted
`work_pending` flag, public mode, or configuration axis is added.

## Frozen Resource And Acceptance Gates

Let `G` be the number of nonzero entity keys plus distinct accepted user-index
prefix keys in one complete generation. A count record uses one 16-byte key and
a fixed value containing magic/version, generation ID, full 32-byte logical-key
digest, and exact `u64` count. Patch 2 must keep that value at or below 64 bytes
and the fixed generation header at or below 256 bytes.

- Logical steady count payload is bounded by `80G + 256` bytes.
- Physical candidate high-water, measured on an isolated store, must remain at
  or below `256G + 262,144` bytes above the pre-build baseline.
- Repeated clear/build/publish/clear/build cycles must reuse the two namespaces
  and stop increasing physical high-water after both slots have reached the
  maintained maximum shape.
- One row insertion/deletion contributes at most one entity delta. Each actual
  accepted user-index put/delete contributes at most four prefix deltas.
  Same-key deltas are coalesced before stable writes, so prepared delta events
  satisfy `D <= R_insert_delete + 4I_user_actual`.
- Patch 5 must retain the 0.229 30-billion fold-callback ceiling, including the
  65,536-key accepted-index shape. A ceiling increase is not authorized. If
  exact maintenance cannot fit, Patch 5 stops for a design decision rather
  than weakening exactness or adding paged convergence.
- Ready direct COUNT must retain zero row-store gets and zero index-entry reads.
  Missing/Building/stale/malformed evidence must return the maintained result
  through conservative execution, never through an estimate or guessed zero.
- `post_upgrade` must perform no canonical row/index scan and remain under the
  maintained 25-million startup-observation ceiling.
- The complete 0.230 line may add no Candid method/mode or export and may not
  grow the final raw `sql_perf` Wasm by more than 131,072 bytes without a
  reviewed simplification or scope decision. Raw Wasm is authoritative; gzip
  remains secondary.

These gates deliberately make maximum accepted-index maintenance and physical
slot reuse blocking evidence. They are not permission to raise an inherited
ceiling or add a second admission/scheduling route.

## Patch 1 Measurements

The current `sql_perf` production artifact at the exact predecessor is:

| Measure | Observed |
| --- | ---: |
| Compiler-emitted Wasm | 4,888,976 bytes |
| Final deployable raw Wasm | 4,269,683 bytes |
| Deterministic gzip in the current tool environment | 1,648,186 bytes |
| Candid | 60,428 bytes |
| Candid SHA-256 | `5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1` |
| Final exports | 70 |

The compiler and raw sizes exactly match the accepted 0.229.1 status. The gzip
is 524 bytes above its recorded 1,647,662-byte secondary value; later
comparisons use this refreshed environment and do not treat gzip as authority.

The maintained branch-set direct-count fixture observed 494,655 execute and
1,155,600 total local instructions for the plain count, and 496,218 execute and
1,174,774 total for the duplicate-literal count. Both performed zero data gets,
index gets, index range scans, and index entry reads. These values are baseline
observations, not new ceilings.

Startup observation measured 160,897 instructions while fresh/Recovering,
378,507 when Ready, and 250,821 immediately after upgrade/Recovering, all below
25,000,000. The current code performs no cardinality reconstruction there.

The maintained collection-scale run recovered all 10,001 target rows, used 179
Forward and 49 Verify advances, observed maxima of 27,922,379 and 7,583,571
instructions, and preserved stable-memory length exactly at 1,124,139,008
bytes across upgrade recovery. This is predecessor workload/baseline evidence;
Patch 3 must enforce the structural page gates and measure candidate storage,
while Patch 6 owns production real-canister instruction measurement.

## Validation Record

- Focused `icydb-core` cardinality selection: 19 passed, 0 failed.
- Exact schema empty-proof classification: 1 passed, 0 failed.
- Production-shaped branch-set/cardinality planner selection: 1 passed,
  0 failed; 29 filtered out.
- Collection-scale durable mutation/upgrade fixture: 1 passed, 0 failed.
- Startup observation/upgrade fixture: 1 passed, 0 failed; 3 filtered out.
- Production `sql_perf` Wasm/Candid/export report: passed with the identities
  above.

Patch 1 intentionally does not run or claim the user-owned full validation
suite. Initial build instructions, stable bytes per generated count, candidate
peak storage, incremental write cost, and post-Ready populated planning remain
future implementation evidence owned by Patches 2 through 6.

## Patch 1 Change Surface

- Runtime, persisted state, public API, Candid, exports, stable-memory IDs,
  timers, schedulers, modes, and configuration: unchanged.
- Documentation: one implementation-status tracker, accepted design status,
  cursor-free wording correction, and programme reconciliation.
- Complexity: production remains neutral. The future state space is narrowed to
  one header, one bounded build cursor, two physical slots, and Building/Ready;
  no duplicate entity/index generations or historical registry is authorized.

## Patch 2 Durable Generation Identity And Codec Results

### Current persisted forms

- `db/schema/cardinality_generation.rs` owns one current version-1 codec set.
  There is no predecessor constant, decoder, inspector, translation, or
  fallback form.
- The generation header is exactly 192 bytes: a 160-byte fixed body plus a
  domain-separated 32-byte SHA-256 fingerprint. Its source identity occupies
  141 fixed bytes and binds every frozen database/store/schema/index/watermark
  dimension.
- A count record is exactly 57 bytes and the schema key remains exactly 16
  bytes, for 73 logical bytes per stored nonzero count. The value retains the
  complete 32-byte digest, so the 15-byte physical digest prefix cannot become
  an alternate authority after a collision.
- The build cursor has a 197-byte fixed body, an optional bounded physical key,
  and a 32-byte fingerprint. Its maximum index-checkpoint form is 16,897 bytes
  (16,913 logical bytes with its schema key), below the frozen 20-KiB limit;
  the maximum row-checkpoint form is 491 bytes.
- `RawSchemaKey` now reserves exact control keys in namespace `0x05` and
  digest-prefixed count keys in slots `0x06` and `0x07`. A focused stable-map
  reopen proves all three record families remain decodable from the existing
  schema allocation.
- Generation increment, encoded-length arithmetic, cursor bounds, and all
  reader offsets are checked; the exact count form accepts through `u64::MAX`
  while zero remains absence. Zero generation/count values,
  unknown versions, malformed lengths, trailing data, fingerprint corruption,
  invalid source closure, phase/checkpoint mismatch, and typed source or count
  identity mismatches fail closed.

Patch 2 defines persisted authority but has no production writer or consumer.
No existing database contains these records until Patch 3 activates bounded
construction, so this landing does not require migration or perform a startup
scan.

### Patch 2 artifact and storage evidence

| Measure | Observed | Delta from Patch 1 |
| --- | ---: | ---: |
| Header value | 192 bytes | New current form; below 256-byte gate |
| Count key + value | 73 bytes | New current form; below 80-byte logical allowance |
| Maximum build cursor value | 16,897 bytes | New current form; below 20 KiB |
| Compiler-emitted Wasm | 4,888,976 bytes | 0 bytes |
| Final deployable raw Wasm | 4,269,683 bytes | 0 bytes |
| Deterministic gzip | 1,648,180 bytes | -6 bytes, secondary only |
| Candid | 60,428 bytes | 0 bytes |
| Candid SHA-256 | `5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1` | unchanged |
| Final exports | 70 | 0 |

The zero raw-Wasm delta confirms that the dormant codecs do not enter the
production artifact before their bounded builder owner is activated. Patch 3
must measure physical candidate high-water and repeated slot reuse; Patch 2
can establish only the exact per-record logical storage forms.

### Patch 2 validation record

- Focused `icydb-core` cardinality selection: 29 passed, 0 failed, including
  nine direct codec/identity tests and one schema-allocation stable-reopen test.
- Warning-denied `icydb-core` all-feature library/test Clippy: passed.
- Durability-documentation and memory-ID invariant guards: passed.
- Production `sql_perf` Wasm/Candid/export report: passed with the identities
  above and no local ICP network dependency.

The user-owned full validation/release suite was not run. Patch 3 owns bounded
scan pages and physical stable-byte growth, Patch 4 owns restart behavior, and
Patch 6 owns production instruction evidence.

### Patch 2 change surface

- Implementation: one approximately 1,170-line owner module (about 830 lines
  of bounded codec/identity logic and 330 lines of direct tests), 40 schema-key
  lines, and one module declaration.
- Tests: one approximately 112-line stable schema-allocation reopen fixture in
  the maintained store test owner.
- Runtime execution routes, planners, drivers, timers, schedulers, public
  configuration, Candid, exports, and stable-memory IDs: unchanged.
- Persisted state space: one current header, one bounded build cursor, and two
  reusable count slots are now concretely encoded. No generation history,
  alternate count owner, legacy format, or compatibility route was added.
- Complexity necessarily increases at the dormant codec boundary while the
  active production execution shape remains neutral. No safe semantic
  consolidation remains inside Patch 2; construction belongs to Patch 3.

## Patch 3 Bounded Populated-Store Build Results

### Canonical construction route

- `db/schema/cardinality_build.rs` is the sole construction owner. One call
  performs at most one bounded inactive-slot clear or one bounded `Rows` or
  `Indexes` scan page. There is no alternate builder, startup scan, timer,
  scheduler, public mode, or caller-settable budget.
- Build authority is derived from one paired read of the canonical accepted
  root and verified immutable bundle, the database incarnation, active store
  allocation quartet, sorted accepted physical user-index IDs, and exact fold
  watermark. Generated models do not participate.
- Rows are visited only through the canonical predecessor map after an
  exclusive physical checkpoint. Every raw key must decode to an accepted
  entity; the row key and payload bytes are charged before the row contributes
  one exact entity-count increment.
- Index entries are visited only through the canonical predecessor map after
  an exclusive physical checkpoint. Raw key, primary-key witness, and entry
  witness decode before use. Present entries of the exact accepted physical
  user-index set contribute every nonempty component prefix; system,
  candidate/rejected, and `Missing` entries remain explicitly excluded.
  Accepted-index arity mismatch and malformed raw input fail closed.
- Page-local digest increments are bounded, sorted, and coalesced before any
  stable count write. Full-digest collision, duplicate uncoalesced input,
  generation mismatch, count overflow, cursor/header mismatch, and totals
  overflow fail during read-only preflight.
- Count writes and the next checksummed cursor are applied in one mechanical
  boundary after complete preflight. Once stable mutation begins, the only
  operations are infallible BTree insertions; a backend trap retains IC
  whole-message rollback rather than returning a partial-page error.
- A source mismatch returns its exact typed dimension without reusing or
  mutating the candidate. Patch 3 deliberately left a fully scanned candidate
  planner-invisible; Patch 4 now owns bounded discard/restart and atomic
  `Ready` publication.

### Page and storage evidence

| Measure | Frozen limit | Focused evidence |
| --- | ---: | ---: |
| Source entries per page | 4,096 | Exact boundary and 4,097-row/index exclusive resume pass |
| Source key/value bytes per page | 16,777,216 | Exact stop-before boundary and checked arithmetic pass |
| Accepted prefix updates per page | 16,384 | 4,096 four-component entries stop exactly; the 4,097th resumes after reopen |
| Build cursor value | 20 KiB | Existing maximum 16,897-byte codec evidence retained |
| Inactive-slot clear | 4,096 records | 4,097-record clear takes two pages and installs the Rows cursor only after empty closure |
| Candidate physical growth for 4,096 distinct keys | 1,310,720 bytes maximum | 786,432 bytes above the initialized schema-store baseline |

The 4,096-distinct-key observation satisfies
`C <= 256G + 262,144` at this maintained page shape. Patch 4 now proves logical
two-slot clear/build/publish reuse, while Patch 6 retains the final repeated
maximum-shape physical high-water and real-canister instruction evidence.

The refreshed production `sql_perf` artifact is 4,888,513 compiler-emitted
bytes and 4,269,046 final raw bytes, respectively 463 and 637 bytes below the
Patch 2 artifact. Deterministic gzip is 1,650,925 bytes, 2,745 bytes above
Patch 2 and secondary only. Candid remains 60,428 bytes with SHA-256
`5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`,
and the final artifact retains 70 exports. The builder adds no public surface
and remains far inside the 131,072-byte raw-Wasm review gate.

### Patch 3 validation record

- Focused `icydb-core` cardinality selection: 39 passed, 0 failed.
- Eight focused builder tests pass, covering every page dimension, populated
  row and maximum-fanout index pagination, row-to-index transition, exclusive
  resume after stable reopen, accepted-prefix totals, domain exclusions,
  malformed keys, source mismatch, canonical authority derivation, and
  physical candidate growth.
- Three focused schema-store tests pass for current-record reopen, checked count
  overflow/duplicate-input rejection, and bounded inactive-slot clearing.
- Warning-denied all-feature `icydb-core` library and test-target Clippy passes.
- The production `sql_perf` Wasm/Candid/export report and durability-format and
  memory-ID invariant guards pass. The user-owned full validation/release suite
  and real-canister instruction and repeated slot-reuse evidence were not run
  in this slice.

### Patch 3 change surface

- Production implementation adds one approximately 470-line builder owner,
  about 290 schema-storage/preflight lines, one 31-line canonical row visitor,
  and narrow generation/snapshot accessors. Tests add approximately 635 lines
  of bounded page, corruption, reopen, overflow, and storage evidence.
- Persisted state remains the Patch 2 version-1 header, cursor, and two count
  namespaces. Patch 3 adds no stable-memory ID, generated authority, history
  registry, compatibility form, scheduler, timer, configuration, Candid
  method, or export.
- Patch 3 left runtime construction deliberately unreachable until Patch 4
  could pair source-drift restart with atomic `Ready` publication. Complexity
  increased only at the bounded construction boundary; no second count owner
  or scan route was introduced.

## Patch 4 Revision Invalidation And Ready Publication Results

### One lifecycle and publication route

- `db/schema/cardinality_build.rs` now owns the complete store-local lifecycle.
  It derives the exact source before and after each mutating page, treats any
  changed database/store/root/index-set/watermark dimension as stale, advances
  the generation with checked arithmetic, selects the other reusable slot,
  and removes the obsolete cursor in the same replicated transition. Counts
  from the stale slot are never merged into the new candidate.
- Natural exhaustion of both canonical domains creates a private complete-
  candidate proof before stable mutation. Publication revalidates the exact
  Building header, source, and cursor, then mechanically switches the header
  to `Ready` and removes the cursor. An incomplete, stale, replayed, or
  mismatched candidate cannot publish. Stable-map traps retain whole-message
  rollback; there is no normally returned error after the header switch.
- A physically empty canonical row and index domain publishes exact zero with
  one bounded root observation and no build cursor or row/index scan. A valid
  `Ready` header must have no build cursor. Missing, malformed, or contradictory
  evidence remains unavailable rather than being repaired or exposed.
- The existing 0.225 generated watchdog is the only scheduler. Once startup
  recovery and generated-schema reconciliation are complete, it advances at
  most one store-local clear/scan/restart/publication page, self-chains while
  work remains, and stops after all registered journaled stores are current.
  Install and upgrade arm that same watchdog even when ordinary startup is
  already `Ready`; startup observation itself remains a fixed-control read and
  never scans canonical rows or indexes.
- Store selection is bounded by the existing registered-store limit and sorted
  only for deterministic optional-work order. There is no new timer, persisted
  work flag, public endpoint, caller-settable budget, generation history,
  generated-model fallback, or alternate builder. Cardinality corruption stops
  optional construction without changing ordinary readiness; Patch 5's
  conservative planner route remains the correctness fallback.

### Drift, publication, and reuse evidence

- Focused lifecycle tests cover exact-zero publication, complete populated
  publication, cursor retirement, stale complete-candidate rejection, row-
  phase drift, index-phase drift, watermark drift, accepted-index identity
  drift, generation rollover, alternating-slot reuse, and removal of a retired
  index prefix count before the reused slot becomes `Ready`.
- The maintained generated-driver test proves an externally `Ready` database
  publishes an empty generation through the existing driver, returns one
  continuation result, and then quiesces. Generated-source invariants prove
  both `Ready` and `Recovering` lifecycle registration use the same watchdog;
  native libtests drain that same production driver rather than constructing
  cardinality authority directly.
- Patch 4 does not claim real-canister build instructions or the final repeated
  maximum-shape stable high-water. Patch 6 remains the owner of those
  production-shaped measurements. The focused slot-reuse test establishes the
  logical two-namespace closure and absence of cross-generation count leakage.

### Patch 4 artifact evidence

| Measure | Patch 3 | Patch 4 | Delta |
| --- | ---: | ---: | ---: |
| Compiler-emitted Wasm | 4,888,513 bytes | 4,937,643 bytes | +49,130 bytes |
| Final deployable raw Wasm | 4,269,046 bytes | 4,312,818 bytes | +43,772 bytes |
| Deterministic gzip | 1,650,925 bytes | 1,669,420 bytes | +18,495 bytes |
| Candid | 60,428 bytes | 60,428 bytes | 0 bytes |
| Exports | 70 | 70 | 0 |

The raw-Wasm increase activates the already-frozen builder plus restart,
publication, and driver coordination. It remains 87,300 bytes below the
complete-line 131,072-byte raw-Wasm review gate. Candid SHA-256 remains
`5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`;
there is no public method or mode change.

### Patch 4 validation record

- Focused `icydb-core` cardinality selection: 44 passed, 0 failed. The focused
  startup-driver selection passes 9 tests, and the generated store/watchdog
  selection passes 6 tests.
- The dedicated current-driver fixture passes and proves exact-zero
  publication plus quiescence without changing `DatabaseStartupState::Ready`.
- Warning-denied all-feature `icydb-core` library/test Clippy and warning-denied
  `icydb-model` all-target Clippy pass.
- The production `sql_perf` Wasm/Candid/export report passes with the artifact
  identities above. Durability-format and memory-ID invariant guards and final
  formatting pass; diff hygiene is checked at handoff.
- The user-owned full validation/release suite, PocketIC build-instruction
  measurements, and repeated maximum-shape physical slot high-water are not
  run in this landing patch; Patch 6 owns the latter evidence.

### Patch 4 change surface

- Production changes add the lifecycle coordinator and complete-candidate
  proof, about 100 lines of atomic restart/publication storage transitions,
  two bounded canonical-emptiness observations, and about 50 lines connecting
  optional work to the existing startup driver. Generated wiring changes only
  lifecycle registration and continuation decisions for that same driver.
- Focused tests add roughly 300 lines across lifecycle drift/publication,
  existing-driver zero publication, slot reuse, and generated-source
  invariants. The implementation adds no stable-memory ID or record form beyond
  Patch 2's header/cursor/two count namespaces.
- Execution shape grows only by activating the accepted optional generation
  lifecycle. Scheduler/timer count, public configuration, persisted state
  axes, planner routes, and count authorities remain unchanged at the Patch 4
  boundary. Patch 5 now connects incremental maintenance and exact planner
  consumption without adding another lifecycle owner.

## Patch 5 Incremental Maintenance And Planner Consumption Results

### One exact maintenance and consumption boundary

- Complete-batch convergence derives count changes from the already-prepared
  canonical row transitions. Final row and physical index targets are
  coalesced by raw key, so same-key repetitions contribute only their final
  canonical transition and retain
  `D <= R_insert_delete + 4I_user_actual`.
- Accepted entities contribute one presence transition. Only present entries
  from the exact accepted physical user-index set contribute each nonempty
  prefix; system, candidate/rejected, and `Missing` entries remain excluded.
  Accepted-index arity, row witness, raw key, count identity, truncated-key
  collision, and signed arithmetic are validated before canonical mutation.
- The schema store preflights every checked `u64` count addition/subtraction,
  zero-record deletion, and the next source-bound `Ready` header before the
  first row or index Apply. The mechanical fold phase then applies canonical
  effects, count records, and the new complete watermark. Any impossible
  post-preflight backend contradiction traps, retaining IC whole-message
  rollback instead of returning a partial success.
- Accepted-schema and migration families invalidate the old source rather than
  attempting to reinterpret its count domain. The existing optional driver
  observes the stale identity and rebuilds through the two-slot lifecycle;
  there is no second maintenance job, cursor, timer, or scheduler.
- Journaled row and index stores retain only signed aggregate live-overlay
  deltas relative to canonical state. They do not duplicate values. Canonical
  folds hidden by a newer positioned effect apply the inverse base transition,
  so exact visible counts remain stable while older batches retire. Malformed
  or arithmetically impossible volatile evidence becomes unavailable.
- `StoreHandle` is the single proof boundary. Heap stores may reuse their exact
  volatile counts. Journaled stores validate live and canonical accepted-root
  agreement, the `Ready` source, absent build cursor, exact count record, and
  accepted logical target before combining the durable base with the signed
  delta. Volatile index enumeration is only a proposal and is consumed after
  durable parent/child totals prove it complete. Every unavailable or
  mismatched case returns `None` to the maintained conservative route.
- Direct COUNT, empty-branch pruning, prefix-sum routing, SQL DDL emptiness, and
  accepted-schema historical-row proofs consume that same boundary. The old
  streaming-only cardinality helper was removed, leaving one pruning route.

### Patch 5 artifact evidence

| Measure | Patch 4 | Patch 5 | Delta |
| --- | ---: | ---: | ---: |
| Compiler-emitted Wasm | 4,937,643 bytes | 4,975,617 bytes | +37,974 bytes |
| Final deployable raw Wasm | 4,312,818 bytes | 4,346,191 bytes | +33,373 bytes |
| Deterministic gzip | 1,669,420 bytes | 1,682,220 bytes | +12,800 bytes |
| Candid | 60,428 bytes | 60,428 bytes | 0 bytes |
| Exports | 70 | 70 | 0 |

The final raw artifact is 76,508 bytes above the accepted 0.229.1 baseline,
leaving 54,564 bytes inside the complete-line 131,072-byte review allowance.
Candid SHA-256 remains
`5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`;
no public method, method mode, export, stable-memory ID, configuration, timer,
or scheduler changed.

### Patch 5 validation record

- The focused `icydb-core` cardinality selection passes 46 tests covering
  codecs, construction, publication, source drift, every accepted prefix,
  exclusions, checked maintenance, volatile deltas, populated reopen, and the
  durable-base-plus-live-delta route. The final enriched insert/update/delete,
  same-key fold, and conservative-Building regression also passes directly.
- Both maintained online complete-batch convergence regressions and the
  batch-final unique-release regression pass. Warning-denied all-feature
  `icydb-core` library/test Clippy passes.
- Durability-format, memory-ID, and generated-endpoint invariant guards pass.
  The complete production Wasm report passes and retains the artifact and
  public-surface identities above.
- The user-owned full validation/release suite was not run. Patch 6 still owns
  real-canister populated build/upgrade/planner instruction evidence, the
  maximum-shape fold measurement under 30 billion instructions, direct COUNT
  read counters after reopen, and repeated maximum-shape two-slot high-water.

### Patch 5 change surface

- Production adds checked Ready-generation maintenance at the existing fold
  boundary, two bounded volatile signed-delta maps, and proof-bound consumers.
  It reuses the Patch 2 count/header formats and Patch 4 driver, adding no
  persisted state axis or execution route.
- The principal implementation spans recovery, row/index cardinality owners,
  schema count preflight, the store handle, and existing planner/schema
  consumers; focused tests extend the existing store and journaled-session
  fixtures. Boxing the index delta avoids imposing the journaled backend's
  larger enum footprint on heap-only stores.
- Complexity increases only by the required incremental evidence and delta
  maintenance. Removing the duplicate streaming pruning helper consolidates
  consumption, and no safe semantic deletion remains before Patch 6 supplies
  cross-owner real-canister evidence and closeout reconciliation.

## Patch 6 Populated Real-Canister Evidence And Closeout Results

### Populated lifecycle closure

- The production-shaped SQL audit canister loads and canonically folds 2,048
  rows with three maintained secondary indexes, then publishes a new accepted
  `active` index over the populated state. The DDL producer scans and writes
  exactly 2,048 rows/keys before the sole existing watchdog starts the isolated
  cardinality candidate.
- A current-Wasm upgrade first performs the existing bounded generated-schema
  handoff under `Recovering`. Ordinary readiness then returns while the
  cardinality generation remains incomplete and the same watchdog remains
  armed. `COUNT(active = true)` returns the exact 512 result through the
  conservative index route during that interval; no startup row/index scan or
  guessed cardinality is involved.
- Four subsequent optional watchdog messages complete the populated
  generation. The same count then uses `IndexPrefixCardinality` with zero data
  gets and zero index-entry reads. One live inserted row immediately changes
  the exact visible result to 513 through the bounded overlay delta and remains
  513 after the canonical fold.
- Dropping and recreating the accepted index changes the exact source, rotates
  construction through the alternate slot, and later reuses the first slot.
  The recreated producer scans and writes exactly 2,049 rows/keys; its final
  exact count is 513 with zero data gets and zero index-entry reads.
- Total stable memory is 796,983,296 bytes after the first Ready publication,
  after the drop/rebuild, and after recreate/rebuild. The focused 4,096-key
  physical-store fixture reaches 851,968 bytes in slot A and 1,703,936 bytes
  after slot B, then remains exactly 1,703,936 bytes when slot A is reused.
  Together these prove bounded two-slot reuse rather than physical shrinkage or
  history-growing generation storage.

### Instruction and maximum-shape reconciliation

| Production-shaped evidence | Observed instructions | Maintained boundary |
| --- | ---: | ---: |
| 4,096 zero-index row fold | 2,329,378,503 | < 30,000,000,000 |
| 2,048 rows with three secondary indexes | 3,736,427,024 | < 30,000,000,000 |
| 240 rows with 64 secondary indexes | 10,575,418,790 | < 30,000,000,000 |
| 65,536-key accepted-index maximum fold callback | 21,089,837,408 | < 30,000,000,000 |
| Populated active-index creation | 6,868,434,605 | < 40,000,000,000 IC message limit |
| Populated active-index drop | 7,195,350,945 | < 40,000,000,000 IC message limit |
| Populated active-index recreation | 6,649,671,715 | < 40,000,000,000 IC message limit |
| 65,536-key accepted-index producer | 32,566,926,423 | < 40,000,000,000 IC message limit |

The maximum accepted-index recovery uses five complete-batch callbacks and
records 33,186,355,292 total instructions. Its largest callback remains the
21.090-billion fold above. No ceiling was raised: normal convergence retains
the 30-billion callback policy and every measured producer remains within the
IC 40-billion message limit.

### Final artifact closure

| Measure | 0.229.1 baseline | Clean v0.230.3 release | Delta |
| --- | ---: | ---: | ---: |
| Compiler-emitted Wasm | 4,888,976 bytes | 4,997,103 bytes | +108,127 bytes |
| Final deployable raw Wasm | 4,269,683 bytes | 4,364,835 bytes | +95,152 bytes |
| Deterministic gzip | 1,648,186 bytes | 1,692,313 bytes | +44,127 bytes |
| Candid | 60,428 bytes | 60,428 bytes | 0 bytes |
| Exports | 70 | 70 | 0 |

The authoritative raw artifact remains 35,920 bytes inside the accepted
131,072-byte complete-line allowance. Candid SHA-256 remains
`5bc8b2c65e8e57ec1bee7db4677d7fff3bcab686159128fc2604fe40c00882d1`.
The closeout audit endpoint is compiled only under `test-admin-api` and is
absent from the production Candid and 70-export artifact.

### Validation record

- Focused `icydb-core` cardinality selection: 50 passed, 0 failed, including
  the repeated maximum-shape physical slot-reuse proof.
- Populated build/upgrade/maintenance/revision/reuse real-canister fixture:
  1 passed, 0 failed. Maintained 4,096-row, 2,048-row/three-index,
  240-row/64-index, and 65,536-key accepted-index fixtures: 4 passed, 0 failed.
- The maintained complete-batch trap/rollback and canonical watchdog retry
  fixture passes with required readiness separated from optional build
  quiescence.
- Warning-denied all-target/all-feature core Clippy, all-feature SQL audit
  canister Clippy, focused recovery-closeout integration Clippy, and the
  focused generated-watchdog source test pass.
- Final formatting, dependency, deployment-inventory, durability-format,
  memory-ID, generated-endpoint, authority, atomicity, release, read-admission,
  schema/model, SQL-ownership, and Wasm post-link invariants pass. Earlier broad
  runs exposed the corrected schema, recovery, cache-concurrency, and
  maximum-fanout fallout; every focused rerun passed, with the fanout case at
  5,075,414 of 5,150,250 instructions. The user-owned `make patch` then ran the
  repository's complete `make validate` gate against exact candidate
  `8e72cbb3c4ac1697a5f2aa13986970ad00af0901` before version mutation. The
  verified candidate and release-gate receipts close the complete-suite
  evidence without an agent-owned duplicate run.

### Independent closeout correction

- Journaled entity and prefix consumers previously accepted volatile full
  counts before validating durable evidence. They now require one
  source-matching `Ready` base plus the exact overlay delta.
- Partial accepted user-index prefixes were incorrectly rejected unless their
  length equalled full index arity. The maintained authority now accepts every
  nonempty prefix through full arity.
- A newly published live accepted root could leave the old canonical-root
  generation consumer-visible until folding. Consumer authority now requires
  live/canonical accepted-root equality.
- Bounded child enumeration bypassed `StoreHandle`. It now crosses that proof
  boundary and proves the proposed child set complete with durable exact
  parent/child totals.
- The initial correction duplicated a stable accepted-bundle read and decode
  for every count proof, exposing a maintained branch-performance failure.
  Consumers now borrow the existing verified effective bundle only after its
  root selection exactly matches the independently loaded canonical root. This
  adds no cache or proof lifetime and makes the exact branch 48 percent cheaper
  than the maintained conservative comparator.
- The broad validation log exposed another repeated-proof cost at the
  maximum-fanout boundary: 6,264,502 instructions exceeded the frozen
  5,150,250 ceiling. The final path carries the already-admitted root and
  incarnation, binds the exact index delta to its canonical fold watermark,
  batches control/root reads, precomputes allocation identity, and reuses only
  decoded data whose raw stable bytes still match. The corrected case is
  5,075,414 instructions without changing the ceiling.
- Two generated-startup schema fixtures attempted successor application before
  required recovery and durable exact-count construction had completed. They
  now drive the existing bounded owners to `Ready` before asserting the schema
  outcome; production admission was not weakened.
- The concurrent `ic-testkit` 0.8 hard cut left three fixture-pool calls on the
  removed method name. They now use the maintained structured `acquire` API;
  the SQL test target compiles and the maximum accepted-index scenario passes.
- The later no-default core run passed 1,429 tests before an older recovery
  assertion treated the new empty-tail volatile projection reset as a
  canonical rewrite. The regression now requires exactly one data/index
  projection-generation advance and unchanged canonical row/index lengths;
  its exact no-default rerun passes.
- The same broad run's workspace lane changed source inputs while a fixture
  Cargo build was active, so the exact cache correctly rejected publication.
  With the worktree held quiescent, the exact query-canister regression passes;
  the cache guard remains fail-closed and was not weakened.

### Closeout change surface

- Patch 6 and the audit correction add no production persisted record,
  stable-memory ID, value owner, execution route, timer, scheduler, mode,
  configuration, Candid method, or export. The audit endpoint remains
  audit-feature-only, and the correction reuses the existing accepted-bundle
  cache without cloning its maximum-sized structure.
- Focused evidence adds one populated real-canister scenario and one direct
  two-slot physical high-water regression. Generated watchdog source is
  simpler by consolidating behavior-identical `Ready` and `Recovering` arms.
- The complete line has one durable generation/header, one bounded derived-
  evidence cursor, two reusable physical count slots, one builder, one fold
  maintenance boundary, one proof-bound consumer, and the existing replicated
  driver. No compatibility path or generation history remains to remove.
