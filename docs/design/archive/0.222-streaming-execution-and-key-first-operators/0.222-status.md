# IcyDB 0.222 Implementation Status

Status: all eight implementation outcomes landed; compatibility correction and
closeout Corrections A-P implemented; every correctness, structural,
performance, interface, and closeout gate passes; closeout accepted

Design: [0.222-design.md](0.222-design.md)

Patch 1 authority:
[0.222-patch-1-authority.md](0.222-patch-1-authority.md)

Design cut: 2026-08-07

Contract review incorporated: 2026-08-07

## Baseline

- Previous release: `0.222.1` published as `v0.222.1`
- Commit: `c6213824cf7abf208aee137462096ebdbf289292`
- Tree: `4eb9cfc6eea55046cdcd87008213287f750e74fd`
- Branch: `main`
- Workspace package version: `0.222.1`
- Rust: `rustc 1.97.1 (8bab26f4f 2026-07-14)`
- `ic-wasm`: `0.11.0`
- Binaryen: `wasm-opt version 108`

Patch 8 starts from the clean published `0.222.1` tree. Patches 5-7 were
published in that release. Release tooling remains the only workspace-version
owner.

## Design Reconciliation

The source idea proposed a new key/row streaming architecture. The current
engine already owns concrete `OrderedKeyStream` execution, merge/intersection,
top-N seek contracts, resumable pages, late primary-row loading, and shared
structural slot decoding. 0.222 therefore contracts remaining eager ownership
inside those maintained owners instead of adding a parallel iterator or
executor framework.

Patch 1 audited these materialization owner families:

- materialized physical key resolution and bounded prefix families;
- one-key-at-a-time sparse intersection;
- covering/hybrid projected-row vectors;
- structural `RowView::into_owned` boundaries;
- scalar retained/order rows before the page boundary;
- distinct retained output rows; and
- grouped/hash state that may retain more source data than its aggregates need.

The machine-readable inventory classifies each family before behavior changes.
Each entry records a primary semantic reason plus lifetime, size authority,
backing ownership, resumability, and action owner. Complete retained backing
bytes remain explicitly unmeasured until Patch 5; a small borrowed slice may
not hide a large retained payload.

## Contract Review Resolution

The pre-implementation audit is accepted. The design now requires:

- `seek_head_at_or_after` held-head semantics, with positioning separate from
  consumption and direction defined by traversal order;
- an interruption-by-interruption intersection continuation proof and strict
  monotonic progress before planner eligibility;
- bounded per-child operator state as a separately reviewed cursor-format
  outcome if the current scalar continuation cannot prove progress;
- ordered-adjacent versus complete global distinct strategies and closed
  ordered-group versus complete hash-group strategies;
- compact owned values at blocking boundaries or accounting for the complete
  retained backing payload;
- an indivisible page-work-unit rule, typed oversize failure, and a prohibition
  on unchanged successful empty-page cursors;
- bounded prefix-family fan-out, deterministic conservative planner costing,
  complete seek/storage charging, and typed accepted-index corruption; and
- fixed named performance fixtures requiring both physical-counter and
  instruction evidence.

Patch 1 is a contract/inventory/baseline patch, not a behavioral optimizer. It
publishes the cursor-feasibility and blocking-route matrices before later
routes can rely on them.

## Published Baseline Measurements

| Shape | `v0.221.9` instructions |
| --- | ---: |
| 200 repeated identical point queries | 54,435,151 |
| 200 distinct parameterized point queries | 88,949,803 |
| 200 ordered empty scans | 50,871,720 |

| Subject | Raw final Wasm |
| --- | ---: |
| Dynamic query, feature off | 2,479,797 |
| Dynamic query, request diagnostics | 2,523,044 |
| Typed query | 1,792,871 |

Patch 1 adds the named materialization/intersection/order/distinct/grouped
fixtures, fixed data seed, exact SQL, expected row counts, evidence owners, and
review gates before implementation measurements are accepted.

## Patch 1 Evidence

- The audit schema now owns independent single-index and exact compound-index
  entities over the same deterministic 2,048-row distribution.
- A machine-readable contract freezes 23 fixture identities, their SQL,
  direction, continuation mode, expected rows, evidence counter, and gate.
- Focused PocketIC execution verifies the distribution and every one-shot
  result. Representative costs range from 630,186 instructions for the early
  primary limit to 195,217,080 for the complete sort.
- The structural baseline shows 21 row fetches for a sparse two-predicate
  candidate versus one index entry for its compound control, and 2,048 fetches
  for both late residual `LIMIT 1` and wide top-N.
- The current scalar cursor cannot represent divergent held heads in a
  resumable intersection. Patch 4 remains blocked from planner eligibility
  until Patches 2-3 prove a monotonic safe watermark or trigger a separately
  reviewed bounded per-child continuation.
- The audit-only `sql_perf` final Wasm changes from 3,710,630 to 3,719,672 raw
  bytes (`+9,042`, `+0.244%`); gzip changes by `+2,043` bytes. Executor behavior
  and production Candid remain unchanged.

## Patch 2 Evidence

- `HeldHeadKeyStream` separates `ensure_head`,
  `seek_head_at_or_after`, and `consume_head`; seeking returns a borrowed held
  key and therefore cannot consume or repull it accidentally.
- `RepeatedPullHeldHeadKeyStream` is the reference implementation. It makes no
  physical range jump and is not planner-visible, so successful production
  queries and physical routes remain unchanged.
- A finite pull-attempt envelope returns `PageStop` before starting the next
  indivisible pull unit. Exhaustion is distinct, while hard-budget and
  corruption failures return typed errors with no page.
- Checked counters cover pull attempts, comparisons, skipped occurrences, and
  total consumption. Failed accounting leaves a held key intact; partial work
  before an upstream failure remains charged.
- Ten focused tests cover concrete-envelope composition, ASC/DESC
  behind/equal/ahead targets, duplicates,
  page-stop resume with a changed envelope, held-head interruption, exact-once
  consumption, hard failure, entity/order corruption, overflow, and 128
  deterministic randomized sequence/direction cases against a suffix oracle.
- The frozen query fixtures retain identical index-entry, store-get, row, and
  peak-retention counters. Representative instruction movement is within
  `-0.0379%` to `+0.1092%`, below the 1% control-review threshold and with no
  claimed optimization.
- The production dynamic-query fixture remains exactly 2,479,797 raw final
  Wasm bytes and 973,166 deterministic gzip bytes (`0` byte delta). Candid and
  generated method modes are unchanged.
- Patch 2 spans seven implementation, test, contract, and documentation files
  at approximately `+910/-0` lines. Internal complexity grows by one concrete
  staged protocol and its oracle; the production execution shape remains
  unchanged until Patch 3 supplies a physical consumer.

## Patch 3 Evidence

- Primary and eligible primary-key-ordered index range leaves implement the
  held-head contract with direct raw-bound repositioning. Finite-window leaves
  retain repeated logical progress so a seek cannot bypass offset/limit work.
- Repositioning charges cursor work and checked bound bytes, distinguishes
  logical skips from physical seeks, and preserves the first qualifying key as
  an unconsumed head. Accepted index entries that are malformed or name a
  missing row remain typed corruption rather than exhaustion.
- The eager prefix-family candidate vector, sort, deduplication, and truncation
  route is removed. Construction now checks the canonical 128-child limit and
  descriptor/bound bytes before allocation; a bounded merge heap owns one head
  per concrete child and handles ASC/DESC duplicates deterministically.
- Focused heap-store tests cover ASC/DESC primary repositioning, exact
  primary-key-ordered index jumps, finite-window fallback, page stop before a
  store pull, maximum/max-plus-one fan-out, and malformed index propagation.
  The frozen PocketIC fixture also passes through the journaled backend with
  every expected row and counter shape unchanged.
- The final frozen `prefix_family_max_fanout` measurement is 6,437,409
  instructions and 68 index entries, versus 6,438,154 and 68 after Patch 2
  (`-0.0116%`). The old eager fallback was dormant for this 16-child fixture;
  the bounded heap improves merge complexity from a linear child-head scan per
  output to logarithmic child-count work but does not claim the line's 20%
  performance gate. Physical seek value remains staged for Patch 4's sparse
  intersection alignment.
- Sparse intersection controls remain planner-unchanged: two-child sparse is
  2,545,172 instructions with 21 index/store reads, while three-child DESC is
  7,970,504 with 120 index/store reads. Patch 4 must prove cursor progress and
  improve the required structural counters before enabling a new route.
- The production dynamic-query canister is 2,529,166 raw final Wasm bytes and
  991,865 deterministic gzip bytes. Against the Patch 2 baseline this is
  `+49,369` raw bytes (`+1.99%`) and `+18,699` gzip bytes; its 4,670-byte Candid
  and exported method modes remain unchanged.
- Patch 3 touches eight executor/contract files plus its status, design,
  inventory, and Unreleased evidence at approximately `+950/-160` lines.
  Internal complexity grows moderately through two physical seek state
  machines and a bounded heap, while the row-count-owned eager prefix path is
  deleted.

## Patch 4 Evidence

- The planner admits only two or three distinct exact accepted-index prefixes
  whose remaining suffix is the complete primary key and whose requested order
  is absent or primary-key-only. Exact compound access is formed first and
  remains preferred.
- Synchronized prefix cardinalities must prove every child and their checked
  sum fit the 256-entry atomic envelope. Missing, stale, overflowing, or
  over-cap authority keeps the planner-preferred first index and the complete
  residual predicate, so fallback cannot change results.
- The exact overlap probe retains only ordered primary keys. Its maximum vector
  slots are precharged, every encoded key backing is charged, and the total is
  capped by the persisted primary-key maximum. The retained keys become the
  execution stream, eliminating the initially measured duplicate child scan;
  no row, payload, or projected value crosses this boundary.
- Frozen PocketIC evidence moves two-child sparse execution from 2,545,172 to
  2,535,426 instructions (`-0.38%`) while row fetches fall from 21 to 1; its 41
  index entries replace the removed residual row reads. Three-child DESC moves
  from 7,970,504 to 5,394,711 instructions (`-32.32%`), with 161 index entries
  and one row fetch instead of 120 index entries plus 120 row fetches.
- The empty overlap completes at 2,474,603 instructions with 41 index entries
  and zero row fetches. The dense range route remains ineligible and unchanged
  at 2,048 index/row reads; the exact compound control still uses one index
  entry and zero row fetches. The two-child 20% line-wide instruction target
  remains open even though its payload-work target is satisfied.
- `EXPLAIN JSON` reports `planner_exact_index_intersection`, the complete
  residual predicate required by fallback, and distinct compound-index
  selection. Focused tests also cover worst-case dense costing and arithmetic
  overflow fail-closed behavior.
- The production dynamic-query canister is 2,533,360 raw final Wasm bytes and
  993,818 deterministic gzip bytes. Against Patch 3 this is `+4,194` raw bytes
  (`+0.166%`) and `+1,953` gzip bytes; its 4,670-byte Candid and exported method
  modes remain unchanged.
- Patch 4 touches planner, traversal, cardinality, diagnostics, focused tests,
  and design evidence at approximately `+500/-30` lines. Complexity grows
  moderately through one bounded runtime-costed access strategy; public and
  persisted formats remain unchanged.

## Patch 5 Evidence

- Cursor-emitting scalar projections now use `SlotsOnly` whenever the compiled
  retained layout owns the projection and order boundary. A 300 KiB unrelated
  payload is fetched and decoded at most once for the current row, then dropped;
  it no longer stays attached to every page candidate until cursor assembly.
- Model-identity, nested field-path, and outward entity-row consumers retain
  the full raw-row modes because their successful output still requires that
  payload. Focused mode tests pin both sides of this ownership decision.
- Ordinary non-`DISTINCT` projection now creates its final owned value row
  directly. The reusable borrowed `RowView` and subsequent deep `to_vec` clone
  are removed; `RowView` is an owned-only blocking transport left to Patch 7's
  `DISTINCT` contract.
- Hybrid covering projection replaces per-row component/field `BTreeMap`s with
  compact `(slot, value)` vectors and one query-shape ownership plan. Tests
  prove a 300 KiB final-use blob keeps the same allocation while repeated-use
  values clone only until their final owned use.
- A live-page test resumes a projection with hidden nullable/mixed ordering
  values through the compact slot payload. The materialization inventory now
  classifies ordinary candidate rows as output ownership and keeps only the
  owned `DISTINCT` transport assigned to Patch 7.
- Frozen PocketIC controls remain within the 1% review threshold: empty,
  two-child, three-child, dense, and compound intersection fixtures move by
  `+0.079%`, `+0.067%`, `-0.014%`, `+0.059%`, and `+0.157%` respectively, with
  their physical counters unchanged.
- The production dynamic-query canister contracts from 2,533,360 to 2,525,393
  raw final Wasm bytes (`-7,967`, `-0.315%`). Deterministic gzip is 994,998
  bytes (`+1,180`), while the 4,670-byte Candid and exported modes remain
  unchanged.
- Patch 5 changes eleven implementation/contract files at approximately
  `+252/-377` lines. The shape becomes simpler overall: three borrowed visitor
  families and two per-row maps are removed in favor of explicit owned output,
  compact current-row values, and one payload-mode decision.

## Patch 6 Evidence

- A scalar route whose access order already satisfies final order now stops the
  row scan immediately after residual filtering produces the requested matched
  window. An added early residual-match control reads one stored row and
  returns one row; the absent-match and existing last-row controls still read
  all 2,048 rows and therefore cannot hide incomplete exhaustion.
- One-shot execution keeps exactly `offset + limit` rows and performs no
  unneeded lookahead. Cursor-emitting execution keeps the one lookahead needed
  by the existing authenticated continuation, while resumed non-primary order
  remains uncapped because its progress is not physically reconstructible from
  the current cursor.
- Incompatible-order projection retains compact canonical order/tie/output
  slots. The fixed top-N fixture peaks at ten candidates and 4,000 complete
  backing bytes, below its smallest unrelated 40 KiB payload; the complete-sort
  control still retains all 2,048 candidates as required.
- Full-entity ordering now selects its bounded winners during the scan instead
  of collecting every raw row first. Only outputs that genuinely require a
  complete entity retain winner payloads, and diagnostics charge the complete
  retained allocation rather than the size of a borrowed order field.
- Ordering entries, comparisons, and complete backing bytes remain charged for
  every candidate before it crosses the blocking boundary. The implementation
  deliberately keeps immediate monotonic accounting so a later row-read error
  cannot discard a partially buffered accounting frame.
- At the Patch 6 handoff, the frozen `topn_wide_payload` cost moved from 81,990,155 to 81,855,556
  instructions (`-0.16%`) and its retained count moves from eleven to ten. This
  is an instruction and structural improvement, but it does not meet the fixed
  20% line gate; that gate remained explicitly open at the handoff rather than
  being weakened or moved to a friendlier fixture.
- Frozen 1% controls remain inside their review threshold: full sort moves by
  `+0.53%`, while point get, ordered empty scan, and dense full return move by
  `+0.20%`, `+0.23%`, and `+0.24%` respectively.
- Against the Patch 5 handoff, the production dynamic-query canister grows from
  2,525,393 to 2,526,613 raw final Wasm bytes (`+1,220`, `+0.048%`) while gzip
  shrinks by 195 bytes. Against published `0.222.0`, raw Wasm remains 6,747
  bytes smaller (`-0.266%`); its 4,670-byte Candid and exported modes are
  unchanged.
- Patch 6 adds the scalar cap, direct full-entity winner window, retained-byte
  attribution, and focused contracts across thirteen implementation/test
  files at approximately `+680/-250` lines over the Patch 5 handoff. Complexity
  grows moderately through explicit bounded ownership/accounting, while the
  unbounded raw-row collection route is removed.

## Patch 7 Evidence

- Projection DISTINCT now chooses between two explicit contracts. Direct
  projected slots use adjacent execution only when their complete equality
  tuple leads the resolved total order; hidden order fields before that tuple,
  expression projections, and other unproven shapes use complete global
  replay. Model identity remains adjacent-safe through primary-key uniqueness.
- Adjacent execution retains one canonical current-group key and publishes a
  representative only after its group is closed. Its continuation boundary is
  the final duplicate in that group, so the next group remains an unconsumed
  lookahead on resume.
- Global execution completes its bounded canonical set before output. Replay
  skips the authenticated prior-output count and retains projected rows only
  for the requested output window; later unique keys retain canonical key
  state but not unused source or projected rows.
- Focused operator tests cover nonadjacent duplicates across two replayed
  pages, adjacent closed-group resumption, hidden-order rejection, deterministic
  representatives, null/nested canonical keys, and typed retained-state budget
  exhaustion. The real SQL fixture selects both strategies over 2,048 rows.
- `distinct_nonadjacent_cross_page` completes at 89,638,376 instructions while
  retaining one canonical key at a time and 196 complete key/output backing
  bytes. The
  conservative global control completes at 118,401,465 instructions, discovers
  19 unique tuples, retains ten output rows plus the canonical set, and peaks at
  2,704 backing bytes. Neither route retains the fixture's unrelated payload.
- The independent closeout reran the same adjacent DISTINCT SQL against clean
  `v0.222.0` and the current worktree. Instructions moved from 100,490,316 to
  90,382,000 (`-10.06%`), so the missing clean-tag comparison is closed with a
  measured instruction improvement and the retained-state reduction.
- The production dynamic-query canister is 2,532,160 raw final Wasm bytes and
  997,233 deterministic gzip bytes. This is `+5,547` raw bytes from Patch 6,
  within the 8,192-byte patch allowance, and `-1,200` bytes from published
  `0.222.0`; Candid remains 4,670 bytes with unchanged exported modes.
- Patch 7 changes the projection coordinator, DISTINCT state owner, bounded
  metrics/attribution, focused fixtures, and contracts across roughly fifteen
  implementation/test files at approximately `+700/-150` lines. Internal
  complexity grows moderately through two explicit progress state machines,
  while full source-row retention and ambiguous early global completion are
  removed.

## Patch 8 Evidence

- Grouped execution now reports complete owned-state evidence. Hash and
  ordered groups charge canonical group-key backing, and grouped top-K charges
  the independently owned key, aggregate, and order-value backing that crosses
  its blocking boundary. The public diagnostics attribution adds
  `peak_estimated_state_bytes`.
- Ordered execution retains exactly one active group and publishes it only
  after a different key or source exhaustion proves it closed. After the
  closeout planner correction, the original frozen SQL selects the accepted
  `(group_key, id)` index and stops after ten closed groups: 1,329 rows
  scanned, one peak live group/state, 496 peak estimated state bytes, and
  61,322,708 instructions in the focused rerun.
- Hash execution completes a bounded build before emission. The later
  `LIMIT 10` semantic test scans all 2,048 rows, combines the three groups
  whose members occur far apart, retains three peak live groups/states, owns
  1,516 peak estimated state bytes, and completes in 69,425,663 instructions.
  It does not close the frozen fixture: Patch 1 fixed that SQL without a
  `LIMIT`, and current grouped admission rejects it because no explicit hard
  grouped limits can be derived.
- Scalar aggregate `DISTINCT` membership replaces a linear scan of every
  retained value with stable bounded hash buckets. Exact `Value` equality is
  still checked within collision buckets, so the change reduces expected
  admission work without changing duplicate semantics.
- The independent closeout adds real 10,001-row live and exhaustive canister
  traversal. Both modes return all rows exactly across ten calls and visit only
  one replayed lookahead at each page boundary; the earlier coordinator-only
  evidence is no longer used as production proof.
- The production dynamic-query canister is 2,532,107 raw final Wasm bytes and
  994,567 deterministic gzip bytes. Against published `0.222.1`, this is
  `-53` raw bytes and `-2,666` gzip bytes. Candid remains 4,670 bytes and all
  six exported method modes are unchanged.
- Patch 8 changes ten implementation/test/changelog files at approximately
  `+383/-33` lines before status/design evidence. Complexity grows moderately
  through explicit owned-byte accounting and collision buckets; grouped route
  ownership becomes more explicit, while scalar distinct admission becomes
  algorithmically simpler than repeated full-set scans.
- At the Patch 8 handoff, implementation of all eight planned outcomes was
  complete, but the 0.222 closeout was not accepted. The frozen 20% gates remained open for
  `prefix_family_max_fanout`, `intersection_2_sparse`, and
  `topn_wide_payload`. Patch 7's clean-tag instruction comparison is now
  reproduced and passes. The three-child sparse intersection gate passed with a 32.32%
  instruction reduction and its structural improvement. No open target is
  weakened or replaced by the grouped fixtures.

## Closeout Compatibility Correction

- Generated database access now calls one hidden ensure boundary. It reads
  `committed_allocations()` first, bootstraps only when the runtime is absent,
  and otherwise adopts without repeating IcyDB's default policy identity.
- Adoption seals the linked declaration snapshot and requires every declaration
  registered by the generated `icydb.<namespace>` authority to appear exactly
  in the committed allocation capability. A missing authority or declaration
  fails with the existing typed declaration-snapshot mismatch.
- Focused coverage bootstraps the shared runtime under a distinct upstream
  policy, proves IcyDB adoption preserves its generation, and proves that the
  removed direct default-policy rebootstrap would fail with a policy-identity
  mismatch. Generated-token coverage pins the new ensure call and proves that
  applications need no bootstrap hook or policy configuration.
- The compiler-emitted raw `canister_audit_default_empty` Wasm remains 104,093
  bytes before and after the correction (`0` byte delta). The branch executes
  only during generated database initialization, so steady-state query and
  mutation execution are unchanged; no query-performance claim is made.
- The correction changes six implementation/test files plus the changelog and
  this status at approximately `+260/-25` implementation/test lines. Internal
  bootstrap complexity grows slightly through explicit adoption and declaration
  validation, while the integration lifecycle becomes single-owner and removes
  the conflicting second bootstrap attempt. No persisted format, Candid, query
  semantics, or runtime policy identity changes.

## Independent Closeout Audit — 2026-08-08

The audit reconstructed `v0.221.9` (`c24ffe0186f6083e29f6be56b9b4e1341234c264`,
tree `c8411b8f5691e2c28a81972bb96c59a129b0e171`) as the exact pre-0.222
baseline and audited through published `v0.222.2`
(`03208689608691e107654be07853f52715a408d1`, tree
`36b855ff201b17aee3eefc285be83782a05a33a1`) plus the closeout worktree.
The audit's initial closeout decision was **NOT ACCEPTED**.

### Confirmed corrections

- Grouped order matching now proves that a grouped key may be a leading index
  sequence with trailing tie-break fields. The original ordered SQL therefore
  selects `(group_key, id)` without adding a filtering predicate.
- The two Patch 1 grouped declarations are restored verbatim and pinned by a
  focused contract test so later evidence cannot silently substitute different
  SQL or page-window values.
- The Patch 7 clean-tag comparison is reproduced successfully.
- The frozen continuation fixture now loads 10,001 real rows and traverses
  exact IDs `1..=10,001` through ten separate live calls and ten separate
  revision-proof-bearing exhaustive calls. Each mode visits 10,010 entries:
  every source row plus exactly one replay of the unconsumed lookahead at each
  of nine page boundaries. This evidence first exposed and then corrected false
  exhaustion after row 1,025: authenticated primary-key progress is now applied
  to the raw primary range before the page-local fetch hint consumes its
  allowance.

### Remaining blocking finding

1. Production cursor-emitting scalar execution applies the finite
   key/index-entry envelope to routes whose primary-key ordering makes one
   physical frontier reconstructible. The existing authenticated token carries
   the last fully examined raw data key separately from the last emitted
   logical row; ASC and DESC selective scans can therefore return empty pages
   with strictly advancing continuations. Closeout Correction B2 additionally
   preflights every proven scalar candidate inspection before storage polling,
   constrains page-aware physical leaf refills to one entry, and commits only
   the exact hard-budget delta after the unit completes. Cumulative row,
   stored/decoded/materialized byte, nested-value, predicate, cursor, and
   temporary work can therefore stop successfully without consuming the next
   candidate. A stream shape without a finite per-pull proof fails closed
   instead of silently bypassing the envelope. Projection/output result-byte
   admission and work completed during access-route construction remain outside
   this correction and must not be claimed as closed by the scan receipt.
### Closeout Correction B2 evidence

- The production scalar row loops now open an indivisible page unit before
  `next_key`, row fetch, decode, and residual evaluation. Page admission uses a
  maintained worst-case reservation; completion commits the exact monotonic
  hard-budget counter delta and releases unused reservation.
- Primary and accepted index leaves refill one physical entry while that unit
  is active. Concatenated and merged prefix families publish the checked sum of
  their child bounds. Routes without a finite proof cannot execute beneath the
  production page owner.
- Focused tests cover cumulative stored/decoded/materialized row bytes,
  cumulative predicate work, a route unit too large for an empty envelope,
  hard-budget failure with no successful page, and the existing ASC/DESC empty
  selective-page resume path.
- This correction intentionally does not treat post-scan projection/result
  bytes as admitted scan work. That remaining page-output boundary stays open
  in the closeout findings rather than being hidden behind the candidate cap.
- The maintained dynamic-query fixture is 2,550,768 raw final Wasm bytes,
  `+14,050` from B1 and `+27,689` from clean `v0.221.9`; Candid remains 4,670
  bytes with the same exports. B2 is approximately `+520/-20` Rust lines
  across eleven implementation/test files. The execution shape becomes
  moderately more complex because page admission now has a production framed
  owner instead of only a candidate-count proxy.

### Closeout Correction C evidence

- Missing authoritative rows reached through accepted secondary indexes now
  return typed store corruption. The policy is derived recursively from the
  accepted semantic access tree and is consumed by scalar, pure-covering, and
  hybrid-covering execution; covering-only reads still perform the required
  row-presence check.
- A mixed union preserves ordinary exact-primary semantics. Bounded exact-key
  siblings remove absent candidates before merging with an accepted-index
  stream, so `id = missing OR indexed_field = other` remains an empty success
  while a stale accepted-index key cannot be silently skipped.
- The residual secondary-index predicate fallback remains only because its
  predicate program cannot yet run inside the borrowed physical index leaf.
  Its complete vector capacity and encoded-key backing, vector growth, sort,
  and deduplication work are now charged; the machine-readable materialization
  inventory classifies it as bounded blocking state. Budget exhaustion remains
  typed.
- Focused dynamic and SQL tests remove an authoritative row while preserving
  its accepted index entry, proving typed corruption for ordinary and covering
  reads. Recursive composite-plan, mixed-union, temporary-byte exhaustion, and
  inventory-source tests pass, as do warnings-denied core lint and focused
  executor invariants.
- The maintained dynamic-query fixture is 2,555,570 raw final Wasm bytes,
  `+4,802` from B2 and `+32,491` from clean `v0.221.9`; deterministic gzip is
  1,004,693 bytes. Candid remains byte-identical at 4,670 bytes with the same
  six exports. Correction C adds approximately 300 Rust/test lines across nine
  source/test files. Complexity grows moderately through explicit composite
  provenance handling and complete fallback accounting; no persisted or cursor
  format changes.

### Closeout Correction D evidence

- Projection-global `DISTINCT`, scalar aggregate `DISTINCT`, grouped aggregate
  distinct sets, and grouped hash side indexes now charge structural capacity
  as well as canonical values. Stable hash entries reserve four physical slots
  per logical entry, including control/alignment allowance; collision and
  output vectors use the same conservative four-slot envelope.
- Reservations are charged before fallible `try_reserve` calls. A pinned
  1,024-entry oracle proves actual standard-library hash/vector capacity stays
  within the maintained envelope. Typed hard-budget tests prove that fitting
  canonical values without their hash capacity no longer succeeds.
- Grouped hash tables no longer preallocate from source-row cardinality hints.
  They grow only after grouped policy and request-wide hard-budget admission;
  the retired sizing helper and its two pass-through candidate-hint adapters
  are removed. Exact collision equality remains unchanged.
- Real PocketIC execution preserves all `DISTINCT` and grouped results.
  Adjacent/global projection peaks are now 292/10,176 complete bytes. Ordered
  grouping remains 496 bytes, while complete hash grouping reports 5,740 bytes
  instead of the previous partial 1,516-byte estimate.
- Adjacent `DISTINCT` completes in 92,231,605 instructions, still 8.22% below
  clean `v0.222.0`; global `DISTINCT` is 121,201,136 instructions (`+2.36%`
  from its partial-accounting handoff). Ordered/hash grouping move by `+0.071%`
  and `+0.009%`, respectively.
- The maintained dynamic-query fixture is 2,556,526 raw final Wasm bytes,
  `+956` from Correction C and `+33,447` from clean `v0.221.9`; deterministic
  gzip is 1,004,992 bytes. Candid remains byte-identical at 4,670 bytes with
  the same six exports. Correction D is approximately `+530/-171` Rust/test
  lines across seventeen files. Complexity grows moderately through one shared
  reservation authority, while eager grouped preallocation and three obsolete
  sizing/adaptation paths are removed.

### Closeout Correction E evidence

- The original frozen `group_hash_noncontiguous` SQL now executes verbatim,
  without a substituted `LIMIT`. SQL lowering attaches one shared
  planner/executor ceiling of 10,000 groups and 16 MiB of completely accounted
  grouped state; exceeding either ceiling is a typed failure, never truncation.
- A no-`LIMIT` canonical group-key order is admitted only when both grouped
  ceilings are finite and non-zero. Aggregate-driven grouped top-K ordering
  without `LIMIT` remains a typed query-plan rejection, and public dynamic
  grouping still requires caller-supplied `grouped_limits`.
- The exact frozen hash query returns all three noncontiguous groups after
  scanning 2,048 rows in 69,418,993 instructions, with a peak of three groups
  and 5,740 completely accounted retained bytes. The corrected ordered fixture
  remains complete at 61,299,586 instructions, 1,329 rows scanned, one peak
  group, and 496 retained bytes.
- An operator-local grouped byte ceiling now returns
  `ExecutionBudgetExceeded` with resource, limit, observed usage, request
  scope, execution lane, and normalized query-shape facts instead of generic
  `Internal(24)`.
- The maintained dynamic-query fixture is 2,556,563 raw final Wasm bytes,
  `+37` from Correction D and `+33,484` from clean `v0.221.9`; deterministic
  gzip is 1,005,012 bytes. Candid remains byte-identical at 4,670 bytes with
  the same six exports. Correction E is approximately `+210/-20`
  implementation/test lines across nine files. Complexity grows slightly for
  explicit SQL admission and typed local-limit failures, while the duplicated
  executor default constants are removed.

### Closeout Correction F evidence

- Maximum-fan-out prefix reads now merge the accepted disjoint ranges directly
  inside one physical index backing. The merge retains one lazy head per child,
  selects winners through a bounded tournament tree, and decodes an index value
  only when that head is consumed. Mixed journal overlays retain the ordinary
  charged stream route. Exact synchronized prefix cardinality permits the
  covering route to omit redundant authoritative-row probes; other routes keep
  the integrity check.
- Cursorless exact-prefix intersections whose complete preflight is at most 256
  entries use one atomic structural probe. Every raw key and row witness is
  validated, non-surviving children retain only compact primary-key values,
  and only overlap keys become complete data keys. Cursor-bearing execution
  retains the proved held-head route, while dense or uncertain shapes retain
  the conservative single-index fallback.
- Blocking primary-key Top-N and full-sort routes can keep one physical range
  iterator open across the bounded scan. Candidate work is admitted from the
  key before a stable payload is materialized; each qualifying row is borrowed
  for structural decoding and only compact owned winner/output state crosses
  the blocking boundary. The maintained Top-N fixture therefore performs no
  duplicate point reads while still evaluating all 2,048 candidates exactly.
- The final frozen run returns the exact expected rows and records
  `prefix_family_max_fanout` at 5,130,154 instructions and 65 index entries,
  `intersection_2_sparse` at 2,023,313 instructions, 41 entries, and one row
  read, `intersection_3_sparse_desc` at 3,578,748 instructions and one row read,
  and `topn_wide_payload` at 50,999,430 instructions, zero point reads, ten
  retained candidates, and 4,000 retained backing bytes. All four frozen
  instruction and structural gates pass.
- `full_sort_control` improves 15.21% and `dense_full_return_control` moves
  +0.25%. The point-get and ordered-empty controls move +1.50% and +1.73%, or
  only 11,331 and 13,609 instructions. Their 1% lines are explicitly review
  thresholds rather than automatic failures; the small movement is accepted
  because the routes retain the mandatory page-unit and index-integrity work,
  their physical counters do not regress, and no semantic or fixture change
  was made. Patch 7 adjacent `DISTINCT` remains 8.10% below its clean-tag
  baseline.
- The maintained dynamic-query fixture is 2,596,121 raw final Wasm bytes,
  `+39,558` from Correction E and `+73,042` (`+2.895%`) from clean `v0.221.9`;
  deterministic gzip is 1,016,845 bytes. Candid remains byte-identical at
  4,670 bytes with the same five query methods plus the metadata pointer.
  Correction F is approximately `+1,000/-250` implementation/test lines across
  fifteen files. Complexity grows moderately through three bounded specialized
  routes, while duplicate row lookups and eager prefix/intersection ownership
  are removed.

### Closeout Correction G evidence

- Production scalar output now continues the exact page-work tracker recorded
  by physical scanning. Projection admits one completed row at a time, charges
  result rows, nested traversal, materialized backing, and result bytes before
  the row crosses the executor response boundary, and retains transient
  materialization charges when a row is not returned.
- Candidate inspection reserves enough overlapping nested/materialized
  headroom for one maximum bounded output row. A page therefore cannot consume
  all shared work before its first matching row is emitted. Cumulative output
  exhaustion returns the last admitted logical boundary and deliberately does
  not anchor past the unreturned match; a row that cannot fit an empty output
  envelope returns typed `PageWorkUnitTooLarge`.
- Non-`DISTINCT`, ordered-adjacent `DISTINCT`, and complete-global `DISTINCT`
  all use the same output authority. A focused production test with a 32-byte
  result envelope returns three ordered rows across three authenticated calls
  without duplication or omission, and focused tests cover combined scan and
  output receipts, discarded-row hard charges, oversized first units, and
  strategy-specific `DISTINCT` boundaries.
- The frozen fixture rerun remains passing: `prefix_family_max_fanout` is
  5,131,634 instructions, `intersection_2_sparse` is 2,023,573,
  `intersection_3_sparse_desc` is 3,577,263, `topn_wide_payload` is 51,146,893,
  and Patch 7 adjacent `DISTINCT` is 92,270,598. Structural counters and exact
  result oracles remain unchanged.
- The maintained dynamic-query fixture is 2,601,650 raw final Wasm bytes,
  `+5,529` (`+0.213%`) from Correction F and `+78,571` (`+3.114%`) from clean
  `v0.221.9`; deterministic gzip is 1,018,592 bytes. Candid remains
  byte-identical at 4,670 bytes with the same five query methods plus the
  metadata pointer. Correction G is approximately `+670/-35` lines across ten
  code/test files and adds one explicit emission owner while removing the
  previous whole-page output charge, so executor complexity grows moderately
  but page progress and accounting now share one authority.

### Closeout Correction H evidence

- The direct covering prefix-family route validates its active child count
  against the planner-owned 128-child ceiling before building its bound vector.
  It precharges the vector slots, cloned raw bound backing, and construction
  cursor steps before fallible reservation.
- The index merge now receives lazy concrete range construction plus an
  executor-owned admission callback. Before reserving any range, held-head, or
  tournament-tree vector it computes and charges all inline slots, the
  power-of-two winner tree, and the second set of bound-key clones retained by
  the concrete range iterators. Rejected admission reads and decodes no entry
  and becomes typed `ExecutionBudgetExceeded` on `TemporaryBytes`.
- Focused ASC/DESC and early-rejection tests pass, including max-plus-one
  fan-out rejection and typed temporary-byte exhaustion. The final frozen run
  keeps exact results and records `prefix_family_max_fanout` at 5,142,082
  instructions, 65 index entries, and zero row reads, still 20.13% below its
  immutable baseline and below the 5,150,250 ceiling.
- The maintained dynamic-query fixture is 2,605,257 raw final Wasm bytes,
  `+3,607` (`+0.139%`) from Correction G and `+82,178` (`+3.257%`) from clean
  `v0.221.9`; deterministic gzip is 1,020,267 bytes. Candid remains
  byte-identical at 4,670 bytes with the same six exports. Correction H is
  approximately `+220/-20` implementation/test lines across four files. It
  adds one explicit structural-admission contract while removing eager range
  collection from each backing branch, so executor complexity grows slightly
  and allocation ownership becomes centralized.

### Closeout Correction I evidence

- The active scalar page now exposes its immutable finite
  `KeyIndexEntriesVisited` ceiling while the physical stream is constructed.
  Primary-range leaves freeze their refill size to the smaller of that ceiling
  and the normal 64-entry chunk, preserving the existing production chunk when
  the maintained 10,000-entry envelope is active.
- A zero-entry envelope still assigns one framed physical entry, so attempting
  work returns typed `PageWorkUnitTooLarge` rather than falsely reporting source
  exhaustion. The chosen bound remains the bound preflighted by the page unit,
  and the physical leaf cannot pull beyond it.
- The formerly failing selective live-page fixture passes in ASC and DESC. Its
  first two calls each examine four filtered rows and return distinct
  authenticated continuations; the third call returns the late match and proves
  exhaustion. The frozen live and exhaustive 10,001-row canister traversals
  also remain exact across ten separate page calls.
- The final frozen one-shot run remains passing:
  `prefix_family_max_fanout` is 5,141,281 instructions,
  `intersection_2_sparse` is 2,020,956,
  `intersection_3_sparse_desc` is 3,575,835, and `topn_wide_payload` is
  51,177,123. Their structural counters and exact result oracles are unchanged.
- The maintained dynamic-query fixture is 2,605,972 raw final Wasm bytes,
  `+715` (`+0.027%`) from Correction H and `+82,893` (`+3.285%`) from clean
  `v0.221.9`; deterministic gzip is 1,020,445 bytes. Candid remains
  byte-identical at 4,670 bytes with the same six exports. Correction I is
  approximately `+80/-5` implementation/test lines across six files and adds
  one frozen refill field plus one page-limit accessor, so complexity grows
  slightly without introducing another execution route.

### Closeout Correction K evidence

- Unsafe covering sibling-prefix order no longer owns an eager fallback. When
  multiple active covering prefixes cannot prove ordered concatenation or
  ordered merge, covering execution declines the route and the maintained
  bounded scalar executor preserves the query result. The complete-vector
  scan with `usize::MAX`, sort, deduplication, and truncation is deleted.
- Prefix-set classification names this outcome as fallback rather than
  materialization. Single-prefix, proven ordered-concatenation, and proven
  ordered-merge covering routes remain unchanged; the existing 50-row hybrid
  audit fixture still returns the same rows with 50 late row fetches because
  its liveness proof leaves one broad prefix rather than the retired unsafe
  sibling set.
- Generic ordered-key merge construction precharges its complete child-slot
  and heap-slot topology before fallible reservation. Pair merges precharge
  their boxed children, access-plan unions precharge their source stream slots,
  and each heap head retains only the allocation-free entity/primary-key
  identity instead of cloning a decoded key and its raw backing.
- Focused tests prove typed temporary-byte rejection before flat-merge
  construction, compact ASC/DESC heap result equivalence, and unchanged
  real-canister query results. Public Candid, persisted data, cursor encoding,
  planner semantics, and successful query results are unchanged.
- The frozen one-shot matrix passes. `prefix_family_max_fanout` completes at
  5,142,805 instructions and 65 index entries, below its immutable 5,150,250
  ceiling; `intersection_2_sparse` completes at 2,025,095, and
  `topn_wide_payload` at 51,455,473. The maintained 50-row hybrid audit remains
  6,891,023 total instructions with 50 row reads and 256 index entries.
- The maintained dynamic-query fixture is 2,604,191 raw final Wasm bytes and
  1,020,137 deterministic gzip bytes, 2,156 raw bytes smaller than published
  `0.222.3`; Candid remains 4,670 bytes with the same six exports. Correction K
  changes fourteen code/test/doc files at approximately `+270/-229` lines.
  The production shape becomes simpler overall: one unbounded fallback and its
  dedicated materialized-window helper are removed, while maintained merge
  construction gains explicit fallible admission.

### Closeout Correction L evidence

- The cursorless direct intersection now exhausts every exact-prefix child
  instead of stopping when the synchronized cardinality count has merely been
  reached. Every physical entry must decode to a `Present` row witness, and
  missing witnesses, extra physical entries, short scans, malformed keys, and
  malformed witnesses return typed store corruption. The synchronized current
  data-generation stamp remains the row-authority proof for discarded keys;
  surviving overlap keys retain the normal authoritative late-row check.
- Child-key slots, every pairwise-overlap allocation, final decoded-key slots,
  and the conservative comparison upper bound are computed with checked
  arithmetic and precharged once before the first allocation. Comparison work
  is charged as intersection cursor coordination rather than blocking-sort
  work. The two/three-child limit and 256-entry atomic envelope are unchanged.
- Focused tests cover ASC/DESC result equivalence, stale row witnesses,
  uncounted physical entries, pre-allocation temporary-byte rejection, and the
  unchanged conservative cost gate. Public Candid, persisted data, cursor
  encoding, planner eligibility, and successful query results are unchanged.
- The unchanged frozen matrix passes: `prefix_family_max_fanout` completes at
  5,143,521 instructions and 65 entries; `intersection_2_sparse` completes at
  2,041,718 instructions, 41 entries, and one row read, 778 instructions below
  its immutable 2,042,496 ceiling; `intersection_3_sparse_desc` completes at
  3,642,752; and `topn_wide_payload` completes at 51,161,016.
- The maintained dynamic-query fixture is 2,606,080 raw final Wasm bytes and
  1,020,869 deterministic gzip bytes, 1,889 raw bytes larger than Correction K
  but 267 bytes smaller than published `0.222.3`. Candid remains 4,670 bytes
  with the same six exports. The combined worktree through Correction L is
  fifteen files at approximately `+730/-280` lines; relative to Correction K,
  L adds roughly 460 lines, predominantly focused fixtures. Runtime complexity
  grows slightly through one explicit generation/witness validation boundary
  while the direct route count, public surface, and persisted formats stay
  unchanged.

### Closeout Correction M evidence

- Scalar continuations now authenticate the stable public or trusted
  page-envelope profile instead of the exact operational limits used by the
  preceding call. Exact per-call envelope identities remain in page work
  receipts, so diagnostics can still distinguish changed limits.
- The original public and trusted profile values are frozen from their v1
  operational-envelope identities. The authenticated token layout, public
  Candid shape, and ordinary unchanged-envelope cursor bytes therefore remain
  unchanged; only test-tuned or future limit changes stop invalidating
  otherwise valid progress.
- Focused coordinator evidence accepts changed finite operational limits while
  rejecting changed semantic offset, total `LIMIT`, or lane profile. The
  dynamic live-page fixture resumes across 32-, 64-, and 48-byte output
  envelopes, returns each row exactly once, and uses a final empty page to
  prove exhaustion after an exact-full page. The existing authenticated hidden
  order/mixed-direction fixture still rejects a changed total query `LIMIT`.
- This correction changes six core source files at `+109/-44` lines plus
  direct docs. Runtime shape remains neutral: one stable
  profile identity is carried beside the existing exact receipt identity; no
  route, row owner, public field, persisted format, or result semantics change.
- The matching six-export dynamic-query fixture is 2,606,099 raw final Wasm
  bytes and 1,020,901 deterministic gzip bytes, respectively `+19` and `+32`
  bytes from Correction L. Candid remains byte-identical at 4,670 bytes with
  the same six exports. No physical query route or structural counter changes,
  so the frozen instruction gates remain Correction L evidence rather than
  being relabelled as new performance results.

### Closeout Correction N evidence

- The real accepted-schema session/page boundary now exercises projection
  DISTINCT through an internal test-only intent bit. It uses the production
  planner, ordered scalar execution, projection materializer, authenticated
  cursor codec, completed-output offset, and page response rather than calling
  the DISTINCT accumulator directly. No public `DynamicQuery` builder or SQL
  grammar is added.
- Seven relation-backed rows contain null and non-null duplicate projection
  groups. Leading `(parent_id, id)` order proves ordered-adjacent execution;
  primary-key order makes the same projection require complete-global replay.
  With the two-row test page, both strategies terminate in two authenticated
  calls and return exactly `NULL, 1, 2, 3` once each. Every nonterminal cursor
  is unique, page row/work counts agree, and both routes report physical work.
- Existing focused operator proofs still pin the adjacent boundary to the last
  duplicate in a closed group and rebuild the complete global set before
  skipping prior output. Together with the session traversal, duplicate groups
  on both sides of a page boundary cannot be forgotten or emitted twice.
- The correction is test-only at runtime: three Core files add approximately
  `+98` lines, with no production branch, Wasm, Candid, cursor, persisted
  format, or instruction change. Patch 7's reproduced `-8.18%` adjacent
  instruction result and retained-state counters remain the performance
  authority.

### Closeout Correction O evidence

- Ordered grouping now crosses the production accepted-schema session,
  planner, physical traversal, aggregate fold, grouped response, authenticated
  cursor codec, and resume boundary in one focused test. The source contains a
  70-row first group, exceeding the maintained 64-entry physical refill,
  followed by groups of three and one rows.
- A generic `COUNT` plus `SUM` reducer proves the first group is not emitted at
  a refill boundary: its exact result is `(10, 70, 2485)`. Two later calls
  return `(20, 3, 216)` and `(30, 1, 74)` exactly once. Both nonterminal
  cursors are present and unique, and the final page proves exhaustion.
- The frozen 2,048-row `group_ordered_mid_group` fixture remains unchanged and
  continues to prove the exact declared SQL, its ten closed first-page groups,
  single-live-group ownership, and early scan stop. Focused ordered-fold tests
  separately retain transition, stop, monotonic-key, backing-byte, and
  oversized-key coverage.
- Correction O is test and documentation only. It adds no production branch,
  allocation owner, public API, Candid export, cursor/persisted format, Wasm,
  or instruction change; Patch 8's frozen measurements remain the performance
  authority.

### Closeout Correction P evidence

- The final redundancy audit found that composite unions admitted their child
  stream slots, but generic and fallback intersections allocated an equivalent
  vector and pairwise boxed reduction topology through separate uncharged
  flows. All composite child vectors now use one reservation helper, and
  intersection precharges every box plus every reduction-round slot before the
  first topology allocation.
- Exact-intersection preflight cardinalities now use a fixed three-slot record,
  matching the maintained two/three-child limit without a heap allocation.
  Ordered concatenation retains its incoming admitted vector instead of
  decomposing and rebuilding the two-child case. The now-single-use generic
  pairwise reduction helper is removed.
- A zero temporary-byte budget rejects intersection construction before the
  topology exists. Pairwise result equivalence, direct accepted-index
  integrity, ASC/DESC ordering, frozen real-canister results, and all four
  instruction gates remain passing.
- The final dynamic-query fixture is 2,607,510 raw Wasm bytes and 1,021,616
  deterministic gzip bytes, respectively 1,163 and 968 bytes above published
  `0.222.3`. Candid remains byte-identical at 4,670 bytes with six exports.
  Correction P leaves the physical routes and successful results unchanged
  while making generic composite accounting complete.

### Reproduced performance gates

| Gate | `v0.221.9` | Current worktree | Delta | Verdict |
| --- | ---: | ---: | ---: | --- |
| `prefix_family_max_fanout` | 6,437,813 | 5,143,209 | -20.11% | Pass |
| `intersection_2_sparse` | 2,553,120 | 2,040,937 | -20.06% | Pass |
| `intersection_3_sparse_desc` | 8,021,691 | 3,641,987 | -54.60% | Pass |
| `topn_wide_payload` | 81,990,155 | 51,161,071 | -37.60% | Pass |
| Patch 7 adjacent DISTINCT (`v0.222.0` baseline) | 100,490,316 | 92,270,598 | -8.18% | Pass |

The prefix route improves its structural counter from 68 to 65 index entries.
The sparse two- and three-child intersections improve from 21 and 120 row
reads to one, respectively. Top-N removes all 2,048 duplicate point lookups and
retains only its ten compact winners. These physical changes accompany, rather
than substitute for, the passing instruction results.

### Binary and interface evidence

- A clean `v0.221.9` dynamic-query canister is 2,523,079 raw final Wasm bytes.
  The closeout worktree through Correction J is 2,606,347 bytes (`+83,268`,
  `+3.300%`). Correction A added 1,492 raw bytes relative to the audited
  pre-correction worktree; Correction B1's physical-progress wiring reached
  2,536,718 bytes, and B2's candidate-unit admission adds another 14,050 raw
  bytes. Corrections C, D, E, and F add 4,802, 956, 37, and 39,558 raw bytes,
  respectively; Correction G adds 5,529 raw bytes, Correction H adds 3,607,
  Correction I adds 715, and Correction J adds 375.
- Both builds export byte-identical 4,670-byte Candid with the same five query
  methods plus the Candid metadata pointer. No cursor/persisted-format source
  changed in the 0.222 diff.
- Correction L measures 2,606,080 raw bytes (`+83,001`, `+3.289%`) from clean
  `v0.221.9`, 1,020,869 deterministic gzip bytes, and the same 4,670-byte
  Candid/six-export surface. Raw Wasm remains the footprint authority.
- The generated-access compatibility correction passes its cold-bootstrap and
  foreign-policy adoption tests and validates every declaration registered by
  the generated authority against the adopted committed capability.

### Outcome verdicts

| Outcome | Verdict |
| --- | --- |
| 1. Execution authority and fixed baseline | Pass: >10k evidence passes and Correction K removes the unclassified covering materialization owner |
| 2. Held-head seek protocol | Pass: state machine/oracle, primary resume, scan/output admission, and page-aware refill progress pass |
| 3. Physical seeks and prefix merge | Pass: frozen gate passes; Correction K removes eager covering fallback and precharges generic merge topology/backing |
| 4. Intersection and costing | Pass: sparse gates/results pass; direct probes exhaust synchronized child prefixes, while direct and generic intersection topology is fully precharged |
| 5. Late row loading and ownership | Pass: accepted-index corruption is typed for ordinary, covering, and direct-intersection routes without eager row payload reads |
| 6. Scalar termination and top-N | Pass: progress and Top-N gates pass; continuations bind stable lane profiles while changed operational envelopes preserve results and monotonic progress |
| 7. DISTINCT routes | Pass: ordered-adjacent and complete-global strategies resume through authenticated session pages with duplicate groups across boundaries; unit ownership and clean-tag performance also pass |
| 8. Grouped routes and compatibility | Pass: frozen ordered/hash results and ownership pass; a refill-spanning ordered group resumes through authenticated production session pages without splitting, skipping, or duplication; generated bootstrap adoption also passes |

## Landing Plan

| Patch | Outcome | Status |
| --- | --- | --- |
| 1. Execution authority and fixed baseline | Publish the classified inventory/flags, formal progress matrices, bounded counters, and named fixtures | Complete inventory and >10k live/exhaustive evidence pass |
| 2. Held-head seek protocol | Implement and prove positioning/consumption semantics with a repeated-pull reference adapter | Complete: protocol, cross-call resume, candidate/output admission, and page-aware refill progress pass |
| 3. Physical seek leaves and bounded prefix merge | Add charged range jumps and remove eligible primary/index/prefix convenience vectors | Complete: direct merge, pre-allocation structural charging, and frozen gate pass |
| 4. Proven resumable intersection and costing | Enable sparse intersection only after current-cursor progress proof and conservative costing pass | Complete: atomic sparse route, conservative fallback, and frozen gates pass |
| 5. Late row loading and retained-value ownership | Keep row values borrowed only during current-row evaluation and own/charge blocking state correctly | Complete: decode-once, accepted-index corruption, and blocking backing pass |
| 6. Early scalar termination and minimal top-N | Stop at the earliest legal limit/lookahead boundary and retain compact owned ordering state | Complete: Top-N gate plus candidate and output admission pass |
| 7. Resumable distinct routes | Separate ordered-adjacent distinct from complete bounded global distinct | Complete semantics, performance, and hash/collision/output backing evidence |
| 8. Resumable grouped routes and closeout | Separate closed ordered groups from complete hash builds and close performance/Wasm/complexity evidence | Published (`0.222.2`); complete backing and exact frozen hash fixture pass |

## Current Authorization

Patch 8 and the compatibility correction are published in `v0.222.2`. The
independent audit found contract gaps that invalidate an accepted closeout and
made one bounded grouped-index planner correction in the worktree. Closeout
Correction A additionally closes the frozen 10,001-row evidence gap and the
false primary-range exhaustion it discovered. Corrections B1 and B2 now publish
monotonic physical progress when a primary-ordered selective page reaches its
finite candidate envelope, including empty ASC and DESC pages, and preflight
bounded scalar candidate inspection before storage work without changing the
cursor wire shape. Correction C types missing accepted-index rows as store
corruption without changing ordinary missing point lookups and restores the
residual materialized-key fallback to the complete charged inventory. The
Correction D precharges complete hash-table, collision-vector, and retained
output capacity for blocking DISTINCT/grouped owners and removes eager grouped
cardinality preallocation. Correction E restores the exact frozen no-`LIMIT`
hash query under finite planner-owned grouped ceilings without weakening public
dynamic admission. Correction F closes every frozen instruction gate with its
required structural improvement, and Patch 7's clean-tag comparison remains
passing. Correction G carries the physical receipt into row-at-a-time output
admission and preserves resumable logical progress when result bytes fill the
page. Correction H closes the direct covering prefix merge's structural
accounting gap by precharging bound copies, concrete ranges, held heads, and
the winner tree before reservation while preserving its frozen performance
gate. Correction I closes the selective-primary failure by freezing each
primary-range leaf's refill bound to the smaller of 64 entries and the active
finite page-entry ceiling. Correction J applies the same page authority to
secondary-index leaves and divides the available refill entries across active
prefix-family children, preventing a valid small-envelope index page from
preflighting the default 64-entry pull as one oversized unit.
Correction K removes the remaining eager covering sibling-prefix fallback,
admits generic merge topology before allocation, and replaces cloned heap-head
backing with compact key identities.
Correction L exhausts each generation-synchronized direct-intersection child,
validates every entry witness and physical count, and precharges its complete
key/comparison topology before allocation.
Correction M separates the stable cursor-authenticated envelope profile from
the exact per-call envelope receipt, so finite operational limits may change
on resume without weakening semantic window or lane validation.
Correction N drives ordered-adjacent and complete-global DISTINCT through real
authenticated session pages and proves duplicate groups are neither forgotten
nor repeated across the output boundary without adding public query grammar.
Correction O drives a generic refill-spanning ordered group through the same
accepted-schema grouped session and authenticated cursor boundary, proving
exact closed aggregates and monotonic multi-page resume.
Correction P consolidates composite child-slot admission, precharges generic
intersection boxes and reduction rounds, replaces exact preflight allocation
with fixed bounded state, and removes the redundant pairwise concat rebuild.
The selective ASC and DESC fixtures now publish authenticated monotonic
progress, the normal 10,000-entry envelope retains 64-entry refills, and a
zero-entry envelope still fails with typed `PageWorkUnitTooLarge`.
All Corrections A-P are complete in the worktree. The 0.222 closeout is
**ACCEPTED**. Starting another minor still requires explicit user
authorization naming that minor.

## Closeout Correction Plan

| Correction | Bounded outcome | Status |
| --- | --- | --- |
| A. Cross-call primary progress | Load and traverse the frozen 10,001-row live/exhaustive fixtures; bind primary continuation before page fetch limits | Complete in worktree |
| B1. Production candidate envelope | Bound primary-ordered cursor pages by examined candidates and authenticate monotonic empty-page progress | Complete in worktree for ASC and DESC |
| B2. Scalar candidate-unit admission | Move cumulative row/byte/expression preflight and indivisible-unit failure into the production scalar loop | Complete in worktree for proven primary/index/merge pulls; unproven pulls fail closed |
| C. Accepted-index integrity and inventory | Type missing indexed rows as corruption and remove or fully classify the residual materialized key-vector path | Complete in worktree |
| D. Blocking backing ownership | Charge complete DISTINCT/group hash-table, collision bucket, and retained backing capacity | Complete in worktree |
| E. Frozen grouped authority | Resolve the original no-limit hash fixture against bounded admission without changing the fixture | Complete in worktree |
| F. Frozen instruction gates | Pass prefix-family, two-child intersection, and top-N targets with both structural and instruction improvements | Complete in worktree |
| G. Production output admission | Continue the scan receipt through row-at-a-time projection/result admission and preserve the last returned boundary | Complete in worktree for scalar and ordered/global DISTINCT pages |
| H. Direct prefix structural accounting | Precharge direct covering bounds, range iterators, held heads, and tournament-tree backing before allocation | Complete in worktree |
| I. Page-aware primary refill | Reduce the primary-range physical pull to the active finite envelope while preserving complete charging and monotonic empty-page progress | Complete in worktree |
| J. Page-aware index refill | Bound each secondary-index leaf pull by its share of the active page-entry envelope without reducing the normal 64-entry refill | Complete in worktree |
| K. Covering fallback and merge admission | Remove unsafe covering-prefix materialization, precharge generic merge topology, and retain compact allocation-free head identities | Complete in worktree |
| L. Direct intersection integrity and accounting | Exhaust synchronized child prefixes, type witness/count disagreement as corruption, and precharge all key/comparison state | Complete in worktree |
| M. Envelope-independent scalar resume | Authenticate stable envelope profiles while retaining exact per-call receipts and rejecting changed semantic windows | Complete in worktree |
| N. Authenticated DISTINCT resume | Traverse ordered-adjacent and complete-global DISTINCT through real session pages with duplicates across boundaries | Complete in worktree |
| O. Authenticated ordered-group resume | Close a group spanning a physical refill, then traverse later groups through real authenticated session pages | Complete in worktree |
| P. Composite topology redundancy | Consolidate child-stream reservation, precharge generic intersection reduction, and remove redundant bounded allocations | Complete in worktree |
