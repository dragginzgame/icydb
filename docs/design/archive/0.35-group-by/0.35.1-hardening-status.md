# 0.35.1 Status (as of 2026-03-01)

## 0.35.1 Scope

This status tracks grouped hardening closeout for `0.35.1` from:

- `docs/design/archive/0.35.1-hardening/0.35.1-design.md`
- `docs/design/archive/0.35-group-by/0.35-design.md`

`0.35.1` is a stabilization addendum before `0.36` feature expansion.

---

## Release Snapshot (`0.35.1`)

- [ ] `0.35.1` released
- [x] hardening design addendum published
- [x] hardening status tracker created
- [x] grouped strategy metrics are emitted deterministically for grouped plans
- [x] grouped paged `LIMIT` path uses bounded canonical-key selection (no full grouped re-buffer)
- [x] grouped pagination tests enforce no single-group split across continuation boundaries
- [x] grouped predicate filtering now streams from ordered key traversal (no pre-filter row materialization)
- [x] grouped planner validation now rejects unsupported `DISTINCT` and non-prefix `ORDER BY` grouped shapes
- [x] grouped hardening checklist verified and closed
- [x] `0.35.1` changelog entries published

Current `0.35.1` status: **in progress (release pending).**

---

## Hardening Checklist

- [x] predicate execution is streaming and order-preserving
- [x] `DISTINCT` invalidates ordered grouping unless adjacency-based eligibility is proven
- [x] `LIMIT` after grouping does not force full-result buffering
- [x] `ORDER BY` insertion disables ordered grouping unless prefix-aligned with grouped keys
- [x] merge/intersect paths preserve strict monotonic key ordering
- [x] cursor resume cannot split a single group across boundaries
- [x] plan metrics record grouping strategy deterministically
- [x] no executor path implicitly falls back to `Vec` buffering

---

## Exit Criteria (`0.35.1`)

`0.35.1` is complete when all checklist items are true and release docs are
aligned for handoff into `0.36`.

---

## Verification Readout (Current)

Current targeted verification evidence:

- `cargo test -p icydb-core db::executor::tests::stream_key -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::paged_builder::grouped_fluent_execute_supports_cursor_continuation -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::route::aggregate::route_plan_grouped_wrapper_observability_vector_is_frozen -- --nocapture` (pass)
- `cargo test -p icydb-core obs::sink::tests::metrics_report_grouped_plan_strategy_counters_accumulate -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::route::planner_mode::grouped_paged_limit_selection_remains_bounded_without_full_group_buffer -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::paged_builder::grouped_fluent_execute_does_not_split_single_group_across_pages -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::stream_key -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::route::planner_mode::grouped_path_predicate_filtering_remains_streaming_and_order_preserving -- --nocapture` (pass)
- `cargo test -p icydb-core db::query::plan::tests::group -- --nocapture` (pass)
- `cargo test -p icydb-core db::query::intent::tests::grouped_load -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::route::planner_mode::grouped_path_avoids_kernel_row_buffer_materialization_fallback -- --nocapture` (pass)

Full release gate for closure:

- `make fmt-check`
- `make clippy`
- `make check`
- `make test`
