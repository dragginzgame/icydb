# 0.50 Slice B: Pipeline/Orchestrator Simplification

Status: Completed  
Owner area: `crate::db::executor::pipeline::orchestrator`

## Intent

Slice B simplifies pipeline/orchestrator structure by reducing duplicated mode
branching and keeping stage-state transitions explicit and uniform.

## Invariants (must remain unchanged)

- plan semantics: identical plan -> identical execution behavior
- continuation semantics: resume tokens remain stable and monotonic
- explain semantics: explain node correlation remains deterministic

## Work plan

### B1. Normalize mode branching (completed)

- consolidated load-mode checks behind `LoadExecutionMode` helpers
- removed direct mode-field branching in payload and grouping/projection flow
- added explicit stage-envelope constructors for access/payload/surface
  transitions in orchestrator stage loop

Files:

- `crates/icydb-core/src/db/executor/pipeline/orchestrator/mod.rs`
- `crates/icydb-core/src/db/executor/pipeline/orchestrator/payload.rs`

### B2. Entrypoint surface adapter cleanup (completed)

- reduce repeated scalar/grouped surface projection boilerplate
- keep adapter error contracts unchanged

Files:

- `crates/icydb-core/src/db/executor/pipeline/entrypoints/scalar/surface.rs`
- `crates/icydb-core/src/db/executor/pipeline/entrypoints/grouped.rs`

### B3. Stage-state boundary audit (completed)

- verify no direct `mode.mode`/`mode.tracing` field reads remain outside
  orchestrator boundary methods

Result:

- no remaining raw mode-field branching found in orchestrator/entrypoint paths
  (`rg` audit over orchestrator + entrypoints)

## Validation record

- `cargo check -p icydb-core`: pass
- `cargo test -p icydb-core db::executor::tests::route::load_matrix:: -- --nocapture`: pass (`14` passed)
- `cargo test -p icydb-core db::executor::tests::pagination::limit_no_cursor_matrix:: -- --nocapture`: pass (`3` passed)
- `make check-invariants`: pass

## Exit gates

- stage-loop behavior is unchanged under existing route/load matrix tests
- orchestrator mode branching is centralized behind helper methods
- no regression in pagination/load invariant checks
- no semantic changes in load/grouped response surfaces
