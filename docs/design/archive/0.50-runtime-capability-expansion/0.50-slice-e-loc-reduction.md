# 0.50 Slice E: Executor LOC Reduction Sweep (Mechanical Only)

Status: In progress  
Owner area: executor-wide mechanical cleanup

## Intent

Slice E performs strictly mechanical LOC and module-surface reduction after
A-D structural slices, without changing semantics or contracts.

## Guardrails

Allowed:

- module merging where ownership is already aligned
- dead code removal
- helper consolidation
- comment/doc cleanup

Not allowed:

- semantic logic changes
- algorithm changes
- planner contract edits

## Work plan

### E1. Mechanical target list from baseline (completed)

- pick highest-LOC modules with straightforward mechanical consolidation
- prioritize comment/docs and small helper de-dup before module moves

Current non-test baseline snapshot:

- module files: `167`
- total LOC: `24,791`
- distribution: `p50=102`, `p90=383`, `p95=428`, `max=1,046`

Initial mechanical-only target list:

- `crates/icydb-core/src/db/executor/explain/descriptor.rs` (`1,046`)
- `crates/icydb-core/src/db/executor/aggregate/projection/mod.rs` (`562`)
- `crates/icydb-core/src/db/executor/aggregate/terminals.rs` (`501`)
- `crates/icydb-core/src/db/executor/aggregate/fast_path.rs` (`500`)
- `crates/icydb-core/src/db/executor/pipeline/operators/post_access/mod.rs` (`479`)
- `crates/icydb-core/src/db/executor/mutation/commit_window.rs` (`464`)
- `crates/icydb-core/src/db/executor/aggregate/mod.rs` (`434`)
- `crates/icydb-core/src/db/executor/runtime_context/mod.rs` (`428`)

E1 prioritization rule:

- first pick files where cleanup is structural/mechanical only
  (helper extraction, dead code pruning, comment/doc compaction)
- defer files likely to require semantic changes for later slices

### E2. Execute no-op cleanup batches (in progress)

- apply cleanup in small, reviewable batches
- keep each batch independently verifiable

Batch `E2-1` (`0.50.1`) completed:

- consolidated repeated aggregate-output decoding in
  `crates/icydb-core/src/db/executor/aggregate/terminals.rs`
- consolidated shared index-backed existing-row reducer setup used by COUNT/EXISTS terminals
- maintained mechanical-only scope (no planner/executor contract or algorithm changes)
- reduced `aggregate/terminals.rs` LOC from `501` to `491`

`E2-1` validation:

- `cargo check -p icydb-core`
- `cargo test -p icydb-core aggregate_count_pk_cardinality_fast_path_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
- `cargo test -p icydb-core aggregate_count_primary_key_stream_fast_path_emits_hit_marker_for_by_ids_unordered_shape -- --nocapture`
- `cargo test -p icydb-core aggregate_exists_secondary_index_covering_fast_path_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
- `cargo test -p icydb-core aggregate_last_unbounded_desc_large_dataset_scans_full_stream -- --nocapture`
- `make check-invariants`

Batch `E2-2` (`0.50.1`) completed:

- consolidated shared FIRST/LAST projection terminal wrapper mechanics in
  `crates/icydb-core/src/db/executor/aggregate/projection/mod.rs`
- consolidated repeated index-bounds component scan dispatch paths in covering
  projection decoding
- consolidated repeated payload-window and consistency row-read loops in
  `crates/icydb-core/src/db/executor/runtime_context/mod.rs`
- kept behavior/mechanics equivalent with no planner/executor contract changes

`E2-2` validation:

- `cargo check -p icydb-core`
- `cargo test -p icydb-core aggregate_field_target_covering_constant_projection_terminals_match_effective_window -- --nocapture`
- `cargo test -p icydb-core aggregate_field_target_covering_constant_projection_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
- `cargo test -p icydb-core aggregate_field_target_covering_index_projection_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
- `cargo test -p icydb-core aggregate_field_target_covering_index_projection_terminals_match_effective_window -- --nocapture`
- `cargo test -p icydb-core aggregate_bytes_key_range_window_parity_desc -- --nocapture`
- `cargo test -p icydb-core aggregate_bytes_pk_fast_path_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
- `make check-invariants`

Batch `E2-3` (`0.50.1`) completed:

- removed unused `Deref` pass-through from
  `crates/icydb-core/src/db/executor/pipeline/operators/post_access/mod.rs`
  and switched post-access helper call sites to explicit plan references
- collapsed non-test unused-phase suppressors into one tuple binding in
  post-access execution stats wiring
- removed single-use prepared-row delta metrics wrapper in
  `crates/icydb-core/src/db/executor/mutation/commit_window.rs` and inlined
  equivalent call-site wiring

`E2-3` validation:

- `cargo check -p icydb-core`
- `cargo test -p icydb-core budget_safety_metadata_order_contract_stays_aligned_with_route_helper -- --nocapture`
- `cargo test -p icydb-core unique_index_delete_then_insert_same_value_succeeds -- --nocapture`
- `cargo test -p icydb-core delete_emits_remove_from_prepared_row_deltas -- --nocapture`
- `make check-invariants`

Batch `E2-4` (`0.50.1`) completed:

- consolidated repeated continuation metadata annotation wiring in
  `crates/icydb-core/src/db/executor/explain/descriptor.rs`
- consolidated repeated EXPLAIN fetch-node descriptor wiring for top-n and
  index-range limit pushdown nodes
- simplified explain access-prefix helper branching with equivalent behavior
- reduced `explain/descriptor.rs` LOC from `1,046` to `1,026`

`E2-4` validation:

- `cargo check -p icydb-core`
- `cargo test -p icydb-core session_load_explain_execution_projects_descriptor_tree_for_ordered_limited_index_access -- --nocapture`
- `cargo test -p icydb-core session_load_explain_execution_predicate_stage_and_limit_zero_matrix_is_stable -- --nocapture`
- `cargo test -p icydb-core load_trace_marks_secondary_order_pushdown_outcomes -- --nocapture`
- `make check-invariants`

Batch `E2-5` (`0.50.1`) completed:

- consolidated aggregate-window empty-shape checks into one equivalent boolean
  contract in `crates/icydb-core/src/db/executor/aggregate/mod.rs`
- consolidated zero-output derivation to one equivalent option combinator path
- reduced `aggregate/mod.rs` LOC from `434` to `423`

`E2-5` validation:

- `cargo check -p icydb-core`
- `cargo test -p icydb-core aggregate_count_pk_cardinality_fast_path_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
- `cargo test -p icydb-core aggregate_exists_secondary_index_covering_fast_path_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
- `make check-invariants`

Additional E2 cleanup in progress:

- simplified runtime-context consistency read path by removing a single-use
  helper layer in `crates/icydb-core/src/db/executor/runtime_context/mod.rs`
- validated with focused bytes parity tests and invariant checks
- consolidated duplicated verbose explain fetch-line formatting into one shared
  helper in `crates/icydb-core/src/db/executor/explain/descriptor.rs`
- validated with focused explain descriptor/trace tests and invariant checks

### E3. Re-measure simplification metrics (pending)

- recompute module count / max LOC / distribution
- update `0.50-executor-baseline.md` with before/after deltas

## Exit gates

- only mechanical diffs in slice E
- no semantic/contract drift in tests or explain outputs
- measurable LOC/module simplification captured in baseline doc
