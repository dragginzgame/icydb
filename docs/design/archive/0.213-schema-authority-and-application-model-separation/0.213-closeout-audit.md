# IcyDB 0.213 Closeout Audit

Status: complete after closeout corrections
Audit date: 2026-07-28

0.213.40 addendum: the final authoring cleanup removes independent
`source_key`/`ident` inputs, derives proposal keys from current names, replaces
the parameterized audit-timestamp declaration with bare `timestamps`, and
removes implicit rename reconciliation. Accepted IDs remain runtime identity;
renames that must preserve them require the explicit per-entity migration
contract designed for 0.218. The proposal transport remains current version 1.
Every maintained pre-0.218 entity declaration also remains at version 1.
Focused validation passes, and the maintained `sql_perf` subject is 5,205,108
raw non-gzipped Wasm bytes, 2,294 bytes (+0.0441%) above 0.213.39.

0.213.41 addendum: the timestamp declaration is now a typed parser structure
rather than a boolean. Bare `timestamps` keeps the conventional field names;
the non-empty nested form can choose either current field name while retaining
the other default. The declaration still lowers immediately into ordinary
managed fields, and assigned booleans, empty lists, duplicate or unknown
options, invalid or equal names, and explicit-field collisions reject. This
adds no runtime timestamp-policy owner and no inferred migration behavior. The
maintained `sql_perf` subject is 5,192,740 raw non-gzipped Wasm bytes, 12,368
bytes (-0.2376%) below 0.213.40. The final correction also deletes the residual
type, record-member, enum-variant, and composite-path metadata-rename
machinery. Existing-head declarations now require exact accepted names and
shapes; there is no empty-domain rename exception or rename-capable catalog
mutator left for migrations to resurrect accidentally.

## 1. Executive Verdict

**Verdict: complete after closeout corrections.**

The 0.213 line established the intended accepted-schema architecture and
removed most of the former generated persistence/runtime machinery. No P0 row
corruption or loss-of-commit-atomicity defect was found in the audited
snapshot. Closeout C5 corrects the final reachable P1 contradiction by making
`icydb-model` the sole application behavior owner.

Closeout C1 corrects the generated identity boundary. C2 corrects
single-live-store application and replaces generated-proposal reconstruction
with a marker-owned durable checkpoint of the exact accepted candidate. C3
corrects accepted removal of closed recursive named-type components. C4 gives
bounded receipts deterministic terminal retention and pending generated
checks an acknowledgement-bound atomic abort. C5 deletes the competing core
visitor, validator, and normalizer stack. C6 deletes the remaining core
generated-field/runtime-schema authority and its parallel physical codec. C7
closes the SQL feature leak, panic-style runtime invariants, corrupt
empty-domain classification, graph-diagnostic ordering, and active
documentation drift. No reachable second runtime schema or persistence
authority remains.

## 2. Reconstructed Final Architecture

The intended and predominantly implemented direction is:

```text
icydb-model authored declarations
        |
        | sealed host graph and exact database closure
        v
icydb-schema bounded proposal
        |
        | database-scoped composition and catalog-native reconciliation
        v
accepted candidate + current-name proposal bindings
        |
        | one marker-owned durable publication
        v
accepted catalog / row layout / value catalog
        |
        +--> structural and typed reads/writes
        +--> SQL planning and writes
        +--> index and relation maintenance
        +--> integrity, activation, replay, and recovery
```

`icydb-schema` owns portable proposal contracts and canonical public scalar
values. `icydb-model` owns source authoring, proposal construction, and
optional source-bound typed adapters. `icydb-core` owns proposal admission,
accepted catalogs, runtime enforcement, physical rows, durable application,
activation, and recovery. `icydb` is the public runtime facade.

This direction is now absolute for application publication, recovery, and
generated-check terminalization: live-only schema allocations restore from
exact durable accepted-candidate bytes, never from an authored/generated
proposal, while an abort mutates accepted activation/source-binding authority
and the canonical validation job under one marker. Explicit application
behavior now has one owner in `icydb-model`; neither `icydb-core` nor the
runtime facade exposes a parallel visitor, validator, or normalizer stack.
Runtime field meaning, SQL/planner metadata, and physical structural encoding
consume accepted field contracts directly; the core generated-field model and
model-derived `SchemaInfo` no longer exist. Runtime failures now preserve
typed corrupt-state, invariant, conflict, and valid-rejection boundaries
without panic-based production control flow.

## 3. Baseline and Scope

- Exact pre-0.213 baseline: tag `v0.212.5`, commit
  `4011d11d654eccd4e1270bd27da408b799f7c775`.
- Audited committed head: tag `v0.213.36`, commit
  `2aaea5343d404d566621d12242356ea588ddbe95`.
- Baseline-to-head scope: 1,334 files, 52,492 insertions, 255,739 deletions.
- Audited workspace graph: 30 Cargo packages.
- Worktree at audit start: clean.
- Current worktree: closeout C5-C7 code, tests, changelog, status, and this
  report are intentionally uncommitted.

The audit covered the complete `baseline..HEAD` change, current manifests and
feature edges, maintained generated fixtures, schema application/publication,
accepted runtime consumers, physical encoding, SQL, typed adapters, named
types, identity reconciliation, generated removal, activation, recovery, and
active documentation. Historical designs and changelogs were used as evidence
of prior intent, not treated as current API documentation.

## 4. Changelog Coverage Matrix

Status values are:

- **closed**: the final owner and maintained evidence agree;
- **corrected Cn**: a closeout defect was found and corrected in the named
  closeout patch.

| Version | Final implementation owner | Important evidence | Status / closeout action |
| --- | --- | --- | --- |
| 0.213.0 | `icydb-schema` proposal/scalar contract; core target issuance | proposal codec/composition tests; package graph | closed |
| 0.213.1 | accepted source-binding catalog and bundle codec | source-binding codec/fingerprint tests | closed |
| 0.213.2 | commit marker, schema publication, recovery; bounded codecs | commit recovery, live checkpoint, and multi-store publication tests | corrected C2 for live-only durable recovery; C7 confirms interrupted marker recovery |
| 0.213.3 | variant source bindings in accepted catalog | source-binding enum codec/reconciliation tests | closed |
| 0.213.4 | SQL-DDL source-key generation and accepted mutation | SQL DDL field/index/check lifecycle tests | closed |
| 0.213.5 | application store, receipt and marker-owned record | application-store codec/capacity tests | corrected C4: pending records are retained and one deterministic terminal record is recycled at capacity |
| 0.213.6 | `application_lowering` plus atomic publication | initial lowering and replay tests | corrected C2: single volatile-store apply now uses marker/checkpoint/receipt publication |
| 0.213.7 | Cargo feature gating for application tests | no-default and exact SQL-only test checks | corrected C7: SQL EXPLAIN helpers no longer leak through `cfg(test)` |
| 0.213.8 | accepted enum/composite catalogs and initial lowering | named-type closure and round-trip tests | closed |
| 0.213.9 | composite member source bindings | member rename/codec tests | closed |
| 0.213.10 | application replay and accepted future defaults | exact replay/default reconciliation and marker recovery tests | corrected C2: recovery restores accepted authority before exact facade replay; C7 confirms the focused recovery path |
| 0.213.11 | source-key reconciliation in `application_lowering` | rename and source-binding tests | closed |
| 0.213.12 | generated-check removal | source-key removal tests | corrected C4: pending generated-check abort removes the activation and exact source identity atomically |
| 0.213.13 | maintained cardinality plus generated-check admission | empty-domain check-add tests | closed |
| 0.213.14 | canonical unpublished-check evaluator | bounded proof tests | closed |
| 0.213.15 | application record plus constraint activation runner | continuation/acknowledgement/abort tests | corrected C4: exact `Aborted` terminal replay, job cleanup, and bounded retention |
| 0.213.16 | catalog-native field removal and dense layouts | dependency and empty-entity removal tests | closed |
| 0.213.17 | catalog/index physical-domain removal | index-domain proof tests | closed |
| 0.213.18 | accepted named-type removal | store-local recursive removal tests | corrected C3: complete unreferenced accepted-ID sets are removed atomically |
| 0.213.19 | relation removal and reverse-domain proof | source/reverse domain tests | closed |
| 0.213.20 | entity removal and source-binding subtree deletion | empty-entity removal tests | closed |
| 0.213.21 | composite member rename plus exact physical proof | record-member rename tests | superseded by the 0.213.40 explicit-migration boundary |
| 0.213.22 | accepted query context and trusted SQL | dynamic/SQL read tests | closed |
| 0.213.23 | accepted after-image writer | structural/SQL write parity tests | closed |
| 0.213.24 | entity registration without callback-bearing runtime hooks | hard-cut searches and runtime registration tests | closed |
| 0.213.25 | `icydb-model` sealed graph | seal/duplicate/late-registration and constructor-order tests | corrected C7: invalid registration retains a canonical lexical diagnostic |
| 0.213.26 | explicit keys in authored fragments | source-key validation and composition tests | superseded by 0.213.40 current-name-derived proposal keys |
| 0.213.27 | sealed database closure and fragment codec | repeated-field/payload/tuple tests | closed |
| 0.213.28 | explicit audit timestamp proposal metadata; accepted writer | omission/management tests | closed |
| 0.213.29 | generated typed adapters over accepted public values | typed adapter compile/runtime tests | closed |
| 0.213.30 | explicit application normalizers | application behavior tests and callback absence | corrected C5: `icydb-model` is the sole behavior owner; the core/facade stack is deleted |
| 0.213.31 | accepted public-value adapter boundary | typed adapter compile fixtures | closed |
| 0.213.32 | `icydb-model`/`icydb-model-macros` package boundary | schema-only and typed-adapter fixtures | corrected C6: the core field-model/metadata residue and facade trait export are deleted |
| 0.213.33 | maintained consumer cutover, bounded SQL and mixed stores | canister builds, SQL response/recovery and exact feature tests | corrected C1 for generated SQL name dispatch, C2 for durable live-store reconstruction, and C7 for SQL-only ownership |
| 0.213.34 | accepted-ID named cycles and finite values | collection-scale graph, bounded value, and recursive removal tests | corrected C3 for closed component removal |
| 0.213.35 | accepted built-in numeric/length constraints | durable-rule ownership and ingress parity tests | closed; C7 confirms accepted projection and ingress remain fallible and total |

## 5. Authority Matrix

| Concern | Construction boundary | Canonical/durable owner | Runtime and recovery consumers | Duplicate or conversion disposition |
| --- | --- | --- | --- | --- |
| Source bindings | proposal reconciliation | `AcceptedSourceBindingCatalog` in accepted bundle | rename/removal/replay/recovery | authored keys are inputs only |
| Entities | entity fragments | accepted entity snapshots keyed by `EntityTag` | all reads/writes/SQL/integrity | generated entity nodes remain proposal/adapter input |
| Fields | field fragments | accepted fields, IDs and row layouts | after-image writer, decoder, planner | C6 deletes the core `FieldModel` mirror and accepted-to-model conversion |
| Record members | named fragments | accepted composite catalog and member IDs | value admission/codec/introspection | source spelling must never decode rows |
| Named types | named fragments | accepted enum/composite catalogs by stable IDs | input/output codecs, compatibility, recovery | C3 removes complete unreferenced accepted-ID sets without changing recursive admission |
| Enum variants/payloads | enum fragments | accepted enum catalog and variant IDs | value admission/output/codec | qualified source paths terminate at proposal boundary |
| Indexes | proposal or SQL DDL | accepted index catalog plus physical index store | planner, writes, recovery/integrity | accepted rebuild contracts are authoritative |
| Relations | proposal | accepted relation catalog plus physical reverse domains | write maintenance/recovery/integrity | no callback registry remains |
| Accepted checks | rule/constraint fragments or SQL DDL | accepted constraint catalog | one compiled accepted evaluator | application validators are a distinct owner |
| Numeric/length rules | model source-rule declaration | ordinary accepted constraints per field | typed/structural/SQL after-image admission | no generated semantic mirror intended |
| Insert defaults | field proposal/DDL | accepted future insert policy | accepted after-image writer | typed omission is preserved, not defaulted by adapter |
| Audit timestamps | typed `timestamps` declaration | accepted field management | accepted after-image writer | conventional or explicitly named current fields; adapters do not invent timestamps |
| Normalizers | explicit application call | `icydb-model` application behavior | caller-controlled before database admission | runtime and facade expose no competing behavior owner |
| Application validators | explicit application call | `icydb-model` application behavior | caller-controlled typed domain checks | never accepted automatically; runtime and facade expose no competing behavior owner |
| Typed row adapters | model compiler | source-bound generated adapter contract | accepted `OutputValue` conversion | no physical row access |
| Typed write adapters | model compiler | source-bound write-intent contract | accepted structural write API | omission/null/value stay distinct |
| Public dynamic values | public runtime/schema boundary | `InputValue` / `OutputValue` contracts | structural, typed and SQL boundaries | physical `Value` conversion remains core-owned |
| Row layouts | accepted schema publication | accepted layout/revision; exact accepted-candidate checkpoint for live-only projections | physical encoder/decoder and recovery | generated order/proposal material is not recovery authority |
| Physical row codec | runtime persistence boundary | `icydb-core::db::data` | normal reads, recovery, integrity | generated row bridge removed |
| Schema application jobs | application admission | application record plus commit marker | continuation, abort, and recovery | C4 retains every pending record and recycles only terminal evidence |
| Activation jobs | accepted constraint activation | canonical constraint validation store/runner | continuation, acknowledgement, abort, recovery | application records carry only the supported generated-check identity |
| Findings/acknowledgements | activation scan | durable validation receipts/jobs | runner and introspection | abort reuses the canonical exact acknowledgement check before atomic job deletion |
| SQL metadata | accepted catalog projection | accepted `SchemaInfo`/entity authority | parser lowering/planner/executor | C6 deletes model-only construction and fallback slots |
| Application receipts | accepted application boundary | 64-record stable application store | replay, continuation, and abort | terminal receipts form a deterministic bounded retention window; pending evidence is never evicted |

## 6. Findings

### P0

No P0 finding was established. The marker path binds candidate publications,
live accepted checkpoints, and application-record replacement in one commit,
and recovery verifies and replays the same marker payload.

### P1-1: Application receipts reject a valid single volatile-store apply — corrected C2

- **Evidence:** `publish_accepted_schema_candidates_with_optional_application_record`
  in `crates/icydb-core/src/db/commit/schema_publication.rs` has a direct
  volatile single-store publication branch, but returns
  `store_unsupported()` whenever an application record accompanies it.
- **Violated invariant:** the canonical application protocol must work for
  every registered database configuration that can otherwise publish an
  accepted candidate.
- **Consequence:** a schema proposal for a database containing only one heap
  store can lower successfully and still fail solely because application
  receipts were added.
- **Correction:** C2 routes this case through the same marker protocol as
  mixed-store application and checkpoints the exact accepted candidate before
  exposing the live catalog or receipt. Focused completion and interrupted
  revision-2 recovery tests cover the corrected case.

### P1-2: Mutually recursive named types cannot be removed — corrected C3

- **Evidence:** `lower_existing_store_candidate` rejects more than one type
  removal, while `with_removed_type` validates the remaining catalog after
  each individual deletion.
- **Violated invariant:** explicit absence should remove an unreferenced
  accepted named-type component, including a valid admitted cycle, by stable
  IDs.
- **Consequence:** a two-node accepted SCC introduced by 0.213.34 can be
  created but never removed even when no entity or external named type
  references it.
- **Correction:** C3 resolves every source removal to each store-local
  accepted ID, subtracts the complete bounded enum/composite set, then
  validates the retained catalogs and entity fields once. Internal cycle
  edges are therefore removed atomically, while partial removal and external
  references fail closed.

### P1-3: Application history permanently exhausts after 64 submissions — corrected C4

- **Evidence:** `MAX_SCHEMA_APPLICATION_RECORDS` is 64 in
  `application_store.rs`; before C4 capacity rejected before mutation and no
  retention, compaction, or terminal-record deletion path existed.
- **Violated invariant:** bounded durable replay metadata must not turn normal
  long-lived schema evolution into permanent unavailability.
- **Consequence:** the 65th distinct submission key fails regardless of record
  size or whether all prior jobs are terminal.
- **Correction:** C4 preserves all pending records and, only when admitting a
  new key at capacity, deterministically retires the lowest stable-key
  terminal record. An all-pending store fails closed. Exact replay remains
  available for retained terminal records; retirement explicitly ends that
  bounded replay window rather than blocking all future applications.

### P1-4: Application behavior has two public owners — corrected C5

- **Evidence before C5:** `crates/icydb-core/src/visitor/**` and
  `crates/icydb-model/src/visitor/**` contain substantially parallel visitor,
  validation, and normalization contracts; `icydb-core` also exposes
  behavior-facing model traits while the final architecture assigns
  application behavior to `icydb-model`.
- **Violated invariant:** accepted runtime owns durable enforcement and
  `icydb-model` owns explicit application behavior; neither should be a
  fallback for the other.
- **Consequence:** types and semantics can drift, including scalar coverage,
  diagnostics, and traversal behavior, while both stacks continue compiling.
- **Correction:** call-site proof found no database or generated-model consumer
  of the core stack. C5 deletes the core modules, scalar implementations,
  `TypeKind`, facade exports, and tests whose only purpose was preserving the
  retired module boundary. `icydb-model` remains the sole owner, and a renamed
  typed-adapter fixture proves runtime-facade scalar identity interoperates
  with that owner without a bridge.

### P1-5: Generated schema/SQL surfaces retained mutable identity authority — corrected C1

- **Evidence before C1:** generated schema called
  `try_describe_entity_by_name` with a Rust path, and generated SQL emitted one
  `identifiers_tail_match` arm per authored Rust path and display name.
- **Violated invariant:** generated code may bind through immutable source
  identities but accepted catalog names are the runtime SQL/introspection
  authority.
- **Consequence:** an accepted rename could make the schema endpoint fail and
  make SQL reject the new accepted name before catalog resolution.
- **Correction:** C1 adds source-key introspection and delegates non-empty SQL
  entity dispatch to the accepted-catalog runtime. Generated fixture-reset
  statements still use the current authored names because fixtures are
  explicit destructive application behavior, not query routing.

### P2-1: Generated proposal remains reachable in live-only recovery — corrected C2

- **Evidence:** public `rebuild_generated_live_schema` accepts a
  `SchemaProposal`, discovers retained durable bindings, and publishes absent
  `LiveRebuiltMetadata` candidates outside ordinary `apply_schema`.
- **Risk:** the old lane reconstructed every live store as revision 1 and
  retained only cross-store entity tags. Evolved field/type/object identities
  and the committed accepted fingerprint therefore had no durable recovery
  owner.
- **Correction:** C2 deletes the generated rebuild entry point and special
  lowering/retained-ID machinery. Marker publication now compare-and-replaces
  a bounded, checksummed checkpoint containing the exact accepted bundle/root;
  recovery restores that checkpoint before ordinary marker replay and verifies
  the same bytes. The final version-1 database identity rejects pre-checkpoint
  development stores instead of adding a legacy reconstruction fallback or a
  version ladder.

### P2-2: SQL-only library-test feature ownership is inconsistent — corrected C7

- **Evidence:** helpers compiled under `cfg(any(test, feature =
  "sql-explain"))` consume `LoweredSqlQuery`, while its module ownership is
  SQL-feature dependent. The SQL-only lib-test combination does not express
  one coherent gate.
- **Risk:** the supported feature matrix and the test feature matrix diverged.
- **Correction:** C7 makes every EXPLAIN diagnostic helper
  `sql-explain`-owned, removes one stale SQL predicate rewrite re-export and
  its misplaced tests, and narrows query re-exports to the SQL feature that
  consumes them. The exact `sql` without `sql-explain` library-test check and
  both no-default facade/core checks pass.

### P2-3: Model-era metadata survives beyond a justified boundary — corrected C6

- **Evidence before C6:** public `FieldTypeMeta`, core `FieldModel`/
  `CompositeFieldModel`, and model-derived `SchemaInfo` constructors survive
  alongside accepted-native runtime contracts.
- **Risk:** these DTOs can rebuild a second view of field/type/layout meaning
  and make retired generated-model machinery appear supported.
- **Correction:** C6 deletes the public trait and complete core generated-field
  model, makes `SchemaInfo` accepted-only, removes model-only planner and
  predicate fallbacks, and drives physical structural/primary-key codecs
  directly from `AcceptedFieldKind`. Accepted-native enum/composite fixtures
  replace tests that converted generated declarations back into runtime
  catalogs.

### P2-4: Application state exposes unreachable transitions — corrected C4

- **Evidence before C4:** `SchemaChangeOutcome::Failed` was decodable and
  replayable, but production application flow had no failure-terminalization
  operation; `SchemaChangeActivationKind` included non-check variants while
  continuation rejected every activation except `Check`.
- **Risk:** persisted variants imply lifecycle guarantees the application
  engine cannot produce or complete.
- **Correction:** C4 hard-cuts `Failed`, its failure DTO, and the activation
  kind enum. The application record now carries only the store/entity/check
  identity actually supported by the canonical runner, with `Applied` and
  `Aborted` as the only terminal pending transitions. New version-1 application
  and database magic identities reject obsolete development bytes.

### P2-5: Pending generated-check application has no explicit abort/expiry — corrected C4

- **Evidence before C4:** continuation could advance/acknowledge the one check
  activation, but the public application protocol had no abort or expiry path
  tied to the receipt/job owner.
- **Risk:** a pending job can retain application state indefinitely and
  interact poorly with later removal/reconciliation.
- **Correction:** C4 adds an explicit owner-invoked abort. A validating job
  cannot be aborted past a retained finding page without its exact sequence
  acknowledgement. One marker deletes the canonical validation job, removes
  the accepted pending activation and immutable source binding, advances the
  accepted head, and replaces the application record with replayable
  `Aborted`. There is deliberately no clock-based expiry: pending evidence is
  never evicted implicitly.

### P2-6: Diagnostics flatten distinct closeout failures — corrected C7 where authority is final

- **Evidence:** application lowering contains broad
  `store_unsupported()` translations across identity, graph, removal, bound,
  and physical-proof failures; repository tests still have areas that assert
  broad classes rather than exact typed domains.
- **Risk:** source mismatch, unsupported graph placement, stale target, and
  corrupt storage could become difficult to distinguish at public boundaries.
- **Correction:** C7 adds one catalog-native exact-empty proof used by
  generated entity, field, index, relation, and record-member mutations.
  Missing or uninspectable cardinality is now store corruption, a positive
  exact count is a valid unsupported/non-empty rejection, and missing
  candidate authority is a store invariant. Generator, commit-store,
  accepted-decode, and catalog failures now propagate typed errors. Invalid
  proposal subreasons that legitimately share the current
  `RuntimeUnsupported` public class remain a diagnostic-granularity risk, not
  a correctness or authority contradiction; splitting that public taxonomy
  would require a new design.

### P2-7: Production physical/schema paths retain panic-style invariants — corrected C7

- **Evidence:** production `unreachable!` remains in accepted structural-field
  conversion, persisted-row reading, schema type projection, physical access,
  and SQL lowering.
- **Risk:** corrupt persisted state or a future exhaustive-match change could
  trap rather than produce the required typed corruption/internal error.
- **Correction:** accepted structural conversion, persisted-row reading,
  schema projection/allocation, primary-key decoding, commit-store access,
  SQL lowering, ULID generation, and catalog inspection are fallible or
  conservative at inconsistent-state boundaries. The public pre-1.0
  `GenerateKey::generate` and `show_entities` contracts now return `Result`
  rather than concealing failure. Strict production Clippy with `panic`,
  `unwrap_used`, and `expect_used` denied passes for `icydb-core`; the only
  remaining `unreachable!` match is test-only.

### P3-1: Sealed graph reports construction-order “first error” — corrected C7

- **Evidence:** `Schema::register` retains the first duplicate/late error and
  `seal` returns it.
- **Risk:** invalid graphs with multiple failures could report a constructor
  order-dependent diagnostic even though successful graph bytes are
  deterministic.
- **Correction:** `SchemaGraphError` has a total order and registration retains
  the lexical minimum error. Reversing constructor order now produces the
  same duplicate-registration diagnostic.

### P3-2: Active roadmap documentation uses retired terminology — corrected C7

- **Evidence:** `docs/1.0-FEATURES.md` still calls application behavior
  “sanitizers”; later active design prose also uses the retired word when
  describing current snapshots.
- **Risk:** current-facing docs taught an API spelling removed by 0.213.
- **Correction:** the 1.0 feature roadmap, architecture visibility map, and
  active 0.214/0.215 designs use `normalizer` and list the current accepted
  runtime owners. Historical changelogs and superseded design evidence remain
  unchanged.

## 7. Sediment and Redundancy Inventory

| Item | Current disposition |
| --- | --- |
| Generated Rust-path/display-name SQL arms | deleted in C1 |
| Hidden `identifiers_tail_match` macro re-export used only by those arms | deleted in C1 |
| Generated schema lookup by Rust path through a name API | replaced by immutable source-key adapter lookup in C1 |
| Core and model visitor/validator/normalizer stacks | duplicate core/facade owner deleted C5; `icydb-model` retained as the sole explicit application-behavior owner |
| `FieldTypeMeta`, `FieldModel`, `CompositeFieldModel` runtime-era metadata | deleted C6, including the facade export and scalar implementations |
| Model-only/generated `SchemaInfo` construction and field-slot fallbacks | deleted C6; accepted snapshots/value catalogs are mandatory |
| Generated-kind physical structural codec and accepted-to-model projection | deleted C6; `AcceptedFieldKind` drives the sole structural and primary-key codec |
| Generated enum-catalog conversion used by runtime-shaped tests | replaced C6 with accepted-native bounded fixtures, including accepted-ID recursion |
| Generated live-schema rebuild proposal path, selected-store lowering, retained-ID allocator branch, facade bridge, and obsolete test | deleted C2; exact durable accepted-candidate checkpoint is the recovery owner |
| Live accepted-candidate checkpoint codec/store | added C2 and retained: distinct bounded durable recovery format for schema allocations with no local history |
| Application `Failed` outcome, failure DTO, and non-check activation kind | deleted C4; only reachable generated-check pending identity and `Applied`/`Aborted` terminal transitions remain |
| Application receipt record store without retention | corrected C4; deterministic stable-key terminal retirement preserves the fixed 64-record bound and all pending evidence |
| One-at-a-time named-type removal | replaced C3 with whole-set accepted-ID removal and final retained-catalog validation |
| SQL EXPLAIN helpers exposed through test-only ownership | narrowed C7 to `sql-explain`; exact SQL-only tests no longer compile a partial diagnostic graph |
| Stale SQL predicate rewrite re-export and misplaced tests | deleted C7; accepted SQL lowering owns the surviving normalization path |
| Infallible key generation, commit-store access, and entity catalog wrappers | hard-cut C7 to typed `Result` contracts; no fallback or panic bridge retained |
| Repeated generated-object empty-domain checks | consolidated C7 behind one exact accepted/cardinality proof with corrupt/non-empty distinction |
| Constructor-order graph error retention | replaced C7 with canonical ordered selection |
| Accepted enum/composite/source-binding codecs | retained: distinct durable formats with explicit owners |
| Structural physical value codec and accepted public-value conversion | retained: distinct physical and public boundaries |
| Historical docs mentioning retired packages/terminology | retained as historical evidence; only active docs will change |

The focused codec inventory found one justified shared mechanical layer and no
remaining duplicate semantic format owner. `schema::wire` provides bounded,
checked big-endian primitive framing to the enum, composite, source-binding,
accepted-bundle, and accepted-root codecs; each caller still owns its magic,
version, ordering, duplicate policy, fingerprint preimage, and semantic
validation. Persisted schema snapshots use their existing bounded Candid
envelope. Physical rows/values, indexes, journals, commit markers, cursors,
application records, and checkpoints retain separate codecs because their
wire contracts and recovery lifecycles differ. C6 changes no durable bytes,
and every active pre-1.0 format constant remains version 1.

## 8. Durability and Recovery Analysis

The current durable schema application sequence is:

1. issue a target from recovered registry authority and the exact accepted
   database head;
2. digest and check the proposal/submission identity;
3. load current accepted bundles and build catalog-native candidates;
4. preflight physical emptiness/check activation requirements;
5. construct one receipt/application record;
6. preflight every candidate, live accepted checkpoint, and
   application-record compare value;
7. acquire a commit marker containing store journal batches and the exact
   application-record operation;
8. append journal batches, checkpoint live-store accepted candidates, apply
   accepted candidates, and apply the record;
9. finish/remove the marker;
10. return the durable receipt.

Observed interruption behavior:

| Interruption point | Observable/recovery behavior | Audit status |
| --- | --- | --- |
| Before marker publication | no candidate or receipt is visible | coherent |
| Marker durable, before journals/checkpoints | recovery restores the previous live checkpoint, applies marker checkpoints, validates the exact marker payload, and completes it | coherent for journaled and live-only stores |
| Some journals appended | marker identity and batch IDs drive idempotent completion | coherent |
| Candidates applied, receipt absent | recovery applies the same record operation | coherent |
| Receipt applied, marker present | compare-and-replace verification makes replay idempotent | coherent |
| Marker finished | accepted heads and receipt agree | coherent |
| One volatile store plus application record | one marker owns checkpoint, accepted candidate, and receipt | corrected C2 |
| Completed live projection missing after upgrade | recovery republishes the exact durable accepted checkpoint before runtime access | corrected C2; no authored/generated fallback |
| Interrupted evolved live revision after upgrade | recovery restores the prior or already-advanced checkpoint and replays the exact marker candidate idempotently | corrected C2 |
| Pending generated check continuation | canonical check runner advances one bounded page and binds acknowledgement receipt | coherent |
| Abort before validation starts | marker removes the accepted activation/source binding and terminalizes the application record | coherent; no validation job may exist |
| Abort with a validation job | exact retained-page acknowledgement is checked before the marker deletes the job, publishes the catalog-native candidate, and records `Aborted` | coherent; wrong/missing acknowledgement fails before mutation |
| Abort marker interruption | recovery replays candidate publication, validation-job deletion, and application-record replacement from the same marker | coherent; focused journaled recovery evidence |
| Terminal receipt capacity | the lowest stable-key terminal record is retired only while admitting a new record at the 64-record limit | coherent; pending records are never selected |
| All-pending capacity | admission conflicts before marker publication | coherent; no pending evidence is discarded |

Exact proposal replay returns the stored receipt only when database identity,
submission key, proposal digest, and prior expected head all match. A different
proposal under the same idempotency key conflicts. The marker carries the
application record, so an unrelated operation cannot overwrite its key without
failing preflight. Terminal replay is exact while the receipt remains in the
bounded retention window; once retired, an old expected head is stale and
cannot recreate or overwrite current accepted authority.

## 9. SQL / Structural / Typed Parity Matrix

| Concern | Structural path | SQL path | Typed path | Verdict |
| --- | --- | --- | --- | --- |
| Entity resolution | accepted context | accepted display-name context | source-bound adapter then accepted context | C1 removes generated SQL prefilter |
| Reads | accepted row decode/output values | same accepted authority through planner/executor | output-value adapter | coherent |
| Inserts | accepted after-image resolver | lowers into accepted mutation | write-intent adapter into structural ingress | coherent |
| Updates | accepted after-image resolver | same mutation authority with SQL policy bounds | write-intent adapter | coherent |
| Deletes | accepted entity/relation authority | SQL lowering to canonical delete path | structural identity adapter | coherent |
| Omission/null/value | explicit input intent | parsed SQL intent | adapter preserves all three | coherent |
| Defaults | accepted insert policy | accepted insert policy | adapter does not apply | coherent |
| Audit timestamps | accepted management | accepted management | adapter does not own | coherent |
| Built-in rules/checks | compiled accepted constraints | same after-image evaluator | same structural ingress | coherent; C7 evidence expansion |
| Index/relation maintenance | commit/mutation core | same commit/mutation core | same structural ingress | coherent |
| Response bounds | structural page/result contracts | atomic bounded SQL response | typed conversion after bounded read | SQL-focused evidence exists |
| Errors | corrupt/invariant/invalid/stale classes remain typed | query/runtime wrappers preserve the same causes | facade conversion preserves failure | coherent; finer invalid-proposal subreasons remain a documented taxonomy risk |
| Recovery | accepted physical/catalog boundary | same recovered session | adapter rebinds after recovery | coherent; live projections restore from accepted checkpoints, never adapter/proposal metadata |

## 10. Named-Type and Boundedness Analysis

0.213.34 correctly distinguishes a cyclic schema graph from an infinite value.
Named references are resolved to stable accepted IDs; catalogs can contain
strongly connected components; runtime values remain finite and value
admission/encoding consumes explicit depth and byte budgets. The collection-scale
`Values -> FieldValue -> Value -> Values` graph and qualified payload enums
round-trip through proposal lowering, accepted bundle encoding, recovery, and
finite value encoding.

The audit found no recursive catalog inlining requirement in the accepted
representation. Resolution/publication traversals use identity-based state,
and persisted codecs bound collection counts and input bytes before
construction. C3 closes the former lifecycle contradiction by resolving the
explicit removal set across every store-local accepted copy and subtracting
all selected IDs before validation. This removes any fully selected closed
component without requiring recursive SCC expansion. Retained catalog
validation rejects references from a non-selected named type, and accepted
bundle validation rejects entity-field references. Source bindings and their
variant/member children are retired only in the valid candidate; row layouts
do not change.

Bounds inventoried for the closeout include stores, fragments, entities,
fields, named definitions, graph edges, variants, members, tuple arity,
nesting depth, proposal/accepted bytes, source bindings, receipt bytes/count,
activation findings/pages, SQL response bytes/rows, and physical proof pages.
C4 keeps the receipt store at 64 records and makes that bound a retention
window: only terminal records can be recycled and the stable application key
defines the deterministic candidate. Decoding remains bounded to 64 KiB per
record and 64 records before any retained record is inspected.

## 11. Corrections Made

Closeout patch C1 changes:

- `icydb-core` resolves an accepted entity context from an exact
  `EntitySourceKey` and then uses the same accepted schema description path as
  display-name lookup;
- the `icydb` facade exposes that source-bound introspection operation;
- generated schema endpoints embed immutable entity source keys instead of
  Rust paths passed to a display-name API;
- generated SQL surfaces retain only the fact that the model graph is
  non-empty and delegate every actual entity name to accepted SQL authority;
- obsolete generated query/DDL/update match-arm builders and the hidden
  `identifiers_tail_match` re-export are removed;
- model token tests cover immutable schema binding, absence of generated
  identity allowlists, and the empty-graph fail-closed case.

No accepted schema, row, receipt, marker, cursor, or public value wire format
changes in C1.

Closeout patch C2 changes:

- all live-only accepted-schema publications use the marker path and
  compare-and-replace an exact accepted-candidate checkpoint before the live
  projection or optional receipt is exposed;
- recovery restores completed checkpoints, handles a checkpoint that advanced
  before an interrupted live projection, replays the same marker candidate,
  and verifies accepted/checkpoint bytes together;
- one live-only store plus an application receipt is admitted through the same
  protocol as mixed-store publication;
- generated proposal-based live reconstruction, its special initial-lowering
  selection/retained-ID machinery, facade bridge, and obsolete retained-ID
  test are deleted;
- the final database identity hard cut is consolidated with C4: the current
  envelope stays at version 1 under new magic, making pre-checkpoint
  development stores explicitly incompatible. Receipt, application-record,
  accepted-bundle/root, journal, and marker bytes remain unchanged by C2.

Primary C2 files are `schema/live_schema_checkpoint.rs` (bounded durable
checkpoint owner), `commit/schema_publication.rs` (marker/preflight/apply),
`commit/recovery.rs` and `schema/store.rs` (restore/replay/verification),
`schema/application.rs`, `schema/application_lowering.rs`, and both session
catalog facades (generated rebuild deletion), plus `database_format/mod.rs`
and the persisted-format corpus (explicit format-2 hard cut). Capability
comments, module exports, root `Unreleased`, and the 0.213 tracker/report were
updated to describe the final owner.

Closeout patch C3 changes:

- accepted enum and composite catalogs remove a deterministic set of stable
  accepted IDs and validate retained definitions after the whole set is
  absent;
- existing-head lowering no longer rejects multiple type removals or validates
  them in source/declaration order;
- source-bound variant/member identity closure is retired after the candidate
  catalogs pass;
- a two-store enum-record cycle regression proves recursive admission remains,
  entity and partial-cycle references reject, and the complete unreferenced
  component disappears from both catalogs and source bindings.

The primary C3 files are `schema/application_lowering.rs`,
`schema/enum_catalog/mod.rs`, and `schema/composite_catalog.rs`. Root
`Unreleased`, the tracker, and this report record the completed correction.
No persisted format, row layout, public adapter, or runtime value contract
changes.

Closeout patch C4 changes:

- the application store deterministically recycles one terminal receipt at
  capacity while preserving every pending record and failing closed when all
  64 records are pending;
- the public core/facade catalog session exposes an explicit generated-check
  abort and terminal `Aborted` progress;
- abort resolves store, entity, constraint, activation, and source-binding
  authority from the accepted catalog and retained application record, never
  from an authored/generated model;
- exact finding acknowledgement is reused from the canonical validation job
  before that job can be deleted;
- one commit marker owns accepted candidate publication, validation-job
  deletion, and pending-to-aborted record replacement, making interruption and
  replay idempotent;
- the unreachable failure DTO/outcome and activation-kind enum are deleted;
- new application-header, application-record, and database magic identities
  keep every current format at version 1 while making the lifecycle hard cut
  explicit for development stores.
- persisted-format policy and CI invariants require every later pre-1.0
  incompatible change to choose a new magic/profile identity at version 1;
  active format constants above 1 fail repository validation.

The primary C4 files are `schema/application.rs`,
`schema/application_receipt.rs`, `schema/application_store.rs`,
`schema/source_binding.rs`, `commit/schema_publication.rs`, both catalog
session facades, `database_format/mod.rs`, and the persisted-format corpus.
This is approximately 14 code/test files and +500/-100 lines for the explicit
abort/recovery tests and protocol, with the state vocabulary becoming smaller.
Root `Unreleased`, the tracker, and this report record the correction. No row
layout, physical row, SQL, public value, named-type, or adapter format changes.

Closeout patch C5 changes:

- the duplicate core visitor, validator, and normalizer modules and their
  scalar implementations are deleted after call-site inspection established
  that no database, recovery, SQL, structural, typed-adapter, or generated
  runtime path consumed them;
- `TypeKind` and all facade behavior re-exports are deleted rather than
  retained as aliases or forwarding contracts;
- privacy UI tests that existed only to preserve the retired core stack are
  deleted;
- the renamed typed-adapter fixture binds a runtime-facade scalar to the
  maintained `icydb-model::visitor::Visitable` contract through the canonical
  schema scalar identity;
- the layer-authority invariant now rejects application visitor, validator, or
  normalizer ownership in both runtime crates.

Primary C5 paths are the deleted `icydb-core` visitor stack, core scalar
modules, `icydb-core` and `icydb` public traits/exports, the maintained
typed-adapter fixture, and the layer-authority invariant. Before documentation,
the implementation is approximately +25/-1,410 lines across 33 files, so the
source and ownership shape are materially simpler. C5 changes no persisted,
accepted-schema, row, cursor, receipt, marker, or public-value format.

Closeout patch C6 changes:

- `SchemaInfo` is accepted-only and always carries accepted field kinds plus
  the accepted value-catalog handle; every model-derived constructor,
  optional-catalog branch, and model-only field slot is deleted;
- the public core `FieldTypeMeta` contract, core `model::field` DTOs and
  semantics, facade export, and scalar implementations are hard-cut;
- predicate/grouped planning, SQL metadata, integrity, inspection, index-key,
  relation, and row-layout consumers use accepted contracts directly;
- physical structural fields and primary-key components encode/decode through
  `AcceptedFieldKind` without projecting back to a generated kind; duplicate
  structural codec modules and relation-key dispatchers are deleted;
- persistence and write-policy enums move under accepted schema, making their
  runtime owner explicit;
- accepted-native enum/composite fixtures replace generated-model catalog
  conversion while retaining mutual-recursion and malformed/boundary
  coverage.

The retained codec split is intentional: schema binary envelopes share only
bounded mechanical framing through `schema::wire`, while schema snapshot,
physical row/value, commit, journal, cursor, application, and checkpoint
formats retain their semantic owners. C6 changes no durable bytes or versions.
Before documentation, the C6 implementation is approximately +1,071/-4,158
lines across 85 core/facade source and test files, so both ownership and code
shape are materially simpler.

Closeout patch C7 changes:

- SQL EXPLAIN helpers and diagnostics are owned only by `sql-explain`, while
  SQL runtime/query re-exports are owned by `sql`; the stale predicate
  identifier rewrite export and its misplaced tests are deleted;
- accepted field projection, structural and primary-key decoding, physical
  reads, schema allocation/admission, ordered overlays, commit-store access,
  SQL lowering, numeric projection, and key generation fail with typed errors
  or conservative total behavior instead of production panics;
- `GenerateKey::generate` and core/facade `show_entities` are fallible pre-1.0
  hard cuts, with callers propagating the exact runtime failure;
- one catalog-native exact-empty helper separates corrupt/uninspectable
  cardinality from an exact positive count and is shared by all relevant
  generated-object transitions;
- invalid sealed graphs retain the lexically canonical registration error;
- active documentation names normalizers and current accepted runtime owners,
  while historical release evidence remains unchanged.

C7 is approximately 80 newly affected or overlapping files and +780/-752
lines relative to the recorded C5-C6 worktree, including focused tests,
changelog, tracker, and this audit. The implementation shape stays neutral to
slightly smaller in production code despite the closeout evidence added here,
while failure and feature ownership become simpler. It changes no persisted
format, row layout, accepted contract, or successful runtime algorithm.

## 12. Validation Evidence

C1 validation completed:

- `cargo fmt --all`;
- `cargo test --locked --offline -p icydb-model --lib` — 120 passed;
- SQL and SQL-explain check for `icydb-core` and `icydb` — passed;
- generated `canister_test_sql` check — passed;
- no-default checks for `icydb-core` and `icydb` — passed;
- affected-crate all-target/all-feature clippy with warnings denied — the
  first final run exposed the now-unreachable public identifier matcher; its
  visibility was narrowed and the rerun passed;
- `make check-invariants` — passed;
- four independent source searches covering retired generated dispatch
  symbols, source-key call sites, deleted token fields, and retired package
  manifest edges — passed for the intended C1 boundary;
- `git diff --check` and `cargo fmt --all --check` — passed.

Full workspace tests are explicitly user-owned under repository governance and
are not claimed here. Raw Wasm and runtime performance were not remeasured
because C1 removes generated comparison arms and changes neither persistence
nor response work; the expected effect is neutral runtime cost and smaller
generated code for multi-entity hosts.

C2 validation completed:

- `cargo test -p icydb-core live_only --lib -- --test-threads=1` — 2 passed;
- `cargo test -p icydb-core live_schema_checkpoint --lib --
  --test-threads=1` — 2 passed;
- `cargo test -p icydb-core
  database_boot_record_malformed_corpus_fails_closed --lib --
  --test-threads=1` — 1 passed;
- `cargo clippy -p icydb-core -p icydb -p icydb-model --all-targets
  --all-features -- -D warnings` — passed;
- `cargo check -p icydb-core --no-default-features` and
  `cargo check -p icydb --no-default-features` — passed;
- `make check-invariants` — passed;
- exact retired-symbol, conceptual generated-recovery, accepted-publication
  call-site, package/dependency, and persisted-checkpoint/recovery searches
  found no generated reconstruction path or unowned production publication;
- `cargo fmt --all --check` and `git diff --check` — passed.

The first exploratory live-publication fixture run failed with persisted
format error 19 because the test bypassed the production format-admission
gate; the fixture was corrected to use `ensure_recovered`. The first
checkpoint test compile exposed one obsolete retained-ID test, which was
deleted with its retired owner. Warnings-denied clippy then found only local
style/dead-code findings in the new slice; each was corrected and the final
run above passed.

Full workspace tests remain explicitly user-owned under repository
governance. Raw Wasm and runtime performance were not remeasured. C2 adds
bounded recovery work per registered live store and one full-candidate
checkpoint write per changed live-store schema, but adds no row/query hot-path
work.

C3 validation completed:

- `cargo test -p icydb-core
  existing_recursive_named_type_removal_is_closed_and_store_local --lib` — 1
  passed;
- `cargo test -p icydb-core db::schema::application_lowering::tests --lib` —
  15 passed;
- `cargo clippy -p icydb-core --all-targets -- -D warnings` — passed after
  correcting one production import and two redundant test clones exposed by
  the first run;
- `cargo check -p icydb-core --no-default-features` — passed;
- `make check-invariants` — passed;
- exact searches for singular removal APIs, the former multi-removal
  rejection, and remaining named-type removal call sites found only the
  whole-set owner;
- `cargo fmt --all --check` and `git diff --check` — passed.

Full workspace tests remain explicitly user-owned. Raw Wasm and runtime
performance were not remeasured: C3 adds no runtime or recovery hot-path work.
Schema application now builds two bounded ID sets and validates each retained
catalog once instead of cloning and validating after each individual removal.

C4 validation completed:

- `cargo test -p icydb-core db::schema::application::tests --lib --
  --test-threads=1` — 4 passed;
- `cargo test -p icydb-core db::schema::application_store::tests --lib --
  --test-threads=1` — 8 passed;
- `cargo test -p icydb-core db::schema::application_receipt::tests --lib` — 3
  passed;
- `cargo test -p icydb-core
  forward_and_verify_progress_preserve_receipt_and_revision_invariants --lib`
  — 1 passed;
- `cargo test -p icydb-core
  database_boot_record_malformed_corpus_fails_closed --lib` — 1 passed;
- `cargo test -p icydb-core db::commit::schema_publication::tests --lib --
  --test-threads=1` — 2 passed;
- `cargo clippy -p icydb-core -p icydb --all-targets -- -D warnings` —
  passed after development runs identified one long production function, two
  deliberately end-to-end test functions, duplicate terminal validation arms,
  and one trivially copied corpus argument; all were corrected or narrowly
  justified before the final run;
- `cargo check -p icydb-core --no-default-features` and
  `cargo check -p icydb --no-default-features` — passed;
- `make check-invariants` — passed;
- exact searches for deleted failure/activation symbols, conceptual
  generated-model abort authority, application-record call sites, feature
  edges, and persisted application/recovery formats found no retired lifecycle
  owner;
- a repository-wide active format-version scan found only the three
  closeout-introduced database/application constants above 1; all three now
  use new version-1 identities, and the durability invariant rejects
  recurrence;
- `cargo fmt --all --check` and `git diff --check` — passed.

Full workspace tests remain explicitly user-owned. Raw Wasm and runtime
performance were not remeasured: C4 adds bounded control-plane work only when
admitting at receipt capacity or explicitly aborting a schema activation. It
adds no row, query, SQL, or ordinary value-codec hot-path work. The application
state machine is simpler after deleting unreachable variants; atomic abort is
the one necessary new protocol branch.

C5 validation completed:

- `cargo check -p icydb-core`, `cargo check -p icydb`, and
  `cargo check -p icydb-model` — passed;
- `cargo test -p icydb-model --lib` — 120 passed, including maintained
  validator/normalizer behavior and durable-rule separation;
- `cargo test -p icydb-testing-model-typed-adapter --lib` — 1 passed;
- `cargo test -p icydb-core --features sql --test compile` — the maintained
  core compile fixture passed;
- `cargo clippy -p icydb-core -p icydb -p icydb-model
  -p icydb-testing-model-typed-adapter --all-targets -- -D warnings` — passed;
- no-default checks for `icydb-core` and `icydb` — passed;
- `cargo check -p icydb-model --target wasm32-unknown-unknown` — passed;
- exact retired-symbol, surviving-trait call-site, core module-owner, and
  documentation searches found no maintained runtime or facade application
  behavior owner;
- `make check-invariants` — all dependency, panic, generated-build, index,
  layer-authority, mutation, durability, read-admission, schema/model, SQL
  branch, and memory-ID invariants passed;
- `cargo fmt --all --check` and `git diff --check` — passed.

Full workspace tests remain explicitly user-owned. Raw Wasm and runtime
performance were not remeasured: C5 removes an unused application surface,
changes no database execution path, and adds no runtime work; the expected
runtime effect is neutral and the code size direction is smaller.

C6 validation completed:

- `cargo check -p icydb-core --all-features --tests`,
  `cargo check -p icydb-core --no-default-features`,
  `cargo check -p icydb --all-features`, and
  `cargo check -p icydb --no-default-features` — passed;
- `cargo check -p icydb-testing-model-typed-adapter --all-features` — passed;
- accepted enum-catalog tests — 14 passed, including mutual recursion, stable
  vectors, bounds, ordering, and corruption;
- accepted composite-catalog tests — 8 passed, including resolved recursion,
  stable vectors, bounds, and corruption;
- accepted structural-field tests — 3 round-trip/rejection tests plus the
  persisted malformed corpus passed;
- primary-key component binary tests — 8 passed, including 128-bit relation
  payloads;
- predicate normalization tests — 18 passed; accepted grouped-literal tests —
  3 passed;
- `cargo clippy -p icydb-core --all-features --tests -- -D warnings` and
  `cargo clippy -p icydb --all-features -- -D warnings` — passed after the
  first core run identified two redundant closures and one missing trailing
  semicolon, which were corrected;
- `make check-invariants` — all dependency, panic, generated-build, index,
  layer-authority, mutation, durability, read-admission, schema/model, SQL
  branch, and memory-ID invariants passed;
- active format/version, retired exact-symbol, conceptual generated-fallback,
  surviving model-module, codec-call-site, and package/dependency searches
  found no retained C6 owner; domain revisions above 1 remain correctly
  distinct from format versions;
- `cargo fmt --all` and `git diff --check` — passed.

The focused `make build-sql-perf-wasm` subject build passed. Raw non-gzipped
Wasm is 5,019,962 bytes with SHA-256
`df97f504dc2cda89ca85c7e0be56838179886ce75f39cf341544bbff8b18deda`,
7,239 bytes (+0.1444%) above the pre-C6 local artifact and 137,520 bytes
(-2.6661%) below the 0.213 opening baseline. Runtime performance was not
remeasured because C6 adds no query, mutation, persistence-I/O, or recovery
operation. Full workspace tests remain explicitly user-owned.

C7 validation completed:

- `cargo test -p icydb-core --all-features primary_key_component_binary
  --lib` — 8 passed;
- `cargo test -p icydb-core --all-features leaf_field_binary --lib` — 4
  passed;
- `cargo test -p icydb-core --all-features db::ordered_overlay::tests --lib`
  — 5 passed;
- `cargo test -p icydb-core --all-features types::ulid::tests --lib` — 6
  passed;
- focused interrupted live-only recovery, marker-owned live-only publication,
  pending generated-check abort, exact-empty proof, non-scalar relation-key
  projection, and canonical registration diagnostic tests — 6 passed;
- `cargo check -p icydb-core --no-default-features`,
  `cargo check -p icydb --no-default-features`,
  `cargo check -p icydb-core --all-features`,
  `cargo check -p icydb-core --no-default-features --features sql --tests`,
  `cargo check -p icydb --all-features --tests`, and
  `cargo check -p icydb-model --target wasm32-unknown-unknown` — passed;
- `cargo clippy -p icydb-core --all-features --tests -- -D warnings`,
  `cargo clippy -p icydb --all-features --tests -- -D warnings`, and
  `cargo clippy -p icydb-model --all-targets --all-features -- -D warnings`
  — passed;
- `cargo clippy -p icydb-core --all-features --lib --no-deps --
  -D clippy::panic -D clippy::unwrap_used -D clippy::expect_used` — passed;
- `make check-invariants` — all dependency, executor-panic, generated-build,
  index, layer-authority, mutation, durability, read-admission, schema/model,
  SQL-branch, and memory-ID invariants passed;
- exact retired-symbol, conceptual authority, feature/dependency,
  persisted-format/recovery, panic, active-terminology, and version searches
  found no reachable retired owner or active format version above 1;
- `cargo fmt --all`, `cargo fmt --all --check`, and `git diff --check` —
  passed.

The focused `make build-sql-perf-wasm` subject build passed. Final raw
non-gzipped Wasm is 5,014,656 bytes with SHA-256
`db4627023ea30a152d03317adcf1b24a2f39c3db37807a3f9fe515e88f598105`,
5,306 bytes (-0.1057%) below the pre-C7 local artifact and 142,826 bytes
(-2.7693%) below the 0.213 opening baseline. Runtime performance was not
remeasured because C7 changes error propagation, feature ownership, and
failure-side control flow without adding successful query, mutation, or
persistence-I/O work. Full workspace tests remain explicitly user-owned.

## 13. Remaining Risks

No blocker or known reachable duplicate authority remains.

Exact residual risks are:

1. Invalid proposal subreasons continue to share the public
   `RuntimeUnsupported` class where the existing contract has no finer stable
   code. Correctness, mutation atomicity, and corrupt-state classification are
   preserved; adding a more granular public taxonomy is a new design decision,
   not an unfinished 0.213 authority correction.
2. The downstream reference application test
   `ops::collection::effects::tests::rarity_mode_change_preserves_vendor_policy_snapshots`
   is not present in this repository. Equivalent qualified payload-enum,
   recursive accepted-ID, finite-value, publication, persistence, and recovery
   regressions pass here; the downstream suite remains consumer-owned.
3. Full repository/workspace tests were not run because repository governance
   assigns them to the user push workflow. The focused package, feature,
   recovery, codec, invariant, Clippy, formatting, and Wasm evidence above is
   the exact validation claimed.

No compatibility shim, dual reader/writer, or migration fallback is planned.
Every current pre-1.0 format remains version 1.
