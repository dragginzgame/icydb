# 0.212 Bounded Resumable Integrity Check: Implementation Status

Status: implementation and closeout corrections complete; human push validation pending

Updated: 2026-07-24

Baseline: tag `v0.211.2`, commit
`0f9ffbe6b95d912475d47fa3169fa2ffa01e67b8`

Package version metadata remains human-owned and is not changed by design
slices.

## Landing Plan

| Patch | Status | Intended result |
| --- | --- | --- |
| 1. Inventory and accepted inspection plan | complete | One fingerprinted accepted-native artifact carries schema, catalog, and compiled row-program authority |
| 2. DTOs, database incarnation, and Quick | complete | Durable lifecycle identity plus one bounded typed Quick contract |
| 3. Bounded row phase | complete | Resumable physical row verification through accepted current and historical layouts |
| 4. Reverse derived-state phases | complete | Active forward-index and source-owned reverse-relation verification |
| 5. Journal phase and stable completion | complete | Separate progress persistence, exact proof vectors, replay, and stable terminal outcomes |
| 6. Recovery decoupling | complete | Bounded recovered-effect verification replaces the global integrity report |
| 7. SQL, documentation, and closeout | complete | Trusted typed, direct SQL, generated controller endpoint, bounded retention, measured evidence, and final protocol corrections are complete |

The original six-slice sequence placed database incarnation after the public
Quick result that requires it. The seven-patch sequence moves incarnation
ahead of Quick and keeps each landing unit independently coherent. It adds no
product scope.

## Patch 1 Result

`AcceptedInspectionPlan` now compiles from one verified accepted catalog
selection and owns:

- the accepted entity/store identity;
- the exact accepted entity snapshot;
- the store-local enum/composite catalog authority;
- the ordinary final-after-image write program;
- a deterministic fingerprint binding entity/store identity, accepted schema
  revision/version/fingerprint, accepted store-root fingerprint, and the
  inspection-semantics version.

The session accepted-schema cache now carries this one artifact rather than
parallel snapshot, identity, catalog, and compiled-program fields. Existing
query and mutation consumers continue to receive the same accepted snapshot,
catalog, schema fingerprint, and write program through the context boundary.
No public API, SQL behavior, persisted bytes, cursor format, dependency, or
package version changed.

The plan deliberately reuses the existing accepted entity fingerprint as the
commitment to canonical snapshot meaning. It does not persist a duplicate
snapshot or redundant plan-payload digest.

## Opening Bound And Owner Inventory

| Family | Current hard bound | Quick/Deep ownership |
| --- | --- | --- |
| One accepted entity snapshot, including all fields, layouts, constraints, activations, indexes, and relations | 512 KiB canonical current-form bytes; vector cardinality is therefore bounded by the same authenticated payload rather than a second count limit | Quick checks complete accepted-plan closure |
| Accepted recursive field/value structure | depth 64 | Quick checks plan/catalog closure; Rows checks values |
| Accepted check expression | depth 32, 256 nodes, 64 children, 64 membership items, 4 KiB literal bytes, 16 KiB canonical expression bytes | Quick checks program closure; Rows evaluates validated checks |
| Primary/index key shape | 4 primary-key components and 4 index-key components | Quick checks definitions; Rows/IndexEntries check physical witnesses |
| Constraint name | 256 UTF-8 bytes | Quick |
| Store-local enum and composite catalogs | 512 KiB each, inside a 16 MiB accepted bundle | Quick checks accepted root/catalog identity and plan reachability |
| Accepted schema bundle/store path | 16 MiB bundle; 4 KiB store path | Quick |
| Pending 0.211 validation job | 64 KiB, 16 store revisions, 64 findings per receipt, 32 fields per finding | Quick checks descriptor/job closure only |
| Commit marker and journal batch/control record | 16 MiB each | Quick checks current control topology; JournalTails checks every tail unit |
| Stable-memory allocation identity | complete 256-value `u8` memory-ID domain | Quick |
| Rows, active forward entries, active source-owned reverse entries, journal tails | no one-call cardinality bound | named Deep phases only |

The 512 KiB accepted entity payload is the single count owner for its contained
vectors. Adding parallel per-vector maxima would create a second admission
policy without reducing the already mandatory bounded decode. Quick must
consume the decoded accepted artifact and may not reparse an unbounded authored
graph.

### Focused Evidence

- deterministic plan fingerprinting is stable for identical authority;
- entity-schema and accepted-root fingerprint changes alter plan identity;
- catalog-revision mismatch fails closed before a plan is constructed;
- the write-admission row program compiles inside the plan rather than beside
  it in the session cache; and
- the focused `icydb-core` plan tests pass.

The repository-wide suite remains human-owned under `AGENTS.md`.

## Patch 2 Result

The current version-1 database-control envelope now carries one nonzero
`DatabaseIncarnationId` beside the transient commit-marker payload:

- virgin database initialization mints it from lifecycle authority outside the
  persisted payload;
- ordinary reopen, marker publication, marker clear, and recovery preserve it;
- zero or malformed identities fail closed;
- the accepted schema fingerprint does not include it; and
- the sole maintained control envelope is current-form only. Pre-0.212 control
  bytes have no decoder, synthesis path, or upgrade-on-open behavior.

Native initialization uses platform entropy. Wasm initialization binds the
canister identity, IC time, and a checked process-local sequence under a
domain-separated hash. The repository has no supported raw restore/import
surface; any future lane capable of reusing prior proof counters must mint and
atomically publish a new incarnation before serving the restored database.

The Quick engine now:

- resolves a registered entity through runtime routing;
- consumes the canonical accepted inspection plan rather than generated schema
  meaning;
- runs after recovery and revalidates the current control envelope;
- resolves every source-owned accepted relation through its existing active
  projection and includes the direct target store in bounded control checks;
- validates canonical heap/journaled capability and allocation shapes without
  treating well-formed volatile storage as corruption;
- consumes the committed `ic-memory` allocation capability and independently
  checks its bounded declaration closure for duplicate keys or slots;
- reads each participating journaled store's existing bounded tail/fold proof
  identity, reporting readable control drift as a typed finding and corrupt
  control bytes as `Uninspectable`;
- returns the accepted entity identity, durable incarnation, accepted schema
  version/fingerprint, typed completion state, exact finding count, bounded
  finding prefix, and exact omitted count;
- distinguishes a selected load-bearing plan failure as typed
  `Uninspectable` rather than clean completion; and
- retains only 64 findings while continuing exact checked counting.

Patch 7 now exposes the final request enum only through the trusted typed,
direct SQL, and generated controller-gated frontends. All three call this same
Quick route.

The former aggregate `IntegrityReport`, `IntegrityStoreSnapshot`, and
`IntegrityTotals` DTOs are no longer part of the public core re-export.
At this patch boundary recovery's historical scanner remained private; Patch 6
later deleted it after bounded recovered-effect verification became
authoritative. No alias or public wrapper was added.

### Focused Evidence

- current control-slot encode/decode and marker transitions preserve one
  incarnation;
- zero incarnation and malformed current control state fail closed;
- direct and canonical marker encoders remain byte-identical;
- interrupted atomic-batch recovery remains idempotent with the expanded
  control envelope;
- Quick clean results bind accepted identity and the durable incarnation;
- canonical heap storage remains clean while accepted relation Quick resolves
  its direct target-store control closure;
- valid but inconsistent journal control completes with one machine-readable
  `JournalControlMismatch` finding;
- malformed journal control returns `Uninspectable(Corruption)` rather than
  clean completion or an untyped outer failure;
- committed allocation declarations reject duplicate stable keys or slots;
- selected authority failure cannot report clean completion; and
- zero, exactly-at-cap, and one-over-cap Quick result accounting is bounded and
  exact.

The current control payload grows by exactly 16 raw bytes. No row, index,
relation, schema, journal, cursor, or marker-payload encoding changed. Raw Wasm
size and instruction comparisons remain closeout evidence because this patch
does not choose between alternative hot-path implementations.

## Patch 3 Result

The accepted inspection plan now also compiles the exact current/historical
row contract and active forward-index projection once per accepted identity.
Its one fingerprint-bound accepted row program serves both write admission and
integrity: ordinary writes evaluate accepted checks plus pending gates, while
the integrity sub-unit view selects only validated accepted checks in stable
constraint-ID order. No accepted expression is compiled twice. Integrity
loading is accepted-native: it no longer rejects a valid DDL-evolved accepted
schema merely because it differs from the current generated proposal. Typed
query and mutation routes retain their separate generated-compatibility
checks.

The internal row engine now:

- traverses only the canonical raw data-key interval for the selected accepted
  entity;
- resumes from an exact private `BeforeFirst`, `Within`, or `After` physical
  checkpoint rather than a semantic primary key;
- validates row envelopes through the accepted row-layout authority, including
  authenticated historical layouts and frozen fills;
- classifies the physical key, each active field, primary-key agreement, each
  validated accepted check, expected active forward-index entries, relation
  targets, and expected reverse-relation entries in deterministic sub-unit
  order;
- excludes pending check activation from accepted-corruption evaluation;
- records malformed prerequisites once and explicitly blocks dependent
  verifier families instead of reporting silent clean success;
- fails closed if an exact within-row resume key disappears or falls outside
  the entity interval; and
- enforces independent row, verifier-atom, finding, and loaded-row-byte bounds.

A full page is not treated as exhaustion. Exhaustion is reported only after
the canonical physical interval itself ends. The data-store visitor supplies
one bounded `RawRow` at a time; the engine does not retain the entity row set,
all keys, or all findings.

Initial accepted relations legitimately use physical generation zero, while
activation candidates require a nonzero isolated generation. The shared
relation projector now exposes distinct active and candidate constructors so
row inspection can verify both without weakening candidate admission.

### Focused Evidence

- tiny atom pages resume repeatedly within rows and eventually classify the
  exact entity interval;
- malformed row envelopes produce one typed finding and blocked dependent
  families;
- disappeared exact resume rows fail closed as corruption;
- field-path and expression-index rows report missing expected forward
  entries by stable logical index ID;
- accepted relation rows report missing expected reverse entries by stable
  relation ID; and
- the existing accepted structural reader remains the sole decoder for current
  and authenticated historical row layouts.

The row core is crate-private and has no typed or SQL application surface.
Until Patch 5 binds it to durable authorized Deep jobs, narrowly documented
`dead_code` allowances keep this independently landable patch warning-clean;
Patch 5 must remove those allowances. No persisted row, index, relation,
schema, journal, cursor, or public request format changed.

## Patch 4 Result

The accepted index and relation projectors now also own their exact active
physical domains. Logical `SchemaIndexId` and `RelationId` remain diagnostic
identity; dense ordinals and physical generations are used only to delimit
current active keyspaces. Candidate and retired generations are outside those
ranges and remain owned by their existing activation and cleanup lifecycles.

The internal derived-state engine now:

- traverses one active accepted forward-index or source-owned reverse-relation
  generation at a time in canonical raw-key order;
- resumes from the same exact private `BeforeFirst`, `Within`, or `After`
  checkpoint vocabulary as the row phase;
- validates current key/value framing and exact active-generation identity;
- point-loads each referenced authoritative source row and reuses the accepted
  structural reader plus canonical index/relation projector to prove the exact
  physical witness;
- classifies malformed, orphaned, conditional-membership-drifted, and divergent
  forward entries;
- proves unique-key multiplicity with a bounded two-witness component-prefix
  lookup, including duplicates split across page boundaries;
- classifies malformed, orphaned, and divergent source-owned reverse entries;
- enforces independent entry, atom, finding, and decoded-byte bounds; and
- materializes neither the selected row set nor a global live-key map.

The historical private diagnostic report also stopped constructing its global
entity-to-live-key map. Until Patch 6 replaces that report as a recovery
dependency, it performs direct authoritative row point lookups for reverse
references.

### Focused Evidence

- one-entry pages resume exactly across active forward domains;
- malformed values, missing source rows, stale conditional/key projections,
  and stable logical index identities produce typed findings;
- unique duplicate witnesses remain visible when the entry and unique atoms
  are separated by a `Within` checkpoint;
- source-owned reverse scans detect missing and changed source rows and retain
  stable relation identity and field paths; and
- the then-maintained diagnostic integrity tests preserved their observable
  counts after removal of the global live-key map; Patch 6 later deleted that
  transitional report surface and those report-specific tests.

The derived-state core remains crate-private and has no typed or SQL
application surface. Patch 5 must bind the row and derived cores to durable
authorized Deep jobs and remove their narrowly documented `dead_code`
allowances. No persisted row, index, relation, schema, journal, cursor, public
request, dependency, or package-version format changed.

## Patch 5 Result

Deep now has one durable internal controller over the accepted inspection
plan. Its canonical order is Quick metadata, rows, active forward-index
domains, active source-owned reverse-relation domains, every participating
journal tail, and the final unchanged-proof check. The controller:

- captures and persists one typed proof vector containing database
  incarnation, accepted-plan identity, database control, allocation
  generation, participating store generations/journal identities, and active
  index/relation generations;
- requires exact A/B proof equality before idempotent start and exact pre/post
  equality around every bounded page;
- discards candidate findings and checkpoint advancement on proof drift before
  persisting terminal `Invalidated`;
- keeps private raw checkpoints, cumulative counters, blocked-family state,
  the exact replay key, and one bounded receipt inside one current-form job;
- replays lost start/page/terminal responses without scanning or advancing
  twice;
- preserves the outstanding page through abort or inactivity expiry by first
  persisting `TerminalPending`, then emits the terminal receipt only after the
  exact page acknowledgement;
- persists terminal acknowledgement and deletes only acknowledged terminal
  records after the frozen retention window; and
- rejects heap Deep explicitly until a trustworthy heap runtime epoch exists.

The journal phase validates current envelopes, sequence continuity, fold
watermark agreement, and non-reused batch identity. Duplicate identity proof
is constant-memory and resumable: one candidate batch is compared against
earlier live sequences under two-batch/decoded-byte page bounds rather than
retaining an unbounded ID set. A malformed batch, sequence gap, or repeated
batch identity is a bounded typed finding and traversal continues from an
exact checkpoint; malformed prior batches also block the batch-identity
verifier family. Corrupt ordered-container topology that prevents identifying
the next physical batch remains terminal `UninspectableStorage`.

`InspectionProgressStore` occupies a separately declared stable-memory
allocation and never participates in an inspected store's marker, journal,
mutation revision, allocation generation, or proof vector. It has one
current-form version-1 header and checksummed job envelope, a 512 KiB record
bound, 64-job global capacity, eight-job owner capacity, bounded 16-record
retention pages, and fail-closed job/state/proof/checkpoint validation.
Decoded checkpoints must name a verifier family owned by their phase, a
captured index/relation/store ordinal, and a journal sequence inside the
captured fold/append interval. Corrupt individual job records remain isolated
from database recovery.

The schema/build contract now reserves and collision-checks the progress
memory ID and stable key. Maintained canister fixtures allocate one new memory
slot; no existing row, index, relation, accepted-schema, journal, cursor, or
commit-marker payload changed. The progress header/job envelope is a new
diagnostic-progress persisted surface, not a compatibility reader for any
older form.

The Patch 3/4 row and derived cores are now reachable only through the durable
controller in production. Their direct page helpers are test-only, their
temporary `dead_code` allowances were removed, and Patch 7 now exposes the
controller only through its authorized typed and SQL frontends.

### Focused Evidence

- exact bounded journal traversal covers empty, chunked, resumed,
  duplicate-identity, gap, malformed, and fold-boundary cases;
- a journal envelope corrupted without changing proof counters produces a
  typed finding, blocks batch-identity proof, and reaches stable
  `DeepCompleteWithFindings`;
- proof capture is stable without mutation, changes after a durable write, and
  rejects heap storage without a runtime epoch;
- start and page response loss replay exact cached receipts;
- the empty accepted fixture traverses every phase and acknowledges one clean
  terminal receipt;
- proof drift after start terminalizes as `Invalidated` with no candidate
  findings;
- abort and inactivity expiry preserve the outstanding receipt until exact
  acknowledgement;
- acknowledged terminal retention deletes the job while unacknowledged
  terminal state remains;
- fair maintained retention reaches jobs beyond the frozen 16-record page
  bound and wraps only after exhausting the progress keyspace;
- wrong owners, corrupt current job bytes, and ninth retained owner jobs
  reject with typed failures; and
- decoded phase-family and journal-interval checkpoints cannot skip captured
  work; and
- focused all-feature `icydb-core` checking and Deep/journal tests pass.

Repository-wide tests, all feature combinations, Wasm builds, raw Wasm size,
and instruction measurements remain human-owned closeout evidence under
`AGENTS.md`.

## Patch 6 Result

Recovery no longer invokes a whole-database integrity scan after replay.
Before marker clearance it now verifies only the bounded final effect set
already owned by the recovered marker:

- every final marker-owned row put matches exact authoritative row bytes;
- every final row delete is absent from authoritative row storage;
- rebuilt forward-index and reverse-relation effects for recovered row puts
  match the accepted rebuild projection exactly;
- persisted schema snapshots, accepted-schema publication identity, and
  constraint-validation job changes match their recovered records;
- every marker batch sequence is covered by the durable fold watermark; and
- every journaled store has no physical tail batch remaining after fold.

The verifier walks at most the existing 16 MiB marker payload and retains one
bounded identity per final logical effect. Journal closure is one ordered-map
lookup per registered journaled store. Row deletes require no old-row
reconstruction: the preceding canonical rebuild cleared derived storage and
rebuilt it solely from rows that remain authoritative.

The former aggregate integrity scanner, report DTOs, and tests that preserved
that superseded surface were deleted outright. Explicit Quick and Deep
inspection remain the sole integrity workflows; recovery neither invokes nor
aliases them.

### Focused Evidence

- recovered marker effects pass after normal row/index rebuild;
- deleting a recovered authoritative row makes verification fail closed;
- deleting one recovered derived index entry makes verification fail closed;
- existing marker/fold/index-rebuild interruption tests still cross the
  verification boundary before marker clearance; and
- focused all-feature `icydb-core` checking and recovery tests pass.

No public API, SQL behavior, dependency, package version, accepted-schema,
row, index, relation, journal, cursor, or marker encoding changed. The old
aggregate report removal reduces internal code and authority count.

## Patch 7 Typed Surface Result

The trusted typed frontend now exposes one `IntegrityCheckRequest` enum with
exact `Quick`, `DeepStart`, `DeepContinue`, and `DeepAbort` variants over the
existing durable controller. Quick and Deep start pin the generated entity
selector's tag, path, and store path, then require an exact match with current
accepted authority. Continue and abort accept only the engine-issued job ID;
private phase and checkpoint state remain inside the progress record.

`DbSession::execute_admin_integrity` requires the caller to supply a bounded
stable owner identity after enforcing controller or equivalent
integrity-specific authorization. The same owner is checked for start replay,
continuation, terminal acknowledgement, and abort. Candid-decoded entity,
owner, submission-key, and job-ID inputs are revalidated at execution instead
of trusting Rust constructors.

The public facade preserves job-protocol failures as typed
`IntegrityCheckError::Job` variants and maps accepted-authority or physical
engine failures through the normal typed database error payload. Existing
Quick and Deep lifecycle tests now exercise every public request variant. No
second integrity runner, raw checkpoint input, SQL behavior, persisted format,
or package version was added.

## Patch 7 Direct SQL Result

The direct administrative SQL frontend accepts exactly the four design forms:
entity-bearing Quick and Deep start, plus entity-free Deep continue and abort.
It uses the existing reduced-SQL lexer and one dedicated grammar, then lowers
directly into `IntegrityCheckRequest`. SQL returns the same
`IntegrityCheckResult` receipts and owns no report DTO or execution runner.

Quick/start entity names resolve only to registered runtime selectors. The
typed controller then performs its existing exact tag, entity-path, store-path,
and accepted-plan validation before inspection. Continue and abort accept a
64-character hexadecimal engine-issued `IntegrityJobId`; malformed, zero,
unknown, wrong-owner, and stale identities retain typed protocol failures.
SQL cannot provide a phase, physical checkpoint, proof vector, predicate,
projection, ordering, limit, or extra continuation entity.

Focused parity coverage starts through one frontend and replays or advances
through the other, proving exact Quick results and Deep receipts rather than
comparing independently reconstructed output. Parser coverage rejects missing,
extra, negative, and unsupported forms. Generated endpoint and config exposure
were kept outside this direct-API slice instead of temporarily routing
integrity through query or DDL capability.

## Patch 7 Generated Endpoint Result

`[canisters.<name>.sql] integrity = true` now emits the distinct
controller-gated `icydb_integrity` update endpoint; the default remains off.
The endpoint derives one bounded stable job owner from the authenticated
caller principal, maps authorization/bootstrap failures into the existing
typed SQL/integrity envelope, and calls `execute_admin_integrity_sql(...)`.
It has no entity-specific dispatch arms, report DTO, checkpoint input, or
runner of its own.

Config parsing, build options, generated token contracts, the SQL canister
fixture, and wasm endpoint-surface reporting all carry the same explicit flag.
Generated-source checking proves the configured canister compiles, while
token guards prove controller authorization and caller binding occur before
canonical integrity dispatch.

## Patch 7 Bounded Retention Result

Every well-formed authorized typed or direct SQL integrity attempt now runs one
progress-only maintenance page after the requested operation. Each page
visits at most 16 retained job records, expires idle jobs by persisting
`TerminalPending(Expired)`, and removes only terminal records whose receipt
acknowledgement and retention deadline are already durable.

The maintained scan uses one heap-only cursor per progress allocation so
records after the first page cannot starve. Exhaustion wraps the cursor to the
first key; ordinary restart safely loses only that scheduling hint and resumes
from the first key. The cursor is not persisted authority, is absent from job
or proof formats, and cannot change inspection results. Requested operations
run before maintenance so an abort or continuation result does not depend on
the advisory cursor position. A maintenance failure remains typed and
fail-closed.

Focused evidence covers 17 retained jobs across the 16-record page boundary,
wraparound without repeated expiry, and a real typed Quick request freezing an
expired job before a later abort observes the existing expiry reason.

## Patch 7 Contract Closure Result

The maintained read-admission, write-admission, durability, referential
integrity, SQL subset, persisted-format inventory, foundations, and durability
operations documents now describe the implemented Quick/Deep authority and
claim boundaries. The operator workflow records exact acknowledgement/replay,
expiry, invalidation, volatile-heap, and no-repair semantics. Stale wording
that described integrity inspection as future work was removed.

Focused all-feature and no-default core checking, warnings-denied core/facade
Clippy, the 28-test session integrity selection, allocation-registry closure,
the live controller-gated generated endpoint, formatting, diff hygiene, and
the relevant documentation/build invariant guards pass.

## 0.212.1 Closeout Measurements

The maintained measurement subject is the `sql` canister built with the
repository `wasm-release` profile and SQL enabled. The released `v0.212.0` tag
and the later `b1d77c788` 0.212.0 implementation baseline both produce
3,951,719 raw bytes and 3,678,441 canonical shrunk bytes, so the post-tag
recovery correction does not change this Wasm subject's reachable code.

The released `v0.212.1` subject measures:

| Artifact | 0.212.0 bytes | 0.212.1 bytes | Delta |
| --- | ---: | ---: | ---: |
| Raw non-gzipped Wasm | 3,951,719 | 4,346,802 | +395,083 (+10.00%) |
| Canonical shrunk Wasm | 3,678,441 | 4,044,544 | +366,103 (+9.95%) |
| Deterministic raw gzip | 1,397,791 | 1,507,023 | +109,232 (+7.81%) |
| Canonical shrunk gzip | 1,346,211 | 1,457,739 | +111,528 (+8.29%) |

The enabled surface adds exactly the generated `icydb_integrity` export. The
raw subject adds 456 functions and 15,688 data-section bytes. Raw non-gzipped
bytes remain the primary size verdict; gzip is secondary context.

A live PocketIC run over 32 journaled rows measures the public administrative
SQL surface and the canonical controller:

- Quick: 318,191 canister-local instructions and a 993-byte Candid result;
- Deep start and seven continuations: 6,991,392, 11,249,081, 11,436,434,
  11,464,123, 11,459,800, 11,417,435, 11,282,312, and 11,083,812
  canister-local instructions;
- largest live Deep Candid result: 752 bytes;
- retained memory `(Wasm, stable)` moves from `(1,835,008, 83,951,616)` to
  `(1,835,008, 92,340,224)` after Quick and
  `(1,900,544, 92,340,224)` after Deep.

The 8 MiB stable-memory increase is the dedicated progress allocation's
retained capacity. The completed run retains one additional 64 KiB Wasm page.
Canister status reports retained memory, not peak temporary heap; this evidence
therefore does not claim a peak-heap measurement.

The canonical native codec run measures a 945-byte clean Quick response, a
752-byte largest clean Deep response, and a 1,772-byte largest persisted job
record across the same complete lifecycle. The maintained guards cap Quick at
2 million local instructions, each Deep call at 30 million, responses and job
records at 512 KiB, and retained memory growth at 16 MiB.

Source evidence from `v0.211.2` through `v0.212.1` is 83 code/test files at
approximately `+11,059/-1,239` and 26 documentation files at approximately
`+9,708/-281`. The 0.212.1 implementation range from `b1d77c788` contains 32
code/test files at `+2,001/-246` and 13 documentation files at `+365/-31`,
before release-only metadata. Complexity increased materially because bounded
physical inspection and durable replay did not previously exist. Authority
remains concentrated in the five designed owners: accepted inspection plan,
Quick control closure, Deep phase machine, physical phase engines, and the
independent progress store; no second scanner or lifecycle runner was added.

## Accepted-check And Proof-invalidation Measurements

The next bounded evidence slice keeps the same 32-row journaled subject and
compares a clean accepted catalog with one promoted
`CHECK (age >= 0)` constraint:

| Subject | Quick instructions | Deep page instructions | Quick + Deep total |
| --- | ---: | --- | ---: |
| No accepted check | 321,640 | 6,982,192; 11,249,345; 11,423,597; 11,453,436; 11,454,186; 11,400,334; 11,305,321; 11,099,621 | 86,689,672 |
| One accepted check | 332,473 | 7,007,194; 11,285,626; 11,532,304; 11,556,772; 11,548,861; 11,566,663; 11,386,859; 11,141,339; 11,124,841 | 98,482,932 |

The accepted check adds one bounded Deep call on this fixture. Total
instructions increase by 11,793,260 (+13.60%), while the most expensive page
increases by 112,477 (+0.98%). Quick remains below 333 thousand instructions.
Both live result shapes remain bounded at 993 Quick bytes and at most 752 Deep
bytes.

Activation setup leaves a journal tail that must be folded before steady-state
inspection is comparable. A same-Wasm upgrade followed by the maintained
guarded-reentry probe measures that cold recovery separately at 7,689,929,923
instructions, within its existing 8-billion-instruction bound. The accepted
check figures above are sampled only after that fold and guarded re-entry, so
they do not disguise recovery as expression-evaluation cost.

A separate proof-drift scenario starts Deep inspection, performs an ordinary
journaled mutation, and then continues the job. Start uses 6,707,959
instructions; the continuation returns the typed `Invalidated` terminal in
11,712,932 instructions and 847 response bytes. It cannot falsely complete or
publish findings from the discarded proof attempt.

This slice changes only the audit canister, integration evidence, status, and
Unreleased release note. It adds no production owner, persisted format, public
IcyDB API, or runtime fallback.

## Many-index Measurements

The maintained `PerfAuditUser` scale subject exercises 16 rows and three active
indexes: one scalar equality index, one ordered composite index, and one
expression index. A complete clean run measures:

- Quick: 21,844,951 instructions and 975 response bytes;
- Deep: 11 calls ranging from 7,180,953 to 12,786,872 instructions;
- Quick plus Deep: 150,755,658 instructions;
- largest Deep response: 752 bytes; and
- isolated same-Wasm guarded recovery: 5,822,022,365 instructions.

Receipt phases prove the run visits Quick metadata, four row pages, three
active-index pages, journal tails, and the final proof boundary. The existing
30-million Deep-page and 8-billion guarded-reentry ceilings remain sufficient.
The richer accepted catalog makes Quick materially more expensive than the
simple primary-key-only subject, so this evidence adds a reviewed 30-million
audit regression ceiling for complex-catalog Quick while retaining the
2-million simple-subject guard. These are measurement guards, not new
production admission limits.

The subject reuses the existing scale fixture and canonical active-index
definitions. This extension changes only integration evidence and status;
production IcyDB code and raw production Wasm are unchanged.

## Relation Measurements And Projection Ownership

The audit schema now contains one minimal accepted relation: 16 source rows
reference eight of 16 target rows, producing 16 active reverse witnesses. A
complete clean run proves `Rows`, `ReverseRelations`, `JournalTails`, and final
proof phases and measures:

- cold relation-plan Quick: 194,253,310 instructions;
- warm Quick: 325,162 instructions;
- seven Deep calls ranging from 7,214,007 to 12,542,633 instructions;
- warm Quick plus Deep: 78,247,265 instructions;
- 985-byte Quick and at most 752-byte Deep results; and
- isolated same-Wasm guarded recovery: 12,418,322,940 instructions.

The first measurement exposed duplicate target-schema and relation-projection
construction before proof, during physical inspection, and after proof. Deep
pages reached 538,552,621 instructions even though every copy represented the
same source accepted fingerprint. `AcceptedInspectionPlan` now compiles the
active source-owned relation projections once beside its row, check, and index
artifacts. Proof capture, row verification, and reverse verification borrow
that one projection while retaining their independent pre/post store,
generation, journal, and database-incarnation checks.

The hottest relation Deep page falls by 97.67% to 12,542,633 instructions.
Cold plan construction remains explicit rather than being hidden by warm-up;
the audit guards it at 250 million instructions, warm complex Quick at
30 million, each Deep page at 30 million, and relation-enabled guarded recovery
at 15 billion. No public API, cursor, row/index/relation encoding, progress
format, or compatibility path changes.

## Live Journal-tail Measurements

A fixed audit fixture writes six journaled rows as six separate commits and
then runs Deep inspection without folding the tail. This differs from the
steady-state accepted-check, many-index, and relation measurements, which
isolate recovery first so an empty tail does not contaminate their subject
cost. The live-tail run measures:

- Quick: 347,923 instructions and 993 response bytes;
- Deep: 20 calls totalling 221,383,805 instructions;
- Quick plus Deep: 221,731,728 instructions;
- 16 receipts in the `JournalTails` phase;
- hottest Deep call: 11,366,936 instructions; and
- largest Deep response: 752 bytes.

Production journal inspection admits at most two decoded batches per page.
The 16 journal receipts are therefore expected rather than redundant: each
candidate batch is compared incrementally with earlier live batch identities,
and the exact comparison checkpoint travels forward without retaining an
unbounded identity set. The run reaches the final unchanged-proof phase and
completes cleanly. Every call remains below the existing 30-million Deep-page
guard; no production admission limit or journal representation changes.

## Finding-saturated Page Evidence

A focused native controller test constructs 32 current accepted unique-index
witnesses whose authoritative source rows are absent and whose logical unique
key is duplicated. The canonical derived-state page classifies two independent
findings per witness: 32 `OrphanIndexEntry` findings and 32
`DuplicateUniqueIndexKey` findings. This reaches the maintained 64-finding
receipt ceiling exactly through typed Deep start/continue, durable progress
persistence, and lost-response replay.

The saturated receipt encodes to 20,911 bytes and its persisted job record to
22,032 bytes, both well below the maintained 512 KiB bound. Repeating the same
acknowledgement returns the exact persisted 64-finding receipt; cumulative
`findings_seen` remains 64 rather than double-counting the replay.

The corruption setup stays inside the native test boundary. Adding a canister
instruction sample would require exposing a raw physical-corruption hook in a
non-test IcyDB build, which would weaken the production surface merely to
measure an impossible healthy ingress. The live canister evidence therefore
covers clean, accepted-check, active-index, relation, journal-tail, and
proof-invalidation costs, while this focused native boundary supplies the
exact maximum finding-response and durable-record evidence.

The cumulative post-`v0.212.2` audit subject measures:

| Artifact | `v0.212.2` bytes | Current bytes | Delta |
| --- | ---: | ---: | ---: |
| Raw non-gzipped Wasm | 5,078,585 | 5,170,139 | +91,554 (+1.80%) |
| Canonical shrunk Wasm | 4,728,944 | 4,815,148 | +86,204 (+1.82%) |
| Deterministic raw gzip | 1,612,656 | 1,635,858 | +23,202 (+1.44%) |
| Canonical shrunk gzip | 1,573,624 | 1,598,111 | +24,487 (+1.56%) |

Raw non-gzipped bytes remain the primary size verdict. This cumulative delta
includes the accepted-check validation endpoint, relation fixture and loader,
live-tail fixture, measurement paths, and plan-owned relation projection.

Before release-only changelog metadata, the cumulative implementation and
evidence changes 13 files at approximately `+939/-121`. Production core
changes are approximately `+85/-49`; the net addition is the plan-owned
relation field, compilation boundary, and focused test seam, while the three
downstream projection-reconstruction paths and their forwarding helper are
removed. Runtime owner count decreases and the execution shape is simpler
despite the added relation and journal-tail fixtures and evidence. The
additional 160-line native test is bounded evidence rather than runtime code.

## Final Closeout Correction

The final read-only review found one incomplete Deep failure transition and
several protocol descriptions that exceeded the maintained synchronous
controller:

- Deep start now returns typed `Uninspectable` when accepted inspection-plan
  authority can be identified but not loaded, and publishes no job;
- A/B proof drift returns typed `StartInvalidated` rather than incorrectly
  claiming a database-incarnation mismatch;
- accepted-plan load failure during continuation atomically persists a
  replayable terminal `Uninspectable` receipt instead of leaving the job live;
- the unreachable busy-state error was removed because the maintained
  non-yielding controller cannot expose concurrent in-flight advancement;
- Quick's unreachable physical-container outcome was removed because ordered
  physical traversal belongs only to Deep; and
- the design's receipt projection, abort/expiry table, and retained-memory
  evidence now describe the implemented single job authority exactly.

Focused controller evidence proves rejected starts occupy no submission
identity, proof-drift starts are typed, continued plan failure terminalizes and
replays, and advanced/conflicting start retries retain their exact protocol
errors. This correction changes no accepted-schema, row, index, relation,
journal, cursor, commit-marker, or integrity-progress persisted format.

## Next Step

Run the human-owned repository push suite. No further 0.212 implementation
slice is currently identified; peak temporary heap is deliberately not claimed
because the maintained IC status surface reports retained memory and no
production allocation hook is justified solely for audit sampling.
