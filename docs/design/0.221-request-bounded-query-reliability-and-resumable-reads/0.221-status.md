# IcyDB 0.221 Implementation Status

Status: implementation underway; Patches 1-5 complete

Design: [0.221-design.md](0.221-design.md)

Design cut: 2026-08-05

## Implementation Baseline

- Line baseline: `v0.220.2`
- Commit: `1432d9877b5854d0b03ef636c510aed02905b938`
- Tree: `dc76fe7d0502f153007695cbee71262a7999fc4e`
- Current landing baseline: pushed `0.221.3` Patch 5 handoff
- Current landing commit: `06bfeba7623514dd36ceeea740e8a880104f2d5b`
- Current landing tree: `201c1506bdca239861119630ae5f759c692f44a4`
- Branch: `main`
- Workspace package version: `0.221.3`
- Baseline worktree state: clean before each landed patch
- Rust: `rustc 1.97.1 (8bab26f4f 2026-07-14)`
- `ic-wasm`: `0.11.0`
- Binaryen: `wasm-opt version 108`

The user confirmed the complete 0.220 test/build/Wasm matrix before starting
0.221 and confirmed `0.221.2`, containing Patches 3 and 4, as pushed. Patch 5
used that release as its matched measurement baseline. The user has since
published `0.221.3`, containing Patch 5 and host-fixture improvements; that
clean tagged handoff is the current implementation baseline. Release tooling
remains the version owner and this patch does not edit Cargo versions.

## Patch 1 Evidence — Accepted Runtime Root

- One `AcceptedSchemaRuntimeRoot` now captures every registered store root and
  compiles all accepted entity inspection plans, `SchemaInfo` tables, row
  layouts, fingerprints, and executor authority before publishing one `Rc`
  handle. A context retains that complete root revision.
- Runtime-root identity binds the durable database incarnation plus the
  canonical ordered set of present and absent store roots. Structural plan and
  SQL compile cache keys also bind that complete identity, so unchanged entity
  fingerprints cannot reuse a plan across a different accepted root.
- Warm lookup revalidates compact persisted store-root records. It does not
  encode or hash accepted entity snapshots. SQL DDL invalidates the whole root,
  while ordinary publication/recovery drift is detected by root revalidation.
- The former per-entity query cache, per-context lazy `SchemaInfo`, repeated row
  layout construction, and projection-time accepted snapshot fingerprinting
  are removed. Accepted schema snapshots remain the sole runtime authority.
- Counter-backed native coverage executes 1,000 point queries after warm-up
  and observes zero accepted snapshot fingerprint builds, runtime-root identity
  hashes, entity-runtime compilations, or root publications. Separate tests
  prove atomic replacement, retained old-root contexts, canonical store-root
  identity, and mixed-root plan-cache rejection.

The maintained audit canister now exposes bounded point and ordered empty-scan
loops for this fixed-cost workload. The baseline subject uses the exact
`v0.220.2` core plus the same measurement endpoint and Candid contract. PocketIC
15 reports local canister instructions inside each 1,000-query loop:

| Shape | `v0.220.2` | Patch 1 | Delta |
| --- | ---: | ---: | ---: |
| Repeated exact point query | 374,226,936 | 243,649,156 | -130,577,780 (-34.8927%) |
| Repeated ordered empty scan | 348,175,956 | 216,398,832 | -131,777,124 (-37.8479%) |

The matched measurement-fixture Candid is byte-identical at 428 bytes with
SHA-256 `800ff556b34ffccae48e99a6d913f8293ef2d3c1b93a1d927e52fe9457a7ee0a`.
Raw non-gzipped final Wasm grows from 2,432,420 bytes to 2,434,132 bytes:
`+1,712` bytes (`+0.0704%`). Baseline and candidate SHA-256 values are
`8b54c78670dda204661f9ab5d8bfdbdf00b8e9f5d43ff31337ba1905e8a53345`
and `4b06fdd5c91ea24ddcc885869fd1ff93e2b542ba0d3a1f200362bebaf4294fdf`.
The byte delta is directional because the archived baseline used an isolated
workspace/target root; the instruction comparison uses identical source
workload, Candid, compiler profile, post-link transform, and PocketIC version.

Focused Patch 1 validation passed the all-feature core check and warning-denied
core lint, the warning-denied measurement-canister lint, all five runtime-root
unit tests, the maintained typed/dynamic query-canister PocketIC regression,
the ignored matched instruction measurement, and the raw-Wasm/Candid report.
Repository-wide suites remain user-owned by project policy.

The patch touches 41 files at approximately `+1,144/-446` lines including
tests, the measurement fixture, design/status evidence, and the 0.220 closeout
record. The implementation shape is more complex because database-wide root
publication and mixed-generation rejection are new authority contracts; warm
query setup is simpler because it now borrows one compiled root.

## Patch 2 Evidence — Native Bounded Key Batches

- `DbSession::get` and `DbSession::get_many` accept entity-bound `Id<E>` values
  and execute their primary-key reads directly against the accepted store.
  They do not construct a `DynamicQuery`, compile a plan, or populate the
  general plan cache.
- Generated scalar and composite entities implement the existing `EntityKey`
  contract. `get_many` returns exactly one `Option<Row>` per input key in
  request order. Missing positions remain `None`; duplicate positions remain
  duplicated in the result while sharing one stored-row fetch and decode.
- One batch admits at most 1,024 positions, 256 KiB of canonical stored-key
  bytes before deduplication, 4 MiB of raw rows across distinct keys, and
  4 MiB of logical projected result bytes. Count and input bytes charge the
  original request; physical reads/raw bytes charge distinct keys; result
  bytes charge every original position, including duplicates.
- Exact reads validate one current accepted typed binding, borrow the Patch 1
  row contract and schema tables, resolve one store handle, and validate each
  found row's persisted primary key before projection. Typed-binding issuance
  and currentness checks also stopped rebuilding accepted row-layout state.
- New bounded numeric diagnostics distinguish item, input-byte, stored-byte,
  and result-byte exhaustion. The CLI renders each boundary explicitly rather
  than relying on a wildcard. Stale bindings fail closed without accessing a
  store. No persisted schema, row, index, journal, or cursor format changed.
- Focused coverage proves scalar and generated composite key wiring, exact
  ordering, missing and duplicate positions, one physical read per distinct
  key, stale-root rejection, every admission bound, and matching heap and
  journaled behavior. The maintained generated adapter test exercises both
  `get` and `get_many` without SQL.

PocketIC 15 measures the old per-key dynamic loop and one native batch inside
the same canister binary. Both workloads request 1,000 missing keys:

| Shape | Dynamic loop | Native batch | Delta |
| --- | ---: | ---: | ---: |
| 1,000 distinct keys | 895,455,213 | 40,555,935 | -854,899,278 (-95.4709%) |
| One key repeated 1,000 times | 250,988,840 | 6,606,844 | -244,381,996 (-97.3677%) |

The standard one-entity typed audit canister hard-cuts its point read from the
dynamic planner to `get`. Against the clean `v0.221.0` worktree, final raw
non-gzipped Wasm falls from 2,363,342 bytes (SHA-256
`cf24bc535b7da04298a938bd4bd9004832512b98874a4679969c8f922e479ac3`) to
1,785,015 bytes (SHA-256
`ea29621fe5bbd6a5d09a97a61af15c668992d3d187cffca868b50fcc9f8bbd6b`):
`-578,327` bytes (`-24.4707%`). The Candid contract is byte-identical at 64
bytes with SHA-256
`1005c63f5415d1de4bb7da89190f33212d1c926c881108a1acfeadfa93fff766`.

Focused Patch 2 validation passes warning-denied all-feature core, facade,
macro, CLI, and audit-canister lint; the exact-key resource, diagnostic, and
CLI rendering tests; heap/journal and stale-root tests; generated-adapter
tests; the query-canister PocketIC regression; the ignored instruction
measurement; and the matched raw-Wasm/Candid report. Repository-wide suites
remain user-owned push validation. The patch touches 29 files at approximately
`+1,122/-49` lines.
The implementation is moderately more complex because exact reads now own a
bounded execution/result contract; callers and retained canisters become much
simpler because point reads no longer construct planner requests.

## Bounded Patch 2 Follow-up — Typed Filter Atom Rehydration

- The accepted-schema filter canonicalizer now rehydrates the five previously
  incomplete string-backed atoms: `Blob`, `Date`, `Duration`, `Subaccount`, and
  `Timestamp`. Comparisons, `IN`/`NOT IN`, list/set containment, and relation
  keys share that authority.
- Blob filters now use bounded hexadecimal text and duration filters use exact
  millisecond text instead of debug formatting. Malformed, wrong-width, and
  oversized hexadecimal input fails before decoded allocation.
- The remaining scalar families were audited: account, decimal, finite floats,
  signed/unsigned integers, principal, text, ULID, bool, and unit already use
  reversible canonical forms; enums continue through accepted-catalog
  admission; maps remain deliberately non-queryable.
- Focused all-feature tests cover representation, exact rehydration, malformed
  input, relation keys, collection elements, and Subaccount primary-key codec
  round trips. Warning-denied all-target core Clippy and the no-default-feature
  core check pass. No persisted, Candid, cursor, or successful-result shape
  changes.

## Patch 3 Evidence — Bounded Parameterized Templates

- Fully covered equality, ordered-range, and non-empty `IN` predicates now
  derive typed value-independent contracts after accepted-schema
  normalization. Equality and varying-width `IN` exact sets share one list
  shape; unsupported, over-64-slot, over-1,024-item, or over-256-KiB predicates
  keep the literal-specific planning path.
- Dynamic and SQL queries converge on the same accepted-root, schema,
  visibility, operator/type, projection, ordering, and window identity. A hit
  rebuilds current literal bounds from the retained selected-index authority;
  one replaceable bound-plan memo preserves the repeated-identical-query
  control without retaining one plan per literal.
- The shared plan/template cache is deterministic FIFO with a 1,024-entry cap
  and a 4 MiB retained-template allowance. Each template is conservatively
  charged 24 KiB plus its key charge; insertions, evictions, and rejected
  oversize residents are attributed explicitly.
- Cursor continuation signatures still bind the current normalized predicate,
  so two executions sharing one template cannot exchange cursors. Wrong-typed
  values fail schema validation before reuse, and no removed prepared-session
  API or compatibility path returns.

PocketIC 15 measures 1,000 executions per shape against a clean `v0.221.1`
baseline carrying the identical measurement endpoint:

| Shape | `v0.221.1` | Patch 3 | Delta |
| --- | ---: | ---: | ---: |
| Repeated identical point query | 243,649,156 | 244,587,340 | +938,184 (+0.3851%) |
| Distinct-literal point queries | 850,779,546 | 416,400,274 | -434,379,272 (-51.0566%) |
| Repeated ordered empty scan | 216,398,832 | 217,039,009 | +640,177 (+0.2958%) |

The matched Candid remains byte-identical at 612 bytes with SHA-256
`6b0b35b9498df3dd69731bf3883160ab6a61258546274d8c04bbda1c17e4c12a`.
Raw non-gzipped final Wasm grows from 2,435,362 bytes (SHA-256
`576e4c4178458830980f37b7123acac6efdc24f625bda9a2f7509ee95ff9a3da`)
to 2,441,943 bytes (SHA-256
`b3a0f18517d27ac9ffe582915e3b6263e7999b53d0d70271843e759f87e7dc95`):
`+6,581` bytes (`+0.2702%`), within the Patch 3 `+8,192`-byte gate. Gzip
grows from 953,108 to 956,317 bytes as secondary context.

Focused Patch 3 validation passes warning-denied all-feature core lint, eleven
parameter/template/cursor tests, four weighted-cache tests, the maintained
query-canister PocketIC benchmark, and the matched raw-Wasm/Candid report.
Repository-wide suites remain user-owned push validation. The patch touches
19 implementation and test files at approximately `+1,344/-53` lines before
this status/changelog handoff. The implementation is moderately more complex
because shared planning now has an explicit value-independent template and
bounded resident contract; distinct-value execution is simpler and materially
cheaper because it no longer repeats full schema/index planning.

## Patch 4 Evidence — Hard Budget Model And Charge Ownership

- `HardExecutionBudget` now owns finite ceilings for 21 physical resource
  families, an explicit typed scope and lane, one literal-free normalized-shape
  fingerprint prefix, and reserved typed-failure headroom. `precharge` and
  periodic charging share one monotonic counter path. Failed charges and
  arithmetic overflow retain saturated usage; there is no refund operation.
- `ExecutionBudgetExceeded` is public diagnostic leaf E273. It carries the
  exhausted resource, limit, attempted cumulative usage, execution/request
  scope, public/trusted/diagnostic/mutation/recovery lane, and normalized shape
  prefix. It carries no query literal, key, row, or payload. Host rendering
  gives every compact resource, scope, and lane a maintained label.
- The planner-free exact-key batch is the reference execution. One batch
  charges requested result positions before deduplication, temporary encoded
  key bytes, distinct physical key visits, found rows and stored bytes,
  decode/materialization work, projected value traversal, and logical result
  bytes. Existing exact-key item/input/stored/result boundary diagnostics stay
  authoritative when their specialized cap is the first failure.
- The exact-key profile declares a 4.5-billion instruction-work ceiling plus
  500-million instruction units and 64 KiB of response capacity reserved for a
  typed failure. Patch 5 still owns actual instruction-watermark checks and
  complete physical-route instrumentation; Patch 6 still owns the one
  non-resettable request root. This patch therefore does not claim that all
  IcyDB work, or an application endpoint as a whole, is already trap-proof.
- Counter tests cover all 21 resources, precharge and periodic increments,
  retained failed work, overflow, failure headroom, fact-schema validation,
  and exact CLI labels. A heap-backed exact-key integration test proves the
  real route returns E273 under an injected zero allowance. Existing heap and
  journaled duplicate/missing/order semantics remain unchanged.

PocketIC 15 measures the 1,000-key exact batch against the clean pre-budget
Patch 3 handoff commit using byte-identical measurement Candid and the same
audit-only Wasm feature profile:

| Shape | Patch 3 handoff | Patch 4 | Delta |
| --- | ---: | ---: | ---: |
| 1,000 distinct missing keys | 37,314,077 | 37,488,673 | +174,596 (+0.4679%) |
| One missing key repeated 1,000 times | 6,497,620 | 6,479,398 | -18,222 (-0.2804%) |

The two dynamic-query control loops remain exactly 416,583,991 and
248,567,656 local instructions, isolating the delta to exact-key accounting.
The production one-entity typed-query Candid remains byte-identical at 64
bytes with SHA-256
`1005c63f5415d1de4bb7da89190f33212d1c926c881108a1acfeadfa93fff766`.
Raw non-gzipped final Wasm grows from 1,784,867 bytes (SHA-256
`bfb22054dcf892648c8e5ba9e22d36f94f4cbff85a082f0f4185c499755b18bb`)
to 1,786,868 bytes (SHA-256
`d4296b332e44c52503500124a3ca1759c6fa6f4bef51d8a250be23ebfba9b593`):
`+2,001` bytes (`+0.1121%`). Gzip grows from 678,697 to 679,696 bytes as
secondary context.

Focused Patch 4 validation passes warning-denied diagnostic-code, all-feature
core, and CLI lint; no-default-feature core checking; the complete compact
diagnostic registry suite; hard-budget and exact-key tests; the matched
PocketIC measurement; formatting; and the raw-Wasm/Candid report.
Repository-wide suites remain user-owned push validation. The patch touches
10 implementation/test files plus these status and release notes at roughly
`+1,016/-72` implementation lines before documentation. The implementation is
moderately more complex because it introduces explicit resource authority and
typed attribution; future route integration becomes simpler because every
operator shares one charge and exhaustion contract.

## Patch 5 Evidence — Physical-Route Accounting

- Prepared scalar, grouped, scalar-aggregate, direct-count, SQL, dynamic,
  public, and trusted read routes now execute under one finite per-execution
  hard budget. A synchronous nested prepared route inherits the active counter
  set instead of installing fresh counters. Patch 6 still owns the non-resettable
  request root across separate calls, sessions, retries, and `.await`.
- Physical key/index visits, row fetches, stored bytes, decoded/materialized
  bytes, predicate/projection and nested-value work, sort entries/comparisons
  and temporary storage, grouped/distinct entries and state, cursor work,
  diagnostics, and logical result rows/bytes are charged at maintained owners.
  Grouped sorts and top-k heaps precharge before their expensive operation.
- Primary scans account for the current duplicate backend read instead of
  pretending it is free. Covering routes avoid row-fetch charges when they do
  not fetch a row; hybrid/fallback routes charge the payload they actually
  obtain. Patch 10 still owns eliminating unnecessary or duplicate fetch and
  decode work.
- On Wasm, maintained loops open an instruction watermark after 64 charge
  seams or one charge of at least 1 MiB, periodically sample it, and close it
  before returning. Small bounded executions rely on their work-unit ceilings
  and reserved headroom rather than paying a performance-counter call on every
  query. The production budget profile is immutable and borrowed, so its 21
  limits are not copied per execution.
- Optional diagnostic recording is fail-soft once its finite allowance is
  exhausted. It cannot select a different plan or turn a successful query into
  an error, while its admitted work is still charged.
- The guarantee remains limited to maintained charged database work. Planning
  performed before the current per-execution scope, arbitrary application
  work, and final application/Candid encoding are not claimed as endpoint-wide
  trap protection. Patch 6 moves scope ownership to request entry and owns
  aggregate planning/request accounting.
- Focused injected-budget coverage proves fallback, covering, residual,
  grouped state, actual SQL grouped sorting, cursor, result, and exact-key
  routes return E273 for the corresponding zero allowance. Existing successful
  heap/journal exact-key and scalar/grouped results remain unchanged.

PocketIC 15 measures 1,000 executions per control shape against the clean
pushed `0.221.2` handoff using byte-identical measurement Candid:

| Shape | `0.221.2` | Patch 5 | Delta |
| --- | ---: | ---: | ---: |
| Repeated identical point query | 244,588,340 | 246,750,554 | +2,162,214 (+0.8840%) |
| Distinct-literal point queries | 416,401,274 | 418,657,817 | +2,256,543 (+0.5419%) |
| Repeated ordered empty scan | 217,039,009 | 218,777,228 | +1,738,219 (+0.8009%) |

All three instruction controls remain inside the Patch 5 `+1%` ceiling. Raw
non-gzipped final Wasm grows from 2,447,976 bytes (SHA-256
`2b45892ab398aedcf8b1424a78b12005aeac846af4dec0bb617061bba6cbc05c`)
to 2,455,121 bytes (SHA-256
`cae7c261d3d460ce25ebd33dfbd7de2fe7530a6e74f2b48a7d8b02cd1f34263b`):
`+7,145` bytes (`+0.2919%`), within the `+8,192`-byte gate. Gzip grows
from 958,842 to 961,903 bytes as secondary context. The measurement Candid is
byte-identical at 612 bytes with SHA-256
`6b0b35b9498df3dd69731bf3883160ab6a61258546274d8c04bbda1c17e4c12a`.

Focused Patch 5 validation passes warning-denied all-target/all-feature core
lint, no-default-feature and all-feature core checking, all hard-budget tests,
the physical-route matrix, exact-key typed exhaustion and heap/journal
semantics, the matched PocketIC measurement, formatting, and raw-Wasm/Candid
reporting. Repository-wide suites remain user-owned push validation. The patch
touches 33 implementation/test files at approximately `+1,336/-83` lines
before documentation. The implementation is more complex because every
physical route now exposes explicit finite accounting; execution ownership is
simpler because those routes share one budget, attribution, and typed failure
contract instead of independent implicit limits.

## Landing Plan

| Patch | Outcome | Status |
| --- | --- | --- |
| 1. Accepted runtime root | Publish one incarnation/root-bound runtime authority and remove warm per-query schema rebuilds | Complete |
| 2. Native bounded key batches | Add direct typed `get`/`get_many` with exact bounded semantics | Complete |
| 3. Bounded parameterized templates | Add maintained shared typed parameter slots and bounded template reuse | Complete |
| 4. Hard-budget model and charge ownership | Define hard resources, monotonic charging, and typed exhaustion | Complete |
| 5. Physical-route accounting | Charge every maintained physical execution route | Complete |
| 6. Root request scopes | Make aggregate request work non-resettable across sessions and retries | Not started |
| 7. Scalar page coordinator and work envelopes | Separate resumable page progress from hard failure | Not started |
| 8. Authenticated scalar page API and cursors | Hard-cut scalar enumeration to explicit live/exhaustive pages | Not started |
| 9. Read-set proofs and atomic job advance | Add canonical source proofs and idempotent progress commits | Not started |
| 10. Decode-once materialization | Avoid unnecessary payload fetch/decode and duplicate reads | Not started |
| 11. Request diagnostics and closeout | Expose bounded N+1 evidence and close the Toko-shaped matrix | Not started |

Next authorized landing: Patch 6 only.
