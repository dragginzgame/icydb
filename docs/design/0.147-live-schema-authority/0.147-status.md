# 0.147 Live Schema Authority Status

## Status

In progress. The 0.147 line is being closed as an exact-match and
generated-compatible runtime-guard phase, not as the first additive schema
transition phase.

The current repository has accepted schema metadata authority plus
generated-compatible accepted row-layout guards for prepared execution, typed
saves, and SQL `INSERT` / `UPDATE`. Runtime field decode and final write slot
selection are still generated-layout based, so non-exact schema transitions
remain rejected.

## Already Complete Before 0.147

0.146 completed the accepted-schema metadata foundation:

* persisted schema snapshots are stored in the schema store
* startup reconciliation writes first snapshots and rejects unsupported drift
* accepted schema snapshots are fallibly constructed and integrity checked
* accepted schema feeds SQL metadata, SQL admission, planning field types,
  cache fingerprints, and prepared static execution metadata
* nested leaf metadata is persisted and used for field-path inference/admission
* schema-store and accepted-schema footprint metrics are reported
* schema transition rejection diagnostics are concise enough for local upgrade
  testing

## 0.147.0 Closeout Already Shipped

The 0.147.0 closeout patch shipped:

* `scripts/dev/sql.sh --upgrade` for stable-memory-preserving local upgrades
* startup schema reconciliation before row replay/index rebuild when recovery
  is required
* concise row-layout transition diagnostics with field metadata for the first
  changed or extra field-slot fact

These changes support 0.147 work but do not complete live schema authority.

## 0.147.1 through 0.147.5 Progress

Later 0.147 patches added:

* transition-policy metrics separate from reconciliation metrics
* accepted row-layout runtime descriptors for generated-compatible layouts
* prepared execution handoff through descriptor-derived authority
* typed save and SQL `INSERT` / `UPDATE` guards before generated write codecs
  run
* trait-owned persisted-row field codecs, with removed `#[icydb(...)]` row
  hints rejected at compile time
* runtime-only `Value` boundaries documented separately from typed persistence
* text and blob length metadata carried through schema snapshots, with
  intentionally unbounded text/blob schema fields required to say `unbounded`
* accepted-schema `DESCRIBE` / `SHOW COLUMNS` field rendering through one
  persisted-field format, including explicit text/blob length contracts
* descriptor-owned SQL write slot and field-kind lookup before structural
  patches hand slots to generated-compatible codecs
* descriptor-owned generated-compatible row-layout primary-key slot lookup
* schema-runtime-owned generated-compatible layout validation before executor
  row decode consumes accepted descriptors
* explicit generated-compatible row-shape proof handoff, with the old hidden
  descriptor compatibility shim removed from production and row-decode tests
* named accepted field decode contract used by the generated-compatible
  row-shape proof for payload kind, storage-decode lane, and leaf codec checks
* accepted primary-key name, kind, and physical slot carried directly by the
  runtime descriptor before generated-compatible row decode/write paths consume
  the layout
* generated and managed write-policy facts persisted in accepted schema
  snapshots and carried on accepted runtime fields for SQL/direct write
  admission
* commit schema fingerprints now include generated/managed write-policy facts
  because they affect write admission
* production write admission narrowed to descriptor-owned accepted field
  projections where fields are already resolved, with generated/managed policy
  read from accepted schema metadata instead of generated field metadata
* public structural generated-field authorship rejection moved into session
  accepted-schema preflight so executor staging no longer carries a
  public/internal origin flag for that policy
* structural row value materialization now consumes a named field-level decode
  contract instead of taking raw generated field models directly
* grouped single-slot row decode now caches the named structural field decode
  contract instead of retaining generated field-model pointers in the grouped
  runtime fast path
* generated-compatible accepted row-shape proof now checks field nullability
  as part of the named field decode contract consumed by structural decode
* row-reader cache setup, dense missing-field diagnostics, and fail-closed
  all-slot validation now walk declared slot count plus field decode contracts
  instead of iterating generated field models directly
* `StructuralRowContract` no longer exposes its generated field table to
  production callers; remaining generated field-model access is named as a
  slot-scoped generated-compatible bridge
* `CanonicalSlotReader` now exposes a separate field decode contract method so
  runtime value decode can request structural decode facts without reopening
  generated field-model metadata
* predicate scalar fast paths and direct scalar `OCTET_LENGTH(field)`
  projection checks now inspect `StructuralFieldDecodeContract` facts instead
  of generated field models
* structural row-reader scalar cache validation, primary-key validation, direct
  projection, and value-storage scalar reads now inspect
  `StructuralFieldDecodeContract` facts instead of generated field models
* internal persisted-row runtime value decode now has a contract-first helper,
  leaving the model/slot adapter as compatibility glue for generated runtime
  surfaces
* internal generated-compatible runtime value encode now has a resolved-field
  helper, leaving the model/slot encode adapter as compatibility glue for
  generated runtime surfaces
* remaining private patch and row-reader helpers that return generated
  `FieldModel` entries are named as generated-compatible bridges rather than
  generic field contracts
* the persisted-row slot-to-`FieldModel` lookup helper is now named as a
  generated-compatible bridge at every internal call site
* structural row contracts now store generated field metadata under an explicit
  generated-compatible field name rather than a generic `fields` member
* SQL compiled-command cache miss compilation through the descriptor-derived
  authority paired with accepted `SchemaInfo`
* session-owned structural patch construction through accepted row-layout
  descriptors for callers that can build patches with a `DbSession`
* session-level structural write tests migrated to the session-owned patch
  constructor, leaving generated-model patch helpers only in low-level
  executor/data compatibility coverage

## Remaining Correctness Boundary

The main unresolved boundary is:

```text
accepted persisted schema
  -> transition decision
  -> live row-layout runtime descriptor
  -> row decode/write behavior
```

Until that is complete:

* generated row layout remains the runtime decode/write authority
* accepted schema remains metadata, planning, and compatibility-check authority
* schema evolution remains rejected except exact match
* field-level database defaults are not applied to stored rows

Current decode bridge audit result:

* `StructuralRowContract` and `RowLayout` still require generated
  `FieldModel` entries for payload decode, scalar validation, and relation /
  index after-image checks.
* `AcceptedRowLayoutRuntimeDescriptor` now owns generated-compatible
  compatibility proof, primary-key facts, write-side field lookup, and accepted
  write-policy projection. Its generated-compatible bridge now also checks
  accepted field kind, storage-decode lane, and leaf codec through one named
  accepted field decode contract, but it does not yet provide a complete
  accepted-field decoder.
* The next real transition boundary is therefore an accepted-field decoder
  contract, not another session call-site rewrite. Until that exists,
  additive/remapped layouts must remain rejected before row decode/write
  staging.

## Work Items

### Transition Policy

* [x] Extend `SchemaTransitionDecision` with accepted transition plans.
* [x] Report transition-policy metrics separately from schema reconciliation
      outcomes.
* [x] Detect generated append-only field drift and stored-extra field drift as
      explicit unsupported additive/removal transition shapes while keeping
      both rejected.
* [x] Cover reconciliation-level additive/removal drift rejection and metrics
      so the future transition shapes stay visible outside unit policy tests.
* [x] Report the first changed nested leaf contract for record-subfield schema
      drift instead of only reporting stored/generated nested-leaf counts.
* [ ] Define the first safe additive-field transition policy.
* [ ] Keep field removal, type changes, primary-key changes, and slot reorders
      rejected unless a specific migration plan exists.
* [ ] Ensure transition diagnostics remain concise and schema-owned.

### Live Row Layout Runtime Descriptor

* [x] Define the accepted-schema runtime descriptor boundary for row decode.
* [x] Include durable field ID, physical slot, field kind, leaf codec, and
      absence/default policy.
* [x] Keep descriptor construction in schema authority, not executor
      ad hoc logic.
* [x] Route shared prepared session row-layout construction through accepted
      runtime descriptors when the accepted layout is generated-compatible.
* [x] Carry accepted primary-key name and physical slot on the descriptor so
      row decode no longer re-derives that slot from generated model metadata.
* [x] Carry accepted primary-key persisted kind on the descriptor so SQL write
      primary-key literal normalization no longer reads that fact from the raw
      accepted snapshot after descriptor construction.
* [x] Centralize the session handoff that pairs accepted schema snapshots with
      descriptor-derived execution authority.
* [x] Expose generated-compatible accepted row shape as a named schema-runtime
      proof before row decode builds its structural row contract.
* [x] Keep generated-compatible row-shape proof tied to field-level decode
      contract equality, including payload kind, storage-decode lane, and leaf
      codec.
* [x] Expose accepted field decode contracts as a named schema-runtime shape
      for generated-compatible proof checks.
* [x] Split accepted row-layout proof from executor `RowLayout` projection so
      shared query/session handoff consumes a schema-runtime proof instead of
      recomputing compatibility in executor authority.
* [x] Route row decode/write call sites through the runtime descriptor.

### Row Decode

* [ ] Route all raw row slot count and slot lookup paths through accepted
      layout where a snapshot exists.
* [x] Make structural scalar row decode consume the frozen row contract when it
      opens persisted rows for retained-slot scalar reads.
* [x] Add a slot-reader field-contract lookup so contract-backed readers can
      serve local field metadata without forcing callers through
      `SlotReader::model()`.
* [x] Remove `SlotReader::model()` from the trait surface; generated model
      access is now explicit at typed compatibility constructors or authority
      objects instead of hidden behind generic slot readers.
* [x] Route SQL projection materialization, expression-backed ordering, and
      predicate runtime scalar fast-path checks through the field-contract
      reader seam where they do not need full generated model metadata.
* [x] Remove the generated model from `RowLayout`; executor row layouts now
      retain only the frozen `StructuralRowContract`.
* [x] Add a validated contract-backed row opener and use it for reverse
      relation source-row checks that need eager all-slot validation.
* [x] Use the validated contract-backed row opener for commit-marker decode and
      unique-index validation, keeping their fail-closed full-slot validation
      without retaining a generated model in the row reader.
* [x] Add a named structural field decode contract and route runtime slot value
      materialization through that contract instead of accepting raw generated
      field models.
* [x] Cache structural field decode contracts in the grouped single-slot row
      decode path instead of generated field-model pointers.
* [x] Include nullable field facts in generated-compatible field decode
      contract proof checks.
* [x] Route structural row-reader cache and validation loops through declared
      slot count plus structural field decode contracts.
* [x] Remove production access to the full generated field table from
      `StructuralRowContract`, leaving a named slot-scoped generated-compatible
      field-model adapter for transitional trait surfaces.
* [x] Split the `CanonicalSlotReader` field surface so structural readers can
      provide `StructuralFieldDecodeContract` directly while typed
      materialization keeps the generated-compatible `FieldModel` bridge.
* [x] Route predicate scalar fast-path admission, scalar compare payload
      selection, and direct scalar `OCTET_LENGTH(field)` projection checks
      through `StructuralFieldDecodeContract` instead of generated field
      metadata.
* [x] Route structural row-reader scalar cache validation, primary-key
      validation, direct projection, and value-storage scalar reads through
      `StructuralFieldDecodeContract` instead of generated field metadata.
* [x] Name the internal runtime slot decode seam as a field-contract-first
      helper so production persisted-row callers no longer look like they
      depend on model/slot decode authority.
* [x] Name the internal runtime value encode seam as a resolved-field helper
      so persisted-row write/canonicalization callers avoid re-entering the
      exported model/slot adapter after field lookup has already happened.
* [x] Rename private patch and structural row-reader helpers that return
      generated `FieldModel` entries so only the compatibility trait surface
      keeps the transitional `field_contract` name.
* [x] Rename the persisted-row slot-to-`FieldModel` helper as a
      generated-compatible lookup so internal callers do not present generated
      metadata as generic field authority.
* [x] Rename `StructuralRowContract`'s private generated field slice so the
      row contract does not hide generated metadata behind a generic `fields`
      member.
* [x] Route structural index-key construction through an explicit entity-model
      compile-time input plus contract-backed slot readers, instead of reading
      the model back from the slot reader.
* [x] Make commit-row re-emission use commit authority for dense canonical row
      output instead of depending on a model-backed decoded row reader.
* [x] Remove generated model retention from `StructuralSlotReader`; the
      model-backed constructor now only derives a structural contract and then
      uses the same contract-backed reader shape as accepted-layout opens.
* [x] Remove generated model retention from serialized patch payload replay;
      sparse patch-backed readers now retain a structural row contract for
      field lookup and diagnostics.
* [ ] Replace the temporary generated-compatible bridge with accepted field
      decode contracts before additive/remapped layouts can be accepted.
* [x] Preserve fail-closed decode behavior for malformed persisted bytes.
* [ ] Add old-row read coverage after an accepted additive transition.
* [x] Keep unsupported transitions failing before row decode.

### Writes

* [x] Guard typed save execution with the generated-compatible accepted
      row-layout descriptor before generated write codecs run.
* [x] Guard SQL `INSERT` / `UPDATE` staging with the generated-compatible
      accepted row-layout descriptor before generated write codecs run.
* [x] Keep dynamic `Value` out of typed persisted-field contracts while
      preserving schema-validated runtime boundary adapters.
* [x] Carry text and blob `max_len` metadata through generated and accepted
      schema, and require explicit `unbounded` for unconstrained text/blob
      fields.
* [x] Route SQL write field selection and required-column inference through
      accepted descriptor fields before generated codec handoff.
* [x] Route SQL generated/managed field policy checks through accepted
      descriptor fields and accepted schema write-policy metadata.
* [x] Route direct structural sparse-patch generated-field detection through
      accepted descriptor fields and accepted schema write-policy metadata.
* [x] Enforce direct structural generated-field authorship at session
      accepted-schema preflight and remove the executor request-origin policy
      switch.
* [x] Persist generated/managed write-policy metadata in accepted schema
      snapshots so write admission no longer consults generated field metadata
      for insert-generation or write-managed ownership.
* [x] Include generated/managed write-policy metadata in commit schema
      fingerprints because those facts change write admissibility.
* [x] Provide a descriptor-field write-policy projection for SQL write loops
      that already iterate accepted runtime fields, avoiding accepted-name
      re-resolution inside the loop body.
* [x] Remove the descriptor name-based write-policy projection, keep slot
      projection private, and route production write checks through accepted
      runtime field objects once field resolution has already happened.
* [x] Route SQL structural patch target slots through accepted descriptor
      fields before generated codec validation.
* [x] Route SQL write `RETURNING` all-column labels and slot counts through
      accepted descriptor fields before generated value extraction.
* [x] Add a session-owned structural patch constructor that routes target slot
      selection through accepted layout.
* [x] Migrate session-level structural patch tests to the session-owned
      constructor so accepted schema metadata is exercised at the same boundary
      as production session callers.
* [x] Move or retire the standalone generated-model `StructuralPatch::set_field`
      convenience once public callers have migrated to the session-owned patch
      constructor.
* [x] Apply database defaults only when explicitly present in schema metadata.
* [x] Keep Rust struct defaults separate from database defaults.
* [x] Keep unsupported accepted-schema transitions failing before write staging.
* [ ] Add new-row write coverage after an accepted additive transition.

### Metadata and SQL

* [x] Keep `DESCRIBE` / `SHOW COLUMNS` output accepted-schema based for the
      exact-match/generated-compatible line.
* [x] Route accepted-schema `SHOW COLUMNS` field enumeration through persisted
      snapshot fields instead of generated field order.
* [x] Route accepted-schema `SchemaInfo` field enumeration through persisted
      snapshot fields instead of generated field order.
* [x] Keep SQL SELECT/INSERT/UPDATE/DELETE using accepted-schema `SchemaInfo`
      for field admission and planning metadata.
* [x] Keep cache fingerprints tied to accepted schema identity.
* [ ] Reconfirm these contracts after the first supported non-exact transition.

### Cleanup

* [ ] Remove generated-layout fallback paths once accepted row-layout authority
      is complete.
* [ ] Update status/design docs when the first supported transition is chosen.
* [ ] Add any remaining pre-1.0 hard-cut notes for obsolete schema metadata
      shapes if new internal formats are introduced.

## Deferred Beyond 0.147 Unless Explicitly Pulled In

* broad migration scripting
* multi-version row decoding
* per-row schema versions
* online chunked row rewrites
* field removal with physical slot compaction
* relation-target migration

## Validation To Maintain

Minimum local validation for 0.147 schema patches:

```bash
cargo fmt --all
cargo test -p icydb-core db::schema --features sql -- --nocapture
cargo test -p icydb-core db::commit --features sql -- --nocapture
cargo test -p icydb-core db::session --features sql -- --nocapture
cargo check -p icydb-core --tests --features sql
cargo clippy -p icydb-core --all-targets --features sql -- -D warnings
make check-invariants
git diff --check
```

Use `scripts/dev/sql.sh --upgrade` for manual canister upgrade tests that must
preserve stable memory.
