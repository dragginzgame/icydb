# IcyDB 0.252 Nested Storage Contract Status

Status: 0.252.0–0.252.4 RELEASED; 0.252.5 CLOSEOUT CORRECTIONS COMPLETE — READY FOR USER-OWNED RELEASE VALIDATION

Last updated: 2026-09-03

## Decision

The user explicitly started 0.252 after reporting `v0.251.0` pushed. Direction
B is the accepted pre-1.0 product boundary: named-record scalar paths retain
their exact supported cells, while lists, sets, and maps remain bounded
owner-local aggregates with whole-field replacement.

Accepted schema remains the sole runtime authority. This line adds no document
mode, repeated-member traversal, multikey index, subpath mutation, nested
relation or uniqueness, capability tier, cursor format, or persisted state.

## Entry Audit

The released implementation already fails closed for unsupported nested paths:

- accepted nested-leaf metadata admits only scalar paths through named records;
- SQL and dynamic query lowering reject unresolved paths before execution;
- index DDL rejects unresolved or non-indexable paths before catalog mutation;
- generated fluent APIs expose no rooted repeated-member reference; and
- structural mutation resolves only complete root-field names.

The entry audit found one diagnostic-classification defect:
`mutation_structural_field_unknown` mapped an unknown root name, including a
dotted subpath spelling, to the compact executor invariant. Landing Slice 2
removes that special constructor and routes failed accepted-root lookup through
the existing executor-origin `RuntimeUnsupported` boundary.

The read-only source evidence is:

- accepted scalar-path lookup is owned by
  [`SchemaInfo::nested_sql_capabilities`](../../../crates/icydb-core/src/db/schema/info.rs);
- SQL projection and grouping consume that accepted lookup in
  [`sql/lowering/select`](../../../crates/icydb-core/src/db/sql/lowering/select/mod.rs);
- index DDL consumes it in
  [`sql/ddl/index`](../../../crates/icydb-core/src/db/sql/ddl/index.rs);
- structural patches resolve complete field names in
  [`session/write`](../../../crates/icydb-core/src/db/session/write.rs); and
- the shared executor-unsupported boundary is classified in
  [`error/mod`](../../../crates/icydb-core/src/error/mod.rs).

## Future Design Supplement

The [Future Path Capability Ladder Supplement](0.252-future-path-capability-ladder-supplement.md)
records a non-authorizing evaluation order for possible scalar-path parity,
record-member update, bounded-list predicate, positional, multikey-index, and
relational-expansion work. Every rung requires a separate raw-Wasm,
performance/resource, and complexity baseline audit before it may enter a
delivery tracker, an attributable and cumulative receipt for every authorized
landing slice, and a final cumulative receipt against the original tagged
predecessor. The supplement adds no 0.252 landing slice; all three planned
slices are complete.

After the user reported 0.252.0 pushed, the mandatory Gate-1 audit for the
supplement's first rung froze the immutable tag, optimized SQL actor matrix,
direct ordered/hash grouping controls, current capability cells, downstream
reachability, and the direct-slot state-space boundary. Its verdict is
**eligible for a separate design**, not implementation authority. The exact
evidence and prospective rejection gates are in the
[0.252.1 scalar record-path parity pre-start audit](0.252.1-scalar-record-path-parity-pre-start-audit.md).

The user then authorized the separate design. The accepted
[0.252.1 Scalar Record-Path Grouping Parity design](0.252.1-scalar-record-path-parity-design.md)
preserves the direct `FieldSlot` key set, introduces a path-aware key set only
for queries that contain an accepted path, and splits semantic hash admission
from ordered path-index optimization. Slice 2 froze the comparable actor and
rejected premature zero-benefit owner scaffolding; user-visible runtime
implementation starts in Slice 3.

After 0.252.1 implementation closeout, the user authorized documenting the
remaining generic grouped-ingestion cleanup candidate in the 0.252 line. The
[tentative convergence addendum](0.252-grouped-ingestion-convergence-addendum.md)
limits that candidate to the scan/read/filter/accounting prefix repeated by
the generic unordered runner's direct-borrowed, direct-owned, and path-aware
loops. It does not classify ordered grouping, compact grouped count, or global
distinct as duplicate executors. Implementation began only after the immutable
`v0.252.1` predecessor existed, with a no-build rejection explicitly allowed.

Landing Slice 2 reached that no-build result. A concrete feed removed the
repeated source prefix and 306 raw/307 code bytes, but added one function and
raised the path cold-runtime premium from 17.48% to 21.26%, failing the
maintained 20% gate. Inlining restored the function count but grew raw/code by
1,465 bytes. Production source is exact `v0.252.1`; the addendum is closed with
no runtime or changelog change. See the
[rejection receipt](0.252-grouped-ingestion-convergence-slice-2-receipt.md).

## 0.252.0 Landing Ledger

| Slice | Outcome | Status |
| ---: | --- | --- |
| 1 | Publish the maintained nested-storage contract, named-list example, modeling guidance, and exact current rejection audit | Complete |
| 2 | Correct unknown structural root/subpath diagnostics before execution without adding a mutation route | Complete — [receipt](0.252-landing-slice-2-receipt.md) |
| 3 | Measure one generated typed composite/collection to structural-input adapter over the accepted binding | Complete; closeout evidence corrected — [receipt](0.252-landing-slice-3-receipt.md) |

## 0.252.1 Addendum Audit Ledger

The supplement requires this evidence before any implementation landing slice
may be assigned. It is therefore an audit item, not a retroactive fourth 0.252.0
landing slice.

| Item | Outcome | Status |
| ---: | --- | --- |
| A1 | Freeze the scalar record-path parity source, Wasm, runtime, downstream, complexity, and rejection baselines | Complete — eligible for the separately authorized and subsequently released design — [receipt](0.252.1-scalar-record-path-parity-pre-start-audit.md) |

## 0.252.1 Landing Ledger

| Slice | Outcome | Status |
| ---: | --- | --- |
| 1 | Publish the accepted scalar record-path grouping contract, direct-preserving owner, measurement gates, and bounded delivery ledger | Complete — [design and receipt](0.252.1-scalar-record-path-parity-design.md) |
| 2 | Freeze the exact comparable actor and reject premature group-key owner scaffolding that taxes unaffected downstream code | Complete — [receipt](0.252.1-landing-slice-2-receipt.md) |
| 3 | Introduce the direct-preserving/path-aware owner and admit accepted scalar paths through SQL/structural lowering and complete hash grouping, including grouped projection, raw-key `HAVING`, order, limits, and cursors | Complete — [receipt](0.252.1-landing-slice-3-receipt.md) |
| 4 | Admit the existing ordered grouped strategy for matching accepted path indexes only when stream completeness is proven | Complete — [receipt](0.252.1-landing-slice-4-receipt.md) |
| 5 | Rebuild and publish the cumulative `B0`/final Wasm, runtime, entity-scale, Toko, contract, and changelog closeout | Complete — [receipt](0.252.1-landing-slice-5-receipt.md) |

## 0.252.1 Crate-Family Cohesion Correction Ledger

The user explicitly authorized this independent release-blocking correction
while scalar-path Slice 5 remained pending. It does not enter the grouping
rung's measured cost ledger or replace its cumulative closeout.

| Item | Outcome | Status |
| ---: | --- | --- |
| C1 | Expose model authoring through the public facade and make every published intra-workspace dependency resolve to the exact same patch release | Complete |

## 0.252 Generic Grouped-Ingestion Cleanup Ledger

This separately authorized tentative addendum does not reopen or renumber the
completed 0.252.1 feature ledger and does not assign itself a release patch.

| Slice | Outcome | Status |
| ---: | --- | --- |
| 1 | Publish the exact generic unordered row-admission cleanup boundary, no-build rule, candidate order, and acceptance gates | Complete — [design](0.252-grouped-ingestion-convergence-addendum.md) |
| 2 | Freeze `v0.252.1`, evaluate one concrete eligible-row feed, and retain it only if semantics, hot paths, Wasm, interfaces, and complexity all pass | Complete — rejected; [receipt](0.252-grouped-ingestion-convergence-slice-2-receipt.md) |

## 0.252 Closeout Correction Ledger

The post-release audits separated nineteen bounded corrections. C2 reconciles
pre-0.252.1 wording and stale release-state metadata with the shipped
planner/runtime. C3 removes duplicated dotted path-label rendering, and C4
removes impossible grouped `HAVING` ownership state. C5 removes the stale
owned-value branch from grouped row-slot access. C6 removes three private
runtime-value tag mappings in favor of the stable value-owned tag. C7 removes
four private projection diagnostic mappings in favor of the canonical
operator, function, and aggregate enums, and replaces the remaining numeric
access code with an explicit internal enum. C8 removes the corresponding
planner wrappers and converges two equivalent projection-to-executor error
terminals. C9 removes the duplicate planner type family, C10 gives numeric
arithmetic lowering one operator-owned mapping, and C11 carries function
evaluation failures as internal facts until the builder or executor boundary.
C12 converges variable-prefix order policy. C13 converges SQL-DDL constraint
removal publication, C14 the supported `LIKE` prefix shape, C15 grouped
`HAVING` compilation, C16 accepted-index mutation predicate compilation, and
C17 the shared SQL ordered-window suffix. The final hygiene audit adds C18 to
complete C12's convergence through covering selection, C19 to hard-cut current
performance evidence naming to format version 1, and C20 to restore required
module ownership headers and the misplaced order-contract documentation. All
code cleanups remain independent from the rejected grouped-ingestion
convergence experiment.

| Item | Outcome | Status |
| ---: | --- | --- |
| C2 | Publish scalar record-path `GROUP BY`, exact raw-key `HAVING`, null-group, and ordered-index completeness semantics in the maintained contracts; close stale 0.252.1 planning state | Complete |
| C3 | Converge canonical dotted planner-expression labels on `PathSpec`; retain only if focused behavior, runtime, Wasm, and interface gates pass | Complete — retained for 0.252.2 |
| C4 | Contract grouped `HAVING` plan identity to one borrowed source; retain only if fingerprint, explain, runtime, Wasm, and interface gates pass | Complete — retained for 0.252.2 |
| C5 | Contract decoded grouped row slots to one borrowed access path; retain only if grouped behavior, runtime, Wasm, and interface gates pass | Complete — retained for 0.252.2 |
| C6 | Converge grouped-key, aggregate-runtime, and compiled-expression value discrimination on canonical `ValueTag`; retain only when focused semantics, representative reachability, interfaces, and complexity justify the measured result | Complete — retained for 0.252.3 |
| C7 | Converge projection diagnostic context on canonical expression enums and one explicit access-kind enum; retain only when focused semantics, representative Wasm, interfaces, and complexity justify the measured result | Complete — retained for 0.252.4 |
| C8 | Remove redundant planner diagnostic wrappers and converge equivalent projection-to-executor error terminals; retain only when exact diagnostic facts, focused execution, representative Wasm, interfaces, and complexity justify the result | Complete — retained for 0.252.4 |
| C9 | Remove the duplicate planner expression-type family and convert canonical inferred types only at the stable diagnostic boundary | Complete — retained for 0.252.4 |
| C10 | Converge general numeric-arithmetic lowering on one `BinaryOp` owner and remove the projection fallback to addition | Complete — retained for 0.252.4 |
| C11 | Remove the projection-function `QueryError` construction-and-inspection roundtrip while preserving compact reasons and checked numeric failures | Complete — retained for 0.252.4 |
| C12 | Converge variable-prefix secondary-order compatibility on the planner-owned order contract and remove executor policy rediscovery | Complete — retained for 0.252.5 |
| C13 | Converge SQL-DDL constraint-removal publication on one validation-job-aware owner | Complete — retained for 0.252.5 |
| C14 | Converge supported `LIKE` prefix classification across direct predicate parsing and SQL expression lowering | Complete — retained for 0.252.5 |
| C15 | Converge grouped finalization on one route-owned `HAVING` compiler and error conversion | Complete — retained for 0.252.5 |
| C16 | Converge accepted field-path and expression index mutation predicates while preserving integrity corruption reporting | Complete — retained for 0.252.5 |
| C17 | Converge `SELECT`, `UPDATE`, and `DELETE` ordered-window suffix parsing while retaining statement-specific diagnostics | Complete — retained for 0.252.5 |
| C18 | Complete variable-prefix secondary-order convergence across planner access satisfaction, covering selection, and executor pushdown through one access-kind classifier | Complete — retained for 0.252.5 |
| C19 | Hard-cut the current SQL performance evidence identity and attribution policy to format version 1, then regenerate current PocketIC 16 evidence without a legacy decoder | Complete — retained for 0.252.5 |
| C20 | Add the required module ownership headers to the eight touched modules and repair the misplaced `DeterministicSecondaryOrderContract` documentation | Complete — retained for 0.252.5 |

## 0.252.5 Host-Harness Maintenance

The separately requested maintenance update moves the host integration package
from `ic-testkit 0.8.9` to `0.9.0` and therefore from PocketIC 15 to PocketIC
16. The maintained SQL performance environment now names
`pocket-ic-server 16.0.0`; older captured evidence stays historical rather than
being accepted as comparable. IcyDB does not consume the changed optional
managed-server hard-TTL accessor. Native upstream simulator types now come from
the complete `ic_testkit::pocket_ic` re-export, while `ic_testkit::pic` retains
only fixtures, policy types, and extension traits at IcyDB call sites. No direct
PocketIC dependency is added, and the dependency remains outside the
representative canister's Wasm graph. All integration test targets compile,
the focused cache and performance-environment contracts pass, and a real
PocketIC 16 canister-runtime smoke test passes.

The current diagnostics-attribution identity and cache-mode policy now encode
only version 1. No predecessor decoder, alias, upgrade bridge, or old-form
fixture is retained. A regenerated local PocketIC 16 instrumentation receipt
records `icydb-sql-attribution/0.215/v1` and
`cold_warm_at_entry_and_loop_local_v1` over a 4,394,557-byte raw Wasm subject.
Its 3,248,715 attributed and 3,229,180 total-only median instructions produce
19,535 instructions or 60 basis points of harness overhead. Because the
worktree is intentionally dirty, this is method validation rather than a
comparable clean baseline.

## 0.252.1 Slice 1 Result

The accepted design fixes one single-valued scalar-path contract and one
converged grouped execution flow. Direct-only queries keep the existing
`Vec<FieldSlot>` representation and hot loops. Only queries containing a path
use the path-aware key set, whose prepared reader hashes, compares, and
materializes values through the existing scalar path and `GroupKey` authorities.

The implementation plan separates the direct-preserving enabling change,
complete hash semantics, ordered path-index proof, and cumulative closeout into
independent measured handoffs. This adds no runtime code in Slice 1. The design
touches documentation only, leaves the 0.252.0 changelog unchanged, and keeps
Candid, generated APIs, formats, persistence, recovery, and Wasm reachability
unchanged.

## 0.252.1 Slice 2 Result

The retained `group_path_sql_query` actor freezes one direct scalar, required
and optional nested records, complete and omission-capable path-index workloads, a
2,048-row bounded loader, SQL query/DDL/mutation entry points, and no public
performance instrumentation. Its actor and generated schema source SHA-256
values are respectively
`e014f79760c79259e58630ee514b74174dddb7175480dd906ca77907f8f56283`
and
`b33429d5140d8c53d06e79da8fa3ddec471dd8cc746888904d9c49f3434f3b6c`.

The first candidate propagated a nominal direct/path owner through planner and
runtime DTOs. It stayed within the SQL actor ceiling but grew the SQL-off
reachable controls by about 3.8 KiB each. A transparent newtype reduced that
to about 215 raw bytes, and an erased alias reduced the exact normalized-path
Toko comparison to +27 raw/+37 code bytes. All three production candidates
were rejected because Slice 2 adds no user-visible behavior and its downstream
gate permits no growth. No owner, branch, wrapper, callback, registry, or
runtime route remains.

The final production source is identical to `v0.252.0`. The maintained
`one_entity_sql_query` artifact is byte-identical at 3,053,099 raw and
2,881,728 code bytes. The frozen comparable actor and its exact B0 source
overlay are byte-identical at 3,551,458 raw, 3,358,917 code, and 177,879 data
bytes, with 9,014 defined functions, 793 indirect calls, nine exports, and
11,644-byte Candid. Direct ordered/hash controls preserve exact rows and
resources while measuring 61,306,251 and 69,489,431 instructions, both below
the frozen controls.

The workload contract executes the direct control and freezes required,
optional, and mixed path-grouping rejection before Slice 3. Executing either
frozen nested-index DDL workload exposed the predecessor's structural
index-value serialization boundary as `RuntimeCorruption`, both during
backfill and subsequent indexed writes. Slice 2 does not freeze that defect as
expected behavior or widen into an index-storage repair; it is an explicit
precondition for the ordered-index work in Slice 4.

Slice 3 now owns the group-key representation and scalar-path hash semantics
as one atomic review boundary. This keeps Slice 2 simpler, avoids a
zero-benefit execution axis, and does not change public APIs, accepted schema,
persistence, cursors, formats, Candid, or runtime behavior.

## 0.252.1 Slice 3 Result

Accepted scalar record paths now execute through complete hash grouping with
exact required/optional/null, mixed-key, projection-expression, raw-key
`HAVING`, ordering, limit, cursor-emission, and row-local aggregate-input
behavior. Direct-only queries retain their existing key representation and
hot loops. Path-aware probes borrow row values and allocate an owned tuple only
when opening a group.

The attributable actor grows by 9,417 raw and 9,200 code bytes with 37 defined
functions, passing its 24 KiB/48-function gate. The cold path premium
is 17.59%; maintained direct ordered/hash controls remain within 0.37% of
`B0`. Indirect calls, exports, and Candid are exact.

The user accepted a bounded 4 KiB fixed shared-cost allowance rather than
rejecting the feature on the original literal zero-growth rule. After removing
a duplicate grouped cache-key representation and impossible dual row-decoder
state, the SQL-off one/ten controls grow by 2,530/2,533 raw bytes. Their raw
entity slope moves by three bytes and their function slope is exact. The
path-normalized frozen Toko Miner control grows by 2,922 raw and 2,870 code
bytes with 16 defined functions; `default_empty` returns to exact identity.
Indirect calls, exports, and Candid remain exact. Slice 3 was accepted while
Slice 4 was still pending at that boundary.

## 0.252.1 Slice 4 Result

Matching complete path indexes now select the existing ordered grouped route.
The route planner proves stream completeness from accepted field omittability
and the existing predicate-implication authority before execution: required
non-null paths admit directly, nullable omission-capable paths require a query
proof excluding null/missing rows, and mixed direct/path keys require one
matching composite prefix. Uncertain cases retain complete hash grouping.

The ordered path workload returns exact rows and cursors at a 14.82% instruction
premium over its mirrored direct workload, below the 15% gate. The comparable
actor adds 3,131 raw and 3,144 code bytes while removing six functions from
Slice 3; cumulative growth from `v0.252.0` is 12,548 raw, 12,344 code, and 31
functions.
The SQL-off reachable pair stays inside its fixed 4 KiB/24-function allowance
with raw/code slopes 10 bytes below `v0.252.0` and an exact function slope.

Converging accepted/rebuild path traversal on one resolver also makes
`default_empty` smaller by 646 raw, 625 code, 13 data bytes, and 16 defined
functions. This favorable result does not meet the original literal
byte-identity wording. The accepted simplification direction rejects retaining
duplicate semantic owners solely to reproduce larger predecessor bytes;
indirect calls, exports, Candid, and empty-actor behavior remain unchanged.

Planner simplification removed a duplicate accepted-path order selector and a
separate authority switch. Planning and reranking now consume the same reduced
accepted-schema-derived semantic index contracts. No strategy, cursor form,
public API, persisted state, generated type, callback, or indirect dispatch was
added. Slice 4 was accepted; the cumulative SQL/Toko rebuild and closeout are
recorded in Slice 5 below.

## 0.252.1 Slice 5 Result

The cumulative closeout passes every declared feature-actor, runtime, SQL,
SQL-off entity-scale, frozen Toko, Candid, export, and indirect-call gate. The
final feature actor adds 11,320 raw bytes, 11,119 code bytes, and 28 functions
from `v0.252.0`. Cold and ordered path premiums are 17.49% and 14.69%; direct
ordered/hash controls stay within 0.27% of their frozen baselines.

The first closeout Toko build exceeded the fixed 4 KiB raw/code ceiling.
Closeout therefore removed an allocated grouped-fingerprint projection,
converged duplicate plan/explain `HAVING` walkers behind one concrete borrowed
context, and added a no-allocation direct-field resolver path. The final frozen
same-tool Toko A/B is +3,596 raw bytes, +3,540 code bytes, and +19 functions,
with exact Candid, exports, and indirect calls.

The SQL-off one/ten actors add +2,386/+2,373 raw bytes against the frozen
ledger. Raw/code entity slopes both move by -13 bytes and function slope is
exact. Maintained SQL actors remain below their cumulative ceilings;
`default_empty` retains Slice 4's favorable shrink. The correction deletes
duplicate code and adds no public, persisted, generated, cursor, strategy,
configuration, callback, or registry axis.

All focused agent-owned gates passed. The full repository/workspace suite was
user-owned under repository policy. At this boundary, 0.252.1 implementation
and documentation were complete and ready for that release validation.

The first user-owned release-validation run exposed one stale test-only
cardinality assertion after the retained actor inventory grew from 20 to 21.
The correction removes that historical number and proves exact canister/package
identity parity between the installable fixture inventory and maintained build
policies instead. The complete focused integration-library target passed. The
user subsequently completed the release boundary and published immutable
`v0.252.1`; no release validation remains outstanding in this tracker.

## 0.252.1 Crate-Family Cohesion Correction Result

Runtime-enabled downstream crates can now use `icydb::model::prelude` with one
direct `icydb` dependency. Model macro expansion prefers a direct
`icydb-model` package for standalone schema tooling and otherwise resolves the
facade re-export, including renamed facade dependencies. The facade already
depended on the model package, so the re-export adds no dependency edge or
cycle.

The root workspace pins `icydb`, `icydb-core`, `icydb-diagnostic-code`,
`icydb-model`, `icydb-model-macros`, and `icydb-schema` to the exact current
version. Published member manifests inherit those requirements, and release
versioning advances the pins atomically with workspace package versions. A
future mismatched patch family therefore fails dependency resolution instead
of compiling incompatible macro output.

One facade-only workspace fixture proves model declarations and generated
runtime adapters with no direct model/core/schema dependency. This correction
changes no database semantics, Candid, persistence, cursor, recovery, or
runtime execution route. It adds no reachable production body; Wasm and
instruction measurements are not applicable to the package-resolution and
compile-time facade boundary.

## 0.252.2 Path-Label Ownership Cleanup Result

`PathSpec` now owns the sole loop that renders canonical dotted field-path
identity. Seven builder, expression, predicate, inference, and SQL consumers
delegate to that owner, and the compiled scalar-path root accessor made unused
by the convergence is removed. The nine touched production files remove 54
net production lines; one owner-local test leaves the complete slice at 39 net
lines removed.

The optimized `group_path_sql_query` actor is 3,562,300 raw bytes and 3,369,562
code bytes, 474 bytes smaller in both measures than immutable `v0.252.1`.
Data remains 178,030 bytes; 9,042 defined functions, 793 indirect calls, nine
exports, and the exact 11,644-byte Candid interface are unchanged. Cold and
ordered path premiums are 17.30% and 14.47%, below their 20% and 15% ceilings.

Focused owner and nested-predicate tests, all-feature strict package linting,
and the complete nine-test scalar-path integration target pass. This cleanup
adds no behavior axis, allocation, public API, planner route, persisted state,
cursor or format. The full repository/workspace suite remains user-owned.

## 0.252.2 HAVING Ownership Cleanup Result

The canonical `GroupPlan` accessor now borrows its `HAVING` expression as
`&Expr`. Fingerprint projection carries one borrowed plan variant instead of
separate borrowed and impossible owned variants, and explain keeps the same
single clone at its owned DTO boundary. The four touched production files
remove 26 net lines without adding an allocation, branch, behavior axis, or
compatibility surface.

The first candidate removed the accessor as well. Although it saved 64 raw and
code bytes, its deterministic ordered-path sentinel reached a 15.009% premium
and failed the 15% gate. Retaining the narrow borrowed accessor removed more
code and passed at 14.82%, so the direct-field variation was rejected without
weakening the gate.

The final optimized actor is 3,562,221 raw bytes and 3,369,483 code bytes. This
HAVING slice removes 79 bytes from each measure beyond the path-label slice,
for cumulative `0.252.2` reductions of 553 raw and code bytes from
`v0.252.1`. Data, functions, indirect calls, exports, and Candid remain exact;
the cold path premium passes at 17.74%.

Sixteen focused `HAVING` tests, 45 fingerprint tests, all-feature strict
package linting, and the complete nine-test scalar-path integration target
pass. The full repository/workspace suite remains user-owned.

## 0.252.2 Grouped Row-Slot Ownership Cleanup Result

`RowView` now exposes one borrowed slot accessor over its dense-test,
single-slot, and retained decoded storage. The duplicate `Cow<Value>` accessor,
its impossible owned variants, and the matching aggregate-state ownership
branch are removed. Field-path expression evaluation keeps its separate
legitimate `Cow` boundary because a missing nested member must still synthesize
an owned `NULL`. The two touched production files remove 25 net lines with no
allocation, dispatch, behavior, public API, cursor, persistence, or format
change.

The optimized actor is 3,561,374 raw bytes and 3,368,636 code bytes. This slice
removes another 847 bytes from both measures, for cumulative `0.252.2`
reductions of 1,400 raw and code bytes from `v0.252.1`. Data remains 178,030
bytes; 9,042 defined functions, 793 indirect calls, nine exports, and the exact
11,644-byte Candid interface remain unchanged.

The complete nine-test scalar-path integration target passes. Isolated cold
execution measures 5,019,023,425 direct and 5,899,388,560 path instructions, a
17.54% premium below the 20% ceiling. Ordered execution measures 6,026,199,491
direct and 6,915,933,466 path instructions, a 14.76% premium below the 15%
ceiling. All 93 focused grouped core tests and all-feature strict package
linting pass. The full repository/workspace suite remains user-owned.

## 0.252.3 Runtime Value-Tag Ownership Cleanup Result

The stable value-owned `ValueTag` now carries runtime value identity in
grouped-key failures, accepted aggregate runtime contracts, and compiled
expression diagnostics. Three private 25-way mappings and their independent
numeric assignments are deleted. Accepted-schema field-kind identity remains
separate: narrow integers still lower to their stored runtime kind, relation
keys preserve their accepted contract, and structured composites preserve the
existing list-or-map admission rule.

The three touched production files remove 249 net lines. The replacement adds
no allocation, generic convenience layer, trait object, callback, registry,
execution route, persisted state, cursor, format, compatibility surface, or
behavior axis. Typed error context remains intact; only equivalent terminal
error mappings are contracted.

Clean same-tool comparisons against immutable `v0.252.2` leave
`default_empty` and `one_entity_typed_query` byte-identical, including code,
data, functions, indirect calls, exports, and Candid. The optimized
`group_path_sql_query` actor is 3,561,424 raw and 3,368,682 code bytes, a
46-byte increase in each measure; gzip decreases by 177 bytes. Data remains
178,034 bytes, while 9,042 defined functions, 793 indirect calls, nine exports,
and the exact 11,644-byte Candid interface are unchanged. This bounded
aggregate/path-reachable movement is retained because the empty and typed
entity controls prove no generic or per-entity tax and the source removes
three semantic owners; no unrelated byte-saving change is folded into the
slice to manufacture literal no-growth.

All 68 focused canonical-tag, grouped-key, aggregate, and compiled-expression
tests pass, as does strict all-target/all-feature core linting. The full
repository/workspace suite remains user-owned.

## 0.252.4 Expression Diagnostic And Evaluation Ownership Cleanup Result

Projection diagnostics now carry canonical `UnaryOp`, `BinaryOp`, `Function`,
and `AggregateKind` values directly. Four private numeric adapter mappings and
their duplicated exhaustive matches are deleted. The independent access-origin
concept now uses one ordinary `ProjectionAccessKind` enum instead of an opaque
numeric code.

Planner validation now retains canonical `ExprType` values through inference
instead of projecting them into a duplicate `ExprPlanTypeClass`. Numeric
General numeric-arithmetic lowering is shared through `BinaryOp` instead of
repeated by builder preview, aggregate input, compiled execution, and
projection evaluation.
Projection-function evaluation now carries invalid-call, compact projection,
and checked numeric facts directly; it no longer constructs a `QueryError`,
reads its diagnostic back, and reconstructs an executor error.

Twenty-five production files remove 373 net lines. Planner validation now maps
canonical operators and functions once into stable diagnostic-fact enums,
without three planner-specific wrappers or their duplicate constant tables.
Scalar and grouped executors also use one projection-error terminal instead of
two methods with identical bodies. The hard cut adds no allocation, generic
convenience layer, trait object, callback, registry, execution route, persisted
state, cursor, format, compatibility surface, or behavior axis. Existing
focused expectations move mechanically to the maintained enums.

Clean same-tool comparisons against immutable `v0.252.3` reduce
`default_empty` by 4 raw/data bytes and reduce `one_entity_typed_query` by 150
raw and 145 code bytes, 4 data bytes, and one defined function. The optimized
`group_path_sql_query` actor falls from 3,561,424 to 3,558,845 raw bytes, from
3,368,682 to 3,366,149 code bytes, from 178,034 to 177,990 data bytes, and from
9,042 to 9,040 defined functions. All three actors retain exact indirect-call
and export counts and byte-identical Candid interfaces.

The three broader candidates were measured independently against the completed
initial cleanup. C9 saves 155 raw/code SQL bytes and 146 raw/145 code typed
bytes with one fewer typed function. C10 saves 1 raw and 15 code SQL bytes but
adds 12 data bytes and two functions; it remains worthwhile because the primary
raw/code gates pass and four mappings plus one unsafe fallback become one
fail-closed semantic owner. C11 saves 2,345 raw and 2,338 code SQL bytes plus
three functions; the empty and typed controls each lose only 4 data bytes.

Focused diagnostic, type-inference, compiled-expression, projection, and
grouped-fold tests pass, as do the repository invariant gate and strict
all-target/all-feature core linting. The full repository/workspace suite
remains user-owned.

## 0.252.5 Flow Convergence Cleanup Result

`DeterministicSecondaryOrderContract` now owns the single decision that a
multi-lookup or branch-set variable prefix requires a full index-order match.
Planner order satisfaction and executor pushdown consume that decision through
access-shape facts, while covering caches the same classifier result instead of
locally mapping access variants to policy. Route selection and rejection
payloads remain unchanged.

SQL-DDL constraint removal now selects ordinary publication or atomic
validation-job removal through one helper used by unique, check, and not-null
activations. The supported `LIKE` prefix shape has one classifier shared by
direct predicate parsing and SQL expression lowering. Grouped count and generic
finalization compile route-owned `HAVING` through one helper. Accepted
field-path and expression index mutation use one fail-closed persisted
predicate compiler, while integrity inspection retains its typed corruption
boundary. `SELECT`, `UPDATE`, and `DELETE` parse their ordered-window suffix
through one method while retaining statement-specific clause-order diagnostics.

The eight affected predicate, schema-DDL, expression-lowering, and statement
parser modules now publish their required ownership headers. The displaced
`DeterministicSecondaryOrderContract` documentation is attached directly to
the type it describes.

Seventeen core production-source files add 35 net lines after including one
owner-local semantic regression test and forty mandatory module-header lines.
Excluding those documentation lines, the maintained core remains 5 net lines
smaller. The replacements add no allocation, public surface, generic layer,
execution route, persisted state, compatibility surface, or behavior axis;
implementation shape becomes simpler through six semantic or mechanism owners.

The current-lock representative `group_path_sql_query` actor is 3,558,935 raw
bytes, 3,366,257 code bytes, and 177,953 data bytes with 9,044 defined
functions. C18 is byte-identical to its exact same-lock `HEAD` baseline for all
of those primary measures, as well as 793 indirect calls, nine exports, and the
11,644-byte Candid interface; gzip moves by two bytes as secondary context.
Against the historical immutable `v0.252.4` artifact, the cumulative current
result is +90 raw, +108 code, -37 data, and +4 functions.

Focused SQL parser, predicate parser/lowering, grouped `HAVING`, index-plan,
DDL publication/removal, `LIKE`, order-contract, and secondary-order tests
pass. The membership runtime control also passes at 1,550,619 instructions for
the `IN` route and 1,582,884 for `NOT IN`; against the secondary-order-only
candidate these move by +456 and -212 instructions respectively, both below
0.03%. The current-v1 environment contract and real PocketIC 16 instrumentation
capture pass, as do strict core and focused integration linting plus the
repository formatting, invariant, and whitespace gates. The full
repository/workspace suite remains user-owned.

## 0.252.0 Slice 1 Boundaries

Landing Slice 1 changes documentation and release notes only. It does not
change Candid, public Rust payloads, accepted schema, persistence, indexes,
planning, cursors, write execution, recovery, or Wasm reachability.

The canonical maintained owner is
[NESTED_STORAGE.md](../../contracts/NESTED_STORAGE.md). Other maintained guides
link to that contract and retain only their surface-specific explanation.

## 0.252.0 Slice 1 Result

The contract now publishes the exact shape matrix, scalar member-path cells,
whole-list/set predicates, optional missing/null and omission-index behavior,
whole-field write boundary, named-list durable maximum, and entity-modeling
decision. The maintained README, foundations, query, SQL, write,
schema-authoring, and diagnostics entry points converge on that owner.

The change is documentation-only: no production function, generated artifact,
public payload, persisted format, or Wasm reachability changed. Repository
complexity stays neutral: one canonical contract and one short status ledger
replace duplicated or implicit guidance, while the known diagnostic defect is
isolated instead of adding a second execution path.

Focused formatting, invariant, and whitespace checks pass. Full repository
tests and Wasm measurement are intentionally not applicable to this
documentation-only slice.

## 0.252.0 Slice 2 Result

Unknown structural root fields and dotted subpath spellings now return the
existing executor-origin `RuntimeUnsupported` diagnostic. The obsolete
invariant constructor and unused entity-path plumbing are removed. No new
diagnostic enum, fact schema, public payload, registry branch, mutation mode,
or execution route is retained.

Focused coverage proves the same classification for a single mutation and a
late invalid item in an atomic batch, verifies the earlier batch item remains
uncommitted, and confirms complete root-field replacement still succeeds.

The maintained reachable actors both shrink against `v0.251.0`: one entity by
568 raw/558 code bytes and ten entities by 506 raw/494 code bytes. Exact Candid
and 717 indirect calls are unchanged. The raw entity slope is 40,115 bytes,
only 62 bytes above baseline, and continues to pass the 8 KiB gate. Full
evidence is recorded in the
[Landing Slice 2 receipt](0.252-landing-slice-2-receipt.md).

Focused core regression and all-feature Clippy checks pass, as do repository
formatting, invariant, and whitespace checks. The full repository suite remains
user-owned and was not run.

## 0.252.0 Slice 3 Result

`DbSession::bind_typed_input` now converts an authored generated value through
one exact current `TypedEntityBinding` into the ordinary structural
`InputValue` boundary. It reuses the sole generated `TypedInputValue` encoder,
rejects stale bindings before conversion, maps unavailable accepted source
identities to `FieldUnavailable`, and leaves complete-field accepted admission
as the mutation authority.

The API adds no bound-value wrapper, second write cell, registry, mutation
variant, callback, persisted state, or compatibility route. Returning
`InputValue` also leaves `Omitted`, `Default`, `Null`, and `Value` intent with
the existing `WriteCell` owner.

The final representative pair now executes a generated nested record/list
through accepted conversion plus single and atomic-batch writes. The
one-entity actor is 2,910,803 raw bytes and the ten-entity actor is 2,952,157
raw bytes. The nine-entity delta is 41,354 bytes, or 4,595 bytes/entity, and
passes the 8 KiB gate. The earlier scalar isolation probe still attributes only
about 61 raw bytes per added entity to the adapter wrapper itself. Exact
190-byte Candid remains unchanged. Full evidence is recorded in the
[Landing Slice 3 receipt](0.252-landing-slice-3-receipt.md).

## 0.252.0 Closeout Result

The read-only closeout found that the first Slice 3 audit reached only a scalar
root and that facade use was compile-tested rather than executed. The retained
correction makes a generated list of records, unit and payload enums, scalar
members and null optionals runtime-reachable; verifies one successful single
write, one successful atomic two-row batch, and one unavailable-source
rejection through the public facade; and preserves the independent accepted
binding lifecycle proof for stale authority.

All planned 0.252.0 slices and closeout corrections are complete, and the user
reported the release pushed. The 0.252.1 audit, design, five implementation
slices, and independent crate-family cohesion correction are also complete.
The full repository/workspace release suite remains user-owned.
