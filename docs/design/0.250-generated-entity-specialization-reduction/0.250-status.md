# IcyDB 0.250 Generated Entity Specialization Reduction Status

Status: READY — ALL SEVEN LANDING SLICES COMPLETE; CLOSEOUT PASSES

Last updated: 2026-08-31

## Decision

The user explicitly started 0.250 after the 0.249 closeout verdict. The line
targets measured generated-entity specialization in a real downstream actor,
not schema declaration cost in an actor where unused adapters are dead-stripped.

Supplied intake evidence attributes 49.7–54 KiB of direct database/entity
monomorphization and 112–128 KiB of all entity-specialized supporting code to
11 reachable generator entities. A disposable common-execution experiment
reduced the optimized code section by approximately 49–51 KiB, but its dynamic
page callback added approximately 95 functions through vtable shims. The code
saving qualifies the owner; the callback shape does not.

Landing Slice 1 now freezes the downstream identities and preserved artifacts,
records the missing experiment-source limitation, and adds a reproducible
maintained one-to-ten reachable-entity pair. See the exact
[Landing Slice 1 receipt](0.250-landing-slice-1-receipt.md).

Landing Slice 2 first evaluated a concrete driver that saved 27,201
code-section bytes and 31,578 raw bytes. It was removed after missing the
original 32 KiB target by 5,567 bytes. The user then explicitly reopened the
decision rather than rejecting useful work solely on that threshold. The
simplified cursor removes stored lane state and a duplicate builder surface;
the final exact Toko A/B saves 30,969 code bytes, 35,345 raw bytes and 12
defined functions with flat `call_indirect`. It is retained under a recorded
candidate-specific exception, 1,799 code bytes short of the original target.
See the [Landing Slice 2 receipt](0.250-landing-slice-2-receipt.md).

Landing Slice 3 hard-cuts the allocation-backed generated binding request path
to one core-owned static descriptor validated by accepted schema authority.
Relative to Slice 2, the exact Toko actor removes another 20,089 raw bytes,
31,462 code bytes and 19 defined functions with flat `call_indirect`. The
descriptor adds 11,416 data-section bytes but more than offsets them in code,
so final raw Wasm improves and the no-code-to-data-growth gate passes. See the
[Landing Slice 3 receipt](0.250-landing-slice-3-receipt.md).

Landing Slice 4 hard-cuts the entity-generic exact-key execution path to one
concrete prepared terminal. Relative to Slice 3, the exact Toko actor removes
another 12,319 raw bytes, 12,306 code bytes and five defined functions with
flat `call_indirect`. Across the maintained pair, the nine-added-entity raw
slope falls by 40,761 bytes. See the
[Landing Slice 4 receipt](0.250-landing-slice-4-receipt.md).

Landing Slice 5 admits delete through the source-bound typed mutation contract
and adds one concrete same-entity batch admission over the existing atomic
executor. Relative to Slice 4, the exact Toko actor removes another 14,123 raw
bytes and 13,711 code bytes with flat `call_indirect`; the required proving-
consumer migration deletes its parallel structural encoder family. See the
[Landing Slice 5 receipt](0.250-landing-slice-5-receipt.md).

Landing Slice 6 completes cumulative qualification without changing production
behavior. The exact Toko actor is 81,876 raw bytes and 88,448 code bytes
smaller than `v0.249.6`, with 32 fewer functions and flat `call_indirect`.
Under the then-shared 2 KiB ceiling, the maintained reachable-entity slope of
4,466 raw bytes per added entity produced an incomplete size finding. See the
[Landing Slice 6 receipt](0.250-landing-slice-6-receipt.md).

Landing Slice 7 applies the user's explicit correction after identifying that
the 2 KiB ceiling came from the approximately 154-byte dead-strippable schema
subject. The schema comparison keeps that 2 KiB fail-closed gate and passes at
1,038 bytes per entity. The distinct reachable-adapter comparison now has an 8
KiB fail-closed gate and passes at 4,466 bytes per entity. See the
[Landing Slice 7 receipt](0.250-landing-slice-7-receipt.md).

0.250 retains one concrete prepared-page cursor, one static generated binding
contract, one prepared exact-key terminal and one same-entity typed-write
terminal. It adds no planner, executor, storage path, persisted state, runtime
registry or compatibility layer.

## Predecessor Boundary

| Input | Exact receipt |
| --- | --- |
| IcyDB release | `v0.249.6` |
| Revision | `ef277d64a75e304b1e75d1c1e2d2922c2075a2ff` |
| Tree | `dfcdd7d333b8ebc9df9a6b6d2d1cc29ee95d5bd9` |
| Release date | 2026-08-30 |
| 0.249 state | 17 landing slices complete; root and detailed `0.249.6` notes present |

The unrelated working-tree `Cargo.lock` dependency drift present when this
design was created is not part of the 0.250 baseline and was not edited.

## Supplied Intake Attribution

| Attribution | 11 generator entities | Approximate per entity |
| --- | ---: | ---: |
| Direct database/entity monomorphization | 49.7–54 KiB | 4.5–4.9 KiB |
| All entity-specialized supporting code | 112–128 KiB | 10–12 KiB |
| Complete generator-studio removal | about 425 KiB | not attributable solely to monomorphization |

The rows overlap and must not be added. Twiggy attribution, inlining and
Binaryen merging assign shared code differently.

| Named family | Supplied attribution |
| --- | ---: |
| Typed page execution | 36.8 KiB |
| Typed page decoding | 29.8 KiB |
| Single-row insert | 34.6 KiB |
| Single-row update | 28.1 KiB |
| Atomic delete | 16.3 KiB |
| Typed query/delete wrapper methods | about 59.7 KiB collectively |
| Generated insert/update encoders | about 93.2 KiB across 79 functions |
| Generated `typed_binding` | about 4.5 KiB |

These values remain overlapping intake lenses. Landing Slice 1 records their
exact available source, generated-source, tool and artifact identities without
claiming reproducibility for the removed proof-of-concept source.

## Measurement Result

The maintained one-to-ten-entity actor currently reaches an exact-key read for
only `TenSimpleEntity01`. Its finalized 145-byte-per-entity slope measures
additional schema declarations whose unused adapters can be removed. It does
not contradict the supplied cost of making typed adapters reachable for every
entity.

The new maintained pair reaches bounded page, exact-key, single insert, single
update, same-entity batch and delete paths through the same runtime-selected
harness for every admitted entity. Across nine added reachable entities it
adds 83,540 final raw bytes, 82,173 code-section bytes and 110 defined
functions, with no `call_indirect` increase. Its 9,283-byte final raw slope is
4.53 times the 2 KiB ceiling and is intentionally report-only until closeout.

The existing schema-cardinality control remains fail-closed under its original
identity. The new comparison does not reinterpret or weaken that passing gate.

After Landing Slice 3, the maintained reachable pair spans 83,938 final raw
bytes, 82,335 code-section bytes and 106 defined functions across nine added
entities, with no `call_indirect` increase. Relative to Slice 2 this is +242
raw bytes, +12 code bytes and -1 function in the slope, while both individual
actors improve in absolute raw and code bytes. The exact Toko actor remains the
acceptance subject for heterogeneous generated binding shapes.

After Landing Slice 4, the maintained reachable pair spans 43,177 final raw
bytes, 41,590 code-section bytes and 90 defined functions across nine added
entities, with no `call_indirect` increase. The resulting 4,798-byte raw slope
per added entity remains above the eventual 2 KiB closeout ceiling and remains
report-only until Landing Slice 6.

After Landing Slice 5, the maintained reachable pair spans 40,188 final raw
bytes, 38,604 code-section bytes and 73 functions across nine added entities,
with no `call_indirect` increase. Both actors improve materially in absolute
raw/code bytes. Landing Slice 6 classifies the resulting 4,466-byte raw slope
as incomplete under the then-shared 2 KiB ceiling. Landing Slice 7 separates
the comparison budgets: the schema-only pair passes its unchanged 2 KiB gate
at 1,038 bytes per entity, and the reachable pair passes its explicitly
authorized 8 KiB gate at 4,466 bytes per entity with 3,726 bytes of headroom.

## Landing Ledger

| Slice | Outcome | Status |
| ---: | --- | --- |
| 1 | Freeze exact downstream evidence and a maintained reachable-entity specialization comparison | Complete |
| 2 | Concrete prepared-page cursor with explicit public/trusted execution and generated decode-only leaf | Complete — retained by explicit exception after simplification |
| 3 | Static generated entity descriptors validated against accepted authority | Complete |
| 4 | Concrete exact-key terminal returning prepared rows and position metadata | Complete |
| 5 | Same-entity typed-write batch rows, typed delete admission and removal of parallel proving-consumer encoders | Complete |
| 6 | Cumulative artifact, cost, authority and residual-specialization closeout | Complete — recorded incomplete result under then-shared 2 KiB ceiling |
| 7 | Distinct fail-closed schema and reachable-adapter ceilings | Complete — closeout pass |

## Landing Slice 1 Receipt

Landing Slice 1 is measurement/infrastructure only. It:

- identifies the exact 11-entity Toko source, manifest, lockfile and generated
  source fingerprint;
- distinguishes the workspace Canic `0.109.28` selection from the installed
  Canic `0.109.29` CLI;
- records final raw/code/function/indirect-call and named Twiggy artifacts;
- adds the maintained `reachable_entity_scale` comparison alongside the
  original `schema_entity_scale` control;
- executes focused page, exact-key, insert, update, batch and delete sentinels;
- hard-cuts the report identity to `icydb-wasm-footprint/0.250/v1` while
  retaining representation version `1`; and
- changes no production IcyDB runtime, schema, facade or generated API.

The retained downstream experiment artifact is exactly measured at -57,436
code-section bytes, +15 defined functions and +1 `call_indirect`. Its removed
source is unavailable, so it remains intake rather than an accepted
reproducible A/B. Slice 2 must independently qualify its concrete candidate on
the maintained pair.

## Complexity Delta

Landing Slice 1 left production structure neutral. Its measurement structure
is more complex by two audit actors, one shared operation harness, one focused
live control and three additional machine-reported Wasm structure fields. The
complexity is bounded to measurement infrastructure and replaces the previous
ambiguous unused-entity interpretation with two explicitly named authorities.

## Landing Slice 2 Receipt

The retained cursor converges public/trusted execution and prepared-row
creation without callbacks, stored lane state or a second query builder. It
preserves all 274 export names and the byte-identical embedded Candid payload.
The final Toko A/B removes 35,345 raw bytes, 30,969 code bytes and 12 functions
with flat `call_indirect`. The maintained actor pair adds only 156 raw and 150
code bytes to its nine-entity slope and improves that slope by three functions.

The final candidate misses the original 32 KiB target by 1,799 code bytes. The
user explicitly approved retention after simplification; this is documented as
an evidence-backed candidate exception, not a lowered gate. Focused instruction
qualification passes: page calls move by +690 and +754 instructions, all other
selected operations are unchanged or slightly lower, and none approaches the
material regression boundary.

Production adds one transient cursor type but no behavior axis, persisted
state, execution route, callback or compatibility surface. It deletes the old
parallel typed page wrappers and centralizes preparation. The result is a
modestly larger source surface with a simpler execution flow and neutral
runtime state space.

## Landing Slice 3 Receipt

Generated entities now provide one associated static descriptor containing
only source identity, ordered primary-key identity, field shape and
nullability. Accepted snapshots resolve and validate that data before issuing
the existing opaque binding. The retired facade/core request DTOs, recursive
conversion methods, temporary request vector and generated per-field binding
statements are removed without a compatibility path.

The exact sequential Toko A/B removes 20,089 raw bytes, 31,462 code bytes and
19 defined functions, with flat `call_indirect`, unchanged 274 normalized
exports and byte-identical embedded Candid. Cumulatively against `v0.249.6`,
the first three slices remove 55,434 raw bytes, 62,431 code bytes and 31
defined functions.

Focused scalar/list/named/nullability, composite-key, rename, stale-binding,
generated compile, lint and PocketIC controls pass. Production gains no
runtime authority or behavior axis; its binding flow becomes one static data
source feeding one accepted validation owner. Landing Slice 4 subsequently
completed the concrete exact-key boundary without changing that authority.

## Landing Slice 4 Receipt

Exact-key count and byte limits, duplicate lowering, lookup and execution
budgets now run once over concrete `PrimaryKeyValue` inputs. The facade
prepares each distinct present row once and returns bounded position metadata;
only final entity decode and duplicate reconstruction remain generic. The old
generic structural wrapper is removed.

The exact sequential Toko A/B removes 12,319 raw bytes, 12,306 code bytes and
five defined functions with flat `call_indirect`, unchanged 274 normalized
exports and byte-identical embedded Candid. Cumulatively against `v0.249.6`,
the first four slices remove 67,753 raw bytes, 74,737 code bytes and 36 defined
functions.

Focused missing, duplicate, ordering, one/many parity, composite-key, stale
binding, budget, heap/journal and public compile controls pass. No cache, key
format, execution route, persisted state or behavior axis is added. The
maintained reachable slope was nearly halved but remained above 2 KiB, making
Landing Slice 5 the next reviewable handoff at that boundary.

## Landing Slice 5 Receipt

Source-bound typed mutations now include delete. One same-entity core admission
validates a binding/catalog once, lowers the bounded intent sequence and enters
the existing canonical atomic executor. The facade enforces exact binding and
row cardinality, then returns one shared prepared projection. The mixed-entity
typed batch remains unchanged for heterogeneous writes.

A provisional facade-only delegation through mixed-entity admission was
rejected after the ten-entity delete sentinel regressed by 294,446 instructions
(`21.7%`). The retained narrowed owner makes that sentinel 75,800 instructions
cheaper than Slice 4. Every selected operation is stable or improves
materially where targeted.

The exact Toko A/B removes 14,123 raw bytes and 13,711 code bytes with flat
`call_indirect`, unchanged 274 normalized exports and byte-identical embedded
Candid. Cumulatively against `v0.249.6`, the first five slices remove 81,876
raw bytes, 88,448 code bytes and 32 functions.

Focused mixed-operation, bounds, stale/mixed binding, atomic rejection,
recovery, order/cardinality, single/batch parity and delete-before-image
controls pass. No commit path, limit, persisted state or behavior axis is
added. Landing Slice 6 subsequently completed the read-only cumulative
closeout and retained this implementation unchanged.

## Landing Slice 6 Receipt

The sequential downstream ledger records 35,345 raw bytes removed by the
prepared-page boundary, 20,089 by static descriptors, 12,319 by the exact-key
terminal and 14,123 by the same-entity write boundary. Cumulatively the exact
Toko actor removes 81,876 raw bytes, 88,448 code bytes and 32 functions, with
flat `call_indirect`, unchanged normalized exports and byte-identical embedded
Candid.

The maintained pair closes at 40,188 raw bytes, 38,604 code bytes and 73
functions across nine added reachable entities, with no indirect-call slope.
Its 4,466-byte raw slope exceeds the 2,048-byte ceiling, so 0.250 does not meet
its complete size objective. Final attribution names 28,795 duplicate bytes in
the per-entity operation/conversion, single-write and batch-write leaves; the
common page, exact-key and atomic execution owners no longer appear as
entity-scaled groups.

The remaining seam is a separately reviewable generated codec/leaf descriptor,
not a closeout correction. It would need to prove a sufficient final raw
saving without adding the function-pointer, erased-callback, runtime-registry
or shim complexity excluded by this line. Landing Slice 7 subsequently changes
only the measurement budget and leaves this residual ledger intact.

## Landing Slice 7 Receipt

The previously shared 2 KiB entity ceiling is hard-cut into two explicit
fail-closed authorities. Dead-strippable schema growth remains capped at 2,048
bytes per added entity. Reachable page, exact-key and write adapters use the
user-authorized 8,192-byte ceiling. The report-only reachable path is removed;
both comparisons now fail closed against their own budget.

The exact maintained artifacts pass both gates: schema-only growth is 9,341
raw bytes across nine entities, rounded up to 1,038 bytes per entity, while
reachable-adapter growth is 40,188 raw bytes, rounded up to 4,466 bytes per
entity. The respective headroom is 1,010 and 3,726 bytes per entity.

Focused exact-limit, one-byte-over-limit, rounding, real-artifact, formatting,
command-build, shell-syntax and selected all-feature Clippy controls pass. No
production API, runtime behavior, Candid, cursor, persistence format or Wasm
artifact changes. All seven landing slices and every corrected closeout
criterion now pass; 0.250 is ready for user-owned release validation.
