# IcyDB 0.250 Landing Slice 6 Receipt

Date: 2026-08-31

Disposition: closeout audit complete; the line's size objective is incomplete.
The retained implementation removes 81,876 final raw bytes and 88,448
code-section bytes from the exact Toko proving actor, but the maintained
reachable-entity comparison remains at 4,466 raw bytes per added entity and
does not pass the 2,048-byte ceiling.

Subsequent disposition: superseded by the explicitly authorized Landing Slice
7 budget correction. Slice 7 retains the dead-strippable schema ceiling at 2
KiB, assigns the distinct reachable-adapter comparison an 8 KiB ceiling and
records a passing final closeout. This receipt remains the exact Slice 6
finding under the ceiling in force at that boundary.

## Closeout Verdict

All six planned landing slices are complete and the retained production work
passes its semantic, authority, artifact and cost controls. Public exports,
Candid, cursor bytes and persisted version-1 formats remain unchanged. No
callback/vtable slope, compatibility path, runtime registry or second
execution owner was introduced.

The size objective nevertheless remains incomplete. The final one-to-ten
reachable-operation pair grows by 40,188 raw bytes across nine added entities.
The allowed growth is 18,432 bytes, leaving a 21,756-byte total excess, or
2,418 bytes per entity after the report's ceiling division. The ceiling is not
raised and the passing dead-strippable schema-cardinality control is not used
as a substitute.

This is not a rejection of the retained work. Relative to the opening
reachable baseline, the nine-entity raw slope falls by 43,352 bytes, from
83,540 to 40,188, while the exact downstream actor becomes 81,876 raw bytes
smaller. The result is useful but does not satisfy every 0.250 exit criterion.

## Exact Sequential Downstream Ledger

Landing Slice 1 changed measurement only, so the frozen `v0.249.6` Toko
artifact is also its production baseline. Every later row uses the immediately
preceding retained artifact and the exact source/toolchain receipt frozen by
Slice 1.

| Boundary | Raw Wasm | Raw delta | Code section | Code delta | Functions | Function delta | `call_indirect` |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `v0.249.6` / Slice 1 | 11,195,648 | 0 | 10,363,988 | 0 | 22,349 | 0 | 1,042 |
| Slice 2 | 11,160,303 | -35,345 | 10,333,019 | -30,969 | 22,337 | -12 | 1,042 |
| Slice 3 | 11,140,214 | -20,089 | 10,301,557 | -31,462 | 22,318 | -19 | 1,042 |
| Slice 4 | 11,127,895 | -12,319 | 10,289,251 | -12,306 | 22,313 | -5 | 1,042 |
| Slice 5 / Slice 6 closeout | 11,113,772 | -14,123 | 10,275,540 | -13,711 | 22,317 | +4 | 1,042 |
| Cumulative | 11,113,772 | **-81,876** | 10,275,540 | **-88,448** | 22,317 | **-32** | **1,042** |

Final data grows by 6,628 bytes, from 648,078 to 654,706, while raw and code
both fall. Deterministic gzip falls by 19,371 bytes, from 3,832,344 to
3,812,973. Raw non-gzipped Wasm remains the acceptance authority.

The final Toko artifact is exactly:

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| Final Wasm | 11,113,772 | `865bf2a90507b2ec52ce74ff97b1784ec05ca7d476afa3f9f71040e88be10abb` |
| Deterministic gzip | 3,812,973 | `cdc8d3b4f627470a10d442930bdb3ea852b764248b79e6c27482ff9cb03a8465` |
| Generated Candid | 130,588 | `387e594e371ea60a04c076be464d5c9290ac357662415f23dd26853774b31bfe` |

The normalized 274-name export set and embedded Candid payload remain
byte-identical to the frozen control. The embedded payload SHA-256 remains
`df39a9bf1d60efd6a6b1fba629f50905ec4bbddb87a51246748296d4235628c9`.
An independent Slice 5 rebuild reproduced final Wasm and gzip byte-for-byte;
Slice 6 changes no production source and rechecks those preserved artifacts.

## Maintained Reachable-Entity Result

| Metric | One entity | Ten entities | Nine-entity delta | Per entity |
| --- | ---: | ---: | ---: | ---: |
| Final raw Wasm | 2,871,318 | 2,911,506 | 40,188 | 4,466 |
| Code section | 2,706,204 | 2,744,808 | 38,604 | 4,290 |
| Functions | 7,451 | 7,524 | 73 | 9 |
| `call_indirect` | 717 | 717 | 0 | 0 |
| Data section | 152,701 | 154,135 | 1,434 | 160 |
| Deterministic gzip | 1,163,203 | 1,163,809 | 606 | 68 |

The artifacts expose the same 190-byte Candid interface with SHA-256
`f8d63096cbc2d7ff9d22b9de8d880ee5b5acc7d7fd2e96b19f46a3b75ad21045`.
Their exact final Wasm SHA-256 values are
`a40024530220cc19d5dbe177c5286451eb7220154a4106ea4b5db1c612e41c7d`
and
`a29b2080efc13e05f3ecb6c4acc2714be8b52d255862f793cf073fc4f2e96e56`.

The maintained report-only gate returns `status=exceeded`. The focused live
control still reaches bounded page, exact-key, insert, patch, same-entity batch
and delete for both actors with the exact retained Slice 5 results and
instruction counts. No selected operation crosses the material regression
boundary.

## Residual Specialization Ledger

Final symbol-bearing attribution was rebuilt for both reachable-operation
actors after Slice 5. This profile is intentionally non-deployable and keeps
names, so its Twiggy bloat values are attribution evidence rather than bytes
that can be added to the optimized final delta.

| Residual family | Twiggy duplicate bloat | Classification |
| --- | ---: | --- |
| Runtime-selected per-entity operation and conversion wrapper | 17,217 bytes | Separately reviewable future design; the current audit harness makes six generated leaves reachable through one generic wrapper, but it is not a production execution authority to erase inside closeout |
| Single typed write encode/decode leaf | 7,213 bytes | Required Rust input encoding and final row decoding around an already concrete terminal; a smaller generated codec descriptor would require a separately measured design |
| Same-entity typed batch encode/decode leaf | 4,365 bytes | Required input encoding and final row decoding around the concrete batch terminal; shares the same future codec-descriptor question |
| Static entity descriptor and exact typed key/value leaves | Not isolated additively | Accepted current cost; generated source data and conversions still differ by entity while accepted snapshots remain runtime authority |

The three named groups contain 28,795 bytes of duplicate bloat in the
attribution profile. No entity-scaled prepared-page execution, exact-key
lookup, atomic batch executor or structural delete group remains in the named
ledger. `call_indirect` is flat, so the rejected callback/vtable experiment has
not resurfaced.

The only plausible next optimization seam is a generated static codec/leaf
descriptor that can share runtime dispatch without rebuilding common control
flow. It must be designed independently because function pointers, erased
callbacks or a runtime registry would add state and indirect-call complexity
explicitly excluded from 0.250. Closeout does not pre-authorize that design or
assume it will clear the remaining 21,756-byte gap.

## Authority And Compatibility Audit

- accepted schema snapshots remain the runtime authority; generated static
  descriptors are validated proposal/source contracts only;
- page, exact-key and same-entity batch terminals delegate to the existing
  query, lookup, mutation, commit and recovery owners;
- no cursor, persisted-row, journal, commit-marker, schema/catalog codec or
  format-version source changed in the 0.250 worktree;
- the maintained pair's Candid bytes match exactly, while Toko preserves its
  normalized export set and embedded Candid payload; and
- no compatibility alias, fallback decoder, second format or legacy route was
  added.

## Validation And Complexity

The final attribution rebuild, report-only entity ceiling check, focused live
operation-mix test and cursor string round-trip test pass. The ceiling command
passes as a report and correctly classifies the measurement as exceeded.
Prior slice receipts record focused core/facade semantics, generated compile
contracts, accepted-authority validation, recovery, all-feature Clippy,
operation controls and reproducible downstream builds. The full repository
suite and downstream CI/install remain user-owned validation.

Across Slices 1-5, production and focused-test changes touch 17 files at
approximately `+1,140/-545` lines. Slice 6 adds documentation only. Delivery
crosses Other Core, model generation, Build / Canister measurement,
Integration Tests and documentation.

The source surface is larger because the new concrete boundaries and their
invariants are directly tested. Runtime structure is simpler: common page,
exact-key and same-entity batch work each has one concrete owner, generated
binding construction becomes static data, and the proving consumer deletes a
parallel structural encoder family. Runtime state space remains neutral. The
line's implementation-shape verdict is simpler execution with more explicit
source and test coverage; its size-objective verdict remains incomplete.
