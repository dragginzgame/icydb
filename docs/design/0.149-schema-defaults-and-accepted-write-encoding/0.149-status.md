# IcyDB 0.149 - Status

## Current State

In progress.

0.149 has started with accepted write encoding and the first schema-owned
default payload contract. Defaults are now representable as accepted schema
slot payloads, can materialize missing appended row slots, and have a narrow
macro authoring surface through supported literal `default = ...` values and
explicit `db_default = ...` overrides.
Accepted structural insert/replace staging now uses those accepted defaults
when sparse write input omits defaulted fields.
Schema description rows also expose encoded database-default metadata without
decoding default payloads into runtime values.
The default payload boundary is now persisted-slot-byte based end to end:
macro-authored scalar defaults emit scalar slot envelopes, transition policy
validates through accepted field decode contracts, and structural insert/replace
tests cover omitted defaulted fields.
Accepted old-row canonicalization now emits rows through the selected row
contract, so accepted sparse patch replay and accepted row re-emission encode
through accepted field contracts instead of the generated runtime-value
canonicalizer.
Sparse structural patch serialization is now split into accepted and
generated lanes. Accepted writes encode through accepted field contracts only;
generated field-model encoding remains isolated to generated-only compatibility
callers.
Dense runtime-value row emission now has the same split, so accepted old-row
overlays emit final canonical rows through accepted field contracts only.
Mutation write-bound validation also chooses accepted or generated field
authority once before iterating patch or row slots.
Persisted-row direct read helpers now do the same for selected-slot decode,
direct projection, and value-storage scalar reads.
The shared row-contract decode helpers now expose the same accepted/generated
lane split for runtime value decode, scalar decode, and structural validation.
Structural save staging now splits existing-row identity validation, update
after-image construction, and commit before-image construction into accepted
and generated helpers too.
Typed save commit before-image construction follows the same split.
Structural mutation inputs now use separate generated sparse, accepted sparse,
and accepted complete constructors instead of carrying optional schema authority
inside the shared payload constructor.
Existing-row save-rule resolution now also has accepted-contract and
generated-contract entrypoints, so typed and structural save staging select row
identity authority before loading before-images.
Structural patch write-bound validation now follows the same pattern with
accepted-contract and generated-contract entrypoints before scalar bound loops.
Structural update patch replay and typed/structural commit-marker before-image
construction now select accepted or generated helpers directly from the staging
boundary instead of using option-taking helper wrappers.

## Completed

- Added accepted structural field encoding from `PersistedFieldKind`.
- Added accepted runtime-value field-contract encoding from
  `AcceptedFieldDecodeContract`.
- Routed accepted structural patch serialization through accepted field
  contracts when a row contract is attached.
- Preserved decimal scale normalization in the accepted write encoder.
- Added generated-compatible parity tests for accepted structural field
  encoding.
- Kept `Value` runtime-only; persisted rows remain slot-framed field-codec
  bytes.
- Added `SchemaFieldDefault::SlotPayload(...)` for encoded persisted defaults.
- Added `FieldDatabaseDefault::EncodedSlotPayload(...)` as the generated-model
  bridge for future explicit database defaults.
- Added `DefaultIfMissing` missing-slot policy for accepted row contracts.
- Materialized missing accepted slots from encoded default payloads through the
  accepted field decode contract.
- Accepted append-only generated fields with explicit persisted default payloads
  in transition policy.
- Added explicit `db_default = ...` field macro syntax for overriding database
  defaults separately from Rust construction defaults.
- Encoded supported required primitive default literals into generated
  `FieldDatabaseDefault::EncodedSlotPayload(...)` metadata.
- Made supported literal `default = ...` values emit database defaults by
  default, while function/path defaults such as `Ulid::generate` remain
  Rust-only.
- Added fail-closed derive validation for unsupported default targets,
  optional/repeated fields, text bounds, literal-kind mismatches, and numeric
  range overflow.
- Validated append-only explicit default payloads through the accepted field
  kind during transition policy and rejected defaults that decode as `NULL`.
- Routed accepted structural insert/replace serialization through a complete
  after-image serializer that fills omitted nullable/defaulted slots from the
  accepted row contract.
- Added generated and accepted schema description metadata for encoded database
  defaults using `default=slot_payload(bytes=N)`.
- Fixed macro-authored scalar database defaults to emit persisted scalar slot
  envelopes instead of Structural Binary field payloads.
- Moved transition-policy default validation onto
  `AcceptedFieldDecodeContract` so schema defaults are checked through the same
  accepted decode boundary runtime row materialization uses.
- Added structural insert/replace tests proving omitted defaulted fields are
  filled from accepted schema defaults and replace does not inherit old values.
- Added a transition-policy guard proving database-default changes on existing
  fields remain rejected.
- Added an old-row canonical re-emission guard proving defaulted appended fields
  are written into the current accepted row image.
- Routed accepted structural row re-emission and accepted sparse patch replay
  through a row-contract canonical row emitter.
- Removed the dead generated-model runtime-value canonicalizer helper.
- Split sparse structural patch serialization into accepted-contract and
  generated-only compatibility lanes.
- Removed generated field fallback branching from the accepted sparse patch
  encoder.
- Split dense runtime-value row emission into accepted-contract and
  generated-only compatibility lanes.
- Routed accepted serialized patch replay through accepted-only dense row
  emission.
- Split mutation structural patch bounds validation into accepted-contract and
  generated-contract helpers.
- Split persisted structural row invariant validation into accepted-contract
  and generated-contract helpers.
- Split direct selected-slot decode into accepted-contract and
  generated-contract helpers.
- Split direct projection materialization into accepted-contract and
  generated-contract helpers.
- Split value-storage scalar reads into accepted-contract and generated-contract
  helpers.
- Split row-contract runtime value decode, scalar decode, and structural
  validation helpers into accepted-contract and generated-contract lanes.
- Split structural save existing-row identity validation into accepted-contract
  and generated-contract helpers.
- Split structural update after-image construction into accepted-contract and
  generated-contract helpers.
- Split structural commit-marker before-image construction into
  accepted-contract and generated-contract helpers.
- Split typed commit-marker before-image construction into accepted-contract
  and generated-contract helpers.
- Replaced the option-taking structural mutation input constructor with
  generated sparse, accepted sparse, and accepted complete constructors.
- Tightened structural patch write-bound validation so generated row-layout
  lookup only runs in the generated lane.
- Split existing-row save-rule resolution into accepted-contract and
  generated-contract entrypoints.
- Split structural patch write-bound validation into accepted-contract and
  generated-contract entrypoints.
- Removed option-taking structural update replay and commit-marker before-image
  wrappers from typed and structural save staging.

## Remaining

- Keep unsupported defaults and unsupported transition shapes fail-closed.

## Validation

- `cargo test -p icydb-core structural_field --features sql -- --nocapture`
  passed for the initial accepted encoder slice.
- `cargo test -p icydb-core accepted_row --features sql -- --nocapture`
  passed after defaulted missing-slot materialization.
- `cargo test -p icydb-core schema_transition_policy --features sql -- --nocapture`
  passed after defaulted additive transition admission.
- `cargo test -p icydb-schema-derive field::tests -- --nocapture` passed after
  explicit macro default authoring.
- `cargo test -p icydb-schema-tests entity -- --nocapture` passed after
  generated model database-default coverage.
- `cargo test -p icydb-core schema_transition_policy --features sql -- --nocapture`
  passed after transition-policy default payload validation.
- `cargo test -p icydb-core existing_field_default_changes --features sql -- --nocapture`
  passed after adding the existing-field default-drift guard.
- `cargo test -p icydb-core accepted_row_contract_reemits_defaulted_rows_with_accepted_default --features sql -- --nocapture`
  passed after adding the old-row defaulted re-emission guard.
- `cargo test -p icydb-core serialize_complete_structural_patch_with_accepted_contract_fills_missing_database_defaults --features sql -- --nocapture`
  passed after accepted insert/replace default-fill serialization.
- `cargo test -p icydb-core mutation_save --features sql -- --nocapture` passed
  after accepted insert/replace default-fill routing.
- `cargo test -p icydb-core mutation_save --features sql -- --nocapture` passed
  after persisted database-default payloads moved to scalar slot envelopes.
- `cargo test -p icydb-core schema_describe_includes --features sql -- --nocapture`
  passed after schema description default metadata rendering.
- `cargo test -p icydb-core schema_describe_uses_accepted_top_level_field_metadata --features sql -- --nocapture`
  passed after accepted-schema description default metadata rendering.
- `cargo check -p icydb-core --tests --features sql` passed after accepted
  insert/replace default-fill routing.
- `cargo clippy --workspace --all-targets -- -D warnings` passed after the
  accepted insert/replace default-fill slice.
- `cargo check -p icydb-core --tests --features sql` passed after persisted
  database-default payload validation.
- `cargo clippy --workspace --all-targets -- -D warnings` passed after
  persisted database-default payload validation.
- `make check-invariants` passed after persisted database-default payload
  validation.
- `git diff --check` passed after persisted database-default payload validation.
- `make check-invariants` passed after the accepted insert/replace
  default-fill slice.
- `git diff --check` passed after the accepted insert/replace default-fill
  slice.
- `cargo test -p icydb-core omitted_database_default_fields --features sql -- --nocapture`
  passed after persisted-slot default payload correction.
- `cargo test -p icydb-core mutation_save --features sql -- --nocapture` passed
  after end-to-end database-default omission coverage.
- `cargo test -p icydb-core accepted_row --features sql -- --nocapture` passed
  after row-contract canonical row emission.
- `cargo test -p icydb-core mutation_save --features sql -- --nocapture` passed
  after row-contract canonical row emission.
- `cargo check -p icydb-core --tests --features sql` passed after
  row-contract canonical row emission.
- `cargo clippy --workspace --all-targets -- -D warnings` passed after
  row-contract canonical row emission.
- `make check-invariants` passed after row-contract canonical row emission.
- `git diff --check` passed after row-contract canonical row emission.
- `cargo test -p icydb-core accepted_row --features sql -- --nocapture` passed
  after sparse structural patch serialization was split into accepted and
  generated lanes.
- `cargo test -p icydb-core mutation_save --features sql -- --nocapture` passed
  after sparse structural patch serialization was split into accepted and
  generated lanes.
- `cargo check -p icydb-core --tests --features sql` passed after sparse
  structural patch serialization was split into accepted and generated lanes.
- `cargo clippy --workspace --all-targets -- -D warnings` passed after sparse
  structural patch serialization was split into accepted and generated lanes.
- `make check-invariants` passed after sparse structural patch serialization
  was split into accepted and generated lanes.
- `git diff --check` passed after sparse structural patch serialization was
  split into accepted and generated lanes.
- `cargo test -p icydb-core accepted_row --features sql -- --nocapture` passed
  after direct row-read helpers were split into accepted and generated lanes.
- `cargo test -p icydb-core row_decode --features sql -- --nocapture` passed
  after direct row-read helpers were split into accepted and generated lanes.
- `cargo check -p icydb-core --tests --features sql` passed after direct
  row-read helpers were split into accepted and generated lanes.
- `cargo clippy --workspace --all-targets -- -D warnings` passed after direct
  row-read helpers were split into accepted and generated lanes.
- `make check-invariants` passed after direct row-read helpers were split into
  accepted and generated lanes.
- `git diff --check` passed after direct row-read helpers were split into
  accepted and generated lanes.
- `cargo test -p icydb-core mutation_save --features sql -- --nocapture` passed
  after structural and typed save staging helpers were split into accepted and
  generated lanes.
- `cargo test -p icydb-core mutation_save --features sql -- --nocapture` passed
  after structural mutation input construction, structural patch write-bound
  validation, existing-row save-rule resolution, update replay, and
  before-image construction were split into explicit accepted/generated lanes.
- `cargo check -p icydb-core --tests --features sql` passed after structural
  mutation input construction, structural patch write-bound validation,
  existing-row save-rule resolution, update replay, and before-image
  construction were split into explicit accepted/generated lanes.
- `cargo clippy --workspace --all-targets -- -D warnings` passed after
  structural mutation input construction, structural patch write-bound
  validation, existing-row save-rule resolution, update replay, and
  before-image construction were split into explicit accepted/generated lanes.
- `make check-invariants` passed after structural mutation input construction,
  structural patch write-bound validation, existing-row save-rule resolution,
  update replay, and before-image construction were split into explicit
  accepted/generated lanes.
- `git diff --check` passed after structural mutation input construction,
  structural patch write-bound validation, existing-row save-rule resolution,
  update replay, and before-image construction were split into explicit
  accepted/generated lanes.
