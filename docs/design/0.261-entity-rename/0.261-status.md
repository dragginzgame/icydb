# 0.261 — Status

Authorised 2026-09-20 for the entity-rename outcome discussed with the user.
Authority: [design](0.261-design.md).

## Current handoff

C49 — Prove one-sided primary-key continuation at closeout is complete. The
shared dynamic page test now reads all rows for strict and inclusive lower and
upper bounds in both scan directions under the two-row test envelope. It checks
page progress, exact ordered IDs, physical exhaustion and at most four visited
rows per page. All six focused page-limit tests and strict core library/test
lint pass. This correction changes one test and two notes files, roughly 80 net
lines; runtime shape stays neutral. Raw Wasm, IC cycles and instructions are
unmeasured. The pre-existing lockfile dependency churn is untouched. Full-suite
and release validation remain user-owned. The 0.261 closeout has no remaining
identified correctness or focused-coverage blocker; it is ready within this
scope for user-owned validation.

### C48 handoff

C48 — Qualify the C37 warm-read instruction increase is complete. A matched
current-source A–B–A PocketIC comparison changed only the five-line replicated
startup preparation call. Cold successor reads improve by 94.09–94.51%, while
four warm checkpoints increase by 3.92–8.42%; the repeated baseline reproduced
its raw Wasm identities and instruction counts. The retained call remains the
simplest way to avoid repeated query-only compilation. The whole-query
measurements do not isolate a safe warm-path removal, so C48 makes no runtime
change. Both focused entity-rename cases passed for each variant. Eight
disposable PocketIC servers were started and stopped; no persistent network
was touched. The first sandboxed bind attempt failed and was rerun with local
loopback access. [Qualification](c48-warm-read-qualification.md) records raw
Wasm bytes, IC instructions and cycles. This documentation-only slice changes
two files and leaves runtime complexity neutral. Full-suite and release
validation remain user-owned.

### C47 handoff

C47 — Resume primary-key fast scans from the authenticated cursor boundary is
complete. The fast route now forwards the existing access continuation instead
of rebuilding empty bindings. A focused 16-row test confirms that two-row pages
reach physical exhaustion in ascending and descending order while visiting at
most three rows per page. The cursor format, public API, and stored rows are
unchanged. All five focused page-limit tests, strict core lint, the Wasm library
check, formatting and whitespace checks pass. The slice touches six production
files and one test with about 28 net added lines; removing the empty-binding
helper makes the route simpler. Full-suite and release validation remain
user-owned.

Raw Wasm, cycles, and instructions for C47 are unmeasured; native tests do not
substitute for them.

### C46 handoff

C46 — Seek one-sided primary-key bounds is complete. Strict scalar `>`, `>=`,
`<` and `<=` predicates retain their authored endpoint and select the existing
primary range path with an open side. The physical stream supplies the entity
prefix limit for that side in ascending or descending order. Residual filtering
still decides exclusive comparisons; composite-key and unsupported-coercion
access rules remain unchanged. Existing two-ended fingerprints retain their
bytes, while new open ranges have distinct identities. Affected saved
continuations must restart; stored data and public API shapes do not change.

Focused planner, physical seek, fingerprint, access and explain checks pass
(121 selected tests), as do strict scoped core lint, the Wasm library check,
formatting and whitespace checks. The first lint pass found local style/dead-code
warnings; the required repository lint sweep reported the same warnings,
which were fixed before the scoped gate passed. Raw Wasm, IC cycles and
instruction deltas are unmeasured. The pre-existing dirty lockfile is
preserved. The slice touches 26 files with approximately 255 net added lines,
mostly endpoint propagation, tests and notes; implementation shape becomes
modestly more complex through optional bounds but adds no execution route or
persisted state. Repeated query-only preparation costs and the tiny-page
unfiltered continuation anomaly remain separate follow-ups. Full-suite and
release validation remain user-owned.

### C45 handoff

C45 — Bound dynamic execution by the authored row limit is complete,
authorised after the issue-16 diagnostic. The shared session page owner now
caps its existing execution limit by the authored total, keeping that cap
stable across continuation so pages reuse the same prepared plan. Raising
budgets or introducing another execution route would not resolve the over-read;
this correction adds no runtime axis, format or fallback. Continuation
signatures exclude page limits and the token authenticates the authored total
separately; no cursor-format change or blanket restart is required. Stored
data and API shapes are unchanged.

Before correction, trusted dynamic `LIMIT 1` read all 128 fixture payloads
(267,392 stored bytes). The regression now succeeds within a 4,608-byte
request allowance and at most two row reads, including lookahead, for both
16-row and 128-row stores. These are accounting counters, not IC performance
measurements. Residual predicates and materialized ordering can still read
more than the returned limit.

All 17 selected core tests pass, including four new limit regressions and the
existing page, proof, typed-explain and continuation checks. The four new
tests also pass with test-only page constants temporarily aligned to the
production 1,024-row/10,000-entry envelope; normal test constants are restored.
Strict core all-target/all-feature lint, the public Rust 1.88 check,
documentation references and whitespace checks pass. Zero limits, ASC/DESC order,
full/selected output, cold/warm reuse, store growth, selective empty pages,
multi-page total limits and exhaustive reads are covered. No test or lint
failure occurred during this implementation slice.

Seven files change, approximately 300 net added lines, chiefly direct tests
and documentation; production flow and state-space complexity stay neutral.
No dependencies, Cargo versions, network lifecycle or stored data change.
Raw Wasm, IC-cycle and instruction deltas are unmeasured. Full-suite/release
validation remains user-owned. One-sided PK seeking remains C46, a separate
follow-up. The investigation's tiny-page unfiltered continuation anomaly
still needs isolation and is not claimed repaired by this limit correction.

### C44 diagnostic handoff

C44 — Attribute unavailable typed-binding sources is complete, authorised by
continuation after C43. ICYDB-040's missing source identity now travels from
accepted binding issuance to `TypedAdapterError::BindingUnavailable(context)`.
The context identifies the generated entity and optional primary/ordinary
field; invalid named-type keys identify their containing field. Valid source
keys are complete, while malformed oversized keys retain UTF-8 prefixes within
the schema's 128-byte key bound. Value/row adapter failures keep their separate
meaning, and database failures retain the compact error boundary.

The context-free error could not diagnose this mismatch. The core binding owner
now supplies one bounded context that the facade carries without reclassifying
source text. The only added diagnostic state is entity-versus-field location;
there is no binding mode, new execution flow, heap allocation for the context,
schema dump, persisted format or compatibility fallback. Public exhaustive
adapter-error matches and binding-issuance handlers need source updates.

All 21 focused final tests pass: 19 core adapter tests, the generated native
schema-mismatch probe and the existing generated native read/write probe.
Strict scoped all-target/all-feature lint, the public no-default-feature
Rust 1.88 check, documentation references, four structural fixtures and
whitespace checks pass. Fixture setup initially used the wrong trait/import
and then a Rust module path instead of a generated logical entity key; both
were corrected. No production cache defect was found or repaired.

This slice changes 11 files, approximately 300 net added lines, including
diagnostics, direct tests, exports, guidance and release notes. Diagnostic
payload complexity increases modestly; runtime authority and execution flow
are unchanged. No dependencies, Cargo versions, network lifecycle or stored
data change. Raw Wasm, IC-cycle and instruction deltas for C44 are unmeasured;
C43's released-version measurements do not qualify this new diagnostic.
Full-suite/release and downstream Toko Miner adoption remain user-owned.
Actual downstream mismatch verification and an isolated build-freshness
reproduction remain follow-ups; generic continuation returns to read-only
closeout within this minor, not another implementation or minor line.

### C43 measurement handoff

C43 — Matched Toko Miner application measurement is complete. The
[report](c43-toko-miner-comparison.md) and
[structured evidence](c43-toko-miner-measurements.json) compare registry
IcyDB 0.261.6/0.261.8 on frozen application commit `37f38533`, Canic/CLI
0.110.38 and identical fixtures/features. Only six IcyDB package selections
differ. The live downstream checkout advanced during measurement; its newer
work is outside this proof.

Both managed builds and all eight final focused probe runs pass. Each release's
two repetitions match exactly, including equivalent-work checks across releases.
Documentation references (218 maintained and 268 handoff/history), four
structural fixtures and whitespace checks pass.
Game Shard shrinks by 5,713 raw Wasm bytes; all application roles together shrink
by 9,092 bytes, with User Hub increasing by 99 bytes. Gross action cycles range
from -0.15009% to +0.02351%; action-body instructions range from -0.23212% to
+0.05406%. The report retains retry, checkpoint, enrolment and readiness
regressions as well as improvements. Same-release upgrade delivery saves
10,046,064 cycles; its readiness interval costs 350 more cycles.

No production code, runtime state, dependency pin or Cargo version changes.
Five documentation/evidence files change in this slice, with roughly 770 added
lines; runtime complexity is unchanged. Ten disposable PocketIC fixtures were
used, including preliminary host-probe validation; no existing ICP network was
changed. Setup failures and corrections are recorded in the report. Query
instructions and absolute installation/startup costs remain unmeasured; no host
timing or large application-wide performance claim is made. Full downstream
CI/release validation remains user-owned. Binding diagnostics and query-specific
instruction attribution remain separate follow-ups, not automatic next slices.

### C42 documentation handoff

C42 — Correct DISTINCT resource ownership documentation is complete. Removed
helper examples and the stale module reference are replaced by the maintained
grouped, scalar-aggregate and projected-row DISTINCT owners. Ordinary source
links use the existing documentation gate; no wording assertions are added.
This is documentation-only; runtime behavior and implementation shape are
unchanged. Published 0.261.8 remains untouched; notes open 0.261.9 without
changing Cargo versions.

All three focused DISTINCT state-budget tests, 218 local reference checks,
four structural fixtures and whitespace checks pass. Four documentation files
change with approximately 40 net added lines, including handoff/release notes;
ownership guidance gets clearer without adding runtime complexity. No network
lifecycle action occurred. Wasm, IC-cycle and instruction deltas are unmeasured.
Full repository/release validation remains user-owned. Diagnostic JSON export
remains a separate follow-up, not part of this completed slice.

### Published 0.261.8 handoff

C40/C41 — The user authorised both outstanding follow-ups together. C40's
filtered-cache investigation is complete: current operands are normalized and
admitted before reuse, identical bound plans are memoized, and changed operands
rebind through the existing template. Five focused tests pass for A/B/A values,
arity, wrong types, preparation charging and bounded retention. No measured
cycle/instruction benefit justifies another cache or prepared-input authority;
runtime cache code remains unchanged. A future optimization needs matched Wasm
evidence, not native timing.

C41 is complete: accept a bounded JSON rendering of the existing public
`icydb::Error` through `diagnostic --error-json PATH` (`-` for stdin). The need is
lossless transfer of numeric facts and typed query-field context, which manual
code/fact input cannot represent completely. Reuse the public decoder and
existing diagnostic renderer/resolvers rather than invent a wrapper protocol.
The only new axis is a mutually exclusive CLI input source; no runtime state,
wire shape, cache, dependency or configuration is added. Retain manual lookup
as a maintained convenience, not a compatibility fallback. Decoding is capped
at 64 KiB and requires a JSON object; full-width integer facts remain exact.

All 54 focused diagnostic tests and the five cache tests pass. File-to-renderer
context, stdin delivery, malformed/oversized input, exact byte limits and
conflicting arguments are checked. Initial lint found an overlong command
function, eager optional lookup and a test integer cast; input handling now
delegates to shared schema resolution, the lookup is lazy and the conversion
is checked. The required repository lint sweep and focused CLI lint pass on
the corrected code. Formatting, documentation links and whitespace pass.

This follow-up changes seven files with approximately 280 net added lines.
Host CLI complexity grows modestly for one bounded input source; canister and
cache implementation shape is unchanged. No dependency, Cargo version, format
or network lifecycle change occurred. Notes remain under active 0.261.8.
Raw Wasm, IC-cycle and instruction deltas are unmeasured. Full-suite/release
validation remains user-owned. Both requested investigations are closed;
no cache optimization is claimed, and any future one needs matched measurement.

### C39 DISTINCT handoff

C39 — Remove redundant DISTINCT admission configuration is complete, explicitly
authorised after the read-only follow-up audit. The numeric presence gate is
removed without a replacement flag. Grouped admission retains its group and byte
ceilings; execution owns DISTINCT count and memory enforcement. Explain retains
aggregate counts. Four owner-local behavioral tests cover cap derivation and
saturation, exact count/memory limits, typed overflow precedence, duplicates at
capacity and unchanged keys/accounting after rejection.

All 22 focused tests pass on the final source, including grouped admission and
SQL DISTINCT execution. The required repository lint sweep, strict all-feature
core library/test lint and SQL-free core lint pass. Initial lint caught two
by-reference getters after the policy shrank; both now take the copyable policy
by value. Formatting, 215 local references, admission-guard fixtures and
whitespace checks pass. Full repository/release tests remain user-owned.

Seven files change in this slice, with approximately 150 net added lines, mostly
behavioral tests; production configuration and branching get simpler. The
combined C38/C39 worktree touches 17 files. Resource limits, public APIs, formats,
dependencies and Cargo versions are unchanged. Release notes remain under the
active 0.261.8 entry. No network lifecycle action occurred. Raw Wasm bytes,
IC cycles and instruction deltas are unmeasured; no performance claim is made.
Filtered-cache and CLI investigations remain outside this completed slice;
generic continuation returns to read-only closeout within 0.261.

### C38 documentation handoff

C38 — Correct active contracts and consolidate budget documentation is complete,
authorised after the post-0.261.7 read-only audit. The bounded outcome
includes recovery ownership, page/total-limit distinctions, grouped memory,
nested relations and SQL-free explain, with compiled checks for numeric limits.
Resource policy remains code-owned; the resource model is its documentation
home. No production behavior, API, format, configuration or state is added.
Release notes use 0.261.8; Cargo versions are unchanged.

The numeric table is checked against compiled policy/input owners; static
sentence, heading and constant-spelling assertions are removed. The focused
documentation gate passes all 26 native tests and four structural fixtures.
Another 34 focused checks cover input/policy admission, request counters, live
pagination, typed explain, grouped memory and startup. Strict all-feature core
lint, shell lint with sourced files, formatting, 215 local references and
read-admission guard fixtures pass. An initial in-flight test used the earlier
table expectation; rebuilding the final source resolved that mismatch.

Fifteen files change with approximately 100 fewer lines. Documentation and
checking get simpler; runtime implementation shape and resource limits stay
unchanged. No network lifecycle action occurred. Wasm bytes, IC cycles and
instruction deltas are unmeasured; no performance claim is made. Full release
validation remains user-owned.

Filtered-cache preparation and structured diagnostic CLI input remain separate
investigation candidates, not part of this correction or automatically queued
implementation slices.

Following the compiled owners exposed the policy-representation mismatch now
addressed by the separately authorised C39 outcome above.

### Published 0.261.7 handoff

The user authorised C34–C37 together on 2026-09-23 after the documentation
audit. Release 0.261.6 is tagged; new work is recorded under 0.261.7 without
changing Cargo versions.

| Slice | Outcome | State |
| --- | --- | --- |
| C34 | Replace documentation wording assertions with structural checks and codec-owned behavioral evidence | Complete |
| C35 | Use the existing compiled facade fixture as the onboarding source and keep release pins in one place | Complete |
| C36 | Reconcile durability, recovery, observability and 1.0 readiness against maintained owners | Complete |
| C37 | Measure accepted-runtime preparation in the existing replicated lifecycle; retain only with evidence | Retained |

The demonstrated need is stale prose passing checks while current fixtures and
code already encode the maintained contract. Reuse those owners, remove copied
onboarding declarations, and check document structure independently of wording.
No new runtime state, format, timer, configuration or public API is added.
The retained runtime change reuses the accepted-root cache and existing watchdog,
including typed failure classification. The
[matched measurement](c37-runtime-preparation.md) records 94–95% fewer cold-query
instructions and 68 additional raw Wasm bytes per rename actor. Replicated
preparation has a cost: first-read delivery adds about 31.7 million cycles and
post-publication restart adds 51–53 million. Warm-query measurements are mixed.

The focused documentation gate exercises compiled capability sets, the facade
example and current codec behavior. Structural checker fixtures admit ordinary
wording changes and reject broken links. Five migration cases and all 13 startup
checks pass; atomic accepted-root ownership, focused strict lint, formatting and
invariant checks qualify the maintained boundaries. No full suite is run.
Excluding the user lockfile, 26 files change with approximately 120 fewer lines.
The runtime implementation grows by five lines while documentation and check
duplication shrinks. Disposable PocketIC fixtures were used; no persistent
network was restarted. User Candid lockfile changes are preserved, not attributed
to this work, and shared by both measured variants. Full release validation and
CI remain user-owned; generic continuation stays in 0.261 read-only closeout.

### Published 0.261.6 handoff

**C31 and C32 are retained; the rejected C33 rewrite is removed.** The user
approved removing C33 after the matched Toko investigation. Signed cardinality
updates use the existing checked-add and zero-removal implementation; the
cancellation, zero-removal and both overflow-boundary tests remain. Consumed
row buffers still move into journal publication and canonical folding (C31),
and child enumeration still sorts borrowed parent prefixes (C32).

The changed production sources match the measured C31+C32 variant exactly;
only retained test modules differ. The [measurement report](c31-c33-performance.md)
records the original rejected candidate and the source-isolated comparison.
Against the matched .5 baseline, Game Shard shrinks by 834 raw Wasm bytes;
Translation, User Hub and User Shard shrink by 263, 164 and 334 bytes, while
Game Hub is unchanged. Recovery-worker instruction deltas range from −0.053%
to +0.106%, avoiding C33's consistent 1.7–1.8% increase. Gross-cycle changes
remain mixed: −0.119% to +0.241%, with six improvements and two increases.
These are ownership cleanups with a measured tradeoff, not a uniform cycle win.

The earlier isolation passed all 48 functional cases across six disposable
PocketIC fixtures, with matching timer counters and conserved work. This
correction reuses those measurements after checking production-source equality;
it does not rebuild or rerun the Toko workload. All 20 focused arithmetic and
index-store tests pass, including recovered-fold maintenance and overlay
retirement. Strict all-feature core library/test lint, index-range, mutation
atomicity and read-admission invariants, formatting and whitespace pass.

The requested .4-to-.5 action-application split is also complete. On the frozen
Toko/Canic .35 workload, command handling contributes 12,490,095 additional
instructions (71.6% of the diagnostic walking-32 application increase) and
context handling contributes 4,954,925 (28.4%). Both versions perform 64 calls
of each kind; stationary-32 application instructions instead decrease by
1,215,999. All 16 diagnostic cases pass. Source and dependency checks preserve
the matched scope; no production code, dependency or version changes. The
subsequent nested probe attributes 78.8% of its command-instruction increase
to interaction/locker dispatch, 18.4% to read validation/remainder and 2.8%
to Candid decode/encode. Call and byte counts match; all 16 additional cases
pass. No exclusive IcyDB defect is established. The next bounded downstream
experiment is to reuse the projection already computed by locker `apply`
inside its snapshot logic, retaining all validation. Savings remain unmeasured.
Each profiling step used two disposable PocketIC fixtures; current Canic .36
and concurrent gameplay changes remain unqualified by these measurements.

The whole pending patch touches eight files, including retained measurement
evidence: six fewer production Rust lines, 21 net test lines and documentation.
Moves and borrows simplify ownership; removing the occupied/vacant branches
simplifies the signed-count helper. No new state, format, API or execution route
is introduced. Dependencies and Cargo versions are unchanged; notes remain at
active 0.261.6 above published 0.261.5. No network lifecycle action is needed
for this correction. Full release validation and remote CI remain user-owned;
the next generic continuation is read-only closeout within 0.261.

### C30 handoff

**C30 — Repair indexed-scalar measurement boundaries is complete**,
authorised after closeout identified the stale decimal-width assumption.
The fixed audit actor and its write/recovery matrices now use current binary
magnitude boundaries: all-nines unsigned values fit through 9,856 decimal digits,
and negative signed values through 9,854. The next decimal width rejects, with
the oversized value last in multi-row batches. Existing encoder admission and
atomicity remain the owners; no new production API, format, state or route is
needed. The audit schema's field limit also expands to 8 KiB of LEB128 so it
admits both sides of the binary index boundary. Active notes remain 0.261.5.

Both maintained ignored Wasm matrices pass: 24 writes include 16 accepted and
eight rejected cases with no partially stored rows; 12 accepted fixtures each
survive debt-bearing and debt-free same-artifact recovery (24 observations).
The native exact-byte boundary test, strict host/actor lint, formatting and
whitespace pass. The first Wasm attempt exposed the smaller audit field limit;
the corrected rerun passes in full. Two disposable PocketIC servers were
started and stopped; no persistent network was restarted.

Eight files change, with four net Rust lines in audit fixtures/tests and
documentation. Production core code and state space are unchanged; implementation
shape stays neutral. The [measurement note](c24-c26-performance.md) records the
qualified artifact and separates this repair from earlier cost comparisons.
Raw-Wasm, IC-cycle and instruction deltas for this repair remain unmeasured.
Cargo versions and dependencies are unchanged. The stale boundary follow-up is
closed; scoped closeout is ready for user-owned full release validation and CI.

### C29 handoff

**C29 — Borrow the journal publication key for cardinality is complete**,
authorised after C28. Publication retains the original row key until recording
its journal position, so cardinality bookkeeping can borrow that key directly.
The redundant copy is removed without changing mutation order, validation or
recovery ownership. No new API, state, format or execution route is needed.
Active notes remain 0.261.5.

All 23 focused store, exact-cardinality and publication/recovery tests pass,
including delete/reinsert visibility, mixed-entity interruption boundaries and
typed batch recovery. Strict all-feature core library/test lint, mutation
atomicity invariants, formatting and whitespace pass. Existing tests cover the
maintained behavior; no new test surface is needed. No validation failures or
network lifecycle actions; full release validation and remote CI remain
user-owned.

Four files change: five fewer production Rust lines and documentation. One
temporary key clone is removed, making the implementation simpler without
adding state space. C29 raw-Wasm, IC-cycle and instruction deltas remain
unmeasured. Cargo versions and dependencies are unchanged. The stale ignored
measurement boundary fixture remains a separate follow-up; generic continuation
is read-only closeout within 0.261.

### C28 handoff

**C28 — Borrow primary-row scan bounds is complete**, authorised by
continuation after C27. Data-store visitors borrow their caller's range
bounds through heap and journal traversal, removing the owned-bound conversion
helper and redundant copies. Stable iterators retain their required owned
bounds internally. Ordering, overlay visibility, payload preflight and early
stop remain with their existing owners; no new API, state, format or route is
needed. Active notes remain 0.261.5. Publication-copy cleanup remains separate.

All 20 focused store, primary-scan, cardinality and row-materialization tests
pass. Coverage includes forward/reverse bounds across heap, live and canonical
stores, mixed-overlay visibility, canonical checkpoints and single payload
reads. Strict all-feature core library/test lint, read-admission and mutation
atomicity invariants, formatting and whitespace pass. No validation failures or
network lifecycle actions; full release validation and remote CI remain
user-owned.

Five files change: 11 fewer production Rust lines, 39 net test lines and
documentation. Removing the owned-bound helper makes the implementation simpler
without adding state space. C28 raw-Wasm, IC-cycle and instruction deltas remain
unmeasured. Cargo versions and dependencies are unchanged. The stale ignored
measurement boundary fixture remains a separate follow-up.

### C27 handoff

**C27 — Reuse index-key decoding for row witnesses is complete**, authorised
after the optimisation review. Identity-only entry reads use the existing
strict borrowed primary-key decoder; cardinality accounting reuses its
already-decoded index key. The codec remains the complete-frame validation
owner and entry validation retains witness/error ordering. No new API, parser,
state, format or dependency is needed. Active notes remain 0.261.5. Primary-row
bound borrowing and publication-copy cleanup remain separate follow-ups.

All 101 focused entry, cardinality, index-store, index-plan, codec and reverse-index
tests pass. Full-key corruption checks cover every truncation, trailing bytes,
an invalid primary-key discriminator and precedence over invalid witness bytes.
Strict all-feature core library/test lint, index-range and mutation atomicity
invariants, formatting and whitespace pass. No validation failures or network
lifecycle actions; full release validation and remote CI remain user-owned.

Six files change: one net added production Rust line, 22 net test lines and
documentation. The implementation is simpler through reuse of existing decoders;
state space is unchanged. C27 raw-Wasm, IC-cycle and instruction deltas remain
unmeasured; the C24–C26 measurements do not include this change. Cargo versions
and dependencies are unchanged. The stale ignored measurement boundary fixture
remains a separate follow-up; generic continuation is read-only closeout in 0.261.

### C24–C26 handoff

**C24–C26 are complete**, explicitly authorised together after the read-only
optimisation review: C24 removes unused scan bookkeeping, C25 borrows range
bounds at iterator calls, and C26 encodes index keys without temporary owned
keys. Scan traversal and taxonomy framing remain the canonical owners. Existing
validation, ordering and conservative structural admission are retained; no
new state, format, mode or dependency is needed. These changes remove an inert
counter, redundant buffer copies and duplicate row/sentinel framing. Active
notes remain 0.261.5; prior work is preserved.

All 158 focused index-key, taxonomy, entry, store, range and scan tests pass,
including golden bytes, encoding limits, malformed keys, merged scans over
heap/live/canonical stores, mixed overlays and fold/reopen behavior. Strict
all-feature core library/test lint, index-range, schema/model and mutation
atomicity invariants, formatting and whitespace pass. These implementation checks
needed no network lifecycle actions; full suites and remote CI remain user-owned.

Nine files change in these three items: 44 fewer lines in runtime source files,
53 net added test lines and documentation. Owned index-key conveniences are
limited to tests. The implementation is simpler through one framing owner,
borrowed bounds and deleted bookkeeping; no independent behavior axis is added.
Cargo versions and dependencies are unchanged. The next generic continuation is
read-only closeout within 0.261.

The subsequent [matched measurement](c24-c26-performance.md) isolates C24–C26:
the audit actor shrinks 872 raw Wasm bytes (0.0195%); all twelve indexed-write
cases use fewer instructions and cycles (cycle savings 0.0069–0.3330%). Bounded
index-read cycles change from −1.573% to +0.920%, so read performance is mixed.
Both replay trials reproduce all 24 records per artifact exactly, with matching
read results and selected indexes. Two disposable PocketIC servers were started
and stopped; no persistent local network was restarted.

The initial ignored measurement matrix fails on the pre-change baseline: its
4094-digit unsigned rejection expectation is stale. The disposable host was
restricted to known-valid widths for the matched comparison; repair of the
maintained rejection fixture remains a follow-up. No engine changes were made
during measurement. Earlier C21–C23 and downstream production cost deltas remain
unmeasured.

### C22 handoff

**C22 — Use standard index-key comparisons is complete**, authorised by
continuation after the C23 CI repair. Standard vector comparisons replace two
redundant helpers. The existing component-count comparison still precedes
component values, preserving physical index ordering and primary-key tie breaks.
No state, format, API, dependency or execution route is added. Existing ordering
and range fixtures cover this simplification; no new test surface is needed.
Active notes remain 0.261.5, with C21 and C23 work preserved.

All 103 focused index-key, entry, range and scan tests pass, including mixed
component counts, semantic/raw ordering agreement, primary-key tie breaks,
prefix bounds and directional traversal. Strict all-feature core library/test
lint, index-range invariants, formatting and whitespace pass. No validation
failures or network lifecycle actions; full suites remain user-owned.

Five files change in this slice, with 18 fewer production Rust lines and
documentation. Two custom helpers are removed, making the implementation
simpler without changing state space. Raw-Wasm, IC-cycle and instruction deltas
remain unmeasured. Cargo versions and dependencies are unchanged. The next
generic continuation is a read-only closeout audit within 0.261.

### C23 handoff

**C23 — Bind recovery readiness to registry storage is complete**, prioritised
by the user's CI repair request before C22. CI run 35705907836 failed both
populated rename recovery cases with E259. An isolated 0.261.4 reproduction
confirmed that distinct LocalKey handles to one registry produce different
recovery identities; using the registry storage identity fixes both cases.
Admission, startup witnesses and test readiness now share the existing domain
helper. Commit-allocation isolation and pending-marker admission are preserved.
No new state, format, API or execution route is needed. Active notes remain
0.261.5; C21 work is preserved and C22 remains a separate planned slice.

All 63 focused recovery, startup and schema-application tests pass, including
both CI failures using separate driver and reader handles. Strict all-feature
core library/test lint, schema/model and mutation atomicity invariants,
formatting and whitespace pass. Full suites and the remote CI rerun remain
user-owned; no canister/network lifecycle actions were needed.

This repair touches five files, with unchanged net Rust lines outside the test
module and 54 net added test lines, plus documentation. Recovery identity is
simpler through one canonical helper. Raw-Wasm, IC-cycle and instruction deltas
remain unmeasured. Cargo versions and dependencies are unchanged. C22 remains
the next separate planned landing slice.

### C21 handoff

**C21 — Decode data-store keys from borrowed bytes is complete**, authorised
after the optimisation review following 0.261.4 closeout. Existing raw
entity-prefix and primary-key-suffix accessors feed one canonical primary-key
decode. StoreKey rejection and the retained raw key are preserved. The
unreachable second-decode error variant is removed; owned decode conveniences
are restricted to their remaining test consumers. No new parser, public API,
state, format or dependency is introduced. Active notes are 0.261.5.

All 84 focused data-key, data-store, key-taxonomy and index-scan tests pass,
including scalar/composite and maximum-width keys, complete truncation checks,
trailing bytes and typed unknown-kind rejection. The complete Clippy workflow,
focused all-feature core library/test lint, index-range, schema/model and
mutation atomicity invariants, formatting and whitespace pass. Initial lint
found the owned decoder's now-test-only short-key variant; restricting that
variant and its display arm to tests resolved it before the final lint rerun.
Full suites remain user-owned; no canister/network lifecycle actions were needed.

Six files change, with four added production Rust lines and 36 net test lines
plus documentation. Runtime decoding is simpler through existing accessors and
one decode; state space is unchanged. Raw-Wasm, IC-cycle and instruction deltas
remain unmeasured. Cargo versions and dependencies are unchanged. C22, standard
index-key comparisons, remains the next separate planned landing slice.

### C20 handoff

**C20 — Borrow strict index frames during unique validation is complete**,
authorised by continuation after C19 closeout. Each candidate now compares
earlier page keys without reconstructing their owned components. A shared
private codec frame parser retains complete strict validation for comparisons,
owned decoding and primary-key extraction, removing duplicate framing loops.
The ordering decoder remains separate because it has a wider primary-key bound.
The strict codec remains the owner, with no independent behaviour axis, state,
format or dependency. Active notes remain 0.261.4.

All 65 focused codec, index-entry, range and scan tests pass, including new
duplicate-domain comparisons and complete malformed-frame checks. Three
SQL-canister regressions pass for same-page duplicate findings, expression
indexes and filtered unique indexes. Strict all-feature core library/test
Clippy, index-range, schema/model and mutation atomicity invariants, formatting
and whitespace pass. One disposable PocketIC server was started and stopped;
no validation failures. Full suites remain user-owned.

C20 touches six files with 24 added production Rust lines and 82 test lines,
plus documentation. Parsing is simpler through one shared strict owner;
the borrowed comparison adds a narrow private representation instead of a
parallel decoder. Including C19, the active patch has 20 added production Rust
lines and 82 test lines. Raw-Wasm, IC-cycle and instruction deltas remain
unmeasured. Cargo versions and dependencies are unchanged. The next generic
continuation is scoped closeout within 0.261.

### C19 handoff

**C19 — Reuse owned index-codec buffers is complete**, authorised after the
0.261.3 release and optimisation review. Completed raw keys return directly,
encoded primary-key bytes move through their existing ownership conversion,
and primary-key values decode directly from borrowed bytes. Existing codec
owners retain validation, errors, framing and sentinel handling. No new API,
state, format, dependency or execution route is introduced. Active notes are
0.261.4; Cargo versions remain unchanged.

All 63 focused codec, index-entry, range and scan tests pass, including golden
bytes, scalar/composite primary keys, malformed frames and sentinel bounds.
Strict all-feature core library/test Clippy, index-range invariants, formatting
and whitespace pass. No validation failures or network lifecycle actions;
full test suites remain user-owned.

Four files change, with four fewer production Rust lines and documentation.
Implementation is simpler and state space is unchanged. Raw-Wasm, IC-cycle and
instruction deltas remain unmeasured. Repeated owned decoding in unique-validation
comparisons remains a separate follow-up candidate, requiring its own scope
and validation before implementation. The next generic continuation is scoped
closeout within 0.261.

### C18 handoff

**C18 — Remove temporary index-comparison allocations is complete**, authorised
by continuation after C17. Raw-key comparison now uses fixed arrays of borrowed
slices under the existing four-component bound. Standard array comparison
replaces the separate comparison loop; equal component counts guarantee equal
empty padding. Full-frame validation, component-count-first ordering, primary-key
tie breaking and malformed-byte comparison remain unchanged. The taxonomy
decoder remains the owner; no new dependency, mode, format or persisted state is
introduced. Active notes remain 0.261.3.

Validation passes: 93 focused taxonomy, index-codec, range and scan tests,
including raw/decoded ordering at every component count and malformed frames
on both sides; three SQL-canister cases covering filtered reads, unique
expression indexes and duplicate findings; strict all-feature core library/test
Clippy; index-range, schema/model and mutation atomicity invariants; formatting
and whitespace. One disposable PocketIC server was started and stopped. No
validation failures; full test suites remain user-owned.

C18 touches five files with neutral production Rust line count and 83 added
test lines, plus documentation. Implementation is simpler without temporary
component vectors or a custom comparison loop; state space is unchanged.
Raw-Wasm, IC-cycle and instruction deltas remain unmeasured. Earlier work,
Cargo versions and dependencies are unchanged. All planned follow-ups are
complete; the next continuation is read-only scoped closeout within 0.261.

### C17 handoff

**C17 — Narrow bound DDL field requests is complete**, authorised after the
read-only optimisation review. Five existing-field resolvers now borrow accepted
snapshots. Bound requests retain only the name and, for nullability abort, the
field ID consumed by execution. This removes resolver clones and the second
default/nullability binding clone. The accepted catalogue remains authoritative;
no new helper, mode, format or persisted state is introduced. Active notes remain
0.261.3.

Validation passes: 23 focused native planning and metadata tests; seven
SQL-canister cases covering defaults, nullability, pending activation abort,
rename, empty/nonempty drops and generated-field rejection; full required
Clippy workflow and focused all-feature core library/test Clippy; schema/model,
mutation atomicity and SQL ownership invariants; formatting and whitespace.
Clippy initially flagged repeated field-name suffixes and an overlong test;
private naming and a separate abort test resolved both, with final gates passing.
One disposable PocketIC server was started and stopped. Full test suites remain
user-owned.

C17 touches seven files: two added production Rust lines and 43 added test
lines, for 45 net added Rust lines plus documentation. Implementation is simpler
with narrower request data and unchanged state space. Raw-Wasm, IC-cycle and
instruction deltas remain unmeasured. Earlier work, Cargo versions and
dependencies are unchanged. **C18 — Remove temporary index-comparison
allocations** remains the next separate follow-up.

### C16 handoff

**C16 — Reuse unique-validation page keys is complete**, authorised by the
user's request for more optimisation in the current patch after scoped closeout.
The scan retains successful raw keys in one collection, then moves it into
sorted staging output only for Forward. This removes the second Forward vector
and per-key byte clones in both phases. Verify retains its existing conflict
and presence checks and emits no staged writes. No new helper, mode, format or
persisted state is introduced; page budgets and publication remain with their
existing owners. Active notes remain 0.261.3.

Validation passes: 12 native validation-job tests; four SQL-canister cases
covering ordinary, expression and filtered unique indexes plus same-page
historical conflicts, retained receipts and abort; strict all-feature core
library/test and SQL integration-test Clippy; schema/model, mutation atomicity
and SQL ownership invariants; formatting and whitespace. The new fixture first
used an unsupported update predicate and was corrected to supported primary-key
updates; its focused rerun passed. Two disposable PocketIC servers were started
and stopped. Full test suites remain user-owned.

C16 touches five files: three added production Rust lines and 42 added test
lines, for 45 net added Rust lines plus documentation. Implementation is simpler
with one owner of page keys and unchanged state space. Raw-Wasm, IC-cycle and
instruction deltas remain unmeasured. Earlier work, Cargo versions and
dependencies are unchanged. The next continuation is a read-only scoped
closeout within 0.261.

### C15 handoff

**C15 — Avoid temporary store-path allocation is complete**, authorised by the
user's continuation after C14. Verify needs only the numeric journal revision;
the existing read helper now returns that value. Forward exhaustion owns the
store path when capturing the persisted record. This removes temporary strings
without another helper, mode, state or semantic owner. Both Verify checks and
restart publication ordering remain unchanged. Active notes remain 0.261.3.

Validation passes: 12 existing validation-job tests, including captured
revisions, restart transitions and stable persisted bytes; both SQL-canister
not-null and unique-index activation cases; strict all-feature core library/test
Clippy; schema/model, mutation atomicity and SQL ownership invariants; formatting
and whitespace. One disposable PocketIC server was started and stopped. No
validation failures; full test suites remain user-owned.

C15 touches four files, adding three net production Rust lines plus
documentation. Implementation is simpler because numeric reads no longer build
owned records; state space is unchanged. Raw-Wasm, IC-cycle and instruction
deltas remain unmeasured. Earlier work, Cargo versions and dependencies are
unchanged. All planned follow-ups are complete; the next continuation is a
read-only scoped closeout within 0.261.

### C14 handoff

**C14 — Reuse the known nullability target is complete**, authorised by the
user's continuation after the C11–C13 scoped closeout. The derivation forwards
its known target and requested nullability to index construction, removing a
nested field search. No-op requests retain exact index metadata; constraint and
historical-fill handling remain unchanged. This reuses the existing derivation
owner without adding state, modes, formats or public APIs. Earlier work is
preserved and active notes remain 0.261.3.

Validation passes: eight focused native tests, including both indexed
nullability directions and exact snapshot preservation for repeated requests;
one SQL-canister nullability case; strict all-feature core library/test Clippy;
schema/model and SQL ownership invariants; formatting and whitespace. One
disposable PocketIC server was started and stopped. No validation failures;
full test suites remain user-owned.

C14 touches five files: three fewer production Rust lines and 31 added test
lines, for 28 net added Rust lines plus documentation. Implementation is simpler
and state space is unchanged. Raw-Wasm, IC-cycle and instruction deltas remain
unmeasured. Cargo versions and dependencies are untouched. **C15 — Avoid
temporary store-path allocation** remains the next independent follow-up.

### C11–C13 handoff

The user reported 0.261.2 published and authorised all three subsequent review
candidates together. **C11–C13 are complete** as one combined handoff:

- C11 — Canonical default/rename validation: catalogue admission and SQL
  publication share persisted candidate derivation. The parallel field-comparison
  model is removed; complete after-images reject unrelated metadata drift.
- C12 — Shared validation resume checks: receipt acknowledgement and
  revision-triggered restart have one owner. Both Verify revision checks,
  kind-specific scans and publication ordering are preserved.
- C13 — Allocation-free publication classification: preflight accumulates two
  booleans without a temporary vector. Sorted store order, error priority and
  all/current/mixed publication outcomes are unchanged.

These changes address demonstrated duplication and one temporary allocation.
Existing catalogue derivation, validation jobs and publication remain the semantic
owners. No mode, state, format, dependency or public API is added. Active notes
are 0.261.3; Cargo version changes remain release-owned.

Validation passes: 39 focused native tests; four SQL-canister cases covering
field defaults, indexed renames, not-null activation and unique-index activation;
the required full Clippy gate and focused all-feature core library/test Clippy;
schema/model, mutation atomicity and SQL ownership invariants; formatting and
whitespace. An initial missing import, generated-owned test field and ambiguous
boolean lint were corrected before the final passing runs. One disposable
PocketIC server was started and stopped. Full test suites remain user-owned.

Ten files change: 56 fewer production Rust lines and 94 added test lines,
for 38 net added Rust lines plus documentation. Implementation is simpler;
state space is unchanged. Raw-Wasm, IC-cycle and instruction deltas remain
unmeasured. All three authorised follow-ups are complete; the next continuation
is scoped closeout within 0.261.

### C9–C10 published handoff

The user authorised both follow-ups from the C6–C8 scoped closeout together.
**C9 — One snapshot-version setter** and **C10 — Reuse validated nullability**
are complete as one combined handoff. C9 removes manual snapshot reconstruction;
all callers use the existing consuming version setter, and maintained tests
check detached candidates and complete persisted metadata preservation. C10
returns target requiredness after full after-image validation, eliminating
the separate lookup before activation dispatch. No new state, mode, format or
public API is introduced. C6–C8 work is preserved and active notes remain
0.261.2.

Validation passes: 21 focused native tests covering detached snapshots, encoded
metadata preservation, mutation planning and field-drop boundaries; three
SQL-canister cases covering defaults, nullability and empty field removal;
strict all-feature core library/test Clippy; schema/model, atomicity and SQL
ownership invariants; formatting and whitespace. One disposable PocketIC server
was started and stopped. Full suites remain user-owned.

Eight files change for C9–C10, removing 31 net Rust lines: 33 production lines
removed and two test lines added. Implementation is simpler and state space is
unchanged. Raw-Wasm, IC-cycle and instruction deltas remain unmeasured. Cargo
versions and dependencies are unchanged. Both authorised follow-ups are complete;
the next continuation is scoped closeout within the current minor line.

### C8 handoff

**C8 — Shared validation-job preflight is complete.** Both
checkpoint publication paths now use one private helper for accepted-bundle
loading, store identity and activation/job closure checks. The index path uses
the returned bundle for its existing key validation and still requires journal
recovery; the job-only path retains heap and journal dispatch. Check ordering,
typed errors and publication ownership are unchanged. C6/C7 changes are
preserved and active notes remain 0.261.2.

Validation passes: 15 focused native tests covering validation jobs, exact
activation/job closure and schema publication/recovery; both SQL-canister
not-null and unique-index activation cases; strict all-feature core-library
Clippy; schema/model, atomicity and SQL ownership invariants; formatting and
whitespace. One disposable PocketIC server was started and stopped. No new
tests, dependencies or Cargo-version edits; full suites remain user-owned.

Four files change for C8 with neutral net Rust line count. Implementation is
simpler because duplicated preflight has one owner; state space is unchanged.
Raw-Wasm, IC-cycle and instruction deltas remain unmeasured. C6–C8 are complete;
the next continuation is a read-only scoped closeout in the current minor line.

### C7 handoff

**C7 — Canonical field-drop validation is complete.** SQL DDL
now reuses the existing dense-removal derivation and compares the entire
after-image with the request's admitted schema version. This deletes duplicate
field/index/relation remapping and replaces the layout self-comparison with
an exact check. Constraint and allocator drift also reject. Target identity,
schema-version admission and the empty-entity publication gate remain separate
checks at their existing boundaries. C6 work is preserved and active notes
remain 0.261.2. C8 is the next continuation boundary.

Twenty focused native tests pass, including four new field-drop boundary tests.
Both selected canister cases pass: empty drop publishes and nonempty drop rejects
without schema changes. The new empty fixture initially rejected with E118
because DDL changed the accepted-root binding of its cardinality proof. Rebuilding
that proof through the existing upgrade/startup lifecycle fixed the fixture;
no production admission change was needed. Two disposable PocketIC servers
were started and stopped. Strict core library/test and SQL integration-test
Clippy, focused schema/model, atomicity and SQL ownership checks, formatting
and whitespace pass. Full suites remain user-owned.

Six files change for C7: 72 fewer production-module lines and 224 added test
lines, plus documentation. Implementation is simpler with one field-removal
derivation owner and no new state or format. Raw-Wasm, IC-cycle and instruction
deltas remain unmeasured. Cargo versions and dependencies are untouched.

### C6 handoff

The user reported 0.261.1 live and authorised continuation of the three new
cleanup candidates. **C6 — Shared validation startup is complete** as one bounded
slice; C7 and C8 remain independent follow-ups in this minor line.

C6 replaces duplicate row-local and unique-index startup bodies with one
private runner. It forwards the validation job's existing optional staged
generation and keeps the epoch mismatch check before job publication. The
runner owns orchestration; the job still validates activation closure and the
commit owner still publishes candidate and job atomically. Keeping both bodies
would preserve duplicate transition logic; no additional mode or state is
needed.

Validation passes: 12 focused validation-job tests, both SQL-canister activation
cases (not-null and unique field-path index), strict all-feature core-library
Clippy, schema/model boundary, mutation atomicity and SQL branch ownership
checks, formatting and whitespace. An initial sandbox restriction prevented
PocketIC binding localhost; the approved retry passed. One disposable server
was started and stopped. Full suites remain user-owned.

Four files change, removing 40 net Rust lines. Implementation is simpler with
unchanged state space; raw-Wasm, IC-cycle and instruction deltas remain
unmeasured. Active notes are 0.261.2; Cargo versions and dependencies are
unchanged. C7 is the next continuation boundary.

### C3–C5 published handoff

Release validation exposed a stale schema/model boundary check after C3: it
still required source-binding transitions in the constraint caller as well as
the shared candidate builder. The check now targets the shared owner in
`sql_ddl.rs`; runtime source-binding behavior is unchanged. The earlier scoped
closeout missed this validation gap. Schema/model boundary and SQL branch
ownership checks, shell syntax, formatting and whitespace validation pass;
the full release workflow remains user-owned. Four files change, with four
fewer script lines and no runtime changes. Raw-Wasm, IC-cycle and instruction
deltas remain unmeasured.

The user explicitly authorised all three additional review candidates together.
**C3–C5 — SQL DDL and typed-binding reuse are complete** as one combined handoff.
SQL DDL uses one candidate builder for revision and source-lineage updates;
unchanged snapshots return before constructing a replacement entity map. Index
staging derives both row contracts from one accepted catalogue selection. Typed
input and output preparation share one freshness check. Existing identity,
snapshot, no-op and stale-binding boundaries remain authoritative.

Validation passes: 23 focused index-domain tests, two typed-query/write compile
contracts, seven SQL-canister DDL cases, strict all-feature core/facade library
Clippy, formatting and whitespace. The canister cases cover field/expression and
unique indexes, no-op creation, defaults, nullability and field rename. One
disposable PocketIC server was started and stopped. No new tests or dependencies.

Eight files change for C3–C5: 54 fewer net Rust lines and approximately 20 fewer
total lines including documentation. Implementation is simpler and state space
unchanged; no public API or format changes. Raw-Wasm, IC-cycle and instruction
deltas remain unmeasured; full suites remain user-owned. Active 0.261.1 notes are
current. All three newly authorised candidates are complete; scoped closeout is
the next continuation boundary. Earlier work and the dirty lockfile are preserved.

### C2 handoff

**C2 — Shared startup receipt binding is complete**, authorised by the user's
continuation after C1. Receipt creation and matching use one constructor owned
by `AcceptedHeadBinding`; classification, stale-receipt checks and encoding stay
with their current owners. Existing independent receipt fixtures are retained.

All 13 focused startup tests and strict all-feature core-library Clippy pass,
as do formatting and whitespace checks. Coverage includes receipt round trips,
malformed receipts, terminal-failure publication, stale submissions and startup
readiness. No new tests, network lifecycle actions or dependency changes.

Six files change for C2: six fewer net Rust lines and approximately +20 total
lines including documentation. Implementation is simpler; state space and the
current receipt format are unchanged. Raw-Wasm, IC-cycle and instruction deltas
remain unmeasured; full suites remain user-owned. Active 0.261.1 notes are current.
Both simplifications from the 2026-09-21 review are complete. No further
implementation slice remains from that review; continuation starts scoped closeout.

### C1 handoff

The user authorised the next simplification from the 2026-09-21 source review.
**C1 — Shared validation-job publication is complete.** One private journaled
publisher handles job-only checkpoints and candidate-index entries. Caller
validation, record order and marker ownership are preserved; empty entry lists
skip the candidate-index write borrow. No public API, persisted state or format
changes. The separate startup-receipt conversion finding remains a follow-up.

Both existing native publication/recovery tests and both selected SQL-canister
tests pass: `sql_canister_ddl_endpoint_publishes_alter_column_nullability` and
`sql_canister_ddl_endpoint_publishes_and_drops_supported_unique_field_path_index`.
Strict all-feature core-library Clippy, formatting and whitespace checks pass.
The SQL-only native test build emits 65 dead-code warnings in unchanged code.
An initial offline integration build lacked the already lockfile-selected
hyper-rustls 0.27.10; fetching it resolved the build without lockfile changes.
One disposable PocketIC server was started and stopped for the two tests.

Four files change for C1, removing 36 net Rust lines; documentation leaves the
overall line delta approximately neutral. Implementation is simpler and state
space unchanged. Raw-Wasm, IC-cycle and instruction deltas remain unmeasured.
Full suites remain user-owned. Active 0.261.1 notes are updated; Cargo versions,
the pre-existing lockfile edit and earlier investigation artifacts are untouched.

### I3 handoff

The user-authorised **I3 — ICYDB-039 shared-shard convergence investigation is
complete within its bounded local scope**. The
[evidence](../../reports/investigations/2026/09/21/toko-miner-shared-shard-convergence/01/report.md)
reproduces E263 at virtual second 42 with one explicit round per second; eight
rounds pass the same 160 calls, 480 actions and eight exact replays on unchanged
Wasms. Instrumentation confirms 16 appended batches versus about six retired
per six-second interval after the first wave. Pressure is exactly 64 retained
batches plus a proposed one against the unchanged 64 limit. Convergence succeeds;
ordinary timer service drains the backlog, rejected requests leave inventory
unchanged and succeed unchanged afterwards, and quantities remain conserved.

All three focused managed runs and the diagnostic build pass. ICYDB-039 retains
representative scheduling and live-subnet capacity acceptance with Toko Miner;
no new runtime defect or correction is established. The original-artifact
schedule comparison changes raw Wasm size by zero bytes. Whole-endpoint cycle
and instruction deltas remain unmeasured. Three disposable PocketIC fixtures
were started and dropped; no application network operation occurred. Active
0.261.1 notes include this result without changing Cargo versions or the
pre-existing dirty lockfile. No planned IcyDB slice remains.
I3 changes 19 documentation/evidence files across the two repositories,
approximately +2,300 net lines; production complexity and state space stay neutral.

### I2 handoff

The user authorised the ICYDB-038 catalogue-read investigation on 2026-09-21.
**I2 is complete within its bounded read-comparison scope.** The
[application evidence](../../reports/investigations/2026/09/21/toko-miner-catalogue-reads/01/report.md)
records 46.4–47.1% fewer Resource instructions, 51.4–52.0% fewer customer
instructions and 20.4–20.9% fewer NFT token instructions using existing selected
fields, with identical values and ordering. The focused managed test passes all
24 admitted comparisons; two queries before recovery readiness correctly report
E259. Same-release restoration preserves results. Normal ready-state reads do
not reproduce I1's large preparation penalty that disappears after a replicated
call. Enum conversion reuses the generated decoder and accepted entity binding.

ICYDB-038 remains Candidate: whole-action instructions/cycles, actual request
reuse and integrated receipt/failure-boundary qualification remain application
work. No new runtime feature is justified. The diagnostic actor is 10,216,852 raw
Wasm bytes; production Wasm and cycle deltas are unmeasured. Source/artifact
hashes, documentation links and whitespace pass; full suites remain user-owned.
Three disposable PocketIC runs and one same-release test upgrade occurred;
application networks and production sources are unchanged. Active notes open
at 0.261.1 without changing Cargo versions or the pre-existing dirty lockfile.
This does not start a different minor line or reopen completed rename work.
Eleven documentation/evidence files add approximately 1,465 net lines; production
implementation complexity and state space are unchanged. I3 is the subsequently
authorised investigation.

### Prior release handoff

E1 establishes that source-declared entity rename already exists through
`EntityMigration::from_name`. The local `SchemaMigrationRename` enum only
describes subordinate renames; its lack of an entity variant is not a missing
capability. The idea intake is corrected accordingly.

E2 [reproduced and corrected an admission gap](e2-findings.md), following the
user's explicit approval and a check against the existing affected-entity
contract. A dependency-only companion is admitted only when reversing same-plan
entity-rename target names reproduces its complete accepted source digest.
E2 is complete and ready for a reviewable worktree handoff. Populated self/inbound
renames preserve accepted identities, exact row/index/reverse bytes and source
lineage, reject stale bindings/cursors, and survive exact replay and interrupted
compound publication. Publication/recovery owners and formats are unchanged.
E3 is complete: both generated Item/Holder upgrade/retry/restart scenarios pass,
as do the three maintained physical cast scenarios. Fixed controls use ordinary
accepted writes and typed exact-key reads; no runtime owner changes were needed.
The [E3 evidence](e3-evidence.md) records exact artifacts, inputs and cost scopes.
The authorised [I1 investigation](i1-read-investigation.md) is complete: repeated
query-only reads rebuild accepted runtime/catalog metadata until an ordinary
replicated call retains that preparation. Restart alone does not lower the cost.
No production optimization or application network change is included.
[Q1 is complete and scoped-ready](q1-closeout.md), with that cost limitation
explicitly retained. The subsequently authorised D1 timer update is complete:
ic-timers 0.8.0 passes the existing lifecycle and migration qualification.
The released rename/timer plan is complete; I2 is the newly authorised follow-up.

The user's release validation exposed missing E3 feature propagation in the
bounded SQL package, which shares the SQL actor source. Its manifest now forwards
both existing rename fixture features exactly as the ordinary SQL package does.
This completes the existing fixture configuration; no new runtime mode or
execution owner is introduced. The complete `make clippy` workflow passes, as
do the bounded package's all-target source-feature and all-feature successor
lint checks. Both SQL manifests now have identical feature declarations.
Formatting, whitespace and local links pass. Four files change, +17 net lines;
implementation shape stays neutral. Wasm/cycle/instruction deltas are unmeasured.
No network lifecycle, version/dependency, commit or push action was taken.
Full validation remains user-owned and must be rerun before release.

| Slice | State | Next action |
| --- | --- | --- |
| C45 — Bound dynamic execution by authored row limits | Complete | Bounded read accounting and continuation regressions pass; user-owned release validation. |
| C46 — Seek one-sided primary-key bounds | Complete | Focused planner, physical, fingerprint and explain checks pass; user-owned release validation. |
| C47 — Resume primary-key fast scans | Complete | Five focused page-limit tests, strict lint and Wasm library check pass; user-owned release validation. |
| C48 — Qualify warm read instruction increase | Complete | Matched A–B–A IC fixture confirms the tradeoff; retain C37 without a new runtime path. |
| C49 — Prove one-sided primary-key continuation | Complete | Eight bound/direction cases pass in the focused page-limit target; closeout ready for user-owned validation. |
| C31 — Move consumed publication/fold row buffers | Complete | Focused publication and recovery tests pass; consumed buffers move directly. |
| C32 — Borrow cardinality parent prefixes | Complete | Deterministic deduplication, generation checks and child caps pass. |
| C33 — Update signed cardinality through map entries | Rejected rewrite removed | Existing checked arithmetic restored after measured cycle regression; arithmetic boundary tests retained. |
| C30 — Repair indexed-scalar measurement boundaries | Complete | Both Wasm matrices, native boundary test and strict focused lint pass. |
| C29 — Borrow the journal publication key for cardinality | Complete | 23 focused tests, strict core lint and mutation atomicity invariants pass. |
| C28 — Borrow primary-row scan bounds | Complete | 20 focused tests, strict core lint and scoped invariants pass. |
| C27 — Reuse index-key decoding for row witnesses | Complete | 101 focused tests, strict core lint and scoped invariants pass. |
| C24 — Remove unused scan bookkeeping | Complete | Unread counter and empty destructor removed; scan checks pass. |
| C25 — Borrow scan bounds | Complete | Heap, live, canonical and mixed-overlay traversal checks pass; admission preserved. |
| C26 — Encode borrowed index segments | Complete | Shared framing preserves golden bytes, codec bounds and malformed-key rejection. |
| C23 — Bind recovery readiness to registry storage | Complete | 63 focused tests, strict core lint and scoped invariants pass; remote CI rerun remains user-owned. |
| C21 — Decode data-store keys from borrowed bytes | Complete | 84 focused tests, complete Clippy workflow, focused lint and invariants pass. |
| C22 — Use standard index-key comparisons | Complete | 103 focused tests, strict core lint and index-range invariants pass; scoped closeout is next. |
| C20 — Borrow strict index frames during unique validation | Complete | 65 native tests, three canister cases, strict lint and scoped invariants pass. |
| C19 — Reuse owned index-codec buffers | Complete | 63 focused index tests, strict core lint and index-range invariants pass. |
| C17 — Narrow bound DDL field requests | Complete | Borrowed resolution and narrowed requests pass DDL, no-op, admission and pending-abort checks. |
| C18 — Remove temporary index-comparison allocations | Complete | Fixed component storage preserves ordering, bounds and malformed-key handling; focused gates pass. |
| C16 — Reuse unique-validation page keys | Complete | One key collection; same-page conflicts and all selected activation cases pass. |
| C14 — Reuse the known nullability target | Complete | Forward the admitted change; indexed transitions and exact no-ops pass. |
| C15 — Avoid temporary store-path allocation | Complete | Verify reads only a numeric revision; captured records retain owned paths. |
| C11 — Canonical default/rename validation | Complete, combined with C12–C13 at the user's request | Complete-candidate, no-op and maintained DDL cases pass. |
| C12 — Shared validation resume checks | Complete | Receipt/revision invariants, startup recovery and both activation canister cases pass. |
| C13 — Allocation-free publication classification | Complete | Publication/recovery, generated startup and DDL cases pass; scoped closeout next. |
| C9 — One snapshot-version setter | Complete, combined with C10 at the user's request | Metadata preservation, planning and DDL cases pass; scoped closeout next. |
| C10 — Reuse validated nullability | Complete, combined with C9 | Not-null activation and direct nullable publication pass. |
| C6 — Shared validation startup | Complete | 12 native tests, two canister activation cases, strict lint and focused invariants pass. |
| C7 — Canonical field-drop validation | Complete | 20 native tests, both canister drop cases and focused invariants pass. |
| C8 — Shared validation-job preflight | Complete | 15 native tests, two canister activation cases, strict lint and focused invariants pass; scoped closeout next. |
| C3 — Shared SQL DDL candidate builder | Complete, combined with C4–C5 at the user's request | Seven focused DDL cases pass, including no-op publication. |
| C4 — One catalogue selection during index staging | Complete, combined with C3 and C5 | 23 index-domain tests and affected DDL cases pass. |
| C5 — Shared typed-binding freshness check | Complete, combined with C3–C4 | Both typed-query/write compile contracts and strict facade linting pass. |
| C2 — Shared startup receipt binding | Complete: 13 startup tests and strict core-library linting pass | Reviewable worktree handoff; both review candidates complete, scoped closeout next. |
| C1 — Shared validation-job publication | Complete: four focused tests and strict core-library linting pass | Reviewable worktree handoff; startup-receipt conversion remains separate follow-up. |
| I3 — ICYDB-039 shared-shard convergence | Complete: original-artifact schedule comparison and instrumented journal/atomicity qualification pass | Downstream representative scheduling and live-capacity acceptance remain; preserve existing admission and timer owners. |
| I2 — ICYDB-038 application catalogue reads | Complete: managed selected/full comparisons and restoration checks pass | Application-owned integration and whole-action costs remain before ICYDB-038 closure; no production runtime change. |
| E1 — Audit and contract | Complete: existing rename support identified; 11 focused checks pass | Preserve the bounded contract. |
| E2 — Populated catalog rename | Complete: checked companion admission and native qualification pass | Retain the native authority and recovery coverage. |
| E3 — Generated upgrade and IC qualification | Complete: two generated rename scenarios plus three maintained cast regressions pass | Retain the measured cost limits. |
| I1 — Post-publication read investigation | Complete: both refined actor scenarios pass; recurring accepted runtime preparation identified | Retain the explicit query-only cost limitation. |
| Q1 — Closeout | Complete: ready within the agreed entity-rename scope | User-owned full validation and release; production cost work only if separately authorised. |
| D1 — Timer dependency update | Complete: ic-timers 0.8.0; 11 focused integration tests and affected all-feature lints pass | User-owned full validation and release. |

## Baseline and previous line

- Initially clean IcyDB HEAD and `v0.260.0`:
  `0339efcd9fe35d9a27c4d4f2b809f9b8a6c557c6`.
- Cargo remains 0.260.0. Active planning/release notes open at 0.261.0 only;
  release tooling owns version bumps.
- The preceding read-only 0.260 closeout reported ready within its agreed scope:
  35 focused tests, formatting, whitespace and 48 local links passed; all 149
  recorded Canic artifacts matched. Historical IcyDB measurement binaries were
  unavailable at recorded cache paths, while their committed reports/messages
  remained. Full suites and final-release composed size were not requalified.
- Toko Miner had adopted the E275 diagnostic in local source; broader exact-count
  metadata acceptance and application-specific ICYDB-033 attribution remained
  downstream work. They are not folded into entity rename.
- No edits to the published 0.260 notes or its historical evidence in this slice.

## Validation and scope

### D1 final evidence

Published `ic-timers` 0.8.0 replaces 0.7.1 as one resolved package identity.
Only its version/checksum change in Cargo.lock; all other package records and
IcyDB workspace/package versions are unchanged. Existing cadence/immediate
watchdog calls need no Rust adaptation. The new registration identity and exact
deadline APIs remain upstream capabilities, without new IcyDB execution modes.

All 11 selected PocketIC tests pass: three startup timer tests (trap, actual
instruction exhaustion, upgrade re-registration and overdue coalescing), three
lifecycle composition tests (ordering, ingress surface and upgrade rollback),
and all five schema-migration scenarios, including both populated renames.
All-feature library Clippy passes for the facade and SQL/startup/lifecycle/audit
timer consumers. Formatting, whitespace and local links pass; full suites remain
user-owned. One disposable PocketIC server was started and stopped.

The rename measurements retain I1's exact inputs, output hashes, host rehearsal,
toolchain, build settings and repository-local Cargo home. New Cargo.lock SHA-256:
`cced82bfcae498325318d32b5ea6eeefd765939bae80450bf4e4fc870777d2db`.
Source/successor actors are 8,925,638 / 8,957,951 raw bytes: **+3,393 / +4,156**
versus I1's 0.7.1 builds. Their respective BLAKE3 identities are
`c64374900208f796ee1abc71add2112ef52af094c622d17bfd521db8102d200c` and
`0eaf6f074174d5a6aeae1bbd11959b7e0767941c5ad635e9a7b79f0997c16877`.

| Current measurement (delta from I1) | Direct advance | Restart before advance |
| --- | ---: | ---: |
| Initial Advance cycles | 405,336,769 (+41,794) | 405,479,191 (+34,037) |
| Immediate retry cycles | 133,281,896 (+749,901) | 132,936,489 (+190,967) |
| Retry after restart cycles | 152,184,680 (-9,805) | 152,123,378 (+18,951) |
| Post-publication SQL instructions | 34,604,356 (-47,207) | 34,622,893 (+31,093) |
| Restarted SQL after constraint updates, instructions | 1,980,290 (-1,041) | 1,981,474 (-79,857) |

Cycle figures retain the whole-command envelope; read figures retain the local
SQL interval. Repeated query-only reads still pay the accepted-runtime
preparation cost. This dependency update is not a fix for I1's limitation or an
optimization claim. Migration-body instructions and production release-size
deltas remain unmeasured. E3/I1/Q1 cost records remain historical 0.7.1 evidence.

D1 changes seven files, approximately +50 net documentation lines plus the
dependency substitutions. IcyDB implementation shape stays neutral, with no Rust
edits, new persisted state, or application-network changes. No commits or pushes.

### I1/Q1 final evidence

All 32 selected native/schema/macro tests and both refined generated rename
scenarios pass freshly. The affected integration lint gate, formatting,
whitespace and local documentation links pass. E3's unchanged three physical
cast scenarios and all-feature actor/schema lints are retained evidence.
Full suites remain user-owned. See [closeout](q1-closeout.md) for the scope,
artifact reconciliation, warnings and remaining measurements.

Repeated post-publication reads cost 34.59–34.65 million instructions; one
ordinary update that retains accepted runtime preparation reduces subsequent
reads to 2.04–2.20 million without changing rows. Restart alone does not lower
the cost. I1 actor raw sizes are 8,922,245 / 8,953,795 bytes (+31,550), with a
documented Cargo-home path difference from E3. Command cycle deltas versus E3
are zero. No production optimization is claimed.

I1/Q1 touches nine files, approximately +280 net lines including +52 host-test
Rust lines. Production complexity is unchanged; observation coverage grows
modestly. Two disposable PocketIC servers were started and stopped. Cargo
versions/dependencies, application networks, commits and pushes are unchanged.

### E3 handoff evidence (historical)

Both rename scenarios preserve rows, generated bindings, lookup and constraints,
report zero rewritten rows/rebuilt indexes, and replay exact terminal receipts
before/after restart. The three physical cast regressions pass unchanged. Focused
all-feature actor/schema and integration lints pass. Fixture-only setup/read/
projection assertions were corrected before the final affected-test rerun.
Formatting, whitespace and local links pass; full suites remain user-owned.

Raw source/successor actors are 8,919,185 / 8,950,738 bytes (+31,553). Initial
Advance envelopes are 405,294,975–405,445,154 cycles. Source/read-after-publication/
restarted read intervals are 1,818,723 / 34,591,800–34,651,563 /
1,981,331–2,061,331 instructions. These are fixture lifecycle observations,
not a runtime optimization comparison; migration-body instructions and release
composed size remain unmeasured. See [exact scope and inputs](e3-evidence.md).

E3 adds approximately 600 net lines across 16 files, entirely tests, fixture
configuration/build helpers and docs. Test complexity increases modestly;
production runtime state, formats and execution paths remain unchanged.
Four disposable PocketIC server runs were started and stopped after an initial
sandbox bind failure. No application network, dependencies, Cargo versions,
commits or pushes changed. Q1 is not started in this handoff.

### E2 handoff evidence (historical)

All 32 focused Rust tests pass, including the grammar target's three UI cases.

| Boundary | Evidence |
| --- | --- |
| Populated application/publication | Six native tests: self/inbound rename, unchanged data and derived keys, accepted IDs/layout/generations, exact lineage, indexed-key reads, relation validation/delete restrictions, stale bindings/cursors, replay and repeated recovery. The publication journal has one schema record and zero row/index mutation records; row-mutation revision is unchanged. |
| Invalid transitions | Native admission tests preserve head, lineage and bytes on missing companions, unexplained owner changes, missing predecessors, wrong stores and meaningless bumps. Eleven existing planner tests retain stale-head/version and accepted-identity coverage; the existing source-binding conflict test preserves the predecessor on an occupied target. |
| Canonical proof/codec | Two source-digest tests and seven migration-codec tests pass. Rename proof retains lexical target order and detects reachable named-type and referenced-target field-policy changes. Current plan roundtrip and fixed digest vector remain valid. |
| Generated declaration | Four macro tests and one grammar target pass, including the new companion pass fixture. The obsolete empty-transition compile-fail fixture is replaced, not retained as compatibility coverage. |

Formatting, whitespace, local documentation links, the repository's complete
Clippy workflow and the final focused all-feature lint gate pass. Initial fixture
compile/paging/schema-admission errors and redundant-closure lint findings were
corrected before the final rerun. Trybuild reports non-blocking unused workspace
patch warnings for crates outside its generated fixture graph.

The native interruption tests construct the existing compound marker from the
maintained planner, receipt and lineage owners, stopping before publication or
after receipt application. They are not generated-canister upgrade tests.
Full suites remain user-owned. No Wasm actors, IC execution or network lifecycle
actions were run; raw Wasm bytes, cycles and instruction deltas are unmeasured.

Cumulative 0.261 worktree: 17 changed/added/deleted files, approximately +1,500 net
lines including E1 and the initial E2 probe. Production prefixes across the four
changed owners total +83 net lines; the rest is direct tests and documentation.
Admission-proof complexity increases narrowly; one existing declaration shape
is newly admitted, with no new persisted state, format, opcode or execution flow.
Cargo versions, dependencies, sibling repositories, commits and pushes are unchanged.

### E1 evidence (historical)

Documentation-only E1. Raw Wasm, cycles and instruction deltas are unmeasured;
production structure and state space are unchanged. No Rust edits, dependency
changes, sibling edits, actor builds or network lifecycle actions. Full suites
and publication remain user-owned. All 11 existing migration-planner tests pass
with all features, including accepted-identity renames and typed lineage/head
rejections. All 17 local links in the four detailed documents resolve; whitespace
checks pass. Five documentation files change, approximately +200 net lines.
No actor-level or populated-rename recovery claim follows from these checks.
