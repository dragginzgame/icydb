# 0.47 Status (as of 2026-03-10, closeout workstream active)

## 0.47 Scope

This status tracks `0.47` audit-pass work defined in:

- `docs/design/0.47-audit-pass/0.47-design.md` (primary design, including `Section 10` AccessPath consolidation)
- `docs/design/0.47-accesspath-architecture/0.47-design.md` (historical snapshot retained after consolidation)
- `docs/audits/reports/2026-03/2026-03-09/summary.md`
- `docs/audits/reports/2026-03/2026-03-09/meta-audit.md`

`0.47` is an audit line focused on structural-pressure reduction and contract
preservation before additional feature expansion.

---

## Progress Summary

Completed in `0.47`:

- route/validate branch containment (`executor/route if: 89 -> 18`,
  `query/plan/validate if: 74 -> 9`)
- decision-owner containment (`AccessPath: 5 -> 3`, `RouteShape: 5 -> 3`)
- route-planner hub cross-layer containment (`2 -> 1`)
- large-module concentration reduction (`>=600 LOC: 17 -> 12`)
- velocity locality + load fan-in containment (item-1 closed:
  `median=3`, `p95=5`, fan-in `195 -> 156`)
- continuation spread containment without authority regression (item-2 closed:
  `1,059 -> 1,048`, `AccessPath=3`, `RouteShape=3`)
- DRY high-risk seam closeout (item-3 closed: high-risk divergence patterns
  `1 -> 0`)
- governance hardening for daily baseline reruns + report backfill
- full recurring rerun bundle with closeout snapshot evidence

Deferred to next recurring run (explicit owners/actions below):

- continuation + DRY seam non-regression monitoring

---

## Frozen Remaining Work (0.47 Close)

Frozen items have been completed. No new scope should be added to `0.47`.

1. Continuation spread containment without authority regression
   - owner boundary: `cursor + route continuation contracts`
   - required evidence:
     - `docs/audits/reports/2026-03/2026-03-10/complexity-accretion.md`
   - pass criteria:
     - continuation surface does not grow above current `1,059` mentions
     - `AccessPath decision owners` remains `<= 3`
     - `RouteShape decision owners` remains `<= 3`
   - status: **Closed** (`1,048`, `AccessPath=3`, `RouteShape=3`)

2. DRY high-risk seam closeout
   - owner boundary: `query/plan/route` + `executor/route`
   - required evidence:
     - `docs/audits/reports/2026-03/2026-03-10/dry-consolidation.md`
   - pass criteria:
     - high-risk divergence-prone patterns reduced from current `1` to `0`,
      or explicitly documented as intentional with one planner-owned authority
      + one runtime fail-closed assertion surface
   - status: **Closed** (`high-risk divergence-prone patterns: 0`)

Completion rule:
- once both items pass, mark `0.47` as closed and carry any remaining
  optimizations into `0.48` only.

---

## 0.47.0 Kickoff Readiness

Preparation status: **ready to start**

Kickoff prerequisites closed:

- [x] recurring audit tree/domain naming normalized
- [x] recurring reports migrated to `YYYY-MM/YYYY-MM-DD` layout
- [x] month-level report summaries created (`docs/audits/reports/YYYY-MM/summary.md`)
- [x] design/status files co-located under `docs/design/<slice>/`
- [x] baseline recurring run selected (`2026-03-09`)

Immediate start target:

- first execution slice for `0.47.0`: structural pressure containment
  (`executor/route`, `executor/load`, `query/plan`)

---

## In Scope (`0.47`)

- recurring audit reruns with dated report bundles
- structural pressure reduction (`complexity`, `module`, `velocity`)
- layer-ownership and duplication control
- correctness/taxonomy/compliance contract preservation
- explicit owner/action/target-run follow-ups for high-risk findings

## Out of Scope (`0.47`)

- query-surface expansion by default
- speculative refactors without audit findings
- compatibility shim work
- redesign outside owner boundaries implicated by findings

---

## Roadmap Snapshot (`0.47`)

- [x] A. design tracker established (`docs/design/0.47-audit-pass/0.47-design.md`)
- [x] B. status tracker established
- [x] C. recurring baseline run captured (`2026-03-09` report bundle)
- [x] D. structural pressure containment pass
- [x] E. layer-ownership consolidation pass
- [x] F. correctness/taxonomy/compliance lock pass
- [x] G. AccessPath architecture consolidation pass
- [x] H. recurring rerun + closeout summary

Current `0.47` status: **closed** (all frozen items completed with evidence;
carry further optimization work into `0.48`).

---

## AccessPath Architecture Status (2026-03-10 consolidated)

Reference:

- primary: `docs/design/0.47-audit-pass/0.47-design.md` (`Section 10`)
- historical snapshot: `docs/design/0.47-accesspath-architecture/0.47-design.md`

Status:

- state: `Accepted`
- implementation state: `Complete (monitoring mode)`
- owner boundary: `access` + `query/plan` + `executor/route`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/`

Current metrics snapshot:

- AccessPath variants: `7` (threshold `<=10`)
- AccessPath decision owners: `3` (threshold `<=3`, within target)
- AccessPath references: `121` across `12` files
- AccessPath match-arm proxy (`AccessPath::` arm lines): `17`

Primary action targets:

- keep decision-owner count at `<=3` while landing follow-up slices
- keep direct variant branching centralized under `db/access/dispatch.rs`
- shift runtime consumers toward capability/route-shape contracts

---

## Complexity Feedback Status (2026-03-09)

Feedback source:

- complexity-accretion review against `docs/audits/reports/2026-03/2026-03-09/`

Accepted diagnosis:

- `6 -> 7` is a growth-pattern warning, not a catastrophic break
- dominant signal is route-shape decision-owner spread (`3 -> 5`)
- branch pressure growth concentrated in route and validation seams
- continuation spread grew, but conceptual ownership remains centralized
- architectural invariants remain intact; risk is coordination growth

Status:

- feedback state: `Accepted`
- implementation state: `Complete with deferred residuals`
- owner boundary: `query/plan` + `executor/route` + `query/plan/validate`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/`

Target outcomes:

- RouteShape decision owners: `5 -> <=3`
- `executor/route` branch pressure: reduce `if` density via shape-based
  `match` dispatch
- `query/plan/validate` branch pressure: split into narrower helper modules
- complexity risk index: `7 -> 6` (or lower) within one audit cycle

---

## Velocity Feedback Status (2026-03-09)

Feedback source:

- velocity-preservation review against
  `docs/audits/reports/2026-03/2026-03-09/velocity-preservation.md`

Accepted diagnosis:

- score rise `5 -> 6` reflects coordination pressure, not boundary collapse
- slice-size expansion is the largest signal:
  - average files touched: `2.3 -> 15.0`
  - p95 files touched: `3 -> 17`
- gravity-well pressure grew sharply:
  - top fan-in proxy: `117 -> 255`
- highest-leverage hubs remain:
  - `executor/route/planner/mod.rs`
  - `executor/load/*`

Status:

- feedback state: `Accepted`
- implementation state: `Complete with deferred residuals`
- owner boundary: `executor/route/planner` + `executor/load` + `query/plan`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/velocity-preservation.md`

Target outcomes:

- route planner staged contract containment:
  - planner -> `RouteShape` -> executor dispatch
  - reduce route-planner cross-layer import pressure to `<=1`
- load hub decomposition plan and first split execution:
  - `dispatch`
  - `strategy`
  - `terminal`
- slice-size containment guardrails:
  - median files touched per slice target: `<=8`
  - p95 files touched per slice target: `<=15`
- velocity risk index target: `6 -> 5` (or lower) within one audit cycle

---

## Workstream Tracker

| Workstream | State | Owner Boundary | Next Gate |
|---|---|---|---|
| Structural pressure reduction (`complexity`, `module`, `velocity`) | Complete with deferred follow-ups | `executor/route`, `executor/load`, `query/plan` | reduce continuation residuals and hold velocity/fan-in containment in next recurring run |
| Layer ownership and duplication control (`layer-violation`, `dry`) | Complete with deferred follow-ups | `query/plan`, `executor`, `index` | close remaining high-risk DRY seam in next recurring run (`dry-consolidation`) |
| AccessPath architecture hardening (`Section 10` consolidated contract) | Complete | `access`, `query/plan`, `executor/route` | keep `AccessPath decision owners <= 3` in next recurring run |
| Correctness contract preservation (`boundary`, `cursor`, `index`, `invariants`, `state`, `recovery`) | Complete | cursor/index/commit/executor boundaries | preserve low-risk baseline (`3-4/10`) through next cycle |
| Taxonomy + resource compliance (`error-taxonomy`, `resource-model`) | Complete | query/intent + error/resource contracts | preserve typed class/origin mapping and expanded resource enforcement checklist |
| Meta-audit governance conformance | Complete | docs/audit process boundary | keep preamble/readout/actionability compliance and append-only history |

---

## Residual Findings (Deferred From 2026-03-09 Baseline)

- [x] route-shape policy authority spread reduced in working tree
      (`decision owners: 5 -> 3`, rerun confirmed in `2026-03-09` rerun set)
- [x] elevated route branch pressure materially reduced (baseline
      `executor/route if=89`, rerun proxy `if=18` in
      `complexity-accretion-2.md`)
- [x] elevated validation branch pressure materially reduced (baseline
      `query/plan/validate if=74`, rerun proxy `if=9` in
      `complexity-accretion-2.md`)
- [x] route-planner hub cross-layer pressure reduced (`2 -> 1`) and held across
      reruns (`velocity-preservation-5.md`, `module-structure-2.md`)
- [x] large-module concentration reduced (`>=600 LOC`: `17 -> 12`) in
      `module-structure-2.md`
- [x] same-day rerun baseline discipline enforced in governance docs and
      backfilled in `2026-03-09` rerun reports
- [x] increased continuation surface references contained:
      baseline `925 -> 1,059`, then reduced to `1,048` in
      `2026-03-10/complexity-accretion.md` with owner caps held
      (`AccessPath=3`, `RouteShape=3`).
- [x] velocity slice-size locality restored under guardrails in
      `2026-03-10/velocity-preservation.md`:
      median files touched `3` (`<=8` target pass), p95 `5` (`<=15` target
      pass)
- [x] gravity-well coordination growth contained under frozen item-1 target:
      top fan-in proxy reduced `195 -> 156` in
      `2026-03-10/velocity-preservation-2.md` (target `<=160` pass)
- [x] load grouped route-stage contracts decomposition slice landed
      (`load/contracts/grouped/route_stage.rs 304 ->`
      `load/contracts/grouped/route_stage/mod.rs 7` +
      `route_stage/payload.rs 71` +
      `route_stage/projection.rs 174`)
      so grouped route payload assembly is separated from grouped route-stage
      projection trait/continuation adapter surface
- [x] load execute hub decomposition slice landed
      (`execute/mod.rs 657 -> 332`, new `execute/fast_path.rs 343`)
- [x] load execute-contracts decomposition slice landed
      (`load/execute/contracts.rs 251 ->`
      `load/execute/contracts/mod.rs 7` +
      `contracts/inputs.rs 99` +
      `contracts/stream.rs 100` +
      `contracts/outcomes.rs 55`)
      so execution-input projection contracts, resolved-key-stream contracts,
      and finalization metrics contracts are isolated behind one module boundary
- [x] load pipeline-orchestrate decomposition slice landed
      (`load/entrypoints/pipeline/orchestrate/mod.rs 246 ->`
      `orchestrate/mod.rs 117` +
      `orchestrate/payload.rs 115` +
      `orchestrate/guards.rs 42`)
      so stage orchestration is separated from paging/materialization helpers
      and test guard surfaces
- [x] load ranking-materialized decomposition slice landed
      (`load/terminal/ranking/materialized.rs 249 ->`
      `terminal/ranking/materialized/mod.rs 114` +
      `terminal/ranking/materialized/projections.rs 147`)
      so ranked-row ordering core contracts are separated from terminal
      projection/output adapters
- [x] load grouped-distinct decomposition slice landed
      (`load/grouped_distinct.rs 205 ->`
      `grouped_distinct/mod.rs 7` +
      `grouped_distinct/aggregate.rs 124` +
      `grouped_distinct/paging.rs 86`)
      so grouped DISTINCT aggregate runtime and singleton paging adapters are
      separated behind one grouped-distinct boundary
- [x] load fast-path strategy decomposition slice landed
      (`load/execute/fast_path/mod.rs 229 -> 173`,
      `load/execute/fast_path/strategy.rs 136 -> 192`)
      so fast-path decision/route-selection policy is isolated from access-stream
      resolution/fallback orchestration
- [x] load grouped-output decomposition slice landed
      (`load/grouped_output.rs 193 ->`
      `grouped_output/mod.rs 7` +
      `grouped_output/finalize.rs 84` +
      `grouped_output/projection.rs 117`)
      so grouped output finalization/observability and grouped projection shaping
      are separated under one grouped-output boundary
- [x] load ranking-terminal API decomposition slice landed
      (`load/terminal/ranking/mod.rs 176 ->`
      `terminal/ranking/mod.rs 8` +
      `terminal/ranking/by_slot.rs 143` +
      `terminal/ranking/take.rs 39`)
      so ranking-by-slot terminal adapters and simple `take(k)` terminal helpers
      are separated under one ranking boundary
- [x] projection binary-operator decomposition slice landed
      (`projection/eval/operators/binary.rs 205 ->`
      `operators/binary/mod.rs 119` +
      `operators/binary/comparison.rs 89`)
      so binary arithmetic/boolean dispatch and binary comparison/equality
      coercion helpers are separated under one operator boundary
- [x] grouped aggregate contracts hub decomposition slice landed
      (`aggregate/contracts/grouped.rs 683 -> grouped/context.rs 411`,
      `grouped/engine.rs 289`, `grouped/mod.rs 13`)
- [x] load contracts module decomposition slice landed
      (`load/contracts.rs 580 -> load/contracts/mod.rs 124`,
      `load/contracts/grouped.rs 477`)
- [x] load grouped-contract decomposition slice landed
      (`load/contracts/grouped.rs 477 ->`
      `load/contracts/grouped/mod.rs 13` +
      `load/contracts/grouped/route_stage/mod.rs 7` +
      `load/contracts/grouped/route_stage/payload.rs 71` +
      `load/contracts/grouped/route_stage/projection.rs 174` +
      `load/contracts/grouped/stages.rs 175`)
      so grouped planner/route projection contracts are separated from grouped
      stream/fold stage payload contracts
- [x] grouped runtime decomposition slice landed
      (`load/grouped_runtime.rs 366 ->`
      `load/grouped_runtime/mod.rs 14` +
      `load/grouped_runtime/continuation.rs 242` +
      `load/grouped_runtime/runtime.rs 124`)
      so grouped continuation window/cursor contracts are separated from grouped
      runtime direction/metrics/trace context contracts
- [x] grouped runtime-continuation decomposition slice landed
      (`load/grouped_runtime/continuation.rs 242 ->`
      `grouped_runtime/continuation/mod.rs 7` +
      `continuation/window.rs 70` +
      `continuation/capabilities.rs 91` +
      `continuation/context.rs 84`)
      so grouped continuation window contracts, continuation capability contracts,
      and continuation cursor-token context contracts are separated
- [x] load entrypoint pipeline decomposition slice landed
      (`load/entrypoints/pipeline.rs -> load/entrypoints/pipeline/mod.rs 83`
      + `load/entrypoints/pipeline/orchestrate.rs 369`)
- [x] route planner contract ownership trimming slice landed
      (`ExecutionRoutePlan::{for_mutation, grouped_observability}` moved from
      `executor/route/planner/mod.rs` into `executor/route/contracts.rs`)
- [x] route planner entrypoint decomposition slice landed
      (`executor/route/planner/mod.rs 311 -> 103`,
      `executor/route/planner/entrypoints.rs 224`)
- [x] route contracts decomposition slice landed
      (`route/contracts.rs 914 -> route/contracts/execution.rs 525`,
      `route/contracts/continuation.rs 241`,
      `route/contracts/shape.rs 103`,
      `route/contracts/capabilities.rs 58`,
      `route/contracts/mod.rs 38`)
- [x] route execution-contract decomposition slice landed
      (`route/contracts/execution.rs 525 ->`
      `route/contracts/execution/mod.rs 177` +
      `route/contracts/execution/plan.rs 289` +
      `route/contracts/execution/observability.rs 77`)
      so route mode/shape/spec contracts, grouped route observability payload,
      and execution-route payload behavior are no longer co-located in one file
- [x] route-hints decomposition slice landed
      (`route/hints.rs 258 -> route/hints/mod.rs 7` +
      `route/hints/load.rs 121` +
      `route/hints/aggregate.rs 150`)
      so load-window/index-range hint gates are separated from aggregate
      bounded-probe/seek hint contracts under route-owned hint derivation
- [x] route planner feasibility decomposition slice landed
      (`route/planner/feasibility.rs 405 ->`
      `route/planner/feasibility/mod.rs 236` +
      `route/planner/feasibility/gates.rs 120` +
      `route/planner/feasibility/grouped_strategy.rs 84`)
      so feasibility orchestration, gate policy evaluation, and grouped strategy
      runtime revalidation are separated under planner ownership
- [x] AccessPath architecture decision-owner threshold restored in working tree
      (`decision owners: 5 -> 3`, rerun confirmed in
      `complexity-accretion-2.md`)
- [x] grouped DISTINCT policy-reason projection/reporting centralized under
      `GroupDistinctPolicyReason` authority
      (`planner_group_plan_error` + `invariant_message`) so planner/runtime
      fail-closed edges no longer duplicate reason mapping tables

---

## Closeout Snapshot (2026-03-09 rerun set)

Starting risk indices (`2026-03-09` baseline):

- complexity-accretion: `7/10`
- velocity-preservation: `6/10`
- module-structure: `6/10`
- layer-violation: `5/10`
- dry-consolidation: `5/10`

Current rerun indices (latest comparable runs):

- complexity-accretion: `5.5/10` (`2026-03-10/complexity-accretion.md`)
- velocity-preservation: `4.0/10` (`2026-03-10/velocity-preservation-2.md`)
- module-structure: `5/10` (`2026-03-09/module-structure-2.md`)
- layer-violation: `4/10` (`2026-03-09/layer-violation-2.md`)
- dry-consolidation: `4.5/10` (`2026-03-10/dry-consolidation.md`)

Items reduced:

- route-planner cross-layer hub pressure (`2 -> 1`)
- route/validate branch pressure (`89 -> 18`, `74 -> 9`)
- AccessPath/RouteShape decision owners (`5 -> 3`, `5 -> 3`)
- large-module concentration (`>=600 LOC`: `17 -> 12`)

Items deferred (owner/action/target-run):

- owner boundary: `cursor + route continuation contracts`
  action: non-regression monitor for continuation spread and owner caps
  target run: `docs/audits/reports/2026-03/2026-03-12/complexity-accretion.md`
- owner boundary: `query/plan/route` + `executor/route`
  action: non-regression monitor to keep high-risk divergence-prone DRY
  patterns at `0`
  target run: `docs/audits/reports/2026-03/2026-03-12/dry-consolidation.md`

Accepted residual risk:

- continuation spread improved to `1,048` and now moves to non-regression
  monitoring in the next recurring cycle.
- load-hub fan-in is now within target (`156`) but close to threshold, so drift
  should be monitored in the next comparable velocity run.
- [x] grouped validation policy decomposition slice landed
      (`query/plan/validate/grouped/policy.rs 414 ->`
      `grouped/policy/mod.rs 234` + `grouped/policy/rules.rs 193`)
      to separate planner grouped-policy orchestration from first-violation
      rule evaluation helpers
- [x] validate taxonomy/root decomposition slice landed
      (`query/plan/validate/mod.rs 507 -> 45`,
      new `query/plan/validate/errors.rs 472`)
      so validate module wiring and semantic-error taxonomy/conversion flow are
      no longer co-located in one root file
- [x] planner expr type-inference hotspot decomposition slice landed
      (`query/plan/expr/type_inference.rs 675 ->`
      `query/plan/expr/type_inference/mod.rs 370` +
      `query/plan/expr/type_inference/tests.rs 304`)
      so runtime inference logic and test matrix are separated and planner
      runtime modules remain under the `600 LOC` soft-cap guideline

### Grouped DISTINCT DRY Finding Status

- finding state: `Accepted`
- implementation status: `Mitigated in working tree`
- priority: `High`
- worth fixing in `0.47`: `Yes`
- rationale: this is the recurring divergence-prone seam called out in prior
  DRY audits; left alone, it increases branch and drift pressure at the
  planner/runtime boundary
- action boundary: keep planner semantic authority + executor fail-closed
  runtime guards, but centralize policy-reason projection and remove avoidable
  runtime re-derivation paths
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/dry-consolidation.md`

---

## Cursor Notes (Edge Cases to Validate)

- [x] composite prefix continuation anchor completeness verified:
      `load_composite_range_cursor_pagination_matches_unbounded_and_anchor_is_strictly_monotonic`
      and
      `load_index_range_cursor_anchor_matches_last_emitted_row_after_post_access_pipeline`
      confirm composite index-range cursors preserve full raw-key anchor
      semantics and maintain strict resume monotonicity without duplicates
- [x] reverse-scan continuation semantics verified for DESC paging:
      `load_single_field_desc_range_resume_from_upper_anchor_returns_remaining_rows`
      and `load_composite_desc_range_mixed_edges_resume_inside_duplicate_group`
      confirm reverse traversal uses strict excluded-anchor progression at the
      correct envelope edge

---

## Index Integrity Watchpoint

- [x] unique-enforcement parity between live apply and replay apply:
      `unique_conflict_classification_parity_holds_between_live_apply_and_replay`
      now locks duplicate-unique conflict class parity (`Conflict`) between
      live save enforcement and replay recovery enforcement, with boundary-owned
      origin differences kept explicit (`Index` vs `Recovery`)

Status:

- watchpoint state: `Closed in working tree`
- priority: `Medium-High`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/index-integrity.md`

---

## Verification Readout (Baseline Run: 2026-03-09)

- `bash scripts/ci/check-layer-authority-invariants.sh` -> PASS
- `bash scripts/ci/check-index-range-spec-invariants.sh` -> PASS
- `bash scripts/ci/check-field-projection-invariants.sh` -> PASS
- `bash scripts/ci/check-memory-id-invariants.sh` -> PASS
- `bash scripts/ci/check-architecture-text-scan-invariants.sh` -> PASS
- `cargo check -p icydb-core` -> PASS
- `cargo test -p icydb-core anchor_equal_to_upper_resumes_to_empty_envelope -- --nocapture` -> PASS
- `cargo test -p icydb-core anchor_containment_guard_rejects_out_of_envelope_anchor -- --nocapture` -> PASS
- `cargo test -p icydb-core recovery_replay_is_idempotent -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_plan_rejects_order_without_limit -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_plan_having_order_limit_composition_enforces_bounded_policy -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_fluent_execute_supports_cursor_continuation -- --nocapture` -> PASS
- `cargo test -p icydb-core route_feature_budget_shape_kinds_stay_within_soft_delta -- --nocapture` -> PASS
- `cargo test -p icydb-core access_plan_rejects_misaligned_index_range_spec -- --nocapture` -> PASS
- `make check-invariants` -> PASS
- `cargo check -p icydb-core` -> PASS
- `cargo clippy -p icydb-core -- -D warnings` -> PASS
- `cargo test -p icydb-core grouped_execution_budget_counters_remain_consistent_for_distinct_grouped_fold -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_aggregate_state_enforces_distinct_values_total_limit -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_aggregate_state_enforces_max_groups_hard_limit -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_distinct_without_adjacency_proof_fails_in_planner_policy -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_distinct_with_having_fails_in_planner_policy -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_plan_rejects_global_distinct_unsupported_kind -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_executor_handoff_projects_scalar_distinct_policy_violation_for_executor -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_route_strategy_to_metrics_strategy_mapping_is_stable -- --nocapture` -> PASS
- `cargo test -p icydb-core route_plan_mutation_is_materialized_with_no_fast_paths_or_hints -- --nocapture` -> PASS
- `cargo test -p icydb-core route_plan_mutation_rejects_non_delete_mode -- --nocapture` -> PASS
- `cargo test -p icydb-core route_feature_budget_shape_kinds_stay_within_soft_delta -- --nocapture` -> PASS
- `cargo test -p icydb-core route_hints_use_route_window_and_budget_safety_filter_gates -- --nocapture` -> PASS
- `cargo test -p icydb-core load_composite_range_cursor_pagination_matches_unbounded_and_anchor_is_strictly_monotonic -- --nocapture` -> PASS
- `cargo test -p icydb-core load_index_range_cursor_anchor_matches_last_emitted_row_after_post_access_pipeline -- --nocapture` -> PASS
- `cargo test -p icydb-core load_single_field_desc_range_resume_from_upper_anchor_returns_remaining_rows -- --nocapture` -> PASS
- `cargo test -p icydb-core load_composite_desc_range_mixed_edges_resume_inside_duplicate_group -- --nocapture` -> PASS
- `cargo test -p icydb-core unique_conflict_classification_parity_holds_between_live_apply_and_replay -- --nocapture` -> PASS
- `cargo test -p icydb-core recovery_replay_interrupted_conflicting_unique_batch_fails_closed -- --nocapture` -> PASS
- `cargo test -p icydb-core infer_binary_compare_rejects_unknown_operands_fail_closed -- --nocapture` -> PASS
- `cargo test -p icydb-core infer_sum_aggregate_requires_numeric_target -- --nocapture` -> PASS
- `cargo test -p icydb-core executor_internal_stream_and_window_types_do_not_widen_to_pub_crate -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_structure_rejects_having_group_field_symbol_outside_group_keys -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_projection_compatibility_rejects_binary_expr_with_non_group_field -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_order_prefix_must_align_with_group_keys_in_planner_cursor_policy -- --nocapture` -> PASS
- `cargo test -p icydb-core index_range_aggregate_fast_path_specs_reject_prefix_spec_presence -- --nocapture` -> PASS
- `cargo test -p icydb-core plan_rejects_unorderable_field -- --nocapture` -> PASS
- `cargo test -p icydb-core plan_rejects_duplicate_non_primary_order_field -- --nocapture` -> PASS
- `cargo test -p icydb-core plan_rejects_order_without_terminal_primary_key_tie_break -- --nocapture` -> PASS
- `cargo test -p icydb-core eval_expr_supports_arithmetic_projection -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_projection_mixing_aggregate_and_arithmetic_evaluates -- --nocapture` -> PASS
- `cargo test -p icydb-core session_load_top_k_by_matches_execute_field_ordering -- --nocapture` -> PASS
- `cargo test -p icydb-core session_load_take_matches_execute_prefix -- --nocapture` -> PASS
- `cargo test -p icydb-core -q` -> PASS
- `make test` -> PASS

---

## Running Notes (Append-Only)

- 2026-03-09: status tracker initialized for `0.47` audit pass.
- 2026-03-09: complexity feedback intake recorded as accepted/in-progress.
- 2026-03-09: started `0.47.0` structural-pressure containment execution with
  grouped planner policy-rule consolidation in
  `db/query/plan/validate/grouped/policy.rs` (rule-context + `first_violated_rule`
  boundary); validation test suites passed.
- 2026-03-09: reduced route-feasibility branch-token pressure in
  `db/executor/route/planner/feasibility.rs` by converting simple policy gates
  and hint toggles to combinator-based derivation while preserving invariant
  assertions (`if|match` token proxy: `13 -> 3`); subtree branch proxies now
  trend down (`executor/route if`: `89 -> 74`,
  `query/plan/validate if`: `74 -> 68`); route budget test passed.
- 2026-03-09: centralized route-shape intent mapping in
  `db/executor/route/planner/intent.rs` through one
  `route_shape_kind_for_intent(...)` policy helper (removes duplicated
  aggregate/count/grouped shape derivation branches; `if|match` proxy:
  `5 -> 3`); route budget test re-run passed.
- 2026-03-09: expanded recurring
  `docs/audits/recurring/contracts/resource-model-compliance.md` from
  policy-only checks to policy + runtime-enforcement + budget-lifecycle
  coverage (adds exhaustion fail-closed, propagation, monotonicity, reset, and
  operator-classification checks).
- 2026-03-09: reran architecture guard scripts after route/planner changes;
  layer/index/projection/memory/text-scan checks all PASS, with latest snapshot
  still reporting `AccessPath decision owners: 5` and
  `RouteShape decision owners: 5` (next consolidation slices still required).
- 2026-03-09: reduced route hint/capability gate branching with fail-closed
  combinator guards in `db/executor/route/hints.rs` and
  `db/executor/route/capability.rs`; `if` proxies:
  `hints.rs 21 -> 13`, `capability.rs 8 -> 4`, subtree `executor/route if`
  `89 -> 62` while `query/plan/validate if` remains `68`. Validation:
  `cargo clippy -p icydb-core -- -D warnings` PASS; route-budget and grouped
  planner-policy tests PASS.
- 2026-03-09: consolidated ownership counting seams by merging access-plan
  capability traversal helpers in `db/access/capabilities.rs` and renaming
  route execution helper boundaries
  (`derive_route_capabilities -> derive_execution_capabilities`,
  `derive_route_feasibility_stage -> derive_execution_feasibility_stage`);
  `check-layer-authority-invariants.sh` now reports
  `AccessPath decision owners: 3` and `RouteShape decision owners: 3`.
- 2026-03-09: reduced grouped structure validation branch density in
  `db/query/plan/validate/grouped/structure.rs` by replacing repeated
  `if` rejection chains with guard-pattern `match` arms and fail-closed
  `ok_or_else` checks; file `if` proxy `15 -> 9`, subtree
  `query/plan/validate if` `68 -> 62`. Validation:
  `cargo clippy -p icydb-core -- -D warnings` PASS; grouped structure test PASS.
- 2026-03-09: reduced secondary pushdown route branching in
  `db/executor/route/pushdown.rs` by collapsing repeated precondition
  `if` chains into explicit eligibility booleans and one return expression;
  file `if` proxy `7 -> 0`, subtree `executor/route if` `62 -> 55`.
  Validation: `cargo clippy -p icydb-core -- -D warnings` PASS; route-budget
  test PASS; layer-authority invariants PASS with owner counts still
  `AccessPath: 3`, `RouteShape: 3`.
- 2026-03-09: reduced terminal route fast-path branching in
  `db/executor/route/terminal.rs` by replacing repeated `if` chains with
  option-chain contracts and slice-pattern matching; file `if` proxy `11 -> 3`,
  subtree `executor/route if` `55 -> 47`. Validation:
  `cargo clippy -p icydb-core -- -D warnings` PASS; route-budget test PASS;
  layer-authority invariants PASS with owner counts stable at
  `AccessPath: 3`, `RouteShape: 3`.
- 2026-03-09: reduced intent policy validation branching in
  `db/query/plan/validate/intent_policy.rs` by converting rule evaluators to
  option-guard expressions and returning `first_violated_rule(...).map_or(...)`
  directly; file `if` proxy `9 -> 1`, subtree
  `query/plan/validate if` `62 -> 54`. Validation:
  `cargo clippy -p icydb-core -- -D warnings` PASS; intent-policy test PASS;
  layer-authority invariants PASS with owner counts unchanged.
- 2026-03-09: executed first load-hub decomposition slice by splitting
  `db/executor/load/entrypoints/mod.rs` into orchestration wrappers
  (`86 LOC`) plus `db/executor/load/entrypoints/pipeline.rs` (`408 LOC`) while
  preserving the leaf-module continuation-resolution contract in `mod.rs`.
  Validation: `cargo clippy -p icydb-core -- -D warnings` PASS; load-structure
  stage/leaf contract tests PASS; layer-authority invariants PASS.
- 2026-03-09: reduced grouped policy branch density in
  `db/query/plan/validate/grouped/policy.rs` by replacing per-rule loop/guard
  `if` exits with `find_map(...).map_or(...)` fail-closed returns; file `if`
  proxy `10 -> 7`, subtree `query/plan/validate if` `54 -> 51`.
  Validation: `cargo clippy -p icydb-core -- -D warnings` PASS; grouped policy
  tests PASS; route/load structural guard tests PASS; layer-authority
  invariants PASS with owner counts unchanged.
- 2026-03-09: reduced load contract-module size below soft-cap by moving
  constructor/recovered-context/field-slot helper methods from
  `db/executor/load/contracts.rs` to `db/executor/load/context.rs`
  (`contracts.rs 620 -> 580 LOC`), and added dedicated
  `entrypoints/pipeline.rs` stage module while keeping continuation resolution
  authority in `entrypoints/mod.rs` to satisfy leaf-boundary tests.
  Validation: `cargo clippy -p icydb-core -- -D warnings` PASS; load structure
  tests (`load_pipeline_optional_stage_slots_stay_within_soft_delta`,
  `load_entrypoint_leaf_modules_do_not_resolve_continuation_directly`) PASS;
  grouped policy tests PASS; layer-authority invariants PASS.
- 2026-03-09: executed second load-hub decomposition slice by converting
  `db/executor/load/execute.rs` into directory module
  `db/executor/load/execute/` and extracting fast-path/fallback key-stream
  policy into `execute/fast_path.rs`; size now
  `execute/mod.rs 332 LOC`, `execute/fast_path.rs 343 LOC`
  (previous single file `657 LOC`). Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `load_execute_stage_order_matches_linear_contract` PASS;
  `load_pipeline_optional_stage_slots_stay_within_soft_delta` PASS;
  `load_entrypoint_leaf_modules_do_not_resolve_continuation_directly` PASS;
  `scripts/ci/check-layer-authority-invariants.sh` PASS
  (`AccessPath decision owners: 3`, `RouteShape decision owners: 3`).
- 2026-03-09: updated runtime route-consumer guard coverage in
  `db/executor/tests/route/load_matrix.rs` to follow the new execute module
  layout (`execute/mod.rs`, `execute/fast_path.rs`) after the directory split.
  Validation: targeted guard test
  `runtime_route_consumers_avoid_direct_execution_mode_field_reads` PASS;
  full crate suite `cargo test -p icydb-core -q` PASS.
- 2026-03-09: reduced grouped DISTINCT planner/runtime duplication by replacing
  grouped handoff strategy payload
  `GroupedDistinctExecutionStrategy::GlobalDistinctFieldAggregate { kind, ... }`
  with explicit planner-lowered variants
  (`GlobalDistinctFieldCount`, `GlobalDistinctFieldSum`) and removing runtime
  grouped DISTINCT aggregate-kind policy re-derivation in
  `db/executor/load/grouped_distinct.rs`.
  Validation: `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  grouped DISTINCT handoff/runtime tests PASS
  (`grouped_executor_handoff_lowers_global_distinct_execution_strategy`,
  `grouped_executor_handoff_lowers_global_distinct_sum_execution_strategy`,
  `grouped_executor_handoff_projects_scalar_distinct_policy_violation_for_executor`,
  `grouped_fluent_execute_global_count_distinct_field_emits_single_row`,
  `grouped_fluent_execute_global_sum_distinct_field_emits_single_row`);
  `make check-invariants` PASS.
- 2026-03-09: decomposed grouped aggregate contracts hub by converting
  `db/executor/aggregate/contracts/grouped.rs` into directory module
  `db/executor/aggregate/contracts/grouped/{mod.rs,context.rs,engine.rs}`:
  grouped budget/config/admission ownership moved to `context.rs` and grouped
  state/engine ingest/finalize ownership moved to `engine.rs`; resulting module
  sizes are `context.rs 411 LOC`, `engine.rs 289 LOC`, `mod.rs 13 LOC`
  (previous single file `683 LOC`). Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  grouped contract budget tests PASS
  (`grouped_execution_budget_counters_remain_consistent_for_distinct_grouped_fold`,
  `grouped_aggregate_state_enforces_distinct_values_total_limit`,
  `grouped_aggregate_state_enforces_max_groups_hard_limit`);
  `cargo test -p icydb-core -q` PASS; `make test` PASS.
- 2026-03-09: centralized grouped DISTINCT policy-reason projection by moving
  planner grouped error mapping from
  `db/query/plan/validate/grouped/policy.rs` into
  `GroupDistinctPolicyReason::planner_group_plan_error(...)` in
  `db/query/plan/semantics/group_distinct.rs`, paired with existing
  `invariant_message()` runtime projection. This removes duplicated
  planner/runtime reason mapping tables and keeps one policy-reason authority.
  Validation: `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  grouped policy/runtime bridge tests PASS
  (`grouped_distinct_without_adjacency_proof_fails_in_planner_policy`,
  `grouped_distinct_with_having_fails_in_planner_policy`,
  `grouped_plan_rejects_global_distinct_unsupported_kind`,
  `grouped_executor_handoff_projects_scalar_distinct_policy_violation_for_executor`);
  `make check-invariants` PASS.
- 2026-03-09: trimmed route planner hub ownership by moving
  `ExecutionRoutePlan::{for_mutation, grouped_observability}` from
  `db/executor/route/planner/mod.rs` to route-contract ownership in
  `db/executor/route/contracts.rs`; planner now consumes these contracts
  instead of owning route payload behavior projection. Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  route tests PASS
  (`grouped_route_strategy_to_metrics_strategy_mapping_is_stable`,
  `route_plan_mutation_is_materialized_with_no_fast_paths_or_hints`,
  `route_plan_mutation_rejects_non_delete_mode`);
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: decomposed route planner entrypoint orchestration by moving
  load/aggregate/grouped/mutation route-plan entrypoint methods from
  `db/executor/route/planner/mod.rs` into
  `db/executor/route/planner/entrypoints.rs`, reducing planner root module
  concentration (`mod.rs 311 -> 103 LOC`). Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  route tests PASS
  (`grouped_route_strategy_to_metrics_strategy_mapping_is_stable`,
  `route_plan_mutation_is_materialized_with_no_fast_paths_or_hints`,
  `route_plan_mutation_rejects_non_delete_mode`,
  `route_feature_budget_shape_kinds_stay_within_soft_delta`);
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: decomposed load contracts hub by converting
  `db/executor/load/contracts.rs` into directory module
  `db/executor/load/contracts/{mod.rs,grouped.rs}` and moving grouped
  stage/projection payload contracts under `contracts/grouped.rs`; resulting
  module sizes are `mod.rs 124 LOC` and `grouped.rs 477 LOC`
  (previous single file `580 LOC`). Updated load-structure guard fixture paths
  to the new module root (`contracts/mod.rs`). Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `executor_internal_stream_and_window_types_do_not_widen_to_pub_crate` PASS;
  grouped/load structure tests PASS
  (`grouped_fluent_execute_supports_cursor_continuation`,
  `grouped_fluent_execute_global_count_distinct_field_emits_single_row`,
  `grouped_execution_budget_counters_remain_consistent_for_distinct_grouped_fold`,
  `load_pipeline_optional_stage_slots_stay_within_soft_delta`,
  `load_entrypoint_leaf_modules_do_not_resolve_continuation_directly`);
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: decomposed route hint derivation by converting
  `db/executor/route/hints.rs` into directory module
  `db/executor/route/hints/{mod.rs,load.rs,aggregate.rs}` so load-window and
  index-range hint eligibility is separated from aggregate bounded-probe/seek
  hint policy. Updated runtime guard scan coverage in
  `db/executor/stream/access/tests.rs` to read all new hint module files.
  Validation: `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  route tests PASS
  (`grouped_route_strategy_to_metrics_strategy_mapping_is_stable`,
  `route_feature_budget_shape_kinds_stay_within_soft_delta`,
  `route_plan_mutation_is_materialized_with_no_fast_paths_or_hints`,
  `route_plan_mutation_rejects_non_delete_mode`,
  `route_hints_use_route_window_and_budget_safety_filter_gates`);
  `cargo test -p icydb-core --test compile_fail -- --nocapture` PASS;
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: closed cursor edge-case checklist by validating composite-anchor
  and DESC-resume behavior with targeted pagination tests:
  `load_composite_range_cursor_pagination_matches_unbounded_and_anchor_is_strictly_monotonic`,
  `load_index_range_cursor_anchor_matches_last_emitted_row_after_post_access_pipeline`,
  `load_single_field_desc_range_resume_from_upper_anchor_returns_remaining_rows`,
  and `load_composite_desc_range_mixed_edges_resume_inside_duplicate_group`.
  Outcome: composite range cursors keep full raw-key anchors and strict
  monotonic progression; reverse scans preserve strict excluded-anchor resume
  semantics at the correct envelope edge.
- 2026-03-09: closed index-integrity live/replay unique-parity watchpoint by
  adding commit-level parity lock
  `unique_conflict_classification_parity_holds_between_live_apply_and_replay`.
  The test verifies duplicate-unique conflicts remain `ErrorClass::Conflict`
  in both live save and replay paths while preserving boundary-owned origins
  (`ErrorOrigin::Index` for live enforcement,
  `ErrorOrigin::Recovery` for replay enforcement). Companion replay fail-closed
  guard `recovery_replay_interrupted_conflicting_unique_batch_fails_closed`
  re-run PASS.
- 2026-03-09: marked roadmap slice `F` (correctness/taxonomy/compliance lock
  pass) complete after closing cursor edge-case validation and index
  live/replay unique-conflict parity with explicit recurring-audit checklist
  requirements and passing verification commands.
- 2026-03-09: reduced planner structural-pressure concentration by converting
  `db/query/plan/expr/type_inference.rs` into directory module
  `db/query/plan/expr/type_inference/{mod.rs,tests.rs}` so runtime inference
  logic and test matrix are isolated; runtime planner modules now remain under
  the `600 LOC` soft-cap (`max: 516 LOC`) in the current tree.
  Validation: `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `infer_binary_compare_rejects_unknown_operands_fail_closed` PASS;
  `infer_sum_aggregate_requires_numeric_target` PASS;
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: reduced aggregate route-hint branch pressure in
  `db/executor/route/hints/aggregate.rs` by replacing nested field-target and
  capability gate `if` chains with shape-aware match projection plus shared
  fail-closed eligibility guards (`if` proxy `12 -> 2` in file; subtree
  `executor/route if` `47 -> 37`). Validation:
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `route_hints_use_route_window_and_budget_safety_filter_gates` PASS;
  `make check-invariants` PASS.
- 2026-03-09: reduced grouped-validation branch pressure by converting grouped
  cursor and grouped policy gate checks to fail-closed combinator contracts in
  `db/query/plan/validate/grouped/{cursor.rs,policy/mod.rs}` and by flattening
  grouped structure/projection gate checks in
  `db/query/plan/validate/grouped/{structure.rs,projection_expr.rs}`. Current
  subtree proxy now trends `query/plan/validate if: 51 -> 43`
  (baseline `74`). Validation:
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  grouped validation tests PASS
  (`grouped_structure_rejects_having_group_field_symbol_outside_group_keys`,
  `grouped_projection_compatibility_rejects_binary_expr_with_non_group_field`,
  `grouped_order_prefix_must_align_with_group_keys_in_planner_cursor_policy`);
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: reduced route-guard branch pressure by converting fast-path
  arity invariant checks in `db/executor/route/guard.rs` to fail-closed
  combinator guards while preserving invariant error boundaries. Current route
  subtree proxy now trends `executor/route if: 37 -> 33` (baseline `89`).
  Validation: `cargo clippy -p icydb-core -- -D warnings` PASS;
  `index_range_aggregate_fast_path_specs_reject_prefix_spec_presence` PASS;
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: reduced route capability/fast-path branch pressure by converting
  PK stream verification and aggregate route shape gates in
  `db/executor/route/{fast_path.rs,capability.rs}` to fail-closed eligibility
  combinators. Current route subtree proxy now trends
  `executor/route if: 33 -> 28` (baseline `89`) while authority counters remain
  stable (`AccessPath decision owners: 3`, `RouteShape decision owners: 3`).
  Validation: `cargo clippy -p icydb-core -- -D warnings` PASS;
  `route_feature_budget_shape_kinds_stay_within_soft_delta` PASS;
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: reduced ORDER validation branch pressure by converting ORDER
  field admissibility, duplicate non-PK detection, and PK tie-break checks in
  `db/query/plan/validate/order.rs` to fail-closed combinator guards. Current
  validation subtree proxy now trends `query/plan/validate if: 43 -> 39`
  (baseline `74`). Validation: `cargo clippy -p icydb-core -- -D warnings`
  PASS; ORDER semantics tests PASS
  (`plan_rejects_unorderable_field`,
  `plan_rejects_duplicate_non_primary_order_field`,
  `plan_rejects_order_without_terminal_primary_key_tie_break`);
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: hardened recurring DRY governance rules by requiring a mandatory
  high-risk divergence ledger and explicit owner/action follow-up whenever
  `High-risk divergence-prone patterns > 0` in DRY reports, even if overall DRY
  risk index is below `6` (`docs/audits/AUDIT-HOWTO.md` and
  `docs/audits/recurring/crosscutting/crosscutting-dry-consolidation.md`).
- 2026-03-09: decomposed load projection-eval operator hub by converting
  `db/executor/load/projection/eval.rs` into directory module
  `db/executor/load/projection/eval/{mod.rs,operators.rs}` so row-context field
  resolution and grouped aggregate slot lookup remain in `mod.rs` while unary /
  binary operator arithmetic/boolean/equality/ordering semantics move to one
  focused operator module (`mod.rs 162 LOC`, `operators.rs 253 LOC`; previous
  single file `396 LOC`). Validation: `cargo clippy -p icydb-core -- -D warnings`
  PASS; projection tests PASS
  (`eval_expr_supports_arithmetic_projection`,
  `grouped_projection_mixing_aggregate_and_arithmetic_evaluates`);
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: decomposed load terminal ranking hub by converting
  `db/executor/load/terminal/ranking.rs` into directory module
  `db/executor/load/terminal/ranking/{mod.rs,materialized.rs}` so terminal API
  entrypoint/planner-slot wiring remains in `mod.rs` while deterministic
  materialized top-k/bottom-k ranking/tie-break mechanics move to
  `materialized.rs` (`mod.rs 176 LOC`, `materialized.rs 249 LOC`; previous
  single file `399 LOC`). Validation: `cargo clippy -p icydb-core -- -D warnings`
  PASS; ranking terminal behavior tests PASS
  (`session_load_top_k_by_matches_execute_field_ordering`,
  `session_load_take_matches_execute_prefix`);
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: decomposed grouped validation module concentration by converting
  `db/query/plan/validate/grouped/{projection_expr.rs,structure.rs}` into
  directory modules
  `grouped/projection_expr/{mod.rs,tests.rs}` and
  `grouped/structure/{mod.rs,tests.rs}` so planner runtime grouped validation
  helpers are isolated from their test matrices while keeping grouped
  validation behavior stable. Validation:
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  grouped validation tests PASS
  (`grouped_structure_rejects_having_group_field_symbol_outside_group_keys`,
  `grouped_projection_compatibility_rejects_binary_expr_with_non_group_field`);
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued grouped validation decomposition by converting
  `db/query/plan/validate/grouped/cursor.rs` into
  `grouped/cursor/{mod.rs,tests.rs}` and moving grouped policy test matrix from
  `grouped/policy/mod.rs` to `grouped/policy/tests.rs`, so grouped runtime
  cursor/policy gate code remains focused on planner validation behavior while
  grouped test matrices are isolated under dedicated test modules. Validation:
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  grouped validation tests PASS
  (`grouped_order_prefix_must_align_with_group_keys_in_planner_cursor_policy`,
  `grouped_distinct_with_having_fails_in_planner_policy`);
  `make check-invariants` PASS; `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued route/validate branch containment by flattening
  cursor/fluent policy gates and aggregate route-shape execution-mode
  derivation into tuple-driven contracts in
  `db/query/plan/validate/{cursor_policy.rs,fluent_policy.rs}` and
  `db/executor/route/planner/execution/{shape_aggregate_count.rs,shape_aggregate_non_count.rs}`,
  plus fail-closed fast-path loop/shape gate simplification in
  `db/executor/route/fast_path.rs`. Current subtree proxies now trend:
  `executor/route if: 28 -> 18` and
  `query/plan/validate if: 39 -> 9` (baseline `89` / `74`). Validation:
  `cargo fmt --all` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: stored crosscutting rerun reports after the additional `.5`
  branch/owner containment pass:
  `docs/audits/reports/2026-03/2026-03-09/complexity-accretion-2.md`
  (`Complexity Risk Index: 6/10`) and
  `docs/audits/reports/2026-03/2026-03-09/velocity-preservation-2.md`
  (`Velocity Risk Index: 5/10`), with summary rollup at
  `docs/audits/reports/2026-03/2026-03-09/summary-2.md`.
- 2026-03-09: continued post-`0.47.5` load-hub decomposition by converting
  projection operator evaluation from one file
  `db/executor/load/projection/eval/operators.rs` into:
  - `db/executor/load/projection/eval/operators/mod.rs`
  - `db/executor/load/projection/eval/operators/unary.rs`
  - `db/executor/load/projection/eval/operators/binary.rs`
  so unary and binary semantics no longer co-reside in one operator module
  (`267 LOC` total split as `10 + 52 + 205`). Validation:
  `cargo fmt --all` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `make check-invariants` PASS;
  projection tests PASS
  (`eval_expr_supports_arithmetic_projection`,
  `grouped_projection_mixing_aggregate_and_arithmetic_evaluates`);
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued grouped-fold decomposition by converting
  `db/executor/load/grouped_fold/candidate_rows.rs` into:
  - `db/executor/load/grouped_fold/candidate_rows/mod.rs`
  - `db/executor/load/grouped_fold/candidate_rows/sink.rs`
  so grouped candidate sink strategy state (`bounded`/`unbounded`) is isolated
  from grouped reducer finalization/alignment orchestration. Validation:
  `cargo fmt --all` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `make check-invariants` PASS;
  grouped-fold regression tests PASS
  (`grouped_fluent_execute_supports_cursor_continuation`,
  `grouped_execution_budget_counters_remain_consistent_for_distinct_grouped_fold`);
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued load entrypoint pipeline decomposition by converting
  `db/executor/load/entrypoints/pipeline/orchestrate.rs` into:
  - `db/executor/load/entrypoints/pipeline/orchestrate/mod.rs`
  - `db/executor/load/entrypoints/pipeline/orchestrate/state.rs`
  so staged pipeline state artifacts are isolated from orchestration functions
  while preserving the existing six-stage execution order guard
  (`build_execution_context`, `execute_access_path`,
  `apply_grouping_projection`, `apply_paging`, `apply_tracing`,
  `materialize_surface`). Validation:
  `cargo fmt --all` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `make check-invariants` PASS;
  load structure guard tests PASS
  (`load_pipeline_optional_stage_slots_stay_within_soft_delta`,
  `load_entrypoint_leaf_modules_do_not_resolve_continuation_directly`);
  `route_hints_use_route_window_and_budget_safety_filter_gates` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued fast-path resolution decomposition by converting
  `db/executor/load/execute/fast_path.rs` into:
  - `db/executor/load/execute/fast_path/mod.rs`
  - `db/executor/load/execute/fast_path/strategy.rs`
  so fast-path resolution strategy and route-handler policy are isolated from
  key-stream resolution/fallback orchestration.
  Also updated invariant and guard fixtures to the new path layout in:
  - `scripts/ci/check-index-range-spec-invariants.sh`
  - `db/executor/tests/route/load_matrix.rs`
  Validation:
  `cargo fmt --all` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `make check-invariants` PASS;
  `runtime_route_consumers_avoid_direct_execution_mode_field_reads` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: stored additional crosscutting rerun reports after continued
  post-`0.47.5` load decomposition slices:
  `docs/audits/reports/2026-03/2026-03-09/complexity-accretion-3.md`
  (`Complexity Risk Index: 6/10`) and
  `docs/audits/reports/2026-03/2026-03-09/velocity-preservation-3.md`
  (`Velocity Risk Index: 5/10`), with summary rollup at
  `docs/audits/reports/2026-03/2026-03-09/summary-3.md`.
- 2026-03-09: continued scalar entrypoint decomposition by converting
  `db/executor/load/entrypoints/scalar.rs` into:
  - `db/executor/load/entrypoints/scalar/mod.rs`
  - `db/executor/load/entrypoints/scalar/surface.rs`
  - `db/executor/load/entrypoints/scalar/hints.rs`
  so scalar surface adapters and unpaged hint policy are isolated from scalar
  execution-path orchestration (`261 LOC -> 119 + 73 + 90`). Validation:
  `cargo fmt --all` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `make check-invariants` PASS;
  `runtime_route_consumers_avoid_direct_execution_mode_field_reads` PASS;
  `load_pipeline_optional_stage_slots_stay_within_soft_delta` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued execution contract decomposition by converting
  `db/executor/load/execute/mod.rs` from one mixed contract/finalization file
  into:
  - `db/executor/load/execute/mod.rs` (finalization + guards)
  - `db/executor/load/execute/contracts.rs` (execution input/stream contracts)
  so execution-attempt payload contracts are isolated from load finalization
  hooks (`332 LOC -> 94 + 251`). Validation:
  `cargo fmt --all` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `make check-invariants` PASS;
  `runtime_route_consumers_avoid_direct_execution_mode_field_reads` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: stored additional crosscutting rerun reports after scalar and
  execute-contract decomposition slices:
  `docs/audits/reports/2026-03/2026-03-09/complexity-accretion-4.md`
  (`Complexity Risk Index: 6/10`) and
  `docs/audits/reports/2026-03/2026-03-09/velocity-preservation-4.md`
  (`Velocity Risk Index: 5/10`), with summary rollup at
  `docs/audits/reports/2026-03/2026-03-09/summary-4.md`.
- 2026-03-09: continued load route-hub decomposition by converting
  `db/executor/load/fast_stream_route.rs` into:
  - `db/executor/load/fast_stream_route/mod.rs`
  - `db/executor/load/fast_stream_route/handlers.rs`
  so fast-stream route request/dispatch contracts are isolated from
  route-specific binding/execute helpers while keeping runtime behavior stable.
  Validation:
  `cargo fmt --all` PASS;
  `cargo clippy -p icydb-core -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: stored additional crosscutting rerun reports after
  `fast_stream_route` decomposition:
  `docs/audits/reports/2026-03/2026-03-09/complexity-accretion-5.md`
  (`Complexity Risk Index: 6/10`) and
  `docs/audits/reports/2026-03/2026-03-09/velocity-preservation-5.md`
  (`Velocity Risk Index: 5/10`), with summary rollup at
  `docs/audits/reports/2026-03/2026-03-09/summary-5.md`.
- 2026-03-09: enforced daily-baseline rerun governance in audit docs
  (`docs/audits/AUDIT-HOWTO.md`, `docs/audits/META-AUDIT.md`,
  `docs/audits/recurring/README.md`, recurring templates) and backfilled
  same-day rerun preambles/tables so reruns compare to day baseline files
  instead of prior reruns.
- 2026-03-09: stored additional crosscutting ownership/structure reruns:
  `docs/audits/reports/2026-03/2026-03-09/layer-violation-2.md`
  (`Cross-Cutting Risk Index: 4/10`),
  `docs/audits/reports/2026-03/2026-03-09/dry-consolidation-2.md`
  (`DRY Risk Index: 5/10`),
  `docs/audits/reports/2026-03/2026-03-09/module-structure-2.md`
  (`Structural Risk Index: 5/10`), and consolidated closeout rollup at
  `docs/audits/reports/2026-03/2026-03-09/summary-6.md`.
- 2026-03-09: continued load grouped-contract decomposition by converting
  `db/executor/load/contracts/grouped/route_stage.rs` into:
  - `db/executor/load/contracts/grouped/route_stage/mod.rs`
  - `db/executor/load/contracts/grouped/route_stage/payload.rs`
  - `db/executor/load/contracts/grouped/route_stage/projection.rs`
  so grouped route payload assembly is separated from grouped route-stage
  projection and continuation adapter surfaces (`304 LOC -> 7 + 71 + 174`).
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued load execute-contract decomposition by converting
  `db/executor/load/execute/contracts.rs` into:
  - `db/executor/load/execute/contracts/mod.rs`
  - `db/executor/load/execute/contracts/inputs.rs`
  - `db/executor/load/execute/contracts/stream.rs`
  - `db/executor/load/execute/contracts/outcomes.rs`
  so execution-input projection contracts, resolved-key-stream contracts, and
  finalization metrics contracts are separated under one execute-contract module
  boundary (`251 LOC -> 7 + 99 + 100 + 55`).
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued load pipeline-orchestrate decomposition by converting
  `db/executor/load/entrypoints/pipeline/orchestrate/mod.rs` into:
  - `db/executor/load/entrypoints/pipeline/orchestrate/mod.rs` (stage flow)
  - `db/executor/load/entrypoints/pipeline/orchestrate/payload.rs`
  - `db/executor/load/entrypoints/pipeline/orchestrate/guards.rs`
  so stage-orchestration flow is separated from paging/materialization helpers
  and test-only guard surfaces (`246 LOC -> 117 + 115 + 42`).
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued grouped-runtime continuation decomposition by converting
  `db/executor/load/grouped_runtime/continuation.rs` into:
  - `db/executor/load/grouped_runtime/continuation/mod.rs`
  - `db/executor/load/grouped_runtime/continuation/window.rs`
  - `db/executor/load/grouped_runtime/continuation/capabilities.rs`
  - `db/executor/load/grouped_runtime/continuation/context.rs`
  so grouped pagination window contracts, continuation capability contracts, and
  continuation cursor-token context contracts are separated under one grouped
  runtime continuation boundary (`242 LOC -> 7 + 70 + 91 + 84`).
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued terminal ranking materialized decomposition by
  converting `db/executor/load/terminal/ranking/materialized.rs` into:
  - `db/executor/load/terminal/ranking/materialized/mod.rs`
  - `db/executor/load/terminal/ranking/materialized/projections.rs`
  so ranked-row ordering core contracts and terminal projection/output adapters
  are separated under one materialized ranking boundary (`249 LOC -> 114 + 147`).
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued grouped DISTINCT decomposition by converting
  `db/executor/load/grouped_distinct.rs` into:
  - `db/executor/load/grouped_distinct/mod.rs`
  - `db/executor/load/grouped_distinct/aggregate.rs`
  - `db/executor/load/grouped_distinct/paging.rs`
  so grouped DISTINCT aggregate runtime and singleton paging adapters are
  separated under one grouped-distinct boundary (`205 LOC -> 7 + 124 + 86`).
  Also updated structural guard evidence path in:
  - `db/executor/stream/access/tests.rs`
  to follow the new grouped-distinct aggregate module path.
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-09: continued fast-path strategy decomposition by moving
  route-order evaluation and verified fast-path route execution from:
  - `db/executor/load/execute/fast_path/mod.rs`
  into:
  - `db/executor/load/execute/fast_path/strategy.rs`
  so fast-path decision/route-selection policy is isolated from access-stream
  resolution/fallback orchestration (`mod.rs 229 -> 173`, `strategy.rs 136 -> 192`).
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-10: continued grouped-output decomposition by converting
  `db/executor/load/grouped_output.rs` into:
  - `db/executor/load/grouped_output/mod.rs`
  - `db/executor/load/grouped_output/finalize.rs`
  - `db/executor/load/grouped_output/projection.rs`
  so grouped output finalization/observability helpers and grouped projection
  shaping helpers are separated under one grouped-output boundary
  (`193 LOC -> 7 + 84 + 117`).
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-10: continued ranking-terminal API decomposition by converting
  `db/executor/load/terminal/ranking/mod.rs` into:
  - `db/executor/load/terminal/ranking/mod.rs`
  - `db/executor/load/terminal/ranking/by_slot.rs`
  - `db/executor/load/terminal/ranking/take.rs`
  so ranking-by-slot terminal adapters and simple `take(k)` terminal helpers
  are separated under one ranking boundary (`176 LOC -> 8 + 143 + 39`).
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-10: continued projection binary-operator decomposition by converting
  `db/executor/load/projection/eval/operators/binary.rs` into:
  - `db/executor/load/projection/eval/operators/binary/mod.rs`
  - `db/executor/load/projection/eval/operators/binary/comparison.rs`
  so binary arithmetic/boolean dispatch and binary comparison/equality coercion
  helpers are separated under one binary-operator boundary (`205 LOC -> 119 + 89`).
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-10: continued grouped-route decomposition by converting
  `db/executor/load/grouped_route.rs` into:
  - `db/executor/load/grouped_route/mod.rs`
  - `db/executor/load/grouped_route/metrics.rs`
  - `db/executor/load/grouped_route/resolve.rs`
  so grouped route metrics labeling and grouped route payload assembly are
  isolated under one grouped-route boundary.
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-10: continued fast-stream decomposition by converting
  `db/executor/load/fast_stream.rs` into:
  - `db/executor/load/fast_stream/mod.rs`
  - `db/executor/load/fast_stream/tests.rs`
  so runtime fast-stream invariants are separated from module test contracts.
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-10: stored item-1 closeout velocity rerun at
  `docs/audits/reports/2026-03/2026-03-10/velocity-preservation-2.md`
  (`Velocity Risk Index: 4.0/10`).
  Status against frozen item-1 criteria:
  - slice-size guardrails: PASS (`median=3`, `p95=5`)
  - load fan-in containment target (`<=160`): PASS (`195 -> 156`)
  item-1 is now closed.
- 2026-03-10: consolidated AccessPath architecture companion content into
  `docs/design/0.47-audit-pass/0.47-design.md` (`Section 10`) and kept
  `docs/design/0.47-accesspath-architecture/0.47-design.md` as a historical
  snapshot so no prior context is lost.
- 2026-03-10: reduced continuation spread in index scan runtime by consolidating
  duplicated directional resume/guard loops from:
  - `db/index/scan.rs`
  into shared helpers:
  - `resolve_raw_range_limited(...)`
  - `scan_range_entry(...)`
  so range entrypoints consume one continuation-envelope/advancement guard flow.
  Validation:
  `cargo check -p icydb-core` PASS;
  `cargo clippy -p icydb-core --all-targets -- -D warnings` PASS;
  `make check-invariants` PASS;
  `cargo test -p icydb-core -q` PASS.
- 2026-03-10: stored frozen item-2 closeout evidence at
  `docs/audits/reports/2026-03/2026-03-10/complexity-accretion.md`
  (`Complexity Risk Index: 5.5/10`).
  Status against frozen item-2 criteria:
  - continuation surface cap (`<=1,059`): PASS (`1,048`)
  - owner caps: PASS (`AccessPath=3`, `RouteShape=3`)
  item-2 is now closed.
- 2026-03-10: stored frozen item-3 closeout evidence at
  `docs/audits/reports/2026-03/2026-03-10/dry-consolidation.md`
  (`DRY Risk Index: 4.5/10`).
  Status against frozen item-3 criteria:
  - high-risk divergence-prone patterns: PASS (`1 -> 0`)
  - grouped DISTINCT seam explicitly bounded by:
    - planner reason authority:
      `query/plan/semantics/group_distinct.rs`
    - runtime fail-closed assertion surface:
      `executor/load/grouped_route/resolve.rs`
  item-3 is now closed.
