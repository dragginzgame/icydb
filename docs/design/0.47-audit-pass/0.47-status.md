# 0.47 Status (as of 2026-03-09, audit-pass kickoff)

## 0.47 Scope

This status tracks `0.47` audit-pass work defined in:

- `docs/design/0.47-audit-pass/0.47-design.md`
- `docs/design/0.47-accesspath-architecture/0.47-design.md`
- `docs/audits/reports/2026-03/2026-03-09/summary.md`
- `docs/audits/reports/2026-03/2026-03-09/meta-audit.md`

`0.47` is an audit line focused on structural-pressure reduction and contract
preservation before additional feature expansion.

---

## 0.47.0 Kickoff Readiness

Preparation status: **ready to start**

Kickoff prerequisites closed:

- [x] recurring audit tree/domain naming normalized
- [x] recurring reports migrated to `YYYY-MM/YYYY-MM-DD` layout
- [x] month-level report summaries created (`docs/audits/reports/YYYY-MM/summary.md`)
- [x] design/status files co-located under `docs/design/<slice>/`
- [x] baseline recurring run selected (`2026-03-09`)

Immediate start target:

- first execution slice for `0.47.0`: structural pressure containment
  (`executor/route`, `executor/load`, `query/plan`)

---

## In Scope (`0.47`)

- recurring audit reruns with dated report bundles
- structural pressure reduction (`complexity`, `module`, `velocity`)
- layer-ownership and duplication control
- correctness/taxonomy/compliance contract preservation
- explicit owner/action/target-run follow-ups for high-risk findings

## Out of Scope (`0.47`)

- query-surface expansion by default
- speculative refactors without audit findings
- compatibility shim work
- redesign outside owner boundaries implicated by findings

---

## Roadmap Snapshot (`0.47`)

- [x] A. design tracker established (`docs/design/0.47-audit-pass/0.47-design.md`)
- [x] B. status tracker established
- [x] C. recurring baseline run captured (`2026-03-09` report bundle)
- [ ] D. structural pressure containment pass
- [ ] E. layer-ownership consolidation pass
- [ ] F. correctness/taxonomy/compliance lock pass
- [ ] G. AccessPath architecture consolidation pass
- [ ] H. recurring rerun + closeout summary

Current `0.47` status: **in progress** (kickoff complete; baseline and
feedback intake recorded; implementation workstreams open).

---

## AccessPath Architecture Status (2026-03-09)

Reference:

- `docs/design/0.47-accesspath-architecture/0.47-design.md`

Status:

- state: `Accepted`
- implementation state: `In Progress`
- owner boundary: `access` + `query/plan` + `executor/route`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/`

Current metrics snapshot:

- AccessPath variants: `7` (threshold `<=10`)
- AccessPath decision owners: `5` (threshold `<=3`, action required)
- AccessPath references: `121` across `12` files
- AccessPath match-arm proxy (`AccessPath::` arm lines): `17`

Primary action targets:

- reduce decision-owner count from `5` toward `<=3`
- keep direct variant branching centralized under `db/access/dispatch.rs`
- shift runtime consumers toward capability/route-shape contracts

---

## Complexity Feedback Status (2026-03-09)

Feedback source:

- complexity-accretion review against `docs/audits/reports/2026-03/2026-03-09/`

Accepted diagnosis:

- `6 -> 7` is a growth-pattern warning, not a catastrophic break
- dominant signal is route-shape decision-owner spread (`3 -> 5`)
- branch pressure growth concentrated in route and validation seams
- continuation spread grew, but conceptual ownership remains centralized
- architectural invariants remain intact; risk is coordination growth

Status:

- feedback state: `Accepted`
- implementation state: `In Progress`
- owner boundary: `query/plan` + `executor/route` + `query/plan/validate`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/`

Target outcomes:

- RouteShape decision owners: `5 -> <=3`
- `executor/route` branch pressure: reduce `if` density via shape-based
  `match` dispatch
- `query/plan/validate` branch pressure: split into narrower helper modules
- complexity risk index: `7 -> 6` (or lower) within one audit cycle

---

## Velocity Feedback Status (2026-03-09)

Feedback source:

- velocity-preservation review against
  `docs/audits/reports/2026-03/2026-03-09/velocity-preservation.md`

Accepted diagnosis:

- score rise `5 -> 6` reflects coordination pressure, not boundary collapse
- slice-size expansion is the largest signal:
  - average files touched: `2.3 -> 15.0`
  - p95 files touched: `3 -> 17`
- gravity-well pressure grew sharply:
  - top fan-in proxy: `117 -> 255`
- highest-leverage hubs remain:
  - `executor/route/planner/mod.rs`
  - `executor/load/*`

Status:

- feedback state: `Accepted`
- implementation state: `In Progress`
- owner boundary: `executor/route/planner` + `executor/load` + `query/plan`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/velocity-preservation.md`

Target outcomes:

- route planner staged contract containment:
  - planner -> `RouteShape` -> executor dispatch
  - reduce route-planner cross-layer import pressure to `<=1`
- load hub decomposition plan and first split execution:
  - `dispatch`
  - `strategy`
  - `terminal`
- slice-size containment guardrails:
  - median files touched per slice target: `<=8`
  - p95 files touched per slice target: `<=15`
- velocity risk index target: `6 -> 5` (or lower) within one audit cycle

---

## Workstream Tracker

| Workstream | State | Owner Boundary | Next Gate |
|---|---|---|---|
| Structural pressure reduction (`complexity`, `module`, `velocity`) | Partial | `executor/route`, `executor/load`, `query/plan` | lower route decision-owner and branch-density metrics in next recurring run |
| Layer ownership and duplication control (`layer-violation`, `dry`) | Open | `query/plan`, `executor`, `index` | keep policy/comparator authority centralized with no new duplication drift |
| AccessPath architecture hardening (`0.47` companion doc) | Open | `access`, `query/plan`, `executor/route` | reduce `AccessPath decision owners` from `5` toward `<=3` and keep variant branching centralized |
| Correctness contract preservation (`boundary`, `cursor`, `index`, `invariants`, `state`, `recovery`) | Open | cursor/index/commit/executor boundaries | preserve current low-risk baseline (`3-4/10`) while structural changes land |
| Taxonomy + resource compliance (`error-taxonomy`, `resource-model`) | Open | query/intent + error/resource contracts | preserve typed class/origin mapping and expanded resource enforcement checklist |
| Meta-audit governance conformance | Open | docs/audit process boundary | retain preamble/readout/actionability compliance and append-only history |

---

## Open Findings (From 2026-03-09 Baseline)

- [ ] route-shape policy authority spread (`decision owners: 5`)
- [ ] elevated route branch pressure (baseline `executor/route if=89`,
      current working proxy `if=74`; still above target trend)
- [ ] elevated validation branch pressure (baseline
      `query/plan/validate if=74`, current working proxy `if=68`)
- [ ] increased continuation surface references (`925 -> 1,016`)
- [ ] velocity slice-size expansion:
      avg files touched `2.3 -> 15.0`; p95 `3 -> 17`
- [ ] gravity-well coordination growth:
      top fan-in proxy `117 -> 255` with route/load hub concentration
- [ ] AccessPath architecture thresholds currently exceeded:
      `decision owners: 5` (target `<=3`)
- [ ] grouped DISTINCT policy/runtime divergence seam remains the top DRY
      duplication risk (`query/plan/validate/grouped/policy.rs`,
      `query/plan/group.rs`, `executor/load/grouped_distinct.rs`,
      `executor/aggregate/contracts/grouped.rs`)

### Grouped DISTINCT DRY Finding Status

- finding state: `Accepted`
- priority: `High`
- worth fixing in `0.47`: `Yes`
- rationale: this is the recurring divergence-prone seam called out in prior
  DRY audits; left alone, it increases branch and drift pressure at the
  planner/runtime boundary
- action boundary: keep planner semantic authority + executor fail-closed
  runtime guards, but centralize policy-reason projection and remove avoidable
  runtime re-derivation paths
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/dry-consolidation.md`

---

## Cursor Notes (Edge Cases to Validate)

- [ ] composite prefix continuation anchor completeness:
      for composite index scans (for example `INDEX(a,b,c)` with
      `a = 1 AND b > 5`), verify continuation anchor encoding/validation keeps
      full ordering-key semantics so resume cannot duplicate rows
- [ ] reverse-scan continuation semantics (if/when DESC paging is active):
      verify bound-rewrite direction is inverted correctly for reverse traversal
      and resume uses strict excluded-anchor progression at the correct edge

---

## Index Integrity Watchpoint

- [ ] unique-enforcement parity between live apply and replay apply:
      add/maintain focused checks that both paths enforce identical uniqueness
      outcomes and failure classification for equivalent mutation streams

Status:

- watchpoint state: `Accepted`
- priority: `Medium-High`
- target recurring run: `docs/audits/reports/2026-03/2026-03-12/index-integrity.md`

---

## Verification Readout (Baseline Run: 2026-03-09)

- `bash scripts/ci/check-layer-authority-invariants.sh` -> PASS
- `bash scripts/ci/check-index-range-spec-invariants.sh` -> PASS
- `bash scripts/ci/check-field-projection-invariants.sh` -> PASS
- `bash scripts/ci/check-memory-id-invariants.sh` -> PASS
- `bash scripts/ci/check-architecture-text-scan-invariants.sh` -> PASS
- `cargo check -p icydb-core` -> PASS
- `cargo test -p icydb-core anchor_equal_to_upper_resumes_to_empty_envelope -- --nocapture` -> PASS
- `cargo test -p icydb-core anchor_containment_guard_rejects_out_of_envelope_anchor -- --nocapture` -> PASS
- `cargo test -p icydb-core recovery_replay_is_idempotent -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_plan_rejects_order_without_limit -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_plan_having_order_limit_composition_enforces_bounded_policy -- --nocapture` -> PASS
- `cargo test -p icydb-core grouped_fluent_execute_supports_cursor_continuation -- --nocapture` -> PASS
- `cargo test -p icydb-core route_feature_budget_shape_kinds_stay_within_soft_delta -- --nocapture` -> PASS
- `cargo test -p icydb-core access_plan_rejects_misaligned_index_range_spec -- --nocapture` -> PASS

---

## Running Notes (Append-Only)

- 2026-03-09: status tracker initialized for `0.47` audit pass.
- 2026-03-09: complexity feedback intake recorded as accepted/in-progress.
- 2026-03-09: started `0.47.0` structural-pressure containment execution with
  grouped planner policy-rule consolidation in
  `db/query/plan/validate/grouped/policy.rs` (rule-context + `first_violated_rule`
  boundary); validation test suites passed.
- 2026-03-09: reduced route-feasibility branch-token pressure in
  `db/executor/route/planner/feasibility.rs` by converting simple policy gates
  and hint toggles to combinator-based derivation while preserving invariant
  assertions (`if|match` token proxy: `13 -> 3`); subtree branch proxies now
  trend down (`executor/route if`: `89 -> 74`,
  `query/plan/validate if`: `74 -> 68`); route budget test passed.
- 2026-03-09: centralized route-shape intent mapping in
  `db/executor/route/planner/intent.rs` through one
  `route_shape_kind_for_intent(...)` policy helper (removes duplicated
  aggregate/count/grouped shape derivation branches; `if|match` proxy:
  `5 -> 3`); route budget test re-run passed.
- 2026-03-09: expanded recurring
  `docs/audits/recurring/contracts/resource-model-compliance.md` from
  policy-only checks to policy + runtime-enforcement + budget-lifecycle
  coverage (adds exhaustion fail-closed, propagation, monotonicity, reset, and
  operator-classification checks).
- 2026-03-09: reran architecture guard scripts after route/planner changes;
  layer/index/projection/memory/text-scan checks all PASS, with latest snapshot
  still reporting `AccessPath decision owners: 5` and
  `RouteShape decision owners: 5` (next consolidation slices still required).
