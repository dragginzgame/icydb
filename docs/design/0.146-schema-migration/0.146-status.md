# 0.146 Schema Identity & Layout Authority Status

## Status

In progress. The foundational identity and generated-memory boundary are now
implemented, but live schema reconciliation is not implemented yet.

## Current Implementation State

0.146 starts from the cleaned storage boundary left by 0.144 and the streaming
cleanup completed in 0.145.

The first implementation work established schema identity as an explicit
runtime owner before introducing a durable `__icydb_schema` map:

* separate durable field identity from generated field slot order
* keep generated `EntityModel` as the compiled proposal, not the live authority
* make database-level defaults explicit instead of inferring them from Rust
  `Default`
* prepare a persisted schema snapshot shape that can later become the
  `__icydb_schema` row
* reserve a third generated stable memory for each store so schema metadata has
  a dedicated persistence boundary before reconciliation is implemented

## Completed Foundation

The current code has:

* `FieldId` as the durable logical field identity.
* `SchemaVersion`, `SchemaFieldSlot`, and `SchemaRowLayout` as schema-owned
  layout artifacts.
* `CompiledSchemaProposal` and `CompiledFieldProposal` as the generated
  `EntityModel` / `FieldModel` bridge.
* `PersistedSchemaSnapshot` and `PersistedFieldSnapshot` as the initial owned
  shape for future persisted schema metadata.
* `SchemaFieldDefault::None` as an explicit placeholder that prevents Rust
  struct defaults from being treated as database defaults.
* `SchemaStore` as the per-store stable-memory boundary reserved for future
  schema metadata.
* Generated `#[store(...)]` declarations now require `schema_memory_id`.
* Generated canister wiring now creates `DataStore`, `IndexStore`, and
  `SchemaStore` statics and registers them as one store triplet.
* Schema validation now checks each store's schema memory ID for range,
  reserved-ID, within-store distinctness, and canister-wide uniqueness.
* Commit schema fingerprints now flow through the compiled schema proposal,
  preserving the current fingerprint contract while moving ownership toward
  the schema layer.

## Explicitly Not Live Yet

The third stable memory is only reserved and registered in this slice. It does
not yet contain a `__icydb_schema` BTree, startup reconciliation state, or live
runtime schema authority.

The current runtime still reads generated static schema metadata. That is
intentional until reconciliation can atomically compare the stored live schema
with the compiled proposal and fail closed on incompatible changes.

## Next Correctness Boundary

The next implementation boundary is startup reconciliation:

1. Initialize the concrete schema metadata map in `SchemaStore`.
2. Load the stored schema snapshot for each entity.
3. Compare it with the compiled schema proposal.
4. Accept only safe first-create or explicitly supported transitions.
5. Persist accepted schema snapshots before opening writes.
6. Expose the accepted live schema as the authority for later read/write work.

No row-format compatibility branch, runtime read remapping, or SQL capability
switch should be introduced before this reconciliation boundary exists.

## Guardrails

Do not partially ship a populated durable schema store without startup
reconciliation.

Do not add row-format compatibility branches or per-row schema versions in this
minor version.

Do not treat struct defaults emitted by derive code as database defaults.

Do not let generated `EntityModel` silently override a persisted live schema
once the schema map exists.

Do not route SQL or runtime capability checks through persisted schema data
until that persisted schema has become the accepted live authority.

## Open Work

* [x] Introduce durable schema field identity.
* [x] Add the compiled schema proposal bridge from generated `EntityModel`
  metadata.
* [x] Add schema-owned row-layout identity mapping from durable field IDs to
  generated slots.
* [x] Define the persisted schema snapshot shape.
* [x] Add generated per-store schema stable memory wiring.
* [ ] Define database default metadata separately from Rust construction
  defaults.
* [ ] Add the concrete schema metadata map inside `SchemaStore`.
* [ ] Add startup reconciliation over stored schema versus compiled proposal.
* [ ] Add fail-closed diagnostics for incompatible changes.
* [ ] Move runtime reads through live row-layout authority once reconciliation
  exists.
* [ ] Route SQL capability checks through the live schema model.
* [ ] Extend `DESCRIBE <entity>` so each field reports its physical slot
  number from the accepted live row layout.

## Validation Recorded

The current foundation has been validated with:

* `cargo fmt --all`
* `cargo check -p icydb-core --tests --features sql`
* `cargo check -p icydb-schema --tests`
* `cargo check -p icydb-schema-derive --tests`
* `cargo check -p icydb-build --tests`
* `cargo check --workspace --tests`
* `cargo test -p icydb-core db::schema --features sql -- --nocapture`
* `cargo test -p icydb-core db::registry --features sql -- --nocapture`
* `cargo test -p icydb-schema -- --nocapture`
* `cargo test -p icydb-schema-tests -- --nocapture`
* `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`
* `cargo clippy --workspace --all-targets -- -D warnings`
* `git diff --check`
