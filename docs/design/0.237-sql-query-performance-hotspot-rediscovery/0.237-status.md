# IcyDB 0.237 SQL Query Performance Hotspot Rediscovery Status

Status: ACTIVE — PATCH 2 CLEAN CALIBRATION PENDING

Last updated: 2026-08-22

Design authority: [0.237 design](./0.237-design.md)

Released predecessor: `v0.236.1` at
`2abc8af306743138b9d35b1c0903ef596c985f2c`.

## Assignment

The user explicitly reassigned the unreleased 0.237 number to this SQL
performance evidence line on 2026-08-22. The separate accepted-schema 0.237
design remains closed as a measured no-build; none of its runtime or fixture
work is restored.

## Patch Tracker

| Patch | Outcome | Status |
| --- | --- | --- |
| 1 | Hard-cut the current performance method identity, expand exact scale coverage from 24 to 42 sentinel families, and remove the token-only forced hotspot shortlist | Complete |
| 2 | Capture current P1/scale/P2 calibration evidence and rank absolute, normalized, and sloped hotspots; add only evidence-selected long-ladder canaries | Local P1/scale cohort complete; strict clean calibration pending |
| 3 | Localize removable phases for the leading candidates and close with an optimization intake or no-build verdict | Pending |

## Patch 1 Candidate

The deterministic P1 SQL set remains exactly 1,787 scenarios. The exact scale
set expands from 72 to 126 scenarios: 42 selected families at 16, 256, and
2,048 rows. Added families cover projection/payload width, admitted and
over-cap token branch fanout, primary `OFFSET`, scalar non-count and
distinct/filter aggregation, computed materialization, wide indexed reads, and
full-entity heap/journaled materialization.

The evidence identity hard-cuts from historical
`icydb-sql-performance/0.220/v3` to
`icydb-sql-performance/0.237/v1`. The format version remains exactly 1 and no
old-profile compatibility path is retained. The exact expanded scale-set hash
is `0654aca99f69b89eec75b34829c6015e0ad764b7b74564814df3be02578becd0`;
its eight shard counts are `18/19/12/24/12/13/14/14`.

The 15-case forced hotspot list is removed because it represented one prior
token branch investigation. New P2 candidates come from the complete P1 and
scale rankings plus the existing promoted regression and grouped contract
sentinels.

Production runtime, audit-canister code, schemas, Candid, exports, stable
formats, and package versions are unchanged. Patch 1 changes only host-side
integration measurement declarations and documentation. The current shared SQL
audit build passed at 4,576,629 raw bytes with SHA-256
`dec791d463bb12666711224c692f0a384b56848538d756655f6007c0d82fade9`.
No clean predecessor rebuild was run in this patch, so no numerical Wasm delta
is claimed.

## Patch 1 Live Smoke Evidence

One exact PocketIC 15.0.0 scale shard passed with all 12 assigned observations
and the pinned expanded profile/hash. This is a dirty-worktree candidate smoke
run, not a reviewed comparable baseline. It exercised two new 2,048-row cases
and their existing payload control:

| Scenario | Total instructions | Compile | Execute | Store | Executor | Row gets | Index entries | Output bytes | Rows |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Blob metadata | 2,144,595 | 589,047 | 1,555,548 | 241,424 | 615,547 | 0 | 10 | 0 | 10 |
| Blob full payload | 2,473,663 | 597,672 | 1,875,991 | 554,601 | 565,635 | 10 | 10 | 10,368 | 10 |
| Token two-branch covering | 4,763,296 | 720,060 | 4,043,236 | 1,737,657 | 1,143,332 | 0 | 52 | 0 | 50 |

The full blob payload costs 329,068 more instructions than metadata at the
same fixture cardinality, route, selectivity, index-entry count, and result-row
count. Store attribution accounts for 313,177 of that delta and the payload
performs ten row gets, giving Patch 2 a concrete projection/materialization
comparison to repeat. The token observation is a candidate absolute hotspot,
not yet a conclusion; it needs its two/nine/over-cap peers and repeated clean
evidence.

## Patch 2 Local Candidate Cohort

A complete local candidate cohort was captured against one exact shared Wasm
with PocketIC 15.0.0. All eight P1 shard receipts are complete: 1,787 of 1,787
scenarios succeeded with no failures. All eight scale receipts match their
expected hashes and contain 126 of 126 observations. The scale merge succeeded
with 504 normalized-cost records and 84 adjacent-cardinality slopes.

The exact candidate environment is:

| Field | Value |
| --- | --- |
| Method | `icydb-sql-performance/0.237/v1` |
| Source revision | `865a48dadcabdcf0dcd8b21864e6321c3b3f709b` |
| Source tree | `987f0dd4aaa71e0725c1b4de222f4db55f623d8a` |
| Source dirty | `true` |
| Raw Wasm | 4,576,629 bytes |
| Raw Wasm SHA-256 | `dec791d463bb12666711224c692f0a384b56848538d756655f6007c0d82fade9` |
| Scale report SHA-256 | `e1a7345d0767240d188302377883737d8ad77a2bab9f7394d56f6e31897d2793` |

The ignored local artifacts are under
`artifacts/perf-audit/0.237-patch2-local/`. They are useful candidate evidence,
but they are not a reviewed baseline because the source includes the uncommitted
Patch 1 profile changes and the surrounding preserved worktree.

### P1 Rediscovery Ranking

P1 fixture sizes differ by surface, so this ranking identifies candidates; it
does not compare per-row efficiency across surfaces.

| Scenario | Total | Compile | Planner | Store | Executor | Physical work |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Journaled full entity, materialized age order, limit 10 | 21,730,400 | 445,239 | 263,698 | 6,571,014 | 14,331,943 | 512 row gets |
| Heap full entity, materialized age order, limit 10 | 21,685,724 | 441,672 | 259,486 | 6,539,401 | 14,326,709 | 512 row gets |
| Heap wide name range, materialized age order, limit 10 | 19,984,794 | 509,617 | 299,161 | 18,699,588 | 406,124 | materialized scan |
| Journaled wide name range, materialized age order, limit 10 | 19,979,429 | 513,359 | 303,597 | 18,680,171 | 411,948 | materialized scan |
| Sparse 250-prefix token page, limit 50 | 19,954,834 | 2,386,612 | 7,272,412 | 2,859,178 | 7,279,213 | 53 index entries |

The storage-surface leaders split into two distinct owners: full-entity reads
are executor-heavy, while narrow and wide filtered materialized reads are
store-heavy. The sparse token leader is expensive before row materialization:
compile plus planner attribution is 9,659,024 instructions. These distinctions
prevent one aggregate ranking from being treated as one optimization target.

### Exact Scale Ranking

The strongest 256-to-2,048 slopes are current, deterministic candidates for
phase localization. Absolute instructions are shown before normalized cost.

| Family | 256 rows | 2,048 rows | Absolute delta | Delta per added row | Dominant observed growth |
| --- | ---: | ---: | ---: | ---: | --- |
| Hash grouped many-group `SUM`, window 16 | 20,063,181 | 138,907,742 | 118,844,561 | 66,319.5 | store +42,225,748; executor +76,630,269 |
| Scalar aggregate `DISTINCT`/`FILTER` | 14,193,207 | 109,324,049 | 95,130,842 | 53,086.4 | store +63,352,165; executor +31,785,827 |
| Scalar `COUNT(*)` | 12,453,216 | 96,891,236 | 84,438,020 | 47,119.4 | store +63,349,619; executor +21,089,348 |
| Heap full-entity materialized order | 11,144,590 | 82,503,326 | 71,358,736 | 39,820.7 | store +24,698,058; executor +46,658,599 |
| Unsupported computed order | 9,072,277 | 64,030,914 | 54,958,637 | 30,668.9 | store +54,966,182 |
| Wide secondary projection | 4,767,638 | 27,544,867 | 22,777,229 | 12,710.5 | store +22,786,795 |
| Over-cap token page materialization | 4,368,989 | 14,283,833 | 9,914,844 | 5,532.8 | store +6,558,441; executor +3,380,744 |
| Sparse token equality prefix | 18,592,486 | 18,850,793 | 258,307 | 144.1 | nearly fixed branch/planner work |
| Blob full payload, window 10 | 2,436,709 | 2,473,663 | 36,954 | 20.6 | fixed-window payload work |

The leading hash-grouped `HAVING SUM` peer is 139,168,830 instructions at
2,048 rows and has the same growth shape. It is not retained as a separate
long-ladder candidate because it would duplicate the plain `SUM` signal.

### Paired Controls At 2,048 Rows

| Comparison | Total instructions | Controlled physical-work observation |
| --- | ---: | --- |
| Many-group hash `SUM` / ordered `SUM` | 138,907,742 / 19,642,514 | hash scans 2,048 rows and 4,096 index entries; ordered stops after 358 rows, 384 entries, and 17 observed groups |
| Scalar `COUNT` / `SUM` / `DISTINCT`+`FILTER` | 96,891,236 / 98,266,855 / 109,324,049 | each performs 2,048 row gets; store attribution is approximately 71.843M in all three |
| Narrow / wide secondary projection | 10,734,193 / 27,544,867 | each reads 512 index entries; wide adds 512 row gets |
| Heap / journaled full-entity materialization | 82,503,326 / 82,542,318 | each performs 2,048 row gets; backend delta is 38,992 |
| Two-branch page-only / non-covered / full entity | 2,826,383 / 3,875,411 / 6,912,735 | 51-52 index entries; projections add 0 / 50 / 50 row gets |
| Two-branch / nine-branch / over-cap page-only | 2,826,383 / 4,954,083 / 14,283,833 | admitted paths read 51 entries; over-cap materialization reads 512 |
| Blob metadata / thumbnail / full payload | 2,144,595 / 2,329,007 / 2,473,663 | ten output rows; row gets are 0 / 10 / 10 and output blob bytes are 0 / 1,152 / 10,368 |

The grouped hash/ordered pair is not evidence of a 119M removable phase: the
ordered case performs materially less target-owned physical work because it
can stop after the requested ordered group window. Patch 3 must determine why
each maintained route owns its observed work before proposing any change.

## Long-Ladder Decision

No 8,192-row canary is added in Patch 2. The steep candidates are already
unambiguous over 16, 256, and 2,048 rows, while a fourth point would make the
largest single measurement hundreds of millions of instructions without
changing candidate order or identifying a removable owner. The blob and
admitted token cases are sufficiently flat that a longer ladder is also not
justified. If Patch 3 finds an unresolved nonlinear boundary, it must request
one narrowly named canary instead of broadening the profile speculatively.

## Strict Calibration Blocker

The strict P1 merge was invoked once with the current calibration input and
correctly stopped at P2 selection:

> P2 candidate selection failed: unclean P2 selection current subject:
> performance subject was measured from a dirty source worktree

This gate is not weakened or rerun. No merged P1 report, strict P2 selection,
repeated cold/warm cohort, calibration review, or comparable baseline is
claimed. A clean candidate containing Patch 1 must exist before those artifacts
can be produced honestly. Patch 2 therefore remains active at this handoff;
generic continuation resumes Patch 2 rather than starting Patch 3.

## Separate Release Blocker — ICYDB-011

Status: RESOLVED IN CURRENT CANDIDATE

Toko miner feedback identified that the release candidate's
`verify-tested-tree` check enumerated `git diff` paths and therefore ignored
untracked files. A relevant untracked Rust, build-script, schema, or fixture
file could consequently participate in validation without being present in the
candidate commit or release tag.

The canonical tested-tree gate now rejects every non-ignored untracked path
using Git's standard excludes. This covers both checks surrounding the full
validation workflow without adding a second path classifier. Ignored build
caches and local artifacts remain permitted; a new release note or source file
must be tracked before validation can begin.

The focused release-receipt fixture now proves that an untracked `build.rs`
fails the gate and an ignored local artifact does not. Existing tracked
unstaged, staged, transition, receipt, and post-commit checks remain covered.
ICYDB-011 is a separate release-tooling correction and does not change the SQL
performance method, runtime, or public behavior.

## Validation State

Patch 1's focused profile identity, complete contract/semantic strata, exact
scale declaration, and P2 sentinel-selection checks pass. Its shared SQL audit
Wasm build and smoke shard also pass. Patch 2 adds complete local P1 and scale
receipt/hash checks plus zero total-phase reconciliation residuals across all
126 scale observations. Strict P2/calibration validation remains pending for a
clean landed candidate; this document does not report it as passed. The focused
release-receipt fixture, shell validation, and release-workflow invariant suite
pass with the ICYDB-011 correction.
