# IcyDB 0.150 - Status

## Current State

Started.

0.150 starts from the 0.149 closeout: accepted row contracts now govern runtime
row decode, row emission, save validation, predicate/projection planning, and
session/SQL execution boundaries. Generated helpers that remain around row
materialization are named as generated/model-only or generated/test-only.

The next larger authority gap is index metadata. Accepted schema snapshots do
now persist field-path index contract metadata, and runtime visible-index
selection filters field-path planner bridge indexes through accepted contracts.
Access-choice scoring, fingerprints, and index key construction still consume
generated `IndexModel` data until those planner and executor contracts are
replaced. Expression indexes also remain generated-runtime authority until
their stable accepted expression contract is designed, so expression-index
visibility is preserved explicitly in this interim slice.

`0.150.2` adds the first planner-facing accepted field-path index DTO to
runtime `VisibleIndexes`. The generated `IndexModel` bridge remains inside that
DTO only because current access-path objects still store generated index
metadata; the accepted DTO is now the field-path contract the next planner
scoring and key-construction slices can consume directly. Runtime order-only
field-path access fallback now matches ORDER BY compatibility against those
accepted index terms, while generated order fallback is split into an explicit
model-only lane and expression-index order fallback remains an explicitly
deferred generated lane.

`0.150.3` moves runtime field-path cursor-anchor and forward/unique write index
key construction onto accepted `SchemaInfo` index contracts. Access paths and
the expression-index write lane still carry generated `IndexModel` DTOs, but
field-path cursor anchors and write keys now use accepted index ordinals,
accepted row-layout slots, accepted store paths, accepted uniqueness, and
accepted diagnostic field lists through the preflight read/materialization
boundary.

## Completed

- Added persisted accepted field-path index contracts to schema snapshots.
- Added schema proposal projection from generated field-path indexes to
  accepted `FieldId`, accepted row slot, field path, persisted field kind,
  nullability, uniqueness, ordinal, store path, and predicate SQL display
  metadata.
- Kept expression indexes out of accepted index proposals until canonical
  expression metadata lands.
- Hard-cut the schema snapshot codec to version `3` with accepted field-path
  index contract encoding.
- Added accepted snapshot integrity validation for duplicate index identities,
  empty index keys, and index key field slots that diverge from accepted row
  layout.
- Routed accepted `SchemaInfo` field index membership through persisted index
  contracts instead of generated `EntityModel` index metadata.
- Added a `SchemaInfo` field-path index metadata surface backed by accepted
  persisted index contracts, while keeping generated index projection isolated
  to generated/model-only schema views.
- `0.150.1`: routed runtime `VisibleIndexes` construction through accepted
  `SchemaInfo`, so store-ready session planning filters the temporary generated
  field-path planner bridge by accepted index contract names while preserving
  generated expression indexes until accepted expression contracts exist.
- `0.150.2`: added planner-facing accepted field-path index contracts to
  runtime `VisibleIndexes`, including accepted index identity, store,
  uniqueness, field paths, row-layout slots, and a temporary generated
  `IndexModel` bridge for current access-path DTO assembly.
- `0.150.2`: routed runtime order-only field-path access fallback through
  accepted planner index terms, kept generated field-path order fallback on an
  explicit generated/model-only path, and preserved generated expression-index
  order fallback as a deferred runtime lane.
- `0.150.3`: routed runtime field-path cursor-anchor index key construction
  through accepted `SchemaInfo` index contracts, using accepted index ordinals
  and accepted row-layout slots while preserving the generated expression-index
  anchor lane.
- `0.150.3`: narrowed the cursor-anchor authority call so it receives the
  selected index-range contract instead of a separate generated `IndexModel`
  argument.
- `0.150.3`: routed runtime forward/unique field-path write key construction
  through accepted `SchemaInfo` index contracts, using accepted index ordinals,
  accepted row-layout slots, accepted store paths, accepted uniqueness, and
  accepted diagnostic field lists while preserving the generated
  expression-index write-key lane.
- `0.150.3`: changed commit preflight's field-path forward-index planning gate
  to check accepted `SchemaInfo` index contracts instead of generated model
  index emptiness, while preserving generated expression indexes as the
  explicit deferred lane.
- `0.150.3`: split structural execution EXPLAIN finalization into accepted
  visible-index and explicit model-only lanes, removing the implicit fallback
  from missing `VisibleIndexes` to generated `model.indexes()`.
- `0.150.3`: added reduced lowered index scan facts so executor physical scan
  and covering component scan leaves consume index name/uniqueness facts after
  raw bounds are materialized instead of full generated `IndexModel`
  definitions.
- `0.150.3`: narrowed preflight index-reader ports so field-path write planning
  passes reduced accepted index contract facts instead of generated
  `IndexModel` definitions when decoding raw index-entry membership.

## Initial Targets

- Route planner access choice and query cache fingerprints through accepted
  index contracts.
- Route field-path index key construction through accepted `FieldId` plus
  validated accepted row-layout slots.
- Design expression index contracts separately around canonical expression
  metadata, deterministic derivation, planner eligibility, recovery parity, and
  EXPLAIN visibility.
- Route forward/reverse index key construction through accepted index metadata
  plus accepted row contracts once each contract family is represented.
- Decide whether standalone fluent planning and structural explain stay
  explicitly model-only or gain accepted-schema entrypoints.

## Out Of Scope Until Designed

- SQL DDL.
- General index migration/backfill policy.
- Field rename/remap through index contracts.
- Production fallback from missing accepted index metadata to generated
  `E::MODEL.indexes()`.

## Validation Queue

- Add index contract reconciliation tests.
- Add index fingerprint drift tests.
- Add planner visible-index tests against accepted metadata.
- Add index key construction tests that verify accepted slot authority.
- Extend write-boundary guards around runtime `E::MODEL.indexes()` and
  `IndexModel` usage.
- Add negative compile/search guards proving non-test runtime modules cannot
  call `EntityModel::indexes`, call `E::MODEL.indexes()`, or pass `IndexModel`
  into planner/executor key construction after accepted authority is available.

## Validation

- `cargo test -p icydb-core schema::proposal --features sql -- --nocapture`
  passed after field-path index proposal projection was added.
- `cargo test -p icydb-core persisted_schema_snapshot_round_trips_field_path_indexes --features sql -- --nocapture`
  passed after the schema snapshot codec started carrying accepted index
  contracts.
- `cargo test -p icydb-core accepted_schema_snapshot_try_new_rejects_invalid_index_contract --features sql -- --nocapture`
  passed after accepted index snapshot integrity validation was added.
- `cargo test -p icydb-core accepted_snapshot_schema_info_uses_persisted_index_membership --features sql -- --nocapture`
  passed after accepted `SchemaInfo` index membership moved to persisted index
  contracts.
- `cargo test -p icydb-core accepted_snapshot_schema_info_exposes_persisted_field_path_indexes --features sql -- --nocapture`
  passed after accepted `SchemaInfo` started exposing persisted field-path index
  metadata.
- `cargo test -p icydb-core --test write_boundary_guards accepted_schema_info_index_membership_uses_persisted_index_contracts -- --nocapture`
  passed after extending the accepted `SchemaInfo` index-membership and
  index-metadata guard.
- `cargo test -p icydb-core --test write_boundary_guards runtime_visible_indexes_are_accepted_schema_filtered -- --nocapture`
  passed for the `0.150.1` visible-index filtering slice and again after the
  `0.150.2` accepted planner index DTO and accepted order fallback were added.
- `cargo test -p icydb-core planner_order_only --features sql -- --nocapture`
  passed after runtime order-only field-path fallback started using accepted
  index order terms and generated order fallback was split into an explicit
  model-only lane.
- `cargo test -p icydb-core execute_sql_projection_expression_order --features sql -- --nocapture`,
  `cargo test -p icydb-core expression_index --features sql -- --nocapture`,
  `cargo test -p icydb-core filtered_expression --features sql -- --nocapture`,
  and `cargo test -p icydb-core filtered_composite_expression --features sql -- --nocapture`
  passed after preserving the generated expression-index order fallback lane.
- `cargo test -p icydb-core session_non_ready_secondary_indexes_are_hidden_from_planning_and_execution --features sql -- --nocapture`
  passed for the `0.150.1` visible-index filtering slice and again after the
  `0.150.2` accepted planner index DTO was added.
- `cargo test -p icydb-core --test write_boundary_guards forward_index_write_keys_use_accepted_row_contract_slots -- --nocapture`,
  `cargo test -p icydb-core load_index_range_cursor_anchor_matches_last_emitted_row_after_post_access_pipeline --features sql -- --nocapture`,
  and `cargo test -p icydb-core cursor_anchor --features sql -- --nocapture`
  passed after runtime field-path cursor-anchor key construction moved to
  accepted index contracts.
- `cargo test -p icydb-core save_update_with_changed_index_key_emits_remove_and_insert --features sql -- --nocapture`
  and `cargo test -p icydb-core unique_index_violation_rejected_on_update --features sql -- --nocapture`
  passed after runtime forward/unique field-path write key construction and
  preflight index-reader membership decoding moved to accepted index contract
  facts.
- `cargo check -p icydb-core --tests --features sql`,
  `cargo check --locked -p icydb-core --tests --features sql`,
  `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`,
  `cargo fmt --all`, and `git diff --check` passed after the `0.150.3`
  cursor-anchor, forward/unique write-key, and preflight index-reader contract
  slice.
- `cargo check --locked -p icydb-core --tests --features sql`,
  `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`,
  and `git diff --check` passed after the `0.150.2` accepted planner index DTO
  slice.
- `cargo test -p icydb-core schema --features sql -- --nocapture` passed after
  the accepted field-path index contract slice.
- `cargo check -p icydb-core --tests --features sql` passed after the accepted
  field-path index contract slice.
- `cargo check --locked -p icydb-core --tests --features sql` passed after the
  accepted field-path index contract slice.
- `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`
  passed after the accepted field-path index contract slice.
- `git diff --check` passed after the accepted field-path index contract
  slice.
