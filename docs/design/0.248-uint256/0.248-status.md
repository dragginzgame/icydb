# IcyDB 0.248 Native U256 Status

Status: PATCH 1 COMPLETE — BUILD RECOMMENDED; PRODUCTION IMPLEMENTATION NOT AUTHORIZED

Last updated: 2026-08-29

## Patch Ledger

| Patch | Canonical owner and bounded outcome | Delivery domains | Focused validation | Status |
| ---: | --- | --- | --- | --- |
| 1 | Measurement fixture and admission evidence comparing bounded `NatBig`, `NatBig` plus boundary adapters, and a disposable native `U256` integration | Integration Tests, Build / Canister, documentation | Fixture contract, clean Wasm A/B, layout, allocation, instruction, carrier and density evidence | Complete |
| 2 | Existing schema/value/persistence/planner owners gain one native `U256` storage and query contract | Parser, Lowering / Session, Executor / Planner, Build / Canister, Integration Tests | Focused scalar, codec, schema, query, Candid and Wasm gates | Not authorized |
| 3 | Existing expression and reducer owners gain checked `U256` arithmetic and `SUM` | Executor / Planner, Integration Tests | Arithmetic boundaries, reducer parity, allocation, instruction and Wasm gates | Not authorized |
| 4 | Downstream qualification and release closeout | Build / Canister, Integration Tests, documentation | Complete focused 0.248 matrix plus user-owned release validation | Not authorized |

The line contains four substantive landing patches. Patch 1 changes no
production IcyDB behavior and must leave no public type, tag, codec or runtime
variant behind. Patches 2–4 require the recorded Patch 1 build decision and
explicit user authorization.

Patch 1 recommends building native unsigned `U256` with Candid `nat`. The full
evidence, limitations and Patch 2 retention gates are recorded in
`patch-1-measurement.md`. `I256` remains outside the line.

## Patch 1 Measurement Contract

- Released predecessor: `v0.247.0`.
- Candidate arithmetic dependency: `ethnum 1.5.3`, default features disabled.
- Complete unsigned `NatBig` control: `max_bytes = 37` because unsigned LEB128
  carries seven value bits per byte.
- Bounded workload: 2,048 rows using the checked-in version-1 Ethereum fixture.
- Candidate carrier set: Candid `nat`, exactly validated 32-byte `blob`, and a
  structural four-`nat64` record.
- Raw non-gzipped Wasm and code-section bytes are authoritative; gzip is
  secondary context.
- A no-build result is valid and retains no production candidate behavior.

## Complexity Contract

Patch 1 adds no product behavior axis. Its fixture and evidence distinguish
three measurement controls, but only one or zero representations may proceed
to production. Existing schema, value, planner, executor, index and reducer
owners remain the required convergence points.
