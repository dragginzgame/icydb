# 0.62 Status Tracker

Current state: in progress after `0.62.6`; architecture phase is complete and
the current work is explicit-decoder organization plus measured tightening.

## Scope

This status tracks the `0.62` persisted-row structuralization line and the
follow-up wasm-surface tightening that remained after the runtime serde graph
was removed.

Design reference:

- `docs/design/0.62-structural-persisted-row-decode/0.62-design.md`

---

## Landed Through `0.62.6`

- [x] Persisted rows use one slot-based structural format only; legacy row
  versions were hard-cut.
- [x] `PersistedRow` derive/codegen is the canonical typed boundary for row
  write/materialization.
- [x] Commit/index planning no longer decodes `E` from row bytes.
- [x] Index key extraction, predicate evaluation, projection execution, and
  relation validation are structural/slot-driven in runtime paths.
- [x] Scalar leaf codec v1 is in place for the scalar field families that were
  moved off CBOR.
- [x] Scalar-only predicate compilation exists and dispatches once at the
  execution boundary instead of branching per node.
- [x] Scalar-only projection compilation exists for structural SQL projection
  execution.
- [x] Reverse-index and strong-relation runtime paths decode storage keys
  directly instead of round-tripping through `Value`.
- [x] Commit control envelopes and commit-marker envelopes are binary.
- [x] Commit-marker payload decode is binary and structural.
- [x] Runtime `serde_cbor::from_slice` is gone from persistence/runtime decode
  paths.
- [x] `structural_field` is no longer a monolith; it is split into directory
  modules with explicit ownership boundaries.
- [x] The old mixed `decode_structural_field_by_kind_bytes(...)` root was
  peeled into explicit scalar, leaf, and composite lanes.
- [x] Projection materialization/eval is no longer concentrated in two large
  files; structural SQL row loops and compiled scalar projection eval now have
  their own owner modules.
- [x] The latest kept `structural_field` wasm wins are owner-local:
  `leaf.rs` payload framing cleanup and `value_storage.rs` enum field-name
  matching.

---

## Current Architecture Boundary

Persistence:

- row container: slot-based structural format
- commit frame: binary
- commit payload: binary

Execution:

- hot scalar paths: `SlotReader -> scalar/native execution`
- generic fallback: explicit and isolated

Decode:

- scalar leaves: direct decode
- complex leaves: explicit structural decode in
  `crates/icydb-core/src/db/data/structural_field/`

Typed boundaries:

- entity reconstruction
- response/materialization boundaries that truly need typed values

---

## Current `structural_field` Module Layout

- `crates/icydb-core/src/db/data/structural_field/mod.rs`
  - public boundary and top-level lane router
- `crates/icydb-core/src/db/data/structural_field/cbor.rs`
  - low-level bounded CBOR parsing and raw-slice walkers
- `crates/icydb-core/src/db/data/structural_field/composite.rs`
  - recursive composite `ByKind` list/map/enum decode plus relation re-entry
- `crates/icydb-core/src/db/data/structural_field/scalar.rs`
  - scalar primitive fast path
- `crates/icydb-core/src/db/data/structural_field/leaf.rs`
  - typed wrapper and fixed-shape leaf payloads
- `crates/icydb-core/src/db/data/structural_field/storage_key.rs`
  - direct `StorageKey` and strong-relation decode
- `crates/icydb-core/src/db/data/structural_field/value_storage.rs`
  - externally tagged `Value` payload decode and shallow untyped fallback

This split is now the intended baseline. Follow-up work should preserve these
ownership lines unless there is a strong reason to change them.

---

## Current Projection Module Layout

- `crates/icydb-core/src/db/executor/projection/materialize/mod.rs`
  - shared materialization helpers, validation, grouped projection output, and
    typed test-facing row projection
- `crates/icydb-core/src/db/executor/projection/materialize/structural.rs`
  - structural SQL projection row loop and scalar-vs-generic structural
    dispatch
- `crates/icydb-core/src/db/executor/projection/eval/mod.rs`
  - generic slot-reader and grouped expression evaluation plus shared error
    taxonomy
- `crates/icydb-core/src/db/executor/projection/eval/scalar.rs`
  - compiled scalar projection expressions and scalar slot-reader execution

This split is also now part of the intended baseline. New work should tighten
these modules in place instead of letting `materialize/mod.rs` or
`eval/mod.rs` grow back into mixed-owner sinks.

---

## What Was Verified In The Current Checkpoint

- `cargo fmt --all`
- `cargo check -p icydb-core -p icydb`
- `cargo test -p icydb-core db::data::structural_field::tests -- --nocapture`
- `cargo test -p icydb-core db::executor::projection::tests -- --nocapture`
- `cargo build -p canister_one_simple --target wasm32-unknown-unknown`

---

## Current Retained Hotspots

Measured from `target/wasm32-unknown-unknown/debug/canister_one_simple.wasm`
using `twiggy` on the current retained baseline after the latest reverse-index
and SQL dispatch/lowering owner-local cuts.

Top local retained/shallow surfaces still worth attacking:

- `crates/icydb-core/src/db/sql/lowering/`
  - `bind_lowered_sql_select_query_structural`: `6148`
- `crates/icydb-core/src/db/session/sql.rs`
  - `execute_lowered_sql_dispatch_query`: `5304`
- `crates/icydb-core/src/db/executor/projection/materialize/`
  - `project_data_rows_from_projection_structural`: `6366`
- `crates/icydb-core/src/db/relation/reverse_index.rs`
  - `relation_target_keys_for_source_row`: `6517`
  - `prepare_reverse_relation_index_mutations_for_source_rows`: `3795`
- `crates/icydb-core/src/db/data/structural_field/`
  - `scalar::decode_scalar_fast_path_value`: `4802`
  - `value_storage::decode_value_enum_payload_bytes`: `4076`
  - `leaf::decode_leaf_field_by_kind_bytes`: `3818`

Important interpretation:

- the old generic runtime serde graph is gone
- remaining pressure is now explicit structural decode, projection
  materialization, SQL lowering/dispatch, and relation-key preparation, not
  hidden generic deserialization
- after `0.62.3`, readability and ownership are now a co-equal constraint with
  wasm movement; do not keep accidental ugliness solely because a local symbol
  shrank once

---

## Recent Kept Wins

- Shared CBOR array/map walkers replaced repeated container loops in the
  structural-field decoder.
- Tagged variant parsing was unified for schema enums and `Value` payloads.
- `ValueEnum` payload decode stopped building temporary owned strings before
  constructing `ValueEnum`.
- Untyped enum/shallow fallback dispatch was flattened by branching on CBOR
  major type directly.
- Projection/commit/unique row-key validation now shares one slot-owned
  storage-key validator instead of duplicating `Value -> StorageKey` round
  trips.
- `structural_field` was split into `cbor` / `scalar` / `composite` / `leaf` /
  `storage_key` / `value_storage`, with sibling modules importing from the
  owning boundary instead of reaching through a monolith.
- the old mixed `structural_field` root was peeled into:
  - scalar gate
  - leaf lane
  - composite recursive lane
- projection materialization was split into a `materialize/` directory module,
  with the structural SQL row loop moved into `materialize/structural.rs`
- compiled scalar projection evaluation was split into `eval/scalar.rs`, so
  `eval/mod.rs` now owns only the generic/grouped lane plus the shared error
  taxonomy
- `leaf.rs` dropped repeated top-level payload framing checks for typed wrapper
  leaves
- `value_storage.rs` replaced repeated `variant` / `path` / `payload` text
  comparisons in `decode_value_enum_payload_bytes(...)` with a direct
  encoded-field-name matcher
- `project_data_rows_from_projection_structural(...)` dropped the custom
  `StructuralProjectionPlan` enum in favor of `Option<Vec<ScalarProjectionExpr>>`
- `reverse_index.rs` no longer allocates a temporary `BTreeSet` just to walk
  `old_targets ∪ new_targets` during reverse-index mutation preparation
- `reverse_index.rs` inlined the one-use `StructuralSlotReader::from_raw_row(...)`
  wrapper out of `relation_target_keys_for_source_row(...)`
- `reverse_index.rs` now materializes the singular scalar relation fast-path
  result as `BTreeSet::from([key])` instead of building and inserting manually
- `session/sql.rs` now stays on borrowed lowered query shapes until a specific
  SELECT or DELETE binder actually needs ownership
- SQL session dispatch now calls the specialized lowered SELECT/DELETE binders
  directly instead of routing back through the mixed `LoweredSqlQuery` binder
- SQL select binding now hands its filter/order/page tail off to the shared
  base-query tail helper after the grouped/projection/HAVING phases are done

Measured local wins that were kept:

- `decode_value_enum_payload_bytes`: `4207 -> 4076`
- `decode_untyped_enum_payload_bytes`: `1017 -> 630`
- `decode_untyped_shallow_bytes`: `658 -> 304`
- `project_data_rows_from_projection_structural`: `7018 -> 6366`
- `leaf::decode_leaf_field_by_kind_bytes`: `4353 -> 3818`
- `prepare_reverse_relation_index_mutations_for_source_rows`: `4064 -> 3795`
- `execute_lowered_sql_dispatch_query`: `6900 -> 5304`
- `bind_lowered_sql_select_query_structural`: `6969 mixed root -> 6148 select binder`

---

## Reverted / Non-Paying Directions

- Broad map-helper unification in `structural_field.rs` regressed retained wasm
  and was reverted.
- Direct-loop rewrites for `Value::Map` payload decode regressed retained wasm
  and were reverted.
- Generic field-framing helper extraction in `value_storage.rs` regressed whole
  wasm and was reverted.
- Tail-assembly helper extraction in `value_storage.rs` regressed whole wasm
  and was reverted.
- Scalar-family helper dedup in `scalar.rs` regressed whole wasm and was
  reverted.
- Replacing the projection row loop cache with one `ProjectionSlotState` vector
  regressed whole wasm and was reverted.
- Extracting shared row-preflight helpers from the projection structural row
  loop regressed whole wasm and was reverted.
- Rewriting relation target-key bytes conversion as a manual `BTreeSet` loop
  regressed whole wasm and was reverted.
- Projection-wide `ProjectionField` branch deletion in `materialize/mod.rs`
  regressed whole wasm and was reverted.
- Small decode-surface cleanups that did not delete a real retained path were
  not kept.

Rule kept from these attempts:

- accept or reject by measured retained/runtime wasm movement, not by
  architectural neatness alone

Refinement after the `0.62.4` organization pass:

- do not re-enter symbol-golf mode if it cuts across the new module ownership
  boundaries
- prefer coherent local ownership first, then measure within that cleaned
  structure

---

## Next Recommended Slice

Keep working on the explicit decoder/materialization surface, not on new
representation changes.

Priority order:

1. `crates/icydb-core/src/db/relation/reverse_index.rs`
   - only keep further cuts if they beat the now-lower scalar+union baseline
   - the remaining reader path is still the largest local reverse-index owner
2. `crates/icydb-core/src/db/executor/projection/materialize/structural.rs`
   - keep reducing projection-side retained surface without reintroducing
     generic `Value` work on scalar-only rows
   - avoid helper extraction unless it deletes a real owner-local branch
3. `crates/icydb-core/src/db/sql/lowering/` and `crates/icydb-core/src/db/session/sql.rs`
   - keep using owner-local select/delete/session dispatch cuts, not broad
     helperization
4. `crates/icydb-core/src/db/data/structural_field/value_storage.rs`
   - continue only with narrow, measured cuts; the obvious helper seams are now
     exhausted
5. `crates/icydb-core/src/db/data/structural_field/scalar.rs`
   - only revisit if a concrete repeated scalar-family pattern is proven to pay

Do not restart another representation migration unless measurement proves the
current explicit decoder surface has plateaued.
