# 0.49 Status (as of 2026-03-11, pre-0.49.0 cut)

## 0.49 Scope

This status tracks `0.49` executor architecture stabilization work defined in:

- `docs/design/0.49-executor-architecture-stabilization/0.49-design.md`

`0.49` focuses on architecture hardening and observability consistency:

- executor layer boundary stabilization (`scan -> pipeline -> aggregate -> terminal`)
- continuation compatibility fail-closed behavior
- additive EXPLAIN execution metadata
- additive executor row-flow metrics

No new query-surface language features are in scope.

---

## Progress Summary

Completed:

- Added/locked executor layer authority guardrails:
  - `scripts/ci/check-layer-authority-invariants.sh`
  - `crates/icydb-core/src/db/executor/tests/load_structure.rs`
- Hardened continuation compatibility:
  - grouped cursor replay rejects grouped projection-width drift
    (`COUNT` vs `COUNT + MAX`) in
    `crates/icydb-core/src/db/executor/tests/cursor_validation.rs`
- Expanded EXPLAIN execution metadata in
  `crates/icydb-core/src/db/query/explain/render.rs`:
  - deterministic `node_id`
  - `execution_mode_detail`
  - `predicate_pushdown_mode`
  - `fast_path_selected`
  - `fast_path_reason`
- Added executor row-flow observability counters:
  - `rows_filtered`
  - `rows_aggregated`
  - `rows_emitted`
  - implemented in:
    - `crates/icydb-core/src/metrics/sink.rs`
    - `crates/icydb-core/src/metrics/state.rs`
    - `crates/icydb-core/src/db/executor/plan_metrics.rs`
    - executor finalization paths under
      `crates/icydb-core/src/db/executor/aggregate/load/grouped_output/finalize.rs`
- Added regression coverage for new metrics events and executor emission paths:
  - `crates/icydb-core/src/metrics/tests.rs`
  - `crates/icydb-core/src/db/executor/tests/metrics.rs`
- Added release documentation:
  - root `CHANGELOG.md` now includes `0.49.x`
  - `docs/changelog/0.49.md` includes `0.49.0` detailed notes
- Added contract/docs updates for new metrics vocabulary:
  - `docs/contracts/RESOURCE_MODEL.md`
  - `docs/contracts/CURSOR.md`
  - `docs/governance/code-hygiene.md`

---

## Roadmap Snapshot

- [x] A. finalize executor layer model and forbid legacy `load` fallback paths
- [x] B. lock continuation compatibility fail-closed behavior for grouped drift
- [x] C. expand EXPLAIN execution metadata for layer/fast-path/pushdown visibility
- [x] D. add row-flow metrics (`rows_filtered`, `rows_aggregated`, `rows_emitted`)
- [x] E. align release docs/changelog for `0.49.0`
- [ ] F. run full release gate and cut `0.49.0`

Current state: **implementation complete, release cutover pending final gate**.

---

## Verification Readout

Executed and passing:

- `make check-invariants`
- `cargo check -p icydb-core --tests`
- `cargo test -p icydb-core db::query::explain::tests:: -- --nocapture`
- `cargo test -p icydb-core session_load_explain_execution_ -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::load_structure -- --nocapture`
- `cargo test -p icydb-core db::executor::stream::access::tests -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::cursor_validation -- --nocapture`
- `cargo test -p icydb-core compile_fail -- --nocapture`
- `cargo test -p icydb-core metrics::sink::tests -- --nocapture`
- `cargo test -p icydb-core metrics::tests -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::metrics -- --nocapture`

Not yet executed in this closeout pass:

- `make fmt-check`
- `make clippy`
- `make check`
- `make test`

---

## Cutover Checklist (`0.49.0`)

- [x] design doc present and updated
- [x] status tracker present
- [x] changelog root minor-line entry present (`0.49.x`)
- [x] detailed changelog file present (`docs/changelog/0.49.md`)
- [ ] full workspace release gate re-run
- [ ] final version/tag cut via release flow

