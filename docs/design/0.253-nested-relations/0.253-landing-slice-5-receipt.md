# IcyDB 0.253 Landing Slice 5 Closeout Receipt

- **Status:** Closed with downstream Toko adoption explicitly deferred
- **Date:** 2026-09-05
- **IcyDB outcome:** The nested-relation capability and maintained contracts are
  complete and ready for user-owned release validation.
- **Downstream outcome:** No Toko code or measurement is claimed. The available
  checkout was stale, so the current default branch must be re-audited under
  [dragginzgame/toko#1703](https://github.com/dragginzgame/toko/issues/1703).

## Release Boundary

The external deferral does not weaken the IcyDB contract. Direct and nested
strong relations share one accepted authority, target-key representation,
commit budget, reverse flow, activation flow, integrity flow, and recovery
flow. Historical, conditional, external, cross-canister, and cross-shard
identifiers do not gain a second relation mode.

The following planned downstream evidence is deliberately absent rather than
inferred from stale source:

- Toko's final strong/historical/conditional/external classification ledger;
- duplicate application-validation deletion;
- `project_instance` raw/code Wasm comparison; and
- Toko cold/warm, activation, target-delete, and typical-call instructions.

Issue #1703 requires the future Toko implementation to repeat the semantic
classification against maintained source, preserve application-only policy and
pre-side-effect guards, and run the original downstream size/performance gates.

## IcyDB Closeout Evidence

The final controlled production artifacts remain:

| Actor | Raw bytes | Code bytes | Data bytes | Defined functions | Gzip bytes |
| --- | ---: | ---: | ---: | ---: | ---: |
| No relation | 2,092,385 | 1,968,965 | 113,833 | 5,419 | 826,048 |
| Direct relation | 2,092,488 | 1,968,965 | 113,936 | 5,419 | 825,701 |
| Shallow nested | 2,096,592 | 1,972,212 | 114,752 | 5,428 | 826,781 |
| Repeated nested | 2,099,670 | 1,975,318 | 114,724 | 5,437 | 827,873 |

The cumulative no-relation control is 11,441 raw bytes and 11,564 code bytes
above Slice 2, inside both 12 KiB hard gates. The repeated actor is 7,182 raw
bytes and 6,353 code bytes above the direct control, inside both 32 KiB gates.
Function counts remain attribution signals rather than pre-1.0 landing limits.

The maximum 2,730-row replacement commit completes in 5,444,445,467
instructions and retains 24,555,554,533 instructions below the admitted
30-billion mutation ceiling. Exact limits commit, first over-limit units reject
before mutation, and the recorded 1.266% target-restriction plus 1.098%
same-batch-deletion movements remain accepted small target misses.

Focused core/model/schema linting and relation tests, including the controlled
PocketIC measurement contract, pass. The repository-wide validation and
release workflow remains user-owned.

## Complexity

The IcyDB implementation touches one converged relation flow rather than
generating per-shape executors or adding relation modes. The cumulative
worktree at closeout is approximately 3,400 added and 585 removed lines across
54 code, fixture, test, measurement, and maintained-contract paths. The
implementation shape is more capable but remains authority-neutral: one
interpreter and one lifecycle flow own both direct and nested relations.
