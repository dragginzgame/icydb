# IcyDB 0.253 Landing Slice 3 Receipt

- **Date:** 2026-09-05
- **Status:** Complete
- **Outcome:** Single-valued record-member, optional, named-wrapper, and enum
  payload relations now share one accepted path and one runtime interpreter.

## Implementation

- Proposal discovery emits one bounded rooted program with explicit optional
  and named transitions. Repeated traversal remains excluded for Slice 4.
- Accepted snapshots persist only the stable nominal/member/variant identities
  required by the rooted path. Binding derives exact input, output, and terminal
  contracts from the accepted catalogs instead of caching a second copy.
- Initial publication, reconciliation, activation, migration, integrity,
  recovery, runtime binding, commit projection, reverse maintenance, target
  restriction, source deletion, replacement, and batch overlays consume that
  same accepted source. Direct relations keep their existing fast path.
- Recursive structural edges without a finite relation path terminate cleanly;
  no compatibility decoder, legacy fallback, or second traversal route exists.

## Size And Cost Evidence

| Actor | Compiler Wasm | Final raw | Code | Data | Functions | `call_indirect` | Exports | Candid | Gzip |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| No relation | 2,390,944 | 2,089,930 | 1,966,522 | 113,833 | 5,388 | 596 | 8 | 829 | 824,493 |
| Direct relation | 2,391,072 | 2,090,033 | 1,966,522 | 113,944 | 5,388 | 596 | 8 | 829 | 824,622 |
| Shallow nested | 2,395,847 | 2,094,136 | 1,969,768 | 114,734 | 5,397 | 596 | 9 | 900 | 826,571 |

The shallow actor adds 4,103 raw bytes, 3,246 code-section bytes, and 9 defined
functions over the current direct control, passing its 32 KiB raw/code gate;
the function movement is reported. The no-relation and direct controls remain
byte-identical in code shape,
but the no-relation control adds 8,986 raw bytes, 9,121 code bytes, and 15
functions over its Slice 2 predecessor. Hard-cutting duplicated persisted
contracts and converging active/candidate relation binding recovered 8,750 raw
bytes, 8,617 code bytes, and 8 functions from the initial Slice 3
implementation. The remainder exceeds the isolated 2 KiB/2 KiB/8-function
forecast but passes the authority-preserving 12 KiB/12 KiB common byte gate
accepted from the measured evidence on 2026-09-05. Function movement remains
reported for attribution.

The six controlled shallow phases use 8,854,198; 6,779,245; 18,382,120;
8,423,102; 5,427,432; and 8,496,045 instructions. The largest phase remains far
below both the 30-billion mutation budget and 40-billion IC message ceiling.

## Complexity Evidence

The cumulative Slice 2-3 worktree touches 34 files and is approximately 2,201
lines added and 433 removed. The capability adds one accepted source variant
and one interpreter, so it is more complex than Slice 2, but the final hard cut
removes the parallel cached-contract representation, its repeated checks, and
the duplicate active/candidate binding flow.

## Validation Boundary

Strict linting and focused schema, model, core, codec, activation, migration,
integrity, recovery, reverse-domain, and real-canister shallow-flow checks pass.
Full repository/workspace validation remains user-owned. Slice 4 stays closed
as the next separate landing-slice handoff.
