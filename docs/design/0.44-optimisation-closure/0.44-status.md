# 0.44 Status (as of 2026-03-07, Slices A-F landed; grouped fast-path scope explicitly deferred; post-0.44.1 BYTES stream follow-up landed)

## 0.44 Scope

This tracker covers optimization-closure work defined in:

- `docs/design/0.44-optimisation-closure/0.44-design.md`

The objective is to close existing optimization surfaces without adding new
query semantics.

---

## In Scope (`0.44.x`)

- COUNT closure for safe PK-shaped fast paths first, then guarded secondary
  shapes
- BYTES fast-path completion and diagnostics closure
- LIMIT pushdown consistency closure
- covering projection closure
- grouped aggregate bounded fast-path closure
- explain/trace visibility and parity/safety regression closure

## Out of Scope (`0.44.x`)

- new query terminals or planner semantic families
- new access-path variants
- mutable EXPLAIN DTO construction patterns
- broad planner architecture redesign

---

## Roadmap Snapshot (`0.44.x`)

- [x] A. design doc normalized and reformatted
- [x] B. design sanity-check pass completed
- [x] C. release status tracker established
- [x] D. COUNT phase-1 safe PK fast paths
- [x] F. LIMIT pushdown consistency closure
- [x] E. BYTES fast-path and diagnostics closure
- [x] G. covering projection closure
- [x] H. grouped aggregate bounded fast-path closure (deferred post-`0.44`)
- [x] I. final parity/safety/cursor matrix completion

Current `0.44.x` status: **ready for release cut** (planning/tracking complete;
Slices A-F implemented; grouped bounded fast-path expansion explicitly deferred;
final parity/safety matrix sweep completed with clean fmt/clippy/test gates).
Execution order guidance: `A -> C -> B -> D -> E -> F`.

---

## Closure Tracker

| Workstream | State | Owner Boundary | Next Gate |
|---|---|---|---|
| COUNT Phase 1 (`COUNT(*)`, `COUNT(pk)` on PK-safe shapes) | Complete | Executor | landed in `aggregate::terminals` with parity + scan-accounting tests |
| COUNT Phase 2 (secondary prefix/range guarded closure) | Complete | Route + Access + Executor | route streams strict-compatible secondary COUNT via `ExistingRows`, keeps strict-uncertain residual shapes materialized, and exposes COUNT fold-mode metadata in aggregate EXPLAIN descriptors |
| BYTES fast-path closure | Complete | Executor | PK-safe full-scan/key-range and unordered secondary-stream `bytes()` fast paths landed with explicit parity + eligible-vs-fallback hit-marker coverage and deterministic thread-local diagnostics |
| LIMIT pushdown closure | Complete | Route + Stream Access | ordered-shape planner-gated eligibility, incompatible-order rejection, residual-cap/underfill behavior, and cursor-boundary matrix coverage are in place |
| Covering projection closure | Complete | Route + Executor | index + constant covering projection fast-path diagnostics landed with eligibility/fallback regression coverage; aggregate covering EXPLAIN coverage remains explicit for `EXISTS`/`COUNT` |
| ExecutionTrace optimization taxonomy closure | Complete | Diagnostics + Load Executor | base + top-N variants are explicit (`PrimaryKey`, `PrimaryKeyTopNSeek`, `SecondaryOrderPushdown`, `SecondaryOrderTopNSeek`, `IndexRangeLimitPushdown`) with regression coverage for fast-path and fallback outcomes |
| Grouped aggregate bounded fast paths | Deferred | Route + Grouped Executor | explicit post-`0.44` defer to preserve route complexity guardrails and require a separate grouped safety proof |
| Explain/trace closure for all landed optimizations | Complete | Explain + Diagnostics | all landed optimization tracks now have explain/trace or terminal-level diagnostics coverage; projection node-family EXPLAIN expansion is explicitly deferred post-`0.44` |

---

## Audit-Discovered Additional Surfaces

These were found by codebase scan and normalized into the 0.44 inventory.

| Surface | Owner | State | Resolution Target | Notes |
|---|---|---|---|---|
| COUNT PK scan fast path is still narrow (`ByKey`-only terminal gate) | `aggregate::terminals` | Complete | `0.44` | expanded safe PK coverage for `FullScan`/`KeyRange` |
| COUNT PK direct-access stream fast path for `ByKeys` ordered windows | `aggregate::terminals` | Complete | `0.44.3` | now covers unordered and PK-ordered `ByKey`/`ByKeys` windows with direction-aware stream folding and parity/hit-marker coverage |
| COUNT secondary covering behavior is split between route materialization and terminal fast-path gates | `route::planner` + `aggregate::terminals` | Complete | `0.44` | route COUNT streams strict-compatible index-prefix/index-range with `ExistingRows`, keeps strict-uncertain residual shapes materialized, and surfaces fold-mode diagnostics via aggregate EXPLAIN metadata |
| COUNT index-range limit pushdown is explicitly gated off | `route::planner::feasibility` | Complete | post-`0.44.0` | route COUNT index-range shapes now inherit bounded pushdown fetch specs, with stale-leading `MissingRowPolicy::Ignore` parity coverage and bounded scan-accounting assertions |
| EXISTS covering fast path remains intentionally narrow | `aggregate::terminals` | Complete | post-`0.44.0` | strict-compatible secondary predicate shapes now use covering EXISTS fast path; strict-uncertain and ordered shapes remain explicit fallback |
| BYTES terminal still uses materialized execute + per-row read path | `load::terminal` | Complete | `0.44` | safe PK full-scan/key-range and unordered secondary stream-window `bytes()` fast paths are landed; ordered/residual/distinct shapes remain explicitly deferred to canonical fallback |
| BYTES PK direct-access stream fast path for ordered `ByKeys` windows | `load::terminal` | Complete | `0.44.3` | now covers unordered and PK-ordered `ByKey`/`ByKeys` windows with direction-aware stream traversal and parity/hit-marker coverage |
| Covering projection explain visibility is not fully surfaced | `aggregate::projection` + `executor::explain::descriptor` | Deferred | post-`0.44` | aggregate EXISTS/COUNT descriptors now surface eligible index-only covering scan; projection node-family explain surface remains intentionally deferred |
| ExecutionTrace optimization taxonomy omits several aggregate/terminal optimizations | `diagnostics::execution_trace` | Deferred | post-`0.44` | execution trace is intentionally load-path scoped; aggregate terminals use explain + terminal-local diagnostics in `0.44` |
| Grouped aggregates are still materialized-only at route boundary | `route::planner` + `load::grouped_*` | Deferred | post-`0.44` | grouped fast-path expansion is intentionally deferred to preserve current grouped route complexity envelope |
| Grouped field-target DISTINCT/HAVING expansion remains release-deferred | `query::plan::validate` + grouped fold runtime | Deferred | post-`0.44` | keep explicit defer status to avoid hidden drift |

---

## Sanity Readout (Completed in this pass)

- design terminology aligned to current execution contracts (`ExecutionPathKind`
  names and layer authority boundaries)
- scope confirms no new query-surface features in `0.44`
- explain DTO immutability constraint preserved in design guidance
- completion criteria now explicit so no optimization track remains ambiguous

---

## No Half-Finished Flow Policy (0.44)

For each optimization track above, closure requires one of:

- shipped with tests and diagnostics, or
- explicitly deferred with rationale and ownership

No track may remain implicitly partial at release cut time.
No optimization row tracked as `Open` may remain unresolved at release close.

---

## Completed in This Pass (Slice A)

- Added safe PK-cardinality COUNT fast path for single-path
  `ExecutionPathKind::FullScan` and `ExecutionPathKind::KeyRange` shapes in
  `aggregate::terminals`.
- Preserved canonical COUNT page-window semantics and scan accounting
  (`offset`/`limit`) via explicit window projection in the new fast path.
- Scoped fast path to non-distinct, no-residual-predicate shapes to preserve
  existing DISTINCT+offset bounded-probe suppression behavior.
- Validation:
  - `cargo test -p icydb-core aggregate_count_`
  - `/bin/bash -lc "CARGO_TARGET_DIR=/tmp/icydb-target cargo clippy --workspace --all-targets -- -D warnings"`

## Completed in This Pass (Slice A - Secondary Route Alignment)

- Extended route COUNT pushdown eligibility to include secondary
  index-prefix/index-range shapes under strict prefilter compatibility.
- Route COUNT execution now selects `AggregateFoldMode::ExistingRows` for those
  secondary shapes, while PK-safe shapes remain `AggregateFoldMode::KeysOnly`.
- Added route regression coverage for:
  - secondary COUNT streaming with `ExistingRows`
  - secondary COUNT strict-predicate streaming
  - secondary COUNT strict-uncertainty materialization fallback
  - boundary matrix updates for secondary/index-range COUNT routing modes
- Validation:
  - `cargo test -p icydb-core route_matrix_aggregate_count_ -- --nocapture`
  - `cargo test -p icydb-core count_pushdown -- --nocapture`
  - `cargo test -p icydb-core route_capabilities_ -- --nocapture`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

## Completed in This Pass (Slice A - Secondary Route Diagnostics)

- Aggregate COUNT EXPLAIN descriptors now expose route-selected fold behavior
  through `node_properties.count_fold_mode`:
  - `keys_only` for PK-safe COUNT shapes
  - `existing_rows` for secondary stale-key-safe COUNT shapes
- Added descriptor regression coverage that verifies:
  - non-COUNT descriptors omit `count_fold_mode`
  - PK COUNT descriptors emit `keys_only`
  - secondary COUNT descriptors (including strict-compatible and
    strict-uncertain shapes) emit `existing_rows`
- Validation:
  - `cargo test -p icydb-core aggregate_terminal_execution_descriptor_reports_covering_scan_for_index_covering_shapes`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

## Completed in This Pass (Slice A - PK COUNT Diagnostics)

- Added explicit PK-cardinality COUNT fast-path hit diagnostics
  (`take_pk_cardinality_count_fast_path_hits_for_tests`) so safe PK COUNT
  routing is observable in regression tests.
- Added regression coverage that verifies:
  - eligible PK full-scan COUNT windows emit a fast-path hit
  - ineligible by-id COUNT shapes stay on fallback and emit no hit
- Validation:
  - `cargo test -p icydb-core aggregate_count_pk_cardinality_fast_path_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

## Completed in This Pass (Slice B - Partial)

- Added safe PK full-scan/key-range `bytes()` fast path in `load::terminal`
  for non-distinct, no-residual-predicate, PK-order/no-order shapes.
- Fast path now folds persisted row payload lengths directly from store-window
  traversal, preserving `offset`/`limit` page-window semantics.
- Added explicit BYTES fast-path hit diagnostics (`take_bytes_pk_fast_path_hits_for_tests`)
  plus regression coverage that distinguishes eligible PK full-scan shapes
  from ineligible by-id fallback shapes.
- Validation:
  - `cargo test -p icydb-core aggregate_bytes_`
  - `cargo test -p icydb-core session_load_bytes`
  - `cargo test -p icydb-core aggregate_bytes_pk_fast_path_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
  - `/bin/bash -lc "CARGO_TARGET_DIR=/tmp/icydb-target cargo clippy --workspace --all-targets -- -D warnings"`

## Completed in This Pass (Slice B - Closure Hardening)

- Added key-range-specific BYTES fast-path hit-marker regression coverage:
  - eligible key-range shape emits one hit marker
  - residual-predicate key-range shape stays on fallback and emits zero hits
- Converted BYTES/COUNT/secondary covering test-only fast-path counters to
  thread-local diagnostics to eliminate cross-test interference under parallel
  execution.
- Validation:
  - `cargo test -p icydb-core aggregate_bytes_key_range_fast_path_emits_hit_marker_only_without_residual_predicates -- --nocapture`
  - `cargo test -p icydb-core aggregate_bytes_ -- --nocapture`
  - `cargo test -p icydb-core fast_path_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

## Completed in This Pass (Slice C - Partial)

- Tightened route capability gating for index-range LIMIT pushdown so ordered
  shapes require planner logical pushdown eligibility in addition to access
  shape/order compatibility.
- Added route capability regression coverage:
  - ordered index-range remains eligible
  - no-order index-range remains eligible
  - empty-order planner-bypass shape is rejected
  - incompatible ordered index-range shape is rejected
- Validation:
  - `cargo test -p icydb-core route_capabilities_index_range_`
  - `cargo test -p icydb-core route_matrix_load_index_range_`
  - `cargo test -p icydb-core route_matrix_load_index_range_incompatible_order_disables_limit_pushdown -- --nocapture`
  - `/bin/bash -lc "CARGO_TARGET_DIR=/tmp/icydb-target cargo clippy --workspace --all-targets -- -D warnings"`

## Completed in This Pass (Slice D - Partial)

- Aggregate terminal EXPLAIN descriptor assembly now projects
  `covering_projection=true` for eligible index-only `EXISTS`/`COUNT` shapes
  using terminal fast-path eligibility-aligned gates.
- Added aggregate descriptor regression coverage for:
  - eligible explicit index-prefix `EXISTS`/`COUNT` shape -> covering projection on
  - ordered filtered `EXISTS`/`COUNT` shape -> covering projection off
  - strict-compatible filtered secondary `COUNT` -> covering projection on
  - strict-uncertain filtered secondary `COUNT` -> covering projection off
- Validation:
  - `cargo test -p icydb-core aggregate_terminal_execution_descriptor_reports_covering_scan_for_index_covering_shapes`
  - `cargo test -p icydb-core session_load_terminal_explain_reports_standard_route_for_exists`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

## Completed in This Pass (Slice D - Trace Taxonomy)

- Extended `ExecutionOptimization` with explicit top-N-assisted fast-path
  labels:
  - `PrimaryKeyTopNSeek`
  - `SecondaryOrderTopNSeek`
- Updated load fast-path outcome finalization to decorate optimization labels
  from route `top_n_seek_spec` metadata so trace output distinguishes base
  pushdown from top-N-assisted pushdown.
- Added regression coverage for both categories:
  - PK top-N trace expectations
  - secondary-order top-N trace expectations
- Validation:
  - `cargo test -p icydb-core load_secondary_order_top_n_seek_trace_optimization_is_explicit -- --nocapture`
  - `cargo test -p icydb-core pagination:: -- --nocapture`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

## Completed in This Pass (Slice D - Covering Projection Diagnostics)

- Added explicit covering projection fast-path hit diagnostics in
  `aggregate::projection`:
  - `take_covering_index_projection_fast_path_hits_for_tests`
  - `take_covering_constant_projection_fast_path_hits_for_tests`
- Added regression coverage that verifies eligible vs ineligible hit-marker
  behavior for:
  - constant covering projection shapes
  - index covering projection shapes
- Validation:
  - `cargo test -p icydb-core aggregate_field_target_covering_constant_projection_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
  - `cargo test -p icydb-core aggregate_field_target_covering_index_projection_emits_hit_marker_only_for_eligible_shapes -- --nocapture`
  - `cargo test -p icydb-core aggregate_field_target_covering_ -- --nocapture`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

## Completed in This Pass (Slice F - LIMIT Pushdown Closure)

- Confirmed planner-gated ordered index-range pushdown eligibility boundaries:
  - ordered compatible shapes: eligible
  - ordered incompatible shapes: rejected
  - planner-bypass empty-order shapes: rejected
- Confirmed residual-cap and retry behavior under pagination traces:
  - bounded residual windows retain pushdown
  - underfill retry disables pushdown and falls back safely
  - cursor boundary behavior remains deterministic
- Validation:
  - `cargo test -p icydb-core route_capabilities_index_range_ -- --nocapture`
  - `cargo test -p icydb-core route_matrix_load_index_range_ -- --nocapture`
  - `cargo test -p icydb-core load_index_range_limit_pushdown_ -- --nocapture`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

## Completed in This Pass (Slice F - ExecutionTrace Taxonomy Closure)

- Added non-top-N optimization-label regressions for load traces:
  - PK ordered unpaged shape -> `ExecutionOptimization::PrimaryKey`
  - secondary ordered unpaged shape -> `ExecutionOptimization::SecondaryOrderPushdown`
- Existing coverage already locks top-N and index-range variants:
  - `PrimaryKeyTopNSeek`
  - `SecondaryOrderTopNSeek`
  - `IndexRangeLimitPushdown`
- Validation:
  - `cargo test -p icydb-core load_secondary_order_top_n_seek_trace_optimization_is_explicit -- --nocapture`
  - `cargo test -p icydb-core non_top_n_variant_without_page_limit -- --nocapture`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

## Completed in This Pass (Slice I - Final Parity/Safety Gate)

- Closed the final release-readiness gate with no remaining `0.44` open
  optimization rows and no unresolved fast-path parity regressions.
- Kept load-module structural boundaries intact by routing BYTES PK window
  traversal through executor context adapters (no direct store traversal in
  `load` leaf modules), preserving the structural guard contract.
- Validation:
  - `cargo fmt --all`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `cargo test --workspace`
  - `cargo test -p icydb-core`
  - `cargo test -p icydb-core load_module_has_no_direct_store_traversal -- --nocapture`

## Completed in This Pass (Post-0.44.0 Follow-Up - EXISTS Covering Expansion)

- Expanded secondary covering `EXISTS` fast-path eligibility to include
  strict-compatible predicate shapes (matching strict-predicate COUNT routing
  behavior), while keeping strict-uncertain and ordered shapes on explicit
  fallback.
- Extended the covering EXISTS stream descriptor path to pass strict
  index-predicate execution programs so strict-compatible prefilter semantics
  remain executor-owned and explicit.
- Aligned aggregate EXPLAIN covering-projection eligibility for `EXISTS` with
  the updated strict-compatible predicate gate.
- Added regression coverage for:
  - strict-compatible EXISTS hit-marker eligibility
  - strict-uncertain EXISTS fallback hit-marker suppression
  - strict-compatible/strict-uncertain EXISTS descriptor covering flags
- Validation:
  - `cargo fmt --all`
  - `cargo test -p icydb-core aggregate_exists_secondary_index_covering_fast_path_ -- --nocapture`
  - `cargo test -p icydb-core aggregate_terminal_execution_descriptor_reports_covering_scan_for_index_covering_shapes -- --nocapture`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`

## Completed in This Pass (Post-0.44.0 Follow-Up - COUNT Index-Range LIMIT Pushdown)

- Removed the route feasibility gate that blocked COUNT terminals from deriving
  index-range `LIMIT` pushdown fetch specs.
- COUNT aggregate route execution now accepts route-derived index-range fetch
  specs in streaming mode for eligible index-range shapes.
- Added stale-leading `MissingRowPolicy::Ignore` regression coverage that
  asserts:
  - COUNT index-range pushdown parity matches canonical `execute().count()`
  - bounded pushdown scan accounting stays aligned to the derived fetch window
    (`offset + limit`)
- Validation:
  - `cargo fmt --all`
  - `cargo test -p icydb-core route_matrix_aggregate_count_pushdown_boundary_matrix -- --nocapture`
  - `cargo test -p icydb-core aggregate_count_index_range_limit_pushdown_missing_ok_stale_preserves_parity -- --nocapture`
  - `cargo test -p icydb-core aggregate_count_ -- --nocapture`
  - `cargo test -p icydb-core route_capabilities_index_range_ -- --nocapture`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
  - `cargo test -p icydb-core`

## Completed in This Pass (Post-0.44.1 Follow-Up - BYTES Unordered Secondary Stream Fast Path)

- Extended scalar `bytes()` executor fast-path coverage to eligible unordered
  secondary stream shapes (`no predicate`, `no distinct`, `no explicit order`)
  by folding persisted payload lengths directly from ordered key-stream
  traversal.
- Added context-level ordered key-stream payload-byte folding that preserves
  missing-row safety semantics:
  - `MissingRowPolicy::Error` -> strict read/fail closed
  - `MissingRowPolicy::Ignore` -> stale-key skip behavior
- Kept ordered secondary shapes and predicate/distinct-sensitive shapes on
  canonical materialized fallback.
- Added stream fast-path diagnostics
  (`take_bytes_stream_fast_path_hits_for_tests`) and parity regression
  coverage for:
  - unordered secondary stream-window parity + hit-marker emission
  - ordered secondary bypass (zero stream hit markers)
- Validation:
  - `cargo fmt --all`
  - `cargo test -p icydb-core aggregate_bytes_ -- --nocapture`
  - `cargo test -p icydb-core session_load_bytes -- --nocapture`
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`
