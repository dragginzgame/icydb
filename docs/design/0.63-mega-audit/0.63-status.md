# 0.63 Status Tracker

Current state: in progress. `0.63` is landing as a bounded consolidation line
across query/session boundaries, projection/materialization ownership, and
mutation/query-plan authority cleanup.

## Scope

This status tracks the `0.63` mega-audit line defined in:

- `docs/design/0.63-mega-audit/0.63-design.md`

The goal remains consolidation, not feature expansion:

- collapse same-layer duplication
- keep one owner per authority boundary
- preserve the current wasm/runtime baseline while reducing drift

---

## Landed Audit Slices

- [x] Session query-execution error wrappers are centralized under the
  `db::session` boundary instead of being rebuilt in local query/SQL paths.
- [x] Reverse-index membership deltas and commit-window counter increments now
  flow through one `PreparedIndexDeltaKind` authority.
- [x] Commit-window metrics emission now shares one `PreparedRowOpDelta`
  translation path rather than rebuilding event payloads in parallel save/delete
  wrappers.
- [x] Projection/materialization now shares one prepared projection-plan owner
  across typed projection, slot-row validation, and structural SQL projection.
- [x] Compiled scalar projection evaluation now has one shared core instead of
  split structural-vs-typed recursive evaluators.
- [x] Query-origin unsupported execution construction is now owned by
  `InternalError` / `QueryError` rather than ad-hoc local wrappers.
- [x] Aggregate target field resolution now has one planner/query validation
  owner used by both fluent aggregate terminals and SQL aggregate dispatch.
- [x] Grouped aggregate target-field validation now has one validation helper
  shared by grouped structure validation and grouped global-DISTINCT policy
  checks.
- [x] Cursor invariant reasons now stay raw inside `CursorPlanError`, with the
  canonical executor-invariant prefix applied once at cursor-to-internal error
  conversion boundaries; session paged-query routing also consumes the same
  cursor-owned ordering-contract message.
- [x] SQL query-lane dispatch now uses shared session-owned helpers for
  query-lane unsupported routing, prepared-statement lowering/binding,
  lowered query/explain dispatch, grouped/scalar intent gating, and grouped
  scalar-surface intent rejection instead of repeating local lane/intention
  mappings.
- [x] Planner expression type inference now resolves schema field kinds through
  one local helper instead of repeating the same unknown-field error mapping
  across scalar field and aggregate target inference paths.

---

## Current Authority Baseline

Session/query error mapping:

- `crates/icydb-core/src/db/session/mod.rs`
- `crates/icydb-core/src/db/query/intent/errors/mod.rs`

Projection/materialization:

- `crates/icydb-core/src/db/executor/projection/materialize/mod.rs`
- `crates/icydb-core/src/db/executor/projection/materialize/structural.rs`
- `crates/icydb-core/src/db/executor/projection/eval/mod.rs`
- `crates/icydb-core/src/db/executor/projection/eval/scalar.rs`

Mutation/reverse-index delta authority:

- `crates/icydb-core/src/db/commit/prepared_op.rs`
- `crates/icydb-core/src/db/executor/mutation/commit_window.rs`
- `crates/icydb-core/src/db/relation/reverse_index.rs`

Query-plan symbol resolution:

- `crates/icydb-core/src/db/query/plan/validate/symbols.rs`
- `crates/icydb-core/src/db/query/plan/validate/grouped/structure/mod.rs`
- `crates/icydb-core/src/db/query/plan/validate/grouped/policy/rules.rs`
- `crates/icydb-core/src/db/query/fluent/load/validation.rs`
- `crates/icydb-core/src/db/session/sql.rs`

Cursor invariant normalization:

- `crates/icydb-core/src/db/cursor/error.rs`
- `crates/icydb-core/src/db/cursor/validation.rs`
- `crates/icydb-core/src/db/cursor/mod.rs`
- `crates/icydb-core/src/db/query/plan/continuation.rs`
- `crates/icydb-core/src/db/executor/executable_plan.rs`
- `crates/icydb-core/src/db/error/cursor.rs`
- `crates/icydb-core/src/db/session/query.rs`
- `crates/icydb-core/src/db/cursor/tests.rs`
- `crates/icydb-core/src/error/tests.rs`

SQL/session query-lane cleanup:

- `crates/icydb-core/src/db/session/sql.rs`
- `crates/icydb-core/src/db/query/intent/errors/mod.rs`

Planner expression inference cleanup:

- `crates/icydb-core/src/db/query/plan/expr/type_inference/mod.rs`

---

## Verification In Current Checkpoint

- `cargo fmt --all --check`
- `cargo test -p icydb-core db::session::tests -- --nocapture`
- `cargo test -p icydb-core error::tests -- --nocapture`
- `cargo test -p icydb-core db::query::intent::errors::tests -- --nocapture`
- `cargo test -p icydb-core db::executor::projection::tests -- --nocapture`
- `cargo test -p icydb-core db::commit::tests -- --nocapture`
- `cargo test -p icydb-core prepared_op -- --nocapture`
- `cargo test -p icydb-core db::cursor::tests -- --nocapture`
- `cargo test -p icydb-core db::query::plan::expr::type_inference::tests -- --nocapture`

These checks cover the slices landed so far. The line is still in progress; the
remaining work is now narrower and more local than the earlier `0.62` runtime
structuralization phase.

---

## Current Likely Next Seams

- local SQL/session surface cleanup where unsupported-lane or lowering wrappers
  still exist only as forwarding glue
- further planner/intent validation cleanup where adjacent call sites still
  construct the same intent/invariant mapping locally
- another grouped validation pass to confirm there are no remaining
  structure-vs-policy duplicate fact checks beyond the intentional protective
  split
