# IcyDB 0.220 Implementation Status

Status: design proposed; implementation not started

Design: [0.220-design.md](0.220-design.md)

Design cut: 2026-08-04

Implementation baseline: to be frozen by Patch 1 from the exact published
post-0.219 source

## Planning Baseline

- Planning reference tag: `v0.219.1`
- Planning reference commit: `8792eba910f1262364dcf6025b83b0e7f560b50e`
- Branch: `main`
- Declared workspace package version: `0.219.1`
- Rust toolchain observed by the opening audit: `rustc 1.97.1`
- `ic-wasm` observed by the opening audit: `0.11.0`
- Binaryen observed by the exploratory audit: `108`
- Current host runtime requirement: PocketIC 15
- Worktree at design kickoff: dirty with user-owned 0.219
  changelog/dependency/integration-harness/status maintenance

The dirty planning tree is not an implementation or performance baseline.
Patch 1 records the exact clean post-0.219 source, feature graph, tool digests,
artifact hashes, and performance identity before production edits.

No Cargo package version is changed by implementation landings. Release
tooling owns version bumps.

## Opening Audit Snapshot

The following raw non-gzipped, IC-shrunk values are provisional direction
evidence. Patch 1 must reproduce or replace them under the accepted baseline.

| Subject | Raw Wasm bytes |
| --- | ---: |
| `default_empty` | 6,030 |
| `default_empty_metrics` | 290,878 |
| `one_entity_dynamic_query` | 2,465,649 |
| `one_entity_typed_query` | 2,492,315 |
| `one_entity_sql_query` | 2,918,934 |
| `ten_entity_typed_query` | 2,493,636 |

Exploratory Binaryen `-Oz` reduced the metrics subject by 6.47% and the four
runtime-heavy query subjects by 9.01% through 9.24%. Those artifacts received
structural parsing only, not runtime acceptance.

One current PocketIC 15 P1 shard passed all 235 scenarios. Its aggregate local
instructions were directionally 1.79% below the matching PocketIC 14 scenario
set, but the runtime-version drift makes that result non-comparable for formal
acceptance.

The opening size subtractions are also directional until Patch 1 proves matched
feature pairs with common actor/schema source, explicit feature/export deltas,
and one identical final-artifact pipeline.

## Landing Plan

| Patch | Bounded outcome | Canonical owner | Expected delivery domains | Focused validation | Public/format/cost impact | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1. Measurement authority and opening baseline | Freeze the clean source/tool/profile identity, controlled feature pairs, schema-guard lifecycle, current Wasm matrix, phase/cache semantics, retained-code ledger, and per-patch plus line-level budgets | Wasm audit scripts, SQL performance profile, integration evidence | Build / Canister, Integration Tests | exact installed artifact hashes, pair manifests, first/second/post-upgrade/post-update/query-only schema probes, PocketIC 15 P1/P2 calibration, phase/cache ownership, attribution ledgers | Test/profile hard cut only; no production behavior; defines all later cost gates | Not started |
| 2. Canonical post-link Wasm optimization | Pin one transformer and pass order, make its accepted output the sole deployable artifact, and prove feature/runtime/stack/instruction safety | Build / Canister artifact pipeline | Build / Canister, Integration Tests | deterministic transforms, accepted IC feature set, final-byte Candid/exports, deepest maintained shapes, pre-transform install/populate then transformed upgrade/reopen, sentinels | No public or persisted change; expected broad raw-Wasm reduction; instruction delta measured | Not started |
| 3. Query-context metrics routing | Suppress the default durable sink only inside synchronous closure-scoped query execution while retaining higher-precedence scoped attribution and update metrics | generated endpoint context and metrics sink | Build / Canister, Other Core, Integration Tests | exact sink precedence, no context across future/callback, nesting, query trap followed by query/update, typed/dynamic/SQL runtime | No outward metrics-format change; expected query instruction/allocation and shared Wasm reduction | Not started |
| 4. Compact metrics state and dispatch | Replace the selected representation with explicitly numbered fixed counter arrays and a bounded canonical-path entity map | metrics registry, state, dispatch, report projection | Other Core, Integration Tests | compact/extended report goldens, explicit codes/slots, saturation, bounds, reset-on-upgrade, remove/rename/replace lifecycle, ordering, metrics Wasm subject | Heap-only internal hard cut; no migration; outward metrics unchanged; expected metrics-capability Wasm reduction | Not started |
| 5. Canonical schema-application codegen contraction | Contract one measured repeated schema lowering/codegen shape inside the existing `apply_schema` owner | schema application and application lowering | Lowering / Session, Other Core, Build / Canister, Integration Tests | first/second/post-update/post-upgrade query cost, first create, exact no-op, transition, rejection, recovery, query-only bootstrap, Wasm matrix | No schema/public/persisted change; expected shared runtime Wasm reduction; initialization and any repeated query cost measured | Not started |
| 6. SQL boundary dependency retention hard cut | Remove exactly one evidence-selected general runtime dependency/adapter reachability path | selected SQL/DTO/scalar boundary | Parser or Lowering / Session as selected, Build / Canister, Integration Tests | matched SQL pair final-Wasm/dominator proof, supporting dependency tree, affected scalar/Candid semantics and SQL parity | No wire/value/persisted change; expected SQL-specific raw-Wasm reduction | Not started |
| 7. Ordered-range execution remediation and closeout | Select one current store/executor family, remove its avoidable work budget, publish final matrices, then await required full validation | selected executor/store materialization owner | Executor / Planner, Integration Tests, Build / Canister | P1/P2/scale selected family, typed/SQL parity, counters, cumulative budgets, focused final gates; external full suite/build matrix required for close | No query/public/persisted change; required instruction win or evidence-backed no-change; final status initially awaits full validation | Not started |

Seven patches match the repository's normal minor-line planning target. Each
patch is one reviewable worktree handoff and stops before the next begins.

Every patch may record an evidence-backed no-production-change disposition.
Its bounded measurement, tests, and decision still land; no patch forces an
optimization after evidence rejects its candidate.

## Frozen Cross-Patch Invariants

- Raw non-gzipped final deployable Wasm is size authority; gzip is secondary.
- Accepted schema snapshots remain runtime authority.
- Generated models remain proposal, reconciliation, convenience, and test
  inputs only.
- `apply_schema` remains the sole catalog-native schema application owner.
- No public Candid, method export, successful query result, ordering, cursor,
  mutation atomicity, error semantic, or persisted format changes without an
  explicit prior design revision.
- Query-side global metrics remain non-durable; update-side metrics retain
  current semantics; explicit call-local sinks remain observable.
- No benchmark-specific dispatch, cost model, query hint, old/new runtime
  fallback, compatibility wrapper, or dual artifact pipeline is added.
- Final Wasm identity includes every transform, injected IC/Candid metadata,
  and shrink stage; SHA-256 is taken from the exact installed bytes.
- Controlled size comparisons differ by one declared capability only.
- Metrics dispatch precedence is scoped sink, query suppression, then durable
  global sink; query context is synchronous and never spans `.await`.
- Metrics are heap-only, reset on install/upgrade, use explicit stable metric
  code values, saturating counters, and never index storage by `EntityTag`.
- Phase ownership distinguishes additive children from inclusive parents and
  nested observations; warm-at-entry requires a prior successful update and
  cache-hit proof.
- A size patch reports instructions; a performance patch reports raw Wasm.
- Per-patch deltas and cumulative opening-to-current budgets must both pass.
- Every production handoff reports files, approximate line delta, delivery
  domains, and whether complexity became simpler, neutral, or more complex.
- Full workspace tests remain user-owned push validation.

## Patch 1 Entry Conditions

Patch 1 implementation may begin only after:

1. the 0.219 closeout verdict is ready/complete;
2. the user has explicitly named and started 0.220;
3. the exact post-0.219 source intended as baseline is clean and recorded;
4. current PocketIC 15 and Wasm transformation tools are available with exact
   version/digest evidence; and
5. the implementation turn is explicitly authorized after review of this
   design.

Conditions 1 and 2 are satisfied. Conditions 3 through 5 remain open. This
design landing does not begin Patch 1 implementation.

## Required Cost Evidence Per Production Patch

Every production handoff records:

- exact before/after final raw Wasm bytes and SHA-256 for affected subjects;
- proof that the bytes measured and tested are the exact final installed bytes;
- compiler-output and attribution context where relevant;
- gzip bytes only as secondary context;
- same-profile instruction totals and affected phase deltas;
- maintained sentinel, selected-family, and cumulative line-budget verdicts;
- Candid bytes/hash and method exports where relevant;
- matched final-Wasm/dominator proof for runtime reachability changes, with
  Cargo dependency trees as supporting evidence;
- files touched, approximate line delta, and delivery domains;
- implementation complexity shape; and
- focused validation plus user-owned skipped full-suite validation.

## Completion State Contract

Focused passing checks authorize each implementation handoff. After Patch 7,
the strongest status available from agent-owned focused validation is:

`implementation complete; awaiting full validation`

0.220 becomes complete only after the required user/CI-owned full suite and
complete build/Wasm matrix have actually passed and their run identity and
verdict are recorded here. Merely listing those checks as skipped cannot close
the line.
