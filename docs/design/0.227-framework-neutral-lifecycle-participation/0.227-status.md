# 0.227 Implementation Status

Status: planning only; Patch 1 read-only inventory is authorized and may run
alongside 0.226; no mutating implementation is authorized or started

Design:
[0.227 framework-neutral lifecycle participation](0.227-design.md)

Status cut: 2026-08-14

## Current Boundary

0.227 is reserved for one IcyDB lifecycle participant seam. It is independent
of SQL/schema read authorization semantics and of every external framework.

The maintained standalone form still owns init/post-upgrade and the accepted
0.225 watchdog reconstruction. No participant form, hidden callbacks, or
duplicate latch exists today.

## Frozen Direction

- Keep standalone `icydb::start!()` exact.
- Add only root-level `icydb::start!(participant)`.
- Permit exactly one form per build; duplicate/mixed/nested uses fail compile.
- Emit two `#[doc(hidden)] pub(crate) fn() -> ()` functions under one
  Patch-1-frozen collision-safe namespace and no lifecycle exports in
  participant mode.
- Use one volatile `Idle/Running/Completed` state.
- Completed duplicates are inert before timer mutation; Running re-entry traps
  and rolls back for safe lifecycle-message retry.
- Wrong-phase first invocation is forbidden but not falsely claimed to be
  runtime-detectable; application-root source and complete artifact evidence
  prove correct phase placement.
- Run the participant during init and post-upgrade before every deferred work
  item even while the owning application is inactive, `Prepared`, or not yet
  activated.
- Preserve the exact 0.225 `ic-timers 0.3.1` Watchdog provider and recovery protocol.
- Provide a participant only; do not claim Canic or another framework already
  supplies a compatible composition root.
- Keep any Canic lifecycle-composition API in a separate Canic design line;
  Canic 0.103 needs no further lifecycle content.

## Promotion State

| Gate | State |
| --- | --- |
| 0.226 ready/complete closeout | Required before mutating Patch 2; does not block Patch 1 inventory |
| Exact 0.225 lifecycle/timer baseline | Pending Patch 1 |
| Root-placement and namespace | Proposed; Patch 1 must freeze collision-safe top-level or preferred hidden-module shape |
| Duplicate/trap/Watchdog-claim protocol | Proposed exactly; owner-local proof required |
| Framework-neutral composition harness | Required; no external release dependency |
| User implementation authorization | Not granted |

## Landing Tracker

| Patch | State |
| --- | --- |
| 1. Current-state inventory and frozen baseline | Authorized read-only; may run alongside 0.226; not started |
| 2. Root-only participant and duplicate protocol | Blocked on Patch 1 and promotion |
| 3. Framework-neutral composition contract and documentation | Blocked on Patch 2 |
| 4. Real-canister evidence and closeout | Blocked on Patch 3; final cross-owner evidence only |

## Frozen Non-Goals

- no read-authorization changes;
- no Canic dependency or lifecycle API;
- no generic participant registry or dynamic callbacks;
- no public lifecycle endpoint or external executor;
- no persisted lifecycle state;
- no timer-provider/recovery change;
- no compatibility or dual registration; and
- no implementation/release work.

## Next Authorized Action

Patch 1 only: return the exact lifecycle/timer baseline, complete root-name
collision matrix, frozen participant namespace, full generated
lifecycle/Candid/Wasm export inventory, ordering evidence, and cost evidence.
Do not start Patch 2 or any mutating participant work before 0.226 has a
reported ready/complete closeout and the promotion gate is accepted.
