# 0.187 Findings

This table is the working audit ledger. Add evidence before changing runtime
code.

| Area | Suspected Duplicate | Evidence | Risk | Recommendation | Release Target |
| --- | --- | --- | --- | --- | --- |
| Filter authority | SQL/fluent predicate facts could diverge after lowering. | 0.186 moved `NormalizedFilter` to explicit semantic authority and guards downstream predicate consumers. | Low unless a new bypass appears. | Keep as deliberate shared contract; add invariant tests for new consumers. | Guard mode |
| Strict SQL write/global aggregate filters | UPDATE and global aggregate base WHERE still use strict predicate-admission lanes outside ordinary SELECT/DELETE expression-backed handoff. | `from_update_where_expr` and `from_where_expr_requiring_predicate_subset` are source-guarded; expression-only WHERE tests fail closed. | Low; widening would change SQL admission. | Keep as deliberate specialization. | Guard mode |
| Branch-aware route families | `IndexMultiLookup`, `IndexBranchSet`, and set composites look like branch variants. | 0.185 closeout records distinct proof, diagnostics, cache identity, prefix arity, and cursor semantics. | Low for duplication; high if collapsed without new cursor/route design. | Keep as deliberate specialization; benchmark before cost changes. | Future cost/route design |
| Prefix-cardinality COUNT/EXISTS | Direct SQL count prefix specs and prepared aggregate preflight both lower metadata-count terminal work. | `prefix_cardinality_count_entrypoints_share_proof_and_terminal_authority` guards the shared planned proof and shared terminal path; SQL direct COUNT is forbidden from owning store-cardinality execution. | Low while guard holds; medium if new entrypoints bypass the shared proof or terminal. | Keep the split as deliberate entry-contract specialization; consolidate only mechanical glue later. | 0.187.0 guard |
| Scalar execution setup | Materialized, retained-slot, covering, and aggregate row-sink paths share a spine but still have terminal-specific preparation. | `scalar_entrypoints_share_execution_inputs_spine` guards shared `ExecutionInputs` construction and scalar kernel entry across materialized page, retained-slot page, and aggregate row-sink entrypoints. | Low while guard holds; terminal payload specialization remains intentional. | Keep shared scalar route/input spine; do not collapse covering or aggregate projection payload owners. | 0.187.0 guard |
| Mutation row collection | Typed DELETE, SQL DELETE count, SQL DELETE RETURNING, UPDATE, and INSERT SELECT still stage rows through different public surfaces. | `sql_write_candidate_bounds_keep_mutation_batch_and_delete_boundaries_explicit` guards the shared SQL UPDATE/INSERT mutation-batch bound and the SQL DELETE projection/count pre-commit bound. | Low for duplicate authority while guard holds; medium memory/cycle risk remains for unstreamed DELETE collection. | Keep the split; defer bounded streaming DELETE collection to a post-access collector design. | 0.187.0 guard, streaming deferred |
| Aggregate families | Dedicated global aggregate and grouped/singleton aggregate execution are related but not unified. | 0.184 parity coverage and the shared aggregate operator note define the DTO gate; no new 0.187 duplicate consumer was found. | Low for correctness while parity holds; medium architecture churn if rewritten broadly. | Keep parity tests; defer DTO/physical plan rewrite until it deletes a real duplicate or becomes a shared runtime/EXPLAIN/cache handoff. | Deferred by 0.184 gate |
| EXPLAIN/cache/diagnostics | Diagnostics and cache could become parallel semantic authorities. | 0.184/0.186 source guards and contracts now keep residual and predicate facts planner-owned. | Low unless new derived facts appear. | Keep as projection surfaces; audit repeated expensive derivation separately. | Guard mode |
| Generated endpoints | Generated canister endpoints and session/library surfaces may diverge in policy or result shaping. | Generated SQL canister matrix passed on 2026-06-27: 76 tests, 0 failed. | Low while matrix stays green; rerun after generated/runtime surface changes. | Keep as validation gate for release prep and generated-surface behavior claims. | Validated |
| Panic-shaped query invariants | Some planner/expression projection paths still converted internal shape drift into traps via `.expect(...)` or `unreachable!(...)`. | Query expression preview/evaluation, predicate bridge conversion, grouped EXPLAIN/fingerprint projection, grouped strategy selection, resolved ORDER handling, SQL write primary-key normalization, finalized static-execution-planning metadata, SQL UPDATE/DELETE policy validated-plan helpers, covering aggregate terminal-value selection, and SQL execution routing fallbacks now return `None`, typed invariant errors, or conservative fail-closed projection. | Low after cleanup for touched paths; medium if new reference-returning helpers grow unchecked panics. | Keep runtime invariants recoverable and source-guard static contract, SQL write-policy helper, aggregate projection terminal access, and SQL execution routing fallbacks. | 0.187.x cleanup |
| Lint suppressions | Stale `allow` / `expect` suppressions may hide obsolete code after 0.184-0.186 cleanup. | Source-only inventory found mostly intentional production Clippy shape/style fences and test-only `.expect(...)` noise. | Low; stale suppressions are hygiene risk, not duplicate-authority risk. | Defer removal to a dedicated Clippy-backed hygiene pass. | Hygiene follow-up |
