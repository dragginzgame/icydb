0.90 SQL Surface Closure (Placeholder)
Thesis

0.90 completes the core SQL surface for grouped queries by aligning supported syntax with implemented semantics, eliminating remaining fail-closed gaps, and stabilizing the SQL contract without introducing new execution models.

Problem statement

After 0.85–0.89:

core grouped execution is strong
expression model is unified
ordering is partially complete

Remaining issues:

some SQL constructs still:
parse but fail later
are inconsistently supported
are unnecessarily restricted

This creates:

usability friction
non-obvious rejection cases
mismatch with user expectations
Design goal

Close the SQL surface so that supported features are predictable, consistent, and complete within the existing execution model.

No new major capabilities—just finishing the surface.

Scope
In scope (candidate areas)
1. Expression surface consistency
ensure all scalar expressions allowed in:
projection
HAVING
ORDER BY (within 0.88/0.89 constraints)
2. Alias resolution completeness

Allow:

SELECT AVG(strength) AS avg_strength
ORDER BY avg_strength

Ensure:

consistent alias resolution across:
projection
ORDER BY
HAVING (if applicable)
3. ORDER BY surface closure

Complete:

mixed term handling
alias support
consistent rejection messages
ensure no silent fallbacks
4. Error surface normalization

Ensure:

planner errors are:
precise
consistent
aligned with earlier slices (0.87 style)
5. Remove legacy fail-open / fail-unclear paths

Audit for:

partial support
inconsistent rejection
hidden behavior differences
Out of scope
window functions
subqueries
joins (if not already supported)
DISTINCT aggregates (if not already implemented)
major SQL feature expansion
Architectural principle

0.90 is not about adding features—it is about making the existing feature set complete, explicit, and consistent.

Changes
Planner
unify validation rules across:
projection
HAVING
ORDER BY
ensure alias resolution is single-pass and deterministic
Executor
no new execution paths
reuse all existing machinery
Parser
minimal or no changes
Guardrails
1. No new execution models

Do not introduce:

new evaluators
new grouping semantics
new ordering strategies
2. No silent behavior changes

All changes must be:

explicit
tested
reflected in explain/error output
3. Preserve fail-closed behavior

Unsupported constructs must:

fail early
fail clearly
Acceptance criteria
Functional
no “surprising” SQL rejections within supported surface
consistent alias behavior
consistent expression handling across clauses
Architectural
no duplicate logic paths
no new evaluator surfaces
validation centralized
Quality
improved error messages
cleaner explain output
reduced edge-case behavior
Thesis sentence

0.90 finalizes the SQL surface by aligning supported syntax with the underlying execution model, eliminating inconsistent or partial behaviors, and delivering a predictable, fail-closed query experience without introducing new execution complexity.