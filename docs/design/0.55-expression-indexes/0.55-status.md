# 0.55 Status Tracker

Current state: in progress.

## Scope

This status tracks `0.55` expression-index planning and the prerequisite
foundation hardening work identified from the filtered-index line.

Design reference:

- `docs/design/0.55-expression-indexes/0.55-design.md`

---

## Pre-0.55 Foundation Slice (Prerequisite)

Goal:

- Remove string-authoritative predicate and SQL side paths that would otherwise
  compound drift when expression indexes are introduced.

Boundary:

- This is prerequisite hardening for `0.55` expression indexes.
- This is not a full architecture rewrite.

### Mapped raw-string semantic authority sites

- Conditional-index predicate metadata is still string-authoritative:
  - `crates/icydb-core/src/model/index.rs`
  - `crates/icydb-schema/src/node/index.rs`
  - `crates/icydb-schema-derive/src/node/index.rs`
- The same predicate semantics are reparsed across multiple runtime layers:
  - `crates/icydb-core/src/db/schema/info.rs`
  - `crates/icydb-core/src/db/query/plan/planner/index_select.rs`
  - `crates/icydb-core/src/db/index/plan/mod.rs`
  - `crates/icydb-core/src/db/commit/prepare.rs`
- Commit schema fingerprinting does not include index predicate semantics:
  - `crates/icydb-core/src/db/schema/fingerprint.rs`
- SQL dispatch and EXPLAIN wrappers perform semantic string scanning outside the
  canonical reduced SQL parser:
  - `crates/icydb/src/db/sql/mod.rs`
  - `crates/icydb-build/src/db.rs`
- Identifier normalization logic is duplicated in separate string helpers:
  - `crates/icydb-core/src/db/sql/lowering.rs`
  - `crates/icydb/src/db/sql/mod.rs`
- Projection column labels are reparsed from SQL text in the facade:
  - `crates/icydb/src/db/sql/mod.rs`
- Diagnostics derive entity identity by splitting index name text:
  - `crates/icydb-core/src/db/diagnostics/mod.rs`

### Ranked cleanup plan

- P0 (now): canonical index predicate authority and single lowering boundary.
- P0 (now): include predicate semantics in commit schema fingerprinting.
- P0 (now): route SQL command/explain dispatch through canonical parser tokens.
- P1 (soon): unify SQL identifier normalization into one shared utility/boundary.
- P1 (soon): stop projection-label SQL reparsing; use canonical projection metadata.
- P1 (soon): define typed index key-item metadata (`Field` / `Expression`) for
  expression-index rollout.
- P2 (later): remove `index_name.split('|')` semantic dependency in diagnostics.

### Concrete foundation slices

- [x] F0. Canonical index predicate metadata ownership
  - Introduce canonical predicate authority in index metadata path.
  - Lower once at schema/derive boundary; downstream consumers use canonical form.
  - Implemented:
    - `db::index::predicate::canonical_index_predicate(...)` parse/cache boundary
    - schema validation, planner implication gating, and index-mutation membership
      compile now consume the canonical boundary (no per-layer ad hoc parsing)
    - schema/derive/model raw predicate SQL is explicitly documented as input-only
  - Touch:
    - `crates/icydb-schema/src/node/index.rs`
    - `crates/icydb-schema-derive/src/node/index.rs`
    - `crates/icydb-core/src/model/index.rs`
    - `crates/icydb-core/src/db/schema/info.rs`
    - `crates/icydb-core/src/db/index/plan/mod.rs`
    - `crates/icydb-core/src/db/query/plan/planner/index_select.rs`
    - `crates/icydb-core/src/db/commit/prepare.rs`

- [x] F1. Predicate semantics in schema fingerprinting
  - Add canonical predicate fingerprinting to commit schema hash contract.
  - Implemented:
    - commit schema fingerprint now includes canonical predicate semantics for
      filtered indexes
    - equivalent predicate SQL text with identical semantics hashes identically
    - semantic predicate changes now invalidate commit schema fingerprint
  - Touch:
    - `crates/icydb-core/src/db/schema/fingerprint.rs`
    - predicate fingerprint/hash helpers as needed

- [x] F2. Canonical SQL command routing boundary
  - Replace manual statement/explain/entity string parsing in dispatch path with
    canonical parser outputs.
  - Implemented:
    - core session now exposes canonical parsed statement route metadata
      (`Query` / `Explain` + parsed entity) via `sql_statement_route(...)`
    - generated SQL dispatch routes through `db().sql_statement_route(...)`
      instead of facade manual keyword/entity scanning
    - facade manual `statement_entity_name(...)` / `is_explain_sql(...)` side
      path removed from dispatch flow
  - Touch:
    - `crates/icydb-core/src/db/session/sql.rs`
    - `crates/icydb-core/src/db/session/mod.rs`
    - `crates/icydb-core/src/db/mod.rs`
    - `crates/icydb/src/db/session/mod.rs`
    - `crates/icydb/src/db/mod.rs`
    - `crates/icydb/src/db/sql/mod.rs`
    - `crates/icydb-build/src/db.rs`

- [x] F3. Shared identifier normalization authority
  - Consolidate tail-match and qualifier normalization into one shared boundary.
  - Implemented:
    - added shared core SQL identifier normalization helpers
      (`identifiers_tail_match`, qualifier reduction) under
      `db::sql::identifier`
    - SQL lowering now consumes shared helper authority instead of local
      duplicate implementations
    - facade/build SQL entity route matching now uses the shared core helper
      authority
  - Touch:
    - `crates/icydb-core/src/db/sql/lowering.rs`
    - `crates/icydb-core/src/db/sql/identifier.rs`
    - `crates/icydb-core/src/db/mod.rs`
    - `crates/icydb/src/db/mod.rs`
    - `crates/icydb-build/src/db.rs`
    - `crates/icydb/src/db/sql/mod.rs`

- [x] F5. Typed key-item metadata for expression indexes
  - Add index key-item model that can represent fields and canonical expressions.
  - Keep filtered predicates orthogonal and independently validated.
  - Implemented:
    - schema + runtime index metadata now carry typed key-item contracts
      (`Field` / `Expression`) at the index model boundary
    - derive/codegen now emits explicit field key-items instead of relying on
      implicit field-string authority
    - runtime schema validation fails closed on expression key-items in this
      slice (metadata foundation shipped; execution/planning not enabled yet)
    - index-key build path has defensive expression-key unsupported guard
    - commit schema fingerprint now includes expression key-item semantics
      while preserving field-only parity
  - Touch:
    - `crates/icydb-schema/src/node/index.rs`
    - `crates/icydb-schema-derive/src/node/index.rs`
    - `crates/icydb-core/src/model/index.rs`
    - `crates/icydb-core/src/db/schema/info.rs`
    - `crates/icydb-core/src/db/schema/errors.rs`
    - `crates/icydb-core/src/db/schema/fingerprint.rs`
    - planner/index-key derivation modules

- [x] F4. Canonical projection label ownership
  - Remove facade SQL text reparsing for projection labels.
  - Lower labels from canonical projection metadata.
  - Implemented:
    - core session now exposes canonical SQL projection-column derivation
      (`sql_projection_columns(...)`) from planned projection semantics
    - generated SQL dispatch now requests canonical projection labels from
      core session and passes them through projection row rendering
    - facade SQL module no longer reparses SQL text to derive projection
      labels (`SELECT ... FROM ...` string scanning removed)
  - Touch:
    - `crates/icydb-core/src/db/session/sql.rs`
    - `crates/icydb-core/src/db/query/intent/query.rs`
    - `crates/icydb-core/src/db/session/tests.rs`
    - `crates/icydb/src/db/session/mod.rs`
    - `crates/icydb-build/src/db.rs`
    - `crates/icydb/src/db/sql/mod.rs`

### Exit criteria before expression-index enablement

- No planner/runtime path treats raw predicate SQL text as semantic authority.
- Schema fingerprint changes when filtered-index predicate semantics change.
- SQL route/explain dispatch does not depend on manual keyword scanning helpers.
- Index metadata path has a canonical key-item model ready for expression
  key materialization.

## 0.55 Expression Slice Progress

- [x] S0. Canonical expression key-item metadata owner
  - Introduced typed canonical expression key metadata in schema/runtime index
    models (`IndexExpression`).
  - Removed raw expression string authority from `IndexKeyItem::Expression`.
  - Added deterministic canonical expression display + fingerprint kind tags.
  - Touch:
    - `crates/icydb-schema/src/node/index.rs`
    - `crates/icydb-core/src/model/index.rs`
    - `crates/icydb-core/src/model/mod.rs`
    - `crates/icydb/src/lib.rs`
    - `crates/icydb-core/src/db/schema/fingerprint.rs`

- [x] S1. Schema-time expression key-item validation
  - Replaced fail-closed `expression unsupported` schema gate with typed
    validation for supported canonical expression family:
    - text transforms: `LOWER`, `UPPER`, `TRIM`, `LOWER(TRIM(...))`
    - temporal transforms: `DATE`, `YEAR`, `MONTH`, `DAY`
  - Validation now enforces field existence/queryability and expression input
    type constraints (text vs date/timestamp).
  - Touch:
    - `crates/icydb-core/src/db/schema/info.rs`
    - `crates/icydb-core/src/db/schema/errors.rs`
    - `crates/icydb-core/src/db/schema/types.rs`
    - `crates/icydb-core/src/db/contracts/tests/schema_validate.rs`

- [x] S2. Mutation-path expression key derivation
  - Index key derivation now materializes canonical expression key items in the
    mutation pipeline (`save`/`replace`/`delete` planning).
  - Unique validation now evaluates uniqueness over canonical derived key
    components (not raw source-field string equality side paths).
  - Implemented expression key derivation for:
    - `LOWER`, `UPPER`, `TRIM`, `LOWER(TRIM(...))`
    - `DATE`, `YEAR`, `MONTH`, `DAY`
  - Added live conflict test proving casefold unique-expression semantics.
  - Added conditional+expression composition coverage proving predicate-false
    rows bypass unique validation while predicate-true activation enforces
    canonical `LOWER(email)` uniqueness.
  - Added live/replay classification parity coverage for casefold expression
    unique conflicts.
  - Touch:
    - `crates/icydb-core/src/db/index/key/build.rs`
    - `crates/icydb-core/src/db/index/plan/unique.rs`
    - `crates/icydb-core/src/db/commit/tests.rs`

- [x] S3. Planner expression key-item eligibility
  - Planner prefix and multi-lookup eligibility now evaluates canonical index
    key-items (`Field` / `Expression`) instead of raw leading-field-only checks.
  - `TextCasefold` predicate literals now lower to canonical lookup values for
    `LOWER(field)` expression indexes (EQ / IN / OR-equality rewrite path).
  - Planner expression lookup support is now explicit on
    `IndexExpression` (`supports_text_casefold_lookup`) rather than implied by
    ad hoc expression-pattern matches.
  - Added fail-closed planner coverage for unsupported expression lookup kinds
    (for example `UPPER(field)` under `TextCasefold` Eq/In) to lock deterministic
    fallback behavior.
  - Added filtered+expression composition coverage proving filtered predicate
    implication remains the gate for expression-index eligibility.
  - Range and starts-with planning remain strict-only in this slice; no widening
    of non-expression compare coercion eligibility was introduced.
  - Touch:
    - `crates/icydb-core/src/db/query/plan/planner/prefix.rs`
    - `crates/icydb-core/src/db/query/plan/planner/compare.rs`
    - `crates/icydb-core/src/db/query/plan/planner/predicate.rs`
    - `crates/icydb-core/src/db/query/plan/tests/mod.rs`

- [x] S4. EXPLAIN access-choice parity with expression key-item planning
  - Access-choice candidate/rejection evaluation now mirrors canonical key-item
    semantics for prefix and multi-lookup paths (`Field` / `Expression`).
  - Planner and access-choice now consume one shared key-item matcher/lowering
    helper (`db::query::plan::key_item_match`) to avoid dual semantic ownership.
  - `LOWER(field)` expression indexes are now explain-eligible for
    `TextCasefold` Eq/In predicates; raw field indexes are rejected with
    deterministic mismatch reasons under the same predicate shape.
  - Added explicit rejection coverage for unsupported expression lookup kinds
    so access-choice and planner both fail closed under the same expression
    capability matrix.
  - Added explicit range-family fail-closed parity for expression key-items:
    access-choice now rejects expression-key indexes for range/starts-with
    candidate scoring, matching planner eligibility boundaries in this slice.
  - Added focused unit tests for expression-index explain eligibility and mixed
    literal-set rejection behavior.
  - Added planner regression coverage proving strict `Gt` and `StartsWith`
    predicates do not route through expression indexes in this slice.
  - Touch:
    - `crates/icydb-core/src/db/query/plan/access_choice.rs`
    - `crates/icydb-core/src/db/query/plan/planner/index_select.rs`
    - `crates/icydb-core/src/db/query/plan/tests/mod.rs`

- [x] S5. Planner/access-choice/runtime parity matrix for expression lookups
  - Added executor-level parity coverage for supported expression-index lookup
    subset (`LOWER(field)` + `TextCasefold` `Eq`/`In`) across:
    - planner access-path selection (`IndexPrefix` / `IndexMultiLookup`)
    - access-choice diagnostics (`access_choice_chosen`, `chosen_reason`)
    - runtime route node selection (`IndexPrefixScan` / `IndexMultiLookup`)
  - Added result assertions proving route parity also preserves canonical
    execution semantics for lowercased lookup values and deduplicated IN sets.
  - Touch:
    - `crates/icydb-core/src/db/executor/tests/mod.rs`
    - `crates/icydb-core/src/db/executor/tests/semantics.rs`
