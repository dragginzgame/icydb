# IcyDB 0.217 Implementation Status

Status: Complete; Patches 1-8 complete

Design: [0.217-design.md](0.217-design.md)

Accepted: 2026-08-01

## Baseline

- Release baseline: `v0.216.5`
- Commit: `cc62ebc965371d24df4f1418e6816e268d2b1592`
- Branch: `main`
- Worktree at kickoff: clean
- Declared workspace package version: `0.216.5`

No Cargo package version is changed by implementation landings. Release
tooling owns version bumps.

## Planned Landing Sequence

The endpoint-export work uses the six accepted landings from the original
design. Patches 1 through 4 remain internal and cannot create a downstream
endpoint export; Patch 5 owns the atomic public-authority hard cut. Patches 7
and 8 are the separately approved post-closeout grouped-query activation and
hardening follow-ups.

| Patch | Bounded outcome | Primary domains | Status |
| --- | --- | --- | --- |
| 1. Export and Candid proof foundation | Reusable artifact inspection, exact current ABI goldens, and the frozen nine-canister production/local policy | Build / Canister, Integration Tests | Complete |
| 2. Private capability and stable DTO boundary | Exact private handlers, public SQL performance DTO, feature-independent SQL result ABI, and compilation-only extended metrics | Build / Canister | Complete |
| 3. Internal closed declaration compiler | Package-private endpoint specifications, validation, and wrapper-token emission with no exported macro | Build / Canister | Complete |
| 4. Internal capability, authorization, and fixture locks | Package-private capability/auth plans, admission mapping, fixture coercion, and direct handler behavior | Build / Canister, Integration Tests | Complete |
| 5. Atomic endpoint-authority hard cut | Public source macro, maintained canister migration, explicit builds, and complete TOML/config/build-option/CLI removal | Build / Canister, Integration Tests | Complete |
| 6. Installation, cost, and release closeout | Downstream installation, production/test absence proof, cost evidence, and final cleanup | Build / Canister, Integration Tests | Complete |
| 7. Query-only grouped fluent activation | Expose the existing accepted-schema grouped planner/executor through bounded dynamic and typed query terminals without SQL DTOs | Query / Facade | Complete |
| 8. Grouped facade hardening | Replace caller-visible planner invariants with exact query diagnostics and prove generated typed grouped execution at runtime | Query / Facade, Integration Tests | Complete |

One implementation turn stops after one completed landing and its focused
validation, status evidence, and root Unreleased note.

## Patch 1 — Export And Candid Proof Foundation

Canonical owners:

- reusable artifact inspection in the integration-test support crate;
- exact endpoint ABI goldens generated from current public Rust DTOs; and
- one code-owned nine-canister production/local feature and export policy.

Required evidence:

- deterministic Candid method/mode inspection;
- bounded direct parsing of function exports from raw Wasm;
- agreement between Candid and Wasm method manifests;
- exact transitive ABI text generated from the maintained Rust types;
- all seven config-owned and two implicit-default consumers represented once;
- current local/IC IcyDB export manifests frozen before the authority cut; and
- no production endpoint-generation or public API change.

Public/persisted impact: none. Patch 1 is test infrastructure and frozen
evidence only. It does not add `icydb::endpoints!`, change generated actor
output, or alter accepted or persisted database formats.

### Patch 1 Evidence

- One bounded raw-Wasm parser reads the export section directly and retains
  only function exports with IC query, update, or composite-query prefixes.
  Malformed lengths, overflow, truncation, invalid UTF-8, trailing export
  bytes, and duplicate methods fail closed.
- One Candid service inspector handles nested record/function syntax,
  comments, quoted labels, and both extractor forms with or without the final
  method semicolon. It reports the same ordered method/mode identity as the
  Wasm inspector.
- The checked-in 837-line, 25,856-byte ABI foundation is rendered
  deterministically from the current public `Error`, SQL, integrity, compact
  and extended metrics, storage, and entity-schema DTOs. The query performance
  record is an explicit test-only mirror until Patch 2 gives it a public owner.
- One code-owned policy contains every one of the seven config-owned actors
  and both implicit-default actors exactly once. It records the accepted
  production/local feature sets and the pre-cut local/IC `icydb_*` methods.
- Eighteen focused current-baseline actor builds passed: nine local and nine IC
  builds. Generated Candid and raw Wasm agreed exactly for all methods and
  modes, and their IcyDB subsets matched the frozen policy including actors
  with no IcyDB exports.
- Four focused library tests pass for Candid parsing, raw-Wasm parsing, policy
  completeness/determinism, and ABI-golden drift. The one built-artifact
  matrix test passes. Warnings-denied all-target Clippy passes for the affected
  integration crate.
- The opening production-size reference remains the released 0.216.5 SQL
  performance Wasm: 5,326,191 raw non-gzipped bytes, SHA-256
  `060064ca24498948357ad9fa29711ab6c453d9f7af1a87a1ea4a6853587a007a`.
  Patch 1 changes no canister/runtime source, build option, or generated actor,
  so it has no raw-Wasm or endpoint-instruction delta.
- Patch 1 touches nine files at approximately `+1,756/-1` lines, dominated by
  the normative transitive Candid golden. The Build / Canister and Integration
  Tests domains are crossed only for proof infrastructure. Test complexity
  grows deliberately; production structure and runtime complexity are
  unchanged.

## Patch 2 — Private Capability And Stable DTO Boundary

Canonical owners:

- root-private generated staging handlers for every currently generated SQL,
  fixture, integrity, schema, metrics, reset, and snapshot endpoint body;
- public `icydb::db::sql::SqlQueryPerfResult` as the sole SQL query-performance
  response DTO; and
- `icydb/metrics-extended` as the compilation capability for the rich metrics
  report DTO and construction graph.

Required evidence:

- each existing generated wrapper retains its IC method attribute and performs
  authorization before calling exactly one private handler;
- private handlers are inherited-visibility Rust functions and carry no query,
  update, `export_name`, or `no_mangle` attribute;
- the generated actor-local and CLI-local SQL performance mirrors are deleted;
- `SqlQueryResult::Explain` and its renderer compile under `icydb/sql` with or
  without `icydb/sql-explain`;
- extended metrics compile only through the explicit capability while compact
  metrics remain part of the base runtime; and
- current Candid and Wasm method manifests remain byte-for-byte/mode-for-mode
  compatible with Patch 1's frozen evidence.

Public/persisted impact: the existing SQL performance record gains its public
Rust owner and `SqlQueryResult` gains a capability-independent Rust/Candid
shape. There is no endpoint, authorization, execution, accepted-schema, or
persisted-format change.

### Patch 2 Evidence

- Twenty-six focused actor-codegen tests pass, including semantic wrapper
  signatures, authorization-before-handler ordering, canonical dispatch, and
  forbidden-export-attribute scans for every private handler class.
- The two SQL DTO Candid tests pass under both `sql` alone and `sql-explain`.
  The 837-line, 25,856-byte transitive endpoint ABI golden remains unchanged
  when rendered from the new public performance DTO.
- Base facade checks pass with no features, with `metrics-extended`, and with
  SQL independently of `sql-explain`. The rich EventReport Candid-shape test
  passes with `metrics-extended`; 28 focused CLI shell tests pass using the
  public performance DTO.
- The two focused generated SQL runtime calls pass: the query wrapper reaches
  the maintained query handler, and the integrity wrapper preserves both its
  successful handler result and the exact typed non-controller error.
- All eighteen maintained current-baseline actor builds pass again. Generated
  Candid and raw Wasm still agree exactly for all nine local and nine IC
  surfaces, so handler extraction added or removed no method.
- Warnings-denied all-target Clippy passes for `icydb-model`, `icydb` with the
  base and combined SQL-explain/extended-metrics feature sets, `icydb-cli`, and
  the integration support package.
- Every Patch 1/2 Rust file passes a direct Rustfmt check and diff hygiene is
  clean. A final workspace-wide format check is deferred because concurrent,
  unrelated executor edits arrived after Patch 2 formatting; those files are
  preserved for their owning session and the push workflow must recheck them.
- The current SQL performance `wasm-release` artifact is 5,326,148 raw
  non-gzipped bytes, 43 bytes (0.0008%) below the released 0.216.5 reference,
  with SHA-256
  `44cde015a8810b991570d9b71ecd1ff1d42a7c373af3f859d6a2b9ce7ebd2666`.
  Concurrent text-transform owner cleanup is present in the measured worktree,
  so the byte delta is combined evidence; it nevertheless proves there is no
  retained Wasm growth across the private-handler boundary.
- Excluding Patch 1's proof foundation and the concurrent text cleanup, Patch
  2 touches approximately nineteen files at `+480/-195` lines. Build-time
  structure becomes moderately more explicit; public execution and runtime
  complexity remain neutral because the private handler calls inline away in
  the measured release artifact.

## Patch 3 — Internal Closed Declaration Compiler

Canonical owner:

- the host-only, package-private `build::actor::endpoint` module in
  `icydb-model`, with no public re-export or production caller before the
  atomic Patch 5 authority cut.

Required evidence:

- the declaration vocabulary contains exactly the eleven accepted endpoint
  names and their closed endpoint-specific options;
- only ordinary `cfg` attributes are accepted and duplicate names reject even
  when declarations carry different conditions;
- missing, unknown, repeated, and invalid options remain distinct typed
  failures;
- each accepted declaration emits exactly one fixed wrapper, IC method name,
  query/update mode, authorization path, and private-handler call; and
- no public declarative/procedural macro, hidden downstream helper, re-export,
  or current actor-generation call makes the compiler a second endpoint
  authority.

Public/persisted impact: none. The compiler is host-only and dead outside its
focused internal tests until Patch 5. It changes no current generated actor,
Candid service, Wasm export, accepted schema, database execution, or persisted
format.

### Patch 3 Evidence

- Seven focused compiler tests pass. They cover the complete accepted grammar,
  the exact wrapper-token snapshot, one-method-per-declaration cardinality,
  both closed values for every configurable policy, duplicate and unknown
  names, unsupported attributes, malformed syntax, and all option-shape
  failures.
- All 33 actor-builder tests pass, confirming that the staged compiler does not
  disturb the current config-owned actor generator or Patch 2 private-handler
  boundary.
- Warnings-denied all-target Clippy passes for `icydb-model`. The two Patch 3
  Rust files pass direct Rustfmt and repository diff hygiene is clean.
- Source inspection confirms `build::actor` remains gated to non-Wasm builds,
  the endpoint module is private, and the compiler functions are package
  private. There is no `endpoints!` macro, public endpoint module, or public
  re-export in this landing.
- No raw-Wasm measurement is repeated: the new module has no current call site,
  is excluded from Wasm builds, and cannot affect generated actor tokens. The
  Patch 2 measured artifact remains the current line reference.
- Patch 3 adds one approximately 1,000-line private compiler-and-test module
  plus narrow module wiring and status/release evidence. Build-time test and
  validation complexity increases moderately; production source generation,
  runtime complexity, endpoint instructions, and Wasm complexity are
  unchanged.

## Patch 4 — Internal Capability, Authorization, And Fixture Locks

Canonical owners:

- package-private capability and authorization plans beside the closed
  endpoint declarations;
- one package-private token emitter for the future generated controller
  helpers;
- the existing private generated SQL fixture handlers; and
- the public diagnostic-code registry for the new operational-controller
  failure reserved by the accepted design.

Required evidence:

- every declaration maps to the exact `icydb/sql`, `icydb/sql-explain`,
  `icydb/metrics-extended`, and/or canister `test-admin-api` conjunction;
- SQL, operational, schema, and public authorization remain distinct closed
  plans with exact typed `ErrorOrigin::Interface` failures;
- primary-key-only and bounded-deterministic update declarations select only
  their matching private handlers;
- fixture load emits one explicit `fn() -> Result<(), icydb::Error>` coercion;
- fixture load reuses the same private reset handler, propagates its first
  error, then invokes the checked loader exactly once; and
- none of the staged helpers carries an IC method or linker-export attribute.

Public/persisted impact: the public diagnostic registry appends
`RUNTIME_BOUNDARY_OPERATIONAL_SURFACE_CONTROLLER_REQUIRED` as numeric code
241 without renumbering an existing code. No current generated endpoint emits
that error before Patch 5, and no Candid shape, accepted schema, database
execution, or persisted format changes.

### Patch 4 Evidence

- Twelve private declaration tests pass, covering the full feature-conjunction
  matrix, endpoint-specific authorization plans, exact typed helper tokens,
  update-admission selection, and fixture function-item coercion.
- All 39 actor-builder tests pass. The focused fixture lifecycle proof confirms
  reset precedes the loader through the shared private handler, the first
  reset failure propagates through `?`, and no second reset path or handler
  call is present.
- All ten diagnostic-code tests pass with a sequential 1-through-241 registry,
  exact detail reconstruction, and 242 as the first unknown code. The focused
  facade test proves the operational failure is unsupported-class,
  interface-origin, and reconstructs its exact runtime-boundary detail.
- Twenty CLI diagnostic tests pass, including exhaustive rendering of the new
  operational boundary. Warnings-denied all-target Clippy passes for
  `icydb-model`, `icydb-diagnostic-code`, `icydb`, and `icydb-cli`.
- The focused live-canister integrity test passes and proves a non-controller
  caller receives the exact typed SQL-controller error before the maintained
  private handler runs.
- The current SQL performance Wasm is 5,326,164 raw non-gzipped bytes, 16 bytes
  (0.0003%) above the Patch 2 reference, with SHA-256
  `7d575e57a670e413a884f12a5048df74a1d798a52b7b0877c7068d5a6e781d45`.
  Concurrent executor cleanup is present in this worktree, so this is combined
  evidence rather than an isolated Patch 4 attribution; it nevertheless shows
  no material retained-Wasm cost from the staged plans and equivalent fixture
  handler composition.
- Patch 4 changes six code files plus status/release evidence at roughly
  `+330/-15` lines. Build-time planning becomes moderately more
  explicit; the live fixture path is structurally simpler, while database
  execution and endpoint instruction complexity remain neutral.

## Patch 5 — Atomic Endpoint-Authority Hard Cut

Canonical owners:

- `icydb::endpoints!` declarations in each maintained canister source for the
  complete maintained IcyDB public method surface;
- Cargo features owned by each canister for compilation capability and
  local/test-versus-production endpoint selection;
- the one-argument `icydb::build::build_canister!(Canister)` helper for private
  generated capabilities; and
- the code-owned nine-canister build policy and separate local/production
  artifact directories used by repository tooling and integration proof.

Required evidence:

- `start!()` emits lifecycle/private runtime wiring but no public method, and
  source declarations bind to exactly one crate-root start invocation;
- one active declaration emits exactly one fixed Candid method and declarations
  fail for duplicates, unknown names, missing start wiring, nested placement,
  or missing compiled capability;
- all nine maintained actors build under their exact production and local/test
  feature sets, with generated Candid and raw Wasm agreeing on every name and
  query/update mode;
- fixture endpoints require both their source declarations and the canister's
  explicit `test-admin-api` capability;
- TOML discovery, `icydb-config`, build options, target environment overrides,
  implicit endpoint generation, and the complete CLI config surface are
  deleted without aliases or fallback readers; and
- active installation/build/CLI documentation names only the maintained source
  declaration and Cargo-feature model.

Public/persisted impact: this is the accepted pre-1.0 Rust/build/CLI hard cut.
Endpoint presence is now controlled by source declarations and exact Cargo
feature profiles; undeclared methods are absent rather than disabled stubs.
The `icydb config` command and TOML configuration contract are removed. Fixed
endpoint argument/result shapes and database persisted formats do not change.

### Patch 5 Evidence

- The public `endpoints!` grammar exposes the eleven accepted declarations and
  their closed options. `start!()` now emits only lifecycle and private runtime
  wiring, while generated actor code contains private handlers with no IC or
  linker export attribute. Active declarations carry the sole maintained
  generated public wrappers.
- One focused compile-UI contract passes with two maintained positive fixtures
  and five negative cases covering duplicate declarations, missing SQL
  capability, nested placement, unknown declarations, and declarations without
  `start!()`. Twenty-eight focused actor-builder tests pass, including the
  private-handler and one-method-per-declaration invariants.
- The exact built-artifact matrix passes for all nine maintained actors under
  both production and local/test profiles: eighteen generated Candid services
  agree exactly with eighteen raw-Wasm export manifests and the frozen source
  policy. The build took 310.05 seconds and includes actors with no IcyDB
  declarations as negative-space proof.
- Explicit repository entry points now build local/test and production profiles
  with complete feature sets into separate target trees. One focused renamed
  runtime-dependency consumer passes, and the integration library, binaries,
  and tests compile against the new profile model.
- Ninety-six focused CLI tests pass after deleting config command dispatch,
  parsing, discovery, reporting, preflight, build-target inference, environment
  injection, and dependency wiring. Fixed SQL and observability commands call
  deployed method names directly; fixture refresh treats an absent deployed
  fixture method as ordinary method absence.
- Warnings-denied all-target Clippy passes for the facade/model/CLI packages and
  for all nine maintained local/test canister profiles. Both generated-endpoint
  and read-admission invariant scripts pass, Rustfmt has been applied, and diff
  hygiene is clean.
- A focused live-source and dependency search finds no `icydb-config`,
  `icydb.toml`, config command, legacy build option, generated build target, or
  `ICYDB_CONFIG_PATH`/`ICYDB_BUILD_TARGET` authority outside historical design
  and changelog material. Six canister TOML files plus the root TOML file and
  the entire config crate are deleted.
- The current tracked 0.217 build/CLI/documentation scope is approximately 103
  files at `+1,288/-5,726` lines before new proof artifacts. More than 3,800
  deleted lines are the retired config crate and CLI config tree alone. Build
  and test proof becomes more explicit, while production authority and
  installation complexity become materially simpler.
- Raw non-gzipped Wasm cost attribution and released-package-shaped downstream
  installation remain Patch 6 evidence. Patch 5 therefore does not claim the
  final 0.217 cost or release-closeout verdict.

## Patch 6 — Installation, Cost, And Release Closeout

Canonical owners:

- one released-package-shaped downstream canister fixture with a renamed
  `runtime_api` dependency and no IcyDB configuration file;
- the frozen nine-canister production/local artifact policy;
- raw non-gzipped `wasm-release` artifacts built from both `v0.216.5` and the
  final 0.217 worktree; and
- one ignored release probe that installs both baseline and current SQL actors
  and compares their maintained endpoint-local instruction attribution.

Required evidence:

- downstream native, Candid, and Wasm builds succeed using only runtime/build
  dependencies, `build_canister!`, `start!`, and `endpoints!`;
- endpoint declarations may precede `start!`, dependency renaming remains
  supported, and no TOML manifest or target environment selection is needed;
- production actors omit fixture, local SQL-query, and extended-metrics methods
  plus their exclusively enabled capabilities;
- every production/local artifact retains exact Candid/raw-Wasm agreement;
- raw non-gzipped size and focused endpoint instruction deltas are measured
  against the exact release baseline; and
- packaging, Clippy, invariants, stale-symbol searches, formatting, and diff
  hygiene close without adding a compatibility route.

Public/persisted impact: the package-shaped proof found and corrected one
downstream Rust-path assumption so `endpoints!` now follows the same renamed
dependency contract as generated private actor code. No Candid type, endpoint
name/mode, accepted schema, database operation, or persisted format changes in
this landing. The overall 0.217 endpoint-presence and CLI changes remain the
intentional Patch 5 hard cut.

### Patch 6 Evidence

- The downstream fixture depends on the facade as `runtime_api`, invokes
  `endpoints!` before `start!`, generates its actor from the one-argument build
  helper, and contains no IcyDB TOML. Native/Candid check, warnings-denied
  all-target Clippy, and warnings-denied `wasm-release` compilation pass. Its
  raw non-gzipped Wasm is 372,551 bytes with SHA-256
  `4406bd7cda2949ff24bc7bebb46a29fdf85f7d7bf5f2cae1c04143e4c5a7338d`.
- The facade package file listing succeeds and includes the maintained macro UI
  fixtures. `icydb-core` archive preparation succeeds. Facade archive
  preparation correctly waits for same-version publication of `icydb-core`
  because crates.io's 0.216.5 core does not yet contain the new
  `metrics-extended` feature; release tooling must retain core-before-facade
  publication order after the version bump.
- The complete eighteen-artifact Candid/raw-Wasm matrix passes again after the
  renamed-dependency correction in 170.97 seconds. Raw binary scans find none
  of `icydb_query`, fixture methods, extended metrics, or their generated
  handler names in production RPG and SQL actors, while the matching local
  artifacts contain the declared methods.
- The final raw non-gzipped `wasm-release` matrix is:

| Actor | 0.216.5 production | 0.217 production | Delta | 0.216.5 local | 0.217 local | Delta |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `default_empty` | 34,511 | 34,511 | 0 | 34,511 | 34,511 | 0 |
| `default_empty_metrics` | 328,708 | 334,271 | +5,563 | 328,708 | 334,271 | +5,563 |
| `one_entity_sql_query` | 4,180,220 | 4,180,419 | +199 | 4,180,220 | 4,180,419 | +199 |
| `one_entity_typed_query` | 3,763,164 | 3,763,102 | -62 | 3,763,164 | 3,763,102 | -62 |
| `sql_perf` | 5,326,191 | 5,316,766 | -9,425 | 5,326,191 | 5,326,664 | +473 |
| `ten_entity_typed_query` | 3,764,613 | 3,764,551 | -62 | 3,764,613 | 3,764,551 | -62 |
| `sql` | 5,235,135 | 4,810,932 | -424,203 | 5,235,020 | 5,234,406 | -614 |
| `sql_bounded` | 4,836,386 | 4,413,901 | -422,485 | 4,836,271 | 4,835,742 | -529 |
| `demo_rpg` | 4,795,882 | 3,490,292 | -1,305,590 | 4,844,134 | 4,843,684 | -450 |
| **Total** | **32,264,810** | **30,108,745** | **-2,156,065 (-6.6824%)** | **32,312,832** | **32,317,350** | **+4,518 (+0.0140%)** |

- Production `sql_perf` is 5,316,766 raw bytes with SHA-256
  `cbf6708e58624ec35d76d0516a4e7626a2ea504f8c8e3f982d8d6d35b0261ea5`.
  Local/test `sql_perf` is 5,326,664 raw bytes with SHA-256
  `72ef9be4606de0e1b7a996dcbcdb2f1baae08eb76a862901e055be8685acb856`.
  The production reduction is the intended absence of development-only
  capability and endpoint code; the essentially flat local matrix shows the
  source macro does not add material retained runtime cost.
- Thirty-two identical warm SQL endpoint calls report 22,667,776 baseline and
  22,646,944 current local instructions: 708,368 versus 707,717 per call, a
  20,832-instruction total reduction (-0.0919%). The first PocketIC query-stats
  approach was rejected because that harness does not populate replica query
  counters; the retained probe uses IcyDB's maintained endpoint attribution
  instead of treating zero as evidence.
- Warnings-denied Clippy passes for the facade/model/CLI packages, both affected
  integration targets, the downstream fixture, no-feature core, all nine
  local/test canister profiles, and the final warnings-denied downstream Wasm
  build. Endpoint/read-admission invariants, macro UI, active-source hard-cut
  searches, Rustfmt, and diff hygiene pass.
- The tracked baseline-to-worktree diff is approximately 153 files at
  `+1,698/-6,078` lines before new proof artifacts. The release removes more
  than 3,800 lines from the config crate and CLI config tree alone and keeps
  runtime endpoint execution structurally neutral; test and build proof
  becomes more explicit while the product and installation model becomes
  materially simpler.
- Full workspace and push-time suites are intentionally not run by agents under
  repository governance. They remain the release owner's final validation.

## Patch 7 — Query-only Grouped Fluent Activation

Canonical owners:

- `DynamicQuery` for ordered group keys, aggregates, explicit grouped limits,
  page limit, and opaque continuation input;
- the accepted-schema `StructuralQuery`, shared query-plan cache, grouped
  executor, and grouped public-value finalizer for runtime behavior; and
- `GroupedQueryOutput` for the engine-neutral grouped page returned to dynamic
  callers and through the typed entity-binding boundary.

Public/persisted impact: this landing adds dynamic and typed grouped query
methods under the existing `query` feature. Existing SQL, cursor, accepted
schema, persisted data, and response formats do not change. The new grouped
response receives its initial Candid shape; SQL retains its existing response
adapter and envelope.

### Patch 7 Evidence

- Query-only dynamic and typed callers compile without SQL and execute through
  accepted entity authority, the shared plan cache, public admission, the
  existing grouped executor, and the existing public-value conversion.
- Focused runtime coverage proves public filtered grouping, trusted grouping,
  deterministic opaque-cursor continuation, ordered key/aggregate values, and
  rejection of an ambiguous scalar `.select(...)` on grouped output.
- `icydb-core` checks pass with `query`, `query,diagnostics`, and `sql`;
  facade checks pass independently with `query` and `sql`. Warnings-denied
  Clippy passes for the same five host profiles.
- The query-only facade compile contract passes with both dynamic grouped
  terminals and typed `Query<E>::execute_grouped`. The focused Candid shape
  test and accepted-binding grouped runtime regression also pass.
- The one-entity query-only canister compiles for `wasm32-unknown-unknown`
  without SQL or warnings. Its warnings-denied Wasm Clippy probe reaches one
  pre-existing unrelated `integrity::deep::now_nanos` `unnecessary_wraps`
  diagnostic; that integrity cleanup is not folded into this query landing.
- Forced query-only dead-code evidence falls from the previously recorded 678
  diagnostics to 256 because grouped planning and execution now have a real
  non-SQL caller. The remaining inventory is not changed opportunistically.
- The slice changes approximately 24 code files at `+579/-51` lines including
  focused tests, plus active documentation and status. It adds a bounded
  frontend and response contract but no second planner or executor, so public
  API complexity grows while runtime ownership stays simpler than a parallel
  implementation.
- No isolated raw Wasm size or runtime-performance delta is claimed. Existing
  scalar-only consumers can dead-strip the unused grouped terminal; a consumer
  that calls it will retain the already established grouped engine. SQL and
  non-SQL grouped execution use the same algorithmic path.

## Patch 8 — Grouped Facade Hardening

Canonical owners:

- query intent owns scalar/grouped terminal-shape and grouped-output-shape
  misuse;
- the existing query read-admission taxonomy owns absent, zero, and excessive
  grouped execution bounds; and
- the existing cursor planner owns malformed continuation input.

Public/persisted impact: malformed dynamic and typed grouped requests no longer
surface planner-invariant diagnostics. No diagnostic code is added, and no
successful query result, accepted schema, persisted data, cursor, or SQL
response format changes.

### Patch 8 Evidence

- Exact runtime assertions cover scalar/grouped terminal mismatch, scalar
  selection on grouped output, absent and zero group limits, public group
  budget excess, public full-scan rejection, and tampered opaque cursors.
- The one-entity query-only canister executes the real generated
  `Query<OneSimpleEntity01>::execute_grouped()` terminal natively and verifies
  its missing-limit error crosses the facade with the exact structured
  read-admission diagnostic. It enables `query` but not `sql`.
- Empty entity-name requests now flow through the accepted catalog lookup
  instead of being locally mislabeled as an invalid logical-plan invariant.
- The broad query-without-SQL dead-code expectation remains necessary for the
  recorded 256-item dormant inventory, but its active rationale now names only
  SQL-explain, diagnostic, and frontend-extension capabilities. It no longer
  claims grouped planning and execution are dormant.
- Focused checks pass for core with no features, `query,diagnostics`, and
  `sql`, and for the facade with `query` and `sql`. The query-only facade
  trybuild contract and the one-entity query-only Wasm build pass.
- Warnings-denied all-target Clippy passes for query-only core, facade, and the
  one-entity generated typed-query canister. Rustfmt and diff hygiene pass.
- HAVING, typestate builders, SQL response redesign, new diagnostic codes, and
  unrelated integrity lint cleanup remain outside this bounded landing.
- Patch 8 changes nine files at approximately `+325/-63` lines, dominated by
  focused negative/runtime tests and active design/release evidence. Production
  code is approximately net `+5` lines because local invariant branches are
  replaced by shared diagnostics. No raw Wasm or runtime-performance delta is
  claimed; the generated-canister additions are native test-only and query
  execution remains on the existing planner/executor path.

## Final 0.217 Verdict

All six original endpoint-export patches and the separately approved grouped
query activation and hardening follow-ups are demonstrably complete. The
accepted source macro,
Cargo-feature, authorization, build-profile, CLI-removal, installation,
Candid/Wasm, raw-size, instruction-cost, and query-only grouped execution
contracts are implemented without a compatibility path or persisted-format
change. The current landing is ready for the user-owned push workflow.

## 0.217.2 Closeout Cleanup

The post-0.217.1 closeout removes only unreachable or redundant scaffolding:

- `TypedQueryError::Binding` is hard-cut because execution never produced it;
  typed binding remains an error from `DbSession::query`, while execution owns
  only maintained database and row failures.
- Dynamic and SQL grouped execution share accepted-authority,
  execution-family, and cursor preparation, and response finalization no
  longer carries an unused execution trace.
- The unused executor-local row-flow counter module is deleted; it had no
  production caller and supplied no public diagnostic.

The exact eighteen-artifact production/local Candid-versus-Wasm matrix remains
the release and endpoint-closeout proof, but it is no longer launched
recursively by ordinary Cargo workspace test execution. The test is explicitly
ignored at that layer; `make test`, the version-bump release gate, and the
maintained `make test-canister-artifact-contract` target run it deliberately.
Ordinary Cargo tests retain the fast policy completeness, uniqueness, canonical
ordering, ABI, and artifact-parser coverage without spawning eighteen nested
Cargo builds.

Apart from exhaustive Rust matches removing the never-produced
`TypedQueryError::Binding` arm, this changes no canister feature profile,
endpoint declaration, generated artifact, public or persisted response format,
runtime behavior, performance model, or raw Wasm composition. The landing is
net smaller and keeps one grouped-query preparation path rather than parallel
dynamic and SQL setup.
