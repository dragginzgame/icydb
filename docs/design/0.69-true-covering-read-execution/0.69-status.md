# 0.69 Status Tracker

Current state: in progress. `0.69.0` shipped the first true covering-read
execution cohorts.

## Scope

This status tracks `0.69` true covering-read execution against the active
design line.

Design reference:

- `docs/design/0.69-true-covering-read-execution/0.69-design.md`
- `docs/design/0.69-true-covering-read-execution/0.69-secondary-covering-addendum.md`
- `docs/design/0.69-true-covering-read-execution/0.69-secondary-row-authority-addendum.md`
- `docs/design/0.69-true-covering-read-execution/0.69-read-boundary-materialization-addendum.md`

---

## Shipped In `0.69.0`

- [x] S0. Execution-grade covering route contract
  - Introduced planner-owned `CoveringReadExecutionPlan`.
  - Added explicit `CoveringExistingRowMode`.
  - Route and runtime now consume execution-grade covering contracts instead of
    projection-only covering metadata.

- [x] S1. Explain visibility
  - `EXPLAIN EXECUTION` and `EXPLAIN JSON` now surface
    `existing_row_mode`.
  - The shipped route is now observable as an execution contract instead of a
    hidden terminal shortcut.

- [x] S2. First planner-proven cohort
  - PK-only projection over authoritative primary-store `FullScan`.
  - PK-only projection over strict half-open primary-key `KeyRange`.
  - These shapes may skip row-existence checks because the planner can already
    prove row authority from the chosen access path.

- [x] S3. Widened exact PK lookup cohort
  - PK-only `ByKey`.
  - Canonical ascending PK-only `ByKeys`.
  - These shapes now compile through first-class `CoveringRead`, but they stay
    explicit `row_check_required`.

- [x] S4. Outer-surface coverage
  - Core planner, route, session, and executor snapshot coverage are in place.
  - Demo RPG typed/generated coverage is in place.
  - PocketIC query/explain/perf coverage is in place for the first PK-only
    cohort.

- [x] S5. First explicit secondary witness cohorts
  - Added a route-visible `witness_validated` authority mode for narrow
    secondary covering reads.
  - Store/index witness state is now synchronized and invalidated explicitly
    during commit and recovery.
  - The first shipped cohorts are single-field `User.name` covering reads and
    the first filtered composite `ActiveUser(tier, handle)` order-only read.

## Current Admitted Cohorts

### `planner_proven`

- PK-only projection over primary-store `FullScan`
- PK-only projection over strict half-open primary-key `KeyRange`

### `witness_validated`

- `SELECT id, name FROM User ORDER BY name ASC, id ASC LIMIT 2`
- `SELECT id, name FROM User ORDER BY name DESC, id DESC LIMIT 2`
- `SELECT id, name FROM User WHERE name = ... ORDER BY id LIMIT 1`
- `SELECT id, name FROM User WHERE name = ... ORDER BY id DESC LIMIT 1`
- `SELECT id, name FROM User WHERE name >= ... AND name < ... ORDER BY name ASC, id ASC LIMIT 2`
- `SELECT id, name FROM User WHERE name >= ... AND name < ... ORDER BY name DESC, id DESC LIMIT 2`
- `SELECT id, tier, handle FROM ActiveUser WHERE active = true AND tier = ... ORDER BY handle ASC, id ASC LIMIT 2`

### `row_check_required`

- broader secondary/index-backed covering routes carried forward from `0.68`
- PK-only `ByKey`
- canonical ascending PK-only `ByKeys`

## Explicit Fail-Closed Boundaries

- `ByKeys` plus descending PK order is still not admitted
- broader secondary/index covering routes are not planner-proven yet
- filtered composite descending `ORDER BY handle DESC, id DESC` still fails
  closed to the materialized route
- non-PK primary-store projections are outside the first honest cohort
- grouped, aggregate, and broad DISTINCT execution are still outside `0.69`

## What `0.69.0` Proved

- covering-read admission can now be a real execution route instead of planner
  metadata only
- row-presence semantics can stay explicit at the planner boundary instead of
  being implied by runtime shortcuts
- the first planner-proven scalar cohort is narrow but already measurable
- explicit secondary authority can now be shipped without silently reinterpreting
  all secondary covering reads as planner-proven
- the same witness contract can widen across adjacent cohorts while staying
  centralized in one route classifier
- widening can keep following the same rule: admit only what the planner can
  state explicitly, fail closed otherwise

## Next Decisions

- keep treating current secondary `planner_proven` widening as closed under
  the current storage contract unless a stronger authority proof appears
- decide whether the current explicit witness family is enough for `0.69.0`
  or whether one more adjacent filtered/composite cohort is still worth it
- decide whether exact PK reverse-order multi-lookup can be admitted under one
  planner-owned reorder contract
- keep widening only when the planner can state row-authority semantics
  directly; do not recover eligibility in executor-local code
- follow the secondary-covering addendum guidance:
  - keep row presence checks executor-owned
  - keep single-component secondary fast paths narrow
  - treat row-check cost, not generic materialization churn, as the next major
    secondary-covering lever
  - treat expression key-only prefix/range losses as a runtime materialization
    problem now that route/optimization parity is locked
  - treat the storage-key-only scan bypass as investigated and closed for the
    current expression key-only cohort unless a materially different runtime
    materializer accompanies it
- follow the secondary row-authority addendum guidance:
  - use one explicit authority mode, not heuristics
  - keep looking for step-function SQL drops instead of more marginal local
    cleanup
- follow the read-boundary/materialization addendum guidance:
  - preserve the `0.65.1` structural read hard-cut
  - treat the next read-side design as validate-all plus lazy materialization,
    not a return to best-effort lazy reads
  - take the lower-risk slices first:
    - index-entry streaming decode
    - SQL-only covering direct cell render
  - `StructuralSlotReader` now has the first validate-all plus lazy
    non-scalar materialization split under the same strict row-open
    validation contract
  - the first representative row-backed `CustomerOrder ORDER BY id` cohort is
    now measured on generated and typed dispatch surfaces
  - the earlier `data_rows` fallback suspicion is now closed for that cohort;
    the row-backed SQL lane already stayed on retained `slot_rows`
  - the retained-slot-row executor path is now narrowed above the validated
    reader boundary, so `SlotsOnly` means projected/predicate/order-needed
    slots instead of full slot images
  - the same `CustomerOrder ORDER BY id` cohort now reports
    `full_row_decode_materializations = 0`
  - best-case `SELECT id, name ...` now reports
    `materialized_non_scalar_slots / validated_non_scalar_slots = 0.0`
    and `rows_without_lazy_non_scalar_materializations / rows_opened = 1.0`
    with same-query PocketIC drops of:
    - generated `707,891 -> 640,358` (`-67,533`, `-9.54%`)
    - typed `705,676 -> 636,360` (`-69,316`, `-9.82%`)
  - mixed-case `SELECT id, name, labels ...` now reports
    `materialized_non_scalar_slots / validated_non_scalar_slots = 0.5`
    with same-query PocketIC drops of:
    - generated `731,139 -> 684,337` (`-46,802`, `-6.40%`)
    - typed `728,213 -> 682,963` (`-45,250`, `-6.21%`)
  - ceiling queries that touch every deferred field are now the regression
    guard:
    - generated `798,817 -> 827,546` (`+28,729`, `+3.60%`)
    - typed `796,561 -> 825,827` (`+29,266`, `+3.67%`)
  - the first follow-up cache/layout slice is now measured too:
    - generated best-case `640,358 -> 639,748` (`-610`, `-0.10%`)
    - generated mixed-case `684,337 -> 684,474` (`+137`, `+0.02%`)
    - generated ceiling `827,546 -> 832,509` (`+4,963`, `+0.60%`)
    - typed best-case `636,360 -> 636,642` (`+282`, `+0.04%`)
    - typed mixed-case `682,963 -> 681,225` (`-1,738`, `-0.25%`)
    - typed ceiling `825,827 -> 830,301` (`+4,474`, `+0.54%`)
  - the compact retained-slot sparse row-carrier follow-up is now
    investigated and rejected:
    - generated best-case `639,748 -> 642,124` (`+2,376`, `+0.37%`)
    - generated mixed-case `684,474 -> 686,405` (`+1,931`, `+0.28%`)
    - generated ceiling `832,509 -> 833,043` (`+534`, `+0.06%`)
    - typed best-case `636,642 -> 639,667` (`+3,025`, `+0.48%`)
    - typed mixed-case `681,225 -> 684,030` (`+2,805`, `+0.41%`)
    - typed ceiling `830,301 -> 830,278` (`-23`, effectively flat)
  - the design takeaway is now explicit:
    - retained-slot narrowing changed what work happens
    - cached required-slot layout changed where the slot set is owned
    - compact sparse-row assembly only changed how the same work is packaged
  - the direct projected-row row-backed SQL follow-up is now investigated and
    rejected too:
    - generated best-case `639,748 -> 639,942` (`+194`, `+0.03%`)
    - generated mixed-case `684,474 -> 687,667` (`+3,193`, `+0.47%`)
    - generated ceiling `832,509 -> 830,669` (`-1,840`, `-0.22%`)
    - typed best-case `636,642 -> 637,858` (`+1,216`, `+0.19%`)
    - typed mixed-case `681,225 -> 683,953` (`+2,728`, `+0.40%`)
    - typed ceiling `830,301 -> 828,507` (`-1,794`, `-0.22%`)
    - it did move the local path shape to `projected_rows_path_hits = 1` and
      `slot_rows_path_hits = 0`, but that still was not enough to justify
      another terminal short-path branch
  - so the next step is no longer boundary surgery, more slot-mask work, or
    this compact sparse-row carrier or the direct projected-row lane; it needs
    a materially different ceiling reduction idea above the validated reader
    boundary

## Validation Snapshot

- `cargo clippy -p icydb-core --lib --tests -- -D warnings`
- `cargo test -p icydb-core --lib -- --nocapture`
- `cargo clippy -p canister_demo_rpg --tests -- -D warnings`
- `cargo clippy -p icydb-testing-integration --test sql_canister -- -D warnings`
