# 0.144 Structural Ownership Audit Status

## Current Status

Proposed. No structural patches have started under this design yet.

## Audit States

Use these states consistently:

* `Not started`: no ownership review yet
* `Auditing`: reading imports, exports, file sizes, and caller paths
* `Patch ready`: safe structural move identified
* `Patched`: structural cleanup applied and validated
* `Deferred`: issue found but not safe for this slice
* `Leave alone`: current structure is intentional

## Module Audit Tracker

Audit in this order so upstream ownership decisions settle before downstream
implementation modules are moved.

| Area | State | Owner | Public Surface | Internal Modules | Action |
|---|---|---|---|---|---|
| `db/query` | Not started | TBD | TBD | TBD | Check semantics, identity, cache keys, fluent/internal builders, and expression pipeline boundaries. |
| `db/sql` | Not started | TBD | TBD | TBD | Check aggregate, projection, write execution, explain, and scalar-function ownership. |
| `db/session` | Not started | TBD | TBD | TBD | Check orchestration/cache boundaries, stale direct planner paths, and compatibility helpers. |
| `db/executor` | Not started | TBD | TBD | TBD | Check scalar, grouped, aggregate, delete, projection, and terminal execution ownership. |
| `db/data` | Not started | TBD | TBD | TBD | Check persistence, structural fields, persisted row codec split, and storage helpers. |
| `db/index` | Not started | TBD | TBD | TBD | Check storage, planning, envelopes, continuation traversal, and post-0.34 consolidation notes. |
| `db/model` | Not started | TBD | TBD | TBD | Check model metadata and schema-facing invariants for query/executor leakage. |
| `value` | Not started | TBD | TBD | TBD | Audit value semantics, storage keys, maps, ordering, and conversions before merging current split work. |
| `icydb-schema` | Not started | TBD | TBD | TBD | Check schema AST, validation, and build-facing type boundaries. |
| `icydb-schema-derive` | Not started | TBD | TBD | TBD | Check macro parsing, validation, generated output, and fixture leakage. |
| `icydb-build` | Not started | TBD | TBD | TBD | Check build glue, canister glue, and generated module assumptions. |
| test harnesses | Not started | TBD | TBD | TBD | Check PocketIC, macro tests, SQL perf audit, and wasm audit harness import surfaces. |
| docs/scripts | Not started | TBD | TBD | TBD | Update only when module moves make old references misleading. |

For each row:

* `Owner` names the domain responsibility the module owns.
* `Public Surface` lists supported imports or root re-exports.
* `Internal Modules` lists submodules that external callers should not import.
* `Action` records the concrete move, split, merge, delete, defer, or leave-alone decision.

## Findings

Add findings here as they are confirmed. Keep each finding concrete:

```text
Area:
Owner:
Allowed imports:
Internal-only modules:
Finding:
Classification: must fix / safe cleanup / leave alone / defer
Minimal patch:
Validation:
```

## Patch Log

No patches yet.

## Deferred Work

No deferred items yet.

## Validation Log

No validation run yet for this design slice.
