# 0.199.0 Direct Query Boundary Trace

Status: 0.199.0 audit and cleanup artifact.

Verdict: direct `Query<E>` execution remains necessary for generated, SQL,
diagnostic, and performance harness code, but it should not appear as a normal
public endpoint recipe. The 0.199.0 cleanup hides fluent `.query()` extraction
methods from public docs, keeps direct execution doc-hidden, and updates the
public facade reference to teach semantic terminals first.

## Scope

Inspected files:

- `crates/icydb/src/db/query/mod.rs`
- `crates/icydb/src/db/session/load.rs`
- `crates/icydb/src/db/session/delete.rs`
- `crates/icydb/src/db/session/mod.rs`
- `crates/icydb-core/src/db/query/fluent/load/builder.rs`
- `crates/icydb-core/src/db/query/fluent/load/partial_window.rs`
- `crates/icydb-core/src/db/query/fluent/delete.rs`
- `canisters/audit/sql_perf/src/lib.rs`
- `docs/guides/public-facade-api.md`
- `docs/audits/0.198-closeout/icydb-0.198-closeout-audit-2026-07-08.md`

## Boundary Summary

| Surface | Current status | Risk before cleanup | 0.199.0 action | Remaining risk |
| --- | --- | --- | --- | --- |
| `Query<E>` re-export | `#[doc(hidden)]` in `crates/icydb/src/db/query/mod.rs`. | Low-level direct-query type could be discovered and used as ordinary endpoint API. | No behavior change; keep hidden. | Low. Still public for internal/generated use. |
| `DbSession::execute_query(&Query<E>)` | `#[doc(hidden)]`; runs default read admission before execution. | Hidden but callable; could bypass semantic terminal vocabulary if discovered. | No behavior change; public facade docs classify as Tier 4 internal/generated. | Low/medium. Keep hidden and tested through generated/internal call sites. |
| `DbSession::trace_query(&Query<E>)` | `#[doc(hidden)]`; diagnostic only. | Low. | No behavior change. | Low. |
| `DbSession::execute_query_result_with_attribution(&Query<E>)` | `#[doc(hidden)]` and diagnostics-gated. | Low; heavily used by perf harness. | No behavior change. | Low. |
| Fluent `.query()` extraction | Previously visible in public/core fluent docs while returning a hidden type. | Medium: this made direct-query extraction look like a normal diagnostic command. | Added `#[doc(hidden)]` to load, partial-window, delete, and delete-returning query extraction methods in `icydb` and `icydb-core`; removed `.query()` from the public facade diagnostic command list. | Low. Still callable for generated/perf/internal code. |
| SQL/perf harness use | Perf matrix uses `.query()` and direct attribution helpers intentionally. | Low if isolated. | No behavior change. | Low. Keep as internal harness surface. |

## What Passed

- Direct execution still runs default read admission through
  `ensure_default_query_read_admission(query)` before execution.
- Public docs now teach `load::<E>()` semantic terminals instead of direct
  `Query<E>` construction.
- The `Query<E>` re-export and direct execution methods are doc-hidden.
- Existing perf harness call sites can continue using the hidden extraction
  method without forcing a compatibility wrapper.

## Finding

### 199-DQ-001: Direct Query Is Internal, But Needs Hidden Extraction Too

Category: public API hygiene / generated boundary

Severity: Low after cleanup

Priority: P2

Confidence: High

Evidence:

- `crates/icydb/src/db/query/mod.rs` re-exports `Query` as `#[doc(hidden)]`.
- `crates/icydb/src/db/session/mod.rs` marks `execute_query(...)`,
  `trace_query(...)`, and diagnostics attribution direct-query helpers as
  hidden.
- `canisters/audit/sql_perf/src/lib.rs` intentionally uses `.query()` and
  direct attribution helpers for performance measurement.
- Before this cleanup, public/core fluent `.query()` methods were not hidden.

Recommended policy:

- Keep direct query surfaces callable but doc-hidden until/unless generated
  infrastructure can avoid them entirely.
- Do not add compatibility aliases or a public policy-builder layer around
  direct queries.
- Do not teach direct query construction in README, public facade docs, or
  read-intent docs.
- If a future generated path needs direct query execution, keep that path
  generated/internal and covered by focused compile checks.

Validation run:

- `cargo fmt --all`
- `cargo test -p icydb --lib --all-features`
- `cargo check -p canister_audit_sql_perf`
- `cargo clippy -p icydb-core --all-features --all-targets -- -D warnings`

## Validation Notes

No behavior, read admission, cursor, SQL, or persisted-format changes are
intended.
