# IcyDB Persisted Format Inventory

This inventory names the durable byte surfaces that must be reviewed when a
change touches IcyDB stable-memory compatibility.

It complements `docs/contracts/PERSISTED_FORMAT_POLICY.md`. The policy defines
how changes are classified; this inventory defines which surfaces are in scope
for that classification.

## Scope

This document is a review checklist, not a byte-level layout specification.
The source modules listed below remain the authority for exact encodings,
bounds, and validation behavior.

Every row in this inventory follows the default pre-`1.0.0` posture: one
current internal form, unknown or retired versions fail closed, and no legacy
fallback is retained. Most frames remain version 1; the commit marker is
explicitly version 3. The commit control slot and journal batch are also
version-3 hard cuts; the authenticated scalar cursor and startup failure
receipt remain separate sole-current version-2 hard cuts documented below.

## Active Durable Surfaces

| Surface | Owner Modules | Durable Role | Current Posture |
| --- | --- | --- | --- |
| Stable-memory allocation roles | `crates/icydb-core/src/traits/mod.rs`, `crates/icydb-model/src/node/canister.rs` | Assigns the commit and integrity-progress memory domains plus each journaled store's data, index, schema, and journal memories. `icydb-model` is the sole application-authoring owner. | Stable identity contract; every role is collision-checked and remapping is outside the durability guarantee. |
| Database format gate and live accepted checkpoint | `crates/icydb-core/src/db/database_format/mod.rs`, `crates/icydb-core/src/db/schema/live_schema_checkpoint.rs`, `crates/icydb-core/src/db/schema/migration_lineage.rs`, `crates/icydb-core/src/db/schema/migration_record.rs` | Admits one database-control identity before recovery; durably retains exact accepted candidates for schema allocations without local history; and owns the database-scoped source-lineage and migration lifecycle records. | One current version-1 boot identity and current bounded, checksummed `ICYSLIVE`, `ICYSLIN1`, and `ICYSMIG1` frames. The migration record is capped at 2 MiB, binds exact database/head/proposal/plan identities plus phase, accepted-ID cursors, counters, staged-generation ownership, and bounded typed findings. Transform findings additionally retain optional source, required target, and one closed typed failure reason. Malformed, noncanonical, unsupported, or retired forms fail closed without reconstruction. |
| Commit control slot, database incarnation, and cursor authentication | `crates/icydb-core/src/db/integrity.rs`, `crates/icydb-core/src/db/commit/store/control_slot.rs` | Durable nonzero database-incarnation identity, nonzero 32-byte scalar-cursor authentication key, database-wide commit-sequence high-water, bounded active-plus-retired store-allocation registry, and the presence envelope for pending commit-marker bytes. The bounded marker region occupies the first 257 Wasm pages of the database-control allocation; schema-application records use a disjoint restricted region after it. | Sole current version-3 canonical envelope. Ordinary marker publication advances one database sequence and clear/recovery/reopen preserve the incarnation, cursor key, high-water, and registry; an empty marker payload means no marker. Fresh installation initializes current authority atomically. The only predecessor inspection accepts an exact empty version-2 control during the quiesced empty-tail activation cut; it is not an ordinary marker decoder or retained-debt compatibility route. Version 1, zero authority, malformed bytes, nonempty predecessor state, and unsupported versions fail closed without synthesis or fallback. |
| Startup failure receipts | `crates/icydb-core/src/db/startup/receipt.rs`, `crates/icydb-core/src/db/startup/driver.rs`, `crates/icydb-core/src/db/startup/observe.rs` | Bounded terminal startup-failure authority for database-control, journal-recovery, and schema-reconciliation corruption. | Sole current `ICYSUP01` version-2 envelope in the 2,048-byte startup control cell, with a maximum current encoding of 927 bytes. Journal failures bind the database incarnation, exact store allocation, and journal-tail proof without record-level cursor state. Version 1, malformed bytes, unknown bindings, proof drift, and unsupported versions fail closed without fallback or compatibility decoding. |
| Integrity inspection, generic resumable progress, and mutation-job progress | `crates/icydb-core/src/db/integrity/job.rs`, `crates/icydb-core/src/db/integrity/progress_codec.rs`, `crates/icydb-core/src/db/integrity/progress_store.rs`, `crates/icydb-core/src/db/resumable_job.rs`, `crates/icydb-core/src/db/mutation_job.rs` | Separately persists bounded Deep checkpoints, application-owned revision-bound job state, and durable mutation-job intent/continuation/lifecycle/receipt state. | One current version-1 allocation header. Deep jobs retain the checksummed `ICYIJPTH` version-2 envelope; generic resumable and mutation jobs use distinct checksummed `ICYRJOB1`/`ICYMJOB1` version-1 envelopes and key domains. All families share the 512 KiB record and 64-record global ceiling. Mutation records are capped at 64 KiB and marker-owned replacement binds the hashed key, job identity, expected sequence, SHA-256 before digest, and exact current before/after envelopes. Unknown versions, malformed headers, corrupt checksums, invalid state/receipt closure, wrong identities, and neither-side replacement state fail closed without fallback. |
| Commit marker payload | `crates/icydb-core/src/db/commit/marker.rs`, `crates/icydb-core/src/db/commit/store/marker_envelope.rs` | Marker-bound journal publication and atomic database-control recovery authority. | Sole current envelope version 3 contains bounded journal batches plus at most four canonically ordered exact compare-and-replace effects for schema-application, source-lineage, migration-progress, and at most one mutation-job progress successor under the existing 16 MiB ceiling. A maximum current mutation replacement contributes exactly 37,797 payload bytes. Unknown or retired versions fail closed without a compatibility decoder. |
| Schema-application records | `crates/icydb-core/src/db/schema/application_receipt.rs`, `crates/icydb-core/src/db/schema/application_store.rs` | Database-wide proposal idempotency, pending/applied/aborted receipts, and the exact generated row-local constraint identity owned by a pending schema-change job. | One current version-1 checksummed header and record identity in a restricted database-control BTreeMap region. Records are capped at 64 KiB and the store at 64 retained submissions; pending evidence is never evicted, while the lowest stable-key terminal receipt is recycled at capacity. Impossible pending/terminal closure, noncanonical bytes, retired magic, key/receipt mismatch, checksum failure, and compare-and-replace drift fail closed. Abort is acknowledgement-bound and marker-atomic with accepted activation/source-binding and validation-job removal. No prior record shape, alias, or fallback exists. |
| Journal tail batches and sequences | `crates/icydb-core/src/db/journal/codec.rs`, `crates/icydb-core/src/db/journal/store.rs` | Marker-bound row/schema/constraint-validation replay records, one aggregated Identity range per participating owner, exact offline-migration row/index effects, owner-local fold order, and database-wide commit order. | Sole current version-3 bounded batch payload stored under ordered sequence/chunk keys. Its 93-byte fixed `IJBT` envelope binds payload length, batch ID, marker ID, owner-local sequence, database commit sequence, record count, and a domain-separated SHA-256 fingerprint over the complete canonical envelope outside the fingerprint slot. Decode verifies the fingerprint before materializing records; recovery then completes owner-local read-only validation before same-message apply, watermark/control publication, and retirement. Identity ranges carry exact owner, expected/new u128 high-water, and redundant nonzero `u32` count. Constraint-validation and migration records retain their exact current owner bindings. Versions 1 and 2, fingerprint mismatch, malformed bytes, unknown tags, and unsupported versions fail closed without a retained-tail compatibility decoder; version-2 upgrade is admitted only when every predecessor tail is empty. |
| Journal convergence aggregate controls | `crates/icydb-core/src/db/journal/store.rs` | Exact owner-local retained batch count, record count, encoded envelope bytes, and oldest retained database commit sequence. | One current version-1 fixed control under the existing journal allocation. Append adds the exact immutable version-3 envelope contribution, identical replay proves bounded control/head agreement, and complete-batch retirement subtracts the verified selected contribution while advancing the head. Missing current controls, malformed values, overflow, underflow, impossible empty/nonempty closure, and head drift fail closed; routine startup does not claim a history-sized aggregate recomputation. |
| Fold watermark | `crates/icydb-core/src/db/journal/store.rs`, `crates/icydb-core/src/db/commit/recovery.rs` | Records journal-tail fold progress for recovery/reentry. | Sole durable recovery-progress authority. Recovery advances the watermark only after one complete batch validates and applies in the same replicated message, then stops only between batches. No per-record cursor or replacement progress state is retained. Sequence-zero controls accept only the current watermark, data revision, access revision, and journal convergence aggregate records; removed or unknown controls fail closed. |
| Data-mutation revision | `crates/icydb-core/src/db/journal/store.rs` | Stable revision authority for resumable proofs whose validity changes only when row bytes mutate. | One current version-1 control entry stores the highest row-mutation journal sequence. Ordinary and offline-migration row records advance it; schema, index-only, and validation-job batches do not. Malformed or exhausted authority fails closed. |
| Physical access-state revision | `crates/icydb-core/src/db/journal/store.rs`, `crates/icydb-core/src/db/index/store.rs` | Per-store revision authority for index readiness/generation transitions that can alter physical traversal without changing row bytes. | Journaled stores retain one current version-1 control entry; heap stores retain the same monotonic nonzero authority in memory only. Actual `Building`/`Ready` transitions advance it, unchanged-state publication does not, and malformed or exhausted durable authority fails closed. |
| Raw data-store keys | `crates/icydb-core/src/db/data/store.rs`, `crates/icydb-core/src/db/data/structural_field/primary_key_component/` | Primary-key identity for row storage and commit/journal records. | Structural key encoding is persisted identity and must remain accepted-schema backed. |
| Raw row envelopes | `crates/icydb-core/src/db/codec/mod.rs`, `crates/icydb-core/src/db/data/row.rs`, `crates/icydb-core/src/db/data/structural_row.rs` | Canonical row payload bytes in stable data stores. | The sole current row envelope carries a non-zero entity-local row-layout stamp. Accepted decode requires an admitted stamp and its exact physical slot count before field traversal; unstamped, old-current-form, short-current, long, zero, future, and below-floor rows fail closed. |
| Structural field payloads | `crates/icydb-core/src/db/data/structural_field/` | Per-field persisted payloads inside canonical rows and keys. | Structural Binary v1 plus accepted-field decode authority; malformed payloads fail closed. `Date` retains its `i32` epoch-day bytes, but the current decoder accepts only proleptic Gregorian years 0000 through 9999; out-of-domain development values fail closed without fallback. |
| Value-storage payloads | `crates/icydb-core/src/db/data/structural_field/value_storage/` | Recursive canonical storage for accepted enum and exact-composite values that cannot use scalar fast paths. | Local extension tags, bounded recursion, bounded allocation, exact accepted-kind validation, and fallible decode. It is not a heterogeneous public field contract. |
| Accepted schema snapshots | `crates/icydb-core/src/db/schema/codec.rs`, `crates/icydb-core/src/db/schema/codec/`, `crates/icydb-core/src/db/schema/snapshot.rs`, `crates/icydb-core/src/db/schema/source_binding.rs`, `crates/icydb-core/src/db/schema/store.rs`, `crates/icydb-core/src/db/schema/composite_catalog.rs` | Runtime schema authority for row layout, exact field contracts, nominal enum/composite catalogs, immutable source-to-accepted identity bindings, logical structural identities, indexes, relations, and the structural constraint registry. | One current direct bounded binary mapping with magic `ICYUSNP\0`, format version 1, explicit numeric tags, and a 512 KiB ceiling. It encodes accepted domain accessors directly through the shared schema reader/writer; it has no Candid/Serde wire DTO graph, reflected names, or alternate decoder. Counts, strings, payloads, check trees, and recursive field kinds are bounded before allocation; unknown tags, invalid identities, trailing bytes, noncanonical accepted closure, and obsolete Candid snapshots fail closed. Current accepted bundle/root, enum catalog, composite catalog, and source-binding envelopes retain hard-cut magic identities and version 1. Source bindings are capped at 2 MiB, canonically ordered, one-to-one within each structural namespace, closed over the same accepted bundle, and included in its semantic fingerprint. A bound enum type closes over a type-scoped immutable source binding for every accepted variant. A bound record composite likewise closes over a type-scoped immutable source binding for every stable nonzero member identity. Editable variant and member names are therefore never identity authority. Every entity carries the accepted constraint catalog, live activation records, planner-invisible candidate index and relation owners, and non-reusing allocator, plus current/history-floor layout versions, stable logical index and relation IDs, bounded canonical accepted check expressions, root-field-plus-nominal-type targeted rules, and per-field introduction layout, future insert-default, and frozen historical-fill facts. A targeted-rule semantic edit needs no new wire field: one exact generated activation may share the accepted rule's ID, name, origin, and target while carrying a different operation. The paired accepted operation stays authoritative until promotion, and abort retains it. Targeted rules retain accepted field/type IDs and canonical exact operands for length range, numeric minimum, numeric maximum, numeric range, and integer/decimal multiple-of; they validate reachability through finite catalog traversal and participate in the bundle fingerprint. These identities are fingerprinted contract facts; non-current forms do not decode, and generated models are proposal-only rather than runtime authority. Dense physical index ordinals and isolated candidate index/relation generations remain separate from logical identity. Development stores encoded with the retired Candid `ICYU` graph or older `ICYT` profile must be recreated, with no fallback decoder or translation path. |
| Identity operational state | `crates/icydb-core/src/db/schema/identity_state.rs`, `crates/icydb-core/src/db/schema/store.rs` | Retains active and retired accepted Identity ownership, exact unsigned kind, materialized u128 high-water, and last-applied journal-envelope identity. | One current fixed-width 104-byte checksummed record per lifetime owner in schema-control namespace 4. The database-wide active-plus-retired inventory is capped at 65,536 records. Missing active state, owner/kind/incarnation drift, malformed bytes, nonzero state without exact last-applied evidence, stale replay identity, and materialized state beyond marker-committed state fail as Identity corruption. Removal retires rather than deletes the record; no absent-as-zero, MAX-row reconstruction, prior Sequence decoder, or compatibility fallback exists. |
| Constraint-validation jobs | `crates/icydb-core/src/db/schema/constraint_validation.rs`, `crates/icydb-core/src/db/schema/store.rs` | Bounded durable Forward/Verify progress, revision proof, checkpoints, counters, and one replayable finding receipt for an exact accepted activation. | One current version-1 `ICJB` job profile. Targeted-rule findings retain a bounded typed accepted-ID/ordinal/index path bound to the activation root; noncurrent profiles are not decoded. Every `Validating` activation has exactly one identity/fingerprint-bound job, `EnforcingNewWrites` has none, orphan or malformed jobs fail closed, and promotion/abort removes the job through the same marker as accepted-schema publication. |
| Secondary-index keys | `crates/icydb-core/src/db/index/key/codec/`, `crates/icydb-core/src/db/index/key/ordered/`, `crates/icydb-core/src/db/index/envelope/` | Ordered materialized index identity and scan boundaries. | One current bounded raw-key shape carries entity tag, dense physical ordinal, exact physical generation, ordered index components, and row identity. Generation `0` is the accepted live domain; nonzero activation generations remain planner-invisible until promotion. Pre-generation development keys are not decoded or translated. |
| Secondary-index entries | `crates/icydb-core/src/db/index/entry.rs`, `crates/icydb-core/src/db/index/store.rs` | Row-existence witness for each materialized secondary key. | Bounded one-byte witness payload; stale entries are repaired by guarded recovery/rebuild. |
| Reverse-relation keys | `crates/icydb-core/src/db/relation/reverse_index.rs`, `crates/icydb-core/src/db/index/key/codec/` | Target-to-source membership for accepted relation edges and isolated candidate generations. | Bounded system-index keys carry source entity, accepted relation ordinal, exact physical generation, target identity, and source identity. Candidate generations remain delete-invisible until marker-owned promotion; malformed keys fail closed and recovery rebuilds them from authoritative rows plus the durable validation checkpoint. |
| Cursor tokens | `crates/icydb-core/src/db/cursor/token/codec.rs`, `crates/icydb-core/src/db/cursor/token/scalar.rs`, `crates/icydb-core/src/db/cursor/token/value.rs`, `crates/icydb-core/src/db/cursor/validated.rs` | User-visible continuation bytes that may cross call boundaries. | Grouped tokens retain the sole current version-1 bounded wire. Scalar live/exhaustive pages use the sole current version-2 authenticated bounded wire and bind mode, database incarnation, accepted root, query/window/page contracts, complete mixed-direction order semantics, and logical/physical/lookahead progress. Exhaustive signatures additionally bind the complete canonical source-proof bytes supplied beside the token. Retired scalar tokens, wrong-key or modified bytes, oversized values, and unknown versions fail closed without a compatibility decoder. `Date` boundary values retain the `i32` epoch-day representation and reject years outside 0000 through 9999. |

## Review Rule

Any change touching an owner module above must answer these questions before it
lands:

- Does this change alter durable bytes, durable key ordering, durable memory
  identity, or user-visible continuation bytes?
- If yes, which classification from
  `docs/contracts/PERSISTED_FORMAT_POLICY.md` applies?
- Does the change need malformed-input coverage, compatibility tests, migration
  ordering, or explicit pre-`1.0.0` hard-cut release notes?
- Does the change affect backup/import, checksum, recovery-size, or streaming
  recovery claims?

If the answer is "no persisted-format change", the design, changelog, or
evidence note should say so when the surrounding work touches commit, recovery,
row, schema, index, cursor, or structural-value code.

## Non-Surfaces

The following are not persisted-format surfaces by themselves:

- in-heap query caches and compiled command caches;
- runtime execution plans;
- SQL EXPLAIN rendering strings;
- diagnostics-only counters;
- test-only failpoint names and injected failure sites;
- generated model descriptors, except where they allocate stable-memory IDs or
  propose schema that later becomes an accepted schema snapshot.
- `SchemaFragment`, `SchemaProposal`, and `SchemaMigrationPlan` current-form
  tagged-V1 binary artifacts. They are bounded submission and fingerprint
  preimage contracts rather than retained stable-memory authority. Their
  decoder allocates only after checking artifact, collection, string, literal,
  and nesting bounds. Proposal contract version 2 remains a semantic field
  inside the V1 artifact envelope; obsolete artifact bytes are not decoded,
  translated, or retained through a compatibility shape.
