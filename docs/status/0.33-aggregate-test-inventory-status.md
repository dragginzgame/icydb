# 0.33 Aggregate Test Inventory Classification (as of 2026-02-28)

## Source

- module root: `crates/icydb-core/src/db/executor/tests/aggregate.rs`
- module children: `crates/icydb-core/src/db/executor/tests/aggregate/*.rs`

## Current Totals

- total aggregate tests: `106`

## Reproducible Extraction

Count all aggregate tests:

`{ awk 'BEGIN{test=0} /^#\[test\]/{test=1;next} test && /^fn /{name=$2; sub(/\(.*/,"",name); print name; test=0}' crates/icydb-core/src/db/executor/tests/aggregate.rs; awk 'BEGIN{test=0} /^#\[test\]/{test=1;next} test && /^fn /{name=$2; sub(/\(.*/,"",name); print name; test=0}' crates/icydb-core/src/db/executor/tests/aggregate/*.rs; } | sort -u | wc -l`

List all aggregate tests:

`{ awk 'BEGIN{test=0} /^#\[test\]/{test=1;next} test && /^fn /{name=$2; sub(/\(.*/,"",name); print name; test=0}' crates/icydb-core/src/db/executor/tests/aggregate.rs; awk 'BEGIN{test=0} /^#\[test\]/{test=1;next} test && /^fn /{name=$2; sub(/\(.*/,"",name); print name; test=0}' crates/icydb-core/src/db/executor/tests/aggregate/*.rs; } | sort -u`

## Note

The previous detailed `contract`/`path_parity`/`regression` mapping became stale after aggressive matrix consolidation. Refresh classification from the extracted list before using category counts for planning.
