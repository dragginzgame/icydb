# 0.37 Status (as of 2026-03-01)

## 0.37 Scope

This status tracks aggregate fluent API consolidation planning for `0.37.x`.

Design reference:

- `docs/design/0.37-aggregate-fluent-api-consolidation.md`

Migration reference:

- `docs/migration/0.37-aggregate-builder-hard-cut.md`

---

## In Scope (`0.37.x`)

- composable aggregate builder surface
- fluent aggregate entrypoint consolidation
- removal of combinatorial grouped aggregate helper terminals (hard cut)
- builder-shape/fingerprint/continuation regression locking
- release-doc synchronization for migration guidance

## Out of Scope (`0.37.x`)

- window aggregate DSL
- continuation token shape/version changes
- storage-layer aggregate pushdown
- unrelated grouped semantic expansion

---

## Release Snapshot (`0.37.x`)

- [x] `0.37.0` started
- [x] design doc drafted
- [x] status tracker created
- [x] grouped contract-freeze regression locks carried into `0.37.0` baseline
- [x] `0.37.1` hardening pass landed (typed validation parity + hard-cut compile-fail guards + migration/changelog sync)
- [x] `0.37.2` resource-audit follow-up landed (grouped strategy/runtime relabel + materialized DISTINCT helper consolidation)

Current `0.37.x` status: **0.37 complete for API consolidation scope
(builder-only hard cut, strict parity/resource hardening, and migration-facing
docs are landed).**

---

## Phase Tracker

### Phase 0 - Design Baseline

- [x] design doc drafted
- [x] status tracker created
- [x] grouped explain/cursor freeze regression tests landed as `0.37.0` baseline hardening

### Phase 1 - Builder Surface

- [x] aggregate builder types landed (`AggregateExpr`, `count`, `count_by`, `sum`, `exists`, `first`, `last`, `min`, `max`, `min_by`, `max_by`, `distinct`)
- [x] fluent aggregate entrypoint(s) landed (`Query::aggregate`, `FluentLoadQuery::aggregate`)
- [x] grouped helper terminal surface removed from query/fluent APIs (`group_count*`, `group_sum_distinct_by`, `group_exists`, `group_first`, `group_last`, `group_min*`, `group_max*`)
- [x] aggregate semantic authority converged behind `AggregateExpr` kind helpers (distinct support, field-target capability, fingerprint tags, and extrema/fold direction helpers now consumed via shared builder semantics)

### Phase 2 - Parity Hardening

- [x] builder-shape logical regression tests added
- [x] explain/fingerprint/continuation regression tests added for grouped builder matrix (grouping, ordering, DISTINCT flag, projection order, continuation token encoding)
- [x] typed validation parity verified

### Phase 3 - Migration Readiness

- [x] migration guide documented
- [x] changelog summary aligned with shipped surface

### Phase 4 - Resource Follow-Up

- [x] grouped route strategy labels aligned with runtime materialization (`HashMaterialized` / `OrderedMaterialized`)
- [x] grouped metrics strategy labels remain explicitly materialized
- [x] non-grouped materialized DISTINCT terminal ownership consolidated through one helper boundary
- [x] resource-audit report updated under `docs/audits/reports/2026-03-01`

---

## Exit Criteria (`0.37`)

`0.37` is complete when:

1. composable aggregate builders are available for grouped/global aggregate use
2. combinatorial grouped helper terminals are removed from public query/fluent surfaces
3. parity tests prove no unintended semantic drift
4. resource-model constraints remain unchanged and enforced
