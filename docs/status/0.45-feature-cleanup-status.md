# 0.45 Status (as of 2026-03-07, kickoff established)

## 0.45 Scope

This tracker covers cleanup work defined in:

- `docs/design/0.45-feature-cleanup.md`
- `docs/design/0.45-executor-planner-overlap.md`

Objective: close existing correctness/alignment/maintainability gaps without
adding new query-surface features.

---

## In Scope (`0.45.x`)

- API consistency cleanup (visibility, error surfaces, serialization stability)
- schema correctness cleanup (relation naming and compile-time validation)
- internal maintainability cleanup (overlap removal, file/module gravity-well splits, test rationalization)
- deterministic stability closure (fingerprint, cursor, explain output, ordering assertions)

## Out of Scope (`0.45.x`)

- new query terminals
- new planner semantic families
- new access-path variants
- new executor routing branches
- compatibility shim preservation

---

## Roadmap Snapshot (`0.45.x`)

- [x] A. design docs established (`0.45` cleanup + overlap companion)
- [x] B. status tracker established
- [ ] C. visibility/public-surface reduction sweep (in progress)
- [ ] D. error + schema validation normalization (in progress)
- [ ] E. overlap/access-shape/telemetry/fingerprint convergence
- [ ] F. cursor + explain + ordering stability lock
- [ ] G. module/encoding/test-matrix maintainability pass
- [ ] H. final closure + changelog/status sync

Current `0.45.x` status: **in progress** (Slice C visibility reductions, Slice
D error-boundary + extended serialization-contract locks, Slice F cursor
stability expansion, and Slice G execution-contract/projection module-split
follow-through are landed with passing compile/test gates; Slice D
planner-validation grouped-boundary coverage and Slice E fingerprint
normalization coverage have been extended in this pass, including grouped
structure/projection planner boundary locks, core error/visitor/event-report
field-boundary tightening, account/decimal value-surface field-boundary
tightening, index/runtime-model metadata field-boundary tightening, runtime
metrics summary/state/perf/ops/counters field-boundary tightening, and accessor
migration in facade/runtime callers, plus numeric-widen coercion literal-subtype
fingerprint canonicalization/lock expansion, and text-casefold coercion
literal canonicalization/lock expansion, plus schema compile-fail suffix
boundary locks).

---

## Cleanup Tracker

| Workstream | State | Owner Boundary | Next Gate |
|---|---|---|---|
| Visibility reduction (`pub`/fields) | Partial | API + subsystem boundaries | continue internal-only field/type tightening and add accessors for public DTOs where needed |
| Serialization contract stabilization | Partial | query/runtime API + DTOs | continue extending wire-shape locks beyond `query::expr`/`error`/EXPLAIN + `describe` DTO coverage to remaining public serialized DTO surfaces |
| Error surface normalization | Partial | planner/executor/index/storage | continue mapping cleanup and add schema-side boundary locks in the same slice |
| Executor/planner overlap cleanup | Partial | planner + executor | continue routing overlap removal by moving remaining duplicated semantic gates behind shared route contracts |
| Canonical executor route-shape descriptor adoption | Complete | route/access/executor | closed in this pass (runtime + route tests now read canonical `ExecutionRouteShape`) |
| Fast-path telemetry taxonomy unification | Partial | diagnostics + tests | continue migrating test-only counters/trace labels onto shared optimization taxonomy surfaces |
| Macro validation hardening | Partial | schema macros | extend compile-fail lock matrix beyond relation suffix/primitive mismatch to remaining index/reference boundary cases |
| Planner validation coverage | Partial | planner validation | extend policy-coverage across remaining fluent/entrypoint unsupported-shape boundaries after grouped cursor/policy/structure/projection locks |
| Fingerprint determinism suite | Complete | query/fingerprint | closed in this pass (predicate reorder/map-order/literal-coercion determinism matrix locked across strict, numeric-widen, text-casefold, and coercion-id boundary variants) |
| Cursor stability audit | Partial | continuation/runtime | expand cross-shape signature/decode/resume matrix beyond initial fast-vs-fallback and pushdown-vs-fallback locks |
| EXPLAIN stability snapshots | Complete | explain/diagnostics | closed in this pass (query + runtime + grouped + verbose snapshot locks landed) |
| Module gravity-well splits | Complete | executor/access internals | closed in this pass (`db/executor/continuation`, `db/executor/load/terminal`, `db/executor/load/mod.rs` contract extraction, full `db/access/execution_contract/mod.rs` focused submodule split, and `db/executor/aggregate/projection/mod.rs` covering/decode helper extraction into focused submodules with no behavior change) |
| Encoding consistency audit | Partial | value/hash/predicate/index encoding | continue consolidating remaining cross-boundary encode/digest helpers in index/predicate/value surfaces after shared map-entry comparator adoption, full fingerprint-submodule hash primitive reuse, and index literal encoding authority unification |
| Test matrix rationalization | Open | tests | define target naming layout and migrate first matrix group |
| Changelog/status closure sync | Open | docs/governance | keep `0.45` changelog/status aligned per shipped slices |

---

## Baseline Metrics (Kickoff)

Collected from `crates/icydb-core/src/db` on 2026-03-07:

- `AccessPath` references: `671` across `58` files
- `continuation` references: `1240` across `106` files
- public declarations (excluding `pub(crate)` / `pub(in crate ...)`): `790`
- direct public field declarations: `16`
- telemetry marker references (`ExecutionOptimization` or `take_*fast_path_hits_for_tests`): `119` across `21` files

Largest production module candidates (non-test files):

- `db/query/explain/mod.rs` (`1685` LOC)
- `db/access/execution_contract.rs` (`986` LOC)
- `db/executor/aggregate/projection.rs` (`946` LOC)
- `db/executor/continuation/mod.rs` (`750` LOC)
- `db/executor/load/terminal.rs` (`749` LOC)
- `db/executor/load/mod.rs` (`632` LOC)

---

## Active Slice (`0.45.x`)

Slices D + F are active:

- complete remaining boundary-lock tests for planner/executor/index/storage taxonomy
- extend serialization stability locks across public-facing DTO/query/explain surfaces
- continue cursor signature/decode/resume parity coverage across equivalent and cross-shape routes
- keep wire-shape and continuation-contract changes explicit and test-guarded

---

## Completed in This Pass (Slice C - Initial Visibility Tightening)

- Tightened internal visibility in
  `crates/icydb-core/src/db/query/plan/model.rs`:
  - `DeleteLimitSpec::max_rows`: `pub -> pub(crate)`
  - `PageSpec::{limit, offset}`: `pub -> pub(crate)`
- Tightened internal visibility in
  `crates/icydb-core/src/db/predicate/coercion.rs`:
  - `CoercionSpec::{id, params}`: `pub -> pub(crate)`
  - `CoercionRule` fields (`left`, `right`, `id`) are now private
- Removed direct field coupling on one public query DTO:
  `SortExpr.fields` is now private in
  `crates/icydb-core/src/db/query/expr.rs`,
  replaced by:
  - `SortExpr::new(fields)`
  - `SortExpr::fields()`
- Updated wrapper lowering call site in
  `crates/icydb/src/db/query/expr.rs` to use `CoreSortExpr::new(fields)`.

Validation:

- `cargo check -p icydb-core` (pass)
- `cargo check -p icydb` (pass)

Metric impact (`crates/icydb-core/src/db`):

- direct public field declarations: `16 -> 7`
- public declarations (excluding `pub(crate)` / `pub(in crate ...)`):
  `790 -> 783`

## Completed in This Pass (Slice C - Query Mode Spec Tightening)

- Tightened query-mode spec field visibility in
  `crates/icydb-core/src/db/query/plan/model.rs`:
  - `LoadSpec::{limit, offset}`: `pub -> pub(crate)`
  - `DeleteSpec::limit`: `pub -> pub(crate)`
- Added public read accessors to preserve external read semantics:
  - `LoadSpec::limit()`
  - `LoadSpec::offset()`
  - `DeleteSpec::limit()`

Validation:

- `cargo check -p icydb-core` (pass)
- `cargo check -p icydb` (pass)

Metric impact (`crates/icydb-core/src/db`):

- direct public field declarations: `7 -> 4`
- public declarations (excluding `pub(crate)` / `pub(in crate ...)`):
  `783 -> 783` (net neutral due new accessor methods)

## Completed in This Pass (Slice C - ComparePredicate Field Boundary)

- Tightened comparison-model field visibility in
  `crates/icydb-core/src/db/predicate/model.rs`:
  - `ComparePredicate::{field, op, value, coercion}`: `pub -> pub(crate)`
- Added public read accessors to preserve external read semantics:
  - `ComparePredicate::field()`
  - `ComparePredicate::op()`
  - `ComparePredicate::value()`
  - `ComparePredicate::coercion()`

Validation:

- `cargo check -p icydb-core` (pass)
- `cargo check -p icydb` (pass)

Metric impact (`crates/icydb-core/src/db`):

- direct public field declarations: `4 -> 0`
- public declarations (excluding `pub(crate)` / `pub(in crate ...)`):
  `783 -> 783` (net neutral due new accessor methods)

## Completed in This Pass (Slice C - Facade SortExpr Field Boundary)

- Tightened facade query DTO field visibility in
  `crates/icydb/src/db/query/expr.rs`:
  - `SortExpr::fields`: `pub -> private`
- Added canonical facade accessors:
  - `SortExpr::new(fields)`
  - `SortExpr::fields()`

Validation:

- `cargo check -p icydb` (pass)
- `cargo test -p icydb db::query::expr::tests -- --nocapture` (pass)

## Completed in This Pass (Slice C - Facade Paged/Error DTO Field Boundaries)

- Tightened facade response DTO visibility in
  `crates/icydb/src/db/response/paged.rs`:
  - `PagedResponse::{items, next_cursor}`: `pub -> private`
  - `PagedGroupedResponse::{items, next_cursor, execution_trace}`:
    `pub -> private`
  - added immutable-style constructors/accessors:
    - `PagedResponse::new(...)` (`pub(crate)`)
    - `PagedResponse::{items(), next_cursor(), into_items(), into_next_cursor()}`
    - `PagedGroupedResponse::new(...)` (`pub(crate)`)
    - `PagedGroupedResponse::{items(), next_cursor(), execution_trace(), into_items(), into_next_cursor(), into_execution_trace()}`
- Updated facade session construction sites to preserve behavior while removing
  struct-literal coupling:
  - `crates/icydb/src/db/session/load.rs`
  - `crates/icydb/src/db/session/mod.rs`
- Tightened facade error DTO visibility in `crates/icydb/src/error.rs`:
  - `Error::{kind, origin, message}`: `pub -> private`
  - added canonical read accessors:
    - `Error::kind()`
    - `Error::origin()`
    - `Error::message()`
- Updated `icydb` error mapping tests to assert via accessors, preserving the
  existing semantic contract.

Validation:

- `cargo fmt --all` (pass)
- `cargo check -p icydb` (pass)
- `cargo test -p icydb` (pass)

Metric impact (`crates/icydb/src`):

- direct public field declarations: `0` (no remaining facade public-struct
  field declarations in `src`)

## Completed in This Pass (Slice C - Core InternalError/Visitor Field Boundaries)

- Tightened core error taxonomy DTO field visibility in
  `crates/icydb-core/src/error.rs`:
  - `InternalError::{class, origin, message, detail}`: `pub -> pub(crate)`
  - added canonical read/accessor surface:
    - `InternalError::{class(), origin(), message(), detail()}`
    - `InternalError::into_message()`
- Updated facade error mapping to consume the accessor surface in:
  - `crates/icydb/src/error.rs`
- Tightened visitor issue DTO field visibility in:
  - `crates/icydb-core/src/visitor/context.rs`
  - `Issue::message`: `pub -> private`
  - added canonical constructors/accessors:
    - `Issue::new(...)`
    - `Issue::message()`
    - `Issue::into_message()`
- Updated visitor adapters and validator tests to consume the accessor surface:
  - `crates/icydb-core/src/visitor/mod.rs`
  - `crates/icydb/src/base/validator/num.rs`
  - `crates/icydb/src/base/validator/len.rs`

Validation:

- `cargo test -p icydb-core error::tests -- --nocapture` (pass)
- `cargo test -p icydb base::validator::num::tests -- --nocapture` (pass)
- `cargo test -p icydb base::validator::len::tests -- --nocapture` (pass)
- `cargo test -p icydb error::tests -- --nocapture` (pass)

## Completed in This Pass (Slice C - EventReport Field Boundary)

- Tightened observability metrics DTO field visibility in:
  - `crates/icydb-core/src/obs/metrics/mod.rs`
  - `EventReport::{counters, entity_counters}`: `pub -> private`
  - added canonical construction/read APIs:
    - `EventReport::new(...)`
    - `EventReport::{counters(), entity_counters(), into_counters(), into_entity_counters()}`
- Updated metrics and execution tests to consume accessors instead of direct
  field access:
  - `crates/icydb-core/src/obs/sink.rs`
  - `crates/icydb-core/src/db/executor/tests/mutation_save.rs`

Validation:

- `cargo test -p icydb-core obs::sink::tests -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::mutation_save::batch_lane_metrics_atomic_success_failure_and_non_atomic_partial_are_distinct -- --nocapture` (pass)
- `make test` (pass)

## Completed in This Pass (Slice C - ValueEnum Field Boundary)

- Tightened value-enum DTO field visibility in:
  - `crates/icydb-core/src/value/mod.rs`
  - `ValueEnum::{variant, path, payload}`: `pub -> private`
  - added canonical accessors/mutator:
    - `ValueEnum::{variant(), path(), payload()}`
    - `ValueEnum::set_path(...)` (`pub(crate)`)
- Updated core semantic consumers to use the accessor surface:
  - `crates/icydb-core/src/value/compare.rs`
  - `crates/icydb-core/src/value/hash.rs`
  - `crates/icydb-core/src/db/index/key/ordered/parts.rs`
  - `crates/icydb-core/src/db/predicate/encoding.rs`
  - `crates/icydb-core/src/db/predicate/normalize.rs`
  - `crates/icydb-core/src/db/query/intent/tests.rs`
- Updated schema derive codegen to consume accessor-based enum decoding:
  - `crates/icydb-schema-derive/src/imp/field_value.rs`
- Updated schema integration test fixture expectations:
  - `crates/icydb-schema-tests/src/test/enum_payload.rs`

Validation:

- `cargo test -p icydb-core value::tests -- --nocapture` (pass)
- `cargo test -p icydb-schema-tests test::enum_payload::test::enum_field_value_carries_payload -- --nocapture` (pass)
- `make test` (pass)

## Completed in This Pass (Slice C - Account Field Boundary)

- Tightened account value DTO field visibility in:
  - `crates/icydb-core/src/types/account.rs`
  - `Account::{owner, subaccount}`: `pub -> private`
  - added canonical construction/read APIs:
    - `Account::from_parts(...)`
    - `Account::{owner(), subaccount()}`
- Updated downstream account consumers to use the accessor surface:
  - `crates/icydb-core/src/db/index/key/ordered/parts.rs`
  - `crates/icydb-core/src/db/predicate/encoding.rs`
  - `crates/icydb-core/src/db/data/storage_key.rs`

Validation:

- `cargo test -p icydb-core types::account::tests -- --nocapture` (pass)
- `cargo test -p icydb-core db::data::storage_key::tests -- --nocapture` (pass)
- `cargo test -p icydb-core db::predicate::encoding::tests -- --nocapture` (pass)

## Completed in This Pass (Slice C - DecimalParts Field Boundary)

- Tightened decimal decomposition DTO field visibility in:
  - `crates/icydb-core/src/types/decimal.rs`
  - `DecimalParts::{mantissa, scale}`: `pub -> private`
  - added canonical read APIs:
    - `DecimalParts::{mantissa(), scale()}`
- Updated ordered-index decimal normalization to consume accessor APIs:
  - `crates/icydb-core/src/db/index/key/ordered/normalize.rs`

Validation:

- `cargo test -p icydb-core types::decimal::tests -- --nocapture` (pass)
- `cargo test -p icydb-core db::index::key::tests::ordered_semantics::canonical_encoder_decimal_small_domain_matches_numeric_order -- --nocapture` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo check -p icydb` (pass)

## Completed in This Pass (Slice C - IndexModel Field Boundary)

- Tightened index metadata DTO field visibility in:
  - `crates/icydb-core/src/model/index.rs`
  - `IndexModel::{name, store, fields, unique}`: `pub -> private`
  - added canonical read APIs:
    - `IndexModel::{name(), store(), fields(), is_unique()}`
- Updated planner/access/index/executor consumers and tests to use accessor
  APIs across the `db` boundary, including:
  - access canonicalization/validation/contract summaries
  - planner index-selection/prefix/range/semantic projection flows
  - index key/plan/scan and describe/session/reporting paths
  - pagination/explain/plan regression tests

Validation:

- `cargo check -p icydb-core` (pass)
- `cargo check -p icydb` (pass)
- `make clippy` (pass)
- `make test` (pass)

## Completed in This Pass (Slice C - Runtime Model Field Boundary)

- Tightened runtime model descriptor field visibility in:
  - `crates/icydb-core/src/model/field.rs`
  - `crates/icydb-core/src/model/entity.rs`
  - `FieldModel::{name, kind}`: `pub -> pub(crate)`
  - `EntityModel::{path, entity_name, primary_key, fields, indexes}`:
    `pub -> pub(crate)`
- Added canonical runtime model constructors/accessors:
  - `FieldModel::new(...)`
  - `FieldModel::{name(), kind()}`
  - `EntityModel::new(...)`
  - `EntityModel::{path(), entity_name(), primary_key(), fields(), indexes()}`
- Updated schema derive codegen and schema tests to use constructor/accessor
  surfaces instead of public field literals/reads:
  - `crates/icydb-schema-derive/src/imp/inherent/entity.rs`
  - `crates/icydb-schema-derive/src/imp/entity.rs`
  - `crates/icydb-schema-tests/src/test/field_projection_order.rs`

Validation:

- `cargo check -p icydb-core` (pass)
- `cargo check -p icydb` (pass)
- `cargo check -p icydb-schema-derive` (pass)
- `cargo check -p icydb-schema-tests` (pass)
- `make test` (pass)

## Completed in This Pass (Slice C - EntitySummary Field Boundary)

- Tightened runtime metrics summary DTO field visibility in:
  - `crates/icydb-core/src/obs/metrics/mod.rs`
  - `EntitySummary` fields: `pub -> private`
- Added canonical read API surface:
  - `EntitySummary::{path(), load_calls(), delete_calls(), rows_loaded(), rows_scanned(), rows_deleted(), avg_rows_per_load(), avg_rows_scanned_per_load(), avg_rows_per_delete(), index_inserts(), index_removes(), reverse_index_inserts(), reverse_index_removes(), relation_reverse_lookups(), relation_delete_blocks(), unique_violations(), non_atomic_partial_commits(), non_atomic_partial_rows_committed()}`

Validation:

- `cargo test -p icydb-core obs::metrics::tests -- --nocapture` (pass)
- `make clippy` (pass)
- `make test` (pass)

## Completed in This Pass (Slice C - EventState/EventPerf Field Boundary)

- Tightened runtime metrics state/perf DTO visibility in:
  - `crates/icydb-core/src/obs/metrics/mod.rs`
  - `EventState::{ops, perf, entities, window_start_ms}`:
    `pub -> pub(crate)`
  - `EventPerf::{load_inst_total, save_inst_total, delete_inst_total, load_inst_max, save_inst_max, delete_inst_max}`:
    `pub -> pub(crate)`
- Added canonical constructor/read APIs:
  - `EventState::new(...)`
  - `EventState::{ops(), perf(), entities(), window_start_ms()}`
  - `EventPerf::new(...)`
  - `EventPerf::{load_inst_total(), save_inst_total(), delete_inst_total(), load_inst_max(), save_inst_max(), delete_inst_max()}`
- Updated local report construction to consume constructor surface:
  - `EventReport::new(Some(snap), entity_counters)`

Validation:

- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core obs::metrics::tests -- --nocapture` (pass)
- `cargo test -p icydb-core obs::sink::tests -- --nocapture` (pass)

## Completed in This Pass (Slice C - EventOps/EntityCounters Field Boundary)

- Tightened runtime metrics counters DTO visibility in:
  - `crates/icydb-core/src/obs/metrics/mod.rs`
  - `EventOps` fields: `pub -> pub(crate)`
  - `EntityCounters` fields: `pub -> pub(crate)`
- Added canonical read APIs to preserve public metric-read semantics without
  direct field coupling:
  - `EventOps::{load_calls(), save_calls(), delete_calls(), plan_index(), plan_keys(), plan_range(), plan_full_scan(), plan_grouped_hash_materialized(), plan_grouped_ordered_materialized(), rows_loaded(), rows_scanned(), rows_deleted(), index_inserts(), index_removes(), reverse_index_inserts(), reverse_index_removes(), relation_reverse_lookups(), relation_delete_blocks(), unique_violations(), non_atomic_partial_commits(), non_atomic_partial_rows_committed()}`
  - `EntityCounters::{load_calls(), save_calls(), delete_calls(), rows_loaded(), rows_scanned(), rows_deleted(), index_inserts(), index_removes(), reverse_index_inserts(), reverse_index_removes(), relation_reverse_lookups(), relation_delete_blocks(), unique_violations(), non_atomic_partial_commits(), non_atomic_partial_rows_committed()}`

Validation:

- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core obs::metrics::tests -- --nocapture` (pass)
- `cargo test -p icydb-core obs::sink::tests -- --nocapture` (pass)

## Completed in This Pass (Slice D - Execution Error Boundary Naming)

- Normalized query execution-domain error naming in core intent errors:
  - `QueryExecuteError` renamed to `ExecutionError` in
    `crates/icydb-core/src/db/query/intent/errors.rs`
  - `QueryError::Execute(...)` now wraps `ExecutionError`
- Updated re-export surfaces:
  - `crates/icydb-core/src/db/query/intent/mod.rs`
  - `crates/icydb-core/src/db/mod.rs`
- Updated facade mapping and tests to the canonical execution error name:
  - `crates/icydb/src/error.rs`
  - `crates/icydb-core/src/db/executor/tests/paged_builder.rs`

Validation:

- `cargo check -p icydb-core` (pass)
- `cargo check -p icydb` (pass)
- `cargo test -p icydb-core query_execute_error_from_internal_preserves_class_and_origin_matrix -- --nocapture` (pass)
- `cargo test -p icydb internal_error_class_matrix_maps_to_runtime_kind_and_preserves_origin -- --nocapture` (pass)

## Completed in This Pass (Slice D - Boundary Mapping Test Locks)

- Added explicit planner-boundary mapping lock in
  `crates/icydb-core/src/db/query/intent/errors.rs`:
  - `planner_plan_mapping_stays_in_query_plan_error_boundary`
  - asserts `PlannerError::Plan` remains `QueryError::Plan` (and does not
    drift into execution-domain mapping)
- Extended facade runtime mapping matrix in `crates/icydb/src/error.rs`:
  - `query_execute_preserves_runtime_class_and_origin` now includes
    `CoreErrorOrigin::Planner` -> `ErrorOrigin::Planner`

Validation:

- `cargo test -p icydb-core planner_plan_mapping_stays_in_query_plan_error_boundary -- --nocapture` (pass)
- `cargo test -p icydb query_execute_preserves_runtime_class_and_origin -- --nocapture` (pass)

## Completed in This Pass (Slice D - Storage/Index Boundary Locks)

- Added core query-boundary regression lock in
  `crates/icydb-core/src/db/query/intent/errors.rs`:
  - `query_execute_storage_and_index_errors_stay_in_execution_boundary`
  - verifies store/index-origin runtime failures stay under
    `QueryError::Execute(ExecutionError::...)` and preserve class+origin
- Added facade contract regression lock in `crates/icydb/src/error.rs`:
  - `query_execute_storage_and_index_origins_map_to_runtime_contract`
  - verifies store/index-origin runtime failures map to
    `ErrorKind::Runtime(...)` with preserved origin taxonomy

Validation:

- `cargo test -p icydb-core query_execute_storage_and_index_errors_stay_in_execution_boundary -- --nocapture` (pass)
- `cargo test -p icydb query_execute_storage_and_index_origins_map_to_runtime_contract -- --nocapture` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo check -p icydb` (pass)

## Completed in This Pass (Slice D - Serialization Contract Stabilization, Initial Lock)

- Added explicit serde representation attributes on public `icydb` query/error
  DTO enums in:
  - `crates/icydb/src/db/query/expr.rs`
  - `crates/icydb/src/error.rs`
- Added CBOR wire-shape regression tests for public query expression DTOs in:
  - `crates/icydb/src/db/query/expr.rs`
  - `filter_expr_eq_serialization_shape_is_stable`
  - `filter_expr_and_serialization_shape_is_stable`
  - `sort_expr_serialization_field_name_is_stable`
  - `order_direction_variant_labels_are_stable`
- Added CBOR wire-shape regression tests for public facade errors in:
  - `crates/icydb/src/error.rs`
  - `error_struct_serialization_shape_is_stable`
  - `error_kind_serialization_shape_is_stable`
  - `runtime_error_and_origin_variant_labels_are_stable`
- Added canonical EXPLAIN execution-node JSON snapshot lock in:
  - `crates/icydb-core/src/db/query/explain/tests.rs`
  - `execution_descriptor_canonical_json_shape_is_stable`
- Added `serde_cbor` test dependency for `icydb`:
  - `crates/icydb/Cargo.toml`

Validation:

- `cargo test -p icydb db::query::expr::tests -- --nocapture` (pass)
- `cargo test -p icydb error::tests -- --nocapture` (pass)
- `cargo test -p icydb-core db::query::explain::tests -- --nocapture` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo check -p icydb` (pass)

## Completed in This Pass (Slice D - Serialization Contract Stabilization, Describe/Variant Lock Extension)

- Extended serialization wire-shape regression locks to public core describe
  DTO surfaces in:
  - `crates/icydb-core/src/db/describe.rs`
  - `entity_schema_description_serialization_shape_is_stable`
  - `entity_relation_description_serialization_shape_is_stable`
  - `relation_enum_variant_labels_are_stable`
- Extended facade serialization variant/shape locks in:
  - `crates/icydb/src/error.rs`
  - `query_error_kind_variant_labels_are_stable`
  - `patch_error_variant_labels_are_stable`
  - `update_error_kind_patch_payload_shape_is_stable`
- Extended facade query-expression serialization locks in:
  - `crates/icydb/src/db/query/expr.rs`
  - `filter_expr_text_contains_ci_payload_shape_is_stable`
  - `filter_expr_not_payload_shape_is_stable`
- These additions keep public DTO wire keys and externally-tagged variant
  labels explicit and test-guarded across both core introspection and facade
  query/error surfaces.

Validation:

- `cargo test -p icydb-core db::describe::tests -- --nocapture` (pass)
- `cargo test -p icydb db::query::expr::tests -- --nocapture` (pass)
- `cargo test -p icydb error::tests -- --nocapture` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo check -p icydb` (pass)

## Completed in This Pass (Slice D - Serialization Contract Stabilization, Metrics DTO Lock Extension)

- Extended serialization wire-shape regression locks to public runtime metrics
  DTO surfaces in:
  - `crates/icydb-core/src/obs/metrics/mod.rs`
  - `event_report_serialization_shape_is_stable`
  - `entity_summary_serialization_shape_is_stable`
- These tests explicitly freeze:
  - `EventReport` top-level serialized field keys (`counters`,
    `entity_counters`)
  - `EventState` serialized field keys (`ops`, `perf`, `entities`,
    `window_start_ms`) through report payload coverage
  - `EntitySummary` serialized field keys for canonical averages and relation
    counters
- This closes another public DTO family under the `0.45` serialization
  stabilization track without altering runtime behavior.

Validation:

- `cargo test -p icydb-core event_report_serialization_shape_is_stable -- --nocapture` (pass)
- `cargo test -p icydb-core entity_summary_serialization_shape_is_stable -- --nocapture` (pass)

## Completed in This Pass (Slice D - Serialization Contract Stabilization, Diagnostics DTO Lock Extension)

- Extended serialization wire-shape regression locks to public diagnostics
  snapshot DTO surfaces in:
  - `crates/icydb-core/src/db/diagnostics/tests.rs`
  - `storage_report_serialization_shape_is_stable`
  - `entity_snapshot_serialization_shape_is_stable`
- These tests explicitly freeze:
  - `StorageReport` top-level serialized field keys (`storage_data`,
    `storage_index`, `entity_storage`, `corrupted_keys`,
    `corrupted_entries`)
  - `EntitySnapshot` serialized field keys (`store`, `path`, `entries`,
    `memory_bytes`, `min_key`, `max_key`)
- This closes another public diagnostics DTO family under the `0.45`
  serialization stabilization track without changing runtime logic.

Validation:

- `cargo test -p icydb-core storage_report_serialization_shape_is_stable -- --nocapture` (pass)
- `cargo test -p icydb-core entity_snapshot_serialization_shape_is_stable -- --nocapture` (pass)

## Completed in This Pass (Slice D - Macro Validation Hardening, Core Rules)

- Enforced canonical relation identity suffixes in macro field validation:
  - one/optional relation fields must end with `_id`
  - many relation fields must end with `_ids`
  - implementation: `crates/icydb-schema-derive/src/node/field.rs`
- Added generated relation key-type compile-time assertions to block
  relation-key mismatches between declared relation primitive and related entity
  primary key type:
  - implementation: `crates/icydb-schema-derive/src/imp/entity.rs`
- Updated generated relation helper naming to align with canonical field names
  and added mutation helpers:
  - accessors now mirror field names directly (for example `user_id()`,
    `order_ids()`)
  - generated one/optional setters (`set_<field>(...)`)
  - generated many-relation mutators (`add_<item_id>(...)`,
    `remove_<item_id>(...)`)
  - implementation: `crates/icydb-schema-derive/src/imp/inherent/relation.rs`
- Migrated schema test fixtures to canonical relation field naming:
  - `crates/icydb-schema-tests/src/admin/mod.rs`
  - `crates/icydb-schema-tests/src/e2e/db.rs`
  - `crates/icydb-schema-tests/src/test/relation.rs`
  - `crates/icydb-schema-tests/src/test/identity_borrowing.rs`
- Added relation helper behavior coverage for generated setter/add/remove flows:
  - `crates/icydb-schema-tests/src/test/relation.rs`
- Added derive-layer unit coverage for relation suffix enforcement:
  - `crates/icydb-schema-derive/src/node/field.rs`
  - validates `_id`/`_ids` failure and canonical acceptance paths
- Added derive-layer unit coverage for index validation hardening:
  - `crates/icydb-schema-derive/src/node/entity.rs`
  - validates missing index field rejection
  - validates many-cardinality index field rejection

Validation:

- `cargo check -p icydb-schema-derive` (pass)
- `cargo test -p icydb-schema-derive node::field::tests -- --nocapture` (pass)
- `cargo test -p icydb-schema-derive node::entity::tests -- --nocapture` (pass)
- `cargo check -p icydb-schema-tests` (pass)
- `cargo test -p icydb-schema-tests --test compile_fail -- --nocapture` (pass)

## Completed in This Pass (Slice D - Macro Validation Hardening, Compile-Fail Suffix Locks)

- Added schema-level compile-fail UI locks for relation identity suffix
  enforcement in:
  - `crates/icydb-schema-tests/tests/ui/relation_one_suffix_required.rs`
  - `crates/icydb-schema-tests/tests/ui/relation_many_suffix_required.rs`
  - expected diagnostics:
    - `relation_one_suffix_required.stderr`
    - `relation_many_suffix_required.stderr`
- These locks ensure canonical macro validation remains enforced at the
  external schema usage boundary (not just derive crate unit tests):
  - one/optional relation fields must end with `_id`
  - many relation fields must end with `_ids`

Validation:

- `cargo test -p icydb-schema-tests --test compile_fail -- --nocapture` (pass)
- `cargo test --workspace --all-targets -q` (pass)

## Completed in This Pass (Slice D - Planner Validation Coverage, Initial Lock)

- Added direct planner-policy regression coverage in
  `crates/icydb-core/src/db/query/plan/validate/cursor_policy.rs`:
  - cursor paging requires explicit order
  - cursor paging requires explicit limit
  - cursor order shape rejects missing/empty explicit order contracts
- Added intent-policy regression coverage in
  `crates/icydb-core/src/db/query/plan/validate/intent_policy.rs`:
  - delete `LIMIT` without `ORDER` fails during planning validation
  - delete `OFFSET` fails during planning validation
  - conflicting key-access selectors fail during planning validation
  - `by_ids`/`only` + predicate combinations fail during planning validation
- Added grouped-policy regression coverage in
  `crates/icydb-core/src/db/query/plan/validate/grouped/policy.rs`:
  - grouped `DISTINCT` without adjacency eligibility fails as a policy error
  - grouped `DISTINCT` with `HAVING` fails as a policy error

Validation:

- `cargo check -p icydb-core --tests` (pass)
- `cargo test -p icydb-core query::plan::validate:: -- --nocapture` (pass)

## Completed in This Pass (Slice D - Planner Validation Coverage, Grouped Boundary Lock Extension)

- Extended grouped cursor-policy regression coverage in
  `crates/icydb-core/src/db/query/plan/validate/grouped/cursor.rs`:
  - grouped `ORDER BY` without `LIMIT` fails as a planner policy error
  - grouped `ORDER BY` prefix misaligned with `GROUP BY` keys fails as a
    planner policy error
  - grouped `ORDER BY` with explicit `LIMIT` and aligned group-key prefix
    passes the planner cursor-policy gate
- Extended grouped unsupported-feature regression coverage in
  `crates/icydb-core/src/db/query/plan/validate/grouped/policy.rs`:
  - grouped `HAVING` with unsupported compare operators (for example
    `Contains`) fails as a planner policy error (`HavingUnsupportedCompareOp`)
- Extended intent policy coverage in
  `crates/icydb-core/src/db/query/plan/validate/intent_policy.rs`:
  - delete grouped shapes are explicitly rejected during planner intent-policy
    validation (`DeletePlanWithGrouping`)

Validation:

- `cargo test -p icydb-core query::plan::validate::grouped::cursor::tests -- --nocapture` (pass)
- `cargo test -p icydb-core query::plan::validate::grouped::policy::tests -- --nocapture` (pass)
- `cargo test -p icydb-core query::plan::validate::intent_policy::tests -- --nocapture` (pass)
- `make test` (pass)

## Completed in This Pass (Slice D - Planner Validation Coverage, Grouped Structure/Projection Lock Extension)

- Extended grouped structure-boundary regression coverage in:
  - `crates/icydb-core/src/db/query/plan/validate/grouped/structure.rs`
  - grouped projection expressions referencing non-group fields fail at planner
    validation boundary
  - grouped HAVING group-field symbols outside declared `GROUP BY` keys fail at
    planner validation boundary
  - grouped HAVING aggregate-index symbols outside aggregate declaration range
    fail at planner validation boundary
- Extended grouped projection-boundary regression coverage in:
  - `crates/icydb-core/src/db/query/plan/validate/grouped/projection_expr.rs`
  - alias-wrapped grouped-field expressions remain accepted
  - binary expressions that include non-group fields fail with grouped
    projection compatibility errors
  - projection expression typing rejects unknown fields under planner schema
    typing validation

Validation:

- `cargo test -p icydb-core query::plan::validate::grouped::structure::tests -- --nocapture` (pass)
- `cargo test -p icydb-core query::plan::validate::grouped::projection_expr::tests -- --nocapture` (pass)
- `make test` (pass)

## Completed in This Pass (Slice E - Executor/Planner Overlap Cleanup, Initial Gate)

- Removed duplicated access-order semantic gate logic from:
  - `crates/icydb-core/src/db/executor/route/capability.rs`
  - `crates/icydb-core/src/db/executor/kernel/post_access/mod.rs`
- Introduced one shared route-owned order contract helper in:
  - `crates/icydb-core/src/db/executor/route/pushdown.rs`
  - `access_order_satisfied_by_route_contract::<E, K>(...)`
- Introduced one canonical executor-facing shape descriptor in:
  - `crates/icydb-core/src/db/executor/route/contracts.rs`
  - `ExecutionRouteShape`
  - `ExecutionRoutePlan::shape()`
- Migrated first runtime consumers to the shape descriptor:
  - `crates/icydb-core/src/db/executor/load/execute.rs`
  - `crates/icydb-core/src/db/executor/load/entrypoints/scalar.rs`
- Migrated additional runtime consumers to the shape descriptor:
  - `crates/icydb-core/src/db/executor/aggregate/mod.rs`
  - `crates/icydb-core/src/db/executor/explain/descriptor.rs`
- Migrated route tests from direct execution-mode field reads to shape accessors:
  - `crates/icydb-core/src/db/executor/tests/route/load.rs`
  - `crates/icydb-core/src/db/executor/tests/route/mutation.rs`
  - `crates/icydb-core/src/db/executor/tests/route/aggregate.rs`
- Added anti-regression guard test to prevent runtime direct execution-mode
  field reads from reappearing:
  - `runtime_route_consumers_avoid_direct_execution_mode_field_reads`
  - implementation in
    `crates/icydb-core/src/db/executor/tests/route/load.rs`
- Centralized planner-eligibility activation checks behind:
  - `secondary_order_contract_active(...)` in
    `crates/icydb-core/src/db/executor/route/pushdown.rs`
- Wired both route capability derivation and post-access ordering gate to this
  shared helper to keep planner-owned pushdown semantics under one authority
  surface.
- Added overlap anti-regression tests:
  - `route_capabilities_non_unique_index_prefix_order_requires_post_access_sort`
    in `crates/icydb-core/src/db/executor/tests/route/capability.rs`
  - `budget_safety_metadata_order_contract_stays_aligned_with_route_helper`
    in `crates/icydb-core/src/db/executor/kernel/post_access/tests.rs`
  - `route_plan_shape_descriptor_matches_route_axes`
    in `crates/icydb-core/src/db/executor/tests/route/load.rs`

Validation:

- `cargo check -p icydb-core --tests` (pass)
- `cargo test -p icydb-core route_capabilities_non_unique_index_prefix_order_requires_post_access_sort -- --nocapture` (pass)
- `cargo test -p icydb-core budget_safety_metadata_order_contract_stays_aligned_with_route_helper -- --nocapture` (pass)
- `cargo test -p icydb-core route_plan_shape_descriptor_matches_route_axes -- --nocapture` (pass)
- `cargo test -p icydb-core db::query::explain::tests:: -- --nocapture` (pass)
- `cargo test -p icydb-core runtime_route_consumers_avoid_direct_execution_mode_field_reads -- --nocapture` (pass)

## Completed in This Pass (Slice E - Fingerprint Determinism, Initial Lock)

- Added one explicit regression lock for equivalent decimal literal
  canonicalization in predicate fingerprints:
  - `crates/icydb-core/src/db/query/fingerprint/fingerprint.rs`
  - `fingerprint_and_signature_treat_equivalent_decimal_predicate_literals_as_identical`
- This complements existing deterministic locks for:
  - predicate reordering
  - non-canonical map entry ordering

Validation:

- `cargo test -p icydb-core fingerprint_and_signature_treat_equivalent_decimal_predicate_literals_as_identical -- --nocapture` (pass)
- `cargo check -p icydb-core --tests` (pass)

## Completed in This Pass (Slice E - Fingerprint Determinism, IN-List Canonicalization Lock Extension)

- Added one new fingerprint/signature determinism regression lock in
  `crates/icydb-core/src/db/query/fingerprint/fingerprint.rs`:
  - `fingerprint_and_signature_treat_equivalent_in_list_predicates_as_identical`
  - `fingerprint_and_signature_treat_equivalent_in_list_duplicate_literals_as_identical`
  - verifies equivalent `IN (...)` literal sets with different list ordering
    and duplicate-noop variants produce identical plan fingerprints and
    continuation signatures
- Fixed the uncovered determinism gap by canonicalizing compare-literal list
  ordering for `IN`/`NOT IN` at predicate encoding/hash authority in:
  - `crates/icydb-core/src/db/predicate/encoding.rs`
  - compare value sort-key encoding now normalizes `IN`/`NOT IN` list order
    and removes canonical duplicates
  - compare value fingerprint hashing now normalizes `IN`/`NOT IN` list order
    and removes canonical duplicates
- Added direct encoding regression lock:
  - `predicate_sort_key_normalizes_in_list_literal_order`
  - `predicate_sort_key_normalizes_in_list_duplicate_literals`
  - verifies predicate sort-key encoding is stable for reordered and
    duplicate `IN` literals

Validation:

- `cargo test -p icydb-core predicate_sort_key_normalizes_in_list_literal_order -- --nocapture` (pass)
- `cargo test -p icydb-core predicate_sort_key_normalizes_in_list_duplicate_literals -- --nocapture` (pass)
- `cargo test -p icydb-core fingerprint_and_signature_treat_equivalent_in_list_predicates_as_identical -- --nocapture` (pass)
- `cargo test -p icydb-core fingerprint_and_signature_treat_equivalent_in_list_duplicate_literals_as_identical -- --nocapture` (pass)
- `make test` (pass)

## Completed in This Pass (Slice E - Fingerprint Determinism, Hash-Boundary Coercion/IN Lock Extension)

- Added direct predicate-fingerprint boundary locks in
  `crates/icydb-core/src/db/predicate/fingerprint.rs`:
  - `canonical_hash_is_order_insensitive_for_in_list_literals`
  - `canonical_hash_normalizes_in_list_duplicate_literals`
  - `canonical_hash_treats_implicit_and_explicit_strict_coercion_as_equivalent`
  - `canonical_hash_distinguishes_different_coercion_ids`
- These tests explicitly lock hash-layer behavior where normalization equality
  is not the authority:
  - reordered/duplicated `IN` literals may normalize differently but hash to the
    same canonical fingerprint payload
  - explicit `Strict` coercion is fingerprint-equivalent to implicit default
    strict policy
  - distinct coercion IDs remain fingerprint-distinct

Validation:

- `cargo test -p icydb-core db::predicate::fingerprint::tests -- --nocapture` (pass)
- `cargo check -p icydb-core` (pass)
- `make clippy` (pass)

## Completed in This Pass (Slice E - Fingerprint Determinism, Numeric-Widen Literal Canonicalization Extension)

- Canonicalized compare-literal fingerprint/sort-key encoding under
  `CoercionId::NumericWiden` in:
  - `crates/icydb-core/src/db/predicate/encoding.rs`
  - compare literal hashing/sort-key generation now normalizes
    numeric-coercible values (`Int`, `Uint`, `Decimal`, etc.) into one canonical
    decimal representation when coercion policy is numeric-widen
  - strict coercion remains subtype-sensitive
- Added predicate-encoding regression locks:
  - `predicate_sort_key_numeric_widen_treats_equivalent_literal_subtypes_as_identical`
  - `predicate_sort_key_strict_keeps_numeric_literal_subtypes_distinct`
- Added query-plan fingerprint/signature regression lock:
  - `fingerprint_and_signature_treat_numeric_widen_equivalent_literal_subtypes_as_identical`

Validation:

- `cargo test -p icydb-core db::predicate::encoding::tests -- --nocapture` (pass)
- `cargo test -p icydb-core db::query::fingerprint::fingerprint::tests -- --nocapture` (pass)
- `make clippy` (pass)
- `make test` (pass)

## Completed in This Pass (Slice E - Fingerprint Determinism, Text-Casefold Literal Canonicalization Extension)

- Canonicalized compare-literal fingerprint/sort-key encoding under
  `CoercionId::TextCasefold` in:
  - `crates/icydb-core/src/db/predicate/encoding.rs`
  - compare literal hashing/sort-key generation now casefolds text literals for
    identity when coercion policy is text-casefold
  - strict coercion remains case-sensitive
- Added predicate-encoding regression locks:
  - `predicate_sort_key_text_casefold_treats_case_only_literals_as_identical`
  - `predicate_sort_key_strict_keeps_text_case_variants_distinct`
  - `predicate_sort_key_text_casefold_normalizes_in_list_case_variants`
- Added query-plan fingerprint/signature regression locks:
  - `fingerprint_and_signature_treat_text_casefold_case_only_literals_as_identical`
  - `fingerprint_and_signature_keep_strict_text_case_variants_distinct`
  - `fingerprint_and_signature_treat_text_casefold_in_list_case_variants_as_identical`
  - `fingerprint_and_signature_distinguish_strict_from_text_casefold_coercion`
  - `fingerprint_and_signature_distinguish_strict_from_collection_element_coercion`

Validation:

- `cargo test -p icydb-core db::predicate::encoding::tests -- --nocapture` (pass)
- `cargo test -p icydb-core db::query::fingerprint::fingerprint::tests -- --nocapture` (pass)
- `make fmt-check` (pass)
- `make clippy` (pass)
- `cargo test --workspace --all-targets -q` (pass)

## Completed in This Pass (Slice E - Fast-Path Telemetry Unification, Initial Lock)

- Added one canonical test-only optimization counter taxonomy:
  - `ExecutionOptimizationCounter` in
    `crates/icydb-core/src/db/diagnostics/execution_trace.rs`
- Added one unified counter-consumption entrypoint on `LoadExecutor`:
  - `take_execution_optimization_hits_for_tests(...)` in
    `crates/icydb-core/src/db/executor/mod.rs`
- Migrated aggregate parity matrices to use the unified taxonomy entrypoint:
  - `crates/icydb-core/src/db/executor/tests/aggregate/path_parity_matrices.rs`
  - `crates/icydb-core/src/db/executor/tests/aggregate/secondary_index_matrices.rs`
  - `crates/icydb-core/src/db/executor/tests/aggregate/field_projection_matrices.rs`

Validation:

- `cargo check -p icydb-core --tests` (pass)
- `cargo test -p icydb-core aggregate_bytes_pk_fast_path_emits_hit_marker_only_for_eligible_shapes -- --nocapture` (pass)
- `cargo test -p icydb-core aggregate_exists_secondary_index_covering_fast_path_emits_hit_marker_only_for_eligible_shapes -- --nocapture` (pass)
- `cargo test -p icydb-core aggregate_field_target_covering_index_projection_emits_hit_marker_only_for_eligible_shapes -- --nocapture` (pass)
- `make clippy` (pass)

## Completed in This Pass (Slice F - Cursor Stability Audit, Initial Lock)

- Added fast-path vs fallback cursor-signature boundary lock in:
  - `crates/icydb-core/src/db/executor/tests/pagination/cursor_pk.rs`
  - `load_cursor_rejects_signature_mismatch_between_pk_fast_and_by_ids_shapes`
- Added pushdown vs fallback cursor-signature boundary lock in:
  - `crates/icydb-core/src/db/executor/tests/pagination/index_range.rs`
  - `load_cursor_rejects_signature_mismatch_between_pushdown_and_fallback_shapes`
- Both tests explicitly lock:
  - boundary parity across shape variants for the same ordered window
  - encoded cursor signature mismatch rejection across different plan shapes

Validation:

- `cargo test -p icydb-core load_cursor_rejects_signature_mismatch_between_pk_fast_and_by_ids_shapes -- --nocapture` (pass)
- `cargo test -p icydb-core load_cursor_rejects_signature_mismatch_between_pushdown_and_fallback_shapes -- --nocapture` (pass)
- `cargo test -p icydb-core load_cursor_rejects_signature_mismatch -- --nocapture` (pass)
- `cargo check -p icydb-core --tests` (pass)

## Completed in This Pass (Slice F - Cursor Stability Audit, Limit-Pushdown Replay Matrix)

- Added ASC/DESC continuation replay parity matrix for eligible index-range
  limit-pushdown plans vs canonical full-scan fallback in:
  - `crates/icydb-core/src/db/executor/tests/pagination/index_range.rs`
  - `load_index_range_limit_pushdown_continuation_replay_matches_fallback_for_asc_and_desc`
- The matrix explicitly locks:
  - page1 row parity and continuation boundary parity
  - shared-boundary replay parity for page2 rows
  - `next_cursor.is_some()` parity after replay
  - fast-path eligibility signal (`ExecutionOptimization::IndexRangeLimitPushdown`) for the fast plan

Validation:

- `cargo test -p icydb-core load_index_range_limit_pushdown_continuation_replay_matches_fallback_for_asc_and_desc -- --nocapture` (pass)
- `cargo test -p icydb-core load_index_range_limit_pushdown_ -- --nocapture` (pass)

## Completed in This Pass (Slice F - Cursor Stability Audit, Limit-Pushdown Token Replay Matrix)

- Added ASC/DESC encoded-token replay matrix for eligible index-range
  limit-pushdown plans vs canonical full-scan fallback in:
  - `crates/icydb-core/src/db/executor/tests/pagination/index_range.rs`
  - `load_index_range_limit_pushdown_token_replay_matches_fallback_for_asc_and_desc`
- The matrix explicitly locks:
  - token decode parity with emitted cursor boundaries
  - page2 replay row parity when each shape resumes from its own token
  - `next_cursor.is_some()` parity after token replay
  - cross-shape token signature mismatch rejection (fast token on fallback
    and fallback token on fast)

Validation:

- `cargo test -p icydb-core load_index_range_limit_pushdown_token_replay_matches_fallback_for_asc_and_desc -- --nocapture` (pass)
- `cargo test -p icydb-core load_index_range_limit_pushdown_ -- --nocapture` (pass)

## Completed in This Pass (Slice F - Cursor Stability Audit, PK Fast/Fallback Local-Token Replay Parity)

- Added ASC/DESC local-token continuation replay parity matrix for equivalent
  PK-ordered windows across fast and fallback access shapes in:
  - `crates/icydb-core/src/db/executor/tests/pagination/cursor_pk.rs`
  - `load_cursor_resume_parity_holds_between_pk_fast_and_by_ids_with_shape_local_tokens`
- The matrix explicitly locks:
  - page1 row parity between PK fast-path and `by_ids` fallback shapes
  - page1 cursor boundary parity across both shapes
  - signature divergence across shape variants (distinct continuation signatures)
  - page2 row parity when each shape resumes with its own emitted token
  - `next_cursor.is_some()` parity after replay

Validation:

- `cargo test -p icydb-core load_cursor_resume_parity_holds_between_pk_fast_and_by_ids_with_shape_local_tokens -- --nocapture` (pass)

## Completed in This Pass (Slice F - Cursor Stability Audit, PK Fast/Fallback Encoded-Token Replay Parity)

- Added ASC/DESC encoded-token replay parity matrix for equivalent PK-ordered
  windows across fast and fallback access shapes in:
  - `crates/icydb-core/src/db/executor/tests/pagination/cursor_pk.rs`
  - `load_cursor_token_replay_parity_holds_between_pk_fast_and_by_ids_shapes`
- The matrix explicitly locks:
  - token decode parity against emitted page1 cursor boundaries per shape
  - page1 boundary parity between PK fast-path and `by_ids` fallback shapes
  - signature divergence across shape variants (distinct continuation signatures)
  - page2 row parity and `next_cursor.is_some()` parity when each shape resumes
    from its own encoded token
  - bidirectional cross-shape token rejection with
    `ContinuationCursorSignatureMismatch`

Validation:

- `cargo test -p icydb-core load_cursor_token_replay_parity_holds_between_pk_fast_and_by_ids_shapes -- --nocapture` (pass)

## Completed in This Pass (Slice F - EXPLAIN Stability Snapshots, Initial Lock)

- Added canonical aggregate-terminal explain snapshot helper and frozen
  snapshot tests in:
  - `crates/icydb-core/src/db/query/explain/tests.rs`
- Added stable snapshot locks for:
  - seek route aggregate explain payload
  - standard route aggregate explain payload
- These tests explicitly freeze:
  - `ExplainAggregateTerminalPlan` route + terminal projection
  - query-explain shape projection used by terminal explain
  - execution descriptor fields and node-properties surface
  - canonical `ExplainExecutionNodeDescriptor` JSON for aggregate nodes

Validation:

- `cargo test -p icydb-core db::query::explain::tests:: -- --nocapture` (pass)

## Completed in This Pass (Slice F - EXPLAIN Stability Snapshots, Runtime/Session Lock)

- Added runtime/session aggregate terminal explain snapshot helper and frozen
  snapshot matrix in:
  - `crates/icydb-core/src/db/executor/tests/aggregate/session_matrices.rs`
  - `session_load_terminal_explain_plan_snapshots_for_seek_and_standard_routes_are_stable`
- Added stable runtime snapshot locks for:
  - seek route terminal explain payload (`explain_min`)
  - standard route terminal explain payload (`explain_exists` on deterministic by-key shape)
- These tests explicitly freeze:
  - terminal route projection and aggregate kind
  - query-explain projection carried on runtime terminal plans
  - execution descriptor fields and node-properties surface
  - canonical execution-node JSON emitted from runtime terminal plans

Validation:

- `cargo test -p icydb-core session_load_terminal_explain_plan_snapshots_for_seek_and_standard_routes_are_stable -- --nocapture` (pass)
- `cargo test -p icydb-core session_load_explain_execution_text_and_json_snapshot_for_strict_index_prefix_shape -- --nocapture` (pass)
- `cargo test -p icydb-core db::query::explain::tests:: -- --nocapture` (pass)

## Completed in This Pass (Slice F - EXPLAIN Stability Snapshots, Grouped + Verbose Closure)

- Added grouped explain-plan snapshot helper and frozen grouped snapshot locks in:
  - `crates/icydb-core/src/db/query/explain/tests.rs`
  - `explain_grouped_plan_snapshot_for_ordered_having_shape_is_stable`
  - `explain_grouped_plan_snapshot_for_hash_distinct_shape_is_stable`
- Added full verbose-diagnostics snapshot locks for top-n and index-range
  route shapes in:
  - `crates/icydb-core/src/db/executor/tests/semantics.rs`
  - `query_explain_execution_verbose_diagnostics_snapshot_for_top_n_seek_shape`
  - `query_explain_execution_verbose_diagnostics_snapshot_for_index_range_pushdown_shape`
- These tests explicitly freeze:
  - grouped explain-plan projection fields and shape metadata
  - verbose diagnostics line ordering and values for top-n seek and
    index-range limit-pushdown eligible plans

Validation:

- `cargo test -p icydb-core explain_grouped_plan_snapshot_for_ -- --nocapture` (pass)
- `cargo test -p icydb-core query_explain_execution_verbose_diagnostics_snapshot_for_ -- --nocapture` (pass)
- `cargo test -p icydb-core db::query::explain::tests:: -- --nocapture` (pass)

## Completed in This Pass (Slice G - Module Gravity-Well Split, Continuation)

- Split oversized continuation module into focused children:
  - `crates/icydb-core/src/db/executor/continuation/capabilities.rs`
  - `crates/icydb-core/src/db/executor/continuation/engine.rs`
  - `crates/icydb-core/src/db/executor/continuation/scalar.rs`
  - `crates/icydb-core/src/db/executor/continuation/mod.rs` now acts as
    boundary wiring + re-export surface
- No runtime behavior changes were introduced; this is a structure-only split.
- Existing continuation, load-structure, and grouped runtime tests continue to
  pass after the split.

Validation:

- `cargo fmt --all` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core continuation_capabilities_ -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::load_structure:: -- --nocapture` (pass)

## Completed in This Pass (Slice G - Module Gravity-Well Split, Load Terminal)

- Split oversized load terminal module into focused children:
  - `crates/icydb-core/src/db/executor/load/terminal/mod.rs`
  - `crates/icydb-core/src/db/executor/load/terminal/bytes.rs`
  - `crates/icydb-core/src/db/executor/load/terminal/ranking.rs`
- Kept module boundary and behavior unchanged:
  - no new terminals
  - no new route/planner semantics
  - no runtime behavior changes
- Shared helper and terminal test coverage remain in `terminal/mod.rs`; bytes
  fast paths and top/bottom-k terminal logic now live in dedicated files.

Validation:

- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core db::executor::load::terminal::tests:: -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::load_structure:: -- --nocapture` (pass)

## Completed in This Pass (Slice G - Module Gravity-Well Split, Load Contracts)

- Extracted oversized load module contract/type block from:
  - `crates/icydb-core/src/db/executor/load/mod.rs`
  into:
  - `crates/icydb-core/src/db/executor/load/contracts.rs`
- `load/mod.rs` now serves as boundary wiring + re-export surface for:
  - cursor/page DTOs
  - fast-path key result contracts
  - grouped route/stage projection contracts
  - load executor contract type
- No runtime behavior changes were introduced; this is a structure-only split.

Validation:

- `cargo fmt --all` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core db::executor::tests::load_structure:: -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::load::terminal::tests:: -- --nocapture` (pass)

## Completed in This Pass (Slice G - Module Gravity-Well Split, Access Execution-Contract Helpers)

- Extracted access strategy debug-summary and secondary pushdown matcher
  helpers from:
  - `crates/icydb-core/src/db/access/execution_contract/mod.rs`
  into:
  - `crates/icydb-core/src/db/access/execution_contract/summary.rs`
  - `crates/icydb-core/src/db/access/execution_contract/pushdown.rs`
- Kept execution-contract semantics unchanged:
  - access-plan lowering and route-class derivation remain in
    `execution_contract/mod.rs`
  - only summary/formatting and pushdown helper logic moved into dedicated
    submodules
- This reduces file gravity and keeps debug-summary logic isolated from core
  access execution-contract mechanics.

Validation:

- `cargo fmt --all` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core execution_contract -- --nocapture` (pass)

## Completed in This Pass (Slice G - Module Gravity-Well Split, Access Execution-Contract Module Root)

- Converted split access execution-contract module to canonical directory-root
  layout:
  - moved `crates/icydb-core/src/db/access/execution_contract.rs`
    to `crates/icydb-core/src/db/access/execution_contract/mod.rs`
- Kept behavior unchanged:
  - no execution-contract semantic changes
  - no route/planner/executor behavior changes
- Updated access-boundary runtime guard allowlist to the new canonical module
  root path.

Validation:

- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core db::access::tests::runtime_raw_access_path_references_stay_within_access_boundary -- --nocapture` (pass)
- `cargo test -p icydb-core execution_contract -- --nocapture` (pass)

## Completed in This Pass (Slice G - Module Gravity-Well Split, Access Execution-Contract Focused Submodules)

- Split `execution_contract/mod.rs` internals into focused child modules while
  preserving behavior:
  - `crates/icydb-core/src/db/access/execution_contract/types.rs`
    (`ExecutionMode`, bounds/order/distinct/path payload contracts)
  - `crates/icydb-core/src/db/access/execution_contract/executable.rs`
    (`ExecutableAccessPath`/`ExecutableAccessNode`/`ExecutableAccessPlan`)
  - `crates/icydb-core/src/db/access/execution_contract/route.rs`
    (`AccessRouteClass` + route-class derivation from executable plans)
  - `crates/icydb-core/src/db/access/execution_contract/strategy.rs`
    (`AccessStrategy` ownership, lowering entrypoint, debug summary surface)
- `execution_contract/mod.rs` now acts primarily as module wiring + re-export
  boundary + local tests.
- No route/planner/executor behavior changes were introduced; this is a
  structure-only split aligned with module-boundary guardrails.

Validation:

- `cargo fmt --all` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core execution_contract -- --nocapture` (pass)
- `cargo test -p icydb-core db::access::tests::runtime_raw_access_path_references_stay_within_access_boundary -- --nocapture` (pass)

## Completed in This Pass (Slice G - Module Gravity-Well Split, Aggregate Projection Covering/Decoder Extraction)

- Converted aggregate projection module to canonical directory-root layout:
  - moved `crates/icydb-core/src/db/executor/aggregate/projection.rs`
    to `crates/icydb-core/src/db/executor/aggregate/projection/mod.rs`
- Extracted covering projection helper logic into:
  - `crates/icydb-core/src/db/executor/aggregate/projection/covering.rs`
- Extracted covering projection component decoder logic into:
  - `crates/icydb-core/src/db/executor/aggregate/projection/decode.rs`
- Kept behavior unchanged:
  - projection fast-path eligibility and decode semantics are unchanged
  - no aggregate terminal, planner, or route behavior changes
  - `projection/mod.rs` now focuses on terminal orchestration, while
    covering-order/dedupe/decode mechanics are isolated in dedicated helpers

Validation:

- `cargo fmt --all` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core db::executor::tests::aggregate::field_projection_matrices::aggregate_field_target_covering_index_projection_terminals_match_effective_window -- --nocapture` (pass)

## Completed in This Pass (Slice G - Encoding Consistency Audit, Map Comparator Unification)

- Consolidated canonical map-entry comparator logic under one shared value
  helper:
  - `Value::canonical_cmp_map_entry(...)` in
    `crates/icydb-core/src/value/mod.rs`
- Updated map comparison and hashing paths to use the same comparator:
  - `crates/icydb-core/src/value/compare.rs`
  - `crates/icydb-core/src/value/hash.rs`
- Added explicit regression lock for map-entry ordering semantics:
  - `canonical_cmp_map_entry_orders_by_key_then_value` in
    `crates/icydb-core/src/value/tests.rs`
- This removes one duplicate comparator/encode-order path and locks canonical
  map key/value ordering consistency across value comparison and hashing.

Validation:

- `cargo fmt --all` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core canonical_cmp_map_entry_orders_by_key_then_value -- --nocapture` (pass)
- `cargo test -p icydb-core stable_hash_respects_canonical_map_order -- --nocapture` (pass)
- `cargo test -p icydb-core stable_hash_contract_vectors_are_frozen_for_upgrade_stability -- --nocapture` (pass)

## Completed in This Pass (Slice G - Encoding Consistency Audit, Predicate Sort-Key Comparator Alignment)

- Replaced local predicate map-entry ordering comparator with the shared
  `Value::canonical_cmp_map_entry(...)` authority in:
  - `crates/icydb-core/src/db/predicate/encoding.rs`
- Added a regression test lock to ensure duplicate-key map literals still
  normalize deterministically under predicate sort-key encoding:
  - `predicate_sort_key_normalizes_duplicate_map_keys_by_value_order`
- This removes another duplicate comparator implementation and keeps predicate
  normalization aligned with value compare/hash ordering semantics.

Validation:

- `cargo test -p icydb-core predicate_sort_key_normalizes_map_entry_order -- --nocapture` (pass)
- `cargo test -p icydb-core predicate_sort_key_normalizes_duplicate_map_keys_by_value_order -- --nocapture` (pass)
- `cargo check -p icydb-core` (pass)

## Completed in This Pass (Slice G - Encoding Consistency Audit, Fingerprint Hash Primitive Reuse)

- Removed duplicate value/tag/string hash writers from
  `crates/icydb-core/src/db/query/fingerprint/projection_hash.rs` by reusing
  the shared implementations in:
  - `crates/icydb-core/src/db/query/fingerprint/hash_parts.rs`
- Expanded helper visibility inside the `query::fingerprint` boundary so
  sibling modules now consume one canonical primitive implementation for:
  - `write_tag`
  - `write_u32`
  - `write_str`
  - `write_value`
- This reduces duplicated encoding code inside the fingerprint subsystem and
  keeps projection hashing aligned with the main explain/fingerprint stream
  encoding path.

Validation:

- `cargo test -p icydb-core db::query::fingerprint::projection_hash::tests:: -- --nocapture` (pass)
- `cargo test -p icydb-core db::predicate::encoding::tests:: -- --nocapture` (pass)
- `cargo check -p icydb-core` (pass)

## Completed in This Pass (Slice G - Encoding Consistency Audit, Aggregate Hash Primitive Alignment)

- Removed duplicate `write_tag`/`write_str` hash writers from:
  - `crates/icydb-core/src/db/query/fingerprint/aggregate_hash.rs`
- `aggregate_hash` now reuses the same shared fingerprint primitive helpers
  in:
  - `crates/icydb-core/src/db/query/fingerprint/hash_parts.rs`
- This completes hash primitive reuse across the three fingerprint encoders:
  - `hash_parts`
  - `projection_hash`
  - `aggregate_hash`

Validation:

- `cargo test -p icydb-core db::query::fingerprint::aggregate_hash::tests:: -- --nocapture` (pass)
- `cargo check -p icydb-core` (pass)

## Completed in This Pass (Slice G - Encoding Consistency Audit, Index Literal Encoding Authority Unification)

- Removed redundant literal encoding wrapper in:
  - `crates/icydb-core/src/db/index/predicate/mod.rs`
- `literal_index_component_bytes(...)` is now the single boundary helper for
  canonical index literal component encoding in index-predicate compilation.
- This reduces duplicate literal-encoding entrypoints and keeps compile-time
  index predicate literal lowering on one authority path.

Validation:

- `cargo test -p icydb-core db::index::predicate::tests:: -- --nocapture` (pass)
- `cargo check -p icydb-core` (pass)

---

## No Half-Finished Flow Policy (`0.45`)

Each tracked row above must end in one explicit state:

- complete with tests/docs, or
- deferred with owner + rationale

No row remains implicitly partial at release close.
