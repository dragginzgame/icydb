# 0.42 Status (as of 2026-03-06)

## 0.42 Scope

This status tracks `0.42.x` EXPLAIN work from:

- `AUDIT-FOR-TOMORROW.md`
- `docs/design/0.42-explain.md`
- `docs/design/0.41-execution-completeness.md`

`0.42` is the EXPLAIN surface freeze line that serializes the stabilized
execution model landed in `0.41`.

---

## In Scope (`0.42.x`)

- canonical execution-node descriptor for explain projection
- `EXPLAIN` base surface over descriptor tree
- `EXPLAIN VERBOSE` surface with route/pushdown diagnostics
- `EXPLAIN JSON` stable machine-readable surface
- snapshot/matrix tests that freeze node names and hierarchy

## Out of Scope (`0.42.x`)

- new execution-shape features
- planner redesign beyond explain projection needs
- cost-based optimization
- optimizer trace/cost-estimation surfaces
- join/distributed execution

---

## Roadmap Snapshot (`0.42.x`)

- [x] P0. prerequisites complete (`0.41` execution completeness A-E)
- [ ] A. freeze canonical `ExecutionNodeDescriptor` contract
- [ ] B. add centralized descriptor assembly layer (`execution_plan -> descriptor`)
- [ ] C. implement `EXPLAIN` text tree output
- [ ] D. implement `EXPLAIN VERBOSE` output
- [ ] E. implement canonical `EXPLAIN JSON` output
- [ ] F. lock snapshot/matrix coverage for vocabulary + hierarchy stability

Current `0.42.x` status: **in progress** (descriptor freeze/assembly slice started).

Release sequencing lock:

- `0.41` -> execution completeness (complete)
- `0.42` -> EXPLAIN contract and surfaces

---

## Item Tracker

### A) Canonical Descriptor Freeze

- [x] define one shared execution-node descriptor schema
- [x] include `execution_mode` and `node_properties` in descriptor contract
- [ ] map all frozen `0.41` node types into descriptor variants
      (in progress: access + predicate/order/distinct/pagination families wired)
- [ ] keep seek-family vocabulary explicit (`TopNSeek`, `AggregateSeekFirst`, `AggregateSeekLast`)
- [ ] lock two-stage predicate model (`IndexPredicatePrefilter` + `ResidualPredicateFilter`)
- [x] ensure aggregate terminal explain surfaces consume descriptor path

### B) Descriptor Assembly Layer

- [x] centralize descriptor construction outside renderer code
- [ ] enforce one canonical projection path reused by all explain surfaces

### C) Base EXPLAIN Surface

- [ ] render deterministic text tree from descriptor
- [ ] include core fields (node type, access, predicate, ordering, limit/cursor)

### D) EXPLAIN VERBOSE Surface

- [ ] project pushdown eligibility/rejection diagnostics
- [ ] include route-level seek/materialization reasoning

### E) EXPLAIN JSON Surface

- [x] treat JSON as canonical descriptor serialization
- [x] serialize descriptor tree with stable key naming
- [x] guarantee deterministic child ordering and fixed key ordering

Canonical JSON key order target:
`node_type`, `execution_mode`, `access_strategy`, `predicate_pushdown`,
`residual_predicate`, `projection`, `ordering_source`, `limit`, `cursor`,
`covering_scan`, `rows_expected`, `children`, `node_properties`

### F) Stability and Regression Gates

- [ ] add snapshot tests for text surface
- [ ] add snapshot tests for JSON surface
- [ ] add matrix coverage for key execution shapes:
      pk/prefix/range/multi-lookup, residual predicate split, top-N seek,
      `LIMIT 0`, aggregate seek, grouped strategy
- [ ] add guard tests for node-name and hierarchy drift

---

## Verification Readout (Current Slice)

Planning and implementation baseline:

- `0.41` status confirms execution-shape prerequisites complete
- `0.42` design and status docs created
- scalar aggregate terminal descriptor now emits `execution_mode` and `node_properties`
- scalar aggregate terminal descriptor assembly moved into dedicated executor explain module
- aggregate terminal explain plans now project one canonical execution-node descriptor
  (`execution_node_descriptor`) from the execution descriptor contract
- canonical execution-node descriptors now expose a deterministic base text-tree
  renderer (`render_text_tree`) for terminal explain output
- scalar load queries now project canonical execution-node descriptor trees
  through executor descriptor assembly and query/fluent `.explain_execution()`
- base text tree rendering now includes core descriptor fields
  (`access`, predicate metadata, ordering source, pagination details)
- load execution descriptors now preserve recursive access topology for
  `Union`/`Intersection` nodes and centralize predicate-stage node emission
- execution-node descriptors now expose canonical JSON rendering with fixed
  key order and recursive descriptor/access child serialization

Implementation verification commands (to run per slice as code lands):

- `cargo fmt --all`
- `cargo check -p icydb-core`
- `cargo test -p icydb-core db::query::explain -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::semantics -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::aggregate::session_matrices -- --nocapture`

Latest slice verification (2026-03-06):

- `cargo fmt --all` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core --lib session_load_terminal_explain -- --nocapture` (pass)
- `cargo test -p icydb-core --lib session_load_explain_execution_projects_descriptor_tree_for_ordered_limited_index_access -- --nocapture` (pass)
- `cargo test -p icydb-core --lib route_plan_grouped_explain_projection_and_execution_contract_is_frozen -- --nocapture` (pass)
