# 0.42 Status (as of 2026-03-06, closeout sprint)

## 0.42 Scope

This status tracks `0.42.x` EXPLAIN work from:

- `AUDIT-FOR-TOMORROW.md` (historical scope input)
- `docs/design/0.42-explain.md`
- `docs/design/0.41-execution-completeness.md`

`0.42` is the EXPLAIN surface freeze line that serializes the stabilized
execution model landed in `0.41`.

---

## In Scope (`0.42.x`)

- canonical execution-node descriptor for explain projection
- `EXPLAIN` base surface over descriptor tree
- `EXPLAIN VERBOSE` surface with route/pushdown diagnostics
- `EXPLAIN JSON` stable machine-readable surface
- snapshot/matrix tests that freeze node names and hierarchy

## Out of Scope (`0.42.x`)

- new execution-shape features
- planner redesign beyond explain projection needs
- cost-based optimization
- optimizer trace/cost-estimation surfaces
- join/distributed execution

---

## Roadmap Snapshot (`0.42.x`)

- [x] P0. prerequisites complete (`0.41` execution completeness A-E)
- [x] A. freeze canonical `ExecutionNodeDescriptor` contract (foundation + drift guards)
- [x] B. add centralized descriptor assembly layer (`execution_plan -> descriptor`)
- [x] C. implement deterministic descriptor text tree rendering
- [x] D. implement `EXPLAIN VERBOSE` output
- [x] E. implement canonical `EXPLAIN JSON` output
- [x] F. lock snapshot/matrix coverage for vocabulary + hierarchy stability
- [x] G. add first-class query/fluent text/json/verbose surface adapters

Current `0.42.x` status: **complete** (`0.42` explain contract and surface
freeze line closed).

Release sequencing lock:

- `0.41` -> execution completeness (complete)
- `0.42` -> EXPLAIN contract and surfaces

---

## Item Tracker

### Finalized Slice Focus (`Slice E`)

- closed remaining descriptor-family projection gaps by explicit deferral notes
  plus scalar-surface guard tests
- completed `0.42.x` exit freeze checklist
- preserved contract-only scope (no execution-shape expansion)

### A) Canonical Descriptor Freeze

- [x] define one shared execution-node descriptor schema
- [x] include `execution_mode` and `node_properties` in descriptor contract
- [x] map core load + aggregate seek families into descriptor variants
- [x] keep seek-family vocabulary explicit (`TopNSeek`, `AggregateSeekFirst`, `AggregateSeekLast`)
- [x] lock two-stage predicate model (`IndexPredicatePrefilter` + `ResidualPredicateFilter`)
- [x] ensure aggregate terminal explain surfaces consume descriptor path
- [x] close remaining descriptor-family projection gaps (projection/grouped node usage)
      via explicit scalar-surface deferral + guard tests

### B) Descriptor Assembly Layer

- [x] centralize descriptor construction outside renderer code
- [x] enforce one canonical projection path reused by current explain surfaces
- [x] expose first-class text/json/verbose API adapters over that path

### C) Base EXPLAIN Surface

- [x] render deterministic text tree from descriptor
- [x] include core fields (node type, access, predicate, ordering, limit/cursor)
- [x] expose direct query/fluent text surface (no manual render call needed)

### D) EXPLAIN VERBOSE Surface

- [x] project pushdown eligibility/rejection diagnostics
- [x] include route-level seek/materialization reasoning
- [x] strengthen verbose snapshot/matrix freeze coverage

### E) EXPLAIN JSON Surface

- [x] treat JSON as canonical descriptor serialization
- [x] serialize descriptor tree with stable key naming
- [x] guarantee deterministic child ordering and fixed key ordering

Canonical JSON key order target:
`node_type`, `execution_mode`, `access_strategy`, `predicate_pushdown`,
`residual_predicate`, `projection`, `ordering_source`, `limit`, `cursor`,
`covering_scan`, `rows_expected`, `children`, `node_properties`

### F) Stability and Regression Gates

- [x] add snapshot tests for text surface
- [x] add snapshot tests for JSON surface
- [x] add matrix coverage for key execution shapes:
      pk/prefix/range/multi-lookup, residual predicate split, top-N seek,
      `LIMIT 0`, aggregate seek, grouped strategy
- [x] add guard tests for node-name and hierarchy drift
- [x] add regression tests for new query/fluent text/json/verbose adapters

---

## Verification Readout (Current Slice)

Planning and implementation baseline:

- `0.41` status confirms execution-shape prerequisites complete
- canonical execution-node descriptor exists and is consumed by load + aggregate explain paths
- descriptor text and canonical JSON rendering are implemented and deterministic
- vocabulary and hierarchy drift guards are in place
- descriptor-level text/json/verbose surfaces are stable
- first-class query/fluent text/json/verbose adapters are landed and regression-tested
- verbose diagnostics now include explicit secondary pushdown outcomes, route
  hint diagnostics (`top_n_seek`, `index_range_limit_pushdown`, predicate stage),
  and descriptor-stage operator presence summaries
- verbose diagnostics now include deterministic diagnostics snapshots/matrix
  regression coverage for rejection + top-N + index-range-limit shapes
- projection/grouped descriptor families are explicitly deferred for the
  current scalar explain surface and guarded by node-family non-emission tests
- no remaining `0.42.x` explain-surface blockers

Implementation verification commands (to run per slice as code lands):

- `cargo fmt --all`
- `cargo check -p icydb-core`
- `cargo test -p icydb-core db::query::explain -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::semantics -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::aggregate::session_matrices -- --nocapture`

Latest slice verification (2026-03-06):

- `cargo fmt --all` (pass)
- `cargo test -p icydb-core --lib session_load_explain_execution_` (pass)
- `cargo test -p icydb-core --lib explain_execution_node_type_vocabulary_is_frozen` (pass)
- `cargo test -p icydb-core --lib explain_execution_` (pass)
- `cargo test -p icydb-core --lib query_explain_execution_verbose_` (pass)
- `cargo test -p icydb-core --lib query_explain_execution_scalar_surface_defers_projection_and_grouped_node_families` (pass)
