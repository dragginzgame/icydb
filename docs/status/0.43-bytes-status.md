# 0.43 Status (as of 2026-03-06, implementation closeout pass)

## 0.43 Scope

This status tracks `0.43.x` BYTES terminal work from:

- `docs/design/0.43-bytes.md`

`0.43` adds scalar load terminal payload-size introspection (`bytes()`) on top
of the stabilized `0.42` execution/explain baseline.

---

## In Scope (`0.43.x`)

- scalar fluent terminal `bytes()` on load queries
- executor terminal wiring for scalar load plans
- deterministic payload-byte summation for effective scalar response windows
- regression coverage for basic parity and empty-window behavior

## Out of Scope (`0.43.x`)

- grouped `aggregate(bytes())`
- SQL/CLI explain or SQL parser additions
- new access-path or planner execution-shape features
- explain-node vocabulary expansion
- global storage accounting (`TABLE_BYTES`, `INDEX_BYTES`, disk/block usage)

---

## Roadmap Snapshot (`0.43.x`)

- [x] A. add fluent scalar terminal surface (`FluentLoadQuery::bytes()`)
- [x] B. wire executor terminal (`LoadExecutor::bytes(plan)`)
- [x] C. implement v1 measurement path (sum persisted payload row lengths)
- [x] D. align docs/changelog for shipped `0.43` slices
- [x] E. expand matrix coverage (path/order/offset/limit/distinct interactions)

Current `0.43.x` status: **in progress** (scalar `bytes()` implementation,
coverage expansion, and docs/changelog alignment are complete; release cut is
pending).

Release sequencing lock:

- `0.42` -> EXPLAIN contract/surfaces (complete)
- `0.43` -> scalar BYTES terminal (in progress)

---

## Item Tracker

### Current Slice Focus

- landed first vertical slice: API + executor wiring + baseline tests
- preserved scalar-only scope (no grouped expansion)
- kept execution vocabulary unchanged

### A) API Surface and Policy Gate

- [x] add `bytes()` to core fluent load terminals
- [x] enforce existing non-paged fluent policy gate (`ensure_non_paged_mode_ready()`)
- [x] expose facade session wrapper method `bytes()`

### B) Execution Semantics

- [x] route through canonical scalar execution window (`execute(plan)`)
- [x] sum persisted row payload lengths for resulting ids
- [x] use saturating `u64` accumulation
- [x] return `0` for empty effective windows

### C) Regression Coverage

- [x] session parity test: `bytes()` equals persisted payload sum of execute window
- [x] empty-window test: `bytes()` returns `0`
- [x] add broader path-parity matrix coverage (forced full scan vs forced index-prefix)
- [x] add ordering/window interaction coverage (`order_by + offset + limit`)
- [x] add distinct-window behavior coverage
- [x] add terminal-local saturation coverage (overflow clamps at `u64::MAX`)

---

## Verification Readout (Current Slice)

Landed implementation files:

- `crates/icydb-core/src/db/executor/load/terminal.rs`
- `crates/icydb-core/src/db/query/fluent/load/terminals.rs`
- `crates/icydb/src/db/session/load.rs`
- `crates/icydb-core/src/db/executor/tests/aggregate/mod.rs`
- `crates/icydb-core/src/db/executor/tests/aggregate/path_parity_matrices.rs`
- `crates/icydb-core/src/db/executor/tests/aggregate/session_matrices.rs`
- `docs/changelog/0.43.md`
- `CHANGELOG.md`

Implementation verification commands (run):

- `cargo fmt --all`
- `cargo test -p icydb-core session_load_bytes_ -- --nocapture`
- `cargo test -p icydb-core aggregate_bytes_ -- --nocapture`
- `cargo test -p icydb-core aggregate_parity_distinct_ -- --nocapture`
- `cargo test -p icydb-core aggregate_field_terminal_parity_distinct_ -- --nocapture`
- `cargo test -p icydb-core aggregate_bytes_parity_ordered_page_window_asc -- --nocapture`
- `cargo test -p icydb-core payload_len_sum_ -- --nocapture`
- `cargo check -p icydb-core --all-targets --locked`

Latest slice verification (2026-03-06):

- `cargo fmt --all` (pass)
- `cargo test -p icydb-core session_load_bytes_ -- --nocapture` (pass)
- `cargo test -p icydb-core aggregate_bytes_ -- --nocapture` (pass)
- `cargo test -p icydb-core aggregate_parity_distinct_ -- --nocapture` (pass)
- `cargo test -p icydb-core aggregate_field_terminal_parity_distinct_ -- --nocapture` (pass)
- `cargo test -p icydb-core aggregate_parity_ordered_page_window_desc -- --nocapture` (pass)
- `cargo test -p icydb-core aggregate_bytes_parity_ordered_page_window_asc -- --nocapture` (pass)
- `cargo test -p icydb-core payload_len_sum_ -- --nocapture` (pass)
- `cargo check -p icydb-core --all-targets --locked` (pass)
