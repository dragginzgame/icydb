# 0.40 Status (as of 2026-03-02)

## 0.40 Scope

This status tracks `0.40.x` full-system audit and hardening work from:

- `docs/design/0.40-audit-everything.md`
- `docs/changelog/0.40.md`
- `docs/audits/reports/2026-03-02/0.40-planner-executor-semantic-ownership.md`

`0.40` is an audit line focused on boundary ownership, orchestration
consolidation, and structural guard coverage before larger model changes.

---

## In Scope (`0.40.x`)

- planner vs executor semantic ownership hardening
- load orchestration multiplicity reduction behind one staged execution spine
- continuation boundary centralization and cursor-policy ownership tightening
- architectural guard-test migration to structural/signature/type contracts
- release and tracking artifacts for audit slices

## Out of Scope (`0.40.x`)

- query-surface expansion
- compatibility shim work
- speculative refactors without an audit finding
- cost-based planner or new execution strategies

---

## Release Snapshot (`0.40.x`)

- [x] `0.40.x` root changelog line initialized
- [x] `docs/changelog/0.40.md` initialized
- [x] `0.40` design tracker initialized
- [x] large-module split baseline landed (`0.40.0`)
- [x] executable access contract hardening landed (`0.40.1`)
- [x] continuation + load entrypoint unification landed (`0.40.2`)
- [x] load structure guards and grouped cursor-policy tightening landed (`0.40.3`)
- [x] post-unification hardening landed (`0.40.4`):
      stage-typed load pipeline state, grouped-route semantic re-check removal from executor feasibility, grouped runtime revalidation-surface guards, plan-root wrappers for grouped strategy-hint access, global DISTINCT grouped fail-closed guard coverage (runtime kind guard + planner-bypass invariant test), grouped scalar DISTINCT policy bypass guards, delete-executor grouped-shape gating through executable-plan APIs, grouped DISTINCT policy probing routed behind executable-plan boundary wrappers, typed page-materialization request contracts, typed physical-stream request contracts, aggregate fast-path input-shape convergence, typed load finalization outcome metrics, and grouped route-stage nested ownership bundles
- [ ] planner-vs-executor semantic ownership checklist fully closed
- [ ] remaining `0.40` audit workstreams closed

Current `0.40.x` status: **in progress (`0.40.0`-`0.40.4` landed; audit
closure workstreams remain open).**

---

## Workstream Tracker

### 3.1 Error Taxonomy and Boundary Mapping (P0)

- [ ] verify runtime class preservation at facade conversion boundaries
- [ ] verify runtime query execute class/origin preservation
- [ ] remove remaining string-based classification/fallback paths
- [ ] close ambiguous variant/domain mapping
- [ ] add class x origin conversion matrix tests

### 3.2 Planner vs Executor Semantic Ownership (P0)

- [x] inventory duplicated semantic validation branches in executor modules
- [x] move grouped projection-layout validation authority to planner handoff construction
- [x] remove grouped global-distinct fold direct calls to planner cursor-policy helper
- [x] keep continuation access routed through planner wrapper (`grouped_cursor_policy_violation_for_continuation`)
- [x] lock linear load orchestration stage order with structural guard tests
- [x] reduce pipeline coordination-state drift risk with stage-typed state artifacts and zero optional-slot baseline
- [x] remove grouped HAVING semantic policy re-check from executor route feasibility
- [x] remove grouped aggregate/distinct semantic policy re-checks from executor route feasibility
- [x] add structural guard for grouped ordered-route runtime revalidation surface
- [x] tighten grouped strategy-hint helper visibility and route/explain access through plan-root wrappers
- [x] add planner-bypass invariant guard test for unsupported global DISTINCT grouped aggregate kind
- [x] add fail-closed runtime kind guard for global DISTINCT grouped execution (`COUNT`/`SUM` only) with focused unit coverage
- [x] add executor boundary guard for grouped scalar DISTINCT policy violations via planner helper
- [x] add planner-bypass invariant guard test for grouped scalar DISTINCT policy violations
- [x] remove delete-executor direct logical-plan enum matching in favor of executable-plan grouped shape checks
- [x] route grouped DISTINCT policy guard reads through executable-plan boundary wrapper instead of direct inner-plan probing
- [x] replace multi-contract load page materialization argument surface with typed request contract (`PageMaterializationRequest`)
- [x] replace multi-contract physical stream resolver argument surface with typed request contract (`PhysicalStreamRequest`)
- [x] align secondary aggregate fast-path fold helper with `AggregateFastPathInputs` envelope to reduce parallel argument-shape drift
- [x] replace load finalization output-plumbing argument surface with typed metrics contract (`ExecutionOutcomeMetrics`)
- [x] split grouped route-stage payload into explicit nested ownership bundles (`GroupedPlannerPayload`, `GroupedRoutePayload`, `IndexSpecBundle`, `GroupedExecutionContext`)
- [ ] finish remaining semantic duplicate reduction to invariant-only executor checks
- [ ] add remaining guard tests for planner-bypass invalid-shape behavior where still missing

### 3.3 Projection and Expression Authority (P0)

- [ ] confirm one canonical projection shape construction path
- [ ] remove helper terminal paths that bypass semantic projection builders
- [ ] confirm grouped/scalar projection share canonical authority
- [ ] lock projection unification with regression + structural guard coverage

### 3.4 Architectural Guard-Test Policy (P1)

- [ ] enforce architectural invariants via structural/type/signature/invariant tests only
- [ ] remove remaining `include_str!`-based architectural enforcement in `icydb-core`
- [ ] keep `include_str!` only for fixture/snapshot/golden inputs

### 3.5 Fingerprint Semantics and Isolation (P1)

- [ ] verify fingerprint inputs are semantic-only and deterministic
- [ ] remove alias/explain hash-input coupling where present
- [ ] lock behavior with equivalence and negative drift tests

### 3.6 Cursor, Continuation, and Resume Contracts (P1)

- [ ] audit decode vs invariant continuation validation boundaries
- [ ] keep user-input vs invariant failures separated
- [ ] lock grouped/scalar cursor parity and invalidation behavior with tests

### 3.7 Persistence and Decode Safety (P1)

- [ ] audit persisted decode boundaries for bounded/fallible handling
- [ ] verify corruption vs invalid-input classification boundaries
- [ ] lock malformed-payload fail-closed behavior

### 3.8 Origin Fidelity and Observability (P2)

- [ ] verify origin coverage across planner/cursor/recovery/identity boundaries
- [ ] inventory and intentionally resolve remaining origin collapse sites
- [ ] add telemetry-facing origin-preservation tests

---

## Verification Readout (Current Slice)

- `cargo fmt --all` (pass)
- `cargo check -p icydb-core` (pass)
- `cargo test -p icydb-core db::executor::tests::load_structure -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::route::aggregate::route_plan_grouped_wrapper_downgrades_ordered_strategy_for_unsupported_having_operator -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::route::budget -- --nocapture` (pass)
- `cargo test -p icydb-core db::query::plan::tests::group::grouped_distinct_policy_contract_rejects_distinct_without_adjacency_proof -- --nocapture` (pass)
- `cargo test -p icydb-core grouped_global_distinct_unsupported_kind_is_executor_invariant_only_when_planner_is_bypassed -- --nocapture` (pass)
- `cargo test -p icydb-core grouped_global_distinct_kind_guard_accepts_count_and_sum -- --nocapture` (pass)
- `cargo test -p icydb-core grouped_global_distinct_kind_guard_rejects_non_count_sum -- --nocapture` (pass)
- `cargo test -p icydb-core grouped_scalar_distinct_policy_violation_is_executor_invariant_only_when_planner_is_bypassed -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::semantics::delete_applies_order_and_delete_limit -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::pagination::cursor_pk -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::context::tests -- --nocapture` (pass)
- `cargo test -p icydb-core db::executor::tests::aggregate::tail_matrices -- --nocapture` (pass)
- `cargo clippy -p icydb-core --all-targets -- -D warnings` (blocked in this environment: `Invalid cross-device link (os error 18)`)
- `/bin/bash -lc "CARGO_TARGET_DIR=/tmp/icydb-target cargo clippy -p icydb-core --all-targets -- -D warnings"` (blocked in this environment: `Invalid cross-device link (os error 18)`)
