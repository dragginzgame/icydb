# 0.81 — Bounded Computed ORDER BY Aliases

## 0.81.2 - 2026-04-14 - Admit Direct Bounded Computed ORDER BY Terms

This patch removes the remaining alias-only restriction from the `0.81` scalar
computed ordering slice. The admitted bounded scalar family is unchanged; the
SQL surface now accepts the same direct canonical order terms that alias
normalization was already lowering onto internally.

Examples now admitted:

```sql
SELECT age FROM Customer ORDER BY age + 1;
SELECT age FROM Customer ORDER BY age + rank;
SELECT age FROM Customer ORDER BY ROUND(age / 3, 2) DESC;
```

What changed:

* widened reduced SQL `ORDER BY` parsing to accept direct bounded arithmetic
  and `ROUND(...)` terms on the same admitted family already used internally by
  alias normalization:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/parser/clauses.rs`
  * admitted direct forms now include:
    * `field + literal`
    * `field + field`
    * the same bounded `-`, `*`, `/` family
    * `ROUND(field op rhs, scale)` and `ROUND(field, scale)`
  * unsupported direct `ORDER BY` functions still fail closed on one explicit
    parser feature boundary instead of widening generic expression grammar
* exposed the existing bounded arithmetic and `ROUND(...)` parser helpers to
  the shared clause parser so direct `ORDER BY` syntax reuses the same parser
  ownership boundary as projection parsing:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/parser/projection.rs`
* added parser coverage for the new direct admitted family:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/parser/tests/mod.rs`
  * direct parse coverage now locks:
    * `ORDER BY age + 1`
    * `ORDER BY age + salary`
    * `ORDER BY ROUND(age / 3, 2)`
* added lowering, session execution, and explain coverage for the direct form:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/lowering/tests/mod.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_projection.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_explain.rs`
* added the direct-form cache proof at the session SQL boundary:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_surface.rs`
  * `direct_bounded_numeric_order_terms_use_the_normal_sql_cache_path`
  * this proves direct bounded computed order terms:
    * stay on the normal compiled-command cache lane
    * stay on the normal SQL select-plan cache lane
    * canonicalize onto the same structural query fingerprint and logical plan
      identity as the lowered internal form before cache insertion
* added live PocketIC canister coverage for the direct form:
  * `/home/adam/projects/icydb/testing/pocket-ic/tests/sql_canister.rs`
* added one recurring SQL perf-audit row for the new direct field-to-field
  order family:
  * `/home/adam/projects/icydb/testing/pocket-ic/tests/sql_perf_audit.rs`
  * new scenario key:
    * `user.age_plus_rank.direct_order.asc.limit3`
  * query:
    * `SELECT id, age + rank FROM PerfAuditUser ORDER BY age + rank ASC, id ASC LIMIT 3`
* added the first recurring security-boundary audit report and synced the two
  stale invariant scripts it depends on to the current module layout:
  * recurring definition:
    * `/home/adam/projects/icydb/docs/audits/recurring/security/security-audit.md`
  * first report:
    * `/home/adam/projects/icydb/docs/audits/reports/2026-04/2026-04-14/security-boundary.md`
  * invariant-script sync:
    * `/home/adam/projects/icydb/scripts/ci/check-field-projection-invariants.sh`
    * `/home/adam/projects/icydb/scripts/ci/check-layer-authority-invariants.sh`
  * the script updates are layout-only:
    * `db/predicate/runtime.rs` -> `db/predicate/runtime/mod.rs`
    * `db/index/envelope.rs` -> `db/index/envelope/mod.rs`

What this patch still does not do:

* no grouped computed ordering
* no grouped computed projections
* no aggregate-derived computed ordering
* no generic nested expression grammar
* no new cache lane; this stays on the same query-owned semantic artifact and
  the same cache boundaries established in `0.80` and hardened in `0.81.1`

Why this is the right cut:

* alias ordering in `0.81.0` and `0.81.1` already proved the canonical
  internal computed-order path
* the remaining gap was surface syntax admission, not planner or executor
  ownership
* admitting the direct forms on that same path widens the SQL surface without
  creating a second semantic or cache model

Verification:

* `cargo test -p icydb-core parse_select_statement_with_direct_bounded_computed_order_terms -- --nocapture`
* `cargo test -p icydb-core direct_bounded_numeric_order -- --nocapture`
* `cargo test -p icydb-core bounded_numeric_aliases -- --nocapture`
* `cargo clippy -p icydb-core -p icydb-testing-integration --test sql_canister --test sql_perf_audit -- -D warnings`
* `POCKET_IC_BIN=/tmp/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_canister sql_canister_query_endpoint_executes_direct_bounded_numeric_order_queries -- --nocapture`
* `POCKET_IC_BIN=/tmp/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_perf_audit sql_perf_audit_harness_reports_instruction_samples -- --nocapture`

---

## 0.81.1 - 2026-04-14 - Harden Alias-Order Cache Parity And Coverage

This patch does not widen the shipped `0.81.0` SQL surface. It hardens that
same admitted alias-order family so the cache contract is explicit in tests,
the widened arithmetic family is covered more completely, and the recurring SQL
perf audit now measures one field-to-field alias-order scenario too.

What changed:

* added the explicit cache/parity acceptance test the `0.81` design doc called
  for:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_surface.rs`
  * `compile_sql_query_order_by_bounded_numeric_alias_matches_canonical_structural_query_identity`
  * this proves the admitted aliased SQL form compiles to the same structural
    query fingerprint and logical-plan identity as the canonical lowered
    internal form, so cache identity does not drift between alias spelling and
    normalized semantic shape
* widened the existing alias-order lowering coverage to include bounded
  field-to-field arithmetic and `ROUND(field + field, scale)`:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/lowering/tests/mod.rs`
  * the same canonical internal order-expression assertions now cover:
    * `age + age AS total_age ORDER BY total_age`
    * `ROUND(age + age, 2) AS rounded_total ORDER BY rounded_total`
* widened session-boundary execution and explain coverage for the same admitted
  family:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_projection.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_explain.rs`
  * these tests now cover:
    * literal-RHS arithmetic alias ordering
    * field-to-field arithmetic alias ordering
    * `ROUND(...)` alias ordering over both admitted bounded scalar families
* widened live PocketIC canister coverage for the same SQL family:
  * `/home/adam/projects/icydb/testing/pocket-ic/tests/sql_canister.rs`
  * the reduced SQL canister harness now exercises:
    * `SELECT name, age + rank AS total FROM SqlTestUser ORDER BY total ASC LIMIT 2`
    in the same live query endpoint suite that already covered `age + 1` and
    `ROUND(age / 3, 2)` alias ordering
* added one recurring SQL perf-audit row for field-to-field computed alias
  ordering:
  * `/home/adam/projects/icydb/testing/pocket-ic/tests/sql_perf_audit.rs`
  * new scenario key:
    * `user.age_plus_rank.alias_order.asc.limit3`
  * query:
    * `SELECT id, age + rank AS total FROM PerfAuditUser ORDER BY total ASC, id ASC LIMIT 3`
  * this keeps the SQL perf harness aligned with the admitted `0.81` alias-order
    family instead of only measuring the literal-RHS arithmetic form

Why this patch matters:

* `0.81.0` already depended on one architectural rule: admitted alias ordering
  must canonicalize before cache lookup/insertion
* `0.81.1` turns that from design intent into a locked test contract
* it also closes the most obvious coverage gap inside the admitted family:
  field-to-field arithmetic alias ordering was semantically in-bounds but was
  less explicitly covered than the literal-RHS form
* the perf harness now samples both:
  * `age + 1 AS next_age ORDER BY next_age`
  * `age + rank AS total ORDER BY total`
  so future regressions in computed-alias ordering are visible in the recurring
  SQL audit surface

Verification:

* `cargo test -p icydb-core compile_sql_query_order_by_bounded_numeric_alias_matches_canonical_structural_query_identity -- --nocapture`
* `cargo test -p icydb-core bounded_numeric_aliases -- --nocapture`
* `cargo test -p icydb-core compile_sql_command_normalizes_order_by_alias_for_bounded_numeric_projection_targets -- --nocapture`
* `cargo clippy -p icydb-core -p icydb-testing-integration --test sql_canister --test sql_perf_audit -- -D warnings`
* `POCKET_IC_BIN=/tmp/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_canister sql_canister_query_endpoint_executes_order_by_bounded_numeric_alias_queries -- --nocapture`
* `POCKET_IC_BIN=/tmp/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_perf_audit sql_perf_audit_harness_reports_instruction_samples -- --nocapture`

---

## 0.81.0 - 2026-04-14 - Admit Bounded Computed Scalar ORDER BY Aliases

This patch closes one narrow SQL coherence gap: `ORDER BY <alias>` now works
when the alias resolves to one admitted bounded scalar computed projection
expression. That includes the current scalar arithmetic family and bounded
`ROUND(..., scale)` projection family, while still keeping direct expression
ordering and grouped computed ordering fail-closed.

Examples now admitted:

```sql
SELECT age + 1 AS next_age FROM Customer ORDER BY next_age;
SELECT ROUND(age / 3, 2) AS rounded_age FROM Customer ORDER BY rounded_age DESC;
SELECT dexterity + charisma AS total FROM Character ORDER BY total;
```

What changed:

* widened SQL alias normalization so admitted arithmetic and `ROUND(...)`
  projection aliases canonicalize onto the same internal order-expression form
  as their equivalent non-aliased semantic expression:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/lowering/normalize.rs`
* extended the canonical internal order-expression parser/renderer beyond the
  old `LOWER(field)` / `UPPER(field)`-only family so it now admits:
  * plain field terms
  * bounded arithmetic terms with one field on the left and one field or
    literal on the right
  * bounded `ROUND(expr, scale)` over that same admitted scalar family
  * implementation:
    * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/expr/ast.rs`
    * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/order_term.rs`
* added one shared lowering helper so projection lowering and alias-order
  normalization both derive the same canonical scalar `Expr` tree instead of
  carrying separate projection-only and order-only shapes:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/lowering/select/projection.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/lowering/select/mod.rs`
* switched order validation from the old single-field text-wrapper assumption
  to expression type inference:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/validate/order.rs`
  * arithmetic and `ROUND(...)` alias order terms are now validated as
    orderable numeric expressions instead of being rejected as unknown fields
* kept the old strict planner policy only for the existing casefold
  expression-order family that still depends on matching index-backed access:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/validate/core.rs`
  * `LOWER(field)` / `UPPER(field)` still preserve the current
    index-satisfied-expression-order contract
  * admitted numeric alias ordering now flows through normal materialized sort
    instead of being blocked by that casefold-specific policy gate
* generalized resolved-order lowering so supported order expressions no longer
  assume one `FunctionCall(field)` wrapper:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/semantics/logical.rs`
  * the resolved-order path now walks field leaves through bounded arithmetic
    and `ROUND(...)` expressions before compiling one expression-backed order
    source
* updated cursor-boundary validation so order-expression continuation slots no
  longer assume every supported order term maps back to one single source
  field type:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/cursor/boundary.rs`
  * the continuation boundary path now derives expected slot shape from the
    inferred order-expression type where needed
* added parser/lowering/session/explain/canister coverage for the new admitted
  alias family:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/order_term.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/lowering/tests/mod.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_projection.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_explain.rs`
  * `/home/adam/projects/icydb/testing/pocket-ic/tests/sql_canister.rs`

What this patch does not do:

* no direct `ORDER BY age + 1`
* no direct `ORDER BY ROUND(age / 3, 2)`
* no grouped computed projection widening
* no grouped computed alias ordering
* no aggregate-derived computed alias ordering
* no new cache tier or side cache for order-only semantics

Why this is the right cut:

* the missing piece was alias normalization plus canonical order-expression
  ownership, not a new executor path
* keeping this as alias-only support preserves the bounded SQL surface instead
  of silently turning it into generic direct-expression ordering
* the compiled SQL cache line stays coherent because admitted alias ordering
  canonicalizes onto the same structural order/query representation before
  cache lookup or insertion, instead of creating a second order-only semantic
  shape
* planner/runtime stay alias-neutral after normalization; execution only sees
  the canonical expression-backed order source

Verification:

* `cargo test -p icydb-core order_by_bounded_numeric_aliases -- --nocapture`
* `cargo test -p icydb-core supported_order_expr_helpers_round_trip_bounded_numeric_terms -- --nocapture`
* `cargo clippy -p icydb-core --all-targets -- -D warnings`
* `cargo clippy -p icydb-testing-integration --test sql_canister -- -D warnings`
* `POCKET_IC_BIN=/tmp/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_canister order_by_bounded_numeric_alias -- --nocapture`

---
