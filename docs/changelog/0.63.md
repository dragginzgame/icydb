# 0.63 Changelog

## 0.63.4 - 2026-03-24 - Executor Boundary and Failure-Ownership Follow-Through

### Summary

`0.63.4` continues the same consolidation-only `0.63` audit line after the
aggregate/access-path cleanup in `0.63.3`.

This patch stays in the executor/runtime interior. It does not add new SQL,
planner, cursor, or persistence behavior. The work is another mechanical pass
over obvious owner boundaries:

- move more executor-entrypoint and route/delete/load-surface mismatches onto
  the local contract types that already own those execution shapes
- keep save-validation, scan, continuation, and relation-delete diagnostics on
  the types that already define those boundary contracts
- continue the same owner-side cleanup through cursor payload validation,
  commit/recovery/migration bookkeeping, and the remaining index planning /
  index-only execution boundaries
- finish draining the last inline `Err(InternalError::...)` construction sites
  from production `db` code so call sites stop rebuilding local invariant or
  unsupported failures ad hoc

### Changed

- tightened route/load/delete boundary ownership:
  - `crates/icydb-core/src/db/executor/route/guard.rs`
  - `crates/icydb-core/src/db/executor/route/fast_path.rs`
  - `crates/icydb-core/src/db/executor/route/planner/entrypoints.rs`
  - `crates/icydb-core/src/db/executor/pipeline/orchestrator/mod.rs`
  - `crates/icydb-core/src/db/executor/pipeline/orchestrator/strategy/context.rs`
  - `crates/icydb-core/src/db/executor/delete/mod.rs`
  - route fast-path, grouped/scalar load-surface, and delete-plan mismatches
    now construct through local owners or `InternalError` owner-side helpers
    instead of inline boundary branches
- tightened save preflight and mutation follow-through ownership:
  - `crates/icydb-core/src/error/mod.rs`
  - `crates/icydb-core/src/db/executor/mutation/save_validation.rs`
  - `crates/icydb-core/src/db/executor/mutation/save.rs`
  - `crates/icydb-core/src/db/executor/mutation/commit_window.rs`
  - save preflight now routes schema-invalid, primary-key, field-shape,
    decimal-scale, set/map encoding, duplicate atomic-save key, and
    index-store generation drift failures through owner-side constructors
- continued executor page / scan / continuation cleanup:
  - `crates/icydb-core/src/db/executor/terminal/page.rs`
  - `crates/icydb-core/src/db/executor/scan/fast_stream/mod.rs`
  - `crates/icydb-core/src/db/executor/scan/fast_stream_route/mod.rs`
  - `crates/icydb-core/src/db/executor/scan/secondary_index.rs`
  - `crates/icydb-core/src/db/executor/scan/index_range_limit.rs`
  - `crates/icydb-core/src/db/executor/continuation/engine.rs`
  - `crates/icydb-core/src/db/executor/continuation/scalar.rs`
  - `crates/icydb-core/src/db/executor/continuation/grouped/context.rs`
  - scalar page ordering/paging/delete phase checks, fast-stream route/spec
    checks, and continuation load-shape / cursor-input / scan-budget /
    grouped-boundary arity failures now live on the boundary owners instead of
    inline execution branches
- tightened aggregate, ranking, and entrypoint contract ownership:
  - `crates/icydb-core/src/db/executor/aggregate/contracts/spec.rs`
  - `crates/icydb-core/src/db/executor/aggregate/terminals.rs`
  - `crates/icydb-core/src/db/executor/aggregate/execution.rs`
  - `crates/icydb-core/src/db/executor/aggregate/numeric/mod.rs`
  - `crates/icydb-core/src/db/executor/terminal/ranking/mod.rs`
  - `crates/icydb-core/src/db/executor/pipeline/entrypoints/grouped.rs`
  - `crates/icydb-core/src/db/executor/pipeline/entrypoints/scalar/mod.rs`
  - `crates/icydb-core/src/db/executor/pipeline/entrypoints/scalar/surface.rs`
  - aggregate output-kind mismatches, numeric direct-execution global DISTINCT
    drift, ranking output-kind checks, and grouped/scalar traced-surface plus
    scalar materialized-rows continuation mismatches now live on the local
    contract owners
- continued relation and blocked-delete boundary cleanup:
  - `crates/icydb-core/src/db/relation/validate.rs`
  - blocked-delete proof lifting now happens on `BlockedDeleteProof` itself,
    leaving the typed delete-validation edge to consume one owner-side
    diagnostic conversion
- tightened cursor, commit, and migration ownership follow-through:
  - `crates/icydb-core/src/db/cursor/boundary.rs`
  - `crates/icydb-core/src/db/cursor/anchor.rs`
  - `crates/icydb-core/src/db/cursor/spine.rs`
  - `crates/icydb-core/src/db/cursor/error.rs`
  - `crates/icydb-core/src/db/commit/store.rs`
  - `crates/icydb-core/src/db/commit/marker.rs`
  - `crates/icydb-core/src/db/commit/prepare.rs`
  - `crates/icydb-core/src/db/commit/memory.rs`
  - `crates/icydb-core/src/db/commit/hooks.rs`
  - `crates/icydb-core/src/db/commit/recovery.rs`
  - `crates/icydb-core/src/db/commit/mod.rs`
  - `crates/icydb-core/src/db/migration/mod.rs`
  - cursor boundary validation and anchor payloads now route through
    `CursorPlanError` owner-side constructors instead of inline payload
    rebuilding
  - commit control-slot, marker payload, structural commit-input, memory-slot,
    recovery-integrity, and hook-resolution failures now route through local
    owners or `InternalError` owner-side constructors instead of ad hoc local
    formatting
  - migration step/plan construction, durable cursor compatibility, persisted
    step-index validation, and migration-state serialization now route through
    owner-side constructors instead of local store/serialize payload assembly
- continued index boundary cleanup through key build, planning, and scan:
  - `crates/icydb-core/src/db/index/key/build.rs`
  - `crates/icydb-core/src/db/index/plan/mod.rs`
  - `crates/icydb-core/src/db/index/plan/unique.rs`
  - `crates/icydb-core/src/db/index/plan/commit_ops.rs`
  - `crates/icydb-core/src/db/index/predicate/mod.rs`
  - `crates/icydb-core/src/db/index/scan.rs`
  - `crates/icydb-core/src/db/index/envelope.rs`
  - `crates/icydb-core/src/db/index/entry.rs`
  - index-key field-count, missing-source-field, expression source-type
    mismatch, and component-size failures now route through owner-side
    constructors
  - structural index planning, unique validation, commit-op synthesis, scan,
    predicate execution, envelope continuation checks, and index-entry
    encode/decode wrappers now also construct through owner-side
    `InternalError` helpers instead of repeated inline index payloads
- finished draining the last production inline `Err(InternalError::...)`
  branches in `db`:
  - after this pass, remaining direct `Err(InternalError::...)` matches under
    `crates/icydb-core/src/db` are test-only, and the production side is back
    to owner-side construction rather than local ad hoc wrapper sites

### Validation

- `cargo fmt --all --check`
- `cargo test -p icydb-core error::tests -- --nocapture`
- `cargo test -p icydb-core db::session::tests -- --nocapture`
- `cargo test -p icydb-core db::cursor::tests -- --nocapture`
- `cargo test -p icydb-core db::commit::tests -- --nocapture`
- `cargo test -p icydb-core db::migration::tests -- --nocapture`
- `cargo test -p icydb-core db::index::key::tests -- --nocapture`
- `cargo test -p icydb-core db::index::predicate::tests -- --nocapture`
- `cargo clippy -p icydb-core --all-targets -- -D warnings`

### Notes

- `0.63.4` is still a consolidation-only patch.
- the public SQL, cursor, persistence, and runtime feature surface remains
  unchanged.
- the main outcome is that executor/runtime failure construction is now more
  consistently owned by the boundary types themselves across executor,
  cursor, commit, migration, and index layers, with far fewer inline mismatch
  branches left in production code.

---

## 0.63.3 - 2026-03-24 - Aggregate and Access-Path Contract Ownership Follow-Through

### Summary

`0.63.3` continues the same consolidation-only `0.63` audit line after the
legacy-shell and error-layer cuts in `0.63.1` and `0.63.2`.

This patch stays inside the executor/runtime cleanup surface. It does not add
new SQL, cursor, planner, or persistence behavior. The work is narrower and
more mechanical:

- move more aggregate boundary/result decoding onto the contract types that
  already define those result shapes
- move repeated aggregate kind and reducer-state invariants onto the prepared
  ops and state machines that own them
- tighten covering projection / bytes / grouped DISTINCT decode contracts so
  local execution helpers stop rebuilding the same fail-closed checks inline
- continue the access-path cleanup by making lowered prefix/range spec
  consumption contracts live on the owners that actually consume those specs

### Changed

- continued scalar aggregate boundary/result ownership cleanup:
  - `crates/icydb-core/src/db/executor/aggregate/contracts/spec.rs`
  - `crates/icydb-core/src/db/executor/aggregate/terminals.rs`
  - `crates/icydb-core/src/db/executor/aggregate/projection/mod.rs`
  - `ScalarAggregateOutput` now owns COUNT / EXISTS / id-terminal result-kind
    decoding
  - `ScalarTerminalBoundaryOutput` now owns its own COUNT / EXISTS / id /
    id-pair output-kind mismatch construction
  - `ScalarProjectionBoundaryOutput` now owns its own values / count /
    values-with-ids / terminal-value output-kind mismatch construction
- moved aggregate operation precondition construction onto prepared aggregate
  op owners:
  - `crates/icydb-core/src/db/executor/aggregate/execution.rs`
  - `PreparedScalarTerminalOp` now owns the MIN/MAX/FIRST/LAST and MIN/MAX
    validation used by id-terminal and id-by-slot execution
  - `PreparedScalarProjectionOp` now owns terminal-value aggregate-kind
    validation, covering DISTINCT-strategy requirements, constant-covering
    values-with-ids rejection, and the terminal-value materialized-branch
    fail-closed contract
- collapsed repeated reducer-state and terminal-state invariants onto the
  owning aggregate state machines:
  - `crates/icydb-core/src/db/executor/aggregate/contracts/state.rs`
  - `ScalarAggregateReducerState` and `GroupedAggregateReducerState` now own
    their repeated reducer-state mismatch construction
  - `ScalarTerminalAggregateState` and `GroupedTerminalAggregateState` now own
    the repeated field-target-only and storage-key-required invariants for
    SUM / AVG / MIN / MAX / FIRST / LAST update paths
- tightened field-target extrema contract ownership:
  - `crates/icydb-core/src/db/executor/aggregate/field_extrema.rs`
  - `FieldExtremaFoldSpec` now owns the repeated non-extrema,
    route-fast-path-required, output-finalization, and direction-mismatch
    invariants used by materialized and streaming field-extrema execution
- tightened grouped global DISTINCT decode ownership:
  - `crates/icydb-core/src/db/executor/aggregate/distinct.rs`
  - a local grouped output contract now owns the zero-key grouped-page decode
    invariants for continuation cursor absence, singleton row count, empty
    grouped key, and single aggregate-value width
- tightened covering projection / bytes terminal local contracts:
  - `crates/icydb-core/src/db/executor/aggregate/projection/mod.rs`
  - `crates/icydb-core/src/db/executor/terminal/bytes.rs`
  - covering projection component scan path-shape checks now live on local
    covering-component contracts rather than inline helper branches
  - `bytes(field)` covering-constant and PK fast-path preconditions now live
    on local bytes boundary owners instead of inline terminal branches
- continued lowered spec consumption cleanup across executor access traversal:
  - `crates/icydb-core/src/db/executor/stream/access/bindings.rs`
  - `crates/icydb-core/src/db/executor/stream/access/traversal.rs`
  - `crates/icydb-core/src/db/executor/stream/access/physical.rs`
  - `crates/icydb-core/src/db/executor/traversal.rs`
  - `AccessSpecCursor` now owns missing/unused lowered prefix-spec
    consumption invariants
  - `IndexRangeTraversalContract` now owns lowered index-range spec alignment,
    required-spec, and unused-spec invariants
  - the structural physical access leaf now owns its remaining lowered
    index-prefix spec-count preconditions

### Validation

- `cargo fmt --all --check`
- `cargo test -p icydb-core db::session::tests -- --nocapture`
- `cargo clippy -p icydb-core --all-targets -- -D warnings`

### Notes

- `0.63.3` is still a consolidation-only patch.
- the public SQL, cursor, persistence, and runtime feature surface remains
  unchanged.
- the main outcome is a cleaner executor/access/aggregate ownership baseline,
  with fewer adapter-local invariant constructors left behind in the runtime.

---

## 0.63.2 - 2026-03-24 - Legacy Shell Hard Cuts and Index Boundary Cleanup

### Summary

`0.63.2` continues the same consolidation line as `0.63.0` and `0.63.1`, but
with a narrower target: remove the leftover compatibility shells and test-only
forwarding layers that were still hanging off the cleaned-up runtime
boundaries.

This patch still does not expand the executable SQL or query surface. The main
effect is that several older compatibility/testing seams are now gone entirely,
and a small adjacent index cleanup finishes a couple of owner-side boundary
moves that were still obviously mechanical.

### Changed

- removed the remaining session SQL compatibility shells from both crate
  surfaces:
  - `crates/icydb/src/db/session/mod.rs`
  - `crates/icydb-core/src/db/session/tests.rs`
  - the old `FacadeSqlLegacySurfaceExt` and `SessionSqlLegacySurfaceExt`
    compatibility traits are gone
  - the session test suites now call direct dispatch helpers over the real
    session APIs instead of preserving removed lane-specific helper surfaces
- removed dead test-support and forwarding layers that no longer owned real
  behavior:
  - removed `crates/icydb-core/src/db/error/test_support.rs`
  - removed `crates/icydb-core/src/db/query/explain/test_support.rs`
  - removed `crates/icydb-core/src/db/query/fingerprint/test_support.rs`
  - removed `crates/icydb-core/src/db/query/plan/test_support.rs`
  - removed the now-empty `crates/icydb-core/src/db/error/mod.rs`
  - surviving helper behavior now lives either on the owning types or directly
    in the only test modules that still use it
- continued owner-side constructor cleanup around persisted decode, commit, and
  relation validation:
  - `crates/icydb-core/src/error/mod.rs`
  - `crates/icydb-core/src/db/commit/store.rs`
  - `crates/icydb-core/src/db/commit/marker.rs`
  - `crates/icydb-core/src/db/commit/recovery.rs`
  - `crates/icydb-core/src/db/data/persisted_row.rs`
  - `crates/icydb-core/src/db/data/structural_row.rs`
  - `crates/icydb-core/src/db/relation/mod.rs`
  - `crates/icydb-core/src/db/relation/save_validate.rs`
  - commit corruption, missing persisted-slot, persisted payload decode, and
    strong-relation save-validation failures now construct through the owning
    error types instead of free helper functions
- removed the last obvious compatibility helpers from tests and local helper
  shells:
  - `crates/icydb-core/src/db/executor/tests/mod.rs`
  - `crates/icydb-core/src/db/data/structural_field/mod.rs`
  - `crates/icydb-core/src/db/data/mod.rs`
  - `crates/icydb-core/src/db/executor/terminal/row_decode.rs`
  - the dead scalar `PageCursor` extension trait is gone
  - structural-field tests now call the real decode owners directly instead of
    the old `decode_structural_field_bytes(...)` wrapper and re-export
- tightened the adjacent index ownership boundary without changing index
  behavior:
  - `crates/icydb-core/src/db/index/envelope.rs`
  - `crates/icydb-core/src/db/index/range.rs`
  - `crates/icydb-core/src/db/index/entry.rs`
  - `crates/icydb-core/src/db/index/plan/commit_ops.rs`
  - `crates/icydb-core/src/db/index/mod.rs`
  - `envelope_is_empty(...)` now lives with the rest of the
    continuation-envelope helpers in `index::envelope`
  - `IndexEntryEncodeError` now owns its commit-time lift into `InternalError`
    instead of `commit_ops.rs` rebuilding that mapping inline

### Validation

- `cargo fmt --all --check`
- `cargo test -p icydb db::session::tests -- --nocapture`
- `cargo test -p icydb-core db::session::tests -- --nocapture`
- `cargo test -p icydb-core db::data::structural_field::tests -- --nocapture`
- `cargo test -p icydb-core db::executor::terminal::row_decode::tests -- --nocapture`
- `cargo test -p icydb-core db::index::envelope::tests -- --nocapture`
- `cargo test -p icydb-core db::index::entry::tests -- --nocapture`
- `cargo clippy -p icydb --all-targets -- -D warnings`
- `cargo clippy -p icydb-core --all-targets -- -D warnings`

### Notes

- `0.63.2` is still a consolidation-only patch.
- the public SQL, cursor, persistence, and runtime feature surface remains
  unchanged.
- the main output is a harder cut against leftover compatibility scaffolding,
  with cleaner owner-side boundaries in the remaining index and error-adjacent
  paths.

---

## 0.63.1 - 2026-03-24 - Post-De-Monomorphization Consolidation Audit Follow-Through

### Summary

`0.63.1` continues the same mechanical `0.63` audit line after the initial
`0.63.0` ownership cleanup.

This patch does not expand the executable SQL or query surface. It keeps
draining the same post-`0.62` consolidation seams:

- grouped planner/runtime handoff invariants that were still rebuilt locally
- aggregate, stream, route, continuation, and post-access invariant wrappers
  that still routed through old free constructor paths
- the remaining shared `db/error` constructor layer that sat between DB code
  and the canonical `InternalError` taxonomy owner

The result is a cleaner end state for the audit standard itself: constructor
ownership now lives primarily on the owning error or contract type, and the
old DB-wide forwarding layer is gone.

### Changed

- continued grouped planner/runtime boundary cleanup:
  - `crates/icydb-core/src/db/query/plan/group.rs`
  - `crates/icydb-core/src/db/query/plan/grouped_layout.rs`
  - `crates/icydb-core/src/db/query/plan/semantics/group_distinct.rs`
  - `crates/icydb-core/src/db/query/plan/semantics/group_model.rs`
  - `crates/icydb-core/src/db/executor/pipeline/contracts/grouped/route_stage/projection.rs`
  - `crates/icydb-core/src/db/executor/pipeline/contracts/grouped/stages.rs`
  - `crates/icydb-core/src/db/executor/aggregate/runtime/grouped_output/projection.rs`
  - grouped layout, grouped DISTINCT, grouped HAVING, and grouped route-stage
    invariants now stay on planner/grouped owner types instead of being rebuilt
    ad hoc at executor call sites
- drained the remaining executor-invariant wrapper drift across runtime helpers:
  - `crates/icydb-core/src/db/executor/aggregate/terminals.rs`
  - `crates/icydb-core/src/db/executor/aggregate/projection/mod.rs`
  - `crates/icydb-core/src/db/executor/aggregate/numeric/mod.rs`
  - `crates/icydb-core/src/db/executor/stream/access/traversal.rs`
  - `crates/icydb-core/src/db/executor/stream/access/physical.rs`
  - `crates/icydb-core/src/db/executor/pipeline/orchestrator/mod.rs`
  - `crates/icydb-core/src/db/executor/delete/mod.rs`
  - the remaining dense aggregate, stream/access, delete, post-access,
    orchestrator, and continuation sites now construct query-executor
    invariants through `InternalError` directly instead of the old DB wrapper
- collapsed the shared DB error-constructor layer onto the taxonomy owner:
  - `crates/icydb-core/src/error/mod.rs`
  - `crates/icydb-core/src/db/error/mod.rs`
  - removed:
    - `crates/icydb-core/src/db/error/executor.rs`
    - `crates/icydb-core/src/db/error/planner.rs`
    - `crates/icydb-core/src/db/error/query.rs`
  - `InternalError` now owns the executor, planner, query, and cursor
    constructor helpers that had still been routed through `db/error/*`
  - `db/error` is reduced to real conversion helpers rather than thin
    constructor forwarding
- tightened adjacent owner-side mappings that depended on the removed layer:
  - `crates/icydb-core/src/db/scalar_expr.rs`
  - `crates/icydb-core/src/db/cursor/error.rs`
  - `crates/icydb-core/src/db/cursor/continuation.rs`
  - `crates/icydb-core/src/db/executor/plan_validate.rs`
  - `crates/icydb-core/src/db/relation/mod.rs`
  - `crates/icydb-core/src/db/relation/reverse_index.rs`
  - `crates/icydb-core/src/db/executor/mutation/save_validation.rs`
  - these call sites now use `InternalError` or the owning domain type
    directly, rather than depending on the old shared forwarding layer

### Validation

- `cargo fmt --all --check`
- `cargo test -p icydb-core db::session::tests -- --nocapture`
- `cargo test -p icydb-core error::tests -- --nocapture`
- `cargo test -p icydb-core db::query::plan::tests::group -- --nocapture`
- `cargo test -p icydb-core db::executor::projection::tests -- --nocapture`
- `cargo clippy -p icydb-core --all-targets -- -D warnings`

### Notes

- `0.63.1` is still a consolidation-only patch.
- the public SQL, cursor, and runtime feature surface remains unchanged.
- the main user-visible effect is lower internal drift and a cleaner ownership
  baseline for future maintenance.

---

## 0.63.0 - 2026-03-23 - Post-De-Monomorphization Consolidation Audit

### Summary

`0.63.0` starts the post-`0.62` consolidation line.

This release is not a new runtime feature phase. The large wasm-driven
de-monomorphization work already landed across `0.58` through `0.62`; the
remaining problem was structural drift after that rewrite:

- repeated error and validation wrappers at query/session boundaries
- duplicate field-resolution helpers in planner and SQL entrypoints
- split projection/materialization authority after the structural projection
  refactor
- mutation and reverse-index delta bookkeeping spread across parallel paths
- cursor invariant handling that still mixed raw reasons with already-prefixed
  internal-error messages
- SQL session helpers that kept rebuilding the same lane, grouping, and
  prepared-lowering checks locally

`0.63.0` attacks those seams directly. The result is a cleaner internal
authority map with the current executable surface kept stable.

Design and tracking references:

- `docs/design/0.63-mega-audit/0.63-design.md`
- `docs/design/0.63-mega-audit/0.63-status.md`

### Changed

- consolidated session query error wrappers under one owner:
  - `crates/icydb-core/src/db/session/mod.rs`
  - `crates/icydb-core/src/db/session/query.rs`
  - `crates/icydb-core/src/db/session/sql.rs`
  - session-facing query invariant, unsupported-query, and continuation
    serialization wrappers now live in one session boundary instead of being
    rebuilt ad hoc in local query and SQL paths
- consolidated mutation and reverse-index delta authority:
  - `crates/icydb-core/src/db/commit/prepared_op.rs`
  - `crates/icydb-core/src/db/executor/mutation/commit_window.rs`
  - `crates/icydb-core/src/db/relation/reverse_index.rs`
  - reverse-index membership changes now flow through one
    `PreparedIndexDeltaKind` authority
  - commit-window counter increments and index metric emission now share one
    delta translation path
- collapsed projection/materialization authority:
  - `crates/icydb-core/src/db/executor/projection/materialize/mod.rs`
  - `crates/icydb-core/src/db/executor/projection/materialize/structural.rs`
  - `crates/icydb-core/src/db/executor/projection/eval/mod.rs`
  - `crates/icydb-core/src/db/executor/projection/eval/scalar.rs`
  - typed projection rows, slot-row validation, and structural SQL projection
    now use one prepared projection-plan owner
  - compiled scalar projection evaluation now shares one recursive core instead
    of separate typed and structural paths
- tightened query/session unsupported and intent construction:
  - `crates/icydb-core/src/error/mod.rs`
  - `crates/icydb-core/src/db/query/intent/errors/mod.rs`
  - query-origin unsupported execution construction now belongs to
    `InternalError` / `QueryError`
  - grouped-query intent rejection now uses query-error-owned constructors
    rather than local `QueryError::Intent(...)` assembly
- collapsed duplicate aggregate and grouped field-resolution helpers:
  - `crates/icydb-core/src/db/query/plan/validate/symbols.rs`
  - `crates/icydb-core/src/db/query/plan/validate/mod.rs`
  - `crates/icydb-core/src/db/query/fluent/load/validation.rs`
  - `crates/icydb-core/src/db/session/sql.rs`
  - `crates/icydb-core/src/db/query/plan/validate/grouped/structure/mod.rs`
  - `crates/icydb-core/src/db/query/plan/validate/grouped/policy/rules.rs`
  - aggregate target field resolution now has one planner/query validation
    owner used by fluent aggregate terminals and SQL aggregate dispatch
  - grouped aggregate target-field validation now uses one helper across
    grouped structure validation and grouped global-DISTINCT policy checks
- normalized cursor invariant handling:
  - `crates/icydb-core/src/db/cursor/error.rs`
  - `crates/icydb-core/src/db/cursor/validation.rs`
  - `crates/icydb-core/src/db/cursor/mod.rs`
  - `crates/icydb-core/src/db/error/cursor.rs`
  - `crates/icydb-core/src/db/query/plan/continuation.rs`
  - `crates/icydb-core/src/db/executor/executable_plan.rs`
  - `crates/icydb-core/src/db/session/query.rs`
  - cursor invariant reasons now stay raw inside `CursorPlanError`
  - the canonical executor-invariant prefix is now applied once at the
    cursor-to-internal error boundary
- simplified SQL session query-lane ownership:
  - `crates/icydb-core/src/db/session/sql.rs`
  - query-lane unsupported routing now has one helper
  - prepared SQL statement lowering and typed binding now have one shared owner
  - grouped vs scalar SQL query-intent gating now uses one session helper
  - lowered query/explain lane execution now routes through one shared query-lane
    dispatcher
- simplified planner expression type inference field lookup:
  - `crates/icydb-core/src/db/query/plan/expr/type_inference/mod.rs`
  - schema field-kind lookup for expression inference now uses one local helper
    instead of repeating the same `UnknownExprField` mapping across field and
    aggregate target paths

### Validation

- `cargo fmt --all --check`
- `cargo test -p icydb-core db::session::tests -- --nocapture`
- `cargo test -p icydb-core error::tests -- --nocapture`
- `cargo test -p icydb-core db::query::intent::errors::tests -- --nocapture`
- `cargo test -p icydb-core db::executor::projection::tests -- --nocapture`
- `cargo test -p icydb-core db::commit::tests -- --nocapture`
- `cargo test -p icydb-core prepared_op -- --nocapture`
- `cargo test -p icydb-core db::cursor::tests -- --nocapture`
- `cargo test -p icydb-core db::query::plan::tests::group -- --nocapture`
- `cargo test -p icydb-core db::query::plan::expr::type_inference::tests -- --nocapture`

### Notes

- `0.63.0` is intentionally a hygiene and ownership release rather than a new
  capability release.
- the public SQL/query feature surface remains the `0.62.11` surface
- the main output is lower internal duplication and clearer ownership for the
  next measured follow-up work
