# IcyDB 0.180 Changelog

## 0.180.34

### 📝 Summary

- Compacts schema predicate validation diagnostics by replacing ad hoc operator
  and literal message strings with bounded typed payloads.
- Compacts grouped plan validation diagnostics by carrying `CompareOp` and
  `AggregateKind` payloads instead of rendered operator/kind strings.
- Preserves useful field and field-to-field context while continuing the
  no-runtime-message direction for wasm-sensitive validation paths.

### 🔧 Changed

- Adds compact schema validation operator and literal-reason payloads for
  predicate validation failures.
- Preserves field identity plus field-to-field right-hand field context for
  invalid operator diagnostics.
- Moves grouped HAVING unsupported-operator and grouped aggregate-kind
  unsupported diagnostics onto planner-owned enum payloads.
- Updates affected tests to assert structured variants instead of rendered
  diagnostic prose.
- Removes stale diagnostics-only residual-filter message arguments from the
  direct data-row page path after the compact constructor hard cut.

### 📊 Audit

- Refreshes the comparable wasm-footprint audit against the 2026-06-09
  baseline with profile `wasm-release` and SQL variant `sql-on`.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 6,516 bytes,
  `minimal_metrics` 288,647 bytes, `one_simple` 2,192,755 bytes,
  `one_sql_query` 2,729,686 bytes, `one_fluent_query` 2,192,748 bytes,
  `one_complex` 2,213,588 bytes, `ten_simple` 2,213,431 bytes, and
  `ten_complex` 2,234,324 bytes.
- Canonical shrunk gzipped `.wasm` sizes are now: `minimal` 4,095 bytes,
  `minimal_metrics` 111,769 bytes, `one_simple` 716,659 bytes,
  `one_sql_query` 942,293 bytes, `one_fluent_query` 716,557 bytes,
  `one_complex` 723,253 bytes, `ten_simple` 718,778 bytes, and
  `ten_complex` 725,752 bytes.
- Compared with the 2026-06-09 baseline, raw `.wasm` changes are: `minimal`
  unchanged, `minimal_metrics` unchanged, `one_simple` -78,892 bytes,
  `one_sql_query` -92,259 bytes, `one_fluent_query` -78,892 bytes,
  `one_complex` -78,884 bytes, `ten_simple` -81,584 bytes, and
  `ten_complex` -81,447 bytes.
- Compared with the 2026-06-09 baseline, gzipped `.wasm` changes are:
  `minimal` -1 byte, `minimal_metrics` -2 bytes, `one_simple` -27,387 bytes,
  `one_sql_query` -32,490 bytes, `one_fluent_query` -27,566 bytes,
  `one_complex` -27,198 bytes, `ten_simple` -28,379 bytes, and
  `ten_complex` -27,691 bytes.

### 🧪 Testing

- Validates formatting, focused schema-predicate and grouped-plan tests,
  SQL-enabled `icydb-core` clippy, SQL-enabled `icydb-core` library tests,
  public facade tests, whitespace checks, and the refreshed wasm-footprint
  audit.

## 0.180.33

### 📝 Summary

- Compacts schema transition rejection diagnostics by replacing production
  detail strings with typed transition-detail codes.
- Keeps rich first-difference transition text available in tests without
  retaining that prose in production diagnostic payloads.
- Removes a schema DDL admission wrapper string payload so admission failures
  keep stable typed identity.

### 🔧 Changed

- Routes schema transition rejection detail through compact
  transition-detail codes in production builds.
- Keeps test-only rich detail for transition comparison assertions.
- Updates schema DDL admission conversion to carry the typed
  schema-version-admission error directly.
- Updates affected tests to assert stable diagnostic variants instead of
  rendered transition prose.

### 🧪 Testing

- Validates formatting, focused schema transition tests, targeted SQL surface
  tests, SQL-enabled `icydb-core` clippy, SQL-enabled `icydb-core` library
  tests, whitespace checks, and targeted source scans for stale production
  transition-detail strings.

## 0.180.32

### 📝 Summary

- Compacts reduced SQL parser invalid-syntax diagnostics by replacing message
  strings with bounded syntax reason and token-context payloads.
- Keeps parser diagnostics stable and structured without retaining arbitrary
  reduced syntax prose in runtime payloads.
- Updates parser and lowering tests away from brittle rendered-message checks.

### 🔧 Changed

- Adds compact SQL syntax error reason, expected-token, found-token, integer
  literal, and clause-order payloads.
- Routes parser and lowering invalid-syntax construction through typed
  context instead of free-form message strings.
- Preserves useful token and clause context while removing old reduced
  syntax-message plumbing.

### 🧪 Testing

- Validates formatting, focused SQL parser and lowering tests, SQL-enabled
  `icydb-core` clippy, SQL-enabled `icydb-core` library tests, whitespace
  checks, and targeted scans for stale invalid-syntax message payloads.

## 0.180.31

### 📝 Summary

- Compacts grouped aggregate budget, executor corruption, and unsupported SQL
  parameter-placement diagnostics.
- Replaces remaining internal message/resource strings in those paths with
  bounded code, origin, parameter-index, and reason payloads.
- Preserves useful diagnostic context while continuing the no-runtime-message
  direction for wasm-sensitive error paths.

### 🔧 Changed

- Routes grouped aggregate budget failures through compact limit/need context
  instead of resource prose.
- Routes executor corruption helpers through bounded diagnostic identity rather
  than free-form message strings.
- Carries unsupported SQL parameter placement as an optional parameter index
  plus a compact reason code instead of rendered lowering prose.
- Updates affected tests to assert structured context and stable diagnostic
  identity instead of historical message text.

### 🧪 Testing

- Validates formatting, SQL-enabled `icydb-core` clippy, SQL-enabled
  `icydb-core` library tests, whitespace checks, and targeted source scans for
  stale message-bearing parameter-placement diagnostics.

## 0.180.30

### 📝 Summary

- Restores compact context after earlier diagnostic compaction flattened some
  planner and executor errors too far.
- Keeps the no-runtime-message direction intact by carrying bounded codes,
  indexes, arity counts, offsets, and signature prefixes instead of rendered
  field names or expression strings.
- Covers cursor validation, expression planning, compiled projection
  evaluation, aggregate field reads, and grouped-key hashing.

### 🔧 Changed

- Adds compact cursor payload reason codes plus optional slot/term indexes,
  window offsets, boundary arity values, and continuation-signature prefixes.
- Adds compact projection access/value/operator/function/aggregate codes so
  compiled-expression failures retain useful local context.
- Restores indexed/code context for expression type-admission, aggregate
  field-value, and grouped-key hash diagnostics while avoiding string payloads.
- Updates affected tests to assert structured diagnostic variants and compact
  context instead of historical message text.

### 🧪 Testing

- Validates formatting, SQL-enabled `icydb-core` clippy, SQL-enabled
  `icydb-core` library tests, whitespace checks, and targeted source scans for
  stale unit-shaped cursor/projection diagnostics.

## 0.180.29

### 📝 Summary

- Restores compact context to order-planner diagnostics after the first
  planner compaction pass flattened them too far.
- Carries order term and primary-key component indexes instead of rendered
  field names.
- Keeps `usize` for core-internal indexes because order specs are Vec-backed
  and not architecturally bounded.

### 🔧 Changed

- Adds indexed context to unknown, unorderable, duplicate, and missing
  primary-key tie-break `ORDER BY` diagnostics.
- Updates order validation to populate first/duplicate term indexes and
  missing primary-key component indexes.
- Records the compact-context recovery rule in the 0.180 design note.

### 🧪 Testing

- Validates formatting, SQL-enabled `icydb-core` clippy, SQL-enabled
  `icydb-core` library tests, whitespace checks, and targeted source scans for
  context-free order diagnostics.

## 0.180.28

### 📝 Summary

- Compacts aggregate field, grouped key, and planner expression diagnostics to
  code-only variants.
- Removes discarded field/value/hash/type prose from aggregate extraction,
  grouped key canonicalization, and expression type-admission errors.
- Keeps the existing executor and planner diagnostic taxonomy while moving
  tests away from brittle embedded labels.

### 🔧 Changed

- Collapses aggregate field-value extraction and comparison errors to compact
  variants without field/kind/value payloads.
- Collapses grouped key hash failures to a code-only variant.
- Collapses expression planner mismatch diagnostics for aggregate targets,
  function arguments, unary operands, CASE branches, and binary operands to
  code-only variants.
- Removes obsolete expression rendering helpers that only fed discarded
  diagnostic prose.

### 🧪 Testing

- Validates formatting, SQL-enabled `icydb-core` clippy, SQL-enabled
  `icydb-core` library tests, whitespace checks, and targeted source scans for
  removed aggregate/group/expression payload constructors.

## 0.180.27

### 📝 Summary

- Compacts cursor token wire, cursor-plan continuation, and compiled
  projection diagnostics to code-only variants.
- Removes discarded cursor and expression payload prose while preserving stable
  diagnostic classification, lower-layer class/origin routing, and cursor wire
  behavior.
- Updates affected tests to assert diagnostic variants and compact taxonomy
  instead of historical message text.

### 🔧 Changed

- Collapses cursor token encode/decode and cursor-plan continuation mismatch
  errors to unit/code-only variants.
- Removes cursor boundary label rendering and projection expression payload
  rendering that only fed discarded internal diagnostic prose.
- Keeps projection reader failures class/origin-aware while dropping rendered
  message strings from the projection adapter path.
- Deletes now-dead grouped cursor, lowered-index, function-name, and aggregate
  reducer label helpers.

### 🧪 Testing

- Validates formatting, SQL-enabled `icydb-core` clippy, SQL-enabled
  `icydb-core` library tests, whitespace checks, and targeted source scans for
  removed cursor/token/projection payload constructors.

## 0.180.26

### 📝 Summary

- Hard-cuts `InternalError::new` to a class/origin-only constructor.
- Removes crate-internal diagnostic relabeling that accepted discarded prose.
- Drops generated primary-key decode message strings from macro output while
  preserving compact diagnostic code, class, origin, and detail identity.

### ⚠️ Breaking

- `InternalError::new` now accepts only `ErrorClass` and `ErrorOrigin`.
- Callers that still supplied an ad hoc message must use stable diagnostic
  codes, structured detail, or a rendering-layer lookup instead of relying on
  runtime-owned prose.

### 🔧 Changed

- Collapses executor, registry, visitor, projection, startup rebuild, and
  test-only query-internal conversions onto code-only `InternalError`
  constructors.
- Removes dead `to_string` and `format!` paths where the resulting text was
  already discarded by compact diagnostics.
- Keeps store-origin corruption and invariant detail payloads compact and
  origin-owned.

### 🧪 Testing

- Validates formatting, affected-crate clippy for `icydb-core`, `icydb`, and
  `icydb-schema-derive`, SQL-enabled `icydb-core` library tests, public facade
  tests, macro compile-pass/fail tests, whitespace checks, and targeted source
  scans for the removed message-bearing helper shapes.

## 0.180.25

### 📝 Summary

- Hard-cuts query unsupported and serialize-internal helper constructors to
  code-only forms.
- Removes discarded SQL/query helper prose and cursor serialization prose that
  no longer contributes to the public error contract.
- Preserves compact diagnostic code, class, origin, and detail identity for
  affected unsupported-query and serialize-internal failures.

### 🔧 Changed

- Collapses `QueryError::unsupported_query` and
  `QueryError::serialize_internal` to no-argument compact constructors.
- Routes delete-count wrong-lane execution, SQL DDL missing-derivation,
  EXPLAIN unsupported-lane failures, and cursor token serialization failures
  through the compact constructors without retaining per-call prose.
- Simplifies SQL session test helper rejection paths now that unsupported-query
  diagnostics are code-owned instead of string-owned.

### 🧪 Testing

- Validates formatting, SQL-enabled `icydb-core` clippy, SQL-enabled
  `icydb-core` library tests, write-boundary guards, whitespace checks, and
  targeted scans for argument-bearing unsupported-query and serialize-internal
  constructors.

## 0.180.24

### 📝 Summary

- Hard-cuts the broad query-executor invariant surface to code-only
  diagnostics.
- Compacts generic runtime, planner, cursor, index, relation, aggregate, SQL
  write, and query invariant paths that previously accepted or retained
  discarded prose.
- Preserves stable diagnostic code, class, origin, and compact detail identity
  while keeping human-facing diagnostic prose owned by the CLI/rendering layer.

### 🔧 Changed

- Collapses `InternalError::query_executor_invariant` and
  `QueryError::invariant` to no-argument compact constructors.
- Removes ignored context strings from aggregate output decoding, projection
  boundaries, covering component decoding, ranking, traversal, scalar page
  scans, authority checks, grouped execution, SQL write projection, and SQL
  query guard helpers.
- Deletes or narrows helper plumbing that only existed to build labels for
  compacted runtime failures.
- Updates source guards to assert compact invariant constructors and accepted
  schema/authority boundaries without depending on obsolete message text.

### 🧪 Testing

- Validates formatting, SQL-enabled `icydb-core` clippy, SQL-enabled
  `icydb-core` library tests, write-boundary guards, whitespace checks, and
  targeted source scans for string-bearing query invariant constructors.

## 0.180.23

### 📝 Summary

- Hard-cuts generic runtime, planner, cursor, index, relation, and aggregate
  compact diagnostic constructors to code-only forms.
- Removes discarded invariant, unsupported, and internal prose from commit,
  cursor, relation, grouped planning, projection, scalar expression, mutation,
  and aggregate helper paths.
- Keeps affected failures on stable diagnostic code, class, origin, and compact
  detail identity instead of retaining runtime-only labels.

### 🔧 Changed

- Compacts another layer of central runtime and query diagnostic constructors
  before the broader query-executor invariant cleanup.
- Routes generic planner, cursor, relation, index, and aggregate failures
  through code-only constructors where the prose was already ignored by the
  public error contract.
- Updates affected tests away from brittle internal message checks and toward
  structured diagnostic identity.

### 🧪 Testing

- Validates formatting, SQL-enabled `icydb-core` clippy, focused diagnostic
  tests, and targeted scans for the compacted constructor families.

## 0.180.22

### 📝 Summary

- Hard-cuts another layer of compact store, index, serialize, identity, and
  cursor diagnostic constructors to no-argument code-only forms.
- Removes discarded runtime prose from commit-marker, row-format,
  journal-format, key/index, cursor, hash, covering-component, and index-entry
  decode paths.
- Deletes index-key label helper plumbing that only existed to feed removed
  size-limit diagnostic detail.

### 🔧 Changed

- Keeps affected runtime failures on stable diagnostic code, class, and origin
  identity instead of retaining labels, lengths, source errors, or other
  internal prose in production error constructors.
- Routes commit component, control-slot size, startup index rebuild,
  covering-component length, index-entry decode, and index-component size
  failures through compact constructors without changing the underlying
  validation gates.
- Leaves larger persisted-row, query, and executor diagnostic families for
  later cleanup slices.

### 🧪 Testing

- Validates formatting, SQL-enabled `icydb-core` clippy, SQL-enabled
  `icydb-core` library tests, write-boundary guards, whitespace checks, and
  targeted scans for argument-bearing calls to the compacted constructor set.

## 0.180.21

### 📝 Summary

- Compacts persisted-row encode/decode diagnostics further by removing
  runtime-only mismatch, slot, and row-emission prose from structural field,
  row reader, slot writer, journal batch/tail, commit-marker, key-mismatch,
  covering-projection, and index-plan corruption paths.
- Hard-cuts store corruption, invariant, unsupported, and store-backed
  index-plan diagnostic constructors to no-argument compact-code forms.
- Removes now-dead rich diagnostic string macros and developer-summary text
  helpers that only fed ignored runtime messages.

### 🔧 Changed

- Keeps store and index-plan failures on stable diagnostic code/class/origin
  identity instead of formatting internal payload detail into runtime errors.
- Routes affected call sites through compact constructors so production wasm
  does not retain dead detail strings for these store families.
- Leaves broader diagnostic constructors that still model distinct non-store
  boundaries for later cleanup slices.

### 🧪 Testing

- Validates formatting, SQL-enabled `icydb-core` clippy, SQL-enabled
  `icydb-core` library tests, and targeted source scans for the removed store
  constructor arguments.

## 0.180.20

### 📝 Summary

- Compacts persisted-row scalar decode corruption diagnostics so runtime
  failures retain stable diagnostic-code/class/origin identity and accepted
  field ownership without retaining payload-specific prose.
- Removes scalar decode label plumbing for fixed-width, bool, unit, float,
  integer, text, timestamp, duration, subaccount, and scalar-envelope
  corruption paths.
- Continues the brittleness cleanup by replacing more test assertions on
  internal error messages with diagnostic code, class, and origin checks.

### 🔧 Changed

- Adds a compact persisted-row field decode corruption constructor and routes
  scalar decode helper failures through it.
- Keeps the field name in persisted-row decode diagnostics so ownership and
  accepted-field attribution remain visible where tests intentionally assert
  that context.
- Leaves schema/proc-macro validation tests on rendered diagnostic text because
  those crates do not yet expose stable typed diagnostic codes.

### 📊 Audit

- Refreshes the wasm-footprint audit after persisted-row scalar diagnostic
  compaction.
- The generated audit report is partially comparable because the selected
  historical baseline lacks a matching `minimal_metrics` artifact, but the
  comparable rows all moved down.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 6,516 bytes,
  `minimal_metrics` 288,647 bytes, `one_simple` 2,292,201 bytes,
  `one_sql_query` 2,843,063 bytes, `one_fluent_query` 2,292,322 bytes,
  `one_complex` 2,314,311 bytes, `ten_simple` 2,315,722 bytes, and
  `ten_complex` 2,337,752 bytes.
- Canonical shrunk gzipped `.wasm` sizes are now: `minimal` 4,095 bytes,
  `minimal_metrics` 111,771 bytes, `one_simple` 751,714 bytes,
  `one_sql_query` 982,345 bytes, `one_fluent_query` 751,253 bytes,
  `one_complex` 759,104 bytes, `ten_simple` 754,129 bytes, and
  `ten_complex` 761,623 bytes.
- Compared with the 0.180.19 changelog snapshot, raw `.wasm` changes are:
  `minimal` unchanged, `minimal_metrics` unchanged, `one_simple` -2,725 bytes,
  `one_sql_query` -2,748 bytes, `one_fluent_query` -2,597 bytes,
  `one_complex` -2,910 bytes, `ten_simple` -2,725 bytes, and
  `ten_complex` -2,910 bytes.
- Compared with the 0.180.19 changelog snapshot, gzipped `.wasm` changes are:
  `minimal` -1 byte, `minimal_metrics` -1 byte, `one_simple` -1,308 bytes,
  `one_sql_query` -1,359 bytes, `one_fluent_query` -1,532 bytes,
  `one_complex` -1,412 bytes, `ten_simple` -1,207 bytes, and
  `ten_complex` -720 bytes.

### 🧪 Testing

- Validates formatting, focused persisted-row/schema-runtime/row-decode tests,
  SQL-enabled `icydb-core` lib tests, SQL-enabled `icydb-core` clippy,
  whitespace checks, and the refreshed wasm-footprint audit.
- Confirms the removed scalar payload detail strings and scalar decode labels
  no longer appear in the runtime scalar codec or central error constructors.

## 0.180.19

### 📝 Summary

- Compacts primary-key, store-key, index-key, and runtime/query invariant
  diagnostics onto the existing structured diagnostic-code contract.
- Removes decode-only prose, segment labels, range-anchor reason text, and old
  executor/invalid-plan message prefixes from shared wasm fixtures.
- Keeps behavior, storage bytes, public error payloads, and error taxonomy
  unchanged while moving tests away from brittle internal prose assertions.

### 🔧 Changed

- Replaces rich key-taxonomy and ordered-index-key error rendering with compact
  internal markers while preserving typed error variants for tests and callers.
- Removes display-only fields from primary-key/index equivalence and index
  entry diagnostics that are not part of a public wire contract.
- Compacts `InternalError` planner/query/cursor executor-boundary invariant
  constructors so class, origin, and diagnostic code remain the single runtime
  contract.
- Updates affected tests to assert structured diagnostic identity instead of
  historical message text.

### 📊 Audit

- Refreshes the wasm-footprint audit after key/index and runtime/query
  diagnostic compaction.
- The audit remains partially non-comparable at the report level because the
  `minimal`/`minimal_metrics` fixture split from 0.180.18 changed baseline
  shape, but the non-minimal rows are directly comparable to 0.180.18 sizes.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 6,516 bytes,
  `minimal_metrics` 288,647 bytes, `one_simple` 2,294,926 bytes,
  `one_sql_query` 2,845,811 bytes, `one_fluent_query` 2,294,919 bytes,
  `one_complex` 2,317,221 bytes, `ten_simple` 2,318,447 bytes, and
  `ten_complex` 2,340,662 bytes.
- Canonical shrunk gzipped `.wasm` sizes are now: `minimal` 4,096 bytes,
  `minimal_metrics` 111,772 bytes, `one_simple` 753,022 bytes,
  `one_sql_query` 983,704 bytes, `one_fluent_query` 752,785 bytes,
  `one_complex` 760,516 bytes, `ten_simple` 755,336 bytes, and
  `ten_complex` 762,343 bytes.
- Compared with 0.180.18, raw `.wasm` changes are: `minimal` unchanged,
  `minimal_metrics` unchanged, `one_simple` -13,015 bytes,
  `one_sql_query` -17,580 bytes, `one_fluent_query` -13,015 bytes,
  `one_complex` -13,015 bytes, `ten_simple` -13,015 bytes, and
  `ten_complex` -13,015 bytes.
- Compared with 0.180.18, gzipped `.wasm` changes are: `minimal` +1 byte,
  `minimal_metrics` -1 byte, `one_simple` -3,574 bytes,
  `one_sql_query` -4,880 bytes, `one_fluent_query` -3,911 bytes,
  `one_complex` -4,100 bytes, `ten_simple` -4,816 bytes, and
  `ten_complex` -5,106 bytes.

### 🧪 Testing

- Validates formatting, focused index/range/cursor/error/query-intent tests,
  SQL-enabled `icydb-core` clippy, whitespace checks, and the refreshed
  wasm-footprint audit.
- Confirms the removed executor-invariant and invalid-logical-plan message
  prefixes no longer appear in source.

## 0.180.18

### 📝 Summary

- Hard-cuts generated metrics to explicit opt-in instead of the previous
  metrics-on default.
- Bundles `__icydb_metrics_reset` under the compact metrics surface so
  `metrics.enabled = true` emits both compact metrics and reset, while
  `metrics.extended = true` remains the opt-in rich report add-on.
- Splits the wasm audit fixtures into `minimal` for the true no-endpoint floor
  and `minimal_metrics` for the explicit generated metrics baseline.
- Continues the audit/script cleanup by removing redundant workflow package
  preflight coverage, active inline Python from CI/workstation setup, and
  duplicated invariant-script plumbing.

### 🔧 Changed

- Changes build-codegen defaults so `BuildOptions::default()` emits no
  generated endpoint surfaces.
- Changes `icydb.toml` defaults so an absent config no longer enables metrics
  for downstream canisters.
- Removes the independent `metrics.reset` config/build switch; reset generation
  now follows `metrics.enabled`.
- Keeps the runtime `icydb metrics --reset` command and generated reset endpoint
  behavior unchanged whenever compact metrics are enabled.
- Compacts the default metrics Candid payload to positional records with
  derived window fields, preserving counters and CLI rendering while trimming
  generated-canister wasm size.
- Updates wasm reporting to show `metrics_extended` as a generated surface and
  to tolerate no-endpoint fixture twiggy sections that have no function/code
  section after shrinking.

### ⚠️ Breaking

- `[canisters.<name>.metrics] reset = ...` is no longer accepted. Pre-1.0
  projects should remove the key; `enabled = true` now owns both compact
  metrics and reset generation.

### 📊 Audit

- Refreshes the wasm-footprint audit after the metrics-default hard cut and
  fixture split.
- The audit is intentionally not fully comparable because `minimal` changed
  from a metrics-enabled baseline to the true no-endpoint floor and
  `minimal_metrics` is a new fixture row.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 6,516 bytes,
  `minimal_metrics` 288,647 bytes, `one_simple` 2,307,941 bytes,
  `one_sql_query` 2,863,391 bytes, `one_fluent_query` 2,307,934 bytes,
  `one_complex` 2,330,236 bytes, `ten_simple` 2,331,462 bytes, and
  `ten_complex` 2,353,677 bytes.
- Canonical shrunk gzipped `.wasm` sizes are now: `minimal` 4,095 bytes,
  `minimal_metrics` 111,773 bytes, `one_simple` 756,596 bytes,
  `one_sql_query` 988,584 bytes, `one_fluent_query` 756,696 bytes,
  `one_complex` 764,616 bytes, `ten_simple` 760,152 bytes, and
  `ten_complex` 767,449 bytes.
- Compared with the previous `minimal` metrics-enabled baseline, the new
  no-endpoint `minimal` floor is -280,831 raw bytes and -106,505 gzipped bytes.
- Compared with the previous 0.180.17 audit, raw `.wasm` changes for the
  comparable non-minimal rows are: `one_simple` -68,290 bytes,
  `one_sql_query` -53,403 bytes, `one_fluent_query` -50,295 bytes,
  `one_complex` -68,413 bytes, `ten_simple` -68,308 bytes, and
  `ten_complex` -68,431 bytes.
- Compared with the previous 0.180.17 audit, gzipped `.wasm` changes for the
  comparable non-minimal rows are: `one_simple` -22,452 bytes,
  `one_sql_query` -18,049 bytes, `one_fluent_query` -15,899 bytes,
  `one_complex` -21,638 bytes, `ten_simple` -21,691 bytes, and
  `ten_complex` -21,412 bytes.

### 🧪 Testing

- Validates formatting, changed-package compile checks, focused clippy across
  build/config/CLI/integration/audit fixtures, full `icydb-build`,
  `icydb-config-build`, and `icydb-cli` tests, and the refreshed
  wasm-footprint audit.
- Confirms no active source/config references remain for the removed
  `metrics.reset` generation switch and no stale `minimal_no_metrics` audit
  labels remain.

## 0.180.17

### 📝 Summary

- Removes obsolete PR slice-shape policy scripting, layer-authority and SQL
  duplicate-decision metric scripts, local LOC/tag/demo wrappers, and
  per-canister wasm audit markdown rendering.
- Keeps concrete boundary violation checks while dropping duplicated
  generated-metric and local-helper surfaces that were no longer release
  authority.
- Compacts another pass of production-only internal invariant labels across
  schema, persisted-row, index, relation, SQL parser/lowering, query, and
  executor paths.

### 🔧 Changed

- Replaces long internal `expect`, `panic`, `unreachable`, and assertion
  labels with short subsystem invariant markers on impossible production paths.
- Leaves public diagnostic codes, canister error payloads, query behavior,
  and storage bytes unchanged while moving obsolete internal panic/error-message
  assertions onto stable codes and typed error structure.
- Narrows wasm audit output to canonical JSON, size summaries, twiggy outputs,
  and the top-level audit report instead of duplicating per-canister markdown
  reports.

### 📊 Audit

- Refreshes the wasm-footprint audit after the script cleanup and invariant
  label compaction.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 287,347 bytes,
  `one_simple` 2,325,923 bytes, `one_sql_query` 2,863,363 bytes,
  `one_fluent_query` 2,307,914 bytes, `one_complex` 2,348,226 bytes,
  `ten_simple` 2,349,444 bytes, and `ten_complex` 2,371,667 bytes.
- Canonical shrunk gzipped `.wasm` sizes are now: `minimal` 110,606 bytes,
  `one_simple` 763,370 bytes, `one_sql_query` 988,609 bytes,
  `one_fluent_query` 756,689 bytes, `one_complex` 770,796 bytes,
  `ten_simple` 766,682 bytes, and `ten_complex` 774,102 bytes.
- Compared with the previous 0.180.16 audit, raw `.wasm` changes are:
  `minimal` unchanged, `one_simple` -1,428 bytes, `one_sql_query` -2,613
  bytes, `one_fluent_query` -1,556 bytes, `one_complex` -1,428 bytes,
  `ten_simple` -1,428 bytes, and `ten_complex` -1,428 bytes.
- Compared with the previous 0.180.16 audit, gzipped `.wasm` changes are:
  `minimal` +3 bytes, `one_simple` -439 bytes, `one_sql_query` -1,277 bytes,
  `one_fluent_query` -413 bytes, `one_complex` -128 bytes, `ten_simple`
  -334 bytes, and `ten_complex` -134 bytes.

### 🧪 Testing

- Validates formatting, SQL and no-SQL core clippy, focused persisted-row,
  schema mutation, SQL, and executor tests, and the refreshed wasm-footprint
  audit.
- Confirms the targeted old long invariant labels are absent from the shrunk
  fixture artifacts.

## 0.180.16

### 📝 Summary

- Compacts production-only internal invariant labels across query expression,
  predicate, planner, fingerprint, executor, index, schema identity/layout,
  key taxonomy, account-key, and Structural Binary paths.
- Preserves behavior and rich test assertions while reducing retained release
  strings that are not public diagnostics or wire contracts.
- Keeps the compact diagnostic-code direction focused on production wasm size
  without changing canister error payloads, query semantics, or storage bytes.
- Folds the release-readiness and audit-reporting cleanup into the same slice:
  CI uses the Makefile-owned invariant gate, wasm reports are assembled through
  a Rust helper, and structural audits no longer depend on a generated
  module-structure metrics gate.

### 🔧 Changed

- Replaces long `expect`, `panic`, and `unreachable` invariant labels with
  short subsystem markers on impossible internal paths.
- Keeps test-only detailed lowering/range messages where they prove source
  invariants, while production builds retain compact labels.
- Refreshes wasm-footprint artifacts after the invariant-label compaction.
- Moves wasm size JSON and markdown assembly into the Rust report helper while
  keeping shell scripts focused on orchestration and shared defaults.
- Routes CI invariant readiness through the Makefile-owned invariant target and
  drops the separate generated module-structure metrics gate so structural
  cleanup remains audit-evidence based.

### 📊 Audit

- Refreshes the wasm-footprint audit after compacting production invariant
  labels.
- The audit is comparable to the previous 0.180.15 audit.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 287,347 bytes,
  `one_simple` 2,327,351 bytes, `one_sql_query` 2,865,976 bytes,
  `one_fluent_query` 2,309,470 bytes, `one_complex` 2,349,654 bytes,
  `ten_simple` 2,350,872 bytes, and `ten_complex` 2,373,095 bytes.
- Canonical shrunk gzipped `.wasm` sizes are now: `minimal` 110,603 bytes,
  `one_simple` 763,809 bytes, `one_sql_query` 989,886 bytes,
  `one_fluent_query` 757,102 bytes, `one_complex` 770,924 bytes,
  `ten_simple` 767,016 bytes, and `ten_complex` 774,236 bytes.
- Compared with the previous 0.180.15 audit, raw `.wasm` changes are:
  `minimal` unchanged, `one_simple` -1,338 bytes, `one_sql_query` -3,408
  bytes, `one_fluent_query` -1,338 bytes, `one_complex` -1,338 bytes,
  `ten_simple` -1,338 bytes, and `ten_complex` -1,338 bytes.
- Compared with the previous 0.180.15 audit, gzipped `.wasm` changes are:
  `minimal` -1 byte, `one_simple` -358 bytes, `one_sql_query` -867 bytes,
  `one_fluent_query` -567 bytes, `one_complex` -365 bytes, `ten_simple`
  -423 bytes, and `ten_complex` -427 bytes.

### 🧪 Testing

- Validates focused structural-field, key-taxonomy, account, query/executor,
  index, predicate, and access-path tests.
- Validates SQL and no-SQL core clippy plus the refreshed wasm-footprint
  audit.
- Confirms the targeted old long invariant labels are absent from the shrunk
  fixture artifacts.

## 0.180.15

### 📝 Summary

- Compacts diagnostic `Debug` output to numeric/code-oriented rendering so
  production wasm no longer retains diagnostic enum and struct labels.
- Collapses schema transition, schema admission, reconciliation, and
  field-path startup mutation failure details into stable production buckets.
- Keeps rich schema/rebuild details available in test builds so existing
  source-boundary and behavior tests still prove the detailed invariants.

### 🔧 Changed

- Moves compact diagnostic-code rendering into manual `Debug`
  implementations instead of deriving label-heavy debug output.
- Splits rich schema transition/admission/reconcile detail formatting behind
  test-only paths while production canisters carry low-cardinality messages.
- Keeps schema-owned fail-closed runner and publication preflight behavior
  unchanged while trimming retained error strings from release artifacts.

### 📊 Audit

- Refreshes the wasm-footprint audit after compacting diagnostic debug and
  schema mutation/reconcile details.
- The audit is comparable to the 2026-06-08 baseline and reports
  `PASS=5, PARTIAL=0, FAIL=0`.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 287,347 bytes,
  `one_simple` 2,328,689 bytes, `one_sql_query` 2,869,384 bytes,
  `one_fluent_query` 2,310,808 bytes, `one_complex` 2,350,992 bytes,
  `ten_simple` 2,352,210 bytes, and `ten_complex` 2,374,433 bytes.
- Canonical shrunk gzipped `.wasm` sizes are now: `minimal` 110,604 bytes,
  `one_simple` 764,167 bytes, `one_sql_query` 990,753 bytes,
  `one_fluent_query` 757,669 bytes, `one_complex` 771,289 bytes,
  `ten_simple` 767,439 bytes, and `ten_complex` 774,663 bytes.
- Compared with the 2026-06-08 baseline, raw `.wasm` changes are:
  `minimal` unchanged, `one_simple` -47,542 bytes, `one_sql_query` -47,410
  bytes, `one_fluent_query` -47,421 bytes, `one_complex` -47,657 bytes,
  `ten_simple` -47,560 bytes, and `ten_complex` -47,675 bytes.
- Compared with the previous 0.180.14 audit, raw `.wasm` changes are:
  `minimal` unchanged, `one_simple` -20,168 bytes, `one_sql_query` -20,021
  bytes, `one_fluent_query` -20,046 bytes, `one_complex` -20,159 bytes,
  `ten_simple` -20,186 bytes, and `ten_complex` -20,177 bytes.

### 🧪 Testing

- Validates SQL and no-SQL core clippy, schema transition/reconcile tests,
  write-boundary source guards, the full SQL-enabled core library test suite,
  diagnostic-code crate tests/clippy, and the refreshed wasm-footprint audit.

## 0.180.14

### 📝 Summary

- Compacts structural field-decode corruption diagnostics to one internal
  `FieldDecodeError` marker instead of carrying per-branch message strings.
- Removes decode-only label plumbing from Structural Binary, accepted-field,
  primary-key-component, scalar fast-path, and value-storage decode helpers.
- Preserves row/store corruption classification while moving malformed
  structural payload tests away from brittle message matching and onto compact
  diagnostic identity.

### 🔧 Changed

- Collapses Structural Binary parse, skip, tuple, variant, list, map, scalar,
  and value-storage decode failures onto the same compact decode marker.
- Keeps public canister error payloads numeric-code based while avoiding
  duplicate structural corruption prose in production wasm.
- Shortens decode helper signatures after the diagnostic labels became
  redundant, leaving encode-time value-mismatch messages unchanged.

### 📊 Audit

- Refreshes the wasm-footprint audit after compacting structural decode
  diagnostics.
- The audit is comparable to the 2026-06-08 baseline and reports
  `PASS=5, PARTIAL=0, FAIL=0`.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 287,347 bytes,
  `one_simple` 2,348,857 bytes, `one_sql_query` 2,889,405 bytes,
  `one_fluent_query` 2,330,854 bytes, `one_complex` 2,371,151 bytes,
  `ten_simple` 2,372,396 bytes, and `ten_complex` 2,394,610 bytes.
- Compared with the previous 0.180.13 audit, raw `.wasm` changes are:
  `minimal` unchanged, `one_simple` -27,374 bytes, `one_sql_query` -27,389
  bytes, `one_fluent_query` -27,375 bytes, `one_complex` -27,498 bytes,
  `ten_simple` -27,374 bytes, and `ten_complex` -27,498 bytes.
- Gzipped canonical `.wasm` changes are: `minimal` unchanged, `one_simple`
  -8,671 bytes, `one_sql_query` -8,510 bytes, `one_fluent_query` -7,897
  bytes, `one_complex` -8,034 bytes, `ten_simple` -8,086 bytes, and
  `ten_complex` -7,733 bytes.

### 🧪 Testing

- Validates SQL-enabled structural-field tests, SQL-enabled core library
  tests, SQL-enabled all-target core clippy, and the refreshed wasm-footprint
  audit.

## 0.180.13

### 📝 Summary

- Narrows ULID and Subaccount scalar helper surfaces out of the production
  facade while keeping their storage, cursor, key, and persisted-value byte
  paths unchanged.
- Removes duplicate internal prose from structured query, SQL DDL admission,
  and schema-DDL store diagnostics that already cross public canister
  boundaries as compact diagnostic codes.
- Keeps human-readable diagnostic text owned by CLI/host rendering while
  production canisters retain compact code/detail identity.

### 🔧 Changed

- Splits public ULID parse failures from private ULID generation failures,
  keeps deterministic ULID construction as crate-local test support, and
  keeps private timestamp/randomness construction inside the ULID generator.
- Narrows Subaccount borrowed-byte access to crate-local key paths, keeps
  `from_array` as the fixed-width constructor, and preserves direct 32-byte
  encode/decode authority for account, key, cursor, and value paths.
- Collapses structured query diagnostic messages, schema DDL admission display
  text, and structured DDL store diagnostic messages to compact internal
  labels while preserving stable diagnostic codes/details.

### 🗑️ Removed

- Removes the duplicate `Subaccount::new` constructor in favor of
  `Subaccount::from_array`.
- Removes the orphaned `Subaccount::to_ulid` conversion.
- Removes public ULID generation-error variants from the parse-facing ULID
  error surface.

### 📊 Audit

- Refreshes the wasm-footprint audit after narrowing scalar helper surfaces
  and compacting structured diagnostic strings.
- The audit is comparable to the 2026-06-06 baseline and reports
  `PASS=5, PARTIAL=0, FAIL=0`.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 287,347 bytes,
  `one_simple` 2,376,231 bytes, `one_sql_query` 2,916,794 bytes,
  `one_fluent_query` 2,358,229 bytes, `one_complex` 2,398,649 bytes,
  `ten_simple` 2,399,770 bytes, and `ten_complex` 2,422,108 bytes.
- Compared with the 2026-06-06 baseline, raw `.wasm` deltas are all negative:
  `minimal` -52,121 bytes, `one_simple` -66,203 bytes, `one_sql_query`
  -38,500 bytes, `one_fluent_query` -30,908 bytes, `one_complex` -68,516
  bytes, `ten_simple` -68,028 bytes, and `ten_complex` -70,341 bytes.
- Compared with the previous 0.180.12 audit, raw `.wasm` changes are:
  `minimal` unchanged, `one_simple` +20 bytes, `one_sql_query` -147 bytes,
  `one_fluent_query` -108 bytes, `one_complex` +20 bytes, `ten_simple` +20
  bytes, and `ten_complex` +20 bytes.

### 🧪 Testing

- Validates the focused ULID/Subaccount surface-hardening paths, compact query
  diagnostic behavior, aggregate unknown-field diagnostic identity, SQL-enabled
  core error behavior, and SQL-enabled core clippy.

## 0.180.12

### 📝 Summary

- Moves accepted-catalog `ORDER BY` non-orderable-field failures onto compact
  SQL feature leaf codes instead of retaining dynamic field-name message text
  at the public canister boundary.
- Moves the remaining structured SQL lowering rejection variants onto compact
  SQL lowering leaf codes with CLI-owned explanatory text.
- Routes duplicate SQL DDL execution-unsupported dispatch paths through the
  existing compact SQL lowering code instead of rebuilding the same runtime
  string.

### 🔧 Changed

- Adds compact SQL lowering detail codes for entity mismatch, SELECT
  projection shape, SELECT DISTINCT, DISTINCT `ORDER BY` projection
  derivability, global aggregate projection/GROUP BY, grouped projection
  shape, HAVING shape, aggregate-input expression execution, WHERE expression
  shape, unsupported parameter placement, and unsupported SQL DDL execution.
- Keeps unknown-field lowering on the existing planner/field diagnostic path
  because it carries query planning semantics rather than frontend feature
  admission semantics.
- Extends CLI diagnostic rendering so the public numeric SQL lowering leaf
  codes reconstruct stable human-readable messages outside production
  canister builds.

### 📊 Audit

- Refreshes the wasm-footprint audit after compacting accepted-catalog
  `ORDER BY` and SQL lowering diagnostics.
- The audit is comparable to the 2026-06-06 baseline and reports
  `PASS=5, PARTIAL=0, FAIL=0`.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 287,347 bytes,
  `one_simple` 2,376,211 bytes, `one_sql_query` 2,916,941 bytes,
  `one_fluent_query` 2,358,337 bytes, `one_complex` 2,398,629 bytes,
  `ten_simple` 2,399,750 bytes, and `ten_complex` 2,422,088 bytes.
- Compared with the 2026-06-06 baseline, raw `.wasm` deltas are all negative:
  `minimal` -52,121 bytes, `one_simple` -66,223 bytes, `one_sql_query`
  -38,353 bytes, `one_fluent_query` -30,800 bytes, `one_complex` -68,536
  bytes, `ten_simple` -68,048 bytes, and `ten_complex` -70,361 bytes.

### 🧪 Testing

- Validates the diagnostic-code crate, public `icydb` error reconstruction,
  SQL-enabled core lowering/session behavior, CLI diagnostic rendering,
  integration SQL canister compilation and clippy, and the refreshed
  wasm-footprint audit.

## 0.180.11

### 📝 Summary

- Moves SQL lowering, query projection, and query result-shape failures onto
  compact diagnostic leaf codes instead of retaining detailed public error
  strings.
- Keeps explanatory diagnostic prose in the CLI renderer, reconstructed from
  compact public error codes.
- Preserves query-origin unsupported execution semantics while replacing
  brittle message assertions with structured diagnostic checks.

### 🔧 Changed

- Adds compact query projection and result-shape detail codes for numeric,
  text/blob, CASE, unary/binary operand, and row-shape rejection paths.
- Routes unknown aggregate target-field failures, unsupported projection
  reasons, and result-shape mismatches through compact diagnostics.
- Keeps SQL lowering fallback detail where no typed compact reason exists yet,
  avoiding lossy generic diagnostics while the remaining lowering reasons are
  split into follow-up slices.

### 📊 Audit

- Refreshes the wasm-footprint audit after compacting more query and SQL
  diagnostics.
- The audit is comparable to the 2026-06-06 baseline and reports
  `PASS=5, PARTIAL=0, FAIL=0`.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 287,347 bytes,
  `one_simple` 2,375,538 bytes, `one_sql_query` 2,915,803 bytes,
  `one_fluent_query` 2,357,536 bytes, `one_complex` 2,397,956 bytes,
  `ten_simple` 2,399,077 bytes, and `ten_complex` 2,421,415 bytes.
- Compared with the 2026-06-06 baseline, raw `.wasm` deltas are all negative:
  `minimal` -52,121 bytes, `one_simple` -66,896 bytes, `one_sql_query`
  -39,491 bytes, `one_fluent_query` -31,601 bytes, `one_complex` -69,209
  bytes, `ten_simple` -68,721 bytes, and `ten_complex` -71,034 bytes.

### 🧪 Testing

- Validates the diagnostic-code crate, focused projection/result-shape tests,
  focused SQL lowering tests, CLI diagnostic rendering tests, SQL-enabled core
  tests and clippy, integration SQL canister diagnostics, and the refreshed
  wasm-footprint audit.

## 0.180.10

### 📝 Summary

- Gates audit, demo, and test canister Candid metadata export behind an
  explicit `candid-export` Cargo feature.
- Keeps local/debug staging convenient by enabling Candid export automatically
  for local build paths that need `.did` files.
- Keeps production-oriented `wasm-release` audit builds free of
  `export_candid!()` metadata by default.

### 🔧 Changed

- Adds `candid-export` features to the audit, demo, and SQL test canister
  crates and guards every canister `export_candid!()` call behind that feature.
- Extends the fixture canister build options with `--candid-export
  auto|on|off`, where `auto` enables Candid export for debug/release profiles
  and disables it for `wasm-release`.
- Updates the local app build script to request `candid-export` explicitly so
  local Candid extraction still works without asking users to remember a
  separate config switch.

### 📊 Audit

- Refreshes the wasm-footprint audit after removing Candid metadata from
  `wasm-release` audit builds.
- The audit is comparable to the 2026-06-06 baseline and reports
  `PASS=5, PARTIAL=0, FAIL=0`.
- Every audited `wasm-release` fixture reports Candid export as unavailable.
- Canonical shrunk raw `.wasm` sizes are now: `minimal` 287,329 bytes,
  `one_simple` 2,376,200 bytes, `one_sql_query` 2,918,968 bytes,
  `one_fluent_query` 2,358,339 bytes, `one_complex` 2,398,618 bytes,
  `ten_simple` 2,399,739 bytes, and `ten_complex` 2,422,077 bytes.
- Compared with the prior 0.180.9 audit, the raw `.wasm` reductions are:
  `minimal` -14,806 bytes, `one_simple` -19,215 bytes, `one_sql_query`
  -14,796 bytes, `one_fluent_query` -14,697 bytes, `one_complex` -19,215
  bytes, `ten_simple` -19,215 bytes, and `ten_complex` -19,215 bytes.

### 🧪 Testing

- Validates formatting, the integration harness check, minimal wasm-release
  builds with Candid export disabled and forced on, default wasm-release
  staging with no `.did`, forced Candid export staging with `.did`, focused
  clippy for the changed harness and canister paths, local app-script Candid
  extraction, and the refreshed wasm-footprint audit.

## 0.180.9

### 📝 Summary

- Preflights SQL write `RETURNING` field-list validation before mutation
  execution so unknown and duplicate projected fields reject without mutating
  rows.
- Preflights update-selector policy gates before mutation execution, keeping
  missing `WHERE` predicates and unsupported write `ORDER BY` shapes on compact
  SQL write boundary diagnostics.
- Keeps CLI-owned text rendering for these failures while the public canister
  error payload carries stable numeric codes.

### 🔧 Changed

- Adds compact SQL write boundary codes for unknown `RETURNING` fields,
  duplicate `RETURNING` fields, missing update `WHERE` predicates, and
  unsupported write `ORDER BY` shapes.
- Moves SQL write `RETURNING` field-list validation ahead of insert, update,
  and delete mutation execution.
- Routes update selector lowering failures through compact SQL write boundary
  codes instead of retaining detailed runtime message strings.

### 📊 Audit

- Refreshes the wasm-footprint audit after compacting the remaining SQL write
  projection and update policy diagnostics.
- The audit is comparable to the 2026-06-06 baseline and reports
  `PASS=5, PARTIAL=0, FAIL=0`.
- Canonical shrunk raw `.wasm` deltas are all negative: `minimal` -37,333
  bytes, `one_simple` -47,019 bytes, `one_sql_query` -21,530 bytes,
  `one_fluent_query` -16,101 bytes, `one_complex` -49,332 bytes,
  `ten_simple` -48,844 bytes, and `ten_complex` -51,157 bytes.

### 🧪 Testing

- Validates the diagnostic-code crate, public `icydb` error tests, focused SQL
  write tests, focused CLI diagnostic rendering tests, focused clippy for the
  touched crates, and the refreshed wasm-footprint audit.

## 0.180.7

### 📝 Summary

- Moves SQL write boundary failures onto compact diagnostic leaf codes instead
  of retaining detailed runtime message strings.
- Keeps human-readable SQL write failure text in the CLI diagnostic renderer,
  reconstructed from compact public error codes.
- Preserves query-origin unsupported execution semantics while replacing
  brittle SQL write message assertions with structured diagnostic detail
  checks.

### 🔧 Changed

- Adds compact leaf codes for SQL write primary-key literal shape,
  incompatible primary-key literals, missing primary keys, missing required
  fields, explicit managed/generated field writes, unsupported `INSERT
  SELECT` source shapes, update primary-key mutation, and invalid field
  literals.
- Routes SQL write execution, `INSERT SELECT` preparation, and public
  diagnostic reconstruction through the new SQL write boundary detail.
- Removes the now-dead first-primary-key-name runtime-layout helper after SQL
  write diagnostics no longer need it for message formatting.

### 📊 Audit

- Refreshes the wasm-footprint audit after compacting SQL write diagnostics.
- The audit is comparable to the 2026-06-06 baseline and reports
  `PASS=5, PARTIAL=0, FAIL=0`.
- Canonical shrunk raw `.wasm` deltas are all negative: `minimal` -37,333
  bytes, `one_simple` -47,179 bytes, `one_sql_query` -21,994 bytes,
  `one_fluent_query` -16,133 bytes, `one_complex` -49,492 bytes,
  `ten_simple` -49,004 bytes, and `ten_complex` -51,317 bytes.

### 🧪 Testing

- Validates the diagnostic-code crate, public `icydb` error tests,
  SQL-enabled core error tests, focused SQL write tests, focused CLI
  diagnostic rendering tests, focused clippy for the touched crates, and the
  refreshed wasm-footprint audit.

## 0.180.6

### 📝 Summary

- Moves additional SQL DDL admission failures onto compact diagnostic leaf
  codes while keeping CLI-owned text rendering as the human-facing channel.
- Collapses SQL endpoint surface mismatch construction to stable mismatch
  codes instead of retaining per-call message strings.
- Trims internal DDL bind, lowering, and preparation display strings so typed
  diagnostic details carry the meaning and fallback text stays compact.

### ⚠️ Breaking

- `ErrorKind`, `RuntimeErrorKind`, `QueryErrorKind`, and `ErrorOrigin` no
  longer derive Candid/serde wire traits. Public canister error output remains
  the compact numeric `Error` record introduced earlier in 0.180.

### 🔧 Changed

- Adds leaf codes for invalid DDL defaults, generated-owned index and field
  mutations, required-field `DROP DEFAULT`, and `SET NOT NULL` validation
  failure.
- Preserves query-visible DDL failure semantics by mapping SQL preparation and
  execution failures to structured `SchemaDdlAdmissionCode` values.
- Updates SQL canister and session tests to assert compact error codes or
  diagnostic details instead of matching DDL admission message text.

### 🗑️ Removed

- Removes lower-level codec/parser error text from DDL admission failures that
  now have stable compact leaf codes.
- Removes unused public-helper Candid-shape tests for the old rich error
  helper enums.

### 📊 Audit

- Refreshes the wasm-footprint audit after compacting DDL and SQL surface
  diagnostics.
- The audit is comparable to the 2026-06-06 baseline and reports
  `PASS=5, PARTIAL=0, FAIL=0`.
- Canonical shrunk raw `.wasm` deltas are all negative: `minimal` -37,333
  bytes, `one_simple` -47,905 bytes, `one_sql_query` -20,884 bytes,
  `one_fluent_query` -16,987 bytes, `one_complex` -50,218 bytes,
  `ten_simple` -49,730 bytes, and `ten_complex` -52,043 bytes.

### 🧪 Testing

- Validates the diagnostic-code crate, public `icydb` error tests,
  SQL-enabled core error tests, focused SQL DDL rejection tests, focused
  clippy for the touched crates, SQL canister test-target clippy, and the
  refreshed wasm-footprint audit.
- The targeted runtime SQL canister integration test compiled but could not
  execute locally because the PocketIC server binary was not present.

## 0.180.5

### 📝 Summary

- Changes the default generated `__icydb_metrics` endpoint from rich
  `EventReport` output to compact numeric-code `CompactMetricsReport` output.
- Keeps the rich `EventReport` available behind the opt-in
  `metrics.extended = true` config switch and generated
  `__icydb_metrics_extended` endpoint.
- Adds `icydb metrics --extended` so host tooling can call the opt-in rich
  endpoint while default `icydb metrics` remains on the compact report.

### ⚠️ Breaking

- The default generated `__icydb_metrics` Candid response now returns
  `CompactMetricsReport` instead of `EventReport`.
- Callers that need the old rich metrics DTO must enable extended metrics in
  `icydb.toml`, rebuild and deploy the canister, then call
  `__icydb_metrics_extended` or use `icydb metrics --extended`.

### 🔧 Changed

- Adds sparse compact metric code/value DTOs for the base metrics report so
  canisters do not retain the full rich metrics Candid graph by default.
- Keeps default generated metrics enabled, but makes the rich metrics endpoint
  explicitly opt-in through `metrics.extended`.
- Updates CLI metrics decoding and rendering so the compact report is the
  normal path and the rich report is used only for `--extended`.

### 📊 Audit

- Refreshes the wasm-footprint audit after compacting base metrics.
- The minimal shrunk `.wasm` is 302,135 bytes, down 30,932 bytes from the
  prior local audit artifact and 37,333 bytes against the audit baseline
  comparison.
- Metrics-enabled entity fixtures drop about 31 KiB raw `.wasm`; metrics-
  disabled query fixtures remain effectively unchanged.

### 🧪 Testing

- Adds compact metrics Candid-shape and sparse-counter coverage.
- Adds config parsing and CLI surface coverage for `metrics.extended` and
  `icydb metrics --extended`, including the reset conflict.
- Validates the slice with focused config/build/core metrics tests, full
  `icydb-cli` tests, focused clippy, and the refreshed wasm-footprint audit.

## 0.180.4

### 📝 Summary

- Hard-cuts public canister error output to numeric-only `Error` records:
  `code: nat16`, `class: nat8`, and `origin: nat8`.
- Removes rich diagnostic enum/detail trees from public Candid output while
  keeping Rust-side diagnostic reconstruction available for CLI and host tools.
- Renumbers public diagnostic leaf codes into one compact sequential
  pre-1.0 range.

### ⚠️ Breaking

- Public Candid error records no longer include `detail`, and the remaining
  `code`, `class`, and `origin` fields are numeric wire values.
- Callers that previously matched Candid diagnostic variants should compare
  numeric `code` values or use host-side rendering through IcyDB tooling.

### 🔧 Changed

- Collapses broad diagnostic codes plus structured diagnostic details into one
  public numeric leaf code before errors cross the canister boundary.
- Keeps rich diagnostic details internal to core/Rust APIs so the CLI can still
  render useful human-readable messages without exposing the full diagnostic
  graph in every canister interface.
- Removes the diagnostic-code crate's Candid/serde wire feature and optional
  wire dependencies from the production facade path.
- Makes public error-code raw values sequential from `1..=124`, with test
  coverage guarding that compact layout before 1.0.

### 📊 Audit

- Refreshes the wasm-footprint audit after the wire-shape hard cut.
- The audited canisters are now smaller than the 0.179.5 baseline by about
  5.9-7.0 KiB raw `.wasm` each, and about 19-22 KiB raw `.wasm` smaller than
  the pre-hard-cut 0.180.3 audit snapshot.
- Regenerated audit fixture Candid files no longer contain the rich diagnostic
  enum/type names.

### 🧪 Testing

- Adds facade coverage for the numeric-only `Error` Candid shape and detail
  reconstruction through `Error::diagnostic()`.
- Updates SQL canister integration assertions to compare numeric leaf error
  codes instead of Candid diagnostic detail variants.
- Validates the slice with focused crate tests, SQL canister test compilation,
  workspace clippy, repository invariants, and the wasm-footprint audit.

## 0.180.3

### 📝 Summary

- Hardens CI and developer bootstrap downloads for actionlint and rustup.
- Adds a CI invariant that protects mutation/publication atomicity from future
  async or re-entrant interleaving points.

### 🧹 Cleanup

- Makes the actionlint installer reuse an already-installed pinned binary
  before attempting a network download.
- Downloads actionlint release archives to a temporary file with retries before
  validating and extracting them, avoiding direct `curl | tar` behavior on
  partial HTTP failures.
- Downloads the rustup bootstrap script to a temporary file with retries before
  executing it, avoiding direct `curl | sh` behavior on partial HTTP failures.
- Adds a mutation atomicity invariant to the existing `check-invariants` path,
  rejecting async functions, await points, spawned work, timers, and canister
  calls in guarded mutation/publication/generated mutation paths.

### 🧪 Testing

- Validates the new atomicity guard through the normal invariant suite.
- Adds a local cached-binary smoke check for the actionlint installer so the
  pinned-version reuse path is covered without requiring network access.

## 0.180.2

### 📝 Summary

- Adds compact runtime-boundary diagnostic details for generated SQL/schema
  endpoint rejections and public response-shape errors.
- Keeps canister-facing error payloads code/detail-first while leaving
  human-readable boundary text in the CLI renderer.
- Trims unused internal Candid derives without changing public SQL catalog or
  input/output value DTO wire shapes.

### 🔧 Changed

- Adds a compact runtime-boundary detail family for generated SQL controller
  gates, generated schema controller gates, SQL entity dispatch failures,
  targetless SQL DDL, public query response shape mismatches, mutation result
  shape mismatches, and explicit row-projection field mismatches.
- Routes generated SQL/schema endpoint helpers and public response-shape helper
  errors through the compact runtime-boundary detail instead of returning only
  broad runtime unsupported diagnostics.
- Extends CLI diagnostic rendering so host tools display useful text for the
  new runtime-boundary details without linking that prose into canister-facing
  crates.

### 🧹 Cleanup

- Removes an unused Candid derive from the catalog count helper while keeping
  SQL catalog result DTOs wire-compatible.
- Removes unused Candid derives from the internal runtime value union and enum
  payload helper; public input/output value DTOs keep their wire derives.

### 🧪 Testing

- Adds facade and CLI renderer coverage for compact runtime-boundary detail
  preservation.
- Tightens SQL canister integration assertions so interface-origin generated
  SQL DDL rejects preserve compact runtime-boundary detail across Candid.

## 0.180.1

### 📝 Summary

- Hard-cuts the public `icydb::Error` payload from text-first
  `kind`/`origin`/`message` fields to compact `code`/`class`/`origin`/`detail`
  fields.
- Routes public conversions from core query and runtime errors through
  owner-provided compact diagnostic mappings instead of reconstructing broad
  public kinds from rendered messages.
- Adds CLI-owned text rendering for compact diagnostic payloads so host tools
  display useful messages without linking text rendering into canister-facing
  crates.
- Updates generated endpoint and response helpers to construct compact public
  errors without formatting public error prose.

### ⚠️ Breaking

- Public Candid error records no longer include `message` or `kind`.
- Callers should inspect `code`, `class`, `origin`, and `detail`, then render
  text in host/CLI tooling when human output is needed.

### 🔧 Changed

- Adds core-owned compact mappings for structured error details, schema DDL
  admission reasons, SQL unsupported-feature labels, store details, and query
  error categories.
- Keeps `Error::diagnostic()` as the round-trip public diagnostic accessor for
  callers that want the compact payload as one value.
- Routes decoded shell and observability endpoint errors through the CLI
  renderer, with recovery hints still applied after rendering.

### 🧪 Testing

- Updates facade Candid-shape tests to require the compact wire fields and to
  reject the removed text-first fields.
- Adds focused core and query tests for compact detail preservation across DDL
  admission, SQL unsupported-feature, response-cardinality, and runtime error
  conversions.
- Adds focused CLI renderer tests for schema DDL admission, unsupported SQL
  feature, and detail-free runtime diagnostics.

## 0.180.0

### 📝 Summary

- Starts the compact diagnostics line with a dedicated diagnostic-code crate
  that can be shared by canisters, the public facade, and future CLI rendering.
- Adds a public `Error::diagnostic()` bridge so callers can begin asserting
  compact diagnostic identity before the later production wire hard cut.
- Keeps the public error wire payload unchanged for this first slice.

### ➕ Added

- Adds compact diagnostic codes for query, schema DDL admission, store, and
  runtime failure families.
- Adds compact detail enums for query kind, runtime kind, unsupported SQL
  feature identity, and schema DDL admission reason identity.
- Adds optional wire derives behind the diagnostic-code crate's `wire` feature
  so production and local/debug surfaces can choose the appropriate payload.

### 🧭 Migration Notes

- This slice is additive. Existing public `Error` Candid records still expose
  `kind`, `origin`, and `message`.
- Callers can start using `Error::diagnostic()` and
  `Error::diagnostic_code()` as the migration path toward code-first error
  handling.
