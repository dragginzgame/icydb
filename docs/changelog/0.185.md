# IcyDB 0.185 Changelog

## 0.185.7

### 📝 Summary

- Starts the next branch-tree cleanup by removing another duplicate physical
  prefix-stream path.
- Keeps the cleanup below a full branch-tree replacement: route admission,
  route identity, cursor format, and query semantics are unchanged.
- Makes single-prefix streaming consume the same physical prefix-family helper
  used by branch-set, streaming multi-lookup, and sparse child-prefix routes.

### 🧹 Cleanup

- Single-prefix index streaming now builds through the merged-prefix stream
  spec instead of hand-rolling empty-prefix checks, primary-key suffix resume
  anchors, chunk sizing, and index-range stream construction.
- Materialized multi-lookup and merged-prefix streaming now share one local
  active-prefix pruning helper, so empty-prefix metadata is interpreted once in
  the physical stream owner.

### 🧪 Testing

- Revalidated prefix-focused tests covering empty prefixes, bounded windows,
  cursor-past-end behavior, grouped route planning, and strict prefix EXPLAIN.
- Revalidated full branch-set coverage to guard branch-set, sparse
  child-prefix, and multi-prefix streaming behavior.
- Revalidated SQL+diagnostics Clippy, formatting, and whitespace checks.

### 🧭 Migration Notes

- No schema migration is required.
- No SQL syntax, fluent API, result payload, cursor format, persistence format,
  public endpoint behavior, cache format, route-admission semantics, or route
  diagnostics are intended to change.
- Full branch-tree replacement and cursor-format design remain deferred
  follow-up work.

## 0.185.6

### 📝 Summary

- Extends sparse `IN` child-prefix expansion to primary-key descending order
  when the expanded prefix leaves exactly the primary-key suffix.
- Keeps the route as `IndexMultiLookup` with a child-prefix expansion hint,
  rather than adding a new logical access path.
- Proves reverse expansion across first-page execution, key-only covering
  projection, EXPLAIN diagnostics, and cursor continuation.

### 🔧 Changed

- Route order proof, DESC physical reverse support, and EXPLAIN access-order
  diagnostics now consume the same child-prefix primary-key-order proof.
- Covering projection prep uses the requested primary-key scan direction when
  the route proves the prefix set is primary-key-order safe, so DESC key-only
  sparse `IN` pages avoid materialized sorting.
- Non-unique descending secondary-order routes still fail closed unless the
  child-prefix proof shows the remaining suffix is exactly the primary key.

### 🧪 Testing

- Added route proof that DESC sparse child-prefix expansion is admitted only
  when the expanded suffix is primary-key ordered.
- Added SQL sparse `IN ... ORDER BY id DESC` coverage for route diagnostics,
  key-only projection behavior, bounded child-prefix scans, and zero row-store
  reads for covered projections.
- Added DESC sparse child-prefix cursor continuation coverage proving page-two
  resume stays bounded and does not replay the full prefix set.
- Revalidated focused route, covering, full branch-set, SQL+diagnostics
  Clippy, formatting, and whitespace checks.

### 🧭 Migration Notes

- No schema migration is required.
- No SQL syntax, fluent API, result payload, cursor format, persistence format,
  public endpoint behavior, or cache format changes are intended.
- The current global primary-key cursor boundary remains in use. Full
  per-branch cursor-format redesign remains deferred.

## 0.185.5

### 📝 Summary

- Starts the adaptive sparse `IN` routing work from the documented 0.185
  follow-up queue.
- Lets bounded page queries raise the child-prefix expansion cap from the
  conservative default to the route-owned page lookahead window, while keeping
  a small hard ceiling.
- Allows exact-cap expansion only when any remaining parent prefixes are
  metadata-proven empty, so capped expansion cannot silently drop live rows.

### 🔧 Changed

- Route planning and SQL covering projection prep now consume the same
  child-prefix expansion cap calculation.
- Child-prefix expansion treats the cap as a limit on non-empty expanded child
  prefixes, not on parent literals inspected.
- Exact-cap sparse expansion may continue after the cap is filled only if
  trailing parent prefixes are proven empty by prefix-cardinality metadata.

### 🧪 Testing

- Added bounded-page proof that sparse child-prefix expansion can raise its cap
  to the page lookahead window.
- Added exact-cap sparse expansion coverage for trailing empty parent
  prefixes.
- Revalidated focused branch-set coverage, SQL+diagnostics Clippy, formatting,
  and whitespace checks.

### 🧭 Migration Notes

- No schema migration is required.
- No SQL syntax, fluent API, result payload, cursor format, persistence format,
  public endpoint behavior, or cache format changes are intended.
- This is not a broad cost-based optimizer. Branch-set admission, prefix
  cardinality metadata semantics, cursor format, and dense over-cap streaming
  strategy remain deferred.

## 0.185.4

### 📝 Summary

- Tightens the branch-aware merged-prefix runtime contract after the first
  shared-stream slice.
- Keeps branch-set ordering independent from sparse child-prefix expansion
  hints while still sharing the physical stream construction path.
- Reduces runtime argument drift by carrying merged-prefix behavior through one
  explicit local spec.

### 🔧 Changed

- Branch-set, streaming multi-lookup, and sparse child-prefix expansion now
  share one explicit merged-prefix stream spec for index shape, lowered
  prefixes, continuation, fetch hint, and primary-key suffix resume policy.
- Branch-set execution still consumes the spec-derived branch count before
  entering the stream merger.
- Sparse child-prefix expansion still enters the merged-prefix stream only
  after metadata-backed expansion produces exact child prefixes.

### 🧪 Testing

- Added route proof that branch-set primary-key ordering does not depend on
  sparse child-prefix expansion.
- Revalidated focused route-planning coverage, SQL+diagnostics Clippy,
  formatting, and whitespace checks.

### 🧭 Migration Notes

- No schema migration is required.
- No SQL syntax, fluent API, result payload, cursor format, persistence format,
  public endpoint behavior, cache format, or route-admission semantics are
  intended to change.
- Branch-tree replacement, cursor-format redesign, and adaptive cost routing
  remain deferred follow-up work.

## 0.185.3

### 📝 Summary

- Extends the sparse `IN` branch-aware proof from page/projection behavior into
  scalar terminal behavior.
- Proves COUNT/EXISTS terminal paths can use prefix-cardinality metadata for
  both non-empty and empty sparse prefix sets without row or index-entry scans.
- Keeps the slice below route-admission, branch-cap, cursor-format,
  prefix-cardinality metadata, or cost-model changes.

### 🔧 Changed

- Sparse collection COUNT SQL fixtures now use the same shared collection
  literal helpers as page and fluent sparse `IN` tests.
- Fluent sparse terminal tests now share the same sparse filter helpers as
  fluent sparse page tests, keeping SQL/fluent fixture drift lower.

### 🧪 Testing

- Added SQL sparse `IN` COUNT coverage for an all-missing prefix set, proving
  zero can be answered from prefix-cardinality metadata without index traversal.
- Added fluent sparse `IN` COUNT coverage for non-empty and empty prefix sets.
- Added fluent sparse `IN` EXISTS coverage for true and false prefix-cardinality
  outcomes.
- Revalidated focused sparse terminal tests, full branch-set coverage,
  SQL+diagnostics Clippy, formatting, and whitespace checks.

### 🧭 Migration Notes

- No schema migration is required.
- No SQL syntax, fluent API, result payload, cursor format, persistence format,
  public endpoint behavior, cache format, or route-admission semantics are
  intended to change.
- Cost-based routing, denser over-cap streaming, and DESC child-prefix expansion
  remain deferred follow-up work.

## 0.185.2

### 📝 Summary

- Extends the sparse `IN` over-cap proof from key-only fallback into the
  projection and fluent surfaces that consume the same branch-aware route
  boundary.
- Keeps the slice proof-oriented: it does not add a cost model, change branch
  caps, or introduce a denser over-cap streaming strategy.

### 🔧 Changed

- Sparse collection test fixtures now share one source for parent collection
  literals across SQL ASC, SQL DESC, and fluent sparse `IN` queries.
- Combined child-prefix over-cap fixtures now build expected rows from the same
  deterministic parent/stage helper data used for seeding.

### 🧪 Testing

- Added SQL covering projection proof that over-cap combined parent-prefix
  fallback can decode index components after complete primary-key sorting
  without row-store reads.
- Added SQL hybrid projection proof that over-cap fallback hydrates only the
  final page rows while still scanning the complete parent-prefix route before
  sorting/windowing.
- Added fluent full-entity proof for the same combined over-cap fallback
  boundary, guarding primary-key order and bounded row hydration.
- Added SQL DESC sparse `IN` proof that the route stays materialized/fallback
  and does not report the ASC-only child-prefix expansion hint.
- Revalidated focused branch-set coverage, SQL+diagnostics Clippy, formatting,
  and whitespace checks.

### 🧭 Migration Notes

- No schema migration is required.
- No SQL syntax, fluent API, result payload, cursor format, persistence format,
  public endpoint behavior, cache format, or route-admission semantics are
  intended to change.
- DESC sparse child-prefix expansion, cost-based routing, and denser over-cap
  streaming remain deferred follow-up work.

## 0.185.1

### 📝 Summary

- Hardens the 0.185 branch-aware revisit after the initial shared-stream slice.
- Separates the sparse `IN` continuation proof from the over-cap covering
  fallback fix: the cursor path was a resume-safety proof, while the fallback
  bug was an ordering/projection issue in row-store-free covering execution.
- Keeps the slice below a cursor-format hard-cut and below a cost-based
  optimizer change.

### 🩹 Fixed

- Sparse `IN` key-only covering fallback now materializes unsafe parent-prefix
  sets before deduplicating, sorting, and applying the page window.
- Over-cap child-prefix expansion no longer risks treating a capped or
  incomplete expanded prefix set as a complete ordered branch stream.
- Covering prefix-set resolution now distinguishes merge-safe prefix streams
  from fallback prefix sets whose leaf order is not the final primary-key
  order.

### 🔧 Changed

- Covering component resolution now carries an explicit prefix-set
  merge-safety contract. Proven child-prefix streams can still use lazy merge;
  unsafe fallbacks take the complete materialized path.
- Sparse `IN` adaptive-routing notes now document that over-cap fallback may
  remain row-store-free, but must not use lazy primary-key merge unless each
  prefix stream has a proven ordered suffix.

### 🧪 Testing

- Added sparse `IN` continuation proof for child-prefix-expanded routes,
  guarding page-two resume after the global primary-key cursor boundary.
- Added over-cap child-prefix fallback coverage for both a single dense parent
  prefix and a combined parent-prefix set that exceeds the expansion cap only
  in aggregate.
- Revalidated focused branch-set coverage, covering-projection coverage,
  SQL+diagnostics Clippy, formatting, and whitespace checks.

### 🧭 Migration Notes

- No schema migration is required.
- No SQL syntax, fluent API, result payload, cursor format, persistence format,
  public endpoint behavior, or cache format changes are intended.
- Per-branch cursor hard-cut, cost-based routing, and a better dense over-cap
  streaming strategy remain deferred follow-up work.

## 0.185.0

### 📝 Summary

- Starts the branch-aware query revisit from the 0.183 production baseline.
- Establishes the 0.185 design/status baseline for route representation,
  SQL/fluent convergence, continuation safety, adaptive routing boundaries,
  and deferred cost-based work.
- Keeps this first slice focused on proof and consolidation rather than a
  broad optimizer rewrite.

### 🔧 Changed

- Branch-set EXPLAIN access DTOs now carry the branch field and ordered suffix
  identity explicitly, making the first-slice order-preservation proof visible
  instead of relying only on tuple shape.
- Branch-set, multi-lookup, and sparse child-prefix expansion paths now share
  merged-prefix stream construction for empty-prefix pruning, fair branch
  chunk sizing, primary-key suffix resume anchors, per-prefix stream setup, and
  duplicate-suppressing merge handoff.
- SQL and fluent target branch-set queries are guarded to share the same
  branch-route identity and return the same primary-key ordered page.
- Verbose EXPLAIN now surfaces sparse `IN` child-prefix expansion state,
  target fetch, and cap fetch diagnostics.
- DESC sparse child-prefix expansion remains explicitly rejected until reverse
  continuation and ordering semantics are designed.

### 📚 Documentation

- Added the 0.185 branch-aware revisit design baseline.
- Added a 0.185 status file recording the current runtime contract,
  representation audit, branch-tree inventory, and next planned cleanup.
- Added continuation notes documenting the current global primary-key boundary
  plus per-prefix raw resume-anchor model.
- Added adaptive-routing notes documenting current route families, the admitted
  sparse expansion boundary, and deferred cost/cardinality decisions.

### 🧹 Cleanup

- Removed another source of branch-route duplication by routing runtime
  branch-set, multi-lookup, and child-prefix stream setup through one shared
  helper.
- Kept planner admission free to compute candidate shapes, but kept downstream
  runtime behavior on spec/lowered-prefix data instead of reconstructing branch
  semantics from loose prefix lengths.

### 🧪 Testing

- Added focused route tests for DESC sparse child-prefix expansion rejection.
- Added focused SQL/fluent branch-route identity coverage.
- Added verbose EXPLAIN coverage for sparse `IN` child-prefix expansion
  diagnostics.
- Revalidated focused branch-set and route-planning tests, SQL+diagnostics
  Clippy, formatting, and whitespace checks.

### 🧭 Migration Notes

- No schema migration is required.
- No SQL syntax, fluent API, result payload, cursor format, persistence format,
  query semantics, public endpoint behavior, cache semantics, or EXPLAIN label
  removals are intended to change.
- Per-branch cursor hard-cut, cost-based routing, prefix-cardinality-driven
  route choice, and DESC sparse child-prefix expansion remain deferred follow-up
  work.
