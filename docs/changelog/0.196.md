# IcyDB 0.196 Changelog

## 0.196.6

### 📝 Summary

- Separates grouped aggregate scenarios from scalar ordered-read pushdown
  candidates in the SQL perf matrix classifier.
- Keeps the slice diagnostic-only: no query execution semantics, cursor
  encoding, cache identity, persisted format, public API, public
  read-admission policy, recovery behavior, or stable storage format changed.

### 🔧 Changed

- `GROUP BY ... ORDER BY ... LIMIT` scenarios now classify as
  `materialized_order` / `materialized` with reason
  `grouped_aggregate_materialized`.
- The remaining `eligible_but_not_pushed` bucket now contains 10 scalar
  `PerfAuditUser` `age IN (...) ORDER BY age ASC/DESC, id ASC/DESC LIMIT 1`
  select scenarios.
- Four grouped aggregate scenarios moved to explicit grouped-materialized
  attribution:
  - `user.aggregate.group_age_count`;
  - `user.aggregate.group_active_avg_age`;
  - `user.aggregate.group_age_having_alias`;
  - `account.aggregate.group_tier_count`.

### 🧪 Testing

- Added focused classifier coverage proving grouped aggregate scenarios are
  classified as materialized grouped work instead of scalar pushdown
  candidates.

### 📊 Performance

- A fresh deterministic full matrix after grouped-aggregate classifier splitting
  passed:
  - 1,756 generated scenarios;
  - 1,675 executed scenarios;
  - 81 failed scenarios;
  - `debug` canister wasm profile;
  - 10 remaining `eligible_but_not_pushed` scenarios.
- The saved-report delta against `0.196.5` passed:
  - 1,756 union scenarios;
  - 1,675 common successful scenarios;
  - 81 common failures;
  - 633 improved scenarios;
  - 1,042 regressed scenarios;
  - 0 new failures;
  - 0 resolved failures;
  - 0 result-signature changes;
  - 0 cursor-signature changes;
  - 4 route-fact changes;
  - 0 order-hint or limit-stop attribution changes;
  - 0 `data_store.get`, index-range, index-entry, or row-count delta.
- Aggregate common-success instructions moved from 4,820,024,676 to
  4,825,263,485, a delta of +5,238,809 instructions, about +0.11%.
- No scenario crossed the `>=10%` and `>=100k` instruction regression gate.
- This is not evidence of a new runtime optimisation. It is evidence that the
  full matrix now cleanly separates scalar secondary-order candidates from
  grouped aggregate materialization work.
- The refreshed `v0.196.0` to `0.196.6` line delta also passed:
  - 1,756 union scenarios;
  - 1,675 common successful scenarios;
  - 81 common failures;
  - 0 new failures;
  - 0 resolved failures;
  - 0 result-signature changes;
  - 0 cursor-signature changes;
  - 613 route-fact changes;
  - 0 `data_store.get`, index-range, index-entry, or row-count delta;
  - +10,931,786 aggregate instructions over a 4,814,331,699 baseline;
  - 0 scenarios crossing the `>=10%` and `>=100k` regression gate.

Route-family/outcome coverage in the refreshed full matrix:

| Route family/outcome/reason | Scenarios | Total instructions | `data_store.get` | Index ranges | Index entries | Rows |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `equality_prefix_ordered_suffix` / `pushed` / `equality_prefix_ordered_suffix_limit_stop_proven` | 1 | 2,481,265 | 0 | 1 | 50 | 50 |
| `incompatible_filter_first_order` / `materialized` / `filter_order_mismatch` | 498 | 679,469,958 | 2,089 | 651 | 2,210 | 1,220 |
| `materialized_order` / `materialized` / `grouped_aggregate_materialized` | 4 | 5,471,623 | 24 | 0 | 0 | 12 |
| `materialized_order` / `materialized` / `requires_materialized_sort` | 114 | 150,044,504 | 570 | 78 | 354 | 291 |
| `materialized_order` / `materialized` / `storage_mirror_has_primary_index_only` | 144 | 2,830,489,179 | 73,728 | 0 | 0 | 672 |
| `not_ordered_or_not_paginated` / `unchanged_or_not_applicable` / `not_a_paginated_select` | 13 | 16,500,754 | 6 | 0 | 0 | 35 |
| `primary_order` / `pushed` / `primary_order_limit_stop_proven` | 169 | 212,238,835 | 758 | 32 | 1,016 | 1,069 |
| `residual_filter_ordered_scan` / `residual_unbounded` / `residual_filter_requires_candidate_scan` | 315 | 405,613,464 | 1,366 | 263 | 484 | 884 |
| `secondary_order` / `eligible_but_not_pushed` / `secondary_order_candidate` | 10 | 12,959,909 | 8 | 30 | 34 | 10 |
| `secondary_order` / `missing_tie_breaker` / `index_order_suffix_gap` | 60 | 82,106,808 | 225 | 45 | 135 | 135 |
| `secondary_order` / `pushed` / `secondary_order_limit_stop_proven` | 227 | 263,799,326 | 464 | 333 | 698 | 631 |
| `unsupported_access_kind` / `unsupported` / `order_expression_not_classified` | 120 | 164,087,860 | 600 | 90 | 240 | 310 |

### 📚 Documentation

- Updated the 0.196 implementation results with the grouped-aggregate
  classifier full-matrix refresh and saved-report delta artifacts.
- Recorded that the next runtime optimisation frontier is now the 10 scalar
  secondary-order `age IN (...) LIMIT 1` candidates.

### 📊 Complexity Delta

- Code footprint for this slice is tiny and matrix-focused: one classifier
  branch and one focused integration test, plus release notes and
  design-result evidence.
- Runtime execution complexity is unchanged. The implementation only changes
  matrix classification and tests.
- Wasm-size delta was not measured. The full matrix recorded the same `debug`
  canister wasm profile before and after; no wasm-size artifact was produced.

### 🧪 Validation

- `cargo fmt --all`
- SQL perf matrix classifier focused tests
- Fresh deterministic SQL perf full matrix
- Saved-report `0.196.5` to `0.196.6` classifier delta helper
- Saved-report `v0.196.0` to `0.196.6` line delta helper

## 0.196.5

### 📝 Summary

- Hardens SQL perf matrix route classification so the `eligible_but_not_pushed`
  bucket contains only true remaining secondary-order candidates.
- Keeps the slice diagnostic-only: no query execution semantics, cursor
  encoding, cache identity, persisted format, public API, public
  read-admission policy, recovery behavior, or stable storage format changed.

### 🔧 Changed

- Matrix route classification now treats any observed order-window work as
  `materialized_order` / `materialized` with reason `requires_materialized_sort`
  instead of leaving it in a pushdown-candidate bucket.
- Primary-order and secondary-order paths with predicates that still require a
  candidate scan now classify as `residual_filter_ordered_scan` /
  `residual_unbounded` with reason
  `residual_filter_requires_candidate_scan`.
- Unsupported expression `ORDER BY` shapes now classify as
  `unsupported_access_kind` / `unsupported` with reason
  `order_expression_not_classified`.
- The tightened classifier moves 549 common-success route facts and limit-stop
  facts to more conservative explanations. In particular, 228 scenarios
  previously reported as pushed primary-order routes now report residual
  unbounded candidate scans when the predicate cannot prove bounded admission.

### 🧪 Testing

- Added integration coverage for:
  - primary-order materialized windows;
  - primary-order residual candidate scans;
  - secondary-order materialized windows;
  - secondary-order residual candidate scans;
  - unsupported expression `ORDER BY` classification.

### 📊 Performance

- A fresh deterministic full matrix after classifier hardening passed:
  - 1,756 generated scenarios;
  - 1,675 executed scenarios;
  - 81 failed scenarios;
  - `debug` canister wasm profile;
  - 12 remaining `eligible_but_not_pushed` scenarios.
- The saved-report delta against the previous descriptor route-diagnostics
  matrix passed:
  - 1,756 union scenarios;
  - 1,675 common successful scenarios;
  - 81 common failures;
  - 456 improved scenarios;
  - 1,219 regressed scenarios;
  - 0 new failures;
  - 0 resolved failures;
  - 0 result-signature changes;
  - 0 cursor-signature changes;
  - 549 route-fact changes;
  - 549 limit-stop attribution changes;
  - 0 `data_store.get`, index-range, index-entry, or row-count delta.
- Aggregate common-success instructions moved from 4,821,729,348 to
  4,820,024,676, a delta of -1,704,672 instructions. Compile attribution moved
  +881,042 instructions, execute attribution moved -2,585,714, planner
  attribution moved +1,894,802, executor attribution moved -4,232,655, and
  store attribution moved -244,825.
- No scenario crossed the `>=10%` and `>=100k` instruction regression gate.
- This is not evidence of a new runtime optimisation. It is evidence that the
  full matrix now separates actual pushed routes from materialized sorts,
  residual candidate scans, and unsupported order expressions.
- The refreshed `v0.196.0` to `0.196.5` line delta also passed:
  - 1,756 union scenarios;
  - 1,675 common successful scenarios;
  - 81 common failures;
  - 0 new failures;
  - 0 resolved failures;
  - 0 result-signature changes;
  - 0 cursor-signature changes;
  - 609 route-fact changes;
  - 0 `data_store.get`, index-range, index-entry, or row-count delta;
  - +5,692,977 aggregate instructions over a 4,814,331,699 baseline;
  - 0 scenarios crossing the `>=10%` and `>=100k` regression gate.

Route-family/outcome coverage in the refreshed full matrix:

| Route family/outcome/reason | Scenarios | Total instructions | `data_store.get` | Index ranges | Index entries | Rows |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `equality_prefix_ordered_suffix` / `pushed` / `equality_prefix_ordered_suffix_limit_stop_proven` | 1 | 2,476,324 | 0 | 1 | 50 | 50 |
| `incompatible_filter_first_order` / `materialized` / `filter_order_mismatch` | 498 | 679,339,040 | 2,089 | 651 | 2,210 | 1,220 |
| `materialized_order` / `materialized` / `requires_materialized_sort` | 114 | 150,007,784 | 570 | 78 | 354 | 291 |
| `materialized_order` / `materialized` / `storage_mirror_has_primary_index_only` | 144 | 2,826,027,305 | 73,728 | 0 | 0 | 672 |
| `not_ordered_or_not_paginated` / `unchanged_or_not_applicable` / `not_a_paginated_select` | 13 | 16,489,578 | 6 | 0 | 0 | 35 |
| `primary_order` / `pushed` / `primary_order_limit_stop_proven` | 169 | 212,133,202 | 758 | 32 | 1,016 | 1,069 |
| `residual_filter_ordered_scan` / `residual_unbounded` / `residual_filter_requires_candidate_scan` | 315 | 405,478,754 | 1,366 | 263 | 484 | 884 |
| `secondary_order` / `eligible_but_not_pushed` / `secondary_order_candidate` | 12 | 15,229,146 | 20 | 30 | 34 | 18 |
| `secondary_order` / `missing_tie_breaker` / `index_order_suffix_gap` | 60 | 82,052,969 | 225 | 45 | 135 | 135 |
| `secondary_order` / `pushed` / `secondary_order_limit_stop_proven` | 227 | 263,579,385 | 464 | 333 | 698 | 631 |
| `unsupported_access_kind` / `unsupported` / `order_expression_not_classified` | 122 | 167,211,189 | 612 | 90 | 240 | 314 |

### 📚 Documentation

- Updated the 0.196 implementation results with the classifier-hardening full
  matrix refresh and saved-report delta artifacts.
- Recorded that 0.196.5 narrows the next runtime optimisation frontier rather
  than claiming a new pushed-access runtime win.

### 📊 Complexity Delta

- Code footprint for this slice is small and matrix-focused: the classifier
  helper/test patch touches the integration matrix audit harness, with release
  notes and design-result evidence updated separately.
- Runtime execution complexity is unchanged. The implementation only changes
  matrix classification and tests.
- Wasm-size delta was not measured. The full matrix recorded the same `debug`
  canister wasm profile before and after; no wasm-size artifact was produced.

### 🧪 Validation

- `cargo fmt --all`
- SQL perf matrix classifier focused tests
- SQL perf matrix audit integration test
- SQL perf matrix audit integration clippy target
- Fresh deterministic SQL perf full matrix
- Saved-report classifier delta helper
- Saved-report `v0.196.0` to `0.196.5` line delta helper
- `jq empty docs/design/0.196-sqlite-comparison-audit/implementation-results.json`
- `git diff --check`

## 0.196.4

### 📝 Summary

- Adds scalar-load route-class proof attribution to canonical EXPLAIN
  descriptors so JSON/text tree output exposes the same route-family burden as
  the SQL matrix.
- Keeps the slice diagnostic-only: no query execution semantics, cursor
  encoding, cache identity, persisted format, public API, public
  read-admission policy, recovery behavior, or stable storage format changed.

### 🔧 Changed

- Scalar-load canonical execution descriptor roots now include `route_family`,
  `route_outcome`, and `route_reason` node properties.
- Route-class descriptor labels are derived from executor-owned route facts and
  selected access shape, not from SQL text or matrix scenario names.
- Grouped aggregate descriptors intentionally remain on their grouped-specific
  route contract instead of receiving scalar-load route-family labels.

### 📊 Performance

- A fresh deterministic full-matrix refresh comparing clean `0.196.3` to the
  descriptor route-diagnostics slice passed:
  - 1,756 union scenarios;
  - 1,675 common successful scenarios;
  - 81 common failures;
  - 0 new failures;
  - 0 resolved failures;
  - 0 result-signature changes;
  - 0 cursor-signature changes;
  - 0 route-fact changes;
  - 0 `data_store.get`, index-range, index-entry, or row-count delta.
- The descriptor route-diagnostics refresh measured +4,381,279 total
  instructions over a 4,817,348,069 instruction baseline, about +0.09%, with
  no scenario crossing the `>=10%` and `>=100k` regression gate.
- A clean `v0.196.0` to end-of-196 full-matrix delta also passed:
  - 1,756 union scenarios;
  - 1,675 common successful scenarios;
  - 0 new failures;
  - 0 result-signature changes;
  - 0 cursor-signature changes;
  - 0 access-counter or row-count delta.
- The `v0.196.0` to end-of-196 delta measured +7,397,649 total instructions
  over a 4,814,331,699 instruction baseline, about +0.15%, with no regression
  gate crossings. The 60 route-fact changes are Blob `ORDER BY bucket, id`
  scenarios reclassified to `secondary_order` / `missing_tie_breaker` because
  the declared ordered metadata index is `(bucket, label, id)`.

### 📚 Documentation

- Updated the 0.196 implementation results with the descriptor
  route-diagnostics matrix refresh and the `v0.196.0` to end-of-196 delta
  artifact paths.
- Recorded that post-`0.196.0` work is diagnostics hardening rather than a new
  runtime optimization.

### 📊 Complexity Delta

- Code footprint for this slice is small and diagnostics-focused: 8 tracked
  files changed at the split snapshot, with about +458 / -12 tracked lines
  across the descriptor diagnostics and evidence update.
- Runtime execution complexity is unchanged. The implementation projects
  existing route facts into descriptor output and records the matrix evidence.
- Wasm-size delta was not measured.

### 🧪 Validation

- `cargo fmt --all`
- Focused canonical EXPLAIN descriptor route-class test
- Executor semantics snapshot suite
- `cargo clippy -p icydb-core --all-features --tests -- -D warnings`
- Fresh clean `0.196.3` descriptor route-diagnostics full matrix
- Saved-report descriptor route-diagnostics delta helper
- Fresh clean `v0.196.0` deterministic full matrix baseline
- Saved-report `v0.196.0` to end-of-196 delta helper
- `jq empty docs/design/0.196-sqlite-comparison-audit/implementation-results.json`
- `git diff --check`

## 0.196.3

### 📝 Summary

- Projects bounded ordered-read limit-stop proof attribution into canonical
  EXPLAIN descriptors.
- Keeps the slice diagnostic-only: no query execution semantics, cursor
  encoding, cache identity, persisted format, recovery behavior, public API, or
  public read-admission policy changed.

### 🔧 Changed

- Canonical execution descriptor roots now include `limit_stop_after` when the
  route has a bounded fetch proof.
- Direct streaming routes record the same
  `possible(limit=...,lookahead=...,fetch=...)` label as verbose EXPLAIN.
- Routes with a bounded candidate fetch but materialized final order record the
  stable disabled reason, for example
  `disabled(residual_filter_blocks_direct_streaming)`.

### 🧪 Testing

- Added focused canonical EXPLAIN coverage for the root `limit_stop_after`
  property.
- Updated executor semantics snapshots for bounded and disabled limit-stop
  descriptor labels.

### 📊 Performance

- No runtime performance delta was measured for this descriptor-only slice.
- Full SQL matrix and wasm-size measurement were not rerun for `0.196.3`.

### 📚 Documentation

- Updated the 0.196 design and implementation-result artifacts to distinguish
  canonical descriptor `limit_stop_after` node properties from verbose EXPLAIN
  diagnostics and matrix-level structured attribution.

### 📊 Complexity Delta

- Code footprint for this slice was small and localized to descriptor
  projection and snapshots.
- Runtime execution complexity is unchanged. The implementation only projects
  existing bounded-fetch proof labels into canonical descriptor output.
- Wasm-size delta was not measured.

### 🧪 Validation

- `cargo fmt --all`
- Focused canonical EXPLAIN descriptor limit-stop property test
- Canonical EXPLAIN intent suite
- Verbose EXPLAIN intent suite
- Executor semantics snapshot suite
- `cargo clippy -p icydb-core --all-features --tests -- -D warnings`
- `jq empty docs/design/0.196-sqlite-comparison-audit/implementation-results.json`
- `git diff --check`

## 0.196.2

### 📝 Summary

- Promotes bounded ordered-read limit-stop proof into verbose EXPLAIN route
  diagnostics.
- Keeps the slice diagnostic-only: no query execution semantics, cursor
  encoding, cache identity, persisted format, recovery behavior, public API, or
  public read-admission policy changed.

### 🔧 Changed

- Verbose route diagnostics now include `diag.r.limit_stop_after`.
- Directly streaming bounded ordered reads report the planned limit, lookahead,
  and fetch window, for example
  `possible(limit=3,lookahead=1,fetch=6)`.
- Routes that cannot prove final limit-stop behavior report a stable disabled
  reason such as `requires_materialized_sort`,
  `residual_filter_blocks_direct_streaming`, `continuation_applied`,
  `no_limit`, or `no_bounded_fetch`.

### 🧪 Testing

- Updated verbose EXPLAIN snapshots for top-N, materialized-order,
  index-prefilter, index-range-residual, and fallback route shapes.
- Added a focused assertion that top-N verbose EXPLAIN carries the
  `limit_stop_after` proof beside the existing fetch hint.

### 📊 Performance

- No runtime performance delta was measured for this diagnostics-only slice.
- Full SQL matrix and wasm-size measurement were not rerun.

### 📚 Documentation

- Updated the 0.196 design and implementation-result artifacts to distinguish
  matrix-level structured `limit_stop_after` attribution from the verbose
  EXPLAIN route diagnostic.

### 📊 Complexity Delta

- Code footprint for this slice was small: 8 tracked files changed at the last
  snapshot, with about +97 / -2 tracked lines.
- Runtime execution complexity is unchanged. The implementation only projects
  existing route facts into verbose diagnostics.
- Wasm-size delta was not measured.

### 🧪 Validation

- `cargo fmt --all`
- `cargo test -p icydb-core --all-features db::query::intent::tests::verbose -- --nocapture`
- `cargo test -p icydb-core --all-features db::query::intent::tests::explain -- --nocapture`
- `cargo test -p icydb-core --all-features explain_execution_verbose_reports_top_n_seek_hints -- --nocapture`
- `cargo clippy -p icydb-core --all-features --tests -- -D warnings`
- `python3 -m json.tool docs/design/0.196-sqlite-comparison-audit/implementation-results.json`
- `git diff --check`

## 0.196.1

### 📝 Summary

- Hardens the 0.196 SQLite-audit evidence layer without changing query
  semantics, cursor encoding, cache identity, persisted format, recovery
  behavior, or public read admission.
- Makes materialized ordering and bounded ordered-read proof attribution harder
  to cherry-pick in future matrix closeouts.

### 🔧 Changed

- Materialized `ORDER BY` execution nodes now expose `order_by_idx_hint`, a
  compact normalized shape for the requested deterministic ordering.
- Verbose route diagnostics now include `diag.r.order_by_idx_hint` for
  non-streaming ordered load routes.
- SQL perf matrix samples and delta rows now carry `order_by_idx_hint`.
- Matrix samples and before/after deltas now report `limit_stop_after` as a
  structured proof object with returned limit, lookahead, observed match/index
  counts, and disabled reason.
- The matrix classifier now separates Blob `ORDER BY bucket, id` routes from
  pushable Blob `ORDER BY bucket, label, id` routes because the declared ordered
  metadata index is `(bucket, label, id)`.

### 🧪 Testing

- Added EXPLAIN coverage for materialized-order index-shape hints.
- Updated verbose explain snapshots for materialized order diagnostics.
- Added SQL perf matrix tests for order-hint extraction, Markdown delta output,
  Blob index suffix-gap classification, pushable Blob `(bucket, label, id)`
  classification, and structured `limit_stop_after` attribution.

### 📊 Performance

- A fresh order-hint deterministic matrix refresh passed before the
  classifier/`limit_stop_after` schema hardening:
  - 1,756 union scenarios;
  - 1,675 common successful scenarios;
  - 81 common failures;
  - 0 new failures;
  - 0 resolved failures;
  - 0 result-signature changes;
  - 0 cursor-signature changes;
  - 1,663 order-hint transitions.
- The classifier and `limit_stop_after` schema hardening were validated with
  focused integration tests and clippy. The full matrix was not rerun after
  those report-schema-only changes.

### 📚 Documentation

- Updated the 0.196 design and implementation-result artifacts to describe
  order-shape attribution, Blob index suffix-gap classification, structured
  limit-stop proof attribution, and scratch-artifact retention.

### 📊 Complexity Delta

- Code footprint for this diagnostics slice is report/test heavy: 11 tracked
  files changed at the last snapshot, with about +556 / -33 tracked lines.
- Runtime complexity is unchanged for query execution semantics; the production
  change is additional diagnostics attached to explain output.
- Matrix complexity increased intentionally so future optimizer claims must
  show route shape, limit-stop proof, and disabled reasons.
- Wasm-size delta was not measured. No wasm/generated output was changed.

### 🧪 Validation

- `cargo fmt --all`
- `cargo test -p icydb-core --all-features db::query::intent::tests::verbose -- --nocapture`
- `cargo test -p icydb-core --all-features db::query::intent::tests::explain -- --nocapture`
- `cargo clippy -p icydb-core --all-features --tests -- -D warnings`
- `cargo test -p icydb-testing-integration --test sql_perf_matrix_audit -- --nocapture`
- `cargo clippy -p icydb-testing-integration --tests -- -D warnings`
- `python3 -m json.tool docs/design/0.196-sqlite-comparison-audit/implementation-results.json`
- `git diff --check`
- Fresh order-hint deterministic SQL perf matrix
- Saved-report order-hint SQL perf matrix delta helper

## 0.196.0

### 📝 Summary

- Closes the SQLite-informed ordered-read pushdown slice with adversarial
  before/after performance evidence instead of focused-only proof.
- Adds full deterministic SQL matrix route classification and union-key delta
  artifacts so optimized, unchanged, failed, and regressed scenarios are visible
  across the whole generated matrix.
- Keeps the production runtime slice narrow: no persisted-format change, no
  cursor-token protocol change, no public API compatibility shim, and no
  recovery or journal semantics change.

### 🔧 Changed

- Selected secondary index-range bounds now count as access-proven predicates
  during residual stripping. Queries such as an age-range filter ordered by the
  same age index no longer retain redundant post-access residual work when the
  chosen range already proves the lower and upper bounds.
- The range-bound proof stays conservative. Stricter sibling predicates remain
  residual filters, and composite ranges strip only the fixed prefix equality
  plus the selected range slot bounds.
- Public read admission can now admit exact primary-key `by_id(...)` and
  bounded `by_ids(...)` reads from the selected key-count upper bound, without
  requiring a redundant `LIMIT`.
- Oversized `by_ids(...)` sets still fail the public returned-row policy.

### 🩹 Fixed

- Fixes the `E188` public read-admission failure for exact `by_id(...)`
  lookup paths that were already route-proven as primary-key access but lacked
  a literal `LIMIT`.

### 🧪 Testing

- Added planner tests for access-proven index-range residual stripping,
  stricter residual predicates, and composite prefix-plus-range stripping.
- Added session read-admission tests proving primary-key `by_id(...)` lookup
  is admitted without a redundant `LIMIT`, while broader unbounded public reads
  remain rejected.
- Added adversarial cursor continuation tests for primary-key and secondary
  ordered paths, including deletion, insertion before and after cursor
  boundaries, duplicate ordered values, primary-key tie-breaks, and indexed
  field updates.
- Added diagnostics coverage for bounded secondary-order and equality-prefix
  ordered-suffix routes.
- Extended the SQL perf matrix harness with route family/outcome labels,
  stable result signatures, cursor signatures, canister wasm profile recording,
  and a saved-report delta helper with closeout gates.

### 📊 Performance

- Full deterministic matrix closeout passed:
  - 1,756 union scenarios;
  - 1,675 common successful scenarios;
  - 699 improved scenarios;
  - 976 regressed scenarios;
  - 0 new failures;
  - 0 resolved failures;
  - 0 result-signature changes;
  - 0 cursor-signature changes;
  - 10 route-fact changes from eligible to pushed.
- The largest intended wins came from bucket-range and age-range ordered reads
  where redundant residual bounds were removed from the selected access path.
- Representative improvements:
  - `blob.select.lengths.bucket_range.bucket_label_asc.limit3`: -979,543 total
    instructions, `data_store.get` -6, index ranges -2, index entries -11;
  - `blob.select.lengths.bucket_range.bucket_label_asc.limit1`: -826,953 total
    instructions, `data_store.get` -3, index ranges -3, index entries -14;
  - `user.select.text_expr.age_range.age_desc.limit1`: -509,495 total
    instructions, `data_store.get` -2, index ranges -2, index entries -7;
  - `user.select.numeric_expr.age_range.age_asc.limit1`: -505,639 total
    instructions, `data_store.get` -2, index ranges -2, index entries -7.
- Route aggregate deltas included:
  - `secondary_order` / `pushed`: 255 scenarios, -6,488,741 total
    instructions;
  - `secondary_order` / `eligible_but_not_pushed`: 116 scenarios,
    -3,806,227 total instructions;
  - `primary_order` / `pushed`: 397 scenarios, -1,158,849 total
    instructions;
  - `equality_prefix_ordered_suffix` / `pushed`: 1 scenario, -192,393 total
    instructions.
- The largest regressions were storage-mirror materialized-order scenarios and
  stayed within the configured closeout gate. No broad semantic or failure-key
  regression was accepted.

### 📚 Documentation

- Added the 0.196 design directory with the SQLite-comparison audit, design
  plan, machine-readable findings, implementation results, and compact
  machine-readable closeout summary.
- Updated the read-admission contract to document exact selected primary-key
  access as a valid returned-row bound.
- Added the optimizer canonicalization design as an adjacent artifact. Where it
  overlaps the SQLite-comparison audit execution line, it can merge into 0.196
  rather than standing as a marker that the remaining audit findings moved out
  of scope.

### 📊 Complexity Delta

- Code footprint for the 0.196 implementation slice was mostly evidence and
  tests: 11 tracked files changed before this changelog update, with about
  +2,845 / -79 tracked lines, plus the 0.196 design result files.
- Runtime complexity increased only narrowly: range residual stripping now
  carries selected range-bound clauses, and admission can derive a returned-row
  bound from exact selected primary-key access.
- Matrix and test complexity increased intentionally to make future optimizer
  claims harder to cherry-pick.
- Wasm-size delta was not measured for this slice. The full matrix recorded the
  same `debug` canister wasm profile before and after.

### 🧪 Validation

- `cargo fmt --all`
- `cargo test -p icydb-core --all-features db::query::admission::tests -- --nocapture`
- `cargo test -p icydb-core --all-features primary_key_lookup_without_limit -- --nocapture`
- `cargo test -p icydb-core --all-features planner_index_range_residual_stripping -- --nocapture`
- `cargo test -p icydb-core --all-features planner_composite_index_range_residual_stripping -- --nocapture`
- `cargo test -p icydb-core --all-features read_admission -- --nocapture`
- `cargo test -p icydb-testing-integration --test sql_perf_matrix_audit`
- `cargo clippy -p icydb-core --all-features --tests -- -D warnings`
- `cargo clippy -p icydb-testing-integration --all-features --test sql_perf_matrix_audit -- -D warnings`
- `jq empty docs/design/0.196-sqlite-comparison-audit/implementation-results.json`
- `git diff --check`
- Full after deterministic SQL perf matrix
- Full before/after SQL perf matrix delta helper
