# IcyDB 0.237 — SQL Query Performance Hotspot Rediscovery

## 0.237.2

### ⚡ Performance

- Lower a single `<`, `<=`, `>` or `>=` comparison on the leading component of
  a compound accepted index through the existing one-sided range contract.
  Ordered reads and indexed extrema now begin at the encoded bound instead of
  traversing excluded leading-component groups.
- At 2,048 rows, upper-range `MAX` falls from 121,581,549 to 1,213,725
  instructions, lower-range `MIN` falls from 15,757,520 to 1,444,681, and the
  corresponding descending/ascending covering `LIMIT 1` reads fall from
  246,977,320/274,751,259 to 1,153,168/1,343,875. Each query reads exactly one
  index entry; aggregate cases read one data row and covering reads read none.

```sql
SELECT MAX(age) FROM PerfAuditUser WHERE age < 43;
```

### 📊 Audit

- Reuse the existing semantic range spec, raw low/high trailing-component
  sentinels and aggregate route. No SQL classifier, new route, cache, mutable
  owner, persisted state, invalidation edge or schema authority is added.
- Delete ten net runtime lines across three planner files. Against an isolated
  exact `v0.237.1` archive built with the same current pipeline, final raw Wasm
  shrinks by 108 bytes from 4,450,084 to 4,449,976; deterministic gzip shrinks
  by four bytes from 1,728,371 to 1,728,367. Candid is byte-identical.

### 🧪 Testing

- Add exact duplicate-group, inclusive/exclusive, empty-range, both-direction,
  warm-cache, accessed-corruption and same-Wasm recovery proofs at 0, 16, 256
  and 2,048 rows. `WHERE true`, other-field predicates, computed extrema,
  aggregate `FILTER` and multiple extrema retain conservative behavior.

## 0.237.1

### ⚡ Performance

- Route unfiltered, non-nullable direct-field scalar `MAX` through the existing
  aggregate planner and descending accepted-index edge even when the maximum
  target value is duplicated. At 2,048 rows, focused `MAX(age)` falls from
  114,777,435 to 1,414,045 instructions, saving 113,363,390 with one row and
  one index-entry read; true-warm execution costs 982,067 instructions.
- Keep query predicates, computed extrema, aggregate `FILTER`, multiple
  terminals, nullable targets, unindexed fields and unsupported reverse paths
  on complete reduction. An exploratory `MAX(age) WHERE age < 43` candidate
  visited 193 index entries and cost 23,309,917 instructions at only 256 rows,
  so upper-range admission remains a separately planned intake.

### 📊 Audit

- Remove the redundant tie-free helper after the existing accepted-schema and
  aggregate-route owners prove target/index compatibility and reverse
  traversal. The scalar result cannot expose which tied row supplied the same
  maximum value.
- Change five production files by approximately 53 fewer net runtime lines and
  add no type, module, mutable owner, cache, invalidation edge, persisted state,
  schema authority, endpoint or behavior mode. Release-shaped raw Wasm shrinks
  by 1,049 bytes from 4,583,069 to 4,582,020 bytes; a like-for-like gzip-9
  comparison shrinks by 385 bytes from 1,776,699 to 1,776,314. Generated code,
  Candid, exports, stable formats, package versions and public APIs remain
  unchanged.

## 0.237.0

### 🩹 Fixed

- Closed ICYDB-011 by making the release candidate's tested-tree gate reject
  non-ignored untracked paths. Validation can no longer pass using source or
  fixtures absent from the candidate commit and eventual release tag; ignored
  local build artifacts remain permitted.
- Restored the predecessor's direct materialized-covering fold for existing
  routes while keeping Patch 6's fallible demand-driven source local to its new
  bounded range. The frozen 16-branch page returns from 5,328,287 to 5,285,262
  instructions under its unchanged 5,300,000 ceiling with the same 65 index
  entries and result.

### ⚡ Performance

- Route schema-proven row-equivalent global SQL counts through the existing
  accepted-authority exact entity-cardinality executor. At 2,048 rows,
  `COUNT(*)`, `COUNT(id)`, `COUNT(1)` and `COUNT(*) WHERE true` fall from
  96,891,236–126,777,804 instructions to 815,629–854,348 instructions, saving
  96,075,607–125,950,053 instructions with unchanged results and zero row or
  index traversal.
- Preserve buffered semantics for nullable-field counts, `DISTINCT`, aggregate
  `FILTER` and all other non-row-equivalent shapes. A true-warm exact count
  remains at 731,475 instructions through the existing fingerprint-bound
  compiled-command cache.
- Route one safe non-nullable direct-field scalar `MIN` or `MAX` through the
  existing aggregate route planner and bounded index edge. At 2,048 rows,
  `MIN(id)`, `MAX(id)`, `MIN(age)` and supported range `MIN(age)` fall from
  102,538,164–134,776,857 instructions to 899,317–1,727,704 instructions,
  saving 100,826,309–133,049,153 instructions with at most one row and one
  index-entry read.
- Preserve complete reduction for nullable targets, expressions, aggregate
  `FILTER`, multiple extrema, unindexed fields and non-tie-free secondary
  `MAX`. A true-warm primary `MIN` costs 579,445 instructions through the
  existing compiled-command cache.
- Stop cursorless pure-covering secondary ordered reads after the requested
  present-row window while retaining authoritative row-presence checks. At
  2,048 rows, ascending and descending `LIMIT 1` endpoints fall from
  55,557,996/55,361,305 instructions to 1,510,582/1,211,211, saving
  54,047,414/54,150,094 instructions with one index entry and no row fetch.
- Keep live cursors, residual predicates, prefix families, `DISTINCT`, hybrid,
  reordered and unbounded reads on their existing routes. True-warm ascending
  endpoint execution costs 1,079,099 instructions.
- Admit exactly 17 canonical accepted-index prefix keys for the existing SQL
  direct-count metadata target without raising the shared 16-branch page cap.
  At 2,048 rows the measured 17-key count falls from 42,726,968 to 2,267,492
  instructions, saving 40,459,476 with zero row or index-entry traversal; true
  warm costs 1,238,845 instructions.
- Keep 18 distinct count keys on the scan fallback, keep ordinary 17-value
  pages over the shared branch cap, and leave the public exact-count adapter's
  16-key contract unchanged. Duplicate SQL membership literals canonicalize
  before count admission and do not double count.

### 📊 Audit

- Reassigned the unreleased 0.237 line to a separate SQL performance
  rediscovery audit while preserving the rejected accepted-schema candidate as
  historical no-build evidence.
- Hard-cut the comparable measurement identity to
  `icydb-sql-performance/0.237/v1`; earlier evidence remains historical and
  cannot be treated as a current baseline.
- Expanded the exact-cardinality scale profile first from 24 to 42 selected
  families and then, after broader hotspot localization, to 58 families and
  174 scenarios at 16, 256, and 2,048 rows. The added pairs
  isolate projection and payload width, token branch fanout, over-cap
  materialization, `OFFSET`, non-count and distinct/filter aggregates, computed
  projection, wide indexed reads, heap/journaled full-entity work, count
  equivalence, indexed extrema, secondary ordered endpoints, and the exact
  16/17 prefix-branch boundary.
- Removed the historical 15-case token-only forced hotspot shortlist. Complete
  P1 and scale rankings plus the existing regression and grouped contract
  sentinels now select confirmation work without that prior-investigation bias.
- Captured a complete local candidate cohort: all 1,787 P1 cases and all 126
  exact scale observations passed against one Wasm. The strongest 2,048-row
  costs are hash grouping at about 139.2M instructions, scalar aggregation at
  96.9M-109.3M, and heap/journaled full-entity materialization at about 82.5M.
- Established a strict three-run clean calibration at the committed candidate:
  every run passed 1,787 P1 scenarios, 126 scale observations and 502 repeated
  cold/warm P2 confirmations. All 72,600 reviewed P2 metric envelopes and all
  scale total, normalized and slope envelopes were identical across runs.
- Kept the earlier dirty candidate evidence out of strict P2 selection. The
  clean-source gate rejected it without being weakened; only the later clean
  committed subject contributes reviewed calibration evidence.
- Recorded 245 recurring ranked scenarios already covered by maintained
  sentinels and handed 125 additional recurring scenarios to the localization
  closeout for explicit promotion or rejection. No budget or sentinel is
  changed automatically from a ranking result.
- Closed the 125-case promotion review without adding permanent sentinels: 93
  small-fixture ranking members, 17 heap/journal mirrors and 15 token variants
  duplicate dynamic rankings or maintained scale/route controls.
- Declined to add 8,192-row canaries: three exact cardinalities already separate
  the steep families from fixed-window blob and admitted token paths, and a
  longer ladder would not yet identify a removable phase.
- Reopened the initial one-intake conclusion with 141 query/cardinality probes,
  each measured cold and warm. Four independent candidate owners remain:
  count-equivalent exact cardinality, indexed `MIN`/`MAX`, secondary ordered
  early-stop, and count-specific prefix-cardinality admission. Their measured
  2,048-row opportunities range from about 40.1M to 104.2M instructions. Patch
  4 implemented only the exact entity-count intake; Patches 5–7 subsequently
  evaluated the other three through separate accepted designs and handoffs.
- Rejected a blanket prefix-branch cap raise. The admitted 16-branch page is
  1.6M-2.1M instructions slower than the 17-branch fallback at the two smaller
  fixtures before becoming about 7.0M faster at 2,048 rows, so one static cap
  change is not justified.
- Rejected the apparent 119,265,228-instruction hash/ordered grouping gap as an
  optimization basis because the paired fixtures perform different physical
  work: 2,048 versus 358 row gets and 100 versus 17 observed groups.

### 🧪 Testing

- Pinned the final 58-family scale scenario-set hash and exact eight-shard
  membership while retaining the existing result, route, fixture,
  physical-work, phase, and artifact completeness checks. All eight candidate
  shards and their 174-observation merge pass.
- Confirmed identical instrumentation overhead of 48,155 instructions (366
  basis points) across the three clean runs and zero cross-run measurement,
  semantic or physical-work drift.
- Selected run one as the reviewed opening baseline and re-derived run two's
  independent raw receipts against it without repeating measurement. The
  ordinary comparison passes with all 74,925 P2 deltas, 126 scale totals, 504
  normalized costs and 84 slopes unchanged and no regressions.
- Repeated the final 58-family profile on the clean committed subject three
  times. Every run passed all 1,787 P1 cases, 174 scale observations and 502
  P2 confirmations; all 72,600 P2 envelopes plus 174 scale totals, 690
  normalized costs and 116 slopes had zero cross-run spread. The final
  ordinary comparison also passed with all 74,925 P2 deltas unchanged.
- Aligned the fixture-identity and synthetic merge, comparison and calibration
  goldens with the final profile's 174 observations and 116 slopes. The
  synthetic fixtures retain their own exact 345 normalized-cost rows; the live
  calibration's richer facts retain 690.
- Added focused release-receipt coverage for rejected untracked source and the
  permitted ignored-artifact control.
- Added focused exact-count proofs for empty, 16-, 256- and 2,048-row entities,
  true-warm compiled-cache reuse, accepted nullable-field fallback, distinct
  and filtered controls, nontrivial predicates, unavailable journaled metadata,
  fold maintenance and same-Wasm recovery.
- Added focused extrema proofs for empty, 16-, 256- and 2,048-row entities,
  primary and secondary bounded edges, supported predicates, true-warm cache
  reuse, conservative terminal shapes, unindexed fields, explicit order/limit
  semantics, nullable mixed/all-null fallback, atomic publication, signed
  ordering and same-Wasm recovery.
- Added focused ordered-endpoint proofs for empty, 16-, 256- and 2,048-row
  entities, ascending and descending order, duplicate ties, `OFFSET`, exact
  bounded physical work, accessed missing-row corruption, warm reuse,
  conservative controls and same-Wasm recovery.
- Kept the frozen 16-branch streaming page as a release-level unaffected
  control; the closeout correction restores its predecessor-specialized fold
  and passes the unchanged instruction and 65-entry physical-work gates.
- Added focused 17-key count proofs at 16, 256 and 2,048 rows, native composite
  prefix coverage, duplicate canonicalization, 18-key scan fallback, unchanged
  17-value page fallback, true-warm reuse and same-Wasm recovery.
- SQLite remains a correctness reference for eligible reduced-SQL cases;
  PocketIC IcyDB instructions remain the performance unit. Native SQLite timing
  is not compared with IC Wasm instructions.

### 📊 Audit Footprint

- Patches 1–3 left production runtime, schemas, generated code, public APIs,
  Candid, exports, stable formats and package versions unchanged.
- The SQL audit canister source is unchanged. The current shared build is
  4,576,629 raw bytes; no clean predecessor rebuild was taken in this patch, so
  no numerical Wasm delta is claimed. Maintained complexity growth is one
  release-gate helper and its focused fixture plus declarative host-side scale
  cases and documentation; no runtime state or behavior axis is added.
- Reopened Patch 3 changes declarative host-side scale cases and documentation
  only. Temporary exploratory instrumentation was removed. The SQL audit
  canister and production runtime remained unchanged at that boundary;
  subsequent optimizations required the separate accepted designs summarized
  below.
- The final evidence was captured at clean revision
  `e3c35acfb42925c37579b3e3e88502912a422507`, tree
  `bb48e38f8e5eec18a907fd4d994a8e0de7eb7e1c`, against the same 4,576,629-byte
  Wasm. The expanded measurement line is closed; full release validation and
  publication remain user-owned.
- Patch 4 changes six production files by approximately 90 net runtime lines
  and adds no module, mutable owner, cache, invalidation edge, persisted state,
  schema authority, endpoint or public behavior axis. Raw Wasm grows by 453
  bytes from 4,576,629 to 4,577,082 bytes. Schemas, generated code, public APIs,
  Candid, exports, stable formats and package versions remain unchanged.
- Patch 5 changes eight production files by approximately 180 net runtime lines
  plus focused tests and adds no module, mutable owner, cache, invalidation
  edge, persisted state, schema authority, endpoint or public behavior axis.
  Raw Wasm grows by 2,208 bytes from 4,577,082 to 4,579,290 bytes. Schemas,
  generated code, public APIs, Candid, exports, stable formats and package
  versions remain unchanged.
- Patch 6 changes two production runtime files by approximately 180 net lines
  plus focused tests and adds no module, mutable owner, cache, invalidation
  edge, persisted state, schema authority, endpoint or behavior mode. Raw Wasm
  grows by 1,910 bytes from 4,579,290 to 4,581,200 bytes in the isolated
  corrected Patch 6 snapshot. Schemas, generated code, public APIs, Candid,
  exports, stable formats and package versions remain unchanged.
- Patch 7 changes four production files by approximately 150 net runtime lines
  plus focused tests and adds one evidence-bounded internal count cap, but no
  module, mutable owner, cache, invalidation edge, persisted state, schema
  authority, endpoint or public behavior mode. Against the corrected Patch 6
  snapshot, raw Wasm grows by 1,869 bytes to a final 4,583,069 bytes; the final
  gzip is 1,782,329 bytes. Schemas, generated code, public APIs, Candid,
  exports, stable formats and package versions remain unchanged.
