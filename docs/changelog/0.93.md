# 0.93 Changelog

## 0.93.2

- `0.93.2` keeps the later `0.93` work on the ordinary shared SQL `SELECT` floor and follows the new parser micro-attribution through to concrete parser-side cuts instead of drifting back into diagnostics or metadata work.
- the first cut in this patch removes parser-owned lookahead cloning on the hot postfix predicate and unsupported-function paths:
  - `NOT IN`, `NOT LIKE`, `NOT ILIKE`, and `NOT BETWEEN` postfix detection no longer clones the whole SQL token cursor just to inspect the next token
  - unsupported function-call probing for `OVER (...)` also now uses fixed-offset token lookahead instead of cloning cursor state
  - this immediately lowered the membership-heavy cold rows again, especially the remaining `NOT IN` compile outlier
- the next cut pushes the shared lexer further down by replacing repeated `peek_byte()` / `Option` loops with direct indexed byte scans:
  - whitespace skipping now walks the input slice directly
  - number scanning now reads digit and decimal runs directly from the byte slice
  - identifier scanning now walks identifier bytes directly too
  - that keeps the same reduced SQL surface but trims the remaining tokenize budget on the canonical shared-floor rows
- the next shared-floor cut removes a broad scalar parser tax on plain field projections:
  - simple projection items like `SELECT id` or `SELECT id, name` now use a direct field fast path instead of routing every item through the full projection expression parser
  - that cut materially reduced the `expr` bucket on the canonical ordinary read rows from the older `4,648`-style shape down to the low hundreds
- the last parser follow-through in this patch removes one more broad allocation source at the shared cursor boundary:
  - consumed identifier tokens are now moved out of the token cursor instead of cloned
  - that change applies across entity names, selected fields, grouped keys, and order targets, so it lowers ordinary SQL parser cost broadly instead of helping only one special query family
- by the end of `0.93.2`, the representative shared-floor rows were materially below their checked-in baselines:
  - `user.pk.key_only.asc.limit1`: `477,982 -> 473,810` (`-4,172`)
  - `user.pk.order_only.asc.limit1`: `508,221 -> 500,433` (`-7,788`)
  - `user.pk.order_only.asc.limit2`: `744,682 -> 542,916` (`-201,766`)
  - `user.name.lower.order_only.asc.limit3`: `1,183,160 -> 662,128` (`-521,032`)
  - `user.grouped.age_count.limit10`: `1,192,045 -> 1,021,504` (`-170,541`)
  - `user.age.in.limit3`: `1,723,754 -> 1,373,814` (`-349,940`)
  - `user.age.not_in.limit3`: `1,216,013 -> 872,068` (`-343,945`)
- the same cuts improved the new permanent tiny-query anchors added in `0.93.1`, so the long-term “main query” line is now in better shape too:
  - `SELECT id FROM PerfAuditUser ORDER BY id ASC LIMIT 1`: `477,982 -> 473,810`
  - `SELECT id, name FROM PerfAuditUser ORDER BY id ASC LIMIT 1`: `508,221 -> 500,433`
  - `repeat.user.pk.order_only.asc.limit1.runs10`: `309,952 -> 309,838`
- the parser micro-attribution now points at a much narrower remaining cold cost profile:
  - lexer waste is no longer dominant
  - generic projection expression parsing is no longer the main ordinary read tax
  - the remaining cold parser budget is now mostly the `SELECT` statement shell itself: clause sequencing, optional alias handling, and other thin statement-level parse work around already-simple queries
- focused verification for `0.93.2` stayed on the parser/shared-floor surfaces that actually changed:
  - `cargo clippy -p icydb-core --all-targets -- -D warnings`
  - `cargo test -p icydb-core db::session::tests::sql_surface -- --nocapture`
  - `POCKET_IC_BIN=/home/adam/projects/icydb/.cache/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_perf_audit sql_perf_shared_floor_queries_report_phase_breakdown -- --nocapture`

---

## 0.93.1

- `0.93.1` keeps the later `0.93` work on the shared SQL floor instead of drifting further into explain-only cleanup.
- the first follow-through in this patch makes the parser cost visible at the right boundary:
  - reduced-SQL compile attribution no longer treats parse as one opaque bucket
  - the parser now reports parser-owned subphases for `tokenize`, statement-shell/select parsing, expression roots, and predicate roots
  - the SQL compile attribution and PocketIC audit canister both carry those new fields through to the sampled SQL perf report
- that new parser split immediately changed the shape of the shared-floor investigation:
  - ordinary cold shared-floor rows showed that the remaining parse budget was dominated by `tokenize`, not by expression parsing
  - representative cold rows before the lexer cut looked like:
    - `user.pk.order_only.asc.limit2`: `parse=44,174`, `tokenize=22,204`, `select=17,322`, `expr=4,648`
    - `user.name.lower.order_only.asc.limit3`: `parse=58,406`, `tokenize=30,666`, `select=18,236`, `expr=9,504`
    - `user.grouped.age_count.limit10`: `parse=53,160`, `tokenize=31,075`, `select=17,586`, `expr=4,499`
    - `user.age.in.limit3`: `parse=74,529`, `tokenize=37,195`, `select=20,573`, `expr=5,437`, `predicate=11,324`
    - `user.age.not_in.limit3`: `parse=79,092`, `tokenize=35,155`, `select=19,773`, `expr=4,647`, `predicate=19,517`
- the next cut then went straight at that dominant tokenize bucket:
  - keyword classification now works from borrowed identifier bytes in the shared lexer instead of allocating one temporary `String` for every keyword token just to recognize and discard it again
  - the shared lexer also now preallocates a simple token-buffer capacity hint from the SQL length so cold rows stop growing the token vector from zero
  - those two changes materially reduced the cold parse front door on the representative shared-floor rows:
    - `user.pk.order_only.asc.limit2`: `parse 44,174 -> 35,575`, `tokenize 22,204 -> 13,655`, total `560,527`
    - `user.name.lower.order_only.asc.limit3`: `parse 58,406 -> 46,886`, `tokenize 30,666 -> 19,169`, total `678,573`
    - `user.grouped.age_count.limit10`: `parse 53,160 -> 40,419`, `tokenize 31,075 -> 18,233`, total `1,041,003`
    - `user.age.in.limit3`: `parse 74,529 -> 62,003`, `tokenize 37,195 -> 25,453`, total `1,387,513`
    - `user.age.not_in.limit3`: `parse 79,092 -> 67,239`, `tokenize 35,155 -> 23,714`, total `894,785`
- that leaves the shared SQL floor in a clearer state:
  - the dominant cold parse bucket is no longer lexer waste
  - the next obvious shared-floor work is statement-shell/select parsing
  - the remaining predicate-specific hotspot is mostly the `NOT IN` path, not ordinary scalar `ORDER BY id` reads
- `0.93.1` also closes the long-term visibility gap around tiny-query drift by adding permanent canonical `LIMIT 1` rows to the SQL PocketIC audit:
  - `user.pk.key_only.asc.limit1`
    - `SELECT id FROM PerfAuditUser ORDER BY id ASC LIMIT 1`
  - `user.pk.order_only.asc.limit1`
    - `SELECT id, name FROM PerfAuditUser ORDER BY id ASC LIMIT 1`
  - `repeat.user.pk.order_only.asc.limit1.runs10`
    - `SELECT id, name FROM PerfAuditUser ORDER BY id ASC LIMIT 1`
- those new rows are now seeded into the checked-in SQL audit baseline fixture, so future runs will report direct drift instead of `N/A`:
  - `user.pk.key_only.asc.limit1`: `compile=86,169`, `execute=391,813`, `total=477,982`
  - `user.pk.order_only.asc.limit1`: `compile=97,897`, `execute=410,324`, `total=508,221`
  - `repeat.user.pk.order_only.asc.limit1.runs10`: `compile=23,556`, `execute=286,395`, `total=309,952`
- with those rows in place, the current tiny-query read for the canonical projected PK path is simple:
  - cold `SELECT id, name FROM PerfAuditUser ORDER BY id ASC LIMIT 1`: `508,221`
  - repeat `...runs10`: `309,952`
  - nearby existing `LIMIT 2` rows remain materially below their older checked-in baselines:
    - `user.pk.order_only.asc.limit2`: `744,682 -> 550,573` (`-26.06%`)
    - `repeat.user.pk.order_only.asc.limit2.runs10`: `459,307 -> 353,450` (`-23.04%`)
- focused verification for `0.93.1` stayed on the shared SQL floor and its reporting surfaces:
  - `cargo fmt --all`
  - `cargo clippy -p icydb-core --all-targets -- -D warnings`
  - `cargo test -p icydb-core db::session::tests::sql_surface -- --nocapture`
  - `POCKET_IC_BIN=/home/adam/projects/icydb/.cache/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_perf_audit sql_perf_shared_floor_queries_report_phase_breakdown -- --nocapture`
  - `POCKET_IC_BIN=/home/adam/projects/icydb/.cache/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_perf_audit sql_perf_audit_harness_reports_instruction_samples -- --nocapture`

---

## 0.93.0

- `0.93.0` starts the shared execution cost-reduction line after the `0.92` cache cleanup, with the first concrete cut aimed at redundant prepared-plan rebuilds around the shared lower boundary.
- the first shipped cleanup in this line removes a few obvious “prepared plan -> compiled wrapper -> prepared plan again” hops:
  - typed delete count execution in `db/session/query.rs` now takes the cached `PreparedExecutionPlan<E>` directly instead of rebuilding it through `CompiledQuery<E>`
  - typed SQL delete projection execution in `db/session/sql/execute/write.rs` now does the same
  - fluent explain helpers for `bytes_by(field)` and prepared projection terminals in `db/query/intent/query.rs` now build one `PreparedExecutionPlan<E>` directly for the requested visibility lane instead of routing through `plan_with_visible_indexes(...).into_prepared_execution_plan()`
  - `trace_query(...)` now reads explain/hash/access-family metadata from the cached prepared plan directly instead of combining a compiled-query build with a second prepared-plan lookup
- the same slice also trims the larger typed-wrapper duplication that was still sitting on top of the shared cache:
  - `CompiledQuery<E>` and `PlannedQuery<E>` can now reuse the shared prepared-plan artifact directly through an internal `QueryPlanHandle`
  - session-owned `compile_query_with_visible_indexes(...)` and `planned_query_with_visible_indexes(...)` no longer clone logical-plan state back out of the shared prepared plan just to wrap it as another typed shell
  - direct builder-owned `Query::plan()` / `Query::planned()` behavior stays intact, so the public fluent planning surface still looks the same while the session-owned hot path gets cheaper
- the prepared-plan constructors were also doing one duplicate piece of work on every build:
  - `SharedPreparedExecutionPlan::from_plan(...)` and `PreparedExecutionPlan<E>::build(...)` both froze the planner-owned static execution shape before calling the shared prepared-plan core builder
  - `build_prepared_execution_plan_core(...)` already performs that same freeze internally
  - `0.93.0` removes the outer duplicate pass and keeps one canonical static-shape freeze in the shared builder
- the later `0.93.0` follow-through trims two more shared-floor costs that were still hidden behind otherwise-correct APIs:
  - the session-owned `compile_query_with_visible_indexes(...)` / `planned_query_with_visible_indexes(...)` path was still cloning `SharedPreparedExecutionPlan` twice, once when it came out of the shared cache and again when the typed wrapper constructors borrowed and recloned it; those typed wrapper constructors now take ownership directly on the shared-cache path
  - `PreparedExecutionPlanCore::into_inner()` and aggregate streaming extraction were still cloning the full shared prepared-plan payload even when the core was uniquely owned; that boundary now uses `Arc::try_unwrap(...)` to move the canonical plan/spec payload out directly and falls back to cloning only when another wrapper still holds the shared core
- that means the current `0.93.0` shared-floor model is tighter in three places:
  - fewer rebuilds from shared prepared plan back into typed wrappers
  - fewer duplicate constructor/freeze passes during prepared-plan assembly
  - fewer whole-plan / whole-spec clones when execution consumes a uniquely owned prepared core
- the first benchmark readout for this line is already positive on both surfaces that meet at the shared floor:
  - fluent sampled rows improved again versus their checked-in baselines, including `user.age.order_only.asc.limit3` (`-22.34%`), `repeat.user.age.order_only.asc.limit3.runs10` (`-38.43%`), and `repeat.user.grouped.age_count.limit10.runs100` (`-32.47%`)
  - SQL sampled rows also improved again versus their checked-in baselines, including `repeat.user.pk.order_only.asc.limit2.runs10` (`-22.56%`), `repeat.user.name.lower.order_only.asc.limit3.runs10` (`-51.78%`), and `repeat.user.grouped.age_count.limit10.runs100` (`-15.73%`)
  - the simplified `0.92` cache contract is unchanged during these `0.93.0` cuts: SQL repeat rows still show compiled-command cache hits plus shared-query-plan-cache hits, and fluent repeat rows still reuse only the shared lower prepared-plan cache
- the later `0.93.0` SQL membership follow-through made the remaining compile-side outlier measurable instead of speculative:
  - SQL query attribution now exposes compile subphases for cache-key construction, cache lookup, parsing, aggregate-lane classification, prepared-statement normalization, lowering, structural binding, and cache insertion
  - the focused `IN` / `NOT IN` PocketIC audit showed that the extra compile budget was not a `NOT IN`-specific runtime regression; it was mostly parser + prepare + lower work, plus a fixed aggregate-lane classification cost on plain scalar `SELECT`s
  - that follow-through also adds one cheap parsed-shape fast path before the dedicated global-aggregate lane, so scalar `SELECT`s with no `HAVING` and no aggregate projection items stop paying the heavier aggregate-lane analysis on the compile path
- the next `0.93.0` follow-through hard-cuts the remaining plain membership compile waste on the scalar SQL path:
  - `prepare_select_statement(...)` now short-circuits the already-local scalar `SELECT` family, so field-list projections with local predicates and bare field `ORDER BY` terms no longer rebuild the parsed statement just to discover there is nothing to re-scope or alias-normalize
  - `compile_where_bool_expr_to_predicate(...)` now collapses top-level canonical `OR(eq...)` and `AND(ne...)` chains directly onto one runtime `IN` / `NOT IN` compare instead of expanding those shapes through the generic boolean truth-set compiler and leaving the membership collapse to later predicate normalization
  - together, those two follow-through cuts moved the focused membership compile path materially:
    - `user.age.in.limit3` compile dropped from `253,802` after the aggregate-lane fast path to `199,037`, and total cost dropped to `1,442,231` (`-16.33%` vs the checked-in baseline)
    - `user.age.not_in.limit3` compile dropped from `249,269` after the aggregate-lane fast path to `201,292`, and total cost dropped to `952,839` (`-21.64%` vs the checked-in baseline)
  - the focused subphase probe now shows the remaining membership compile budget concentrated mostly in parser work, not in aggregate-lane classification or SQL statement normalization:
    - `parse` remains about `79k` for `IN` and `85k` for `NOT IN`
    - `prepare` is now about `22.5k`
    - `lower` is now about `38.9k`
    - `bind` is now about `19.2k` for `IN` and `15.8k` for `NOT IN`
- the final `0.93.0` membership follow-through moves the last large SQL-side cost off the parser/lowering front door instead of leaving the fast path to repair it later:
  - the parser now owns a direct `SqlExpr::Membership` node for `IN` / `NOT IN` instead of immediately expanding membership into `OR(eq...)` or `AND(ne...)` boolean chains
  - statement alias normalization, aggregate-call discovery, general SQL-expression lowering, and `WHERE` predicate lowering all understand that narrower membership node directly, so the membership path no longer has to allocate and walk the larger boolean expression tree before collapsing back to one runtime compare
  - that parser-side cut materially reduced the remaining compile buckets on the focused membership probe:
    - `user.age.in.limit3` moved from `compile=199,037`, `parse=79,625`, `prepare=22,508`, `lower=38,864`, `total=1,442,231`
      to `compile=153,371`, `parse=67,907`, `prepare=14,801`, `lower=14,104`, `total=1,400,677`
    - `user.age.not_in.limit3` moved from `compile=201,292`, `parse=85,420`, `prepare=22,664`, `lower=38,864`, `total=952,839`
      to `compile=152,445`, `parse=72,978`, `prepare=13,827`, `lower=14,275`, `total=903,949`
  - relative to the checked-in SQL perf baseline, the final `0.93.0` membership rows now land at:
    - `user.age.in.limit3`: `1,723,754 -> 1,400,677` (`-18.74%`)
    - `user.age.not_in.limit3`: `1,216,013 -> 903,949` (`-25.66%`)
- this is intentionally a narrow first slice:
  - no cache model changes
  - no new SQL-specific logic
  - no new runtime owner
- focused verification for the first `0.93.0` cut stayed on the shared execution and session surfaces that changed:
  - `cargo test -p icydb-core db::session::tests::sql_surface -- --nocapture`
  - `cargo test -p icydb-core db::query::intent::tests::explain -- --nocapture`
- the later follow-through also stayed green on:
  - `cargo test -p icydb-core db::session::tests::query_lowering -- --nocapture`
  - `cargo test -p icydb-core db::sql::lowering::normalize::tests -- --nocapture`
  - `cargo test -p icydb-core db::sql::lowering::predicate::compile::tests -- --nocapture`
  - `cargo test -p icydb-core db::sql::parser::tests::parse_select_statement_with_in_trailing_comma_predicate -- --nocapture`
  - `POCKET_IC_BIN=/home/adam/projects/icydb/.cache/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test fluent_perf_audit fluent_perf_audit_harness_reports_instruction_samples -- --nocapture`
  - `POCKET_IC_BIN=/home/adam/projects/icydb/.cache/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_perf_audit sql_perf_audit_harness_reports_instruction_samples -- --nocapture`
  - `POCKET_IC_BIN=/home/adam/projects/icydb/.cache/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_perf_audit sql_perf_membership_queries_report_compile_subphase_breakdown -- --nocapture`
- the next expected work in `0.93.0` is still the larger wrapper seam:
  - tightening `SharedPreparedExecutionPlan`
  - reducing duplicate lower-boundary inspection in `db/session/query.rs`
  - trimming typed wrapper and logical-plan handoff cost for fluent and SQL equivalents

---
