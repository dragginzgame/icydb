# 0.154.x

## 0.154.6

`0.154.6` adds the developer-facing diagnostic report for the supported
field-path index-add runner path.

### Changed

- Added a stable schema-mutation diagnostic report shape for the supported
  non-unique field-path index add, carrying phase, mutation kind, entity path,
  target index, target store, target fields, scanned rows, written keys,
  validation status, and publish status.
- Wired the report through field-path runner success and failure paths, and
  included the same diagnostic summary in startup reconciliation errors for
  target-index preflight rejection, runner failure, and unexpected
  non-publishable physical work.

### Validation

- `cargo fmt --all`
- `cargo test -p icydb-core field_path_runner_orchestrates_staging_to_publication_handoff --features sql -- --nocapture`
- `cargo test -p icydb-core field_path_runner_rolls_back_staged_writes_after_isolated_validation_failure --features sql -- --nocapture`
- `cargo test -p icydb-core reconcile_runtime_schemas_executes_supported_field_path_index_addition --features sql -- --nocapture`
- `cargo test -p icydb-core schema::mutation --features sql -- --nocapture`
- `cargo test -p icydb-core schema::reconcile --features sql -- --nocapture`
- `cargo check -p icydb-core --tests --features sql`
- `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`
- `git diff --check`

## 0.154.5

`0.154.5` wires the scoped field-path validation path into the runner and
startup reconciliation.

### Changed

- Changed the field-path runner to validate and publish physical work against
  the target index id instead of the whole physical `IndexStore`, while still
  rejecting extra pre-existing entries for the target index.
- Relaxed startup reconciliation from requiring an empty physical index store
  to requiring an empty target index. Unrelated physical index entries can now
  coexist with the supported non-unique field-path index-add path.

### Validation

- `cargo fmt --all`
- `cargo test -p icydb-core field_path_runner_orchestrates_handoff_with_unrelated_index_entries --features sql -- --nocapture`
- `cargo test -p icydb-core reconcile_runtime_schemas_accepts_field_path_index_addition_with_unrelated_index_entries --features sql -- --nocapture`
- `cargo test -p icydb-core reconcile_runtime_schemas_rejects_field_path_index_addition_with_populated_target_index --features sql -- --nocapture`
- `cargo test -p icydb-core schema::mutation --features sql -- --nocapture`
- `cargo test -p icydb-core schema::reconcile --features sql -- --nocapture`
- `cargo check -p icydb-core --tests --features sql`
- `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`

## 0.154.4

`0.154.4` starts the scoped multi-index validation path needed before runtime
startup can safely add one field-path index into a physical store that already
contains unrelated index entries.

### Changed

- Added target-index-scoped validation for isolated field-path index-store
  writes. The scoped check decodes physical index keys, counts only entries for
  the intended target index, and still verifies every staged target entry by
  raw key and raw entry bytes.
- Added matching target-index-scoped physical store publication checks, keeping
  unrelated physical entries from tripping the final ready-state count check
  while still rejecting extra target-index entries.
- Kept the existing whole-store validation path unchanged for current runner
  callers, so runtime startup publication is not widened in this slice.

### Validation

- `cargo fmt --all`
- `cargo test -p icydb-core field_path_rebuild_isolated_index_store_validation_can_scope_to_target_index --features sql -- --nocapture`
- `cargo test -p icydb-core field_path_rebuild_isolated_index_store_validation_rejects_extra_target_entries --features sql -- --nocapture`
- `cargo test -p icydb-core field_path_rebuild_published_store_can_scope_to_target_index --features sql -- --nocapture`
- `cargo test -p icydb-core field_path_rebuild_published_store_scope_rejects_extra_target_entries --features sql -- --nocapture`
- `cargo test -p icydb-core schema::mutation --features sql -- --nocapture`
- `cargo check -p icydb-core --tests --features sql`
- `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`
- `git diff --check`

## 0.154.3

`0.154.3` makes the supported startup schema-runner path more diagnosable
before widening physical index-store publication.

### Changed

- Added a startup index-store preflight for the supported non-unique field-path
  index-add path. The preflight decodes existing physical index keys and
  classifies store occupancy as target-index entries versus unrelated-index
  entries before the runner executes.
- Kept populated physical index stores fail-closed until scoped multi-index
  publication validation exists, but now reports target, unrelated, and total
  entry counts at the startup boundary.
- Removed the remaining active legacy Makefile setup references and refreshed
  the current wasm footprint audit instruction to use `icp-built` and
  `icp-shrunk` artifact names.

### Validation

- `cargo fmt --all`
- `cargo test -p icydb-core field_path_startup_index_store_preflight_classifies_target_and_other_entries --features sql -- --nocapture`
- `cargo test -p icydb-core reconcile_runtime_schemas_rejects_field_path_index_addition_with_populated_index_store --features sql -- --nocapture`
- `cargo test -p icydb-core schema::reconcile --features sql -- --nocapture`
- `cargo check -p icydb-core --tests --features sql`
- `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`
- `cargo test -p icydb-core --features sql`
- `git diff --check`

## 0.154.2

`0.154.2` trims the startup reconciliation duplication introduced by the first
schema-runner execution path without widening physical mutation publication.

### Changed

- Consolidated the accepted-snapshot reconciliation tail shared by
  metadata-only schema-store reconciliation and runtime-store startup
  reconciliation, keeping runtime physical execution isolated to the runtime
  path while sharing reconcile metrics, schema-store footprint recording,
  accepted snapshot validation, and accepted footprint recording.

### Validation

- `cargo fmt --all`
- `cargo test -p icydb-core ensure_accepted_schema_snapshot_rejects_field_path_index_addition_without_runtime_store --features sql -- --nocapture`
- `cargo test -p icydb-core schema::reconcile --features sql -- --nocapture`
- `cargo check -p icydb-core --tests --features sql`
- `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`
- `git diff --check`

## 0.154.1

`0.154.1` completes the narrow database-default encoder slice needed by
generated schema metadata after literal construction defaults became persisted
database defaults, hard-cuts unsigned runtime/persisted metadata naming to the
`Nat*` vocabulary, and wires the first supported schema-runner startup
execution path.

### Fixed

- Every primitive field can now encode database-default payloads through the
  existing persisted field-codec lanes instead of relying on a derive-local
  allowlist with missing primitive coverage.
- Decimal fields now encode database-default payloads through the existing
  by-kind Decimal field codec instead of being rejected by derive-time
  validation.
- Int, Nat, Int128, and Nat128 fields now encode database-default integer
  literals through their existing by-kind integer codecs, including unsuffixed
  128-bit literals that exceed the 64-bit range.
- Date, Duration, and Timestamp fields now encode database-default literals
  through their existing scalar codecs; numeric literals use raw day or
  millisecond units, while string literals use the runtime type parsers.
- Account, Principal, Subaccount, Ulid, Blob, and Unit fields now support
  explicit database defaults through the same by-kind codecs used by normal
  persisted row values.
- Optional single-value fields now accept non-null literal database defaults,
  so missing accepted slots materialize through the existing
  `DefaultIfMissing` absence policy instead of failing derive validation.
- Invalid Decimal text defaults still fail during schema macro validation.

### Runtime Vocabulary

- Unsigned numeric runtime and persisted metadata names now use `Nat*`
  consistently across generated schema primitives, public runtime types, field
  kinds, persisted field kinds, value variants, scalar codecs, and wire labels.

### Startup Execution

- Wired runtime schema reconciliation to execute the supported non-unique
  field-path index-add path on startup by scanning accepted rows, decoding them
  through accepted row contracts, running the field-path index runner, checking
  publication readiness, and publishing the accepted snapshot only after the
  physical runner succeeds.
- Kept metadata-only schema-store callers fail-closed for physical mutation
  work, so only runtime reconciliation owns startup physical execution.
- Required the physical index store to be empty before startup execution can
  write the rebuilt index entries; existing multi-index physical stores stay
  rejected until scoped publication validation exists.

### Validation

- `cargo test -p icydb-schema-derive database_default_`
- `cargo test -p icydb-schema-derive`
- `cargo clippy -p icydb-schema-derive --all-targets -- -D warnings`
- `cargo fmt --all`
- `cargo test -p icydb-core reconcile_runtime_schemas_executes_supported_field_path_index_addition --features sql -- --nocapture`
- `cargo test -p icydb-core reconcile_runtime_schemas_rejects_field_path_index_addition_with_populated_index_store --features sql -- --nocapture`
- `cargo test -p icydb-core schema::reconcile --features sql -- --nocapture`
- `cargo test -p icydb-core schema::mutation --features sql -- --nocapture`
- `cargo test -p icydb-core --test write_boundary_guards schema_mutation_publication_boundary_uses_runner_preflight --features sql -- --nocapture`
- `cargo check -p icydb-core --tests --features sql`
- `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`
- `cargo test -p icydb-core --features sql`
- `git diff --check`

## 0.154.0

`0.154.0` starts the developer-supported accepted-schema mutation path by
adding a fail-closed admission guard for the only physical mutation shape in
scope: one non-unique field-path secondary index add.

### Schema Mutation Admission

- Added a supported execution-path contract that accepts only the ordered
  field-path rebuild shape: build one field-path index, validate physical work,
  then invalidate runtime state.
- Added a plan-level supported-path guard so mixed catalog changes cannot pass
  just because their physical execution steps look valid.
- Recognized exactly one added non-unique field-path index as an explicit
  accepted snapshot delta while keeping multiple index additions, unique
  indexes, expression indexes, and reordered/changed prior indexes
  incompatible.
- Routed reconciliation diagnostics through the supported-path admission guard
  so the future startup runner path is distinct from generic unsupported
  physical mutation work.
- Kept expression-index rebuilds, secondary-index drops, metadata-only plans,
  full rewrites, unsupported mutations, unique index targets, empty field-path
  targets, and malformed execution plans rejected before supported runner
  wiring can consume them.

### Validation

- `cargo fmt --all`
- `cargo test -p icydb-core supported_developer_physical_path_admits_only_single_field_path_index_add --features sql -- --nocapture`
- `cargo test -p icydb-core snapshot_delta_ --features sql -- --nocapture`
- `cargo test -p icydb-core schema::mutation --features sql -- --nocapture`
- `cargo test -p icydb-core --test write_boundary_guards schema_mutation_publication_boundary_uses_runner_preflight --features sql -- --nocapture`
- `cargo check -p icydb-core --tests --features sql`
- `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`
- `git diff --check`
