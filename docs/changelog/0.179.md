# IcyDB 0.179 Changelog

## 0.179.2

### 📝 Summary

- Continues the repeated-audit cleanup line with a broad owner-local module
  split and lint-suppression tightening pass.
- Splits CLI command, config, ICP, observability, schema-check, and test
  domains into focused modules while preserving the existing command surface.
- Splits the core metrics sink event taxonomy into an owner-local child module
  while preserving metrics sink and reporting behavior.
- Converts fulfilled lint suppressions to checked expectations, leaving
  generated-code emitters and intentionally unfulfilled staged-contract
  suppressions as `allow`.

### 🧹 Cleanup

- Moves CLI config initialization, discovery, reporting, endpoint gates, clap
  command groups, ICP fixture refresh helpers, ICP manifest parsing,
  snapshot/schema report rendering, metrics report rendering, schema-check
  recommendations, schema-check field/index comparison, and CLI test domains
  behind owner-local modules.
- Keeps the public CLI command behavior stable while reducing root CLI and
  observability hub pressure.
- Moves metrics sink event classification into a dedicated metrics-sink child
  module without changing the public metrics sink/report surface.
- Consolidates schema DDL field-metadata validation around a named
  metadata-change kind, removing closure-heavy helper wiring while preserving
  accepted-after drift checks.
- Consolidates relation scalar target descriptors and save-time strong relation
  target validation through owner-local helpers without changing
  accepted-schema relation checks.
- Narrows relation reverse-index metadata helper visibility to its owner module
  while preserving commit and delete-validation reverse-index surfaces.
- Narrows relation facade error constructors, delete-validator hook types, and
  reverse-index target-key construction to their owner-local modules.
- Removes a stale relation metadata dead-code suppression now that schema
  describe owns the production consumer surface.

### 🧪 Testing

- Keeps formatting, whitespace, focused core/library Clippy, integration-test
  Clippy, audit-canister Clippy, macro-test Clippy, and CLI binary Clippy
  validation green for the cleanup slice.

## 0.179.1

### 📝 Summary

- Records the 2026-06-05 module-structure audit and follows through on its
  highest-pressure cleanup targets.
- Records the 2026-06-05 complexity-accretion audit with current branch,
  variant, flow, semantic-spread, and issue-ledger artifacts.
- Tightens CLI argument metadata while keeping command behavior stable.
- Narrows internal value, decimal, and field-kind classifier surfaces without
  changing crate-visible capability contracts.

### 🧹 Cleanup

- Splits SQL DDL publication/execution helpers out of schema reconciliation so
  startup reconciliation and SQL DDL publication pressure remain owner-local.
- Splits SQL DDL field-metadata publication validation out of the SQL DDL
  reconciliation child, keeping field metadata checks separate from
  index/publication envelope execution.
- Splits SQL DDL statement parsing out of the root SQL statement parser,
  reducing root statement parser branch pressure while preserving parser
  behavior and diagnostics.
- Moves schema transition admission identity/version-fingerprint gating into a
  transition child module, narrowing the transition planning root without
  changing admission-matrix behavior.
- Moves generated-extension transition compatibility policy into a transition
  child module, dropping the transition root below the branch-hotspot threshold.
- Moves range access-choice per-key constraint classification into a range
  evaluator child module, dropping the range evaluator root below the
  branch-hotspot threshold.
- Moves persisted schema index and relation integrity checks into schema
  integrity child modules, dropping the schema integrity root below the
  branch-hotspot threshold.
- Moves SQL metadata result shaping out of the SQL execution root into a
  focused execution child module.
- Moves relation reverse-index target-key storage into a reverse-index child
  module without merging relation validation or execution ownership.
- Moves the CLI `clap` dependency to the workspace, adds CLI value hints/names,
  and flattens config-init endpoint flags into a dedicated derive argument
  group.
- Narrows duration microsecond/nanosecond truncation helpers to the parent type
  module while keeping timestamp conversion behavior intact.
- Narrows internal decimal arithmetic, value hash-contract, map, semantic,
  text-helper, wire-decode, and field-kind classifier details while keeping
  public and crate-visible capability surfaces intact.

### 📊 Audit

- Tightens the module-structure audit preflight and hub-threshold guard for
  current schema, relation, and SQL execution hubs.
- Refreshes the module-structure runtime metrics artifact after cleanup and
  adds a threshold row for the new SQL DDL reconciliation child module.
- Completes the CA-1.4 complexity-accretion audit with required artifacts,
  reporting a moderate `4.8/10` complexity risk index, `5,712` branch sites,
  `14` branch hotspots, and `0` fanout hotspots.
- Refreshes the post-cleanup complexity metrics after SQL parser, SQL DDL
  reconciliation, schema transition, range access-choice evaluator, and schema
  integrity splits, reducing branch hotspots from `14` to `9`.
- Tracks follow-up pressure around SQL statement parsing, metrics reporting,
  schema transition planning, and the schema-owned SQL DDL reconciliation
  child.

### 🧪 Testing

- Keeps focused SQL, DDL, relation, write-boundary guard, module-structure
  threshold, compile, clippy, and whitespace validation green for the cleanup
  slice.

## 0.179.0

### 📝 Summary

- Starts the repeated-audit cleanup line with a fresh DRY consolidation audit
  over `icydb-core`.
- Applies the audit's first owner-local cleanup by centralizing repeated SQL
  DDL publication envelope wiring inside schema reconciliation.
- Follows through on the remaining optional DRY targets by sharing relation
  tuple-edge validation facts and tightening SQL result/default-cache shells.
- Keeps SQL DDL admission, mutation compatibility, physical runner admission,
  and identity-checked publication behavior unchanged.

### 🧹 Cleanup

- Adds a local schema-owned SQL DDL publication envelope helper for the common
  accepted-before snapshot, accepted-after snapshot, publication gate, and
  identity-checked publication wiring.
- Leaves operation-specific target checks, metadata drift validation, physical
  cleanup validation, and runner-backed index publication paths explicit at
  their existing call sites.
- Keeps the existing schema-store publication helper as the only accepted
  snapshot write path for SQL DDL execution.
- Adds a relation-owned tuple-edge descriptor that validates accepted
  relation-edge arity and component kind facts once before save validation and
  reverse-index preparation map those facts into their own execution structs.
- Routes the remaining compiled SQL default-cache result shells through the
  existing local helper instead of open-coding default cache attribution.

### 📊 Audit

- Records the 2026-06-04 DRY consolidation audit and TSV artifacts.
- Classifies the remaining duplication pressure as moderate, with no
  high-risk divergence-prone pattern found.
- Closes the audit's optional relation tuple-edge and SQL result shell cleanup
  targets without merging authority or execution boundaries.

### 🧪 Testing

- Keeps focused DDL, publication, mutation, write-boundary guard, and
  module-structure threshold validation green.
