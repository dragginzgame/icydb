# 0.46 Changelog

## 0.46.13 - 2026-03-09 - Version Sequence Alignment

### Summary

`0.46.13` is a metadata-only patch that finalizes `0.46.x` version-sequence
alignment after a release-script error.

### Changed

- No runtime, planner, executor, or schema behavior changed.
- Changelog/version sequence was aligned so the release line now correctly
  reflects `0.46.13` as latest.

### Validation

- none (metadata-only patch)

---

## 0.46.12 - 2026-03-09 - Version Sequence Alignment

### Summary

`0.46.12` is a metadata-only patch created during release-script correction.

### Changed

- No runtime, planner, executor, or schema behavior changed.
- Patch increment recorded to preserve release-sequence continuity.

### Validation

- none (metadata-only patch)

---

## 0.46.11 - 2026-03-09 - Temporal First/Last Projection Boundary Locks

### Summary

`0.46.11` extends temporal boundary coverage to scalar first/last projection
terminals so temporal values remain semantic at runtime boundaries.

### Changed

- Added temporal scalar projection boundary regression coverage:
  - `first_value_by("occurred_on")` / `last_value_by("occurred_on")` now lock
    `Value::Date` output typing.
  - `first_value_by("occurred_at")` / `last_value_by("occurred_at")` now lock
    `Value::Timestamp` output typing.
  - `first_value_by("elapsed")` / `last_value_by("elapsed")` now lock
    `Value::Duration` output typing.
- Added temporal id/value projection boundary regression coverage:
  - `values_by_with_ids("occurred_on")` now locks `(Id<E>, Value::Date)`.
  - `values_by_with_ids("occurred_at")` now locks
    `(Id<E>, Value::Timestamp)`.
  - `values_by_with_ids("elapsed")` now locks `(Id<E>, Value::Duration)`.
- Added deterministic first/last parity locks for those temporal projection
  terminals under an ordered scalar window.
- Added deterministic id/value pair ordering parity locks for temporal
  `values_by_with_ids(...)` under an ordered scalar window.
- Added ranked temporal projection boundary regression coverage:
  - `top_k_by_values(...)` / `bottom_k_by_values(...)` now lock semantic
    `Value::Date`, `Value::Timestamp`, and `Value::Duration` typing.
  - `top_k_by_with_ids(...)` / `bottom_k_by_with_ids(...)` now lock typed
    id/value temporal projection pairs.
- Added deterministic ranked-ordering parity locks for temporal ranked
  projection terminals under one scalar window.
- Added ranked temporal row-terminal boundary regression coverage:
  - `top_k_by(...)` / `bottom_k_by(...)` now lock semantic
    `Date`/`Timestamp`/`Duration` view typing on returned entity rows.
- Added deterministic ranked-row ordering parity locks for temporal row
  terminals under one scalar window.
- Added verbose explain parity locks for temporal ranked-order query shapes
  (`ORDER BY temporal_field, id LIMIT k`) that back `top_k_by`/`bottom_k_by`
  semantics, freezing key route/descriptor/plan diagnostic fields between
  equivalent top-like and bottom-like forms.
- Added a verbose explain diagnostics snapshot lock for one temporal ranked
  top-like shape, freezing full diagnostic-line ordering and values as part of
  the explain stability contract.
- Added scalar terminal explain parity locks for temporal ordered windows:
  `explain_first()` / `explain_last()` now lock shared route/query/execution
  shape fields and descriptor metadata parity, while preserving terminal-specific
  node labels (`AggregateFirst` vs `AggregateLast`).
- Updated `0.46` design tracking to include temporal first/last projection
  boundary coverage alongside existing `views()`, `values_by(...)`,
  `values_by_with_ids(...)`, ranked temporal projections and row terminals,
  explain parity locks for temporal ranked-order shapes and
  `explain_first()`/`explain_last()` temporal shape parity,
  `distinct_values_by(...)`, and grouped-key temporal locks.
- Closed the `0.46` standards-alignment design matrix by marking all listed
  tracks complete and recording line closeout status in the open-notes section.

### Validation

- `cargo fmt --all`
- `cargo test -p icydb-core session_load_temporal_first_last_projection_values_preserve_semantic_types -- --nocapture`
- `cargo test -p icydb-core session_load_temporal_values_with_ids_preserve_semantic_types -- --nocapture`
- `cargo test -p icydb-core session_load_temporal_ranked_projection_values_preserve_semantic_types -- --nocapture`
- `cargo test -p icydb-core session_load_temporal_ranked_row_terminals_preserve_semantic_types -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_temporal_ranked_order_shape_parity -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_diagnostics_snapshot_for_temporal_ranked_shape -- --nocapture`
- `cargo test -p icydb-core session_load_terminal_explain_first_last_preserve_temporal_order_shape_parity -- --nocapture`
- `cargo clippy -p icydb-core --tests -- -D warnings`

---

## 0.46.10 - 2026-03-09 - Temporal Grouped and Distinct Boundary Locks

### Summary

`0.46.10` extends temporal runtime boundary coverage into grouped and distinct
scalar projection outputs so temporal values remain semantic at API boundaries.

### Changed

- Added grouped temporal boundary regression coverage in session matrix tests:
  - `group_by("occurred_on")` keys must remain `Value::Date`.
  - `group_by("occurred_at")` keys must remain `Value::Timestamp`.
  - `group_by("elapsed")` keys must remain `Value::Duration`.
- Added temporal distinct projection boundary regression coverage:
  - `distinct_values_by("occurred_on")` must return `Value::Date`.
  - `distinct_values_by("occurred_at")` must return `Value::Timestamp`.
  - `distinct_values_by("elapsed")` must return `Value::Duration`.
- Added first-observed ordering parity locks for temporal distinct projections
  under deterministic ordered windows.
- Updated `0.46` standards-alignment design tracking with grouped and distinct
  temporal projection boundary coverage.

### Validation

- `cargo fmt --all`
- `cargo test -p icydb-core session_load_temporal_grouped_keys_preserve_semantic_types -- --nocapture`
- `cargo test -p icydb-core session_load_temporal_distinct_projection_values_preserve_semantic_types -- --nocapture`

---

## 0.46.9 - 2026-03-09 - Prefix-Like Fallback and Compile Parity Hardening

### Summary

`0.46.9` hardens prefix-like predicate parity by tightening strict
index-predicate `starts_with` compile contracts and making verbose fallback
reason paths more specific for text operators.

### Changed

- Expanded strict index-predicate `starts_with` compile coverage:
  - non-empty prefixes now lock to bounded range compare pairs
    (`>= prefix`, `< upper(prefix)`).
  - high-unicode prefixes now lock surrogate-gap-safe upper-bound derivation
    (`...D7FF` -> `...E000`).
  - max-unicode prefixes now lock to lower-bound-only form (`>= prefix`).
  - strict-all-or-none compile policy now explicitly accepts these prefix-like
    lowerings.
- Expanded verbose predicate pushdown fallback reason specificity:
  - text operators that remain on safe full-scan fallback now report
    `fallback(text_operator_full_scan)` for `TextContains`, `TextContainsCi`,
    and strict `EndsWith`.
  - collection-element `Contains` remains on
    `fallback(non_strict_compare_coercion)` and is explicitly excluded from
    text-operator fallback classification.
  - non-strict `EndsWith` (for example casefold coercion) also remains on
    `fallback(non_strict_compare_coercion)` by precedence.
- Added explain/runtime parity locks for prefix-like equivalence:
  - `starts_with(prefix)` vs equivalent bounded-range predicates.
  - max-unicode `starts_with(prefix)` vs equivalent lower-bound-only range
    predicates.
  - indexed strict prefix-like shapes now lock route-stage and descriptor
    parity (`index_prefilter(strict_all_or_none)`,
    `has_index_predicate_prefilter=true`,
    `has_residual_predicate_filter=false`).
- Updated `0.46` standards-alignment design tracking with these compile and
  fallback-precedence parity locks.

### Validation

- `cargo fmt --all`
- `cargo test -p icydb-core compile_index_program_starts_with_compiles_to_bounded_range_compare_pair -- --nocapture`
- `cargo test -p icydb-core compile_index_program_starts_with_high_unicode_skips_surrogate_gap_upper_bound -- --nocapture`
- `cargo test -p icydb-core compile_index_program_starts_with_max_unicode_compiles_to_lower_bound_only -- --nocapture`
- `cargo test -p icydb-core compile_index_program_strict_mode_accepts_starts_with_bounded_prefix -- --nocapture`
- `cargo test -p icydb-core compile_index_program_strict_mode_accepts_starts_with_max_unicode_prefix -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_equivalent_prefix_like_route_stage_parity -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_indexed_prefix_like_strict_prefilter_stage -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_max_unicode_prefix_like_parity -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_reports_text_operator_full_scan_fallback -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_keeps_collection_contains_on_generic_full_scan_fallback -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_non_strict_ends_with_uses_non_strict_fallback_precedence -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_text_operator_fallback_reason_path -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_keeps_collection_contains_on_generic_full_scan_fallback -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_non_strict_ends_with_uses_non_strict_fallback_precedence -- --nocapture`
- `cargo clippy -p icydb-core --lib -- -D warnings`

---

## 0.46.8 - 2026-03-09 - Standards Alignment Continuation

### Summary

`0.46.8` continues standards-alignment with terminal API parity, temporal
return-shape locks, and predicate pushdown/diagnostic stability expansion.

### Changed

- Aligned scalar emptiness/existence terminals across core and facade:
  - `db::query::fluent::load::FluentLoadQuery::not_exists()`
  - `is_empty()` now routes through this alias boundary.
- Added explicit `EXPLAIN` alias parity for NOT EXISTS:
  - `FluentLoadQuery::explain_not_exists()` now aliases
    `FluentLoadQuery::explain_exists()`.
  - regression locks require identical explain descriptors for both.
- Expanded aggregate/session terminal parity coverage:
  - parity assertions now include `exists()`, `not_exists()`, and `is_empty()`
    on the same scalar window.
  - added a scan-budget lock that asserts all three terminals preserve the same
    early-stop contract (`offset + 1` keys scanned on non-empty ordered windows).
- Updated `icydb` facade load/delete terminal wrappers:
  - `is_empty()` and `count()` now delegate directly to core terminal helpers
    instead of materializing through `execute()`.
  - facade scalar load queries now expose `exists()` and `not_exists()`
    convenience terminals.
- Extended temporal boundary coverage:
  - added `Date` semantic-view boundary lock (`Date::as_view()` /
    `Date::from_view(...)`).
  - added `Date` serde wire-shape lock (`"YYYY-MM-DD"`).
  - added one temporal session matrix lock over runtime boundaries:
    `Date`/`Timestamp`/`Duration` remain typed in both `views()` payloads and
    `values_by(...)` scalar projection paths.
- Expanded predicate pushdown contracts and stability:
  - planner now lowers impossible `primary_key IS NULL` directly to
    `AccessPath::ByKeys([])`.
  - `AccessPlan::intersection(...)` treats explicit empty `ByKeys([])` as an
    annihilator; `AccessPlan::union(...)` treats explicit empty `ByKeys([])` as
    an identity when non-empty branches exist.
  - verbose execution explain now emits
    `diagnostic.plan.predicate_pushdown=...` labels (`none`, `applied(...)`,
    `fallback(...)`) with explicit reason paths:
    `fallback(is_null_full_scan)`,
    `fallback(non_strict_compare_coercion)`, and
    `fallback(starts_with_empty_prefix)`.
  - text operators that remain on safe full-scan fallback now report one
    explicit reason label:
    `fallback(text_operator_full_scan)` for `TextContains`,
    `TextContainsCi`, and strict `EndsWith`.
  - collection-element `Contains` predicates remain on
    `fallback(non_strict_compare_coercion)` and are explicitly excluded from
    text-operator fallback classification.
  - non-strict text operators preserve existing precedence:
    casefolded `EndsWith` reports `fallback(non_strict_compare_coercion)` and
    is not reclassified as text-operator fallback.
  - added parity locks requiring equivalent predicate classes to keep reason and
    route-stage diagnostics aligned:
    - `primary_key IS NULL` vs strict `primary_key IN []`
    - canonical-equivalent `IN` sets
    - equal-bounds `BETWEEN`-style predicates vs strict equality
    - prefix-like `starts_with(prefix)` vs equivalent bounded-range predicates
  - strict index-predicate compilation now lowers non-empty
    `starts_with(prefix)` into bounded range compares (`>= prefix` and
    `< upper(prefix)`) so prefix-like and equivalent bounded-range shapes keep
    route predicate-stage parity; indexed strict prefix-like shapes now lock to
    `diagnostic.route.predicate_stage=index_prefilter(strict_all_or_none)` with
    explicit descriptor-node parity (`has_index_predicate_prefilter=true`,
    `has_residual_predicate_filter=false`).
  - strict-all-or-none compile policy now explicitly accepts these
    prefix-lowered shapes (bounded pair and max-unicode lower-bound-only), so
    strict route prefilter eligibility is locked at the compile boundary.
  - high-unicode bounded-prefix lowering now locks surrogate-gap handling:
    strict `starts_with` compile upper bounds skip scalar surrogate space
    (`...D7FF` -> `...E000`) to remain parity-safe with planner range lowering.
  - max-unicode prefix-like forms (`starts_with(char::MAX)`) now lock to the
    equivalent lower-bound-only range form (`>= prefix`) for both compile shape
    and verbose route diagnostics.
  - non-primary `IS NULL` remains on safe full-scan fallback until nullable
    index pushdown contracts are introduced.
- Updated `0.46` design tracking with these additional locks and behavior notes.

### Validation

- `cargo fmt --all`
- `cargo test -p icydb-core session_load_terminal_explain_not_exists_alias_matches_exists_plan -- --nocapture`
- `cargo test -p icydb-core session_load_aggregate_terminals_match_execute -- --nocapture`
- `cargo test -p icydb-core session_load_exists_not_exists_and_is_empty_share_early_stop_scan_budget -- --nocapture`
- `cargo test -p icydb-core test_as_view_roundtrip_preserves_semantic_date_type -- --nocapture`
- `cargo test -p icydb-core test_serde_boundary_uses_iso_string_not_raw_day_count -- --nocapture`
- `cargo test -p icydb-core session_load_temporal_views_and_projection_values_preserve_semantic_types -- --nocapture`
- `cargo test -p icydb-core intersection_constructor_explicit_empty_annihilates_children -- --nocapture`
- `cargo test -p icydb-core intersection_constructor_nested_explicit_empty_annihilates_children -- --nocapture`
- `cargo test -p icydb-core union_constructor_explicit_empty_is_identity_for_non_empty_children -- --nocapture`
- `cargo test -p icydb-core union_constructor_only_explicit_empty_children_stays_explicit_empty -- --nocapture`
- `cargo test -p icydb-core normalize_intersection_with_explicit_empty_collapses_to_empty -- --nocapture`
- `cargo test -p icydb-core normalize_union_with_explicit_empty_collapses_to_non_empty_branch -- --nocapture`
- `cargo test -p icydb-core normalize_union_only_explicit_empty_children_stays_empty -- --nocapture`
- `cargo test -p icydb-core plan_access_primary_key_is_null_lowers_to_empty_by_keys -- --nocapture`
- `cargo test -p icydb-core plan_access_secondary_is_null_retains_full_scan_fallback -- --nocapture`
- `cargo test -p icydb-core plan_access_secondary_in_empty_in_and_group_collapses_to_empty_by_keys -- --nocapture`
- `cargo test -p icydb-core plan_access_primary_key_is_null_or_secondary_eq_collapses_to_secondary_branch -- --nocapture`
- `cargo test -p icydb-core plan_access_primary_key_is_null_or_primary_key_is_null_stays_empty -- --nocapture`
- `cargo test -p icydb-core plan_access_stability_starts_with_and_equivalent_range_share_identical_access_plan -- --nocapture`
- `cargo test -p icydb-core plan_access_stability_max_unicode_starts_with_and_equivalent_lower_bound_share_plan -- --nocapture`
- `cargo test -p icydb-core plan_access_stability_single_field_between_equal_bounds_and_eq_share_identical_access_plan -- --nocapture`
- `cargo test -p icydb-core session_load_primary_key_is_null_lowers_to_empty_access_without_scan -- --nocapture`
- `cargo test -p icydb-core session_load_primary_key_is_null_or_id_eq_matches_id_eq_branch_parity -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_diagnostics_snapshot_for_top_n_seek_shape -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_diagnostics_snapshot_for_index_range_pushdown_shape -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_diagnostics_snapshot_for_rejection_shape -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_is_null_predicate_pushdown_reason_paths -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_non_strict_predicate_fallback_reason_path -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_empty_prefix_starts_with_fallback_reason_path -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_text_operator_fallback_reason_path -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_keeps_collection_contains_on_generic_full_scan_fallback -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_non_strict_ends_with_uses_non_strict_fallback_precedence -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_equivalent_empty_contract_reason_paths -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_equivalent_empty_contract_route_stage_parity -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_equivalent_in_set_route_stage_parity -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_equivalent_between_and_eq_parity -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_equivalent_prefix_like_route_stage_parity -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_indexed_prefix_like_strict_prefilter_stage -- --nocapture`
- `cargo test -p icydb-core query_explain_execution_verbose_reports_max_unicode_prefix_like_parity -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_prefix_like_and_equivalent_range_share_label -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_distinguishes_is_null_and_non_strict_full_scan_fallbacks -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_distinguishes_empty_prefix_starts_with_full_scan_fallback -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_reports_text_operator_full_scan_fallback -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_keeps_collection_contains_on_generic_full_scan_fallback -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_non_strict_ends_with_uses_non_strict_fallback_precedence -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_reports_none_when_no_predicate_is_present -- --nocapture`
- `cargo test -p icydb-core predicate_pushdown_label_reports_empty_access_contract_for_impossible_shapes -- --nocapture`
- `cargo test -p icydb-core compile_index_program_starts_with_compiles_to_bounded_range_compare_pair -- --nocapture`
- `cargo test -p icydb-core compile_index_program_starts_with_rejects_empty_prefix -- --nocapture`
- `cargo test -p icydb-core compile_index_program_starts_with_high_unicode_skips_surrogate_gap_upper_bound -- --nocapture`
- `cargo test -p icydb-core compile_index_program_starts_with_max_unicode_compiles_to_lower_bound_only -- --nocapture`
- `cargo test -p icydb-core compile_index_program_strict_mode_accepts_starts_with_bounded_prefix -- --nocapture`
- `cargo test -p icydb-core compile_index_program_strict_mode_accepts_starts_with_max_unicode_prefix -- --nocapture`
- `cargo check -p icydb`

---

## 0.46.7 - 2026-03-08 - Boundary Naming and Visibility Tightening

### Summary

`0.46.7` tightens executor/internal visibility surfaces, removes one remaining
ambiguous query-facing error type name, and updates the layer-authority metric
to measure duplicated decision ownership rather than repeated accessor tokens.

### Changed

- Updated layer-authority complexity auditing:
  - `scripts/ci/check-layer-authority-invariants.sh` now tracks
    cross-layer predicate duplication using decision-owner function declarations
    (for example `fn ...`) instead of field/accessor token reuse.
  - this removes false-positive duplication drift from passive contract
    propagation and keeps the metric focused on policy re-derivation risk.
  - added explicit complexity-accretion baselines for decision-owner counts:
    - `AccessPath decision owners`
    - `RouteShape decision owners`
    - `Predicate coercion owners`
  - CI now fails when those counts drift above baseline, forcing intentional
    ownership review at the boundary where complexity expands.
- Tightened remaining executor core visibility from `pub(crate)` to
  `pub(in crate::db)` where ownership is db-internal:
  - `ExecutorPlanError`
  - `Context` (and db-internal context helpers)
  - `DeleteExecutor`
  - `ExecutablePlan`
  - aligned caller method visibility in `db/mod.rs` and `db/session.rs` to
    preserve private-interface correctness under `-D warnings`.
- Renamed query intent execution error taxonomy for boundary clarity:
  - `db::query::intent::ExecutionError` -> `QueryExecutionError`
  - updated root re-exports and downstream `icydb` facade mapping accordingly.
- Consolidated route-shape ownership in staged route planning:
  - `RouteIntentStage` now carries `route_shape_kind` as intent-owned state.
  - execution-stage derivation consumes this staged value directly instead of
    re-deriving shape from grouped/aggregate flags.
  - reduced route-shape decision-owner count baseline from `6` to `5`.

### Validation

- `cargo fmt --all`
- `bash scripts/ci/check-layer-authority-invariants.sh`
- `make clippy`
- `cargo test -p icydb-core`

---

## 0.46.6 - 2026-03-08 - Planner Stability Authority Consolidation

### Summary

`0.46.6` centralizes query-plan stability helpers under one planner-owned
module and applies explicit `IN` literal canonicalization before access-shape
construction to reduce stability logic drift.

### Changed

- Added a dedicated planner stability helper module:
  - `db/query/plan/stability.rs`
  - owns canonical `IN (...)` literal normalization (`sort + dedup`)
  - owns planner -> access canonical normalization handoff via
    `normalize_planned_access_plan_for_stability(...)`
- Updated planner compare lowering to consume stability-owned `IN` canonicalization:
  - primary-key strict `IN` now builds from canonicalized key sets
  - secondary strict `IN` multi-lookup planning now receives canonicalized
    literal sets before index compatibility filtering
- Updated planner orchestration to route final access-plan normalization through
  the new stability helper instead of directly calling access canonicalization.
- Renamed route-level execution mode enum for boundary clarity:
  - `db::executor::route::ExecutionMode` -> `RouteExecutionMode`
  - updated route planner/runtime consumers and route matrix assertions to use
    the route-owned name and avoid ambiguity with access execution-contract
    `ExecutionMode`
- Added planner stability helper unit coverage:
  - canonical `IN` set normalization is order/duplicate-insensitive
  - planner stability normalization remains idempotent

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-core`
- `make clippy`
- `cargo test -p icydb-core db::query::plan::stability::tests -- --nocapture`
- `cargo test -p icydb-core db::query::plan::tests -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::route::aggregate_matrix -- --nocapture`

---

## 0.46.5 - 2026-03-08 - Numeric Streaming Fold Hardening and Top-N Guard Tightening

### Summary

`0.46.5` extends scalar numeric aggregate streaming coverage for safe shapes,
adds regression locks for canonical deduped key-set windows and duplicate-risk
fan-out fallbacks, and makes Top-N hint derivation enforce deterministic
ordered contracts directly.

### Changed

- Expanded numeric field aggregate streaming fold (`sum_by`, `avg_by`) under
  conservative eligibility:
  - no residual predicate
  - no `DISTINCT`
  - single-path-safe access kinds only
  - ordered paged windows allowed only when `ORDER BY` is exactly primary key
    (ASC or DESC) on PK-safe shapes
- Added one explicit numeric streaming optimization marker:
  - `ExecutionOptimizationCounter::NumericFieldStreamingFoldFastPath`
- Added numeric streaming fold regression coverage for:
  - hit-path activation on unpaged and PK-ordered paged windows
  - hit-path activation on key-direct (`by_id`) windows, including paged
    `ORDER BY id + LIMIT` shape
  - hit-path activation on canonical deduped key-set (`by_ids`) windows
  - fail-closed fallback on non-PK ordered paged windows and predicate-bearing
    shapes
  - fail-closed fallback on index multi-lookup fan-out shapes (explicit
    `IndexMultiLookup` access-shape lock)
  - bounded scan-budget behavior for PK-ordered paged windows (`offset + limit`)
- Tightened route Top-N hint derivation:
  - `top_n_seek_spec` now explicitly re-checks
    `secondary_order_contract_is_deterministic(...)` before deriving hints
  - non-deterministic ordered shapes fail closed directly at the Top-N hint
    boundary even if indirect capability gates change later
- Expanded planner stability regression coverage for canonical `IN` semantics:
  - secondary-index `IN` predicates now lock permutation/duplicate-value
    canonicalization to one identical index multi-lookup access plan
  - primary-key `IN` predicates now lock permutation/duplicate-value
    canonicalization to one identical by-keys access plan
- Updated `0.46` design tracking with the added numeric fail-closed coverage
  and explicit Top-N deterministic guard status.

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-core`
- `cargo test -p icydb-core db::executor::tests::aggregate::core_contract_matrix -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::route::load_matrix -- --nocapture`
- `cargo test -p icydb-core load_secondary_order_top_n_seek_trace_optimization_is_explicit -- --nocapture`
- `cargo test -p icydb-core load_secondary_order_trace_reports_non_top_n_variant_without_page_limit -- --nocapture`
- `cargo test -p icydb-core`

---

## 0.46.4 - 2026-03-08 - Deterministic Ordered Pushdown Guarding

### Summary

`0.46.4` hardens ordered pushdown eligibility and continues planner/runtime
contract cleanup by moving one remaining window projection policy to explicit
planner accessors.

### Changed

- Hardened ordered pushdown contract gates:
  - route pushdown derivation now uses one shared planner helper,
    `secondary_order_contract_is_deterministic`, before deriving secondary
    order applicability.
  - index-range `LIMIT` pushdown shape eligibility now requires the same
    deterministic order contract.
  - mixed direction and missing-primary-key tie-break ordered shapes now
    fail closed before Top-N or bounded index-range hint derivation.
- Extended route-matrix regression coverage for ordered index-range shapes:
  - added explicit rejection tests for:
    - missing PK tie-break (`ORDER BY rank`)
    - mixed directions (`ORDER BY rank ASC, id DESC`)
  - incompatible ordered shapes now assert both:
    - no index-range limit pushdown spec
    - no Top-N seek spec
- Simplified scalar planner-to-route window projection API:
  - removed `AccessWindowLookaheadPolicy`.
  - `ScalarAccessWindowPlan` now exposes explicit `keep_count()` and
    `fetch_count()` accessors.
  - route continuation projection consumes those explicit projections directly.
- Improved strict `IN ()` lowering behavior in planner compare planning:
  - non-primary-key strict `IN []` now lowers to `AccessPlan::by_keys(Vec::new())`
    instead of a full-scan fallback.
  - added planner regression coverage that locks this constant-empty lowering.
- Updated `0.46` design tracking with:
  - deterministic ordered-contract guard status
  - index-range `LIMIT` eligibility guard status
  - `IN []` constant-empty lowering status
  - explicit keep/fetch access-window projection status

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-core`
- `cargo test -p icydb-core db::executor::tests::route::load_matrix -- --nocapture`
- `cargo test -p icydb-core db::query::plan::tests::pushdown -- --nocapture`
- `cargo test -p icydb-core db::query::plan::planner::tests -- --nocapture`

---

## 0.46.3 - 2026-03-08 - Executor Boundary Hardening and Route Lint Cleanup

### Summary

`0.46.3` continues `0.46` cleanup by tightening executor visibility boundaries,
locking those boundaries with structural tests, and removing remaining route
planner lint regressions.

### Changed

- Tightened executor boundary visibility for key internal contracts:
  - `LoadExecutor` and `SaveExecutor` are now `pub(in crate::db)`.
  - `ExecutorError` and ordered key-stream internals remain scoped to
    `pub(in crate::db::executor)`.
  - `CursorPage` remains `pub(in crate::db)` because session-level routing reads
    page fields directly.
- Expanded load-structure guard coverage:
  - added regression checks that fail if executor-only key/window/error types
    or executor facade types are widened back to `pub(crate)`.
- Completed executor window contract cleanup:
  - removed legacy `needs_extra` toggling from executor window helpers in favor
    of explicit keep/fetch projections.
  - post-access pagination now consumes shared keep-count math.
- Resolved route planner clippy regressions under `-D warnings`:
  - made `index_range_limit_spec_for_execution_mode` a `const fn`.
  - removed `if !... { None } else { ... }` patterns in feasibility derivation.

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-core`
- `cargo test -p icydb-core --test compile_fail -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::load_structure -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::stream_key -- --nocapture`
- `cargo test -p icydb-core db::query::plan::tests::structural_guards -- --nocapture`

---

## 0.46.2 - 2026-03-08 - Window and Covering Contract Consolidation

### Summary

`0.46.2` continues `0.46` contract cleanup by removing duplicate pagination and
covering-eligibility logic across planner, route, explain, and aggregate
projection paths, and by tightening executor-internal visibility boundaries.

### Changed

- Consolidated scalar window ownership under planner contracts:
  - route window derivation now consumes planner-projected
    `ScalarAccessWindowPlan` directly.
  - executor-side duplicate offset/limit keep-window reconstruction was removed.
- Preserved DISTINCT+offset bounded-probe safety after window refactor:
  - bounded aggregate probe-hint suppression now keys to logical DISTINCT
    intent (`scalar_plan().distinct`) so DISTINCT+offset windows remain
    unbounded where required.
- Expanded covering optimization reuse for `count_distinct_by(field)`:
  - `count_distinct_by` now routes through `distinct_values_by` projection
    paths, enabling existing index-covering and constant-covering fast paths
    for eligible shapes.
- Added planner-owned covering eligibility and order contracts:
  - new `query/plan/covering.rs` now owns:
    - index-covering existing-row terminal eligibility
    - covering projection context/order derivation
    - adjacent-distinct eligibility checks for covering projections
  - route terminal selection and EXPLAIN covering-projection reporting now
    consume the shared planner helper instead of duplicating local checks.
  - aggregate covering projection runtime now consumes planner-owned covering
    order/context contracts via a thin adapter.
- Removed executor-local window toggle branching:
  - `executor/window` now exposes explicit keep/fetch projections without the
    legacy `needs_extra` toggle contract.
  - post-access pagination now consumes one shared keep-count helper.
- Tightened executor-only visibility boundaries and locked them with structural
  guards:
  - narrowed ordered key stream traits/types, key comparator, merge/intersect
    stream adapters, and `ExecutorError` to `pub(in crate::db::executor)`.
  - narrowed `LoadExecutor`/`SaveExecutor` to `pub(in crate::db)`.
  - kept `CursorPage` at `pub(in crate::db)` because `db/session` consumes page
    fields directly.
  - added load-structure guard coverage preventing key/window/error executor
    internals from widening back to `pub(crate)`.

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-core`
- `cargo test -p icydb-core db::executor::route::contracts::tests -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::route::load_matrix -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::route::aggregate_matrix -- --nocapture`
- `cargo test -p icydb-core db::query::plan::covering::tests -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::aggregate::field_projection_matrix -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::aggregate::secondary_index_matrix -- --nocapture`
- `cargo test -p icydb-core --test compile_fail -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::load_structure -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::stream_key -- --nocapture`

---

## 0.46.1 - 2026-03-08 - Constant-Row Terminal and EXISTS Early-Stop Coverage

### Summary

`0.46.1` adds a lightweight constant scalar terminal and locks one additional
EXISTS short-circuit contract in the aggregate parity matrix.

### Changed

- Added one session-level constant scalar terminal:
  - `DbSession::select_one()` now returns `Value::Int(1)` directly.
  - This path bypasses query planning, routing, and access-layer execution for
    projection-only constant use cases.
- Added regression coverage for the constant terminal execution boundary:
  - `session_select_one_returns_constant_without_execution_metrics` asserts the
    terminal returns `1` and emits no execution metrics.
- Expanded EXISTS early-stop scan-budget coverage:
  - `aggregate_exists_full_scan_window_scans_offset_plus_one` now locks the
    full-scan offset-window contract to `offset + 1` scanned keys.
- Updated `0.46` design tracking to mark `SELECT 1` and EXISTS tracks as
  actively in progress.

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-core`
- `cargo test -p icydb-core session_select_one_returns_constant_without_execution_metrics -- --nocapture`
- `cargo test -p icydb-core aggregate_exists_full_scan_window_scans_offset_plus_one -- --nocapture`

---

## 0.46.0 - 2026-03-08 - Standards-Alignment Kickoff and Early-Exit Planning

### Summary

`0.46.0` starts the standards-alignment line by landing the first
planner/executor early-exit slices and the supporting stability contracts.

The patch keeps query behavior unchanged while reducing unnecessary work for
zero-row and contradiction shapes.

### Changed

- Added planner-level `LIMIT 0` lowering:
  - `db/query/plan/limit_zero.rs`
  - intent planning now emits an empty by-keys execution shape before access
    routing when the effective fetch window is zero.
- Added planner-level constant predicate folding:
  - `db/query/plan/constant_predicate.rs`
  - canonical constant-true predicates are removed from logical planning input
  - canonical constant-false predicates lower to empty execution shapes.
- Added empty-window aggregate short-circuiting in executor aggregate terminals:
  - shared helpers in `db/executor/aggregate/mod.rs`
  - terminal/numeric aggregate execution now returns SQL-correct zero-window
    outputs without entering scan loops when the window is provably empty.
- Extended predicate normalization with conservative contradiction/tightening
  simplification for `AND` compare clauses:
  - conflicting equality/bound combinations now fold to `FALSE`
  - compatible bound combinations tighten to stricter canonical forms.
- Added plan-stability regression coverage for equivalent canonical predicate
  shapes:
  - equivalent forms are locked to identical access-plan shape
  - contradictory forms are locked to the same constant-false/empty shape.
- Added and updated design/status tracking for the `0.46` line:
  - `docs/design/0.46-standards-alignment/0.46-design.md`

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-core`
- `cargo test -p icydb-core query::intent::tests -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::aggregate::core_contract_matrix -- --nocapture`
- `cargo test -p icydb-core db::executor::tests::aggregate::path_parity_matrix -- --nocapture`
- `cargo test -p icydb-core db::predicate::normalize::tests -- --nocapture`
- `cargo test -p icydb-core db::query::plan::tests -- --nocapture`
