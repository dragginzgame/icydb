# 0.146.x

## 0.146.13

`0.146.13` fixes the final prepared-plan handoff for accepted-schema slot
authority. Cached/session planning can now freeze static execution metadata
with the selected accepted `SchemaInfo`, and executor-core lowering preserves
that frozen shape instead of re-finalizing it through the generated-model
fallback. Metrics now also report schema-store footprint gauges so operators
can see the observed persisted schema snapshot count, total encoded bytes, and
latest-snapshot bytes per entity and in aggregate, plus prepared-shape
finalization outcome counters that distinguish preserved schema-selected
shapes from generated-model fallback finalization. Accepted-schema field fact
handoffs now also keep kind, row-layout slot, and nested-leaf metadata grouped
inside `AcceptedSchemaSnapshot` so `SchemaInfo` consumes one schema-owned fact
surface instead of reassembling those facts from separate lookups.

### Prepared execution handoff

- Added an explicit finalized-static-shape check on `AccessPlannedQuery`.
- Changed `EntityAuthority::finalize_static_planning_shape` so it only applies
  the generated fallback when a plan has not already been finalized.
- Added an executor authority regression test proving accepted-schema direct
  projection slots survive authority finalization.
- Added prepared-shape finalization outcome metrics so executor lowering
  reports when a plan arrived already finalized versus when it used the
  generated-model fallback.

### Schema-store footprint diagnostics

- Added raw schema-store footprint measurement that counts encoded snapshot
  payload bytes without decoding the payloads or exposing schema metadata.
- Added global `EventOps` and per-entity `EntitySummary` gauges for schema
  snapshot count, total encoded schema bytes, and latest-snapshot encoded
  bytes.
- Routed reconciliation through the footprint recorder after successful
  first-create writes, exact-match accepts, rejected drift, corrupt latest
  snapshots, and failed store writes.

### Accepted-schema field facts

- Added one accepted-snapshot field fact accessor that returns persisted kind,
  row-layout slot, and nested leaf descriptors together.
- Updated accepted `SchemaInfo` construction to consume that grouped fact
  handoff instead of asking the accepted snapshot for kind, slot, and nested
  metadata separately.
- Updated accepted-schema DESCRIBE field formatting to consume the same grouped
  fact handoff when rendering top-level slot, kind, and nested leaf metadata.
- Removed now-unused accepted-snapshot slot-only and nested-leaf-only accessors
  so production consumers use the grouped fact surface for multi-fact reads.
- Added a snapshot regression test proving the grouped field fact handoff keeps
  row-layout slot authority when duplicated field-snapshot slot metadata
  differs.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core db::executor::authority --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::schema::snapshot --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core metrics:: --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.12

`0.146.12` expands runtime cache metrics from coarse hit/miss/insert totals
into low-cardinality miss reason buckets. Shared query-plan cache misses now
distinguish cold cache, cache-method version drift, accepted-schema
fingerprint drift, planner visibility drift, and unrelated distinct keys. SQL
compiled-command cache misses now distinguish cold cache, cache-method version
drift, accepted-schema fingerprint drift, query/update surface drift, and
unrelated distinct keys. Prepared execution metadata now also freezes field
and field-path root slots from the selected `SchemaInfo`, so accepted schema
row-layout authority feeds scalar projection, projection reachability,
direct-projection retained slots, duplicate-preserving data-row direct
projection slots, residual-filter, order-expression, grouped aggregate
expression programs, and prepared index predicate slot maps.
Route metrics now also count selected non-index and primary-key route reasons
so full scans, empty access short-circuits, primary-key access, and composite
non-index plans can be distinguished without parsing explain output. SQL
compile metrics now count rejections by broad admission phase so
cache-key/schema failures, parse failures, and semantic compile failures are
visible before execution.

### Cache miss diagnostics

- Added `CacheMissReason` as the stable metrics taxonomy for cache miss
  diagnostics without exposing SQL text, structural query identities, or
  schema fingerprint values.
- Added global `EventOps` counters for shared query-plan and SQL
  compiled-command miss reason buckets.
- Added per-entity `EntitySummary` counters for the same miss reason buckets
  so one entity's cache churn can be found without inspecting only aggregate
  totals.
- Classified shared query-plan misses by comparing a missed key against
  warmed entries for the same structural query identity, schema fingerprint,
  visibility, and cache-method version dimensions.
- Classified SQL compiled-command misses by comparing a missed key against
  warmed entries for the same SQL text, entity path, accepted-schema
  fingerprint, query/update surface, and cache-method version dimensions.

### Route choice diagnostics

- Added `PlanChoiceReason` as the stable metrics taxonomy for selected
  non-index and primary-key route reasons.
- Added global `EventOps` and per-entity `EntitySummary` counters for route
  choices such as full-scan fallback, constant-false predicates, limit-zero
  windows, explicit key-access overrides, primary-key lookup/range access, and
  composite non-index access.
- Routed execution-time plan metrics through `AccessPlannedQuery` so counters
  can use the planner-owned access-choice snapshot instead of re-deriving
  route reasons from only the final access shape.

### SQL compile rejection diagnostics

- Added `SqlCompileRejectPhase` as the stable metrics taxonomy for SQL
  admission failures without exposing SQL text, parser diagnostics, or lowered
  query details.
- Added global `EventOps` and per-entity `EntitySummary` counters for total
  SQL compile rejects plus cache-key/schema, parse, and semantic compile
  buckets.
- Counted cache-key/schema context failures before cache lookup, parse failures
  after a cache miss, and semantic compile failures from the core parsed-SQL
  compiler.

### Accepted slot authority

- Added schema-aware scalar projection compilation helpers that resolve direct
  field and field-path root slots through `SchemaInfo`.
- Routed finalized scalar projection programs, expression residual filters,
  expression order terms, grouped aggregate input/filter expressions, and
  grouped DISTINCT target slots through the selected schema view.
- Routed projection referenced-slot metadata and direct retained-slot
  projection layouts through the selected schema view.
- Routed raw data-row direct projection layouts through duplicate-preserving
  planner-frozen slot metadata instead of executor-local generated model
  lookup.
- Routed prepared index predicate slot maps and index compile targets through
  the selected schema view instead of generated model slot lookup.
- Kept generated-model scalar compilation as the explicit fallback for
  generated-only callers and tests that do not have an accepted schema view.
- Added scalar expression and projection metadata tests proving compiled
  direct-field and field-path roots use accepted row-layout slots instead of
  generated field order.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core db::query::plan::expr::projection --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::query::plan::expr::scalar --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core metrics:: --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::query::plan::group --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.11

`0.146.11` seals accepted schema construction. Runtime reconciliation now
wraps persisted snapshots through `AcceptedSchemaSnapshot::try_new(...)`, which
applies the schema-owned integrity rules before metadata can become accepted
live schema authority. The unchecked constructor remains test-only for
owner-local fixtures that deliberately prove accessor authority on malformed
metadata. Runtime metrics now also count schema reconciliation outcomes by
entity and globally, distinguishing first-create writes, exact-match accepts,
latest-snapshot decode failures, store-write failures, and rejected drift
classes.

### Accepted schema boundary

- Added `AcceptedSchemaSnapshot::try_new(...)` as the production constructor
  for accepted persisted schema metadata.
- Routed startup reconciliation through that fallible accepted-schema
  constructor after transition policy accepts a snapshot.
- Restricted the unchecked accepted-schema constructor to tests.
- Added accepted-schema coverage proving invalid metadata is rejected at the
  accepted-wrapper boundary.

### Metrics observability

- Added global and per-entity schema reconciliation counters for total checks,
  first-create writes, exact-match accepts, corrupt latest snapshots,
  store-write failures, schema-version rejections, row-layout rejections,
  field-slot rejections, and other rejected schema drift.
- Classified transition rejections with a schema-owned low-cardinality taxonomy
  so metrics do not parse or expose detailed diagnostic text.
- Routed reconciliation instrumentation through the schema reconciliation
  boundary instead of the schema store or codec helpers.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core metrics:: --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::schema::reconcile --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.10

`0.146.10` tightens startup schema reconciliation so the schema store cannot
silently ignore a newer persisted schema snapshot for an entity. Reconciliation
now asks `SchemaStore` for the latest stored version for the entity and
compares that accepted candidate against the generated proposal. The schema
metadata boundary also rejects internally inconsistent typed writes and raw
decoded payloads before they can become accepted metadata. The policy is still
fail-closed and exact-match-only; this change only closes the version-selection
and metadata-integrity boundaries before real schema transitions exist.

### Schema reconciliation

- Added `SchemaStore::latest_persisted_snapshot(...)` to scan schema metadata
  entries for one entity and decode the highest stored version.
- Routed startup reconciliation through that latest-version accessor instead
  of directly loading only `SchemaVersion::initial()`.
- Added schema-store coverage proving latest-version lookup ignores other
  entity tags and selects the highest matching version.
- Added schema-store coverage proving latest-version lookup fails closed on a
  corrupt newest payload instead of falling back to an older decodable
  snapshot.
- Added schema-store coverage proving typed snapshots cannot be persisted when
  their snapshot version and row-layout version diverge.
- Added schema-codec coverage proving raw decoded snapshots fail closed when
  the snapshot version and row-layout version diverge.
- Added typed-write and raw-decode coverage proving persisted field slot
  metadata cannot diverge from the row-layout slot mapping.
- Added typed-write and raw-decode coverage proving schema snapshots cannot
  reference a primary-key field that is absent from the row-layout or field
  metadata.
- Added coverage for ambiguous schema metadata, including duplicate
  row-layout slots and duplicate persisted field names.
- Added coverage for invalid nested leaf metadata, including empty nested
  paths and duplicate nested paths under the same top-level field.
- Added reconciliation coverage proving a newer persisted schema version is
  rejected instead of being ignored or overwritten by a recreated initial
  snapshot.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.9

`0.146.9` starts the live row-layout authority handoff without changing row
format or enabling schema migrations. Accepted schema slot lookup now reads
the persisted `SchemaRowLayout` mapping instead of the duplicated slot value on
the field snapshot. Under the current exact-match transition policy those
values are identical, but the accessor boundary now reflects the intended
authority before runtime decoding can move off generated layout metadata.

### Row-layout authority

- Added `SchemaRowLayout::slot_for_field(...)` so schema-owned layout metadata
  can answer durable-field-ID to physical-slot questions directly.
- Routed accepted top-level field slot lookup through `SchemaRowLayout` instead
  of `PersistedFieldSnapshot::slot`.
- Carried schema-owned slot indexes into accepted-schema `SchemaInfo`, giving
  grouped structural validation a schema-owned slot validity surface instead
  of checking directly against generated `EntityModel` slots.
- Removed the generated-model parameter from the grouped structural validation
  sub-boundary now that field-slot validation is schema-owned there.
- Added regression coverage with an intentionally inconsistent owner-local
  accepted snapshot fixture to prove slot answers come from row-layout
  authority.
- Extended accepted-schema `DESCRIBE` coverage so metadata output also proves
  top-level slots come from the row-layout mapping.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::query::plan::validate --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.8

`0.146.8` keeps the next schema-default boundary explicit while leaving the
only current database default as `None`. Derive-generated field metadata now
passes a `FieldDatabaseDefault` contract into `FieldModel`, so Rust construction
defaults cannot accidentally become persistence defaults when database-level
defaults are added later.

Startup schema reconciliation also now routes through an explicit
transition-policy decision. The accepted policy is still exact initial-schema
equality only: first contact writes the generated snapshot, exact matches are
accepted, and every mismatch remains fail-closed with the existing first
difference diagnostic. The change gives later migration work a single
schema-owned decision point to extend instead of letting transition policy hide
inside equality checks or diagnostic formatting. It also tightens the accepted
schema snapshot boundary so metadata, planning, and SQL write paths ask the
accepted wrapper for field facts instead of reaching through to raw field
snapshots.

Runtime metrics now count rejected SQL writes by statement kind and error
class, expose derived Rust helper methods for common read/write efficiency
ratios without changing the Candid report shape, and guard the metrics sink
event/taxonomy ordering with `remain` so future counter families stay aligned
across global and per-entity report updates.

### Schema reconciliation

- Added a dedicated `db::schema::transition` owner for transition decisions,
  with `SchemaTransitionDecision` as the policy result between a persisted
  accepted snapshot and the generated schema proposal.
- Added `SchemaTransitionRejection` so transition diagnostics stay separate
  from the final `InternalError` formatting boundary.
- Narrowed the raw accepted-snapshot accessor to the schema module so
  non-schema callers keep using accepted-schema helper methods instead of
  reopening the persisted payload directly.
- Added accepted-schema field fact accessors for primary-key kind, top-level
  field kind, top-level slot, and nested leaf metadata.
- Routed SQL write literal normalization, accepted-schema `SchemaInfo`
  construction, and accepted-schema `DESCRIBE` projection through those
  accepted-wrapper accessors.
- Renamed the session accepted-schema helper to make its first-contact
  reconciliation side effect explicit: it ensures the initial snapshot before
  returning it, rather than acting as a pure load.
- Kept the only accepted decision as `ExactMatch`; all mismatches still return
  the existing unsupported schema-evolution error with the first rejected
  schema difference.
- Added regression coverage proving the transition policy accepts exact
  snapshots, rejects changed snapshots, and reports row-layout drift before
  runtime row-layout authority work begins.

### Schema authority boundary

- Routed derive-generated field metadata through an explicit
  `FieldDatabaseDefault` contract while keeping the only current database
  default as `None`.
- Exposed `FieldDatabaseDefault` through the public `icydb::model::field`
  generated-code boundary so derive output compiles through the facade crate.
- Added macro-test coverage proving fields with Rust construction defaults still
  emit `FieldDatabaseDefault::None` for the database schema contract.

### Metrics report shape

- Added global and per-entity SQL write error counters by command kind and
  `ErrorClass`, covering unsupported statements, conflicts, not-found errors,
  internal errors, invariant violations, corruption, and incompatible persisted
  formats.
- Added non-persisted helper methods for load selectivity, load filter ratio,
  SQL write mutation ratio, SQL `RETURNING` ratio, index entries per write row,
  reverse-index entries per write row, and relation checks per write row.
- Kept derived ratios out of the Candid report shape so `icydb_metrics()`
  remains a raw-total report and consumers can distinguish missing denominators
  from real zero-valued work.
- Added `remain` ordering guards to the metrics sink event and taxonomy enums,
  plus the single-kind mirror helpers, so future sink counter families stay
  mechanically aligned across global and per-entity report updates.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core metrics:: --features sql -- --nocapture`.
- Ran `cargo test -p icydb-schema-tests rust_construction_defaults_do_not_become_database_defaults -- --nocapture`.
- Ran `cargo test -p icydb-core execute_sql_statement_write_error_metrics_capture_command_shape_and_class -- --nocapture`.
- Ran `cargo check -p icydb-schema-derive --tests`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.7

`0.146.7` records and implements the next accepted-schema boundary for nested
field metadata. Persisted schema snapshots now carry nested leaf descriptors
emitted by the compiled schema proposal, reconciliation compares those nested
descriptors under the current exact-match policy, and `SchemaInfo` can resolve
field-path expression types and SQL projection capabilities from accepted
nested leaves. Runtime row decoding, mutation slot mapping, and nested path
execution still stay on generated layout contracts until transition policy and
accepted live row-layout authority exist. The infallible Rust metadata helpers
remain generated-model views, while the existing fallible metadata helpers are
the accepted-schema surfaces for SQL and diagnostics. Runtime metrics now also
record the SQL write command shape, matched rows, mutated rows, and `RETURNING`
rows so dashboards can distinguish statement-level write efficiency from
lower-level executor save/delete totals.

### Schema authority boundary

- Marked `show_columns` / `describe_entity` as generated-model metadata
  surfaces and `try_show_columns` / `try_describe_entity` as accepted-schema
  surfaces.
- Updated the 0.146 status document so schema-owned SQL capabilities are
  recorded as wired into top-level SQL admission.
- Added a live row-layout boundary audit covering the runtime paths that must
  keep using generated row layout until transition policy exists.
- Added a nested leaf metadata design for moving field-path type inference and
  SQL field-path admission to accepted persisted schema.
- Added persisted nested leaf descriptors to schema snapshots and the schema
  snapshot codec.
- Routed field-path expression type inference through accepted nested leaf
  metadata when the accepted snapshot provides it.
- Routed accepted-schema `DESCRIBE` / `SHOW COLUMNS` nested rows through
  persisted nested leaf metadata when available.
- Routed SQL SELECT field-path projection admission through accepted nested
  leaf SQL capabilities when available.

### Metrics report shape

- Added global and per-entity SQL write command counters for `INSERT`,
  `INSERT ... SELECT`, `UPDATE`, and `DELETE`.
- Added global and per-entity SQL write matched-row, mutated-row, and
  `RETURNING` row totals.
- Routed SQL write metrics from the session SQL execution boundary so they
  describe statement-level write shape without replacing the lower-level
  executor save/delete and write-amplification counters.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core metrics:: -- --nocapture`.
- Ran `cargo test -p icydb-core execute_sql_statement_write_metrics_capture_sql_boundary_shape -- --nocapture`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::query::plan::expr::type_inference --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::sql::lowering::tests::bind_sql_select_with_schema_rejects_non_selectable_accepted_nested_leaf --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.6

`0.146.6` routes SQL admission checks that can safely use top-level accepted
schema metadata through schema-owned SQL capabilities. `SchemaInfo` now carries
the accepted persisted field capability projection beside accepted field types,
and SQL SELECT binding checks direct projection, `SELECT *`, `GROUP BY`, and
direct `ORDER BY` admission from that view. SQL DELETE and UPDATE selector
tails share the same direct-ordering gate, and SQL global aggregate
field-target binding also consumes the same accepted capabilities for `COUNT(field)`,
`SUM(field)`, `AVG(field)`, `MIN(field)`, and `MAX(field)` input admission. The
remaining nested field and expression-result decisions stay with the expression
planner until persisted snapshots carry nested leaf metadata.

Runtime metrics also carry the current load row-efficiency and write-amplification
report shape through the `0.146.6` SQL capability slice so `icydb_metrics()`
continues to expose candidate-row, result-row, and write-maintenance totals
added in the metrics pass.

### Accepted SQL capability boundary

- Added SQL capability lookup to `SchemaInfo`, sourced from accepted persisted
  field kinds when an accepted snapshot is available.
- Routed SQL SELECT projection admission through accepted `selectable`
  capabilities for direct fields and top-level field references inside scalar
  projection expressions.
- Routed SQL `GROUP BY` direct field admission through accepted `groupable`
  capabilities.
- Routed direct-field SQL `ORDER BY` admission for SELECT, DELETE, and UPDATE
  selector tails through accepted `orderable` capabilities while leaving
  computed ORDER BY expressions to expression type planning.
- Routed SQL global aggregate field-target admission through accepted
  aggregate-input capabilities.
- Removed the unused generated-schema global aggregate command-core bridge
  after production compilation moved to the schema-aware entrypoint.

### Metrics report shape

- Kept `EventOps` and `EntitySummary` Candid shape coverage in sync with the
  load row-efficiency counters exposed by `icydb_metrics()`.
- Verified the metrics fixture projects candidate rows scanned, candidate rows
  filtered, and result rows emitted through the per-entity report path.
- Added global and per-entity write-amplification totals for save/delete rows
  touched, primary index entries changed, reverse-index entries changed, and
  relation checks.
- Derived write-amplification totals from the existing save/delete, index,
  reverse-index, and relation-validation events so write diagnostics stay
  aligned with the counters already emitted by the runtime.
- Preserved the rule that dashboards derive scan-to-result ratios from raw
  totals and write-amplification ratios from raw write totals instead of storing
  precomputed percentages in the canister report.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::sql::lowering::tests --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core sql_select --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core sql_write --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core metrics:: -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.5

`0.146.5` moves more planner-facing read boundaries onto accepted schema
metadata. Top-level expression type inference now reads the accepted-schema
`FieldType` stored in `SchemaInfo`, so SQL/fluent planning checks for direct
fields use the reconciled schema snapshot instead of the generated
`FieldKind`. SQL read predicate/filter literal canonicalization now also goes
through the accepted-schema `SchemaInfo` during session compilation and write
selector binding. Nested field-path inference remains generated-model based
until persisted snapshots carry nested leaf metadata. Cache metrics also now
record actual shared query-plan and SQL compiled-command cache hit, miss,
insert, and current-entry events instead of only exposing DTO fields for them.
Load metrics also split scalar/grouped read efficiency into candidate rows
scanned, candidate rows filtered, and result rows emitted, so operators can
derive scan-to-result ratios without mixing in delete or aggregate-only scan
paths.

### Accepted expression type boundary

- Routed direct `Expr::Field` inference through accepted-schema `FieldType`.
- Kept nested `Expr::FieldPath` inference generated-model based until persisted
  snapshots carry nested leaf descriptions.
- Updated direct-field `SUM` / `AVG` aggregate type checks to use the accepted
  inferred expression type rather than generated `FieldKind` numeric helpers.
- Removed the now-dead generated-field planner arithmetic helper.
- Added a guard proving accepted top-level field type metadata controls
  expression inference.

### Accepted SQL read literal boundary

- Added a schema-owned SQL literal canonicalizer on `SchemaInfo`.
- Preserved generated-model fallback for direct lower-level lowering tests.
- Routed session SQL SELECT/DELETE compilation through the accepted-schema
  projection when canonicalizing top-level predicate/filter literals.
- Routed SQL UPDATE target selectors and `INSERT ... SELECT` source selectors
  through the same accepted-schema projection.
- Removed now-unused generated-only SQL binding wrappers.
- Added a guard proving accepted persisted field kinds control strict SQL
  literal canonicalization.

### Cache metrics wiring

- Routed shared prepared-plan cache hit, miss, and insert events into the
  metrics sink.
- Routed SQL compiled-command cache hit, miss, and insert events into the
  metrics sink.
- Added global current-entry gauges for the shared query-plan cache and SQL
  compiled-command cache so reuse counters can be read beside cache pressure.
- Updated per-entity metrics shape coverage so cache counters are populated and
  projected consistently.

### Load efficiency metrics

- Added global and per-entity load row-efficiency totals for candidate rows
  scanned, candidate rows filtered, and result rows emitted.
- Routed the new counters through the shared scalar/grouped load finalizer so
  they stay separate from delete and aggregate-only row-scan metrics.
- Kept ratios derived by metrics consumers rather than storing precomputed
  percentages in the canister report.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core db::query::plan::expr::type_inference --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::session::tests::sql_surface --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core sql_write --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core sql_select --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core metrics:: -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.4

`0.146.4` starts moving cached query planning onto accepted schema metadata.
Session-cached SQL/fluent plans now prepare top-level planner field types from
the accepted schema snapshot, and the shared prepared-plan cache plus SQL
compiled-command cache are now keyed by an accepted-schema cache fingerprint.
Nested field metadata and row-layout decode remain generated-model based until
persisted snapshots carry nested leaf descriptions and live layout authority.
Runtime metrics also gain per-entity save-mode, route-plan, and
executor-outcome attribution so global counters can be traced back to the
entity that produced them.

### Accepted planning type boundary

- Added a `SchemaInfo` constructor that overlays accepted persisted top-level
  field kinds onto the generated model's field table.
- Kept generated `FieldKind` and nested-field metadata in that constructor so
  enum literal canonicalization and nested field planning remain unchanged in
  this slice.
- Updated shared session plan-cache preparation to load the accepted initial
  schema snapshot before planning and normalize/validate the cached plan with
  accepted top-level field types.
- Added a cache-only accepted schema fingerprint that hashes the accepted
  persisted schema snapshot plus the generated index contract.
- Routed shared SQL/fluent prepared-plan cache keys through the accepted schema
  cache fingerprint instead of the generated commit-schema fingerprint.
- Routed SQL compiled-command cache keys through the same accepted schema cache
  fingerprint so SQL text is invalidated when accepted top-level schema
  metadata changes.
- Removed the compiled SELECT artifact's schema-cache-key payload; compiled
  SELECTs now carry only the bound structural query and let the session shared
  plan cache derive accepted schema identity at execution time.
- Kept direct lower-level query planning helpers generated-model based for
  tests and query-layer diagnostics that intentionally bypass session runtime
  authority.
- Added a guard proving accepted top-level field type metadata wins when
  building session-planning schema info.

### Metrics observability

- Added save mutation-mode counters for insert, update, and replace paths, with
  matching row totals for each mode.
- Mirrored detailed route-plan counters into each entity summary so operators
  can identify which entity is responsible for key, index, range, full-scan,
  union, intersection, and grouped materialized route usage.
- Added executor outcome counters for success, aborted spans, and each
  `InternalError` class, with per-entity mirrors for save/delete spans and
  error-only load paths that can classify returned errors.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo fmt --all --check`.
- Ran `cargo test -p icydb-core metrics:: -- --nocapture`.
- Ran `cargo test -p icydb-core`.
- Ran `cargo test -p icydb-core db::schema::info --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::session::tests::sql_surface --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core sql_write --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets -- -D warnings`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.3

`0.146.3` keeps moving schema metadata consumers behind the accepted schema
snapshot boundary and expands the runtime metrics report so operational
snapshots expose the counters the engine already maintains. `DESCRIBE` /
`SHOW COLUMNS` no longer rebuild their own field-name slot map from raw
snapshot fields, accepted metadata now drives the entity header plus top-level
field kind/queryability facts, and `icydb_metrics()` now reports saved-row,
per-entity, detailed plan-route, and window-filter details that were previously
collapsed or hidden.

### Accepted schema lookup boundary

- Added accepted-schema field lookup helpers for persisted field names and
  routed schema description formatting through them.
- Removed the local `BTreeMap` staging from schema description formatting.
- Updated accepted-schema `DESCRIBE` / `SHOW COLUMNS` formatting so top-level
  field kind and queryability facts come from `PersistedFieldKind`.
- Updated accepted-schema `DESCRIBE` headers so entity path/name and
  primary-key label come from the accepted snapshot where available.
- Added a `PersistedFieldKind` to planner `FieldType` projection and used it
  for accepted metadata queryability, preparing the later live-schema SQL
  admission switch.
- Added strict SQL literal canonicalization for `PersistedFieldKind` and routed
  SQL INSERT/UPDATE literal validation through the accepted schema snapshot.
- Kept output shape unchanged: top-level fields still report their current
  accepted slot and nested fields still report no top-level slot.
- Kept nested structured-field rows generated-model based because persisted
  snapshots do not yet carry nested leaf descriptions.
- Added a guard proving accepted top-level metadata wins when formatting field
  descriptions.

### Metrics observability

- Added aggregate and per-entity `rows_saved` accounting so save spans now
  report affected rows instead of only instruction totals.
- Expanded per-entity metrics summaries to expose already-collected save,
  row-flow, index, reverse-index, relation-validation, unique-violation, and
  non-atomic partial-commit counters.
- Split plan metrics into exact top-level route counters for by-key, by-keys,
  key-range, index-prefix, index-multi-lookup, index-range, explicit full-scan,
  union, and intersection paths while preserving the older coarse
  key/index/range/full-scan counters for existing consumers.
- Added metrics window metadata: `window_end_ms`, `window_duration_ms`,
  `window_filter_matched`, `requested_window_start_ms`, and
  `active_window_start_ms`, so empty future-window reports explain why they are
  empty.
- Sorted per-entity summaries by total visible activity so write-heavy and
  maintenance-heavy entities are not hidden below read-heavy entities.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core metrics:: -- --nocapture`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::session::tests::sql_surface::sql_metadata_surfaces_match_typed_payloads --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::session::tests::sql_surface --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core sql_write --features sql -- --nocapture`.
- Ran `cargo check --workspace --tests`.
- Attempted `cargo clippy --workspace --all-targets -- -D warnings`; it now
  fails only on the unrelated dirty metrics const-fn warning in
  `crates/icydb-core/src/metrics/state.rs`.
- Ran `git diff --check`.

---

## 0.146.2

`0.146.2` tightens the schema-authority handoff after the initial schema store
went live. Reconciled schema snapshots now carry an explicit accepted wrapper,
database defaults have their own runtime-model contract, and persisted field
kinds have a schema-owned SQL capability projection ready for the later live
schema admission switch.

### Accepted schema authority

- Added `AcceptedSchemaSnapshot` as the typed handoff for snapshots that have
  passed reconciliation.
- Updated SQL `DESCRIBE` and `SHOW COLUMNS` persisted-schema helpers to accept
  the accepted wrapper instead of arbitrary decoded `PersistedSchemaSnapshot`
  payloads.
- Preserved the current initial-version-only reconciliation policy.

### Database defaults

- Added explicit runtime-model database-default metadata.
- Kept the initial database-default state `None`, preserving the rule that Rust
  `Default` does not authorize database slot synthesis.
- Routed persisted schema proposal construction through the runtime-model
  database-default contract instead of hard-coding it at the proposal boundary.

### SQL capability projection

- Added a schema-owned SQL capability projection over `PersistedFieldKind`.
- The projection records selectable, comparable, orderable, groupable, and
  aggregate-input facts for accepted persisted schema fields.
- Kept this as groundwork only: SQL planning still uses generated model
  metadata until live schema authority is fully switched.
- Added coverage for blob, numeric, collection, structured, and relation-key
  capability edges.

### Describe compatibility

- Kept the `EntityFieldDescription` Candid `slot` field as plain `u16` for
  CLI/canister compatibility.
- Preserved Rust `Option<u16>` accessor semantics by mapping a reserved no-slot
  value for nested fields.
- Added a Candid shape guard so `slot` cannot drift back to an optional wire
  field unnoticed.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::session::tests::sql_surface::sql_metadata_surfaces_match_typed_payloads --features sql -- --nocapture`.
- Ran `cargo check --workspace --tests`.
- Ran `cargo clippy --workspace --all-targets -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.1

`0.146.1` turns the reserved schema memory into a concrete raw schema metadata
map and starts persisting generated initial schema snapshots at the recovery
boundary. `SchemaStore` now owns a stable BTreeMap keyed by entity tag and
schema version, the raw snapshot payload has a typed persisted-schema codec,
and startup reconciliation writes or verifies each registered entity's initial
snapshot before row/index operations proceed. The current `DESCRIBE` /
`SHOW COLUMNS` surfaces also expose generated physical row slots while live
row-layout authority is still pending.

### Schema store map boundary

- Added `RawSchemaKey` as the durable key for one entity schema version.
- Added `RawSchemaSnapshot` as the raw encoded value boundary for future
  persisted schema snapshots.
- Updated `SchemaStore` from a bare stable-memory holder into a thin
  BTreeMap-backed persistence wrapper with insert, lookup, presence, length,
  and test-clear operations.
- Kept raw schema keys and encoded payloads inside the schema store boundary;
  reconciliation now uses typed snapshot insert/load entrypoints.
- Added schema-store tests for key encoding, raw payload round-tripping, and
  raw snapshot persistence.

### Startup reconciliation

- Added a schema-owned reconciliation pass over registered runtime hooks.
- First contact writes the generated initial persisted schema snapshot for each
  entity into its store's `SchemaStore`.
- Later contacts decode the stored snapshot and require an exact match with the
  compiled proposal until explicit schema-evolution rules exist.
- Rejected snapshots now report the first detected schema difference, such as
  entity identity, primary-key field ID, row layout, field count, field kind,
  nullability, default, storage decode, or leaf codec drift.
- Wired reconciliation through the commit recovery gate, guarded once per store
  registry so normal read/write entrypoints do not repeatedly scan schema
  metadata.
- Added focused tests for first-write, idempotent reread, and changed-snapshot
  rejection.

### Persisted snapshot codec

- Added a schema-owned Candid wire codec for `PersistedSchemaSnapshot` without
  deriving wire traits on the internal schema DTOs.
- Encoded the codec version, schema version, entity identity, primary-key field
  ID, row layout, field metadata, database defaults, storage decode policy,
  leaf codec, and recursive persisted field kind graph.
- Added typed round-trip coverage through `RawSchemaSnapshot` for nested
  `Map<Text, List<Uint>>` metadata.

### Describe slot visibility

- Added slot visibility to `EntityFieldDescription`.
- Top-level generated fields now report their current physical row slot.
- Nested structured-record leaves report no slot because they are not separate
  top-level persisted row slots.
- SQL `DESCRIBE` and `SHOW COLUMNS` now use the accepted initial persisted
  schema snapshot for top-level slot values, while the infallible Rust metadata
  helpers remain generated-model based.
- Updated shell rendering so `DESCRIBE` tables and `SHOW COLUMNS` lines include
  the slot value.

### Naming cleanup

- Renamed clear index-store fields, parameters, and locals that were still
  called `store` so row, index, and schema store references stay visually
  distinct during the 0.146 triplet rollout.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo test -p icydb render_ -- --nocapture`.
- Ran `cargo check --workspace --tests`.
- Ran `cargo clippy --workspace --all-targets -- -D warnings`.
- Ran `git diff --check`.

---

## 0.146.0

`0.146.0` starts the schema identity and layout authority work by introducing
schema-owned identity artifacts before adding any durable `__icydb_schema`
payload. Generated `EntityModel` metadata now projects into a compiled schema
proposal, that proposal can build the first persisted-schema snapshot shape for
a new database, and generated stores now reserve a third stable memory for
future schema metadata alongside row and index storage.

### Schema identity foundation

- Added `FieldId` as the durable logical field identity, separate from generated
  field order and executor slot indexes.
- Added schema-owned `SchemaVersion`, `SchemaFieldSlot`, and `SchemaRowLayout`
  types so future reconciliation can map durable field IDs to physical row
  slots without using loose integer pairs.
- Added `CompiledSchemaProposal` and `CompiledFieldProposal` as the bridge from
  generated `EntityModel` / `FieldModel` metadata into schema-identity terms.
- Routed the existing commit-schema fingerprint through the compiled proposal
  without changing the current fingerprint contract.

### Persisted snapshot shape

- Added `PersistedSchemaSnapshot` and `PersistedFieldSnapshot` as the owned
  shape intended for the future `__icydb_schema` payload.
- Added owned `PersistedFieldKind` conversion from generated `FieldKind` so
  live schema snapshots do not depend on generated static metadata for nested,
  relation, enum, collection, or structured field shapes.
- Added `SchemaFieldDefault` with only an explicit `None` state for now, keeping
  database defaults separate from Rust struct `Default` behavior.
- Added schema proposal tests covering initial field IDs, row layout,
  persisted snapshot construction, and storage contracts.

### Store memory boundary

- Added `SchemaStore` as the runtime owner for the per-store schema stable
  memory. It is intentionally only a memory boundary in this slice; startup
  reconciliation and the concrete `__icydb_schema` map remain separate work.
- Extended `StoreHandle` and `StoreRegistry` so generated stores bind row,
  index, and schema stores as one triplet.
- Extended the derive/build path so every `#[store(...)]` declaration now
  supplies `schema_memory_id`, and generated canister wiring emits the matching
  `SchemaStore` static.
- Updated schema validation so `schema_memory_id` is range-checked,
  reserved-ID checked, unique within its store, and unique across all memories
  registered to the same canister.

### Validation

- Ran `cargo fmt --all`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo check -p icydb-schema --tests`.
- Ran `cargo check -p icydb-schema-derive --tests`.
- Ran `cargo check -p icydb-build --tests`.
- Ran `cargo check --workspace --tests`.
- Ran `cargo test -p icydb-core db::schema --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core db::registry --features sql -- --nocapture`.
- Ran `cargo test -p icydb-schema -- --nocapture`.
- Ran `cargo test -p icydb-schema-tests -- --nocapture`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `cargo clippy --workspace --all-targets -- -D warnings`.
- Ran `git diff --check`.
