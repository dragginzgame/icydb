# IcyDB 0.241 Detailed Changelog

## 0.241.0

### 🧹 Cleanup

- Five exact-count and prepared global-aggregate helpers now use private
  visibility because every caller is owned by the same source module.
- The retained ordinary and diagnostics execution branches remain separate.
  The preceding convergence audit showed that merging them increased final
  production raw Wasm despite reducing source lines.

### 📊 Audit

- Two production Rust files are touched, with five visibility qualifiers
  removed and no function body or call site changed.
- The correction adds no type, state, cache, execution route, invalidation
  edge, behavior axis or persisted/public surface.
- Instruction and raw-Wasm measurements were not repeated because the patch
  changes only source-level module reachability while retaining every compiled
  function and caller.

### 🧪 Testing

Focused warning-denied SQL and diagnostics builds plus repository authority
invariants pass.
