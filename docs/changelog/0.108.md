# 0.108 Changelog

## 0.108.1

- `0.108.1` extends the canonical-query-identity cleanup to the grouped fluent `HAVING` seam and adds one live public-SQL proof through the PocketIC canister surface.
- The code fix is in [/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/mutation.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/mutation.rs). Before this patch, repeated grouped `HAVING` builders still stored one raw left-associated `AND` tree in append order:
  - `having_group(...).having_aggregate(...)`
  - and `having_aggregate(...).having_group(...)`
  could remain different outward identity shapes even when they were semantically equivalent.
- The grouped intent boundary now normalizes the combined `HAVING` boolean expression before storing it, matching the scalar `WHERE` ownership model introduced in `0.108.0`. This does not add new grouped semantic canonicalization rules; it only stops grouped fluent mutation order from leaking into identity surfaces that already claim to be semantic.
- The important grouped outcomes are:
  - canonical-equivalent grouped fluent `HAVING` orderings now share one structural query cache key
  - grouped `plan_hash_hex()`, planned-query hash, compiled-query hash, and logical explain now stay aligned for those equivalent `HAVING` spellings
  - grouped fluent `trace()` reuses one shared query-plan cache entry instead of splitting by append order
- The public SQL proof landed in [/home/adam/projects/icydb/testing/pocket-ic/tests/sql_canister.rs](/home/adam/projects/icydb/testing/pocket-ic/tests/sql_canister.rs). That test runs through the live canister `query(sql)` endpoint and proves:
  - `EXPLAIN EXECUTION VERBOSE SELECT ... GROUP BY ... HAVING age >= 24 AND COUNT(*) > 0 ...`
  - and the same query with the `HAVING` conjuncts reversed
  now produce the same outward explain payload on the canister boundary.
- That PocketIC proof matters because `0.108.1` is not only an internal hash/cache cleanup. It confirms the grouped identity alignment survives the real SQL parser, lowering path, canister query endpoint, and outward explain rendering surface.
- Coverage added or tightened in this patch pins:
  - grouped structural cache-key parity in [/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/tests/cache_key.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/tests/cache_key.rs)
  - grouped plan-hash and explain parity in [/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/tests/explain.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/tests/explain.rs)
  - grouped fluent trace/cache reuse in [/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_surface.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_surface.rs)
  - public SQL canister explain parity in [/home/adam/projects/icydb/testing/pocket-ic/tests/sql_canister.rs](/home/adam/projects/icydb/testing/pocket-ic/tests/sql_canister.rs)
- Validation for the shipped `0.108.1` boundary covered:
  - `cargo test -p icydb-core grouped_having -- --nocapture`
  - `bash -lc "POCKET_IC_BIN=/tmp/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_canister sql_canister_query_endpoint_keeps_canonical_equivalent_grouped_having_explain_identity -- --nocapture"`
  - `cargo fmt --all`
  - `cargo clippy -p icydb-core --all-targets -- -D warnings`
  - `cargo clippy -p icydb-testing-integration --all-targets -- -D warnings`

---

## 0.108.0

- `0.108.0` finishes the first canonical-query-identity cleanup by making canonical scalar `filter_expr` the owner for the nearby query-identity surfaces that were still preserving spelling drift after `0.105` and `0.107`.
- The core hashing work landed in:
  - [/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/hash_parts/mod.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/hash_parts/mod.rs)
  - [/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/hash_parts/profile.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/hash_parts/profile.rs)
  - [/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/projection_hash.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/projection_hash.rs)
  - [/home/adam/projects/icydb/crates/icydb-core/src/db/query/explain/plan.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/explain/plan.rs)
- Before this patch, several identity-adjacent surfaces still drifted between:
  - predicate-owned identity when a derived predicate existed
  - expression-owned identity when only semantic `filter_expr` survived
  - fluent insertion-order identity when repeated `.filter(...)` calls built left-associated raw `AND` trees before plan hashing
- The shipped rule is now:
  - when canonical scalar `filter_expr` exists, it is the semantic identity owner for fingerprint and continuation-signature hashing
  - derived predicate remains a fallback identity input only when no semantic filter expression exists
  - explain-side plan hashing uses the same semantic filter model as planned-query hashing instead of rendered filter text
  - repeated fluent `.filter(...)` accumulation normalizes the combined boolean tree before storing it, so intent-owned `filter_expr` no longer preserves insertion-order drift
- That normalization follow-through landed in [/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/mutation.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/mutation.rs). The important fix there is not just cleaner shape; it closes a real outward identity bug where canonical-equivalent fluent filters could still produce different `plan_hash_hex()` values and separate shared query-plan cache entries.
- Publicly, the final `0.108.0` outcomes are:
  - equivalent scalar boolean `WHERE` spellings now share one canonical fingerprint and continuation signature when they already reduce to the same planner-owned boolean form
  - semantically distinct expression-owned scalar filters still stay distinct in identity and cache reuse
  - direct query `plan_hash_hex()`, planned-query hash, compiled-query hash, and explain signature now line up on the same canonical scalar filter identity
  - session `trace_query` and the shared query-plan cache now reuse one entry for canonical-equivalent scalar filters instead of splitting by filter spelling or fluent insertion order
- This patch intentionally does not add new rewrite rules, new planner behavior, or new prepared-template widening. It is an identity-alignment slice only: use the canonical semantic filter the planner already owns, and stop letting nearby hash/cache surfaces preserve older spelling-level drift.

```rust
let left = session
    .load::<SessionSqlEntity>()
    .filter(FieldRef::new("age").gte(20_i32))
    .filter(FieldRef::new("age").lt(40_i32));

let right = session
    .load::<SessionSqlEntity>()
    .filter(FieldRef::new("age").lt(40_i32))
    .filter(FieldRef::new("age").gte(20_i32));
```

- After `0.108.0`, those two fluent queries now reuse the same canonical plan identity instead of producing different outward hashes because of filter insertion order.
- Coverage added or tightened in this patch pins:
  - structural cache-key parity in [/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/tests/cache_key.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/tests/cache_key.rs)
  - direct query plan-hash and explain-surface parity in [/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/tests/explain.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/tests/explain.rs)
  - fingerprint and continuation-signature stability in [/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/tests/stability.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/tests/stability.rs)
  - explain-signature parity in [/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/shape_signature/tests/mod.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/shape_signature/tests/mod.rs)
  - session SQL and fluent cache reuse in [/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_surface.rs](/home/adam/projects/icydb/crates/icydb-core/src/db/session/tests/sql_surface.rs)
- Validation for the shipped `0.108.0` boundary covered:
  - `cargo test -p icydb-core structural_query_cache_key_treats_equivalent_expression_owned_boolean_shapes_as_identical -- --nocapture`
  - `cargo test -p icydb-core fingerprint_and_signature_distinguish_different_expression_owned_filter_expr -- --nocapture`
  - `cargo test -p icydb-core fingerprint_and_signature_follow_canonical_searched_case_filter_identity -- --nocapture`
  - `cargo test -p icydb-core explain_signature_matches_plan_signature_for_expression_owned_filter_expr -- --nocapture`
  - `cargo test -p icydb-core canonical_equivalent_scalar_filter_shapes_share_query_plan_hash_surfaces -- --nocapture`
  - `cargo test -p icydb-core trace_query_reuses_canonical_equivalent_scalar_filter_plan_identity -- --nocapture`
  - `cargo test -p icydb-core fluent_trace_and_plan_hash_reuse_canonical_equivalent_filter_order -- --nocapture`
  - `cargo test -p icydb-core`
  - `cargo fmt --all`
  - `cargo clippy -p icydb-core --all-targets -- -D warnings`

---
