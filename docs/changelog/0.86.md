# 0.86 — Grouped HAVING Expression Widening

## 0.86.2 - 2026-04-17 - Grouped HAVING Fallout and Validation Closeout

This patch closes the main `0.86` fallout pass after the grouped `HAVING`
expression and explain/fingerprint widening landed. No new grouped `HAVING`
semantics were added here; this is the validation and frozen-surface cleanup
that leaves the `0.86` line in a cleaner pushed state.

What changed:

* updated the frozen grouped `HAVING` continuation-signature snapshot to the
  new correct value after widened grouped `HAVING` expression-shape hashing in:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/shape_signature/tests/mod.rs`
* cleaned the clippy fallout surfaced by the widened explain/fingerprint path
  in:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/explain/plan.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/executor/aggregate/runtime/grouped_fold/page_finalize.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/executor/pipeline/orchestrator/state/mod.rs`
* reran the grouped-focused `icydb-core` validation sweep to prove the widened
  grouped `HAVING` line is green after the snapshot and lint follow-through.

Verification:

* `CARGO_TARGET_DIR=/tmp/icydb-086-followup-clippy2 cargo clippy -p icydb-core --all-targets -- -D warnings`
* `CARGO_TARGET_DIR=/tmp/icydb-086-followup-test2 cargo test -p icydb-core db::query::fingerprint::shape_signature::tests::signature_snapshot_grouped_having_shape_is_stable -- --nocapture`
* `CARGO_TARGET_DIR=/tmp/icydb-086-followup-test2 cargo test -p icydb-core grouped_ -- --nocapture`

---

## 0.86.1 - 2026-04-17 - Grouped HAVING Explain and Fingerprint Follow-Through

This patch finishes the main post-`0.86.0` follow-through for widened grouped
`HAVING`. The grouped `HAVING` expression tree is no longer only executable and
cache-keyed; explain reporting and grouping-shape fingerprinting now understand
the widened expression surface too.

What changed:

* grouped explain projection now carries widened grouped `HAVING` as an
  expression tree instead of the old clause-only DTO in:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/explain/plan.rs`
* explain projection prefers planner-owned widened `having_expr` and falls back
  through legacy grouped `GroupHavingSpec` conversion only when older grouped
  plans still surface the clause form.
* grouped grouping-shape fingerprinting now hashes widened grouped `HAVING`
  expression trees directly for both planner-owned and explain-owned grouped
  shapes in:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/hash_parts/mod.rs`
* widened grouped `HAVING` explain/fingerprint regressions now pin:
  * explain output for a non-legacy grouped `HAVING` expression like
    `COUNT(*) + 1 > 5`
  * continuation-signature distinction for grouped queries that differ only in
    widened `HAVING` expression shape
* grouped route/explain snapshot coverage was updated to the new
  expression-shaped grouped `HAVING` representation in:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/explain/tests/grouped.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/executor/planning/route/tests/mod.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/executor/planning/route/tests/grouped.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/fingerprint/tests/grouped.rs`

Important semantic note:

* semantic query fingerprints still intentionally exclude grouping-shape-only
  differences.
* continuation signatures still intentionally include grouping-shape semantics.
* widened grouped `HAVING` therefore changes continuation identity, but does not
  create a new semantic fingerprint for two otherwise-identical grouped plans.

Verification:

* `CARGO_TARGET_DIR=/tmp/icydb-086-explain-fp cargo check -p icydb-core`
* `CARGO_TARGET_DIR=/tmp/icydb-086-explain-fp cargo test -p icydb-core db::query::explain::tests::grouped::explain_grouped_plan_snapshot_for_ordered_having_shape_is_stable -- --nocapture`
* `CARGO_TARGET_DIR=/tmp/icydb-086-widened-regressions-2 cargo test -p icydb-core db::query::explain::tests::grouped::explain_grouped_having_expression_projection_is_reported -- --nocapture`
* `CARGO_TARGET_DIR=/tmp/icydb-086-widened-regressions-2 cargo test -p icydb-core db::query::fingerprint::tests::grouped::grouped_continuation_signature_distinguishes_widened_having_expression_shape -- --nocapture`

---

## 0.86.0 - 2026-04-16 - Post-Aggregate Grouped HAVING Expressions

This patch starts the `0.86` line by widening grouped SQL `HAVING` onto the
post-aggregate expression layer introduced in `0.85`. Grouped `HAVING` no
longer stops at conservative `field/aggregate op literal` clauses; it now
admits bounded boolean comparisons over grouped keys, finalized aggregate
outputs, literals, arithmetic, and `ROUND(...)`.

What changed:

* grouped plan/runtime now carry and evaluate a real grouped `HAVING`
  expression tree instead of requiring the legacy clause form at execution
  time:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/model.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/semantics/group_model.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/semantics/group_having.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/executor/aggregate/runtime/mod.rs`
* grouped SQL parsing now accepts widened `HAVING` value expressions on both
  sides of a compare, including grouped fields, aggregate leaves, literals,
  bounded arithmetic, and `ROUND(...)` in:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/parser/model.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/parser/clauses.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/parser/statement/mod.rs`
* grouped SQL lowering now resolves widened `HAVING` onto the grouped plan
  expression surface instead of rejecting non-legacy forms in:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/lowering/normalize.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/lowering/select/aggregate.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/lowering/select/mod.rs`
* grouped SQL lowering now builds one shared deduplicated aggregate set across
  grouped projection and grouped `HAVING`, so `HAVING`-only aggregates like
  `AVG(age)` are still declared, indexed, and executed even when they are not
  projected.
* grouped projection identity lowering now disables the `ProjectionSelection::All`
  fast path when grouped `HAVING` introduces extra hidden aggregates, so those
  `HAVING`-only aggregate outputs do not leak into visible grouped result rows
  in:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/sql/lowering/select/projection.rs`
* grouped static planning semantics now extend the frozen grouped aggregate
  execution-spec bundle with any aggregate indexes referenced by legacy or
  widened grouped `HAVING`, so runtime sees the full post-aggregate surface in:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/semantics/logical.rs`
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/plan/group.rs`
* grouped structural cache identity now includes widened grouped `HAVING`
  expression trees, so otherwise-identical grouped queries with different
  post-aggregate `HAVING` expressions do not collide in the shared query cache
  in:
  * `/home/adam/projects/icydb/crates/icydb-core/src/db/query/intent/cache_key.rs`

Examples that now work:

```sql
SELECT class_name, COUNT(*)
FROM character
GROUP BY class_name
HAVING COUNT(*) + 1 > 5
ORDER BY class_name
LIMIT 100;
```

```sql
SELECT class_name, AVG(strength)
FROM character
GROUP BY class_name
HAVING ROUND(AVG(strength), 2) >= 10
ORDER BY class_name
LIMIT 100;
```

Deliberately still out of scope:

* grouped `ORDER BY` widening remains unchanged.
* aggregate-input expression widening remains unchanged.
* raw non-grouped field references in grouped `HAVING` remain rejected.
* the existing conservative grouped `HAVING` fluent surface is still carried in
  parallel for compatibility with the older grouped builder path.

Verification:

* `cargo fmt --all`
* `CARGO_TARGET_DIR=/tmp/icydb-086-tests2 cargo test -p icydb-core parse_select_grouped_statement_with_post_aggregate_having_exprs -- --nocapture`
* `CARGO_TARGET_DIR=/tmp/icydb-086-tests2 cargo test -p icydb-core compile_sql_command_select_grouped_post_aggregate_having_exprs_lowers -- --nocapture`
* `CARGO_TARGET_DIR=/tmp/icydb-086-tests2 cargo test -p icydb-core grouped_select_allows_post_aggregate_having_expressions -- --nocapture`
* `CARGO_TARGET_DIR=/tmp/icydb-086-tests2 cargo check -p icydb-core`

Still intentionally pending after `0.86.0`:

* no additional grouped `ORDER BY` widening.
* no aggregate-input expression widening.
