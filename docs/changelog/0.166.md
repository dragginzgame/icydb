# IcyDB 0.166 Changelog

## 0.166.0

### ➕ Added

- Started the SQL surface contract proof audit with a durable matrix that maps
  documented SQL claims to public query, update, and DDL entrypoints.

  ```text
  SQL_SUBSET.md claim -> public SQL entrypoint -> evidence row
  execute_sql_query::<E>(...) -> read/explain/introspection proof
  execute_sql_update::<E>(...) -> mutation proof
  execute_sql_ddl::<E>(...) -> accepted-catalog DDL proof
  ```

- Added the initial matrix rows for supported SELECT, mutation, DDL,
  introspection, blob, predicate, HAVING, alias, RETURNING, and unsupported SQL
  families.

- Mapped the first SQL surface matrix rows to existing public-entrypoint test
  evidence, marking proven rows for scalar SELECT, projection, aliases,
  aggregates, grouped output, EXPLAIN, core SQL writes, and DDL mutation
  families, then resolving the initial partial rows as direct public-surface
  proof was added.

  ```text
  execute_sql_query::<E>(...) tests -> SELECT/projection/aggregate proof
  execute_sql_update::<E>(...) tests -> INSERT/UPDATE/DELETE proof
  execute_sql_ddl::<E>(...) tests -> CREATE/DROP/ALTER proof
  ```

- Added direct public-query entrypoint coverage for metadata SQL so
  `DESCRIBE`, `SHOW COLUMNS`, `SHOW ENTITIES`, and `SHOW TABLES` are proven
  through `execute_sql_query::<E>(...)` rather than only helper surfaces.

- Added direct public-update entrypoint coverage for `INSERT`, `UPDATE`, and
  `DELETE ... RETURNING *` so the wildcard returning contract is proven through
  `execute_sql_update::<E>(...)`.

- Added public-update fail-closed coverage for malformed and oversized SQL blob
  literals so blob parsing errors are proven not to partially mutate rows.

- Split the broad `WHERE` predicate proof row into concrete predicate-family
  rows, with direct public-query evidence for comparisons, boolean trees,
  field paths, field-to-field predicates, membership, ranges, null tests,
  boolean truth tests, prefix text predicates, direct `STARTS_WITH`, and
  searched `CASE`.

  ```text
  WHERE predicates -> per-family proof rows
  field-to-field / BETWEEN / IN / NULL / truth predicates -> public rows
  prefix LIKE / ILIKE / STARTS_WITH -> bounded prefix proof
  ```

- Added public-query fail-closed coverage for unsupported SQL families,
  including joins, subqueries, CTEs, set operations, window functions,
  scalar SQL cursor-token syntax, transaction statements, byte-metric
  functions, simple `CASE`, non-prefix text patterns, and grouped row-field
  text predicates.

- Fixed SQL expression lowering so `LIKE` / `ILIKE` only admit the same bounded
  prefix pattern as predicate parsing: exactly one trailing `%`, with no
  internal `%` or `_` wildcard.

  ```text
  LIKE 'al%' -> admitted prefix predicate
  LIKE '%al%' -> rejected
  LIKE 'a_' -> rejected
  ```

- Added public-update unchanged-state coverage for unsupported subquery/CTE
  mutations and unsupported computed or aggregate `RETURNING` projections.

### 📚 Documentation

- Marked the 0.166 design as in progress and linked the concrete proof matrix
  artifact so subsequent slices can start from contract rows rather than
  implementation convenience.

## 0.166.1

### 🩹 Fixed

- Fixed scalar SQL ordering over composite primary-key components so explicit
  `ORDER BY` terms remain the leading sort semantics and only missing
  primary-key components are appended as deterministic tie-breakers.

  ```text
  ORDER BY y DESC -> y DESC, x ASC
  not -> x DESC, y DESC
  ```

### 📚 Documentation

- Reconciled the 1.0 TODO list with the public SQL proof matrix by adding
  explicit checked items for the proven SQL query, update, DDL, projection,
  predicate, and metadata surfaces without marking broader engine-level 1.0
  TODOs complete.

- Updated the SQL surface mapping note so implementation-facing SQL ownership
  points at the 0.166 proof matrix as the public-entrypoint evidence ledger.

- Advanced the 0.166 design status to a closeout candidate after the first
  public-surface proof pass, while keeping final closeout gated on
  release-slice validation and review of future SQL contract changes.

- Added explicit matrix rows for wrong-entrypoint SQL ownership so query,
  update, and DDL statement families stay separated by public API rather than
  only by implementation helpers.

- Corrected the SQL subset contract for grouped text-function projections:
  text transforms over grouped key values are part of the supported statement
  surface, while raw non-group field text transforms remain outside grouped key
  authority.

## 0.166.2

### 🧪 Testing

- Added explicit fail-closed proof for SQL casts through both public query and
  public update entrypoints, so `CAST(...)` remains outside the admitted
  bounded expression family instead of becoming accidental parser surface or
  mutating state.

### 📚 Documentation

- Made supported aggregate expression-input, `DISTINCT`, and `FILTER` shapes
  explicit in the SQL subset contract and proof matrix, matching the existing
  public aggregate coverage.

## 0.166.3

### 🧪 Testing

- Added public DDL fail-closed proof for `DESC` index order so `CREATE INDEX
  ... (field DESC)` rejects without publishing accepted index metadata.

- Added public DDL fail-closed proof for the legacy broad numeric spellings
  `int` and `nat` in `ALTER TABLE ... ADD COLUMN`, preserving the explicit
  numeric hard-cut at the SQL entrypoint.

- Added public query fail-closed proof for unsupported introspection modifiers,
  keeping `DESCRIBE`, `SHOW INDEXES`, `SHOW COLUMNS`, and `SHOW ENTITIES`
  constrained to the documented metadata command shapes.

- Added public query proof for grouped boolean searched-`CASE` computed
  projections and reconciled the older 1.0 TODO ambiguity by documenting them
  as part of the admitted post-aggregate computed projection family.

- Added public query fail-closed proof for invalid grouped projection shapes,
  including grouped projections without aggregate output, grouped wildcards,
  scalar terms after aggregate outputs, and non-group source fields.

## 0.166.4

### 🧪 Testing

- Added public query proof that global and grouped `HAVING` terms are not
  auto-projected into result payloads when they are used only for filtering.

## 0.166.5

### 🧪 Testing

- Added public query proof that mixed scalar projection shapes preserve ordered
  columns, explicit aliases, canonical computed labels, and row payloads limited
  to requested outputs through `execute_sql_query::<E>(...)`.

- Added public query proof for nullable `ORDER BY` semantics, documenting
  `ASC` nulls-first behavior and reversed `DESC` comparator behavior.

- Added public query proof that global and grouped aggregate labels preserve
  canonical rendered terms and explicit aliases through
  `execute_sql_query::<E>(...)`.

## 0.166.6

### 🧪 Testing

- Added public query proof that admitted scalar clauses compose across
  `WHERE`, computed projection, field-alias ordering, `LIMIT`, and `OFFSET`.

- Added public query proof that admitted grouped clauses compose across
  `WHERE`, `GROUP BY`, `HAVING`, aggregate aliases, aggregate ordering, and
  grouped windowing.

- Added public query proof that multi-field grouped result keys preserve
  declared `GROUP BY` field order and keep aggregate values separate from group
  identity.

- Added public query proof that equivalent parenthesized `AND` / `OR`
  predicate groups preserve public row semantics.

- Added public `EXPLAIN EXECUTION` proof that index-compatible range predicates
  keep an index route while non-index residual predicates remain visible as
  post-access filters.

## 0.166.7

### 🧪 Testing

- Added public query proof that nullable `GROUP BY` keys form one `NULL` group
  and that grouped `HAVING` filters after aggregate counts finalize.

## 0.166.8

### 🧪 Testing

- Added public query proof that bounded scalar boolean searched-`CASE`
  projection returns deterministic `TRUE` / `FALSE` payloads through
  `execute_sql_query::<E>(...)`.

- Added public query proof that `SHOW INDEXES IN Entity` is a direct metadata
  alias of `SHOW INDEXES FROM Entity`.

- Added public query proof that grouped text-function projection over grouped
  key values returns deterministic grouped payloads through
  `execute_sql_query::<E>(...)`.

## 0.166.9

### 🧪 Testing

- Added public query proof that grouped post-aggregate computed projection
  evaluates over finalized grouped outputs through `execute_sql_query::<E>(...)`.

- Added public query proof that global and grouped aggregate input expressions
  evaluate per source row before reduction through `execute_sql_query::<E>(...)`.

- Added public query proof that global and grouped aggregate `DISTINCT` and
  `FILTER` terminals preserve independent aggregate semantics through
  `execute_sql_query::<E>(...)`.

## 0.166.10

### 🧪 Testing

- Added public update proof that `INSERT`, `UPDATE`, and `DELETE` field-list
  `RETURNING` projections preserve requested columns, rows, and delete effects
  through `execute_sql_update::<E>(...)`.

- Tightened the public update mutation-shape proof so plain `DELETE` count
  payloads and persisted row removal are asserted through
  `execute_sql_update::<E>(...)`.

- Tightened the public update mutation-shape proof so `INSERT` and `UPDATE`
  count payloads are paired with follow-up public query assertions for
  persisted inserted and updated row state.

- Tightened wrong-surface DDL proof so `CREATE INDEX` rejected through query or
  update surfaces also proves no accepted index metadata is published.

## 0.166.11

### 🧪 Testing

- Added public query proof that basic global aggregate loads preserve count,
  sum, min, max, and empty-input count/sum semantics through
  `execute_sql_query::<E>(...)`.

- Added public query proof that basic grouped aggregate loads preserve grouped
  keys and aggregate values through `execute_sql_query::<E>(...)`.

- Added public query proof that scalar `DISTINCT` deduplicates before
  `ORDER BY` / `LIMIT` / `OFFSET` windowing through
  `execute_sql_query::<E>(...)`.

## 0.166.12

### 📚 Documentation

- Tightened the SQL surface matrix DDL rows so expression-index and unique-index
  claims name the exact public `execute_sql_ddl::<E>(...)` publication,
  duplicate-rejection, and `SHOW INDEXES` proof tests.

- Tightened the `ALTER TABLE` matrix rows so `ADD COLUMN`, default changes,
  nullability changes, rename, and drop-column behavior cite exact public DDL
  proof tests rather than umbrella test-family labels.

## 0.166.13

### 📚 Documentation

- Closed the 0.166 SQL surface proof audit after the matrix reached a stable
  state with no supported rows classified as partially proven, unproven,
  contradicted, or accidentally accepted.

- Documented the ongoing rule that future SQL contract changes must update the
  proof matrix alongside the behavior, documentation, or test change that
  alters the public SQL surface.

### 🧪 Testing

- Split broad SQL proof tests into smaller public-entrypoint tests so the final
  closeout suite remains clippy-clean without changing the SQL surface claims
  being exercised.

- Added direct `execute_sql_query::<E>(...)` EXPLAIN proof tests for plan,
  execution, JSON, pushdown/residual, and unsupported-feature behavior so
  helper-level EXPLAIN coverage remains supporting evidence only.

## 0.166.14

### 📚 Documentation

- Marked the SQL surface proof matrix as closed and clarified that EXPLAIN rows
  claiming `execute_sql_query::<E>(...)` evidence are closed only by tests that
  call that public query entrypoint directly.

### 🧪 Testing

- Added direct public-query EXPLAIN proof tests for plan, execution, JSON,
  pushdown/residual, and unsupported-feature behavior.

  ```text
  execute_sql_query::<E>(EXPLAIN ...) -> public explain payload
  execute_sql_query::<E>(EXPLAIN EXECUTION ...) -> pushdown/residual proof
  execute_sql_query::<E>(EXPLAIN JSON ...) -> JSON explain proof
  ```
