# 0.162.x

## 0.162.15

### 🔧 Changed

- Moved primary-key-order fast-path and route-planning checks onto
  planner-frozen ordered primary-key field contracts.
- Removed stale scalar-only order helper use from production planning,
  aggregate field-target classification, and structural-save diagnostics.
- Updated PK-ordered continuation validation to decode the full ordered
  primary-key boundary instead of validating only one scalar primary-key field.
- Moved DDL row preflight decoding through `DecodedDataStoreKey` and structural
  row validation instead of forcing scalar `StorageKey` access.
- Changed relation reverse-index paths to use fallible scalar bridges when they
  still require scalar keys, rather than panicking scalar accessors.
- Removed the panicking decoded data-key scalar accessor. Remaining scalar-only
  index row-identity bridges are now explicitly named `try_storage_key()`.

### 🧪 Testing

- Added focused composite cursor-boundary coverage and rechecked relation,
  index-entry, route-planning, DDL row-scan, and scalar-bridge validation with
  focused core tests and clippy.

## 0.162.14

### 🔧 Changed

- Moved model-only EXPLAIN covering-route descriptors onto ordered generated
  primary-key field lists, so composite primary-key projections map to
  component sources instead of the scalar primary-key bridge.
- Updated covering-index bytes/distinct projection strategies to compare
  deterministic secondary-order contracts against the full ordered primary-key
  suffix, preserving index-order fast paths for composite primary keys.
- Removed the live `canic-cdk` dependency in favor of direct IC crates while
  preserving IcyDB-owned wrappers for stable memory and testkit policy.
- Added a dependency-graph guard for sensitive IC/runtime crate drift after the
  direct-IC dependency cut.

### 🧪 Testing

- Added focused model-only EXPLAIN and covering-index projection coverage for
  ordered composite primary-key components, then reran full `icydb-core`
  tests, core clippy, formatting, and diff hygiene.
- Rechecked dependency graph invariants for the direct-IC dependency boundary.

## 0.162.13

### 🔧 Changed

- Moved covering projection primary-key sources onto ordered primary-key
  component indexes for primary-store and hybrid covering routes.
- Tightened composite primary-key null planning so every ordered primary-key
  component is treated as non-nullable row identity.
- Updated storage snapshot diagnostics to accept compact composite data keys
  when counting per-entity storage rows, keeping storage observability
  key-shape agnostic.

### 🧪 Testing

- Added focused covering-route, primary-key null-planning, and storage snapshot
  diagnostics coverage for ordered composite primary-key components.

## 0.162.12

### 🔧 Changed

- Moved id-returning aggregate/projection terminals, grouped row decode,
  materialized numeric aggregates, and materialized bytes terminals onto
  data-key-aware row decoding and scalar-or-composite `PrimaryKeyValue`
  identity.
- Moved index-range cursor anchor derivation, covering-row presence probes, and
  index-anchor primary-key equivalence checks onto compact scalar-or-composite
  row identity instead of scalar `StorageKey` accessors.
- Updated deterministic `ORDER BY` normalization and cursor boundary validation
  to use the full ordered primary-key field list, so composite primary-key
  pagination cannot collapse same-prefix rows into a scalar tie-break.
- Updated secondary-order compatibility contracts to recognize ordered
  composite primary-key suffixes instead of requiring a single terminal
  primary-key field.
- Removed obsolete scalar-only row decode helper entrypoints.

### 🧪 Testing

- Added focused fluent coverage for composite primary-key exact load/delete,
  id-returning terminals, and cursor pagination across same-prefix composite
  keys, plus planner-order contract coverage for composite primary-key suffixes,
  then reran focused cursor/order/equivalence validation, full `icydb-core`
  tests, and core clippy.

## 0.162.11

### 🔧 Changed

- Moved SQL INSERT/UPDATE write-key construction onto the accepted ordered
  primary-key field list, so composite-key SQL writes produce the same
  scalar-or-composite row identity contract as typed write paths.
- Updated SQL UPDATE target selection and primary-key mutation checks to
  project and validate every accepted primary-key component instead of relying
  on a scalar row key channel.
- Moved persisted-row primary-key validation, identity-row projection,
  retained-slot decode, projection ordering, and reverse-relation commit prep
  through `PrimaryKeyValue` row identity boundaries.
- Tightened the large composite row-key validation path so row decode compares
  by reference instead of passing the full primary-key value by copy.

### 🧪 Testing

- Added focused SQL write coverage for composite primary-key INSERT/UPDATE,
  missing component rejection, and primary-key mutation rejection, and rechecked
  core clippy for the composite row-key validation boundary.

## 0.162.10

### 🔧 Changed

- Moved structural data-key construction for exact primary-key access onto the
  scalar-or-composite `PrimaryKeyValue` boundary, so composite `Value::List`
  keys can route through `by_id` / `by_ids` execution paths.
- Updated access-plan primary-key validation to validate ordered composite key
  literals component-by-component against accepted schema primary-key fields
  while preserving scalar key validation.
- Updated typed save preflight to validate every composite primary-key field
  against the entity identity key instead of comparing only the first
  primary-key field.
- Updated schema describe and index-listing output to mark/render every
  ordered composite primary-key field.

### 🧪 Testing

- Added focused structural data-key, access-validation, mutation-save,
  schema-describe, and schema-index rendering coverage for exact composite
  primary-key routing and observability.

## 0.162.9

### 🔧 Changed

- Moved aggregate, grouped-runtime, cursor-anchor, and retained-row executor
  decode paths behind explicit scalar fail-closed bridges. Scalar
  `StorageKey` access is now limited to intentionally scalar aggregate output
  boundaries and other explicit bridge points.
- Added ordered primary-key fields to schema observability payloads so CLI
  schema rendering can display composite primary keys as ordered field lists.
- Updated schema-check drift analysis to compare generated and accepted
  primary-key fields structurally instead of comparing only a rendered scalar
  label.
- Clarified the 0.162 design audit requirements so observability, performance,
  instruction, wasm-size, storage-size, commit/recovery, and index-footprint
  audits include composite primary-key fixtures once the audited surface can
  construct them.

### 🧪 Testing

- Added focused CLI schema and schema-check coverage for composite primary-key
  reporting, rechecked schema description Candid shape, and reran focused
  core/CLI validation plus clippy and diff hygiene for the observability and
  executor bridge slice.

## 0.162.8

### 🔧 Changed

- Moved forward-index mutation planning and accepted index-key builders onto
  scalar-or-composite `PrimaryKeyValue` row identity, keeping scalar
  `StorageKey` access only at explicit fail-closed bridge points.
- Updated unique-index preflight readers and index plan/read ports to return
  `PrimaryKeyValue`, so duplicate detection and accepted-index publication no
  longer assume scalar row identity.
- Changed commit index-delta materialization to rebuild index row identity from
  the planned `PrimaryKeyValue` instead of re-decoding scalar storage keys.
- Updated ordered key-stream witnesses and stream monotonicity checks to use
  scalar-or-composite row identity consistently.
- Moved covering projection primary-key reads, materialized projection decode,
  bytes terminals, materialized ranking, and kernel row decode through
  composite-aware data-key boundaries. Paths that still require scalar row-slot
  decoding now fail closed through the scalar bridge instead of using direct
  scalar accessors.

### 🧪 Testing

- Rechecked focused data-key, composite-covering, recovery, key-stream,
  workspace/core checking, core clippy, formatting, and diff hygiene coverage
  for the row-identity planning/executor boundary slice.

## 0.162.7

### 🔧 Changed

- Moved accepted schema snapshots and schema-store encoding onto ordered
  primary-key field IDs, preserving composite primary-key arity and field order
  in persisted schema metadata.
- Updated schema runtime descriptors and owned row-decode contracts to carry
  ordered primary-key names, kinds, and slot indices while keeping explicit
  scalar bridge accessors for execution paths that have not yet admitted
  composite row identity.
- Changed schema info and schema description surfaces to mark every accepted
  primary-key component and expose ordered primary-key names from accepted
  schema authority.
- Updated schema reconciliation, transition, and mutation publication paths to
  compare ordered primary-key field identities instead of only the first
  primary-key field.
- Moved schema fingerprints onto ordered generated primary-key fields so
  primary-key arity and component identity affect the commit/schema contract.
- Tightened schema-driven SQL admission so DDL primary-key-field drop checks and
  SQL UPDATE deterministic ordering use the full accepted primary-key field
  list.
- Moved schema index-rebuild staging and startup rebuild adapters onto
  scalar-or-composite `PrimaryKeyValue` row identity so DDL-created indexes can
  materialize composite primary-key suffixes.
- Updated index row witnesses to carry scalar-or-composite primary-key identity
  from the raw index key, leaving scalar `StorageKey` access as a fail-closed
  bridge for paths that have not admitted composite identity yet.
- Updated executor index scans and integrity diagnostics to rebuild/compare row
  identity from `PrimaryKeyValue` instead of assuming every index entry decodes
  to a scalar storage key.

### 🧪 Testing

- Added focused schema codec, schema info, runtime descriptor, DDL, SQL
  lowering, fingerprint, rebuild-staging, and index-entry witness coverage for
  ordered scalar-or-composite primary-key identity.

## 0.162.6

### 🔧 Changed

- Moved decoded data-store keys onto the scalar-or-composite primary-key value
  model, allowing raw data-key decode and bounds to preserve composite primary
  keys while existing scalar-only consumers fail closed.
- Extended index primary-key suffix decoding through the same
  composite-aware boundary so index entries can carry composite row identity
  without duplicating primary-key bytes in values.
- Opened derive support for `pk(fields = ["a", "b"])` composite declarations,
  generating public composite key types with key codec contracts and keeping
  composite keys external-only.
- Hard-cut `PrimaryKeyCodec` onto primary-key-owned encode errors instead of
  the old scalar storage-key encoder error.
- Tightened composite primary-key declaration validation for duplicate fields,
  maximum arity, and disallowed `Unit` components.

### 🧪 Testing

- Added derive and compile-fail coverage for composite primary-key declaration
  validation, generated key contracts, scalar-only fail-closed boundaries, and
  composite-aware raw key decoding.

## 0.162.5

### 🔧 Changed

- Split the compact key taxonomy into scalar `PrimaryKeyComponent` values and
  scalar-or-composite `PrimaryKeyValue` values, matching the 0.162 design shape.
- Tightened `CompositePrimaryKeyValue` construction so composite values require
  `2..=MAX_PRIMARY_KEY_FIELDS` components; `pk(fields = ["id"])` remains the
  scalar case and must not encode as a one-component composite.
- Moved `PrimaryKeyCodec` / `PrimaryKeyDecode` signatures onto
  `PrimaryKeyValue`, with scalar row routing still explicitly unwrapping only
  scalar components until `DecodedDataStoreKey` stores composite-aware identity.
- Added hidden facade re-exports for the primary-key value taxonomy so generated
  composite key structs have a stable codegen target in later 0.162 slices.
- Kept raw data and index store-key wrappers able to preserve composite
  primary-key suffix bytes without yet routing persisted rows through composite
  keys.

### 🧪 Testing

- Rechecked focused key-taxonomy coverage, workspace checking, core clippy, and
  diff hygiene for the trait/taxonomy bridge slice.

## 0.162.4

### 🔧 Changed

- Added explicit compact composite primary-key encoding and decoding support to
  the key taxonomy, including the `Composite` kind tag and component-count
  frame.
- Kept composite encoding on explicit side APIs for now, so persisted row
  encode/decode paths remain on the existing scalar primary-key bridge until the
  trait hard cut lands.
- Updated index-key decode error mapping so malformed composite key payloads
  fail closed when encountered at legacy scalar decode boundaries.

### 🧪 Testing

- Added focused composite encoding tests covering roundtrip, invalid component
  counts, `Unit` component rejection, nested composite rejection through scalar
  decode, trailing/error handling, and encoded ordering.

## 0.162.3

### 🔧 Changed

- Changed `icydb-schema-derive` primary-key nodes to retain ordered field-list
  metadata internally while keeping scalar runtime projection as an explicit
  bridge.
- Updated entity validation and generated model storage to walk ordered
  primary-key fields instead of hardcoding a single field assumption.
- Added a fixed-capacity `CompositePrimaryKeyValue` model in the core key
  taxonomy without changing persisted store-key encoding.
- Added generated composite primary-key type scaffolding for ordered
  primary-key metadata while keeping user-facing composite declarations gated.

### 🧪 Testing

- Added focused derive tests for ordered primary-key validation and generated
  key-type scaffolding, plus core key-taxonomy tests for fixed-capacity
  composite primary-key values.

## 0.162.2

### 🔧 Changed

- Added runtime `PrimaryKeyModel` metadata to `EntityModel`, giving generated
  entity models an ordered primary-key field list alongside the existing scalar
  primary-key projection.
- Updated generated entity-model output to emit ordered primary-key field
  references while preserving scalar runtime execution behavior.
- Re-exported the primary-key metadata model through the existing model facade
  so downstream generated code can use the new metadata shape without touching
  storage behavior.

### 🧪 Testing

- Added focused primary-key model tests for scalar and ordered field metadata,
  rechecked schema-derive entity model output, and validated the workspace,
  targeted clippy, formatting, and diff hygiene for the metadata bridge slice.

## 0.162.1

### 🔧 Changed

- Hard-cut the generated scalar primary-key attribute parser to the new formal
  field-list spelling. In this slice, `pk(fields = ["id"])` lowers to the
  existing scalar schema/runtime path while multi-field primary keys reject
  until the composite runtime/schema model lands.
- Moved schema primary-key declaration metadata to an ordered field-list shape,
  while keeping the current scalar runtime projection for existing execution
  code.
- Added compile-fail diagnostics for removed `pk(field = "id")`,
  comma-string `pk(fields = "id, name")`, empty `pk(fields = [])`, and
  composite `pk(fields = ["tenant_id", "local_id"])` declarations.

### 🧪 Testing

- Rechecked schema primary-key metadata tests, schema-derive primary-key parser
  tests, macro compile-fail coverage, workspace checking, schema/derive clippy,
  and diff hygiene for the primary-key syntax slice.

## 0.162.0

### 🔧 Changed

- Started the composite-primary-key release line with an implementation design
  that keeps accepted schema snapshots as runtime authority for primary-key
  arity, field order, durable field IDs, row slots, component kinds, and
  primary-key source.
- Defined the unified primary-key declaration direction for 0.162:

  ```rust
  #[entity(store = Store, pk(fields = ["id"]))]
  #[entity(store = Store, pk(fields = ["battle_id", "entity_id"]))]
  ```

- Documented the compact primary-key storage direction: data-store keys remain
  `EntityTag || EncodedPrimaryKey`, scalar keys keep scalar encoding, composite
  keys use a fixed-capacity runtime value model, and ordinary encode/decode
  paths must remain allocation-free.
- Clarified composite-primary-key constraints before implementation:
  composites are external-only, omitted composite `source` resolves to
  `external`, `Unit` is scalar-only, generated key structs are public `E::Key`
  types, and primary-key field facts duplicated in accepted snapshots must
  validate against the accepted field snapshot.

### ⚠️ Breaking

- Hard-cut generated index attributes to formal field lists parsed by Darling
  instead of IcyDB's old comma-string helper:

  ```rust
  index(fields = ["battle_id", "cell_id"])
  index(fields = ["tenant_id", "LOWER(email)"])
  ```

  The old spelling now rejects:

  ```rust
  index(fields = "battle_id, cell_id")
  ```

### 🧪 Testing

- Rechecked schema-derive and macro compile-fail coverage for formal generated
  index field-list parsing and diagnostics, plus workspace checking, derive
  clippy, formatting, and diff hygiene for the parser/design slice.
