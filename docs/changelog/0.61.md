# IcyDB 0.61

This document aggregates detailed release notes for the `0.61.x` minor line.

Patch numbering in active work follows the next release after the workspace
version in `Cargo.toml`. With the workspace currently at `0.61.3`, the active
follow-up work in this line belongs to `0.61.4`.

## 0.61.4 - 2026-03-20 - Shared Scalar Execute, SQL Dispatch, And Reverse-Relation Flattening

### Summary

The fourth `0.61` patch attacks the first three large duplicated symbol
families from the new `one_simple` / `ten_simple` audit matrix.

The first target was the unpaged scalar `LoadExecutor<E>::execute` entry path,
which the raw debug `twiggy monos` scan was still showing as one copy per
entity in `ten_simple`.

That slice moved the rows-only entry path onto one canister-shared structural
scalar execution helper. The typed `LoadExecutor<E>::execute` method now only
converts the structural page back into the final typed response at the outer
boundary.

The second target was the generated SQL query-lane callback path. After the
shared scalar execute cut, the next large duplicated family was still the
entity-typed SQL dispatch wrapper under `DbSession<C>`.

This patch then keeps flattening that SQL path in three steps:

- query routes now call one narrowed typed query helper
- explain routes now call one narrowed typed explain helper
- lowered query binding now shares one structural builder path before wrapping
  back into `Query<E>`
- explain plan/json rendering now shares one structural core and only leaves
  execution/global-aggregate fallback on the typed path

The third target was reverse-relation mutation preparation during commit
preflight. That path was still carrying one large typed helper family per
entity while deriving the mechanical reverse-index insert/delete set.

This patch pushes the heavy reverse-index mutation body onto structural source
authority and precomputed storage keys, leaving the typed shell to gather the
old/new relation-target sets and then call the shared mutation helper.

### Changed

- Moved the unpaged scalar rows entry path off the entity-generic load wrapper:
  - `LoadExecutor<E>::execute(...)` now converts `ExecutablePlan<E>` into a
    prepared structural load plan, calls one canister-shared scalar execution
    helper, and decodes the final structural page at the typed boundary
- Added one canister-shared scalar rows execution seam:
  - `execute_scalar_rows_for_canister(...)` in the load entrypoint root
  - `execute_prepared_scalar_rows_for_canister(...)` in the scalar entrypoint
    module
  - `prepare_scalar_route_runtime_for_canister(...)` for the shared scalar
    route/runtime preparation path
- Removed the old scalar-rows orchestrator mode and surface branch:
  - deleted `LoadMode::ScalarRows`
  - deleted the untraced scalar-rows `LoadExecutionSurface` variant
  - collapsed the remaining scalar/grouped orchestration surfaces onto traced
    paged payloads only
- Kept the typed boundary narrow:
  - the shared scalar rows path now returns `StructuralCursorPage`
  - only `page.into_entity_response::<E>()` remains on the typed outer edge
- Split generated SQL dispatch callbacks by lane:
  - `sql_dispatch` route tables now expose distinct `query` and `explain`
    callbacks instead of one combined query-lane callback
  - generated SQL route execution now chooses the narrowed callback from the
    parsed statement route before entity-typed execution begins
- Narrowed typed SQL session entrypoints:
  - replaced the combined `execute_lowered_sql_dispatch_query_lane::<E>(...)`
    path with `execute_lowered_sql_dispatch_query::<E>(...)` and
    `explain_lowered_sql_dispatch::<E>(...)`
  - split the typed SQL command execution helpers so query and explain paths
    no longer share one monomorphized entity wrapper
- Flattened lowered SQL query binding onto a shared structural builder:
  - `bind_lowered_sql_query_structural(...)` now owns the heavy lowered-select
    shape application before `Query::from_inner(...)` wraps the structural
    query back into the typed outer shell
  - the old duplicated `bind_lowered_sql_query` family no longer appears in
    the fresh raw debug `ten_simple` monos report
- Flattened explain plan/json rendering onto a shared structural core:
  - `render_lowered_sql_explain_plan_or_json(...)` now owns the shared
    structural plan/json explain rendering path
  - `explain_lowered_sql_dispatch_core(...)` owns the lane guard plus
    structural-first dispatch, leaving only the remaining execution/global
    aggregate fallback in `explain_lowered_sql_dispatch_typed_execution::<E>`
- Flattened reverse-relation mutation preparation onto structural authority:
  - added one structural `ReverseRelationSourceInfo` boundary for source path
    and tag authority
  - precomputed old/new source `StorageKey` values once at the typed boundary
    instead of inside the per-target mutation loop
  - moved reverse-index entry decode/update/re-encode work into one shared
    `prepare_reverse_relation_index_mutation_for_target(...)` helper
  - updated delete-time strong-relation validation to reuse the same
    structural reverse-index helpers so the reverse-index seam is consistent
    across validation and prepare paths

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-core -p icydb -p icydb-build -p icydb-testing-one-simple-fixtures -p icydb-testing-ten-simple-fixtures -p canister_one_simple -p canister_ten_simple`
- `cargo test -p icydb-core --lib --locked`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `WASM_CANISTER_NAME=one_simple WASM_PROFILE=debug bash scripts/ci/wasm-size-report.sh`
- `WASM_CANISTER_NAME=ten_simple WASM_PROFILE=debug bash scripts/ci/wasm-size-report.sh`
- `twiggy monos artifacts/wasm-size/one_simple.debug.dfx-built.wasm > /tmp/one_simple.debug.raw.monos.txt`
- `twiggy monos artifacts/wasm-size/ten_simple.debug.dfx-built.wasm > /tmp/ten_simple.debug.raw.monos.txt`
- `WASM_CANISTER_NAME=one_simple WASM_PROFILE=wasm-release bash scripts/ci/wasm-size-report.sh`
- `WASM_CANISTER_NAME=ten_simple WASM_PROFILE=wasm-release bash scripts/ci/wasm-size-report.sh`

### Measurement

The important result from this patch is that all three targeted duplicated
families moved, and the shipped simple-entity slope dropped materially.

Before this patch, the raw debug `ten_simple` monos report showed:

- `LoadExecutor<E>::execute`: about `132,240` total bytes across `10` copies
  at about `13,224` each
- `DbSession<C>::execute_sql_dispatch_query_lane_prepared`: about `97,830`
  total bytes across `10` copies at about `9,783` each
- `prepare_reverse_relation_index_mutations_for_source`: about `78,820` total
  bytes across `10` copies at about `7,882` each

After this patch:

- the duplicated `LoadExecutor<E>::execute` family no longer appears in the
  fresh raw debug `ten_simple` monos report
- the duplicated combined SQL query-lane family also no longer appears in the
  same fresh raw debug report
- the duplicated `bind_lowered_sql_query` family no longer appears in that
  report either
- the duplicated `prepare_reverse_relation_index_mutations_for_source` family
  no longer appears in that report either
- the remaining top project-owned duplicated families are now:
  - `explain_lowered_sql_dispatch_typed_execution`: about `65,420` bytes
    across `10` copies
  - `prepare_row_commit_for_entity_impl`: about `60,750` bytes across `10`
    copies

That means the first large load-executor wrapper, the first large SQL dispatch
wrapper, the lowered SQL query binder, and the reverse-relation mutation
preflight loop are all now off the top of the simple-entity duplication table.

The shipped `wasm-release` simple-entity slope moved in the right direction as
well.

Before `0.61.4`:

- `one_simple` shrunk wasm: `1,703,927`
- `ten_simple` shrunk wasm: `2,433,630`
- `one_simple -> ten_simple`: `729,703` bytes across `9` added entities
- shrunk wasm.gz slope: `29,585` bytes across `9` added entities

After the full `0.61.4` slice on the current tree:

- `one_simple` shrunk wasm: `1,709,003`
- `ten_simple` shrunk wasm: `2,381,022`
- `one_simple -> ten_simple`: `672,019` bytes across `9` added entities
- shrunk wasm.gz slope: `25,967` bytes across `9` added entities

So this patch improves the simple-entity incremental slope by:

- `57,684` raw shrunk bytes across `9` entities
- `3,618` shrunk `.wasm.gz` bytes across `9` entities

or about:

- `6,409` raw shrunk bytes per added simple entity
- `402` shrunk `.wasm.gz` bytes per added simple entity

So `0.61.4` now materially reduces the simple-entity floor, but it still does
not close it. The remaining top duplicated project-owned families have moved up
into commit preflight and the residual typed SQL explain fallback rather than
the earlier executor-entry and reverse-relation wrappers.

### Next Step

- attack the next duplicated simple-entity family from the fresh `ten_simple`
  raw debug monos report
- prioritize `prepare_row_commit_for_entity_impl` and the remaining typed
  explain fallback before going back to macro-only cleanup, since the
  simple-entity slope is still dominated by base per-entity runtime duplication

---

## 0.61.3 - 2026-03-20 - Release Cut For The `0.61.2` Checkpoint

### Summary

`0.61.3` is the already-shipped release cut for the `0.61.2` checkpoint.

This patch is the release cut for the `0.61.2` checkpoint. It does not add a
new macro-compression or runtime-flattening slice beyond the `0.61.2` work
already recorded below.

Its purpose is bookkeeping clarity: once the workspace version in
`Cargo.toml` moved to `0.61.3`, the deeper simple-entity flattening work
needed to move forward under `0.61.4` instead of staying mixed into the prior
shipped version.

### Changed

- no additional runtime or macro-output changes beyond the `0.61.2` checkpoint
- `0.61.3` now serves as the previous shipped boundary for the `0.61.x` line
- the next active follow-up work in this line is recorded under `0.61.4`

### Validation

- no additional validation beyond the `0.61.2` checkpoint

### Measurement

- no new wasm measurement belongs to `0.61.3`; the next measured slope change
  is recorded under `0.61.4`

### Next Step

- keep `0.61.3` as the prior shipped version and record the next deeper
  simple-entity flattening work under `0.61.4`

---

## 0.61.2 - 2026-03-20 - Shared Container Conversion Helpers And Audit Matrix Reset

### Summary

The third `0.61` patch moves another repeated generated-body family onto one
shared runtime helper seam and resets the wasm audit fixture matrix around
shared helpers under `testing/`.

This slice targets repeated generated container conversion for list, set, map,
and newtype wrapper types. Instead of re-emitting the same collection and map
conversion loops per generated schema type, the derive output now calls shared
helper functions in `traits`.

It also replaces the active wasm audit setup with:

- an empty `minimal` canister that measures the bare database surface with no
  entities
- `one_simple` and `ten_simple` canisters that repeat one thin entity shape
- `one_complex` and `ten_complex` canisters that repeat one richer entity shape
  with embedded records, payload enums, nested collection wrappers, validators,
  sanitizers, and relations

The repeated schema shapes now live behind shared helper macros in
`testing/wasm-fixtures`, so the audit fixtures themselves are thinner and the
measurement matrix is easier to evolve.

### Changed

- Added shared runtime helpers for structured `FieldValue` conversion:
  - `field_value_collection_to_value(...)`
  - `field_value_vec_from_value(...)`
  - `field_value_btree_set_from_value(...)`
  - `field_value_map_collection_to_value(...)`
  - `field_value_btree_map_from_value(...)`
- Added matching shared runtime helpers for generated `From` conversions:
  - `field_value_from_vec_into(...)`
  - `field_value_from_vec_into_btree_set(...)`
  - `field_value_from_vec_into_btree_map(...)`
  - `field_value_into(...)`
- Re-exported those helpers through the `icydb::traits` facade so generated
  code can call them directly without reaching through internal module paths
- Rewrote generated list `FieldValue` impls to use shared runtime helpers:
  - list wrappers now delegate list encode/decode to one shared runtime path
    instead of emitting their own item iteration loop bodies
- Rewrote generated set `FieldValue` impls to use shared runtime helpers:
  - set wrappers now delegate list encode plus duplicate-rejecting set decode
    to one shared runtime path instead of emitting their own loop bodies
- Rewrote generated map `FieldValue` impls to use shared runtime helpers:
  - map wrappers now delegate canonical key sorting, duplicate-key checks, and
    normalized decode to one shared runtime path instead of re-emitting that
    map conversion logic per generated type
- Rewrote generated list/set/map/newtype `From` impls to use shared runtime
  helpers:
  - list and set wrappers now delegate `Vec<I>` conversion to one shared
    runtime path instead of re-emitting `into_iter().map(Into::into).collect()`
    per type
  - map wrappers now delegate pair conversion to one shared runtime path
    instead of re-emitting the key/value conversion body per type
  - newtype wrappers now delegate `Into<T>` conversion to one shared runtime
    path instead of re-emitting the same single-field conversion body per type
- Added one shared wasm-audit fixture helper crate:
  - new crate `testing/wasm-fixtures` owns the repeated simple and complex
    entity-shape macros used by the active wasm audit canisters
  - simple and complex schema fixtures now reuse that shared owner instead of
    carrying the repeated entity-shape definitions in each schema crate
- Rebuilt the active wasm audit schema matrix:
  - `schema/minimal` now defines an empty store-only audit canister with no
    entities
  - new `schema/one_simple` and `schema/ten_simple` crates define the thin
    repeated entity-shape fixtures
  - new `schema/one_complex` and `schema/ten_complex` crates define the richer
    repeated entity-shape fixtures
- Rebuilt the active wasm audit canister matrix:
  - new `canisters/one_simple`, `canisters/one_complex`, `canisters/ten_simple`,
    and `canisters/ten_complex` crates now build those schema fixtures through
    the same generated SQL actor path as the existing audit canisters
  - PocketIC build wiring, DFX canister metadata, and the wasm size/audit
    scripts now recognize `minimal`, `one_simple`, `one_complex`,
    `ten_simple`, and `ten_complex` as the active default audit matrix

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-schema-derive -p icydb-core -p icydb`
- `cargo test -p icydb-schema-tests --locked`
- `cargo check -p icydb-testing-wasm-fixtures -p icydb-testing-minimal-fixtures -p icydb-testing-one-simple-fixtures -p icydb-testing-one-complex-fixtures -p icydb-testing-ten-simple-fixtures -p icydb-testing-ten-complex-fixtures -p canister_minimal -p canister_one_simple -p canister_one_complex -p canister_ten_simple -p canister_ten_complex -p icydb-testing-integration`
- `cargo test -p canister_minimal -p canister_one_simple -p canister_one_complex -p canister_ten_simple -p canister_ten_complex --lib --locked`
- `cargo run -p icydb-testing-integration --bin build_quickstart_canister --locked -- minimal`
- `cargo run -p icydb-testing-integration --bin build_quickstart_canister --locked -- one_simple`
- `cargo run -p icydb-testing-integration --bin build_quickstart_canister --locked -- one_complex`
- `cargo run -p icydb-testing-integration --bin build_quickstart_canister --locked -- ten_simple`
- `cargo run -p icydb-testing-integration --bin build_quickstart_canister --locked -- ten_complex`
- `cargo clippy --workspace --all-targets -- -D warnings`

### Measurement

The first five-canister matrix measurement was recorded after this reset.

- empty-db floor:
  - `minimal` shrunk wasm: `891,287` bytes
  - `minimal` shrunk wasm.gz: `285,707` bytes
- one-entity baselines:
  - `one_simple` shrunk wasm: `1,768,207` bytes
  - `one_complex` shrunk wasm: `1,882,148` bytes
- ten-entity baselines:
  - `ten_simple` shrunk wasm: `2,555,475` bytes
  - `ten_complex` shrunk wasm: `2,765,130` bytes

Two slope views matter:

- empty-db to ten-entity slope:
  - `minimal -> ten_simple`: `1,664,188` bytes total, about `166,419` bytes
    per simple entity
  - `minimal -> ten_complex`: `1,873,843` bytes total, about `187,384` bytes
    per complex entity
- one-entity to ten-entity incremental slope:
  - `one_simple -> ten_simple`: `787,268` bytes across 9 added entities,
    about `87,474` bytes per added simple entity
  - `one_complex -> ten_complex`: `882,982` bytes across 9 added entities,
    about `98,109` bytes per added complex entity

This is the first genuinely useful baseline for the reset matrix:

- the empty-db floor is much smaller than the old entity-bearing `minimal`
- the simple and complex slopes are now separated cleanly
- complex entities still carry a meaningfully larger per-entity slope than
  simple ones, even after the recent helper lifts

### Next Step

- use these new `minimal`, `one_simple`, `one_complex`, `ten_simple`, and
  `ten_complex` numbers as the baseline for the next `0.61` measurement
  checkpoint
- prioritize the remaining generated emitters that explain the complex-entity
  premium over the simple-entity slope
- re-scan the remaining generated enum and schema/model emitters now that field
  walks and container conversions are on shared helper seams

---

## 0.61.1 - 2026-03-20 - Sanitize, Validate, And Schema Compression

### Summary

The second `0.61` patch extends the shared macro-compression seam beyond model
and traversal output.

This patch:

- lifts generated entity and record sanitization onto shared runtime field
  descriptor loops
- lifts generated entity and record validation onto the same runtime seam
- reshapes schema AST emission so repeated field/index/validator/sanitizer
  slices are lowered through local const tables
- remeasures the controlled `minimal` / `twenty` wasm pair to check whether
  those early compression slices are paying off

### Changed

- Lifted entity and record sanitization onto the runtime field-table seam:
  - added `SanitizeFieldDescriptor<T>` plus shared
    `drive_sanitize_fields(...)` helpers in the core visitor module
  - generated entity and record `SanitizeAuto` impls now build one static
    descriptor table and run one shared loop over it instead of emitting inline
    per-field issue-path blocks
- Lifted entity and record validation onto the same runtime seam:
  - added `ValidateFieldDescriptor<T>` plus shared
    `drive_validate_fields(...)` helpers in the core visitor module
  - generated entity and record `ValidateAuto` impls now build one static
    descriptor table and run one shared loop over it instead of emitting inline
    per-field issue-path blocks
- Removed now-dead derive helpers left behind by the loop lift:
  - `FieldWalkBinding` no longer carries the old `self_ref` / `self_mut`
    helpers once entity/record field-walk generation moved fully onto
    receiver-based descriptor helpers
- Compressed generated schema AST emission:
  - `FieldList::schema_part()` now lowers field slices through one local
    `__FIELDS` const table before constructing `schema::node::FieldList`
  - `Item::schema_part()` now lowers validator and sanitizer slices through
    local `__VALIDATORS` / `__SANITIZERS` const tables before constructing
    `schema::node::Item`
  - `Entity::schema_part()` now lowers index slices through one local
    `__INDEXES` const table before constructing `schema::node::Entity`
  - together, these changes reshape generated schema constants away from one
    larger nested literal tree and toward a more table-oriented constant form

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-schema-derive -p icydb-core -p icydb`
- `cargo test -p icydb-schema-tests --locked`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `bash scripts/ci/wasm-size-report.sh`

### Measurement

A fresh `minimal` / `twenty` `wasm-release` measurement was recorded after
these early `0.61` slices.

- `minimal` shrunk wasm: `1,768,823` bytes
- `twenty` shrunk wasm: `3,413,770` bytes
- shrunk wasm delta: `1,644,947` bytes
- added entities: `19`
- estimated shrunk wasm per added entity: `86,576` bytes
- estimated shrunk gz wasm per added entity: `2,841` bytes

The honest read is that these early macro-compression slices did not yet move
wasm size meaningfully. They reduced generated structure and established a
cleaner runtime-loop seam, but the remaining repeated per-entity output is
still large.

### Next Step

- re-scan the remaining generated per-entity emitters now that traversal,
  sanitize, and validate all share one runtime loop seam
- decide whether `FieldValue`, deeper schema/model duplication, or another
  generated metadata family is now the next larger source of per-entity growth

---

## 0.61.0 - 2026-03-20 - Initial Entity Macro Compression

### Summary

The opening `0.61` patch starts the next wasm-focused macro cleanup after
`0.60` removed generated view DTOs.

This patch does not attempt a full descriptor-runtime rewrite. Instead, it
takes the first smaller structural compression steps that match the current
macro code:

- entity runtime model emission no longer fans out into one generated
  `__MODEL_FIELD_*` constant per field
- generated entity and record traversal now runs through a shared
  field-descriptor loop instead of spelling out per-field `drive` bodies
  inline
- visit/sanitize/validate generation now shares one derive-side field-walk
  lowering path instead of each module rebuilding that field metadata shape on
  its own

### Changed

- Reduced runtime model constant fanout in entity macro output:
  - entity generation now emits one direct `__MODEL_FIELDS` array instead of
    one per-field `__MODEL_FIELD_*` constant plus an array wrapper
  - runtime `FieldModel` lowering now lives behind one derive-side helper in
    the inherent-model path
  - this keeps the emitted entity-model authority the same while shrinking the
    generated constant surface
- Lifted entity and record traversal onto a runtime field-descriptor loop:
  - added `VisitableFieldDescriptor<T>` plus shared
    `drive_visitable_fields(...)` helpers in the core visitor module
  - generated entity and record `Visitable` impls now build one static
    descriptor table and run one shared traversal loop over it
  - field-specific traversal still uses typed generated helper functions per
    field, but the repeated inline `drive` call-site bodies are gone
- Introduced one shared derive-side field-walk lowering layer:
  - added a shared field-walk helper module used by entity/record generation
  - `visitable`, `sanitize`, and `validate` now consume that shared lowering
    path instead of each rebuilding field-name/path/member-access metadata
    independently
  - this is primarily a macro-output and maintainability cleanup, but it also
    establishes the exact seam needed for later runtime-loop work
- Marked the `0.61` design line active:
  - the `0.61` design doc now reflects the post-`0.60` reality and is marked
    `In Progress`

### Validation

- `cargo fmt --all`
- `cargo check -p icydb-schema-derive -p icydb-core -p icydb`
- `cargo test -p icydb-schema-tests --locked`
- `cargo clippy --workspace --all-targets -- -D warnings`

### Measurement

No fresh wasm-size rerun was recorded for this opening patch.

The intent of this first slice was to reduce generated macro surface and land
the runtime-loop seam before measuring again.

### Next Step

- lift sanitize and validate onto the same runtime descriptor seam as
  traversal
- then run a fresh `minimal` / `twenty` measurement to see whether the field
  loop family is actually paying off
