# IcyDB 0.213: Schema Authority And Application Separation

## 0.213.17

### 📝 Summary

- Exact existing-head proposals can remove one generated secondary index per
  exactly empty entity by immutable source identity.
- Proposal application and SQL DDL now share one catalog-native dense index
  removal derivation.

### 🔧 Changed

- Application preflight requires exact zero row cardinality and a bounded,
  fallible proof that the entity owns no current physical user-index key.
- Malformed keys, stale entity-local index state, non-ready index storage,
  oversized observation, live activation/candidate state, nonempty entities,
  and independently owned indexes reject before receipt or schema publication.
- Unique-index removal retires its paired accepted constraint. Retained stable
  logical index IDs remain unchanged while only physical ordinals compact.
- Source bindings remove the exact selected index identity, and grouped
  removal transport advances the entity schema version once.
- Empty-domain publication performs no physical index mutation and reuses the
  existing atomic schema-plus-receipt marker boundary.
- Accepted-bundle, entity snapshot, row, index, relation, journal, cursor,
  receipt, and marker codec shapes are unchanged.

### 🧪 Testing

- Focused coverage exercises immutable source selection, public publication
  and exact replay, nonempty rejection without a receipt, entity/kind-scoped
  physical proof, unique-constraint retirement, and logical-ID-preserving
  ordinal compaction.
- The complete focused schema-application group, SQL index-drop derivation,
  core no-default compilation, warnings-denied all-target/all-feature Clippy,
  schema/model, layer-authority, mutation-atomicity, formatting, and diff
  hygiene checks pass.
- Full repository validation remains part of the human-owned push workflow.

### 📊 Audit

- Eleven Rust source/test files change by approximately `+619/-91` lines
  before documentation. The application lane grows, while SQL and proposal
  application converge on one dense-removal authority and one bounded physical
  observer.
- Raw non-gzipped `sql_perf` Wasm is 5,351,653 bytes, 497 bytes (+0.0093%)
  above `0.213.16`; its SHA-256 is
  `06b28eaed0ee5e20c31d91e927ab980b46a228f3a086ba63e53ac761429d9e34`.
- The canonical shrunk artifact is 4,984,509 bytes; deterministic raw gzip is
  1,685,463 bytes.

## 0.213.16

### 📝 Summary

- Exact existing-head proposals can remove one generated field per exactly
  empty entity by immutable source identity.
- Dense field-ID and row-slot reassignment now has one catalog-native owner
  shared by schema application and SQL DDL.

### 🔧 Changed

- Maintained cardinality proves emptiness after recovery and before either the
  durable application receipt or accepted candidate can publish.
- The dense transition advances the row-layout version, removes the retired
  field's not-null owner, and remaps retained primary-key, check, index,
  relation, field-ID, row-slot, and source-binding identities together.
- Primary-key fields, dependent structural owners, live activation/candidate
  state, exhausted layout identity, nonempty entities, and a second
  same-entity field removal reject before candidate construction.
- The entity schema version advances once and the store revision plus receipt
  publish through the existing marker-owned multi-store boundary.
- Named types made unused by the removal remain accepted because proposal
  absence continues to mean unchanged.
- No row is rewritten, and accepted-bundle, row, index, relation, journal,
  cursor, receipt, and marker codec shapes are unchanged.

### 🧪 Testing

- Focused coverage exercises public empty-domain publication and exact replay,
  nonempty fail-before-receipt rejection, dense source-binding remapping, and
  index-dependent rejection.
- The wider schema-application regression group and the existing SQL
  non-trailing `DROP COLUMN` path pass through the shared derivation.
- Core no-default compilation, warnings-denied all-target/all-feature Clippy,
  schema/model, layer-authority, mutation-atomicity, formatting, and diff
  hygiene checks pass.
- Full repository validation remains part of the human-owned push workflow.

### 📊 Audit

- Eleven Rust source/test files change by approximately `+619/-134` lines
  before documentation. Application orchestration grows, while the former
  SQL-only dense-remap logic becomes one shared catalog-native authority.
- Raw non-gzipped `sql_perf` Wasm is 5,351,156 bytes, 9,451 bytes (+0.1769%)
  above `0.213.15`; its SHA-256 is
  `550d51869e6ab13b31fc0126787e5029eff05d2117e3217a6a429015a5772e2a`.
- The canonical shrunk artifact is 4,984,022 bytes; deterministic raw gzip is
  1,689,120 bytes.

## 0.213.15

### 📝 Summary

- Generated accepted checks whose historical proof exceeds one bounded page
  now return a durable pending application job.
- Continuation reuses the canonical 0.211 Forward/Verify activation runner
  instead of introducing proposal-specific progress or check semantics.

### ➕ Added

- Core and facade sessions expose
  `continue_schema_application(job_id, acknowledged_receipt)`.
- Public progress reports Started, Advanced, Findings, Restarted, Applied, or
  Failed while the durable application receipt remains the outcome authority.

### 🔧 Changed

- A clean 256-row direct prefix over a journaled store publishes the generated
  check as `EnforcingNewWrites` together with its pending application receipt.
- Finding pages retain their exact sequence until acknowledged. Missing or
  incorrect acknowledgements replay the same bounded page without rescanning.
- Promotion and application-receipt finalization remain separate atomic marker
  boundaries. Retrying after interruption between them finalizes the receipt
  from the already accepted check.
- Lost terminal responses replay by deterministic job identity without
  re-entering validation.
- The accepted bundle supplies store, entity, path, expression, and row
  authority directly; generated runtime metadata is not a fallback.
- This bounded lane continues to reject volatile stores and proposals with
  more than one non-empty generated-check proof.

### 🧪 Testing

- Focused coverage exercises direct-to-pending admission, exact proposal
  replay, Forward/Verify completion, acknowledged finding replay, repair,
  promotion-before-receipt interruption, and terminal replay.
- The wider schema-application regression group and SQL direct-`ADD CHECK`
  path pass through the maintained evaluator and activation machinery.
- Core no-default compilation, core and facade warnings-denied Clippy,
  architecture guards, formatting, and diff hygiene pass.
- Full repository validation remains part of the human-owned push workflow.

### 📊 Audit

- Ten Rust source/test files change by approximately `+924/-122` lines before
  documentation. The new orchestration retains one checkpoint, receipt,
  proof, promotion, and semantic authority.
- Raw non-gzipped `sql_perf` Wasm is 5,341,705 bytes, 585 bytes (+0.0110%)
  above `0.213.14`; the canonical shrunk artifact is 4,974,997 bytes.
- The slice adds a public progress surface but does not change accepted-schema,
  application-record, validation-job, row, index, relation, journal, cursor,
  or marker codecs, and adds no compatibility path.

## 0.213.14

### 📝 Summary

- Extends generated accepted-check addition from exactly empty entities to
  non-empty historical domains that can be proved completely within one
  bounded validation page.
- Reuses the canonical 0.211 activation evaluator and scanner instead of
  introducing proposal-specific check semantics.

### 🔧 Changed

- Existing-head proposal lowering first reserves the generated check's final
  accepted identity as an unpublished activation.
- Application preflight promotes that activation inside the unpublished
  candidate only after the exact historical scan completes cleanly.
- Empty entities continue to use maintained exact cardinality without a scan.
- A proposal may consume at most one non-empty direct proof. Proofs exceeding
  256 rows remain fail-closed until the durable pending-receipt protocol is
  connected.
- Constraint violations, malformed rows, unavailable cardinality, and bounded
  scan exhaustion reject before schema or receipt publication.
- Ordinary structural row contracts retain borrowed static entity paths; only
  proposal validation owns its dynamic path.

### 🧪 Testing

- Focused application tests cover clean non-empty publication and replay,
  typed constraint violation, malformed persisted rows, and one-page budget
  exhaustion.
- Failure cases prove the accepted schema remains unchanged and no durable
  application receipt is created.
- The existing SQL direct-`ADD CHECK` regression passes through the same
  canonical verifier.
- Core no-default compilation, all-target/all-feature warnings-denied Clippy,
  repository architecture guards, formatting, and diff hygiene pass.
- Full repository validation remains part of the human-owned push workflow.

### 📊 Audit

- The slice changes eight Rust source/test files by approximately `+444/-79`
  lines before documentation. It adds proposal orchestration while retaining
  one accepted evaluator, scanner, and promotion authority.
- Raw non-gzipped `sql_perf` Wasm is 5,341,120 bytes, 80 bytes (+0.0015%)
  above `0.213.13`; the canonical shrunk artifact is 4,974,622 bytes.
- No public API, accepted-schema codec, row format, index/relation format,
  journal, cursor, marker, or compatibility path changed.

## 0.213.13

### 📝 Summary

- Adds the bounded direct-addition lane for generated accepted checks over
  entities with an exactly empty historical domain.
- Keeps non-empty validation reserved for the existing resumable activation
  architecture rather than publishing after a partial scan.

### ➕ Added

- Existing-head proposals can allocate a generated check's accepted
  `ConstraintId` and immutable source binding in one catalog-native candidate.
- Accepted constraint IDs remain entity-local, allowing independent entities
  to use the same local ID without weakening uniqueness inside either entity.

### 🔧 Changed

- Application preflight consults the maintained exact entity cardinality after
  recovery and publishes only when it is `Some(0)`.
- Non-empty or malformed cardinality rejects before receipt persistence or
  accepted-schema publication, leaving the accepted allocator unchanged.
- Future writes use the existing accepted final-after-image check gate; no
  second evaluator, activation runner, or generated-model fallback was added.

### 🧪 Testing

- Focused source-binding, existing-head lowering, cardinality, and journaled
  application tests cover entity-local identity, exact replay, empty-domain
  publication, and fail-closed non-empty rejection.
- Core no-default compilation, all-target/all-feature warnings-denied Clippy,
  schema/model, layering, atomicity, formatting, and diff-hygiene checks pass.
- Full repository validation remains part of the human-owned push workflow.

### 📊 Audit

- The slice changes four Rust source/test files by approximately `+387/-52`
  lines; runtime structure grows modestly without adding another semantic or
  durability owner.
- Raw non-gzipped `sql_perf` Wasm is 5,341,040 bytes, 437 bytes (+0.0082%)
  above `0.213.12`; the canonical shrunk artifact is 4,974,708 bytes.
- No public API, row layout, physical index/relation state, journal, cursor,
  marker, compatibility path, or persisted-codec version changed.

## 0.213.12

### 📝 Summary

- Adds the first explicit existing-head removal lane for generated accepted
  checks.
- Keeps removal catalog-native and metadata-only rather than approximating
  physical or independently owned transitions.

### 🗑️ Removed

- Exact schema proposals may remove generated accepted checks by immutable
  entity and constraint source keys.
- The accepted check and its source binding disappear from the same candidate,
  while the constraint allocator retains its high-water mark so the retired ID
  cannot be reused.

### 🔧 Changed

- Removal-only proposals advance the accepted entity schema version and store
  revision exactly once.
- Combining check removal with another admitted metadata change still advances
  the entity schema version only once.
- Entity and field identity, field definitions, row layout, historical fill,
  indexes, relations, and physical generations remain unchanged when removal
  is the only requested change.
- SQL-DDL-owned checks and entities with live activation or candidate state
  remain under their existing lifecycle owners and reject this generated
  removal lane.
- Field, index, relation, named-type, and entity removals continue to reject
  until their physical or dependency-owning transitions participate.

### 🧪 Testing

- Focused catalog, existing-head lowering, and journaled public-application
  tests cover allocator retirement, owner mismatch, removal-only proposals,
  combined default reconciliation, physical-removal rejection, atomic
  publication, and durable exact receipt replay.
- Core no-default compilation, all-target/all-feature warnings-denied Clippy,
  schema/model, layering, atomicity, formatting, and diff-hygiene checks pass.
- Full repository validation remains part of the human-owned push workflow.

### 📊 Audit

- The slice changes four Rust source/test files by approximately `+679/-49`
  lines; runtime complexity grows modestly around one source-resolved metadata
  transition without adding another constraint authority.
- Raw non-gzipped `sql_perf` Wasm is 5,340,603 bytes, 596 bytes smaller than
  `0.213.11`; the canonical shrunk artifact is 4,974,267 bytes.
- No public API, query execution path, row/index/relation storage, journal,
  cursor, marker, compatibility path, physical migration, or persisted-codec
  version changed.

## 0.213.11

### 📝 Summary

- Adds the first metadata-only rename lane for exact existing-head schema
  proposals.
- Keeps immutable source bindings and accepted structural identity stable
  while editable application names change.

### 🔧 Changed

- Entity names, generated top-level field names, enum type and unit-variant
  names, and composite type paths now reconcile through their accepted source
  bindings.
- Generated indexes are re-proved against the accepted snapshot and rebuilt
  against renamed field/catalog metadata while preserving logical IDs,
  ordinals, and physical generations.
- Accepted checks continue to use field IDs and are verified against the
  renamed candidate without introducing another expression owner.
- Row layouts, field slots, historical fills, relation identities, constraint
  identities, and source bindings remain unchanged.
- Record-member renames remain rejected because their names are embedded in
  canonical record values and require a bounded row and derived-state rewrite.
- Generated-field renames on entities carrying SQL-DDL-owned index metadata
  remain rejected until the catalog-native transition can update that
  independently owned metadata safely.

### 🧪 Testing

- Focused lowering and journaled-session tests cover entity and indexed-field
  renames, enum and variant renames, composite type-path renames, stable
  identity/layout preservation, and fail-closed record-member rename
  rejection.
- Core no-default compilation, all-target/all-feature warnings-denied Clippy,
  schema/model, layering, atomicity, formatting, and diff-hygiene checks pass.
- Full repository validation remains part of the human-owned push workflow.

### 📊 Audit

- The slice changed four Rust source/test files by approximately
  `+857/-169` lines.
- Raw non-gzipped `sql_perf` Wasm is 5,341,199 bytes, 493 bytes smaller than
  `0.213.10`; the canonical shrunk artifact is 4,974,824 bytes.
- No public API, row layout, index/relation storage, journal, cursor, marker,
  or persisted-codec version changed.

## 0.213.10

### 📝 Summary

- Connects the first non-empty existing-head schema-application lane.
- Allows exact source-addressed proposals to change future insert defaults
  while accepted identity and historical row meaning remain frozen.

### 🔧 Changed

- Every declared entity, field, named type, enum variant, record member, index,
  relation, and check is resolved through accepted source bindings and checked
  against current accepted semantics before candidate construction.
- Exact repeated declarations produce durable no-op receipts.
- Default-only candidates advance schema revision while preserving source
  bindings, row layout, historical fill, physical generations, SQL-DDL-owned
  trailing state, and structural constraint identity.
- Existing-head publication now uses the actual prior store revision and does
  not apply the empty-store precondition reserved for initial publication.

### 🧪 Testing

- Focused application tests cover exact no-op replay, future default removal,
  structural-owner preservation, and fail-before-receipt rejection of an
  unsupported rename.
- Core no-default compilation, all-target/all-feature warnings-denied Clippy,
  schema/model, layering, atomicity, dependency, durability, formatting, and
  diff-hygiene checks pass.
- Full repository validation remains part of the human-owned push workflow.

### 📊 Audit

- The slice changed five Rust source/test files by approximately
  `+1,046/-87` lines.
- Raw non-gzipped `sql_perf` Wasm is 5,341,692 bytes, 22 bytes smaller than
  `0.213.9`; the strict application-only closure verifier adds no retained code
  to that canister subject.
- No public API, row layout, index, relation, journal, cursor, marker, or
  persisted-codec version changed.

## 0.213.9

### 📝 Summary

- Gives every accepted record member a stable store-local identity bound to
  its immutable member source key.
- Establishes the identity foundation required for later existing-head member
  rename reconciliation without moving structural authority out of the
  accepted composite catalog.

### 🔧 Changed

- Record members now persist nonzero `CompositeFieldId` values independently
  of editable display-name order.
- Source bindings close exactly over every record member while tuple and
  newtype composites reject member bindings.

### ⚠️ Breaking

- Accepted bundle bytes are hard-cut to the current member-identity shape.
  Development stores written through `0.213.8` must be recreated; no fallback
  decoder or translation path remains.

### 🧪 Testing

- Focused composite-codec, source-binding closure, accepted-bundle wire, and
  initial named-type lowering checks pass.
- Core all-target/all-feature warnings-denied lint, core/facade no-default
  compilation, schema/model, layering, atomicity, dependency, durability,
  formatting, and diff-hygiene checks pass.
- Full repository validation remains part of the human-owned push workflow.

## 0.213.8

### 📝 Summary

- Extends the catalog-native initial schema-application path from scalar-only
  proposals to exact named enum and composite definitions.
- Keeps accepted value catalogs and immutable source bindings as the sole
  runtime authority; generated models remain outside application lowering.

### ➕ Added

- Added deterministic store-local lowering for the complete reachable closure
  of enums, records, newtypes, lists, sets, maps, and tuples.
- Added source-keyed enum-variant binding and enum-default encoding through
  the accepted enum catalog.
- Added catalog-derived nested record leaves to initial entity snapshots.

### 🔧 Changed

- Scalar-only proposals now use the same empty-definition catalog builder as
  named proposals; the obsolete production empty-enum constructor was removed.
- Each store freezes only the named definitions reachable from its assigned
  entities while shared definitions receive independent store-local accepted
  identities.
- Initial named fields use catalog-backed structural decoding. Existing-head
  mutations, removals, pending receipts, and activation reuse remain rejected.

### 🧪 Testing

- Focused lowering covers every named shape, transitive record-to-enum
  closure, source-keyed enum defaults, multi-store reachability, and the real
  journaled public application boundary.
- Core all-target/all-feature warnings-denied lint, core/facade no-default
  compilation, schema/model, layering, atomicity, dependency, durability,
  formatting, and diff-hygiene checks pass.
- Full repository validation remains part of the human-owned push workflow.

### 📦 Compatibility

- No public API or persisted-codec version changes. This extends which
  current-form initial proposals can be accepted and adds no legacy reader,
  alias, or generated-model fallback.

## 0.213.7

### 📝 Summary

- Aligns one test-only multi-store schema-publication export with the
  SQL-gated reconciliation tests that consume it.
- Changes no production schema-application behavior or persisted format.

### 🩹 Fixed

- No-default test compilation no longer reports the plural accepted-candidate
  publication helper as an unused import.

### 🧪 Testing

- Focused no-default test compilation, all-target/all-feature warnings-denied
  core lint, formatting, and diff hygiene pass.
- Full repository validation remained part of the human-owned push workflow.

## 0.213.6

### 📝 Summary

- Connects the first public schema-application path end to end for initial
  scalar database proposals.
- Keeps accepted candidates, source identity, marker publication, and durable
  receipt replay under their existing canonical owners.

### ➕ Added

- Added `DbSession::apply_schema` to the core and public facade.
- Added one catalog-native initial-proposal lowerer for exact scalar fields,
  row layouts, insert defaults, generated and managed write policy, secondary
  indexes, restrictive relations, accepted checks, and immutable entity,
  field, index, relation, and check source bindings.
- Added immutable-source-key check-expression binding through the maintained
  accepted check language and canonical renderer.

### 🔧 Changed

- Application now revalidates the proposal digest, target database, target
  stores, and optimistic accepted head before constructing candidates.
- Exact retries return the durable receipt. Reusing the same submission
  identity with another digest or prior head returns a typed conflict.
- All store-local candidates and the terminal application receipt publish
  through one commit marker. Receipt-only no-ops use the same durable boundary.
- Initial publication requires empty row and index stores and a ready index
  lifecycle. A dirty or incomplete derived store rejects before schema or
  receipt mutation.

### ⚠️ Breaking

- Named types, removals, and structural transitions over an existing accepted
  head remain unsupported and fail before mutation.
- No generated-model reconstruction, compatibility adapter, partial
  publication, or alternate activation path is retained.

### 🧭 Persistence

- This patch introduces no new persisted format beyond the current-form
  accepted source bindings, commit-marker effect, and application record
  established earlier in 0.213.
- Accepted schema, rows, indexes, relations, journals, cursors, and receipt
  records retain their 0.213.5 formats.

### 📊 Audit

- The maintained `sql_perf` subject is 5,333,126 raw non-gzipped Wasm bytes,
  840 bytes (+0.0158%) above 0.213.5 and 175,644 bytes (+3.4056%) above the
  0.213 opening baseline.
- Its raw SHA-256 is
  `acccdc28af3196a72487fe8f39133a8cb4895c8668b9c4f61fdeee714aa954e3`.
- The slice touches 17 Rust source/test files at approximately `+1,850/-27`
  lines before documentation. Runtime structure grows through one real
  lowerer and ingress, while proposal lowering, accepted publication, and
  receipt replay each retain one authority.

### 🧪 Testing

- Focused tests cover scalar defaults, indexes, accepted checks, source
  bindings, named-type rejection, exact replay, conflicting reuse, real
  journaled initial application, dirty-derived-state rejection, and atomic
  candidate-plus-receipt publication.
- Core and facade warnings-denied lint, no-default compilation, formatting,
  diff hygiene, and schema/model, layer-authority, mutation-atomicity,
  dependency, and durability guards pass.
- Full repository validation remains part of the human-owned push workflow.

### 🧭 Follow-up

- Existing-head and named-type lowering, pending receipts, and reuse of the
  0.211 activation jobs remain the unfinished Patch 2B work.

## 0.213.5

### 📝 Summary

- Establishes the durable idempotency and result protocol required by
  database-scoped schema application.
- Keeps receipts subordinate to the existing commit marker and 0.211
  activation architecture rather than creating a second publication or job
  engine.

### ➕ Added

- Added public opaque schema-change job identity, terminal/pending outcome,
  compact typed failure, and durable receipt projections.
- Added guarded receipt lookup by exact database identity and immutable
  submission key.
- Added one bounded database-wide record owner for canonical receipt and
  pending-activation state. Records are limited to 64 KiB, pending records to
  512 canonically ordered activation identities, and the store to 64 retained
  submissions.

### 🔧 Changed

- A commit marker may now own one exact schema-application record
  compare-and-replace effect.
- Marker admission preflights capacity and the expected prior record before
  opening the durable commit window.
- Normal apply and recovery accept only the expected prior bytes or the
  already-applied final bytes. Recovery verifies the final record before
  marker clearance.
- Pending receipts can transition only to `Applied` with their exact reserved
  candidate head or to a typed `Failed` state. Terminal receipts are
  immutable, no-op receipts retain the prior head exactly, and all persisted
  diagnostic identities must be registered current-form values.

### 🧭 Persistence

- Added the first current-form schema-application record format: one
  checksummed stable map in a restricted region after the existing bounded
  commit-control frame.
- The existing stable allocation identity is reused without overlapping the
  marker frame; no new canister memory ID is introduced.
- The transient commit-marker payload gains the optional exact record effect.
  Superseded in-flight development marker bytes fail current-form decoding;
  no compatibility decoder or translation path is retained.
- Accepted schema, row, index, relation, journal, cursor, database-control
  envelope, and 0.211 validation-job formats are unchanged.

### 📊 Audit

- The maintained `sql_perf` subject is 5,332,286 raw non-gzipped Wasm bytes,
  90,712 bytes (+1.7307%) above 0.213.4 and 174,804 bytes (+3.3897%) above the
  0.213 opening baseline.
- Its raw SHA-256 is
  `25b861c1fb3d8d93fbdcdc0ee3519165a9418d5a8a4501a26e419f05eaaf069c`;
  the canonical shrunk artifact is 4,966,388 bytes and deterministic raw gzip
  is 1,679,676 bytes.
- The raw artifact is 71,655 bytes above the original +2.00% review line. The
  overage is retained as the reviewed cost of the planned durable receipt,
  codec, marker/recovery, and public projection capability rather than hidden
  behind shrunk or gzip measurements.
- The implementation touches 15 Rust source/test files at approximately
  1,580 net lines before documentation, including roughly 550 focused
  test/accessor lines. Runtime structure becomes materially more complex
  through one bounded durable owner and one marker effect, but adds no second
  application protocol, publication owner, or activation runner.

### 🧪 Testing

- Focused coverage exercises canonical encoding and checksum failure,
  idempotent insert/replacement, compare conflicts, terminal immutability,
  candidate-head binding, bounded capacity, reopen, marker round-trip, and
  interrupted recovery.
- All-target, all-feature warnings-denied Clippy for `icydb-core` and `icydb`,
  native and Wasm no-default compilation, formatting, diff hygiene, and the
  dependency, generated-build, schema/model, layer-authority,
  mutation-atomicity, and durability-documentation guards pass.
- Full repository validation remains part of the human-owned push workflow.

### 🧭 Follow-up

- Catalog-native proposal lowering must construct these exact record effects.
  The later application-wiring slice will publish them atomically with
  multi-store candidates and resume the existing 0.211 activation owners.

## 0.213.4

### 📝 Summary

- Makes SQL DDL participate in the same immutable source-identity model that
  catalog-native schema proposals will consume.
- Keeps editable SQL names separate from store-local accepted identity without
  adding a second schema catalog or a compatibility backfill.

### 🔧 Changed

- A newly accepted SQL-owned field, secondary index, or `CHECK` constraint now
  receives an immutable source key derived from its entity and monotonic
  accepted publication revision.
- Existing keys follow their structural owner through field rename, dense
  field-ID reassignment, check and index activation, and final promotion.
- Drop and activation-abort transitions remove the corresponding binding in
  the same accepted-schema publication as the structural owner.
- Dense field compaction resolves unchanged-name lineage before considering a
  same-ID rename, preventing a removed field's key from transferring to the
  different field that inherits its physical ID.
- Objects accepted before their maintained admission path supplied immutable
  authorship remain unbound. The release does not infer identity from an
  editable name or reconstruct historical bindings.

### 🧭 Persistence

- The source-binding codec and accepted revision-bundle envelope remain
  unchanged.
- Entity snapshots, rows, indexes, relations, journals, cursors, and commit
  markers retain their existing formats.
- New SQL-DDL bindings participate in the accepted semantic fingerprint as
  part of the same candidate revision that creates their structural owner.

### 📊 Audit

- The maintained `sql_perf` subject is 5,241,574 raw non-gzipped Wasm bytes,
  24,324 bytes (+0.4662%) above 0.213.3 and 84,092 bytes (+1.6305%) above the
  0.213 opening baseline. It remains below the frozen +2.00% review budget.
- Its raw SHA-256 is
  `f9e9fd1ebf50e864a07357d1ba5ba24b87f20fd9e766ee301d06c186ab967636`;
  the canonical shrunk artifact is 4,882,816 bytes.
- The implementation changes six production, test, and structural-guard files
  at approximately 429 net lines before documentation. Runtime structure
  becomes moderately more complex inside the existing source-binding owner
  but adds no second catalog, proposal lowerer, receipt type, or application
  entry point.

### 🧪 Testing

- Focused all-feature coverage exercises field creation, non-trailing dense
  field removal, field rename, direct and activating checks, check abort,
  index creation and drop, and marker-authorized recovery.
- All-target, all-feature warnings-denied `icydb-core` Clippy, native
  no-default compilation, formatting, diff hygiene, schema/model authority,
  layer authority, mutation atomicity, and durability-documentation guards
  passed for the release slice.
- Full repository validation remains part of the human-owned push workflow.

### 🧭 Follow-up

- Catalog-native proposal lowering, durable idempotent receipts, the public
  application caller, and reuse of 0.211 activation jobs remain the coupled
  Patch 2B implementation boundary.

## 0.213.3

### 📝 Summary

- Completes immutable proposal identity for accepted enum variants before
  catalog-native proposal lowering begins.
- Keeps editable enum and variant names separate from their accepted
  store-local identities.

### 🩹 Fixed

- Added the missing enum-type-scoped variant namespace to the existing accepted
  source-binding catalog.
- A bound enum type must now bind every accepted variant exactly once.
  Variant bindings without their bound enum type, missing accepted variants,
  duplicate accepted identities, malformed source keys, and absent structural
  owners fail closed.
- Proposal application can therefore preserve a variant across an editable
  rename instead of reconstructing identity from its name or treating the
  rename as delete-plus-add.

### ⚠️ Breaking

- The accepted source-binding envelope and enclosing accepted revision bundle
  gain the enum-variant binding table. Development stores written by 0.213.0,
  0.213.1, or 0.213.2 must be recreated.
- The current format remains version 1 under the pre-1.0 hard cut. No
  compatibility decoder, translation path, name-based reconstruction, alias,
  or fallback reader is retained.

### 🧭 Persistence

- Accepted revision-bundle bytes and their semantic fingerprints change.
- Entity snapshots, enum definitions, rows, indexes, relations, journals,
  cursors, commit markers, and public response formats do not change.

### 📊 Audit

- The maintained `sql_perf` subject is 5,217,250 raw non-gzipped Wasm bytes,
  10,213 bytes (+0.1961%) above 0.213.2 and 59,768 bytes (+1.1589%) above the
  0.213 opening baseline. It remains below the frozen +2.00% review budget.
- Its raw SHA-256 is
  `920b4c47908d3120b4ec356e9263b8401435cc1ef23b82d8a30980da28407e32`;
  the canonical shrunk artifact is 4,859,888 bytes.
- The release-prep worktree touches eight tracked files at approximately
  `+296/-26` lines, including focused tests and release documentation. Runtime
  structure grows by one necessary namespace inside the existing binding
  catalog; it does not add a second catalog or semantic owner.

### 🧪 Testing

- Focused tests cover current-form round trips, complete enum-variant closure,
  incomplete closure rejection, rejection of the superseded development
  envelope, and the frozen accepted-bundle vector.
- All-target, all-feature warnings-denied `icydb-core` Clippy, native
  no-default compilation, formatting, diff hygiene, layer authority,
  schema/model boundary, and durability-documentation guards pass.
- Full repository validation remains part of the human-owned push workflow.

### 🧭 Follow-up

- Catalog-native proposal lowering, durable idempotent receipts, the public
  application caller, and reuse of 0.211 activation jobs remain the next
  coupled Patch 2B work.

## 0.213.2

### 📝 Summary

- Adds the durable atomic publication foundation required by database-scoped
  schema proposals without exposing a partial application API.
- Consolidates accepted-schema binary envelopes behind one bounded primitive
  codec while preserving their current bytes and format ownership.

### 🔧 Changed

- Plain accepted-schema candidates for multiple journaled stores now preflight
  completely and publish through one canonically ordered commit marker.
- All marker-owned journal batches append before any candidate becomes the
  live accepted root. Recovery completes every store after an interruption
  leaves only a prefix of those batches appended.
- Equal journal sequence numbers from different stores are now treated
  correctly as tail-local identities. Additional batches receive distinct
  marker-local IDs, while established single-store batch bytes remain
  unchanged.
- Enum catalogs, composite catalogs, accepted revision bundles, and source
  bindings now share one checked primitive wire reader/writer. Each envelope
  continues to own its own magic, current version, byte ceiling, canonical
  ordering, and semantic validation.

### 🩹 Fixed

- Fixed the durable commit-marker representation that previously made a
  legitimate commit spanning two journaled stores fail when their next
  tail-local sequences matched or their batches reused the marker ID.
- Mixed already-current and new candidate sets fail closed instead of
  permitting a partially replayed database-scoped publication.
- Multi-store publication involving volatile stores rejects before mutation
  because no durable recovery authority can complete it atomically.

### 🧭 Persistence

- No accepted-bundle, commit-marker, journal, row, index, relation, cursor, or
  public response encoding changes in this patch.
- The current version-1 codecs remain hard-cut. No compatibility decoder,
  alias, translation path, or legacy fallback was added.

### 📊 Audit

- The maintained `sql_perf` subject is 5,207,037 raw non-gzipped Wasm bytes,
  334 bytes (-0.0064%) below 0.213.1 and 46,189 bytes (+0.8950%) above the
  0.213 opening baseline. Its raw SHA-256 is
  `1c006a2047cf8cd54dcca438e5fac330493ce655bdbc8fa249effe1183c6e8ae`.
- The canonical shrunk artifact is 4,850,206 bytes, 263 bytes below 0.213.1.
- The complete release-prep worktree changes 15 files at approximately
  `+803/-448` lines,
  including focused recovery tests and release evidence. Production code is
  approximately 15 lines smaller overall: the shared codec removes more code
  than the atomic publication owner adds.

### 🧪 Testing

- Focused tests cover equal sequence numbers across journal tails, successful
  two-store publication, interruption after a journal prefix, recovery
  completion, and retained single-store activation behavior.
- All-feature warnings-denied Clippy, native no-default compilation,
  formatting, diff hygiene, schema/model layering, mutation atomicity, and
  durability guards pass.
- Full repository validation remains part of the human-owned push workflow.

### 🧭 Follow-up

- Catalog-native proposal lowering, durable idempotent receipts, the public
  application caller, and reuse of 0.211 activation jobs remain the next
  coupled Patch 2B work.

## 0.213.1

### 📝 Summary

- Adds the durable store-local identity owner needed for catalog-native
  proposal lowering without yet exposing partial schema-application behavior.
- Preserves immutable proposal source keys separately from editable names and
  from accepted structural semantics.

### ➕ Added

- Added one bounded accepted source-binding catalog covering entity, named
  type, field, accepted-check, secondary-index, and relation identities.
- Added a canonical current-form codec capped at 2 MiB. Decode validates
  source-key syntax, canonical order, one-to-one accepted identities, exact
  structural-owner closure, and the accepted constraint kind.
- Included source-binding bytes in accepted bundle validation and semantic
  fingerprints.

### 🔧 Changed

- Every maintained generated-reconciliation, SQL-DDL, and constraint-activation
  candidate path now carries the existing source-binding catalog forward
  unchanged.
- Source bindings carry identity only. Accepted snapshots and their structural
  catalogs remain the sole owners of field, type, constraint, index, and
  relation meaning.

### ⚠️ Breaking

- The accepted revision-bundle bytes now include the source-binding envelope.
  This is a pre-1.0 hard cut: development stores created by 0.213.0 must be
  recreated, and no old-bundle decoder or translation path is retained.

### 🧪 Testing

- Focused codec tests cover empty and populated round trips, missing-owner
  rejection, fingerprint sensitivity, and the frozen accepted-bundle vector.
- Structural guards require codec, fingerprint, and schema-transition
  preservation ownership.
- Full repository validation remains part of the human-owned push workflow.

### 📊 Audit

- The maintained `sql_perf` subject is 5,207,371 raw non-gzipped Wasm bytes,
  an increase of 46,523 bytes (+0.9015%) from 0.213.0 and 49,889 bytes
  (+0.9673%) from the 0.213 opening baseline. It remains below the frozen
  +2.00% review budget.
- Its raw SHA-256 is
  `cfad8d30cf46ebdbbda7535ea72a3e47a876c746ef942f401249178f612995aa`;
  the canonical shrunk artifact is 4,850,469 bytes, an increase of 43,894
  bytes (+0.9132%) from 0.213.0.
- The slice touches 12 files and adds approximately 949 lines while removing
  21. The implementation becomes locally more complex by adding one bounded
  persisted identity catalog and codec, but does not add another semantic
  schema owner or a partial application path.

### 🧭 Follow-up

- Catalog-native proposal lowering will allocate and reconcile accepted
  identities through this catalog.
- Durable idempotent receipts, atomic multi-store publication, and 0.211
  activation reuse remain subsequent Patch 2B work.

## 0.213.0

### 📝 Summary

- Opens the 0.213 architecture line by separating the pure public schema
  contract from generated application-model metadata and database runtime
  authority.
- Adds bounded exact proposal composition and a read-only target context for
  future optimistic catalog-native application.
- Does not yet add schema mutation through `SchemaProposal`, durable receipts,
  or multi-store schema publication.

### ➕ Added

- Reclaimed `icydb-schema` as the standalone public proposal and canonical
  scalar leaf. It has no dependency on the database runtime, generated model
  graph, storage, recovery, clocks, generators, or application callbacks.
- Added bounded immutable source identities for entities, fields, named types,
  constraints, indexes, and relations.
- Added a current version-1 proposal envelope with explicit capability
  requirements, database/store routing tokens, an immutable submission key,
  an exact expected accepted head, explicit removals, and deterministic
  canonical encoding and digesting.
- Added exact scalar field contracts, source-keyed enum literals, bounded
  accepted-check source expressions, structural record fields, database insert
  policy, generated-value intent, and managed timestamp declarations.
- Added deterministic database-scoped composition with duplicate-definition,
  name, routing, removal, type-graph, enum-literal, relation-closure, and
  capability validation.
- Added `DbSession::schema_application_target`, which issues an opaque database
  identity, canonically ordered store-routing identities, and one exact
  multi-store accepted head from recovered runtime authority.

### 🔧 Changed

- Moved the previous `icydb-schema` authoring graph intact to the unpublished
  temporary `icydb-model-legacy` package so the reclaimed package cannot mix
  the retired graph with the new proposal contract.
- Made `icydb-schema` the single Rust representation owner for canonical
  scalar atoms. `icydb-core` retains runtime operations and re-exports those
  same representations rather than defining parallel scalar types.
- Split exact proposal composition from durable catalog application.
  Composition validates untrusted source-keyed intent but allocates no accepted
  identities, publishes no schema, and owns no marker or recovery behavior.
- Database identities are bound to the durable database incarnation and the
  complete registered store topology. Store identities additionally cover
  their path, capabilities, and stable allocations.
- The optimistic accepted head covers every store-local accepted root in
  canonical path order, including explicit absence, so a topology or accepted
  root change invalidates stale proposal context.
- Kept ULID generation as an engine-owned operation through `GenerateKey`;
  maintained fixtures import that runtime capability explicitly while the
  standalone schema leaf remains free of randomness and clocks.

### ⚠️ Breaking

- Removed the separate `icydb-primitives` package without an alias or
  compatibility wrapper.
- Reclaimed the `icydb-schema` package name for the new proposal/scalar
  contract. The old authoring API is not preserved under that public path.
- Removed permissive and alias `Date` constructors. Strict construction uses
  `Date::try_new`, while textual input uses `Date::parse`.
- The proposal codec accepts only its current version-1 form. No compatibility
  decoder or translation path exists for superseded development shapes.

### 📊 Audit

- The 0.213 opening `sql_perf` subject was 5,157,482 raw non-gzipped Wasm
  bytes. The closing 0.213.0 subject is 5,160,848 bytes, an increase of 3,366
  bytes (+0.0653%) and well below the frozen +2.00% review budget.
- The canonical shrunk artifact is 4,806,575 bytes.
- The affected source seam grew from 176 Rust files and 33,212 lines to
  approximately 194 files and 37,000 lines. Most growth is the bounded public
  contract, exact composition validation, tests, and the temporary package
  bridge. It remains below the frozen +15% source review tripwire.
- The target-authority slice adds no retained bytes to the measured
  `sql_perf` Wasm subject compared with the preceding exact-composition slice.
- No accepted-schema, row, index, relation, cursor, journal, commit-marker, or
  recovery persisted format changes in 0.213.0.

### 🧪 Testing

- Focused proposal tests cover malformed versions and capabilities, collection
  and byte bounds, source-key identity, canonical ordering and digest vectors,
  exact scalar/default compatibility, enum literals, named-type cycles and
  depth, explicit removals, and cross-fragment relation closure.
- Focused runtime tests prove stable target issuance, canonical multi-store
  ordering, distinct store identities, topology-bound database identity, and
  exact accepted-root coverage.
- The schema leaf, core, facade, moved legacy package, build package, and
  derive seams pass their focused compilation and warnings-denied checks.
- No-default-feature checks, documentation tests, formatting, diff hygiene,
  package-boundary guards, memory-allocation guards, and publication guards
  pass.
- Full repository validation remains part of the human-owned push workflow.

### 🧭 Follow-up

- Catalog-native source-to-accepted bindings, proposal lowering, durable
  idempotent receipts, atomic multi-store publication, and reuse of the 0.211
  activation machinery remain subsequent 0.213 work.
- The temporary unpublished model package deliberately blocks workspace
  publication until the final model-package and consumer cutover removes that
  bridge.
