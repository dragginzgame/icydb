# 0.69 Changelog

## 0.69.1 - 2026-04-06 - Canic Memory API Cleanup

### Summary

`0.69.1` is a narrow integration follow-up to `0.69.0`. It does not widen the
new covering-read authority contract. Instead, it rebases IcyDB's remaining
stable-memory bootstrap and commit-slot plumbing onto the cleaned-up
`canic-memory 0.25.10` public surface.

The important outcome is boundary cleanup, not new query semantics:

- generated canister bootstrap now uses the renamed owner-range bootstrap API
- commit-marker memory validation now uses the new pending-bootstrap and
  inspection/listing helpers
- the last direct low-level memory-registry init in IcyDB is gone
- facade SQL tests and other local bootstrap helpers now use the same public
  memory API that generated canisters use

So after `0.69.1`, IcyDB no longer mixes the public `canic-memory` boundary
with direct registry-runtime calls for its current integration.

### Changed

- rebased generated canister memory bootstrap onto the new `0.25.10` public API
  in:
  - `crates/icydb-build/src/db.rs`
  - generated `core_db()` bootstrap now calls
    `MemoryApi::bootstrap_owner_range(...)`
  - this keeps the codegen-side memory bootstrap aligned with the current Canic
    naming and removes the older `bootstrap_registry(...)` dependency
- updated commit-slot memory configuration to the new `0.25.10` helper set in:
  - `crates/icydb-core/src/db/commit/memory.rs`
  - `configure_commit_memory_id(...)` now uses
    `MemoryApi::bootstrap_pending()`
  - commit-slot open/register now uses `MemoryApi::register(...)`
  - commit-slot validation now uses:
    - `MemoryApi::registered()`
    - `MemoryApi::inspect(...)`
  - this deletes the last direct `MemoryRegistryRuntime::init(None)` call from
    IcyDB's current memory integration path
- updated the test bootstrap path for commit storage in:
  - `crates/icydb-core/src/db/commit/mod.rs`
  - test commit-store initialization now uses
    `MemoryApi::bootstrap_owner_range(...)`
    instead of the earlier bootstrap name
- updated facade SQL test memory wiring in:
  - `crates/icydb/src/db/session/tests.rs`
  - facade test memory slots now use:
    - `MemoryApi::bootstrap_owner_range(...)`
    - `MemoryApi::register(...)`
  - this keeps local test memory setup on the same public Canic surface as the
    generated canister path instead of carrying its own older helper spelling
- refreshed one stale bootstrap comment in:
  - `canisters/quickstart/src/lib.rs`
  - host-parallel quickstart tests now describe the current
    `MemoryApi::bootstrap_owner_range()` behavior instead of the old runtime
    bootstrap name
- confirmed the remaining integration boundary is clean:
  - no `MemoryRegistryRuntime`
  - no `MemoryRuntimeApi`
  - no old `MemoryApi::{bootstrap_registry, register_memory, inspect_memory, registered_memories, find_registered_memory}` calls
  - current `crates/`, `canisters/`, and `testing/` code now sit on the
    `canic-memory 0.25.10` public API for the memory bootstrap/registration
    flows IcyDB actually uses

### Validation

- `cargo test -p icydb-core --lib --no-run`
- `cargo clippy -p icydb-core -p icydb -p canister_quickstart --tests -- -D warnings`
- `cargo test -p icydb --lib facade_explain_sql_execution_matrix_queries_include_expected_tokens -- --nocapture`
- `cargo test -p canister_quickstart --lib generated_sql_dispatch_active_user_filtered_composite_order_only_explain_reports_witness_validated_route -- --nocapture`

---

## 0.69.0 - 2026-04-06 - True Covering Read Execution

### Summary

`0.69.0` starts the `0.69` line by turning covering reads from a
planner-only admission signal into a real execution contract with explicit
row-presence semantics. `0.68` had already widened the scalar read families
that could stay on index-backed routes; `0.69.0` makes the first part of that
contract executable without row payload reads instead of continuing to optimize
the row-materialized fallback around it.

The line now ships two explicit secondary authority modes rather than treating
all index-backed covering reads as one blurred class:

- `planner_proven` for primary-store shapes where the chosen access path already
  proves row authority
- `witness_validated` for narrow secondary/index-backed cohorts where IcyDB can
  prove the data store and index store are synchronized and therefore skip the
  extra row-existence probe without weakening stale-row safety

Current primary-store PK-only reads still fall into two planner-owned cohorts:

- authoritative primary-store traversal (`FullScan` and strict half-open
  `KeyRange`) reports `existing_row_mode = planner_proven` and may skip
  row-existence checks
- exact primary-key lookup (`ByKey` and canonical ascending `ByKeys`) reports
  `existing_row_mode = row_check_required` and keeps explicit row-presence
  checks even though the route is now first-class `CoveringRead`

Secondary covering routes from `0.68` no longer all behave the same. Narrow
single-field and filtered-composite cohorts now have an explicit
`witness_validated` route, while broader secondary and expression-backed
covering families still stay `row_check_required` until a later slice can
prove stronger authority semantics for them.

### Changed

- promoted planner-owned covering admission into an execution-grade contract in:
  - `crates/icydb-core/src/db/query/plan/covering/mod.rs`
  - introduced `CoveringReadExecutionPlan`
  - introduced `CoveringExistingRowMode`
  - introduced `covering_read_execution_plan(...)`
  - moved route payloads away from projection-only covering metadata toward one
    execution-grade contract that runtime can consume directly
- widened the route boundary so covering execution is visible in the execution
  plan instead of remaining an implicit terminal shortcut in:
  - `crates/icydb-core/src/db/executor/route/terminal.rs`
  - `crates/icydb-core/src/db/executor/pipeline/operators/terminal/runtime.rs`
  - `crates/icydb-core/src/db/executor/explain/descriptor/load.rs`
  - current covering routes now surface `existing_row_mode` in
    `EXPLAIN EXECUTION` and `EXPLAIN JSON`
- introduced explicit secondary row-authority state instead of silently
  upgrading existing covering routes in:
  - `crates/icydb-core/src/db/data/store.rs`
  - `crates/icydb-core/src/db/index/store.rs`
  - `crates/icydb-core/src/db/registry.rs`
  - `crates/icydb-core/src/db/executor/mutation/commit_window.rs`
  - `crates/icydb-core/src/db/commit/recovery.rs`
  - data/index stores now track whether secondary covering reads are
    authoritative together
  - commit and recovery flows now synchronize and invalidate that witness state
    explicitly instead of relying on executor-local assumptions
- shipped the first honest planner-proven cohort in:
  - `crates/icydb-core/src/db/query/plan/covering/mod.rs`
  - `crates/icydb-core/src/db/query/intent/model.rs`
  - PK-only projections over authoritative primary-store `FullScan`
  - PK-only projections over strict half-open primary-key `KeyRange`
  - redundant half-open PK predicates are stripped once the exact `KeyRange`
    access path is chosen
- widened the same route to exact primary-key lookup while keeping fail-closed
  row semantics explicit in:
  - `crates/icydb-core/src/db/query/plan/covering/mod.rs`
  - exact `ByKey` PK-only projection now compiles to `CoveringRead` with
    `existing_row_mode = row_check_required`
  - canonical ascending `ByKeys` PK-only projection does the same
  - descending `ByKeys` still fails closed for now rather than inventing an
    executor-local reorder contract
- kept secondary/index-backed covering routes honest in:
  - `crates/icydb-core/src/db/query/plan/covering/mod.rs`
  - the new execution-grade contract carries them forward under one of:
    - `witness_validated`
    - `row_check_required`
  - broader secondary families remain conservative until planner-owned
    row-authority proof exists
- added the first explicit witness-backed secondary covering cohorts in:
  - `crates/icydb-core/src/db/executor/route/terminal.rs`
  - `crates/icydb-core/src/db/executor/covering.rs`
  - `crates/icydb-core/src/db/executor/pipeline/operators/terminal/runtime.rs`
  - `crates/icydb-core/src/db/index/scan.rs`
  - admitted cohorts now include:
    - `SELECT id, name FROM User ORDER BY name ASC, id ASC LIMIT 2`
    - `SELECT id, name FROM User ORDER BY name DESC, id DESC LIMIT 2`
    - `SELECT id, name FROM User WHERE name = 'alice' ORDER BY id LIMIT 1`
    - `SELECT id, name FROM User WHERE name = 'alice' ORDER BY id DESC LIMIT 1`
    - `SELECT id, name FROM User WHERE name >= 'a' AND name < 'c' ORDER BY name ASC, id ASC LIMIT 2`
    - `SELECT id, name FROM User WHERE name >= 'a' AND name < 'c' ORDER BY name DESC, id DESC LIMIT 2`
    - `SELECT id, tier, handle FROM ActiveUser WHERE active = true AND tier = 'gold' ORDER BY handle ASC, id ASC LIMIT 2`
  - all of these now report `existing_row_mode = witness_validated` on the
    real execution route instead of recovering the optimization inside the SQL
    terminal
- kept the widened secondary route DRY by consolidating witness admission into
  one shared classifier in:
  - `crates/icydb-core/src/db/executor/route/terminal.rs`
  - current witness families are now one centralized
    `SecondaryWitnessValidatedCoveringCohort` contract rather than a growing
    pile of shape-specific booleans
- widened core coverage for the new route contract in:
  - `crates/icydb-core/src/db/query/plan/covering/tests.rs`
  - `crates/icydb-core/src/db/executor/route/tests.rs`
  - `crates/icydb-core/src/db/session/tests/mod.rs`
  - `crates/icydb-core/src/db/executor/tests/semantics.rs`
  - checked-in coverage now locks:
    - route payload retention
    - explain-surface `existing_row_mode`
    - fail-closed `ByKeys DESC`
    - execution snapshot parity for the new PK-only covering nodes
- added outer-surface quickstart and PocketIC coverage for the first shipped
  cohort in:
  - `canisters/quickstart/src/lib.rs`
  - `testing/pocket-ic/tests/sql_canister.rs`
  - typed and generated SQL now both lock the new PK-only `CoveringRead` route
    and its explain/perf story for simple `SELECT id FROM User ORDER BY id ASC LIMIT 1`
    style queries
- widened the same outer-surface coverage to the first secondary witness
  cohorts in:
  - `canisters/quickstart/src/lib.rs`
  - `testing/pocket-ic/tests/sql_canister.rs`
  - typed and generated SQL now both lock:
    - `ORDER BY name` witness-backed secondary reads
    - equality-prefix witness-backed secondary reads
    - strict bounded-range witness-backed secondary reads
    - the first filtered composite witness-backed secondary read on
      `ActiveUser`
- recorded the first measurable runtime win from true covering execution on the
  PK-only cohort in:
  - `canisters/quickstart/src/perf.rs`
  - `testing/pocket-ic/tests/sql_canister.rs`
  - generated dispatch `SELECT id FROM User ORDER BY id ASC LIMIT 1` now
    averages about `337,485` local instructions
  - typed dispatch on the same query averages about `334,704`
  - against the older row-backed `SELECT age FROM User ORDER BY id ASC LIMIT 1`
    cohort, that is roughly a `17%` total drop and about a `21%`
    execute-phase drop
- recorded the first step-function secondary SQL wins from explicit witness
  authority in:
  - `testing/pocket-ic/tests/sql_canister.rs`
  - measured PocketIC deltas now include:
    - `SELECT id, name FROM User ORDER BY name ASC, id ASC LIMIT 2`
      - generated: `675,563 -> 590,418` (`-12.60%`)
      - typed: `672,342 -> 587,707` (`-12.59%`)
    - `SELECT id, name FROM User WHERE name = 'alice' ORDER BY id LIMIT 1`
      - generated: `567,143 -> 532,624` (`-6.09%`)
      - typed: `564,379 -> 529,860` (`-6.12%`)
    - `SELECT id, name FROM User WHERE name >= 'a' AND name < 'c' ORDER BY name ASC, id ASC LIMIT 2`
      - generated: `845,016 -> 796,321` (`-5.76%`)
      - typed: `842,704 -> 793,512` (`-5.84%`)
    - `SELECT id, tier, handle FROM ActiveUser WHERE active = true AND tier = 'gold' ORDER BY handle ASC, id ASC LIMIT 2`
      - generated: `1,017,766 -> 945,678` (`-7.08%`)
      - typed: `1,015,209 -> 943,195` (`-7.09%`)

### Validation

- `cargo clippy -p icydb-core --lib --tests -- -D warnings`
- `cargo test -p icydb-core --lib -- --nocapture`
- `cargo clippy -p canister_quickstart --tests -- -D warnings`
- `cargo test -p canister_quickstart generated_sql_dispatch_user_primary_key_covering -- --nocapture`
- `cargo clippy -p icydb-testing-integration --test sql_canister -- -D warnings`
- targeted unrestricted PocketIC witness-route probes for:
  - `ORDER BY name`
  - equality-prefix `WHERE name = ... ORDER BY id`
  - strict bounded range `WHERE name >= ... AND < ... ORDER BY name`
  - filtered composite `ActiveUser` `ORDER BY handle`
- unrestricted PocketIC:
  - `POCKET_IC_BIN=/tmp/pocket-ic-server-13.0.0/pocket-ic cargo test -p icydb-testing-integration --test sql_canister -- --nocapture`

---
