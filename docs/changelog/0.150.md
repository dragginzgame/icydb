# 0.150.x

## 0.150.12

`0.150.12` carries accepted schema authority with runtime visible-index views.
Query-owned visible-index execution explain now reuses the accepted
`SchemaInfo` that authorized index visibility when freezing access-choice
diagnostics instead of dropping back to the generated model-only finalizer.

### Accepted Explain Visibility Authority

- Added accepted `SchemaInfo` retention to `VisibleIndexes::accepted_schema_visible`
  so accepted visibility, accepted field-path planner contracts, and accepted
  schema metadata travel as one runtime view.
- Routed query-owned visible-index execution explain access-choice finalization
  through `finalize_access_choice_for_model_with_accepted_indexes_and_schema`
  when accepted schema info is present.
- Kept the generated model-only finalizer available only for explicit
  generated/model-only visible-index views.
- Extended write-boundary guards to prove visible-index explain carries and
  consumes accepted schema info before access-choice diagnostics are frozen.

### Validation

- `cargo test -p icydb-core --test write_boundary_guards runtime_visible_indexes_are_accepted_schema_filtered -- --nocapture`
- `cargo test -p icydb-core explain_cache_convergence --features sql -- --nocapture`
- `cargo test -p icydb-core --test write_boundary_guards -- --nocapture`
- `cargo check -p icydb-core --tests --features sql`
- `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`

## 0.150.11

`0.150.11` narrows order-only fallback authority. Generated/model-only order
fallback and the explicit expression-index order lane still receive generated
candidate indexes for discovery, but they now match order compatibility and
build selected range access from reduced semantic index contracts instead of
calling generated `IndexModel` order helpers.

### Reduced Order Fallback Matching

- Routed generated/model-only order fallback matching through
  `SemanticIndexAccessContract` key-item order terms.
- Routed the accepted expression-index order fallback lane through the same
  reduced contract order terms before building selected range access from the
  reduced contract.
- Moved generated `IndexModel` order compatibility helpers behind test-only
  exports because production order fallback no longer needs them.
- Moved the generated-index `SemanticIndexRangeSpec::new` constructor to
  test-only use after order fallback switched to the reduced contract
  constructor.
- Extended write-boundary guards to prove order fallback matching uses reduced
  contract order terms and does not call generated index order matching
  helpers.

### Validation

- `cargo test -p icydb-core planner_order_only --features sql -- --nocapture`
- `cargo test -p icydb-core planner --features sql -- --nocapture`
- `cargo test -p icydb-core expression_index --features sql -- --nocapture`
- `cargo test -p icydb-core --test write_boundary_guards -- --nocapture`
- `cargo check -p icydb-core --tests --features sql`
- `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`

## 0.150.10

`0.150.10` continues reducing generated index score authority after the
`0.150.9` planner scoring cleanup. Access-choice explain/rerank score
rebuilding now uses the reduced semantic index contract scoring helper for
filtered and secondary-order compatibility facts instead of calling the
generated secondary-order matcher directly.

### Reduced Access-Choice Score Rebuilds

- Routed access-choice candidate score rebuilding through the planner helper
  that scores from `SemanticIndexAccessContract`.
- Removed the generated `IndexModel` payload from the local access-choice
  scoring context; candidate discovery still receives the temporary generated
  candidate index bridge where expression matching needs it.
- Removed the now-dead generated `IndexModel` secondary-order candidate
  matcher after all planner/access-choice score paths moved to reduced
  contract order terms.
- Routed planner prefix, multi-lookup, ordered-compare, and compound range
  key-shape traversal through `SemanticIndexAccessContract` key metadata
  instead of generated `IndexModel` key/field helpers.
- Removed the now-unused generated-model leading-key and field-indexability
  planner helpers after those key-shape checks moved to reduced contracts.
- Extended write-boundary guards to prove access-choice evaluator scoring no
  longer calls the generated secondary-order matcher directly.
  The guard also blocks planner prefix/compare/range construction from
  returning to generated key/field helper APIs for key-shape extraction.

### Validation

- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo test -p icydb-core --test write_boundary_guards access_choice_candidate_scores_use_reduced_index_contract_facts -- --nocapture`.
- Ran `cargo test -p icydb-core --test write_boundary_guards -- --nocapture`.
- Ran `cargo test -p icydb-core access_choice --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core planner --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core expression_index --features sql -- --nocapture`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `git diff --check`.

## 0.150.9

`0.150.9` narrows access-choice candidate scoring further. Candidate
selection still receives temporary generated candidate indexes for discovery
and expression-index order matching, but filtered-index and key-arity score
facts now come from the reduced `SemanticIndexAccessContract` instead of
reopening generated `IndexModel` predicate metadata.

### Reduced Candidate Score Facts

- Added a reduced filtered-index fact to `SemanticIndexAccessContract`.
- Routed access-choice order-compatible score rebuilding through the reduced
  semantic contract for the filtered-index bit.
- Routed prefix and multi-lookup exact/filtered score construction through
  reduced semantic contract key-arity and filtered facts.
- Routed range single-clause and compound score construction through reduced
  semantic contract filtered/key-arity facts where candidate scoring does not
  need generated expression matching.
- Routed prefix and range evaluator key-item traversal, leading-key lookup,
  and field-key range operator checks through reduced semantic contract key
  metadata instead of generated `IndexModel` key/field helpers.
- Removed the now-unused generated-model `index_key_item_at` and
  `index_key_item_count` planner helpers after range candidate traversal and
  score arity checks moved to reduced contract key slots.
- Added a planner ranking helper that builds candidate scores from reduced
  semantic index contracts, then routed prefix, compare, and range planner
  candidate scoring through it for filtered and order-compatibility facts.
- Routed accepted order fallback's expression-lane shape gate through the
  reduced semantic contract before entering the explicit generated expression
  order matcher.
- Extended write-boundary guards to prove access-choice candidate score paths
  no longer call generated `IndexModel::predicate()` for filtered-score facts
  or generated key/field helper APIs for evaluator key shape facts.
  The guard also proves planner prefix/compare/range scoring no longer calls
  generated `IndexModel::predicate()` or the generated secondary-order matcher
  directly.

### Validation

- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo test -p icydb-core --test write_boundary_guards access_choice_candidate_scores_use_reduced_index_contract_facts -- --nocapture`.
- Ran `cargo test -p icydb-core --test write_boundary_guards -- --nocapture`.
- Ran `cargo test -p icydb-core access_choice --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core planner --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core expression_index --features sql -- --nocapture`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `git diff --check`.

## 0.150.8

`0.150.8` narrows accepted planner field-path index contracts again. Runtime
visible-index construction still receives generated candidate indexes while
predicate planner construction is on the temporary bridge, but accepted
field-path planner contracts no longer retain that full generated model. They
store accepted identity/order facts plus a reduced `SemanticIndexAccessContract`
and order-only field-path fallback builds selected index-range access from that
reduced contract. The generated order fallback remains explicit for expression
indexes until accepted expression index contracts are designed.

### Reduced Accepted Planner Contracts

- Changed `AcceptedPlannerFieldPathIndex` to retain a reduced
  `SemanticIndexAccessContract` instead of a full generated `IndexModel`
  bridge.
- Added a contract-based `SemanticIndexRangeSpec` constructor so selected
  index-range access can be assembled from reduced contracts after candidate
  selection.
- Routed accepted field-path order-only fallback through the reduced semantic
  access contract when building the selected range scan.
- Added reduced-contract prefix and multi-lookup access constructors and routed
  selected predicate prefix, multi-lookup, and range planner construction
  through reduced semantic contracts after candidate selection.
- Removed the old generated-index `AccessPlan` prefix and multi-lookup
  constructors so selected runtime access construction uses the reduced
  contract constructors.
- Routed runtime access-choice residual reranking and verbose explain
  projection through accepted field-path planner contracts when accepted
  visible-index authority is present, keeping the generated-only candidate
  rebuild path for model-only explain.
- Kept generated-model order matching isolated to the explicit expression-index
  lane.
- Extended write-boundary guards to prove accepted planner field-path contracts
  no longer retain `generated_index_bridge` and selected predicate planner
  construction plus runtime access-choice candidate rebuilds use accepted
  contracts where available.

### Validation

- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo test -p icydb-core --test write_boundary_guards runtime_visible_indexes_are_accepted_schema_filtered -- --nocapture`.
- Ran `cargo test -p icydb-core --test write_boundary_guards runtime_access_choice_projection_uses_accepted_visible_indexes -- --nocapture`.
- Ran `cargo test -p icydb-core --test write_boundary_guards runtime_access_capabilities_use_reduced_index_shape_facts -- --nocapture`.
- Ran `cargo test -p icydb-core planner_order_only --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core access_choice --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core expression_index --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core filtered_expression --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core explain_execution --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core sql_explain --features sql -- --nocapture`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `git diff --check`.

## 0.150.7

`0.150.7` continues the access-path DTO handoff by removing full generated
`IndexModel` retention from selected access-path DTO storage. Planner
construction still accepts the temporary generated candidate models, but
selected paths now store reduced semantic index contracts carrying index name,
ordinal, store path, key items, predicate semantics, and uniqueness. Runtime
access-path consumers no longer expose `selected_index_model` or raw
index-prefix/multi-lookup generated model accessors.

### Reduced Selected Access Paths

- Added a `SemanticIndexAccessContract` projection for selected access paths
  and routed executable payload construction, raw-bound lowering, residual
  access-bound stripping, covering metadata, cache-key projection,
  access-choice diagnostics, and diagnostics-facing access projection through
  reduced selected-index facts.
- Removed runtime `selected_index_model`, raw `as_index_prefix`, and raw
  `as_index_multi_lookup` accessors from access-path surfaces so selected-path
  consumers must use reduced semantic index contracts.
- Changed `AccessPath::IndexPrefix`, `AccessPath::IndexMultiLookup`, and
  `SemanticIndexRangeSpec` to store `SemanticIndexAccessContract` instead of
  full generated `IndexModel` values.
- Added a contract-based structural cursor-anchor key builder so expression
  index anchors use reduced selected-index identity/key metadata instead of
  requiring the access path to retain a generated model.
- Kept planner candidate discovery and access-path constructors accepting the
  existing temporary generated bridge until accepted access-choice contracts
  replace those construction lanes.
- Extended write-boundary guards to prove selected access paths and semantic
  range specs store reduced semantic contracts instead of full generated index
  models.

### Validation

- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo test -p icydb-core --test write_boundary_guards -- --nocapture`.
- Ran `cargo test -p icydb-core access --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core access_choice --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core fingerprint --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core cursor_anchor --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core indexed_covering --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core expression_index --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core filtered_expression --features sql -- --nocapture`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `git diff --check`.

## 0.150.6

`0.150.6` continues the access-path DTO handoff by removing full generated
`IndexModel` retention from lowered executor index-prefix and index-range
specs after raw bounds have been materialized. Lowering still uses the current
temporary generated DTO to encode bounds, but executor scan leaves now carry
only reduced index name, store path, and uniqueness facts alongside raw bounds.
Executable access payloads also now carry reduced index shape facts instead of
full generated index models.

### Reduced Lowered Index Specs

- Extended `LoweredIndexScanContract` with the index store path so covering
  projection scans can resolve stores without reopening a generated
  `IndexModel`.
- Removed the generated `IndexModel` field from `LoweredIndexPrefixSpec` and
  `LoweredIndexRangeSpec`.
- Routed covering projection component scans through lowered scan-contract
  store paths.
- Extended `IndexShapeDetails` with the accepted/generated bridge ordinal and
  routed executable access payloads through `IndexShapeDetails` instead of
  storing generated `IndexModel` values.
- Added an arity-based index-bound builder and routed cursor-anchor envelope
  validation through reduced ordinal/key-arity facts from executable access
  payloads.
- Added an access-plan index-shape predicate backed by reduced access
  capabilities and routed access-planned query shell construction through it
  instead of testing for a selected generated index model.
- Kept raw-bound construction on the existing temporary generated bridge until
  semantic access paths stop storing generated index DTOs.
- Extended write-boundary guards to prove lowered executor specs retain
  reduced scan/store facts and executable access payloads retain reduced index
  shape facts instead of full generated index models.

### Validation

- Ran `cargo test -p icydb-core --test write_boundary_guards -- --nocapture`.
- Ran `cargo test -p icydb-core cursor_anchor --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core indexed_covering --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `git diff --check`.

## 0.150.5

`0.150.5` was a version-only publish step. The release commit advanced Cargo
workspace/package versions through the publish flow and did not carry a
separate accepted-index cleanup slice, so detailed cleanup notes resume in
`0.150.6`.

## 0.150.4

`0.150.4` narrows runtime access capability index-shape authority. Access
paths still carry temporary generated `IndexModel` DTOs for planner/lowering
assembly, but executor-facing `IndexShapeDetails` no longer exposes a full
generated model to route, aggregate, grouped-order, logical slot-target, or
executable-summary code. Those consumers now use reduced name, uniqueness,
key-item, and arity facts instead.

### Reduced Access Capability Index Facts

- Changed `IndexShapeDetails` to carry reduced index name, uniqueness,
  key-item, and arity facts instead of returning a generated `IndexModel`.
- Added key-item order compatibility helpers so runtime route pushdown can
  match deterministic secondary `ORDER BY` contracts from reduced key-item
  facts.
- Routed route pushdown, aggregate extrema tie-free checks, grouped-order
  proof, executable access summaries, and logical index slot/compile-target
  preparation through reduced executable access facts.
- Kept raw access-path DTO construction and generated expression-index key
  items on the explicit temporary bridge until accepted expression contracts
  are designed.
- Extended write-boundary guards to prove runtime access capabilities expose
  reduced index shape facts instead of full generated `IndexModel` values.

### Validation

- Ran `cargo test -p icydb-core --test write_boundary_guards -- --nocapture`.
- Ran `cargo test -p icydb-core expression_index --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core filtered_expression --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core sql_projection --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `git diff --check`.

## 0.150.3

`0.150.3` moves runtime field-path cursor-anchor and forward/unique write
index-key construction onto accepted index contracts. Index-range pagination
and write planning still receive temporary generated `IndexModel` DTOs, but
field-path cursor anchors and write keys now look up accepted `SchemaInfo`
index contracts, use accepted index ordinals for `IndexId`, read key
components from accepted row-layout slots, and carry accepted store/uniqueness
facts through preflight read/materialization boundaries. Generated expression
index construction remains on the explicit deferred lane until accepted
expression contracts land.

### Accepted Cursor Anchor Index Keys

- Added an accepted field-path index-key constructor that consumes
  `SchemaIndexInfo` directly.
- Routed runtime `EntityAuthority::index_range_anchor_key_from_slot_ref_reader`
  through the accepted field-path constructor for field-path indexes.
- Narrowed cursor-anchor key construction so executor authority receives the
  index-range contract instead of a standalone generated `IndexModel` argument.
- Kept expression-index cursor-anchor construction on the generated deferred
  lane because accepted expression index contracts are not designed yet.
- Made missing accepted field-path cursor-anchor contracts fail closed instead
  of falling back to generated index field-slot authority.
- Extended write-boundary guards to prove cursor-anchor key construction uses
  accepted index contracts for field-path indexes.

### Accepted Forward Index Write Keys

- Added an accepted field-path index-key constructor for canonical slot
  readers, sharing the accepted component encoder with cursor-anchor key
  construction.
- Routed structural forward-index mutation planning through accepted
  `SchemaInfo` index contracts for field-path indexes.
- Changed commit preflight's forward-index planning gate so accepted
  `SchemaInfo` field-path index contracts decide the field-path side while
  generated expression indexes remain the explicit deferred lane.
- Routed unique-index stored-row key rebuilds through accepted field-path index
  contracts.
- Carried accepted field-path index store paths, uniqueness, and diagnostic
  field lists into forward-index delta planning and preflight index readers.
- Narrowed preflight index read ports so field-path write planning no longer
  passes generated `IndexModel` definitions merely to decode index-entry
  membership.
- Preserved generated expression-index write key construction on the explicit
  deferred lane.
- Extended write-boundary guards to prove forward/unique field-path key
  construction and preflight index readers consume accepted contract facts.

### Accepted Runtime EXPLAIN Visibility

- Split structural execution EXPLAIN finalization into an accepted
  visible-index lane and an explicit model-only lane.
- Removed the implicit `None => self.model().indexes()` fallback from runtime
  EXPLAIN access-choice finalization.
- Extended write-boundary guards to prove runtime EXPLAIN finalization uses
  caller-resolved `VisibleIndexes` and keeps generated model indexes only on
  the named model-only explain path.

### Reduced Executor Scan Facts

- Added `LoweredIndexScanContract` so lowered index-prefix/range specs carry
  reduced scan facts after raw bounds have been materialized.
- Routed executor physical index scans and covering component scans through
  reduced scan facts for index-name diagnostics and uniqueness validation
  instead of passing generated `IndexModel` definitions into those scan leaves.
- Kept access-plan selection and raw-bound encoding on the existing temporary
  generated bridge until accepted field-path planner candidates replace it.

### Validation

- Ran `cargo test -p icydb-core --test write_boundary_guards forward_index_write_keys_use_accepted_row_contract_slots -- --nocapture`.
- Ran `cargo test -p icydb-core --test write_boundary_guards runtime_visible_indexes_are_accepted_schema_filtered -- --nocapture`.
- Ran `cargo test -p icydb-core load_index_range_cursor_anchor_matches_last_emitted_row_after_post_access_pipeline --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core cursor_anchor --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core explain_execution --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core indexed_covering --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core save_update_with_changed_index_key_emits_remove_and_insert --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core unique_index_violation_rejected_on_update --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo check --locked -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `git diff --check`.

## 0.150.2

`0.150.2` starts the planner access-choice authority handoff by adding a
planner-facing accepted field-path index contract to runtime `VisibleIndexes`
and using that accepted contract for runtime order-only field-path access
fallback. The downstream access planner still assembles access paths with the
temporary generated `IndexModel` bridge, but runtime field-path ORDER BY
compatibility now matches against accepted index terms instead of generated
`IndexModel` field order. Generated order fallback is kept on an explicit
model-only lane, and generated expression indexes remain on their explicit
deferred runtime lane until accepted expression contracts are designed.

### Accepted Planner Index Contracts

- Added `AcceptedPlannerFieldPathIndex` and
  `AcceptedPlannerFieldPathIndexField` as the planner-facing accepted
  field-path index DTOs carried by `VisibleIndexes`.
- Built those planner contracts from accepted `SchemaInfo` index metadata when
  runtime visible indexes are selected.
- Kept the generated `IndexModel` bridge inside the accepted planner contract
  only as a temporary access-path DTO bridge until access paths stop storing
  generated index metadata.
- Added runtime debug invariants that prove accepted planner index contracts
  stay consistent with the temporary generated bridge.
- Routed runtime order-only field-path access fallback through accepted
  planner index order terms.
- Split generated order-only fallback into an explicit generated/model-only
  lane so missing accepted field-path index contracts fail closed in runtime
  planning instead of silently reusing generated `IndexModel` field order.
- Preserved generated expression-index order fallback as an explicit deferred
  runtime lane until accepted expression index contracts exist.
- Extended the visible-index boundary guard and ready/non-ready secondary-index
  session test so accepted planner contracts are present and internally
  consistent before downstream planner scoring consumes them.

### Validation

- Ran `cargo test -p icydb-core --test write_boundary_guards runtime_visible_indexes_are_accepted_schema_filtered -- --nocapture`.
- Ran `cargo test -p icydb-core planner_order_only --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core expression_index --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core execute_sql_projection_expression_order --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core filtered_expression --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core filtered_composite_expression --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core session_non_ready_secondary_indexes_are_hidden_from_planning_and_execution --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo check --locked -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `git diff --check`.

## 0.150.1

`0.150.1` moves the runtime visible-index boundary onto accepted schema
metadata. Store-ready session planning now builds `VisibleIndexes` by filtering
the temporary generated `IndexModel` planner bridge through accepted
`SchemaInfo` field-path index contracts. Generated expression indexes remain
visible through their explicit deferred lane until accepted expression contracts
are designed. This keeps the downstream planner compatible for now while
preventing runtime field-path visible-index selection from using raw generated
model index declarations as the authority.

### Accepted Visible-Index Filtering

- Changed `VisibleIndexes` so it can carry an owned accepted-schema-filtered
  planner bridge plus an accepted field-path index authority marker.
- Preserved generated expression-index visibility explicitly while field-path
  index visibility is filtered through accepted contracts.
- Routed shared query-plan cache misses and typed query visible-index
  callbacks through accepted `SchemaInfo` before choosing planner-visible
  secondary indexes.
- Routed SQL execution explain visible-index resolution through the accepted
  `SchemaInfo` path.
- Extended the non-ready secondary-index session test so the ready state first
  proves visible indexes are backed by accepted field-path contracts.
- Added a write-boundary guard proving runtime visible-index construction uses
  accepted `SchemaInfo` filtering and no longer exposes the old
  `visible_indexes_for_model` / `visible_indexes_for_store_model` helpers.

### Validation

- Ran `cargo test -p icydb-core --test write_boundary_guards runtime_visible_indexes_are_accepted_schema_filtered -- --nocapture`.
- Ran `cargo test -p icydb-core session_non_ready_secondary_indexes_are_hidden_from_planning_and_execution --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo check --locked -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `git diff --check`.

## 0.150.0

`0.150.0` starts accepted index authority with the field-path half of the
index contract. Accepted schema snapshots now have a persisted index-contract
surface, generated field-path indexes project into accepted field IDs and row
slots during schema proposal construction, and the persisted schema codec now
hard-cuts to a shape that carries those index contracts. Accepted `SchemaInfo`
also reads top-level index membership and field-path index metadata from the
accepted snapshot instead of generated `EntityModel` index metadata. Expression
indexes are intentionally not accepted yet; they still need a stable canonical
expression contract before runtime can use them as accepted authority.

### Accepted Field-Path Index Contracts

- Added `PersistedIndexSnapshot`, `PersistedIndexKeySnapshot`, and
  `PersistedIndexFieldPathSnapshot` to accepted schema snapshots.
- Added `PersistedSchemaSnapshot::new_with_indexes(...)` and retained the
  field-only constructor for owner-local field-focused fixtures.
- Projected generated field-path indexes from schema proposals into accepted
  top-level `FieldId`, accepted row slot, full field path, persisted field
  kind, nullability, uniqueness, ordinal, store path, and predicate SQL display
  metadata.
- Skipped generated expression index proposals in this first field-path slice
  so expression contracts cannot become accepted runtime authority before their
  stable representation is designed.
- Hard-cut the persisted schema snapshot codec to version `3` and round-tripped
  accepted field-path index contracts through Candid.
- Added accepted snapshot integrity validation for duplicate index identities,
  empty index keys, and index field slots that diverge from the accepted row
  layout.
- Routed accepted `SchemaInfo` field index-membership flags through persisted
  index contracts instead of generated `EntityModel::indexes`.
- Added an internal `SchemaInfo` field-path index metadata surface backed by
  accepted index contracts, including ordinal, name, store, uniqueness, field
  ID, accepted row slot, field path, persisted kind, nullability, and predicate
  SQL display metadata.
- Kept generated `SchemaInfo` views on generated index metadata for
  proposal/model-only callers.
- Extended write-boundary guards so accepted `SchemaInfo` construction cannot
  regain generated index membership lookup or lose its persisted index metadata
  surface.

### Validation

- Ran `cargo test -p icydb-core schema::proposal --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core persisted_schema_snapshot_round_trips_field_path_indexes --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core accepted_schema_snapshot_try_new_rejects_invalid_index_contract --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core accepted_snapshot_schema_info_uses_persisted_index_membership --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core accepted_snapshot_schema_info_exposes_persisted_field_path_indexes --features sql -- --nocapture`.
- Ran `cargo test -p icydb-core --test write_boundary_guards accepted_schema_info_index_membership_uses_persisted_index_contracts -- --nocapture`.
- Ran `cargo test -p icydb-core schema --features sql -- --nocapture`.
- Ran `cargo check -p icydb-core --tests --features sql`.
- Ran `cargo check --locked -p icydb-core --tests --features sql`.
- Ran `cargo clippy -p icydb-core --all-targets --features sql -- -D warnings`.
- Ran `cargo fmt --all`.
- Ran `git diff --check`.
